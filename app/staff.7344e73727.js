const SUPABASE_URL="https://kqfoniinytvuddxiwvkc.supabase.co";const SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImtxZm9uaWlueXR2dWRkeGl3dmtjIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODMwMDczNTAsImV4cCI6MjA5ODU4MzM1MH0.GSuxsxKYp3XQ_ZsWesTXofudobYxBVWcz77Thbp8uII";const CLIENT_PORTAL_URL="https://clientportal.proptechnologyai.com";const APP_VERSION="v1.3";const APP_BUILD_DATE="Sep 2026";const SUPPORT_CONTACT="";const AI_ASSISTANT_BOT_ID="51a34cba-391e-490e-b18a-44464747854d";const GHL_CLIENT_ID="6a7c6322bd1f6848b4917540-msq22lxx";const GHL_REDIRECT_URI="https://kqfoniinytvuddxiwvkc.supabase.co/functions/v1/oauth-callback";const GHL_SCOPES=["contacts.readonly","contacts.write","opportunities.readonly","opportunities.write","conversations.readonly","conversations/message.readonly","conversations/message.write","locations.readonly","calendars.readonly","calendars/events.readonly","calendars/events.write"];function ghlConnectUrl(state){const params=new URLSearchParams({response_type:"code",client_id:GHL_CLIENT_ID,redirect_uri:GHL_REDIRECT_URI,scope:GHL_SCOPES.join(" "),state});return`https://marketplace.gohighlevel.com/oauth/chooselocation?${params.toString()}`}const GMAIL_CLIENT_ID="618099835839-2ul0surcf6lv8u1folrha35nvpt8a1pd.apps.googleusercontent.com";const GMAIL_REDIRECT_URI="https://kqfoniinytvuddxiwvkc.supabase.co/functions/v1/gmail-oauth-callback";const GMAIL_SCOPES=["https://www.googleapis.com/auth/gmail.send","https://www.googleapis.com/auth/gmail.modify","openid","https://www.googleapis.com/auth/userinfo.email"];const GMAIL_INVOICE_SCOPES=["https://www.googleapis.com/auth/gmail.readonly","openid","https://www.googleapis.com/auth/userinfo.email"];function gmailConnectUrl(state,scopes){const params=new URLSearchParams({response_type:"code",client_id:GMAIL_CLIENT_ID,redirect_uri:GMAIL_REDIRECT_URI,scope:(scopes||GMAIL_SCOPES).join(" "),state,access_type:"offline",prompt:"consent"});return`https://accounts.google.com/o/oauth2/v2/auth?${params.toString()}`}async function callGmailApi(payload){const{data,error}=await sb.functions.invoke("gmail-api",{body:payload});if(error){try{const body=await error.context.json();return{ok:false,error:body.error||error.message}}catch{return{ok:false,error:error.message}}}return data}let sb=null;try{sb=window.supabase.createClient(SUPABASE_URL,SUPABASE_ANON_KEY,{auth:{persistSession:true,storage:window.sessionStorage,flowType:"implicit"}})}catch(e){console.error("Supabase client failed to initialize — check that the CDN script tag loaded.",e)}try{if(window.__opsAttach)window.__opsAttach(sb)}catch(e){}(function(){var PORTAL="staff";var KEY="pt.latencyProbe."+PORTAL;var done=false,tries=0;function median(a){var b=a.filter(function(x){return x!=null}).sort(function(x,y){return x-y});return b.length?Math.round(b[Math.floor(b.length/2)]):null}async function time(url,opts){var t=performance.now();try{await fetch(url,opts)}catch(e){return null}return performance.now()-t}async function probe(url,opts){await time(url,opts);var r=[];for(var i=0;i<5;i++)r.push(await time(url+(url.indexOf("?")<0?"?":"&")+"r="+Math.random(),opts));return median(r)}async function run(){try{if(done||document.hidden||typeof sb==="undefined"||!sb||tries>=3)return;var today=new Date().toISOString().slice(0,10);try{if(localStorage.getItem(KEY)===today){done=true;return}}catch(e){}var s=await sb.auth.getSession();if(!s||!s.data||!s.data.session)return;done=true;tries++;var aws=function(region){return probe("https://dynamodb."+region+".amazonaws.com/ping",{mode:"no-cors",cache:"no-store"})};var row={portal:PORTAL,tz:Intl.DateTimeFormat().resolvedOptions().timeZone||null,supabase_ms:await probe(SUPABASE_URL+"/auth/v1/health",{cache:"no-store",headers:{apikey:SUPABASE_ANON_KEY}}),eu_ms:await aws("eu-central-1"),use1_ms:await aws("us-east-1"),use2_ms:await aws("us-east-2")};var res=await sb.from("latency_probes").insert(row);if(res&&!res.error){try{localStorage.setItem(KEY,today)}catch(e){}}else done=false}catch(e){done=false}}setTimeout(run,45e3);setInterval(run,10*60*1e3)})();let config={supabaseUrl:SUPABASE_URL,anonKey:SUPABASE_ANON_KEY};function currentTheme(){return document.documentElement.getAttribute("data-theme")==="light"?"light":"dark"}function applyThemeIcon(){const icon=currentTheme()==="light"?"☀️":"🌙";const loginBtn=document.getElementById("themeToggleBtnLogin");const appBtn=document.getElementById("themeToggleBtn");if(loginBtn)loginBtn.textContent=icon;if(appBtn)appBtn.textContent=icon}function toggleTheme(){const next=currentTheme()==="light"?"dark":"light";if(next==="light")document.documentElement.setAttribute("data-theme","light");else document.documentElement.removeAttribute("data-theme");try{localStorage.setItem("dashboardTheme",next)}catch(e){}applyThemeIcon()}document.getElementById("themeToggleBtnLogin").addEventListener("click",toggleTheme);document.getElementById("themeToggleBtn").addEventListener("click",toggleTheme);applyThemeIcon();let currentDetailAgent=null;let detailReturnTo=null;let pollHandle=null;let myRole=null;let myUserId=null;let myFullName="";let myTitle="";let myDepartment="";let myEmail="";let myAvatarUrl=null;let myGrade=null;let myChatStatus="offline";let myChatStatusManual=null;let myStatusText=null,myStatusEmoji=null,myStatusUntil=null;function applyUserGreeting(previousLoginAt){const firstName=(myFullName||myEmail||"").trim().split(/\s+/)[0]||"there";$("userGreetingName").textContent=`Welcome, ${firstName}!`;$("userGreetingEmail").textContent=myEmail||"";const greetingTz=nonAgentDisplayTz();$("userGreetingLastLogin").textContent=previousLoginAt?`Last login: ${new Date(previousLoginAt).toLocaleString([],{month:"short",day:"numeric",year:"numeric",hour:"2-digit",minute:"2-digit",...greetingTz?{timeZone:greetingTz}:{}})}`:"First login"}let myPermissions={allowedTabs:[],canManageUsers:false,isProtected:false,tabEdit:{},viewScope:"all",viewableUserIds:[],umSections:{},activitySections:{},leadsSections:{},csubSections:{},dataScienceSections:{},taxesSections:{},timeManagementSections:{},financeSections:{},hrSections:{},dialerAdminSections:{}};let myHasPermissionOverride=false;let myGrantCeiling=null;function canEditTab(tabKey){if(myPermissions.isProtected)return true;return myPermissions.tabEdit[tabKey]!==false}function applyEditGating(tabKey){const keys=tabKey?[tabKey]:DASHBOARD_TAB_KEYS.map(t=>t.key);keys.forEach(key=>{const panel=document.getElementById("tab-"+key);if(!panel)return;const editable=canEditTab(key);panel.querySelectorAll(".edit-only").forEach(el=>{el.disabled=!editable;el.style.opacity=editable?"":"0.4";el.style.cursor=editable?"":"not-allowed";el.title=editable?"":"Your role has view-only access to this tab"})})}let myIsOwner=false;const PROTECTED_ROLES=new Set(["owner","admin"]);const TASK_COLORS=["#3DDC84","#5AC8FA","#BF5AF2","#FF9F0A","#FF375F","#64D2FF","#30D158","#FFD60A"];function $(id){return document.getElementById(id)}function escapeHtml(s){return String(s==null?"":s).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;")}function safeHrefUrl(u){try{const parsed=new URL(String(u??""),location.href);return parsed.protocol==="http:"||parsed.protocol==="https:"?parsed.href:"#"}catch(e){return"#"}}const lightbox={shots:[],index:0};function renderLightbox(){const shot=lightbox.shots[lightbox.index];if(!shot)return;$("lightboxImg").src=shot.url;const lightboxTz=nonAgentDisplayTz();$("lightboxTime").textContent=new Date(shot.taken_at).toLocaleString([],{hour:"2-digit",minute:"2-digit",month:"short",day:"numeric",...lightboxTz?{timeZone:lightboxTz}:{}});$("lightboxActivity").textContent=(shot.activity_pct_at_capture??"—")+"% activity";$("lightboxCounter").textContent=`${lightbox.index+1} / ${lightbox.shots.length}`;$("lightboxPrevBtn").disabled=lightbox.index<=0;$("lightboxNextBtn").disabled=lightbox.index>=lightbox.shots.length-1}function openLightbox(shots,index){if(!shots||!shots.length)return;lightbox.shots=shots;lightbox.index=index;renderLightbox();$("lightboxOverlay").classList.add("active")}function closeLightbox(){$("lightboxOverlay").classList.remove("active");$("lightboxImg").src=""}function lightboxPrev(){if(lightbox.index>0){lightbox.index--;renderLightbox()}}function lightboxNext(){if(lightbox.index<lightbox.shots.length-1){lightbox.index++;renderLightbox()}}$("lightboxCloseBtn").addEventListener("click",closeLightbox);$("lightboxOverlay").addEventListener("click",e=>{if(e.target.id==="lightboxOverlay")closeLightbox()});$("lightboxPrevBtn").addEventListener("click",lightboxPrev);$("lightboxNextBtn").addEventListener("click",lightboxNext);document.addEventListener("keydown",e=>{if(!$("lightboxOverlay").classList.contains("active"))return;if(e.key==="Escape")closeLightbox();else if(e.key==="ArrowLeft")lightboxPrev();else if(e.key==="ArrowRight")lightboxNext()});document.querySelectorAll(".tab-btn").forEach(btn=>{btn.addEventListener("click",()=>{if(btn.dataset.tab==="client_communications"){const first=Array.from(document.querySelectorAll(".side-subnav-cc .tab-btn")).find(b=>b.style.display!=="none");if(first)activateTab(first.dataset.tab);return}if(btn.dataset.tab==="dialer_management"){if(typeof daVisibleSections==="function"&&daVisibleSections().length){activateTab("dialer_management");return}document.querySelectorAll(".side-main-nav > .side-nav-group > .tab-btn").forEach(b=>b.classList.toggle("active",b===btn));return}if(btn.dataset.tab==="dialer"||btn.dataset.tab==="dialer_admin"){const url=btn.dataset.tab==="dialer"?"https://dialer.proptechnologyai.com/":"https://admin.proptechnologyai.com/";const w=window.open(url,"_blank");if(!w)location.href=url;return}activateTab(btn.dataset.tab)})});function tzOffsetMsAt(utcMs,timezone){const parts=new Intl.DateTimeFormat("en-US",{timeZone:timezone,hour12:false,year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",second:"2-digit"}).formatToParts(new Date(utcMs)).reduce((acc,p)=>{acc[p.type]=p.value;return acc},{});let hour=parseInt(parts.hour,10);if(hour===24)hour=0;const asUtc=Date.UTC(parseInt(parts.year,10),parseInt(parts.month,10)-1,parseInt(parts.day,10),hour,parseInt(parts.minute,10),parseInt(parts.second,10));return asUtc-utcMs}function zonedTimeToUtcMs(y,mo,day,hour,min,sec,timezone){hour=hour||0;min=min||0;sec=sec||0;if(!timezone)return new Date(y,mo-1,day,hour,min,sec,0).getTime();const guessUtcMs=Date.UTC(y,mo-1,day,hour,min,sec,0);const utc1=guessUtcMs-tzOffsetMsAt(guessUtcMs,timezone);return guessUtcMs-tzOffsetMsAt(utc1,timezone)}function dayBoundsMsInZone(timezone,refDate,y,mo,day){if(y==null){refDate=refDate||new Date;if(timezone){const p=new Intl.DateTimeFormat("en-US",{timeZone:timezone,year:"numeric",month:"2-digit",day:"2-digit"}).formatToParts(refDate).reduce((acc,x)=>{acc[x.type]=x.value;return acc},{});y=parseInt(p.year,10);mo=parseInt(p.month,10);day=parseInt(p.day,10)}else{y=refDate.getFullYear();mo=refDate.getMonth()+1;day=refDate.getDate()}}const startMs=zonedTimeToUtcMs(y,mo,day,0,0,0,timezone);const endMs=zonedTimeToUtcMs(y,mo,day,23,59,59,timezone)+999;return{startMs,endMs}}function monthBoundsMsInZone(y,mo,timezone){const daysInMonth=new Date(y,mo,0).getDate();const startMs=zonedTimeToUtcMs(y,mo,1,0,0,0,timezone);const endMs=zonedTimeToUtcMs(y,mo,daysInMonth,23,59,59,timezone)+999;return{startMs,endMs,daysInMonth}}function ymdInZoneDash(utcMs,timezone){const p=new Intl.DateTimeFormat("en-US",{timeZone:timezone||"UTC",year:"numeric",month:"2-digit",day:"2-digit"}).formatToParts(new Date(utcMs)).reduce((a,x)=>{a[x.type]=x.value;return a},{});return{y:+p.year,mo:+p.month,d:+p.day}}function workDayStartMsDash(schedule,nowMs){nowMs=nowMs||Date.now();const tz=schedule&&schedule.timezone||null;const cutover=schedule&&Number.isInteger(schedule.day_cutover_hour)?schedule.day_cutover_hour:6;const{y,mo,d}=ymdInZoneDash(nowMs,tz);let boundary=zonedTimeToUtcMs(y,mo,d,cutover,0,0,tz);if(nowMs<boundary){const prev=new Date(Date.UTC(y,mo-1,d-1));boundary=zonedTimeToUtcMs(prev.getUTCFullYear(),prev.getUTCMonth()+1,prev.getUTCDate(),cutover,0,0,tz)}return boundary}function workWeekStartMsDash(schedule,nowMs){nowMs=nowMs||Date.now();const tz=schedule&&schedule.timezone||null;const dayStart=workDayStartMsDash(schedule,nowMs);const{y,mo,d}=ymdInZoneDash(dayStart+12*3600*1e3,tz);const dow=new Date(Date.UTC(y,mo-1,d)).getUTCDay();const daysSinceMon=(dow+6)%7;return dayStart-daysSinceMon*24*3600*1e3}function localDateKeyInZone(dateInput,timezone){const d=new Date(dateInput);const p=new Intl.DateTimeFormat("en-US",{timeZone:timezone||FALLBACK_TIMEZONE,year:"numeric",month:"2-digit",day:"2-digit"}).formatToParts(d).reduce((acc,x)=>{acc[x.type]=x.value;return acc},{});return`${p.year}-${p.month}-${p.day}`}const FALLBACK_TIMEZONE="Africa/Cairo";const FALLBACK_CUTOVER_HOUR=8;function todayRangeISO(){const{startMs,endMs}=dayBoundsMsInZone(FALLBACK_TIMEZONE);return{start:new Date(startMs).toISOString(),end:new Date(endMs).toISOString(),dayStartMs:startMs}}function cairoUtcOffsetMinutes(date){const parts=new Intl.DateTimeFormat("en-US",{timeZone:"Africa/Cairo",timeZoneName:"shortOffset"}).formatToParts(date);const tzPart=(parts.find(p=>p.type==="timeZoneName")||{}).value||"GMT+2";const m=tzPart.match(/GMT([+-]\d+)(?::(\d+))?/);if(!m)return 120;const hours=parseInt(m[1],10);const mins=m[2]?parseInt(m[2],10):0;return hours*60+(hours<0?-mins:mins)}function weekStartCairoISO(){const now=new Date;const offsetMin=cairoUtcOffsetMinutes(now);const cairoNow=new Date(now.getTime()+offsetMin*6e4);const weekday=cairoNow.getUTCDay();let daysSinceMonday=(weekday+6)%7;const cairoMinutesNow=cairoNow.getUTCHours()*60+cairoNow.getUTCMinutes();if(daysSinceMonday===0&&cairoMinutesNow<FALLBACK_CUTOVER_HOUR*60)daysSinceMonday=7;const boundaryCairoWallMs=Date.UTC(cairoNow.getUTCFullYear(),cairoNow.getUTCMonth(),cairoNow.getUTCDate()-daysSinceMonday,FALLBACK_CUTOVER_HOUR,0,0,0);return new Date(boundaryCairoWallMs-offsetMin*6e4).toISOString()}function startOfMonthISO(){const p=new Intl.DateTimeFormat("en-US",{timeZone:FALLBACK_TIMEZONE,year:"numeric",month:"2-digit"}).formatToParts(new Date).reduce((a,x)=>{a[x.type]=x.value;return a},{});const{startMs}=monthBoundsMsInZone(+p.year,+p.month,FALLBACK_TIMEZONE);return new Date(startMs).toISOString()}function fmtShort(sec){sec=Math.max(0,Math.round(sec));const h=Math.floor(sec/3600),m=Math.floor(sec%3600/60);return h>0?h+"h "+m+"m":m+"m"}function fmtDurationLive(sec){sec=Math.max(0,Math.round(sec));const h=Math.floor(sec/3600),m=Math.floor(sec%3600/60),s=sec%60;if(h>0)return`${h}h ${String(m).padStart(2,"0")}m ${String(s).padStart(2,"0")}s`;if(m>0)return`${m}m ${String(s).padStart(2,"0")}s`;return`${s}s`}let statusTimerTickHandle=null;function updateStatusTimers(){const now=Date.now();document.querySelectorAll(".live-status-timer").forEach(el=>{const since=Number(el.getAttribute("data-since"));if(!since)return;el.textContent=`(${fmtDurationLive((now-since)/1e3)})`})}function startStatusTimerTicker(){if(statusTimerTickHandle)return;statusTimerTickHandle=setInterval(()=>{if(!document.hidden)updateStatusTimers()},1e3)}const PRESENCE_HEARTBEAT_MS=3e4;const PRESENCE_STALE_MS=18e4;let presenceHeartbeatHandle=null;const IDLE_AWAY_MS=10*60*1e3;let lastActivityAt=Date.now();function markUserActivity(){lastActivityAt=Date.now()}["mousemove","mousedown","keydown","wheel","touchstart","scroll"].forEach(evt=>{window.addEventListener(evt,markUserActivity,{passive:true,capture:true})});function currentAutoStatus(){if(document.visibilityState!=="visible"||!document.hasFocus())return"away";if(Date.now()-lastActivityAt>=IDLE_AWAY_MS)return"away";return"active"}async function writeHeartbeat(){if(!sb||!myUserId)return;const patch={};if(myStatusUntil&&Date.now()>new Date(myStatusUntil).getTime()){myChatStatusManual=null;myStatusText=null;myStatusEmoji=null;myStatusUntil=null;Object.assign(patch,{chat_status_manual:null,status_text:null,status_emoji:null,status_until:null})}const auto=currentAutoStatus();const effective=myChatStatusManual==="invisible"?"offline":myChatStatusManual||auto;myChatStatus=effective;patch.chat_status=effective;if(myChatStatusManual!=="invisible")patch.last_seen_at=new Date().toISOString();try{await sb.from("profiles").update(patch).eq("id",myUserId)}catch(e){}}function startPresenceHeartbeat(){if(presenceHeartbeatHandle)return;writeHeartbeat();presenceHeartbeatHandle=setInterval(writeHeartbeat,PRESENCE_HEARTBEAT_MS);document.addEventListener("visibilitychange",writeHeartbeat);window.addEventListener("focus",writeHeartbeat);window.addEventListener("blur",writeHeartbeat)}function stopPresenceHeartbeat(){if(presenceHeartbeatHandle){clearInterval(presenceHeartbeatHandle);presenceHeartbeatHandle=null}}function derivePresence(person){if(!person)return"offline";const expired=!!(person.status_until&&Date.now()>new Date(person.status_until).getTime());const manual=expired?null:person.chat_status_manual;if(manual==="invisible")return"offline";let stored=person.chat_status||"offline";if(stored==="offline")return"offline";const seen=person.last_seen_at?new Date(person.last_seen_at).getTime():0;if(!seen||Date.now()-seen>PRESENCE_STALE_MS)return"offline";if(manual==="busy"||manual==="dnd"||manual==="brb")return manual;if(stored==="busy"||stored==="dnd"||stored==="brb")stored="active";return stored}function fmtHM(sec){sec=Math.max(0,Math.round(sec));const h=Math.floor(sec/3600),m=Math.floor(sec%3600/60);return`${String(h).padStart(2,"0")}:${String(m).padStart(2,"0")}`}function compareVersionStrings(a,b){const pa=String(a).split(".").map(n=>parseInt(n,10)||0);const pb=String(b).split(".").map(n=>parseInt(n,10)||0);const len=Math.max(pa.length,pb.length);for(let i=0;i<len;i++){const diff=(pa[i]||0)-(pb[i]||0);if(diff!==0)return diff<0?-1:1}return 0}function colorFor(id,list){const idx=list.findIndex(x=>x.id===id);if(idx===-1)return"#8E8E93";return TASK_COLORS[idx%TASK_COLORS.length]}function exportCSV(filename,headers,rows){const escapeCell=v=>{const s=String(v??"");return/[",\n]/.test(s)?'"'+s.replace(/"/g,'""')+'"':s};const lines=[headers.map(escapeCell).join(",")].concat(rows.map(r=>r.map(escapeCell).join(",")));const blob=new Blob([lines.join("\n")],{type:"text/csv;charset=utf-8;"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=filename;document.body.appendChild(a);a.click();document.body.removeChild(a);URL.revokeObjectURL(url)}async function exportPDF(title,headers,rows){try{await ensureJsPdf()}catch(e){}if(!window.jspdf){alert("PDF export library failed to load — check your internet connection and try again.");return}const{jsPDF}=window.jspdf;const doc=new jsPDF({orientation:rows.length&&headers.length>6?"landscape":"portrait"});doc.setFontSize(14);doc.text(title,14,16);doc.setFontSize(9);doc.setTextColor(120);const exportPdfTz=nonAgentDisplayTz();doc.text(`Generated ${new Date().toLocaleString([],exportPdfTz?{timeZone:exportPdfTz}:{})}`,14,22);doc.autoTable({head:[headers],body:rows,startY:28,styles:{fontSize:8,cellPadding:3},headStyles:{fillColor:[23,27,35]},theme:"striped"});doc.save(title.toLowerCase().replace(/\s+/g,"-")+".pdf")}async function logActivity(action,targetType,targetId,details){try{const{data:{user}}=await sb.auth.getUser();if(!user)return;await sb.from("activity_log").insert({admin_id:user.id,action,target_type:targetType||null,target_id:targetId||null,details:details||null})}catch(e){console.error("Failed to log activity",e)}}const PASSWORD_MAX_AGE_DAYS=30;function isPasswordExpired(changedAt){if(!changedAt)return true;return Date.now()-new Date(changedAt).getTime()>PASSWORD_MAX_AGE_DAYS*24*60*60*1e3}let forcePasswordUser=null;function showForcePasswordScreen(user,reason){forcePasswordUser=user;$("forcePasswordNew").value="";$("forcePasswordConfirm").value="";$("forcePasswordMsg").textContent="";$("forcePasswordMsg").className="msg";$("forcePasswordReason").textContent=reason==="new"?"This is your first sign-in with this password — choose a new one to continue.":`For security, passwords need to be changed every ${PASSWORD_MAX_AGE_DAYS} days. Choose a new one to continue.`;showLoginPanel("forcePasswordScreen");$("loginScreen").style.display="block";$("forcePasswordNew").focus()}$("forcePasswordBtn").addEventListener("click",async()=>{const p1=$("forcePasswordNew").value;const p2=$("forcePasswordConfirm").value;const msg=$("forcePasswordMsg");if(!p1||p1.length<8){msg.textContent="Password must be at least 8 characters.";msg.className="msg error";return}if(p1!==p2){msg.textContent="Passwords don’t match.";msg.className="msg error";return}const btn=$("forcePasswordBtn");const originalLabel=btn.textContent;btn.disabled=true;btn.textContent="Saving…";try{const{error:pwErr}=await sb.auth.updateUser({password:p1});if(pwErr)throw pwErr;const user=forcePasswordUser;const{error:profErr}=await sb.from("profiles").update({must_change_password:false,password_changed_at:new Date().toISOString()}).eq("id",user.id);if(profErr)throw profErr;forcePasswordUser=null;await establishSession(user,{isNewLogin:true})}catch(e){msg.textContent=e.message||"Could not update password.";msg.className="msg error";btn.disabled=false;btn.textContent=originalLabel}});const TIME_EXTENSION_ID="ejpaamckgoggmegolclnifbmdnbnlmei";async function handOverSessionToExtension(){try{if(!(window.chrome&&chrome.runtime&&chrome.runtime.sendMessage))return;const{data,error}=await sb.functions.invoke("extension-session-handover",{body:{}});if(error||!data||!data.ok||!data.tokenHash)return;chrome.runtime.sendMessage(TIME_EXTENSION_ID,{type:"SESSION_HANDOVER",tokenHash:data.tokenHash,userId:data.userId},()=>{void chrome.runtime.lastError})}catch(e){}}async function establishSession(user,{isNewLogin=true}={}){const{data:profile}=await sb.from("profiles").select("role, full_name, avatar_url, chat_status, chat_status_manual, status_text, status_emoji, status_until, must_change_password, password_changed_at, department, title, grade").eq("id",user.id).single();if(await mfaSignInStep(user,profile?.role||"",{isNewLogin}))return;if(profile&&(profile.must_change_password||isPasswordExpired(profile.password_changed_at))){showForcePasswordScreen(user,profile.must_change_password?"new":"expired");return}const{data:roleRow,error:roleErr}=await sb.from("roles").select("name, allowed_tabs, can_manage_users, is_protected, tab_edit, view_scope, viewable_user_ids, viewable_role, um_sections, activity_sections, leads_sections, csub_sections, finance_sections, hr_sections, dialer_admin_sections, can_manage_dialer, can_review_calls, data_science_sections, taxes_sections, time_management_sections, task_category_ids, can_text_clients, can_edit_compensation, can_manual_dial").eq("name",profile?.role||"").maybeSingle();if(roleErr){await sb.auth.signOut();const schemaErr=new Error(`Couldn't load role permissions: ${roleErr.message}. This usually means a database migration hasn't been run yet — check supabase/schema.sql.`);schemaErr.code="role_query_failed";throw schemaErr}const isOwnerRole=profile?.role==="owner";if(!isOwnerRole&&(!roleRow||!Array.isArray(roleRow.allowed_tabs)||roleRow.allowed_tabs.length===0)){await sb.auth.signOut();const noAccessErr=new Error("This account doesn’t have dashboard access.");noAccessErr.code="no_dashboard_access";throw noAccessErr}myRole=roleRow?.name||"owner";let overrideRow=null;try{const{data:ovr}=await sb.from("user_permission_overrides").select("*").eq("user_id",user.id).maybeSingle();overrideRow=ovr||null}catch(e){}const _sec=family=>Object.assign({},roleRow?.[family]||{},overrideRow?.[family]||{});const _flag=name=>typeof overrideRow?.[name]==="boolean"?overrideRow[name]:!!roleRow?.[name];myHasPermissionOverride=!!overrideRow;myPermissions={allowedTabs:isOwnerRole?DASHBOARD_TAB_KEYS.map(t=>t.key):Array.isArray(overrideRow?.allowed_tabs)?overrideRow.allowed_tabs:roleRow.allowed_tabs,canManageUsers:isOwnerRole?true:!!roleRow?.can_manage_users,isProtected:isOwnerRole?true:!!roleRow?.is_protected,tabEdit:_sec("tab_edit"),viewScope:roleRow?.view_scope||"all",viewableUserIds:roleRow?.viewable_user_ids||[],umSections:_sec("um_sections"),activitySections:_sec("activity_sections"),leadsSections:_sec("leads_sections"),csubSections:_sec("csub_sections"),financeSections:_sec("finance_sections"),hrSections:_sec("hr_sections"),dialerAdminSections:_sec("dialer_admin_sections"),dataScienceSections:_sec("data_science_sections"),taxesSections:_sec("taxes_sections"),timeManagementSections:_sec("time_management_sections"),canTextClients:isOwnerRole?true:_flag("can_text_clients"),canEditCompensation:isOwnerRole?true:_flag("can_edit_compensation"),canManualDial:isOwnerRole?true:_flag("can_manual_dial")};myIsOwner=myRole==="owner";myGrantCeiling=myIsOwner?null:{allowedTabs:myPermissions.allowedTabs||[],tab_edit:myPermissions.tabEdit,um_sections:myPermissions.umSections,activity_sections:myPermissions.activitySections,leads_sections:myPermissions.leadsSections,csub_sections:myPermissions.csubSections,finance_sections:myPermissions.financeSections,hr_sections:myPermissions.hrSections,data_science_sections:myPermissions.dataScienceSections,taxes_sections:myPermissions.taxesSections,time_management_sections:myPermissions.timeManagementSections,dialer_admin_sections:myPermissions.dialerAdminSections,can_text_clients:!!myPermissions.canTextClients,can_edit_compensation:!!myPermissions.canEditCompensation,can_manual_dial:!!myPermissions.canManualDial};myFullName=profile?.full_name||"";myTitle=profile?.title||"";if(typeof renderProfileMenu==="function")renderProfileMenu();myDepartment=profile?.department||"";myEmail=user.email;myUserId=user.id;myAvatarUrl=profile?.avatar_url||null;myGrade=profile?.grade||null;if(typeof renderProfileMenu==="function")renderProfileMenu();setTimeout(()=>{if(typeof avatarAutoShrink==="function")avatarAutoShrink()},8e3);myChatStatus=profile?.chat_status||"offline";myChatStatusManual=["busy","dnd","brb","invisible"].includes(profile?.chat_status_manual)?profile.chat_status_manual:null;myStatusText=profile?.status_text||null;myStatusEmoji=profile?.status_emoji||null;myStatusUntil=profile?.status_until||null;sb.from("tasks").select("id, name, archived_at").order("name").then(({data})=>refreshExtensionTaskSections(data||[])).catch(()=>{});let previousLoginAt=null;try{const{data:priorLogins}=await sb.from("activity_log").select("created_at").eq("admin_id",user.id).eq("action","auth.login").order("created_at",{ascending:false}).limit(1);previousLoginAt=priorLogins&&priorLogins[0]?priorLogins[0].created_at:null}catch(e){}applyRolePermissionsToNav();applyEditGating();startPendingCounts(isNewLogin);applyOwnerAdminExportGating();if($("csubNewCampaignBtn"))$("csubNewCampaignBtn").style.display=myIsOwner||myRole==="admin"||canEditCsubSection("campaign")?"":"none";$("loginScreen").style.display="none";$("app").style.display="block";applyUserGreeting(previousLoginAt);initChatWidget();if(myPermissions.allowedTabs.includes("usermanagement"))loadTaskCategoriesList();if(myIsOwner||canViewTimeManagementSection("reports"))ensureFxRate();if(isNewLogin)logActivity("auth.login","auth",user.id,`${user.email} signed in`);pollHandle=setInterval(refreshAll,2e4);(window.requestIdleCallback||(fn=>setTimeout(fn,2500)))(()=>{if(typeof ensureGooglePlaces==="function")ensureGooglePlaces().catch(()=>{})},{timeout:6e3});startStatusTimerTicker();startPresenceHeartbeat();handOverSessionToExtension()}async function doLogin(){const email=$("email").value.trim();const password=$("password").value;if(!email||!password){$("loginMsg").textContent="Email and password are required.";return}if(!sb){$("loginMsg").textContent="Could not connect. Check your internet connection and reload the page.";return}const btn=$("loginBtn");const originalLabel=btn.textContent;btn.disabled=true;btn.textContent="Signing in…";$("loginMsg").textContent="";try{const{data,error}=await sb.auth.signInWithPassword({email,password});if(error)throw error;try{localStorage.setItem("dashboardLastEmail",email)}catch(e){}await establishSession(data.user,{isNewLogin:true})}catch(e){$("loginMsg").textContent=e.message||"Login failed."}finally{btn.disabled=false;btn.textContent=originalLabel}}$("loginBtn").addEventListener("click",doLogin);["email","password"].forEach(id=>{$(id).addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();doLogin()}})});function prefillAndFocusLogin(){let lastEmail="";try{lastEmail=localStorage.getItem("dashboardLastEmail")||""}catch(e){}if(lastEmail){$("email").value=lastEmail;$("password").focus()}else{$("email").focus()}}prefillAndFocusLogin();$("loginVersionLine").textContent=`${APP_VERSION} · ${APP_BUILD_DATE}`;if(SUPPORT_CONTACT){$("loginSupportLink").textContent=SUPPORT_CONTACT;$("loginSupportLink").href=`mailto:${SUPPORT_CONTACT}`;$("loginSupportLine").style.display=""}function showLoginPanel(panelId){["loginFormPanel","forgotPanel","recoveryPanel","forcePasswordScreen","mfaPanel"].forEach(id=>{$(id).style.display=id===panelId?"block":"none"})}let mfaPendingUser=null;let mfaPendingOpts=null;let mfaFactorId=null;let mfaMode=null;let mfaJustVerified=false;async function mfaRequiredRoles(){try{const{data}=await sb.from("app_settings").select("value").eq("key","mfa_required_roles").maybeSingle();const v=data&&data.value?JSON.parse(data.value):[];return Array.isArray(v)?v:[]}catch(e){return[]}}async function mfaSignInStep(user,roleName,opts){if(staffImpersonating)return false;if(!sb||!sb.auth||!sb.auth.mfa||typeof sb.auth.mfa.getAuthenticatorAssuranceLevel!=="function")return false;try{const{data:aal,error}=await sb.auth.mfa.getAuthenticatorAssuranceLevel();if(error)throw error;if(aal.currentLevel==="aal2"){mfaJustVerified=false;return false}if(mfaJustVerified){mfaJustVerified=false;try{if(window.__opsReport)window.__opsReport("two-step sign-in: code accepted but session still "+aal.currentLevel)}catch(_){}return false}mfaPendingUser=user;mfaPendingOpts=opts;if(aal.nextLevel==="aal2"){await mfaShowChallenge();return true}const required=await mfaRequiredRoles();if(required.includes(roleName)){await mfaShowEnroll();return true}return false}catch(e){try{if(window.__opsReport)window.__opsReport("two-step sign-in could not start: "+(e&&e.message?e.message:e))}catch(_){}return false}}function mfaOpenPanel(){$("mfaCode").value="";$("mfaMsg").textContent="";$("mfaMsg").className="msg";showLoginPanel("mfaPanel");$("loginScreen").style.display="block";setTimeout(()=>{try{$("mfaCode").focus()}catch(e){}},0)}async function mfaShowChallenge(){const{data,error}=await sb.auth.mfa.listFactors();if(error)throw error;const f=(data&&data.totp||[])[0];if(!f)throw new Error("no verified authenticator on this account");mfaFactorId=f.id;mfaMode="challenge";$("mfaTitle").textContent="Enter your sign-in code";$("mfaIntro").textContent="Open your authenticator app and type the 6-digit code it shows for PropTech AI.";$("mfaEnrollBox").style.display="none";$("mfaHelp").textContent="Lost the phone with your authenticator? Ask the Owner or an admin to reset your two-step sign-in.";mfaOpenPanel()}async function mfaShowEnroll(){try{const{data:data2}=await sb.auth.mfa.listFactors();for(const f of data2&&data2.all||[]){if(f.status!=="verified")await sb.auth.mfa.unenroll({factorId:f.id}).catch(()=>{})}}catch(e){}const{data,error}=await sb.auth.mfa.enroll({factorType:"totp",issuer:"PropTech AI",friendlyName:"PropTech AI "+new Date().toISOString().slice(0,16).replace("T"," ")});if(error)throw error;mfaFactorId=data.id;mfaMode="enroll";$("mfaTitle").textContent="Set up two-step sign-in";$("mfaIntro").innerHTML="Your account now needs a second step when you sign in. Install <strong>Google Authenticator</strong> or <strong>Microsoft Authenticator</strong> on your phone, scan this code with it, then type the 6-digit code the app shows.";$("mfaQr").src=data.totp.qr_code;$("mfaSecret").textContent=data.totp.secret;$("mfaEnrollBox").style.display="block";$("mfaHelp").textContent="You only do this once. After that, signing in asks for the code the app shows at that moment.";mfaOpenPanel()}$("mfaVerifyBtn").addEventListener("click",async()=>{const msg=$("mfaMsg");const code=$("mfaCode").value.replace(/\D/g,"");if(code.length!==6){msg.textContent="Type the 6-digit code from your authenticator app.";msg.className="msg error";return}const btn=$("mfaVerifyBtn");const label=btn.textContent;btn.disabled=true;btn.textContent="Checking…";try{const{error}=await sb.auth.mfa.challengeAndVerify({factorId:mfaFactorId,code});if(error){msg.textContent=/invalid|expired|verif/i.test(error.message||"")?"That code didn’t match. Codes change every 30 seconds — type the one showing now.":error.message||"Could not check the code.";msg.className="msg error";return}msg.textContent=mfaMode==="enroll"?"Two-step sign-in is on.":"Verified.";msg.className="msg";const u=mfaPendingUser;const o=mfaPendingOpts||{isNewLogin:true};mfaPendingUser=null;mfaJustVerified=true;await establishSession(u,o)}catch(e){msg.textContent=e&&e.message?e.message:"Could not check the code.";msg.className="msg error"}finally{btn.disabled=false;btn.textContent=label}});$("mfaCode").addEventListener("keydown",e=>{if(e.key==="Enter")$("mfaVerifyBtn").click()});$("mfaSignOutLink").addEventListener("click",e=>{e.preventDefault();performSignOut()});let passwordRecoveryInProgress=false;$("forgotPwLink").addEventListener("click",e=>{e.preventDefault();$("forgotEmail").value=$("email").value.trim();$("forgotMsg").textContent="";$("forgotMsg").className="msg";showLoginPanel("forgotPanel");setTimeout(()=>{try{$("forgotEmail").focus()}catch(_){}},0)});$("forgotBackLink").addEventListener("click",e=>{e.preventDefault();$("email").value=$("forgotEmail").value.trim()||$("email").value;showLoginPanel("loginFormPanel");setTimeout(()=>{try{$("password").focus()}catch(_){}},0)});$("forgotEmail").addEventListener("keydown",e=>{if(e.key==="Enter")$("forgotSendBtn").click()});$("forgotSendBtn").addEventListener("click",async()=>{const msg=$("forgotMsg");const email=$("forgotEmail").value.trim();if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)){msg.textContent="Enter the email address you sign in with.";msg.className="msg error";return}if(!sb){msg.textContent="Could not connect. Check your internet connection and reload the page.";msg.className="msg error";return}const btn=$("forgotSendBtn");const label=btn.textContent;btn.disabled=true;btn.textContent="Sending…";try{const{data,error}=await sb.functions.invoke("staff-password-reset",{body:{email}});let reply=data;if(error){try{reply=await error.context.json()}catch(_){reply=null}throw new Error(reply&&reply.error||error.message||"Could not send the email.")}msg.textContent=reply&&reply.message||"If that email belongs to a staff account, a temporary password is on its way.";msg.className="msg ok"}catch(e){msg.textContent=e.message||"Could not send the email.";msg.className="msg error"}finally{btn.disabled=false;btn.textContent=label}});if(sb)sb.auth.onAuthStateChange(event=>{if(event==="PASSWORD_RECOVERY"){passwordRecoveryInProgress=true;showLoginPanel("recoveryPanel");$("loginScreen").style.display="block";$("recoveryPassword").focus()}});$("saveNewPasswordBtn").addEventListener("click",async()=>{const pw=$("recoveryPassword").value;const pwConfirm=$("recoveryPasswordConfirm").value;const msg=$("recoveryMsg");if(!pw||pw.length<8){msg.textContent="Password must be at least 8 characters.";msg.className="msg error";return}if(pw!==pwConfirm){msg.textContent="Passwords don’t match.";msg.className="msg error";return}if(!sb){msg.textContent="Could not connect. Check your internet connection and reload the page.";msg.className="msg error";return}const btn=$("saveNewPasswordBtn");const originalLabel=btn.textContent;btn.disabled=true;btn.textContent="Saving…";try{const{error}=await sb.auth.updateUser({password:pw});if(error)throw error;await sb.auth.signOut();msg.textContent="Password updated — sign in with your new password below.";msg.className="msg ok";setTimeout(()=>showLoginPanel("loginFormPanel"),1500)}catch(e){msg.textContent=e.message||"Could not update password.";msg.className="msg error"}finally{btn.disabled=false;btn.textContent=originalLabel}});function applyRolePermissionsToNav(){document.querySelectorAll(".tab-btn").forEach(btn=>{const key=btn.dataset.tab;const visible=myIsOwner||myPermissions.allowedTabs.includes(key);btn.style.display=visible?"":"none"});const _opsBtn=document.querySelector('.tab-btn[data-tab="ops_health"]');if(_opsBtn)_opsBtn.style.display=opsHealthCanView()?"":"none";const _ciBtn=document.querySelector('.tab-btn[data-tab="client_chat"]');if(_ciBtn)_ciBtn.style.display=ciCanUseInbox()?"":"none";const _cdBtn=document.querySelector('.tab-btn[data-tab="client_disputes"]');const _canDisputes=myIsOwner||myPermissions.allowedTabs.includes("leads")&&canViewLeadsSection("leads_dispute");if(_cdBtn)_cdBtn.style.display=_canDisputes?"":"none";const _ccBtn=document.querySelector('.tab-btn[data-tab="client_communications"]');if(_ccBtn)_ccBtn.style.display=ciCanUseInbox()||_canDisputes?"":"none";const _ngb=$("chatNewGroupBtn");if(_ngb)_ngb.style.display=chatIsSenior()?"":"none";const _hrcBtn=document.querySelector('.hr-subtab-btn[data-hr-section="commission"]');if(_hrcBtn&&_hrcBtn.lastChild)_hrcBtn.lastChild.textContent=ciIsAdmin()?"Recruiter Commission":"My Commission";const _dmBtn=document.querySelector('.tab-btn[data-tab="dialer_management"]');if(_dmBtn){const _canDialer=myIsOwner||myPermissions.allowedTabs.includes("dialer");const _canDialerAdmin=myIsOwner||myPermissions.allowedTabs.includes("dialer_admin");_dmBtn.style.display=_canDialer||_canDialerAdmin?"":"none"}document.querySelectorAll(".da-subtab-btn").forEach(b=>{b.style.display=canViewDialerAdminSection(b.dataset.daSection)?"":"none"});daOpenDeepLinkOnce();const _aub=document.getElementById("addUserBtn");if(_aub)_aub.style.display=myPermissions.canManageUsers?"":"none";const _ncc=document.getElementById("chatNewClientChatBtn");if(_ncc)_ncc.style.display=myPermissions.canTextClients?"":"none";if(myPermissions.canTextClients){gmailNotifyLoadSeen();if(!gmailNotifyPollTimer){pollGmailReplyNotifications();gmailNotifyPollTimer=setInterval(()=>{if(!document.hidden)pollGmailReplyNotifications()},6e4)}}else if(gmailNotifyPollTimer){clearInterval(gmailNotifyPollTimer);gmailNotifyPollTimer=null}const tmBtn=document.querySelector('.tab-btn[data-tab="time_management"]');const canLandOnDashboard=tmBtn&&tmBtn.style.display!=="none"&&(myIsOwner||canViewTimeManagementSection("staff"));const landingBtn=canLandOnDashboard?tmBtn:Array.from(document.querySelectorAll(".tab-btn")).find(btn=>btn.style.display!=="none"&&document.getElementById("tab-"+btn.dataset.tab));if(staffPageRestore())return;if(landingBtn)activateTab(landingBtn.dataset.tab)}const STAFF_PAGE_KEY="pt.staffPage";let staffPageMemoryOff=false;function staffPageSnapshot(){if(staffPageMemoryOff||!myUserId)return;const panel=document.querySelector(".tab-panel.active");const tab=panel&&panel.id.startsWith("tab-")?panel.id.slice(4):null;if(!tab)return;const state={uid:myUserId,tab,tm:tmActiveSection,um:umActiveSection,audit:auditActiveSection,leads:leadsActiveSection,csub:csubActiveSection,finance:financeActiveSection,dataSci:dataSciActiveSection,taxes:taxesActiveSection,hr:hrActiveSection,da:daActiveSection,daPath:daActivePath,actGroup:activeActivityGroup,actKind:activeActivityKind};try{sessionStorage.setItem(STAFF_PAGE_KEY,JSON.stringify(state))}catch(e){}}window.addEventListener("pagehide",staffPageSnapshot);document.addEventListener("visibilitychange",()=>{if(document.hidden)staffPageSnapshot()});function staffPageRestore(){let st=null;try{st=JSON.parse(sessionStorage.getItem(STAFF_PAGE_KEY)||"null")}catch(e){}if(!st||!st.tab||st.uid!==myUserId)return false;if(/^#dialer-admin/.test(location.hash||""))return false;const btn=document.querySelector(`.tab-btn[data-tab="${st.tab}"]`);if(!btn||!document.getElementById("tab-"+st.tab))return false;for(let el=btn;el&&el!==document.body;el=el.parentElement){if(el.style&&el.style.display==="none")return false}if(st.tab==="dialer_management"&&!daVisibleSections().length)return false;if(st.tm)tmActiveSection=st.tm;if(st.um)umActiveSection=st.um;if(st.audit)auditActiveSection=st.audit;if(st.leads)leadsActiveSection=st.leads;if(st.csub)csubActiveSection=st.csub;if(st.finance)financeActiveSection=st.finance;if(st.dataSci)dataSciActiveSection=st.dataSci;if(st.taxes)taxesActiveSection=st.taxes;if(st.hr)hrActiveSection=st.hr;if(st.da){daActiveSection=st.da;daActivePath=st.daPath||null}if(st.actGroup)activeActivityGroup=st.actGroup;if(st.actKind)activeActivityKind=st.actKind;activateTab(st.tab);return true}let mktClients=[];let mktContent=null;let mktAudiences=[];let mktDomains=[];let mktCsv=null;let mktAccess=null;async function mktCall(action,payload){const AUD=["audience_list","audience_create","audience_delete","contacts_import"];const TPL=["content_block","content_variant"];const fn=AUD.includes(action)?"marketing-audiences":TPL.includes(action)?"marketing-templates":"marketing-domains";const{data,error}=await sb.functions.invoke(fn,{body:{action,...payload}});if(data&&data.ok===false)throw new Error(data.error||"Request failed.");if(error){if(error.context&&typeof error.context.json==="function"){try{const b=await error.context.json();if(b&&b.error)throw new Error(b.error)}catch(e){if(e instanceof Error&&e.message&&!/json/i.test(e.message))throw e}}throw new Error(error.message||"Request failed.")}return data}function mktParseCsv(text){const rows=[];let row=[],field="",q=false;const s=String(text).replace(/\r\n/g,"\n").replace(/\r/g,"\n");for(let i=0;i<s.length;i++){const c=s[i];if(q){if(c==='"'){if(s[i+1]==='"'){field+='"';i++}else q=false}else field+=c}else if(c==='"')q=true;else if(c===","){row.push(field);field=""}else if(c==="\n"){row.push(field);rows.push(row);row=[];field=""}else field+=c}if(field.length||row.length){row.push(field);rows.push(row)}return rows.filter(r=>r.some(v=>String(v).trim()!==""))}function mktGuess(headers,kind){const want={email:["email","e-mail","email address","emailaddress","mail"],first_name:["first name","firstname","first","given name","fname"],last_name:["last name","lastname","last","surname","lname"],company:["company","business","organisation","organization","company name"]}[kind]||[];const n=headers.map(h=>String(h).trim().toLowerCase());for(const w of want){const i=n.indexOf(w);if(i>=0)return i}for(let i=0;i<n.length;i++)if(want.some(w=>n[i].includes(w)))return i;return-1}function mktSel(id,headers,sel){return`<select id="${id}"><option value="-1">— none —</option>${headers.map((h,i)=>`<option value="${i}"${i===sel?" selected":""}>${escapeHtml(h||"Column "+(i+1))}</option>`).join("")}</select>`}const MKST_COLORS=["#4f7df3","#a78bfa","#38bdf8","var(--mkst-teal)","#f59e0b","#ec4899","#94a3b8"];let mkstPane="stats";let mkstData=null;let mkstToday=null;let mkstCampaignOptions=[];let mkstTodayTimer=null;let mkstInited=false;function mkstChicagoToday(){return new Intl.DateTimeFormat("en-CA",{timeZone:"America/Chicago",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date)}function mkstShiftDay(iso,days){const d=new Date(iso+"T12:00:00Z");d.setUTCDate(d.getUTCDate()+days);return d.toISOString().slice(0,10)}const mkstN=v=>Number(v||0).toLocaleString();const mkstPctNum=(a,b)=>b?100*a/b:0;const mkstPct=(a,b,dp=1)=>b?`${mkstPctNum(a,b).toFixed(dp)}%`:"—";function mkstDate(iso,withTime){if(!iso)return"—";const opts=withTime?{month:"short",day:"numeric",year:"numeric",hour:"numeric",minute:"2-digit",timeZone:"America/Chicago"}:{month:"short",day:"numeric",year:"numeric",timeZone:"America/Chicago"};return new Date(iso).toLocaleString("en-US",opts)}function initMarketingStats(){if(!mkstInited){mkstInited=true;document.querySelectorAll(".mkst-tab").forEach(b=>b.addEventListener("click",()=>mkstShowPane(b.dataset.mkstPane)));const today=mkstChicagoToday();$("mkstTo").value=today;$("mkstFrom").value=mkstShiftDay(today,-6);document.querySelector('.mkst-quick [data-mkst-days="7"]').classList.add("active");document.querySelectorAll(".mkst-quick button").forEach(b=>b.addEventListener("click",()=>{const t=mkstChicagoToday();$("mkstTo").value=t;$("mkstFrom").value=mkstShiftDay(t,-(Number(b.dataset.mkstDays)-1));document.querySelectorAll(".mkst-quick button").forEach(x=>x.classList.toggle("active",x===b));mkstLoad()}));["mkstFrom","mkstTo"].forEach(id=>$(id).addEventListener("change",()=>{document.querySelectorAll(".mkst-quick button").forEach(x=>x.classList.remove("active"));mkstLoad()}));$("mkstClient").addEventListener("change",()=>{$("mkstCampaign").value="";mkstCampaignOptions=[];mkstLoad()});$("mkstCampaign").addEventListener("change",mkstLoad);$("mkstRefresh").addEventListener("click",mkstLoad);$("mkstMetric").addEventListener("change",mkstRenderTrend);$("mkstTopSort").addEventListener("change",mkstRenderTop);$("mkstNumbers").addEventListener("change",mkstRenderTop);$("mkstTodayRefresh").addEventListener("click",mkstLoadToday)}mkstFillClients();mkstShowPane(mkstPane)}async function mkstFillClients(){const sel=$("mkstClient");if(sel.options.length>1)return;if(!mktClients.length){const{data}=await sb.from("client_accounts").select("id, email, company_name").order("email");mktClients=data||[]}const keep=sel.value;sel.innerHTML='<option value="">All clients</option>'+mktClients.map(c=>`<option value="${escapeHtml(c.id)}">${escapeHtml(c.company_name||c.email)}</option>`).join("");sel.value=keep}function mkstShowPane(pane){mkstPane=pane;document.querySelectorAll(".mkst-tab").forEach(b=>b.classList.toggle("active",b.dataset.mkstPane===pane));["stats","today","manage"].forEach(p=>{$("mkstPane-"+p).style.display=p===pane?"":"none"});clearTimeout(mkstTodayTimer);if(pane==="stats")mkstLoad();if(pane==="today")mkstLoadToday();if(pane==="manage")initMarketingTab()}function mkstVisible(){const panel=document.getElementById("tab-client_submissions");return panel&&panel.classList.contains("active")&&csubActiveSection==="marketing"&&!document.hidden}async function mkstLoad(){const msg=$("mkstMsg");msg.style.color="var(--text-2)";msg.textContent="Loading…";const client=$("mkstClient").value||null;const campaign=$("mkstCampaign").value||null;const{data,error}=await sb.rpc("marketing_stats",{p_client:client,p_campaign:campaign,p_from:$("mkstFrom").value||null,p_to:$("mkstTo").value||null});if(error){msg.style.color="var(--danger)";msg.textContent=error.message;return}mkstData=data;msg.textContent=`Updated ${new Date().toLocaleTimeString([],{hour:"numeric",minute:"2-digit"})}`;if(!campaign){mkstCampaignOptions=(data.campaigns||[]).map(c=>({id:c.id,name:c.name,client:c.client_name}));const sel=$("mkstCampaign");sel.innerHTML='<option value="">All campaigns</option>'+mkstCampaignOptions.map(c=>`<option value="${escapeHtml(c.id)}">${escapeHtml(c.name)}${client?"":" — "+escapeHtml(c.client||"")}</option>`).join("")}mkstRenderFunnel();mkstRenderKpis();mkstRenderTrend();mkstRenderTop()}function mkstRenderFunnel(){const t=mkstData&&mkstData.totals||{};const groups=mkstData&&mkstData.groups||[];const box=$("mkstFunnel");if(!t.delivered&&!t.sent){box.innerHTML=`<div class="mkst-empty">No emails were delivered in this period.${mkstData&&mkstData.campaigns&&mkstData.campaigns.some(c=>c.queued)?" Some campaigns are still waiting to send — see Sending today.":""}</div>`;return}const top=groups.slice(0,6);const rest=groups.slice(6);if(rest.length)top.push({name:"Other",delivered:rest.reduce((s,g)=>s+g.delivered,0),opened:rest.reduce((s,g)=>s+g.opened,0),clicked:rest.reduce((s,g)=>s+g.clicked,0)});const max=Math.max(1,t.delivered||0);const row=(label,key,total,pct)=>`
    <div class="mkst-funnel-l">${label}</div>
    <div class="mkst-funnel-track" title="${label}: ${mkstN(total)}">
      ${top.map((g,i)=>g[key]?`<div class="mkst-funnel-seg" style="width:${(100*g[key]/max).toFixed(3)}%; background:${MKST_COLORS[i%MKST_COLORS.length]};" title="${escapeHtml(g.name||"")}: ${mkstN(g[key])} ${label.toLowerCase()}"></div>`:"").join("")}
    </div>
    <div class="mkst-funnel-pct" title="${label} as a share of delivered">${pct}</div>`;const ticks=[0,.25,.5,.75,1].map(f=>mkstN(Math.round(max*f)));box.innerHTML=`
    <div class="mkst-funnel" style="margin-bottom:4px;"><div></div><div></div><div style="font-size:11px; color:var(--text-2); text-align:center;">Cumulative</div></div>
    <div class="mkst-funnel">
      ${row("Delivered","delivered",t.delivered,"100%")}
      ${row("Opened","opened",t.opened,mkstPct(t.opened,t.delivered,0))}
      ${row("Clicked","clicked",t.clicked,mkstPct(t.clicked,t.delivered,0))}
    </div>
    <div class="mkst-axis">${ticks.map(x=>`<span>${x}</span>`).join("")}</div>
    <div class="mkst-legend">${top.map((g,i)=>`<span><i style="background:${MKST_COLORS[i%MKST_COLORS.length]}"></i>${escapeHtml(g.name||"Unknown")}</span>`).join("")}</div>`}function mkstRenderKpis(){const t=mkstData&&mkstData.totals||{};const card=(label,value,sub,tip,color)=>`<div class="mkst-kpi">
      <div class="mkst-kpi-l">${label} <span class="mkst-info" title="${escapeHtml(tip)}">i</span></div>
      <div class="mkst-kpi-v"${color?` style="color:${color}"`:""}>${mkstN(value)}</div>
      <div class="mkst-kpi-s">${sub}</div></div>`;$("mkstKpis").innerHTML=[card("Emails sent",t.sent,"handed to the mail provider","Accepted for sending by our email provider in this period."),card("Email delivered",t.delivered,`${mkstPct(t.delivered,t.sent)} of sent`,"The receiving mail server confirmed it arrived."),card("Not sent",t.not_sent,"failed, skipped or unconfirmed","Failed at the provider, skipped (the address was suppressed or unsubscribed), or never confirmed. Open a campaign to see why for each one.",t.not_sent?"var(--danger)":""),card("Bounced",t.bounced,`${mkstPct(t.bounced,t.sent)} of sent`,"The address rejected the email. It is suppressed for that client automatically."),card("Unsubscribed",t.unsubscribed,`${mkstPct(t.unsubscribed,t.delivered)} of delivered`,"Clicked unsubscribe. They will not be emailed again by that client."),card("Spam complaints",t.complained,`${mkstPct(t.complained,t.delivered,2)} of delivered`,"Marked as spam. Suppressed on every client. Keep this under 0.1%.",t.complained?"var(--danger)":"")].join("")}const MKST_METRICS={open_rate:{label:"Open rate",rate:true,num:"opened",den:"delivered",totalLabel:["Total opened","Total delivered"]},click_rate:{label:"Click rate",rate:true,num:"clicked",den:"delivered",totalLabel:["Total clicked","Total delivered"]},sent:{label:"Sent",key:"sent"},delivered:{label:"Delivered",key:"delivered"},not_sent:{label:"Not sent",key:"not_sent"},bounced:{label:"Bounced",key:"bounced"},unsubscribed:{label:"Unsubscribed",key:"unsubscribed"}};function mkstRenderTrend(){const m=MKST_METRICS[$("mkstMetric").value]||MKST_METRICS.open_rate;const t=mkstData&&mkstData.totals||{};const days=mkstData&&mkstData.daily||[];const scope=$("mkstCampaign").value?"this campaign":$("mkstClient").value?"this client":"all clients";$("mkstTrendTitle").textContent=`${m.label} (for ${scope})`;$("mkstTrendBox").innerHTML=m.rate?`<div><div class="mkst-kpi-l">${m.label}</div><div class="v">${mkstPct(t[m.num],t[m.den],2)}</div></div>
       <div><div class="r"><span>${m.totalLabel[0]}:</span><b>${mkstN(t[m.num])}</b></div><div class="r"><span>${m.totalLabel[1]}:</span><b>${mkstN(t[m.den])}</b></div></div>`:`<div><div class="mkst-kpi-l">${m.label}</div><div class="v">${mkstN(t[m.key])}</div></div>
       <div><div class="r"><span>Days:</span><b>${days.length}</b></div><div class="r"><span>Busiest day:</span><b>${mkstN(Math.max(0,...days.map(d=>d[m.key]||0)))}</b></div></div>`;const vals=days.map(d=>m.rate?mkstPctNum(d[m.num]||0,d[m.den]||0):d[m.key]||0);$("mkstTrendChart").innerHTML=mkstLineSvg(days.map(d=>d.day),vals,m.rate)}function mkstLineSvg(labels,values,isRate){const W=720,H=240,L=48,R=26,T=12,B=34;if(!labels.length)return'<div class="mkst-empty">No data.</div>';const maxV=Math.max(...values,0);const want=Math.max(isRate?4:4,maxV*1.08)/4;const mag=Math.pow(10,Math.floor(Math.log10(want)));const step=[1,2,2.5,5,10].map(f=>f*mag).find(s=>s>=want)||10*mag;const top=step*4;const x=i=>L+(labels.length===1?(W-L-R)/2:i*(W-L-R)/(labels.length-1));const y=v=>T+(H-T-B)*(1-v/top);const pts=values.map((v,i)=>[x(i),y(v)]);let d=`M${pts[0][0]},${pts[0][1]}`;for(let i=0;i<pts.length-1;i++){const p0=pts[i-1]||pts[i],p1=pts[i],p2=pts[i+1],p3=pts[i+2]||p2;const c1=[p1[0]+(p2[0]-p0[0])/6,p1[1]+(p2[1]-p0[1])/6];const c2=[p2[0]-(p3[0]-p1[0])/6,p2[1]-(p3[1]-p1[1])/6];c1[1]=Math.min(c1[1],H-B);c2[1]=Math.min(c2[1],H-B);d+=` C${c1[0]},${c1[1]} ${c2[0]},${c2[1]} ${p2[0]},${p2[1]}`}const area=`${d} L${pts[pts.length-1][0]},${H-B} L${pts[0][0]},${H-B} Z`;const grid=[0,.25,.5,.75,1].map(f=>{const v=top*f,yy=y(v);return`<line x1="${L}" x2="${W-R}" y1="${yy}" y2="${yy}" stroke="currentColor" stroke-opacity=".12"/>
      <text x="${L-8}" y="${yy+4}" text-anchor="end" font-size="11" fill="currentColor" fill-opacity=".6">${isRate?(Number.isInteger(v)?v:v.toFixed(1))+"%":Math.round(v).toLocaleString()}</text>`}).join("");const every=Math.max(1,Math.ceil(labels.length/8));const xl=labels.map((lab,i)=>i%every===0||i===labels.length-1?`<text x="${x(i)}" y="${H-12}" text-anchor="middle" font-size="11" fill="currentColor" fill-opacity=".6">${lab.slice(5,7)}/${lab.slice(8,10)}</text>`:"").join("");const dots=pts.map((p,i)=>`<circle cx="${p[0]}" cy="${p[1]}" r="${labels.length>45?0:3}" fill="#4f7df3"><title>${labels[i]}: ${isRate?values[i].toFixed(1)+"%":values[i].toLocaleString()}</title></circle>`).join("");return`<svg viewBox="0 0 ${W} ${H}" role="img" aria-label="Trend" style="color:var(--text);">
    <defs><linearGradient id="mkstFill" x1="0" x2="0" y1="0" y2="1"><stop offset="0" stop-color="#4f7df3" stop-opacity=".35"/><stop offset="1" stop-color="#4f7df3" stop-opacity=".03"/></linearGradient></defs>
    ${grid}
    <path d="${area}" fill="url(#mkstFill)"/>
    <path d="${d}" fill="none" stroke="#4f7df3" stroke-width="2.2"/>
    ${dots}${xl}</svg>`}const MKST_CAMP_STATUS={draft:["Draft","var(--text-2)"],scheduled:["Scheduled","var(--mkst-blue)"],sending:["Sending","var(--mkst-teal)"],paused:["Paused","var(--away)"],sent:["Sent","var(--good)"],cancelled:["Cancelled","var(--text-2)"]};function mkstRenderTop(){const rows=(mkstData&&mkstData.campaigns||[]).slice();const sort=$("mkstTopSort").value;const nums=$("mkstNumbers").checked;const score=c=>sort==="open_rate"?mkstPctNum(c.opened,c.delivered):sort==="click_rate"?mkstPctNum(c.clicked,c.delivered):c[sort]||0;rows.sort((a,b)=>score(b)-score(a)||b.delivered-a.delivered);const allClients=!$("mkstClient").value;const tbl=$("mkstTop");if(!rows.length){tbl.innerHTML='<tr><td class="mkst-empty">No campaigns in this period.</td></tr>';return}tbl.innerHTML=`<thead><tr><th>Title</th>${allClients?"<th>Client</th>":""}<th>Execution date</th><th>Status</th><th>Recipients</th><th>Sent</th><th>Delivered</th><th>Not sent</th><th>${nums?"Opened":"Open rate"}</th><th>${nums?"Clicked":"Click rate"}</th></tr></thead>
    <tbody>${rows.map(c=>{const[label,color]=MKST_CAMP_STATUS[c.status]||[c.status,"var(--text-2)"];return`<tr>
        <td><button class="mkst-link" data-mkst-log="${escapeHtml(c.id)}" data-name="${escapeHtml(c.name)}">${escapeHtml(c.name)}</button>
          <div style="font-size:11.5px; color:var(--text-2);">${escapeHtml(c.subject||"")}</div></td>
        ${allClients?`<td>${escapeHtml(c.client_name||"—")}</td>`:""}
        <td style="white-space:nowrap;">${mkstDate(c.started_at,true)}</td>
        <td><span class="mkst-chip" style="color:${color};">${label}</span>${c.queued?`<div style="font-size:11px; color:var(--text-2); margin-top:3px;">${mkstN(c.queued)} waiting</div>`:""}</td>
        <td>${mkstN(c.recipients)}</td>
        <td>${mkstN(c.sent)}</td>
        <td>${mkstN(c.delivered)}</td>
        <td${c.not_sent?' style="color:var(--danger); font-weight:700;"':""}>${mkstN(c.not_sent)}</td>
        <td>${nums?mkstN(c.opened):mkstPct(c.opened,c.delivered,2)}</td>
        <td>${nums?mkstN(c.clicked):mkstPct(c.clicked,c.delivered,2)}</td>
      </tr>`}).join("")}</tbody>`;tbl.querySelectorAll("[data-mkst-log]").forEach(b=>b.addEventListener("click",()=>mkstOpenLog(b.dataset.mkstLog,b.dataset.name)))}function mkstWarmup(d){if(!d.usable)return{label:"Domain not verified",color:"var(--text-2)",pct:0,note:d.domain_status||""};if(d.warmup_paused)return{label:"Warm-up paused",color:"var(--danger)",pct:Math.min(100,(d.warmup_day||0)*10),note:"Nothing sends until it is resumed."};if(d.warmup_completed_at)return{label:"Warm",color:"var(--good)",pct:100,note:`Finished ${mkstDate(d.warmup_completed_at)}`};if(!d.warmup_started_at)return{label:"Not started",color:"var(--mkst-blue)",pct:0,note:"Starts with the first send — 50 on day 1."};const day=Math.min(10,Math.max(1,d.warmup_day||1));return{label:`Warming up — day ${day} of 10`,color:"var(--away)",pct:day*10,note:`Started ${mkstDate(d.warmup_started_at)}`}}async function mkstLoadToday(){clearTimeout(mkstTodayTimer);const{data,error}=await sb.rpc("marketing_sending_today");if(error){$("mkstTodayTable").innerHTML=`<tr><td class="mkst-empty" style="color:var(--danger);">${escapeHtml(error.message)}</td></tr>`;return}mkstToday=data;const rows=data.domains||[];const sum=k=>rows.reduce((s,r)=>s+(Number(r[k])||0),0);const sentToday=sum("sent_today"),allowed=sum("allowance_today"),left=sum("remaining_today");const warming=rows.filter(r=>r.usable&&r.warmup_started_at&&!r.warmup_completed_at&&!r.warmup_paused).length;$("mkstTodayNote").textContent=`Today is ${mkstDate(data.today+"T12:00:00Z")} (Chicago)${data.working_day?"":" — a weekend: no emails are sent on Saturdays or Sundays."}. Refreshes every minute while open.`;const card=(label,value,sub,color)=>`<div class="mkst-kpi"><div class="mkst-kpi-l">${label}</div><div class="mkst-kpi-v"${color?` style="color:${color}"`:""}>${value}</div><div class="mkst-kpi-s">${sub}</div></div>`;$("mkstTodayKpis").innerHTML=[card("Sent today — all clients",mkstN(sentToday),allowed?`${mkstPct(sentToday,allowed,0)} of today's limit`:"no sending limit today"),card("Today's limit",mkstN(allowed),"what warm-up and allowances permit"),card("Still available today",mkstN(left),"across every client"),card("Domains warming up",mkstN(warming),`${mkstN(rows.length)} sending domain${rows.length===1?"":"s"} in total`),card("Campaigns sending now",mkstN(sum("sending_campaigns")),sum("waiting_for_warmup")?`${mkstN(sum("waiting_for_warmup"))} waiting for tomorrow's warm-up limit`:"none waiting on warm-up",sum("waiting_for_warmup")?"var(--away)":"")].join("");const spark=days=>{const max=Math.max(1,...days.map(x=>x.sent));return`<svg viewBox="0 0 84 24" width="84" height="24" aria-hidden="true">${days.map((x,i)=>{const h=Math.max(1,22*x.sent/max);return`<rect x="${i*12}" y="${24-h}" width="9" height="${h}" rx="1.5" fill="#4f7df3" fill-opacity="${i===days.length-1?1:.45}"><title>${x.day}: ${x.sent}</title></rect>`}).join("")}</svg>`};$("mkstTodayTable").innerHTML=!rows.length?'<tr><td class="mkst-empty">No client has connected a sending domain yet.</td></tr>':`
    <thead><tr><th>Client</th><th>Sending domain</th><th>Warm-up</th><th>Sent today</th><th>Left today</th><th>This month</th><th>Last 7 days</th><th>Campaigns</th></tr></thead>
    <tbody>${rows.map(r=>{const w=mkstWarmup(r);const todayPct=r.allowance_today?Math.min(100,100*r.sent_today/r.allowance_today):0;const monthPct=r.monthly_limit?Math.min(100,100*r.sent_this_month/r.monthly_limit):0;return`<tr>
        <td><b>${escapeHtml(r.client_name||"—")}</b></td>
        <td>${escapeHtml(r.domain)}</td>
        <td style="min-width:170px;"><span class="mkst-chip" style="color:${w.color};">${escapeHtml(w.label)}</span>
          <div class="mkst-bar"><span style="width:${w.pct}%; background:${w.color};"></span></div>
          <div style="font-size:11px; color:var(--text-2); margin-top:3px;">${escapeHtml(w.note)}</div></td>
        <td style="min-width:130px;"><b>${mkstN(r.sent_today)}</b> <span style="color:var(--text-2);">of ${mkstN(r.allowance_today)}</span>
          <div class="mkst-bar"><span style="width:${todayPct}%; background:#4f7df3;"></span></div></td>
        <td>${mkstN(r.remaining_today)}</td>
        <td style="min-width:130px;">${mkstN(r.sent_this_month)} <span style="color:var(--text-2);">of ${mkstN(r.monthly_limit)}</span>
          <div class="mkst-bar"><span style="width:${monthPct}%; background:var(--good);"></span></div></td>
        <td>${spark(r.last_7_days||[])}</td>
        <td>${r.sending_campaigns?`<span class="mkst-chip" style="color:var(--mkst-teal);">${r.sending_campaigns} sending</span> `:""}${r.waiting_for_warmup?`<span class="mkst-chip" style="color:var(--away);">${r.waiting_for_warmup} waiting for warm-up</span>`:""}${!r.sending_campaigns&&!r.waiting_for_warmup?'<span style="color:var(--text-2);">—</span>':""}</td>
      </tr>`}).join("")}</tbody>`;mkstTodayTimer=setTimeout(()=>{if(mkstVisible()&&mkstPane==="today")mkstLoadToday()},6e4)}function mkstDelivery(r){if(r.bounced_at)return["Bounced","var(--danger)","The address rejected it."];if(r.status==="failed")return["Not sent — failed","var(--danger)",r.error||"The provider refused it."];if(r.status==="skipped")return["Not sent — skipped","var(--text-2)",r.error||"Suppressed or unsubscribed before sending."];if(r.status==="unknown")return["Not confirmed","var(--away)",r.error||"Sent, but the provider never confirmed it — not retried, to avoid a duplicate."];if(r.status==="queued"||r.status==="sending")return["Waiting to send","var(--mkst-blue)","In the queue."];if(r.status==="sandbox")return["Test only","var(--text-2)","Dry run — nothing delivered."];if(r.delivered_at)return["Delivered","var(--good)",""];if(r.status==="deferred")return["Sent — delivery delayed","var(--away)","The receiving server asked to try again later."];if(r.sent_at)return["Sent — awaiting delivery report","var(--mkst-teal)",""];return[r.status,"var(--text-2)",""]}async function mkstOpenLog(campaignId,name){const st={filter:"all",search:"",offset:0};const overlay=document.createElement("div");overlay.style.cssText="position:fixed; inset:0; background:rgba(0,0,0,.55); z-index:9999; display:flex; align-items:center; justify-content:center; padding:16px;";overlay.innerHTML=`<div class="card" style="width:min(1100px,100%); max-height:92vh; display:flex; flex-direction:column; gap:10px;">
      <div style="display:flex; gap:10px; align-items:center;">
        <div style="flex:1; min-width:0;"><div class="mkst-h" style="overflow:hidden; text-overflow:ellipsis; white-space:nowrap;">Delivery log — ${escapeHtml(name||"Campaign")}</div>
          <div class="mkst-sub" style="margin:2px 0 0;">Every recipient and what happened to their email.</div></div>
        <button class="btn-secondary" data-close style="width:auto;">Close</button></div>
      <div style="display:flex; gap:10px; flex-wrap:wrap; align-items:center;">
        <div class="mkst-log-filters" data-filters></div>
        <input type="search" data-search placeholder="Search email or name" style="width:220px; margin:0 0 0 auto;">
      </div>
      <div style="overflow:auto; border:1px solid var(--border); border-radius:8px;"><table class="mkst-table" data-table><tr><td class="mkst-empty">Loading…</td></tr></table></div>
      <div style="display:flex; gap:10px; align-items:center; font-size:12px; color:var(--text-2);" data-pager></div>
    </div>`;document.body.appendChild(overlay);const q=s=>overlay.querySelector(s);const close=()=>overlay.remove();q("[data-close]").onclick=close;overlay.addEventListener("click",e=>{if(e.target===overlay)close()});let searchTimer=null;q("[data-search]").addEventListener("input",e=>{clearTimeout(searchTimer);searchTimer=setTimeout(()=>{st.search=e.target.value;st.offset=0;load()},300)});const LIMIT=100;const FILTERS=[["all","All"],["sent","Sent"],["delivered","Delivered"],["opened","Opened"],["clicked","Clicked"],["not_sent","Not sent"],["bounced","Bounced"],["waiting","Waiting"]];async function load(){const{data,error}=await sb.rpc("marketing_recipient_log",{p_campaign:campaignId,p_filter:st.filter,p_search:st.search||null,p_limit:LIMIT,p_offset:st.offset});if(!document.body.contains(overlay))return;if(error){q("[data-table]").innerHTML=`<tr><td class="mkst-empty" style="color:var(--danger);">${escapeHtml(error.message)}</td></tr>`;return}const counts=data.counts||{};q("[data-filters]").innerHTML=FILTERS.map(([k,l])=>`<button data-f="${k}" class="${st.filter===k?"active":""}">${l} · ${mkstN(counts[k])}</button>`).join("");q("[data-filters]").querySelectorAll("button").forEach(b=>b.onclick=()=>{st.filter=b.dataset.f;st.offset=0;load()});const rows=data.rows||[];q("[data-table]").innerHTML=!rows.length?'<tr><td class="mkst-empty">Nobody here.</td></tr>':`
      <thead><tr><th>Recipient</th><th>Delivery</th><th>Sent</th><th>Delivered</th><th>Opened</th><th>Clicked</th><th>Unsubscribed</th></tr></thead>
      <tbody>${rows.map(r=>{const[label,color,why]=mkstDelivery(r);const nm=[r.first_name,r.last_name].filter(Boolean).join(" ");return`<tr>
          <td><b>${escapeHtml(r.email)}</b>${nm||r.company?`<div style="font-size:11.5px; color:var(--text-2);">${escapeHtml([nm,r.company].filter(Boolean).join(" · "))}</div>`:""}</td>
          <td><span class="mkst-chip" style="color:${color};">${escapeHtml(label)}</span>${why?`<div style="font-size:11px; color:var(--text-2); margin-top:3px; max-width:260px;">${escapeHtml(why)}</div>`:""}</td>
          <td style="white-space:nowrap;">${mkstDate(r.sent_at,true)}</td>
          <td style="white-space:nowrap;">${mkstDate(r.delivered_at,true)}</td>
          <td style="white-space:nowrap;">${mkstDate(r.opened_at,true)}</td>
          <td style="white-space:nowrap;">${mkstDate(r.clicked_at,true)}</td>
          <td style="white-space:nowrap;">${mkstDate(r.unsubscribed_at,true)}</td>
        </tr>`}).join("")}</tbody>`;const from=data.total?st.offset+1:0,to=Math.min(st.offset+LIMIT,data.total);q("[data-pager]").innerHTML=`<span>${mkstN(from)}–${mkstN(to)} of ${mkstN(data.total)}</span>
      <button class="btn-secondary" data-prev style="width:auto; padding:4px 10px;"${st.offset?"":" disabled"}>Previous</button>
      <button class="btn-secondary" data-next style="width:auto; padding:4px 10px;"${to<data.total?"":" disabled"}>Next</button>
      <button class="btn-secondary" data-reload style="width:auto; padding:4px 10px; margin-left:auto;">Refresh</button>`;q("[data-prev]").onclick=()=>{st.offset=Math.max(0,st.offset-LIMIT);load()};q("[data-next]").onclick=()=>{st.offset+=LIMIT;load()};q("[data-reload]").onclick=load}load()}async function initMarketingTab(){const sel=document.getElementById("mktClientSel");if(!sel)return;if(!mktClients.length){const{data}=await sb.from("client_accounts").select("id, email, company_name").order("email");mktClients=data||[]}if(!sel.dataset.filled){sel.dataset.filled="1";sel.innerHTML='<option value="">— pick a client —</option>'+mktClients.map(c=>`<option value="${escapeHtml(c.id)}">${escapeHtml(c.company_name||c.email)}</option>`).join("");sel.addEventListener("change",mktLoad)}if(sel.value)mktLoad()}async function mktLoad(){const clientId=document.getElementById("mktClientSel").value;const body=document.getElementById("mktBody");if(!clientId){body.innerHTML="";return}body.innerHTML='<div class="card">Loading…</div>';try{const[d,a,acc,con]=await Promise.all([mktCall("list",{client_id:clientId}),mktCall("audience_list",{client_id:clientId}),sb.rpc("marketing_access",{p_client:clientId}),sb.rpc("marketing_template_content",{p_client:clientId})]);if(acc.error)throw new Error(acc.error.message);mktDomains=d.domains||[];mktAudiences=a.audiences||[];mktAccess=acc.data||null;mktContent=con.error?null:con.data||null;mktRender(clientId)}catch(e){body.innerHTML=`<div class="card" style="color:var(--danger)">${escapeHtml(e.message)}</div>`}}function mktContentCard(clientId){const c=mktContent;const canGrant=["owner","admin"].includes(myRole);const head=`<h3 class="mono" style="font-size:11px; color:var(--text-2); margin-bottom:8px; text-transform:uppercase; letter-spacing:1px;">Email content</h3>`;if(!c||!c.exists){return`<div class="card" style="margin-bottom:16px;">${head}<div style="font-size:13px; color:var(--text-2);">This client has not written their email yet.</div></div>`}let state;if(c.staff_blocked){state=`<span style="color:var(--danger); font-weight:700;">On hold by staff</span>`}else if(c.ai_draft){state=`<span style="color:var(--away); font-weight:700;">AI draft &mdash; the client has not approved it</span>`}else if(c.approved_at){state=`<span style="color:var(--good); font-weight:700;">Approved by the client</span> <span style="color:var(--text-2);">${new Date(c.approved_at).toLocaleString()}</span>`}else{state=`<span style="color:var(--text-2);">Written, not approved</span>`}const model=c.ai_model?` <span style="color:var(--text-2);">&middot; drafted by ${escapeHtml(c.ai_model)}</span>`:"";const brief=c.instructions?`<div style="font-size:12px; color:var(--text-2); margin-bottom:8px;"><b>Client asked for:</b> ${escapeHtml(c.instructions)}</div>`:"";const note=c.staff_note?`<div style="font-size:12px; color:var(--danger); margin-top:8px;"><b>Hold note:</b> ${escapeHtml(c.staff_note)}</div>`:"";const controls=canGrant?`<div class="filter-row" style="margin-top:12px;">
      <div style="flex:1; min-width:200px;"><label>Reason (the client sees this)</label>
        <input type="text" id="mktBlockNote" maxlength="500" value="${escapeHtml(c.staff_note||"")}"></div>
      <div style="align-self:flex-end;"><button class="btn-secondary" id="mktBlockBtn">${c.staff_blocked?"Release the hold":"Put this on hold"}</button></div>
      <div style="align-self:flex-end; font-size:12px; color:var(--text-2);" id="mktBlockMsg">A hold stops it being used, whatever the client approved.</div>
    </div>`:"";return`<div class="card" style="margin-bottom:16px;">${head}
    <div style="font-size:13px; margin-bottom:8px;">${state}${model}</div>
    ${brief}
    <div style="border:1px solid var(--border); border-radius:8px; padding:11px 13px; background:var(--inset); font-size:13px; line-height:1.55;">
      <div style="font-weight:700; margin-bottom:6px;">${escapeHtml(c.subject||"(no subject)")}</div>
      <div style="white-space:pre-wrap; color:var(--text-2); max-height:200px; overflow:auto;">${escapeHtml(c.body||"")}</div>
    </div>
    ${note}${controls}
  </div>`}async function mktToggleBlock(clientId){const msg=document.getElementById("mktBlockMsg");const btn=document.getElementById("mktBlockBtn");const on=!(mktContent&&mktContent.staff_blocked);const note=(document.getElementById("mktBlockNote")||{}).value||"";btn.disabled=true;msg.style.color="var(--text-2)";msg.textContent="Saving…";try{await mktCall("content_block",{client_id:clientId,blocked:on,note});await mktLoad()}catch(e){btn.disabled=false;const m=document.getElementById("mktBlockMsg");if(m){m.style.color="var(--danger)";m.textContent=e.message}}}function mktAccessCard(clientId){const a=mktAccess||{};const canGrant=["owner","admin"].includes(myRole);const tick=(ok,label)=>`<div style="display:flex; gap:8px; align-items:center; font-size:13px; margin:3px 0;">
      <span style="font-weight:700; color:${ok?"var(--good)":"#E38B2E"};">${ok?"&#10003;":"&#9675;"}</span>
      <span style="${ok?"":"color:var(--text-2);"}">${label}</span></div>`;return`
    <div class="card" style="margin-bottom:16px;">
      <h3 class="mono" style="font-size:11px; color:var(--text-2); margin-bottom:10px; text-transform:uppercase; letter-spacing:1px;">Email Marketing access</h3>
      <div style="display:flex; gap:28px; flex-wrap:wrap;">
        <div style="min-width:220px;">
          <div style="font-size:13px; line-height:1.7;">
            <b>${Number(a.seats||0)}</b> seat${a.seats===1?"":"s"}
            <span style="color:var(--text-2);">(${Number(a.paid_seats||0)} paid Full-time + Our data, ${Number(a.extra_seats||0)} added by hand)</span><br>
            <b class="mono">${Number(a.monthly_limit||0).toLocaleString()}</b> emails a month &middot;
            <span class="mono">${Number(a.sent_this_month||0).toLocaleString()}</span> sent &middot;
            <span class="mono">${Number(a.remaining||0).toLocaleString()}</span> left
            ${a.has_access?"":'<br><span style="color:var(--danger);">No seat &mdash; the client cannot see Email Marketing.</span>'}
          </div>
          ${canGrant?`
          <div class="filter-row" style="margin-top:10px;">
            <div style="min-width:120px;"><label>Seats added by hand</label>
              <input type="number" id="mktExtraSeats" min="0" max="100" step="1" value="${Number(a.extra_seats||0)}"></div>
            <div style="min-width:130px;"><label>Emails per seat</label>
              <input type="number" id="mktEmailsPerSeat" min="0" max="1000000" step="1000" value="${Number(a.emails_per_seat||1e4)}"></div>
            <div style="align-self:flex-end;"><button class="btn-secondary" id="mktSeatsSaveBtn">Save seats</button></div>
            <div style="align-self:flex-end; font-size:12px; color:var(--text-2);" id="mktSeatsMsg">For test or complimentary accounts.</div>
          </div>`:""}
        </div>
        <div style="min-width:220px;">
          <div style="font-size:11px; color:var(--text-2); text-transform:uppercase; letter-spacing:1px; margin-bottom:4px;">Client setup (in their portal)</div>
          ${tick(a.domain_ready,"1. Sending domain verified")}
          ${tick(a.sender_ready,"2. Sending address added")}
          ${tick(a.template_ready,"3. Email design saved")}
        </div>
      </div>
    </div>`}async function mktSaveSeats(clientId){const msg=document.getElementById("mktSeatsMsg"),btn=document.getElementById("mktSeatsSaveBtn");const n=Number(document.getElementById("mktExtraSeats").value);if(!Number.isInteger(n)||n<0||n>100){msg.style.color="var(--danger)";msg.textContent="Use a whole number from 0 to 100.";return}const per=Number(document.getElementById("mktEmailsPerSeat").value);if(!Number.isInteger(per)||per<0||per>1e6){msg.style.color="var(--danger)";msg.textContent="Emails per seat must be a whole number from 0 to 1,000,000.";return}btn.disabled=true;msg.style.color="var(--text-2)";msg.textContent="Saving…";const{error}=await sb.rpc("marketing_set_extra_seats",{p_client:clientId,p_seats:n});if(error){msg.style.color="var(--danger)";msg.textContent=error.message;btn.disabled=false;return}const{error:perErr}=await sb.rpc("marketing_set_emails_per_seat",{p_client:clientId,p_emails:per});if(perErr){msg.style.color="var(--danger)";msg.textContent=perErr.message;btn.disabled=false;return}await mktLoad();const m=document.getElementById("mktSeatsMsg");if(m){m.style.color="var(--good)";m.textContent="Saved."}}function mktRender(clientId){const dom=mktDomains[0];const a=mktAccess||{};const ready=Boolean(a.setup_complete);const missing=[!a.has_access&&"an Email Marketing seat",!a.domain_ready&&"a verified sending domain",!a.sender_ready&&"a sending address",!a.template_ready&&"an email design"].filter(Boolean).join(", ");const domLine=!dom?"No sending domain connected. The client connects one from their own portal — staff cannot do it for them here.":dom.usable_for_sending?`<b style="color:var(--good)">${escapeHtml(dom.domain)}</b> — verified, ready to send.`:`<b>${escapeHtml(dom.domain)}</b> — <span style="color:#E38B2E">${escapeHtml(dom.status)}</span>, not ready to send yet.`;const rows=mktAudiences.length?mktAudiences.map(a2=>`<tr>
      <td style="padding:7px 10px 7px 0;"><b>${escapeHtml(a2.name)}</b>
        <div style="font-size:11px; color:var(--text-2);">${escapeHtml(a2.source||"")}</div></td>
      <td style="padding:7px 10px 7px 0;" class="mono">${Number(a2.contact_count).toLocaleString()}</td>
      <td style="padding:7px 0; text-align:right;">
        <button class="btn-secondary mkt-upload" data-id="${escapeHtml(a2.id)}" data-name="${escapeHtml(a2.name)}" style="font-size:11px; padding:3px 9px;"${ready?"":' disabled title="The client has not finished setup"'}>Upload CSV</button>
        <button class="btn-secondary mkt-del" data-id="${escapeHtml(a2.id)}" data-name="${escapeHtml(a2.name)}" style="font-size:11px; padding:3px 9px;">Delete</button>
      </td></tr>`).join(""):'<tr><td colspan="3" style="padding:8px 0; color:var(--text-2);">No lists yet.</td></tr>';document.getElementById("mktBody").innerHTML=mktAccessCard(clientId)+mktContentCard(clientId)+`
    <div class="card" style="margin-bottom:16px;">
      <h3 class="mono" style="font-size:11px; color:var(--text-2); margin-bottom:10px; text-transform:uppercase; letter-spacing:1px;">Sending domain</h3>
      <div style="font-size:13px;">${domLine}</div>
    </div>
    <div class="card" style="margin-bottom:16px;">
      <h3 class="mono" style="font-size:11px; color:var(--text-2); margin-bottom:10px; text-transform:uppercase; letter-spacing:1px;">Lists</h3>
      ${ready?"":`<div style="font-size:12.5px; color:#E38B2E; margin-bottom:10px; line-height:1.5;">
        Lists open once the client finishes setup in their portal. Still missing: ${escapeHtml(missing)}.</div>`}
      <table style="width:100%; border-collapse:collapse; font-size:13px;">
        <thead><tr style="text-align:left; color:var(--text-2); font-size:10.5px; text-transform:uppercase; letter-spacing:1px;">
          <th style="padding:0 10px 6px 0;">List</th><th style="padding:0 10px 6px 0;">Contacts</th><th></th></tr></thead>
        <tbody>${rows}</tbody>
      </table>
      <div id="mktUploadArea" style="margin-top:14px;"></div>
      <div class="filter-row" style="margin-top:16px;">
        <div style="min-width:200px;"><label>New list name</label><input type="text" id="mktNewName" placeholder="Chicago investors – Sept"></div>
        <div style="min-width:240px;"><label>Where did it come from?</label><input type="text" id="mktNewSource" placeholder="Conference sign-up, Sept 2026"></div>
        <div style="align-self:flex-end;"><button class="btn-primary" id="mktCreateBtn"${ready?"":" disabled"}>Create list</button></div>
        <div style="align-self:flex-end; font-size:12px; color:var(--text-2);" id="mktMsg"></div>
      </div>
    </div>
    <div class="card" style="margin-bottom:16px;" id="mktCampaignsCard">
      <h3 class="mono" style="font-size:11px; color:var(--text-2); margin-bottom:10px; text-transform:uppercase; letter-spacing:1px;">Campaigns</h3>
      <div id="mktCampaignsBody" style="font-size:13px; color:var(--text-2);">Loading…</div>
      <div id="mktCampEditor"></div>
    </div>`;mktCampaignsLoad(clientId);document.getElementById("mktCreateBtn").addEventListener("click",()=>mktCreate(clientId));const blockBtn=document.getElementById("mktBlockBtn");if(blockBtn)blockBtn.onclick=()=>mktToggleBlock(clientId);const seatsBtn=document.getElementById("mktSeatsSaveBtn");if(seatsBtn)seatsBtn.addEventListener("click",()=>mktSaveSeats(clientId));document.querySelectorAll(".mkt-upload").forEach(b=>b.addEventListener("click",()=>mktShowUpload(b.dataset.id,b.dataset.name)));document.querySelectorAll(".mkt-del").forEach(b=>b.addEventListener("click",()=>mktDelete(b.dataset.id,b.dataset.name)))}let mktCampaigns=[];let mktSenders=[];let mktCampTimer=null;let mktCampEditing=null;async function mktCampCall(action,payload){const{data,error}=await sb.functions.invoke("marketing-campaigns",{body:{action,...payload}});if(data&&data.ok===false)throw new Error(data.error||"Request failed.");if(error){if(error.context&&typeof error.context.json==="function"){try{const b=await error.context.json();if(b&&b.error)throw new Error(b.error)}catch(e){if(e instanceof Error&&e.message&&!/json/i.test(e.message))throw e}}throw new Error(error.message||"Request failed.")}return data}const MKT_CAMP_STATUS={draft:["Draft","var(--text-2)"],scheduled:["Scheduled","#3B82F6"],sending:["Sending","#E38B2E"],paused:["Paused","#E38B2E"],sent:["Sent","var(--good)"],cancelled:["Cancelled","var(--text-2)"]};function mktChip(text,color){return`<span class="mono" style="display:inline-block; font-size:10px; letter-spacing:.06em; text-transform:uppercase; border:1px solid ${color}; color:${color}; border-radius:999px; padding:1px 8px;">${escapeHtml(text)}</span>`}function mktWhen(iso){return iso?new Date(iso).toLocaleString([],{month:"short",day:"numeric",hour:"numeric",minute:"2-digit"}):""}function mktPct(n,of){return of?` <span style="color:var(--text-2); font-size:11px;">${Math.round(100*n/of)}%</span>`:""}async function mktCampaignsLoad(clientId){clearTimeout(mktCampTimer);const box=document.getElementById("mktCampaignsBody");if(!box)return;try{const[c,snd]=await Promise.all([mktCampCall("list",{client_id:clientId}),mktCall("senders_list",{client_id:clientId})]);if(document.getElementById("mktClientSel").value!==clientId)return;mktCampaigns=c.campaigns||[];mktSenders=snd.senders||[];mktCampaignsRender(clientId);if(mktCampaigns.some(x=>["scheduled","sending"].includes(x.status))){mktCampTimer=setTimeout(()=>{if(mkstVisible()&&mkstPane==="manage"&&document.getElementById("mktClientSel").value===clientId&&!mktCampEditing){mktCampaignsLoad(clientId)}},15e3)}}catch(e){box.innerHTML=`<span style="color:var(--danger)">${escapeHtml(e.message)}</span>`}}function mktCampaignsRender(clientId){const box=document.getElementById("mktCampaignsBody");const a=mktAccess||{};const canWrite=["owner","admin"].includes(myRole);const ready=Boolean(a.setup_complete);const rows=mktCampaigns.length?mktCampaigns.map(c=>{const st=c.stats||{};const[label,color]=MKT_CAMP_STATUS[c.status]||[c.status,"var(--text-2)"];const when=c.status==="scheduled"?"Starts "+mktWhen(c.scheduled_at):c.finished_at?mktWhen(c.finished_at):c.started_at?"Started "+mktWhen(c.started_at):"Created "+mktWhen(c.created_at);const acts=[];acts.push(`<button class="btn-secondary mkt-camp-act" data-act="preview" data-id="${c.id}">Preview</button>`);if(c.started_at&&!c.dry_run)acts.push(`<button class="btn-secondary mkt-camp-act" data-act="log" data-id="${c.id}">Delivery log</button>`);if(canWrite){if(c.status==="draft"||c.status==="scheduled"||c.status==="paused"&&!c.started_at)acts.push(`<button class="btn-secondary mkt-camp-act" data-act="edit" data-id="${c.id}">Edit</button>`);if(["scheduled","sending"].includes(c.status))acts.push(`<button class="btn-secondary mkt-camp-act" data-act="pause" data-id="${c.id}">Pause</button>`);if(c.status==="paused")acts.push(`<button class="btn-secondary mkt-camp-act" data-act="resume" data-id="${c.id}">Resume</button>`);if(["scheduled","sending","paused"].includes(c.status))acts.push(`<button class="btn-secondary mkt-camp-act" data-act="cancel" data-id="${c.id}">Cancel</button>`);if(c.status==="draft"||c.status==="cancelled"&&!c.started_at)acts.push(`<button class="btn-secondary mkt-camp-act" data-act="delete" data-id="${c.id}">Delete</button>`)}const sent=st.sent||0;return`<tr style="border-top:1px solid var(--border);">
      <td style="padding:8px 10px 8px 0; vertical-align:top;"><b>${escapeHtml(c.name)}</b>
        <div style="font-size:11.5px; color:var(--text-2);">${escapeHtml(c.subject||"")}</div>
        ${c.last_error?`<div style="font-size:11.5px; color:${c.status==="paused"?"#E38B2E":"var(--danger)"}; margin-top:3px;">${escapeHtml(c.last_error)}</div>`:""}</td>
      <td style="padding:8px 10px 8px 0; vertical-align:top; white-space:nowrap;">${mktChip(label,color)}${c.dry_run?" "+mktChip("dry run","var(--text-2)"):""}
        <div style="font-size:11px; color:var(--text-2); margin-top:3px;">${escapeHtml(when)}</div></td>
      <td class="mono" style="padding:8px 10px 8px 0; vertical-align:top;">${Number(st.total||c.recipient_count||0).toLocaleString()}</td>
      <td class="mono" style="padding:8px 10px 8px 0; vertical-align:top;">${sent.toLocaleString()}${st.queued?`<div style="font-size:11px; color:var(--text-2);">${st.queued.toLocaleString()} to go</div>`:""}</td>
      <td class="mono" style="padding:8px 10px 8px 0; vertical-align:top;">${(st.delivered||0).toLocaleString()}${mktPct(st.delivered||0,sent)}</td>
      <td class="mono" style="padding:8px 10px 8px 0; vertical-align:top;">${(st.opened||0).toLocaleString()}${mktPct(st.opened||0,sent)}</td>
      <td class="mono" style="padding:8px 10px 8px 0; vertical-align:top;">${(st.clicked||0).toLocaleString()}${mktPct(st.clicked||0,sent)}</td>
      <td class="mono" style="padding:8px 10px 8px 0; vertical-align:top;">${(st.unsubscribed||0).toLocaleString()}</td>
      <td class="mono" style="padding:8px 10px 8px 0; vertical-align:top;">${(st.bounced||0).toLocaleString()}${st.complained?`<div style="font-size:11px; color:var(--danger);">${st.complained} spam</div>`:""}</td>
      <td style="padding:8px 0; vertical-align:top; text-align:right; white-space:nowrap;">${acts.join(" ")}</td>
    </tr>`}).join(""):'<tr><td colspan="10" style="padding:8px 0; color:var(--text-2);">No campaigns yet.</td></tr>';box.innerHTML=`
    ${ready?"":'<div style="font-size:12.5px; color:#E38B2E; margin-bottom:10px;">Campaigns open once the client finishes setup in their portal.</div>'}
    <div style="overflow-x:auto;">
      <table style="width:100%; border-collapse:collapse; font-size:13px; min-width:860px;">
        <thead><tr style="text-align:left; color:var(--text-2); font-size:10.5px; text-transform:uppercase; letter-spacing:1px;">
          <th style="padding:0 10px 6px 0;">Campaign</th><th style="padding:0 10px 6px 0;">Status</th><th style="padding:0 10px 6px 0;">Recipients</th>
          <th style="padding:0 10px 6px 0;">Sent</th><th style="padding:0 10px 6px 0;">Delivered</th><th style="padding:0 10px 6px 0;">Opened</th>
          <th style="padding:0 10px 6px 0;">Clicked</th><th style="padding:0 10px 6px 0;">Unsub</th><th style="padding:0 10px 6px 0;">Bounced</th><th></th></tr></thead>
        <tbody>${rows}</tbody>
      </table>
    </div>
    <div style="margin-top:12px; display:flex; gap:10px; align-items:center; flex-wrap:wrap;">
      ${canWrite?`<button class="btn-primary" id="mktCampNewBtn"${ready?"":" disabled"}>New campaign</button>`:""}
      <span id="mktCampMsg" style="font-size:12px; color:var(--text-2);"></span>
    </div>`;const nb=document.getElementById("mktCampNewBtn");if(nb)nb.addEventListener("click",()=>mktCampOpenEditor(clientId,null));box.querySelectorAll(".mkt-camp-act").forEach(b=>b.addEventListener("click",()=>mktCampAction(clientId,b.dataset.act,b.dataset.id)))}async function mktCampAction(clientId,act,id){const c=mktCampaigns.find(x=>x.id===id);const msg=document.getElementById("mktCampMsg");if(act==="edit")return mktCampOpenEditor(clientId,c);if(act==="preview")return mktCampPreview({id});if(act==="log")return mkstOpenLog(id,c&&c.name);if(act==="cancel"&&!window.confirm(`Cancel “${c.name}”? Anyone not emailed yet will not be. This cannot be undone.`))return;if(act==="delete"&&!window.confirm(`Delete the draft “${c.name}”?`))return;try{msg.style.color="var(--text-2)";msg.textContent="Working…";await mktCampCall(act,{id,confirm:act==="delete"?true:void 0});msg.textContent="";await mktCampaignsLoad(clientId)}catch(e){msg.style.color="var(--danger)";msg.textContent=e.message}}async function mktCampPreview(payload){const overlay=document.createElement("div");overlay.style.cssText="position:fixed; inset:0; background:rgba(0,0,0,.55); z-index:9999; display:flex; align-items:center; justify-content:center; padding:16px;";overlay.innerHTML=`<div class="card" style="width:min(760px,100%); max-height:92vh; display:flex; flex-direction:column; gap:8px;">
      <div style="display:flex; gap:10px; align-items:center;"><b id="mktPrevSubject" style="flex:1; min-width:0; overflow:hidden; text-overflow:ellipsis; white-space:nowrap;">Loading…</b>
        <button class="btn-secondary" id="mktPrevClose">Close</button></div>
      <div id="mktPrevMeta" style="font-size:12px; color:var(--text-2);"></div>
      <iframe id="mktPrevFrame" sandbox="" style="width:100%; height:70vh; border:1px solid var(--border); border-radius:8px; background:#fff;"></iframe>
    </div>`;document.body.appendChild(overlay);const close=()=>overlay.remove();overlay.addEventListener("click",e=>{if(e.target===overlay)close()});overlay.querySelector("#mktPrevClose").addEventListener("click",close);try{const r=await mktCampCall("preview",payload);overlay.querySelector("#mktPrevSubject").textContent=r.subject||"(no subject)";overlay.querySelector("#mktPrevMeta").textContent=`As ${r.sample_email||"a sample contact"} will see it.`+(r.has_design?"":" The client has not saved an email design yet.");overlay.querySelector("#mktPrevFrame").srcdoc=r.html}catch(e){overlay.querySelector("#mktPrevSubject").textContent=e.message}}function mktCampOpenEditor(clientId,c){clearTimeout(mktCampTimer);mktCampEditing=c||{};const e=mktCampEditing;const box=document.getElementById("mktCampEditor");const senderOpts=mktSenders.map(s=>`<option value="${escapeHtml(s.id)}"${(e.sender_id?e.sender_id===s.id:s.is_default)?" selected":""}>${escapeHtml(s.from_name)} &lt;${escapeHtml(s.from_local)}@${escapeHtml((mktDomains.find(d=>d.id===s.domain_id)||mktDomains[0]||{}).domain||"")}&gt;</option>`).join("");const chosen=new Set(e.audience_ids||[]);const lists=mktAudiences.length?mktAudiences.map(a=>`<label style="display:flex; gap:8px; align-items:center; font-size:13px; margin:4px 0; text-transform:none; letter-spacing:0;">
      <input type="checkbox" class="mkt-camp-aud" value="${escapeHtml(a.id)}"${chosen.has(a.id)?" checked":""} style="width:auto;">
      ${escapeHtml(a.name)} <span class="mono" style="color:var(--text-2);">${Number(a.contact_count).toLocaleString()}</span></label>`).join(""):'<div style="color:var(--text-2); font-size:12.5px;">No lists yet — upload one above first.</div>';const isOwner=["owner","admin"].includes(myRole);box.innerHTML=`
    <div style="margin-top:18px; padding:16px; border:1px solid var(--border); border-radius:10px;">
      <div style="font-size:14px; font-weight:700; margin-bottom:12px;">${e.id?"Edit campaign":"New campaign"}</div>
      <div class="filter-row">
        <div style="min-width:220px; flex:1;"><label>Campaign name (internal)</label><input type="text" id="mktCampName" maxlength="150" value="${escapeHtml(e.name||"")}" placeholder="September cash-offer blast"></div>
        <div style="min-width:260px; flex:1;"><label>Send from</label><select id="mktCampSender">${senderOpts||'<option value="">No sending address</option>'}</select></div>
      </div>
      <div style="margin-top:10px;"><label>Send to</label>${lists}
        <div id="mktCampEstimate" style="font-size:12px; color:var(--text-2); margin-top:4px;"></div></div>
      <div class="filter-row" style="margin-top:10px;">
        <div style="min-width:260px; flex:2;"><label>Subject</label><input type="text" id="mktCampSubject" maxlength="250" value="${escapeHtml(e.subject||"")}" placeholder="{{first_name|Hi}}, a cash offer for your property"></div>
        <div style="min-width:220px; flex:1;"><label>Preview text (optional)</label><input type="text" id="mktCampPre" maxlength="250" value="${escapeHtml(e.preheader||"")}" placeholder="Shown after the subject in most inboxes"></div>
      </div>
      <div style="margin-top:10px;"><label>Message</label>
        <textarea id="mktCampBody" rows="10" style="width:100%; font-family:inherit;" placeholder="We are local buyers and can make you a fair cash offer…">${escapeHtml(e.body||"")}</textarea>
        <div style="font-size:11.5px; color:var(--text-2); margin-top:4px; line-height:1.5;">
          The client's greeting, logo, sign-off and footer are added around this automatically. Blank line = new paragraph ·
          <span class="mono">**bold**</span> · <span class="mono">[link text](https://…)</span> ·
          <span class="mono">{{first_name}}</span> <span class="mono">{{last_name}}</span> <span class="mono">{{company}}</span>, with a fallback like <span class="mono">{{first_name|there}}</span>.
        </div></div>
      <div class="filter-row" style="margin-top:10px;">
        <div style="min-width:180px; flex:1;"><label>Button text (optional)</label><input type="text" id="mktCampBtnText" maxlength="80" value="${escapeHtml(e.button_text||"")}" placeholder="Get my offer"></div>
        <div style="min-width:260px; flex:2;"><label>Button link</label><input type="text" id="mktCampBtnUrl" maxlength="1000" value="${escapeHtml(e.button_url||"")}" placeholder="https://…"></div>
      </div>
      <div style="margin-top:12px; display:flex; gap:10px; flex-wrap:wrap; align-items:center;">
        <!-- v765: the client approved one email at setup. A campaign starts from
             it rather than from a blank box, and AI can spin a variant on it --
             different wording to the same list reads less like a blast. -->
        <button class="btn-secondary" id="mktCampFromTemplateBtn" title="Fill this in from the email the client approved">Use approved email</button>
        <button class="btn-secondary" id="mktCampVariantBtn" title="Reword the approved email for this campaign">AI variant</button>
        <button class="btn-secondary" id="mktCampSaveBtn">Save draft</button>
        <button class="btn-secondary" id="mktCampPreviewBtn">Preview</button>
        <button class="btn-secondary" id="mktCampCloseBtn">Close</button>
        <span id="mktCampEdMsg" style="font-size:12px; color:var(--text-2);"></span>
      </div>
      <div style="margin-top:16px; padding-top:14px; border-top:1px solid var(--border);">
        <div class="filter-row">
          <div style="min-width:260px; flex:2;"><label>Send a test to (up to 5, comma separated)</label><input type="text" id="mktCampTestTo" placeholder="you@yourcompany.com"></div>
          <div style="align-self:flex-end;"><button class="btn-secondary" id="mktCampTestBtn">Send test</button></div>
        </div>
        <div style="font-size:11.5px; color:var(--text-2); margin-top:4px;">A test is a real email and counts toward the client's monthly allowance.</div>
      </div>
      <div style="margin-top:16px; padding-top:14px; border-top:1px solid var(--border);">
        <div style="display:flex; gap:18px; flex-wrap:wrap; align-items:center; font-size:13px;">
          <label style="display:flex; gap:6px; align-items:center; text-transform:none; letter-spacing:0; font-size:13px;"><input type="radio" name="mktCampWhen" value="now" checked style="width:auto;"> Send now</label>
          <label style="display:flex; gap:6px; align-items:center; text-transform:none; letter-spacing:0; font-size:13px;"><input type="radio" name="mktCampWhen" value="later" style="width:auto;"> Send at</label>
          <input type="datetime-local" id="mktCampAt" style="max-width:220px;">
          ${isOwner?`<label style="display:flex; gap:6px; align-items:center; text-transform:none; letter-spacing:0; font-size:13px;" title="Runs every step at Telnyx in sandbox mode. Nothing is delivered or billed."><input type="checkbox" id="mktCampDry" style="width:auto;"> Dry run (delivers nothing, no charge)</label>`:""}
        </div>
        <div style="margin-top:10px;"><button class="btn-primary" id="mktCampScheduleBtn">Save and schedule</button></div>
      </div>
    </div>`;const refreshEstimate=async()=>{const ids=[...box.querySelectorAll(".mkt-camp-aud:checked")].map(x=>x.value);const el=document.getElementById("mktCampEstimate");if(!ids.length){el.textContent="Choose at least one list.";return}el.textContent="Counting…";try{const r=await mktCampCall("estimate",{client_id:clientId,audience_ids:ids});const over=r.recipients>r.remaining;el.innerHTML=`<b>${r.recipients.toLocaleString()}</b> people will receive it (duplicates, unsubscribes and bounces left out) · ${r.remaining.toLocaleString()} emails left this month`+(over?' · <span style="color:var(--danger)">not enough allowance</span>':"")}catch(err){el.textContent=err.message}};box.querySelectorAll(".mkt-camp-aud").forEach(x=>x.addEventListener("change",refreshEstimate));refreshEstimate();document.getElementById("mktCampCloseBtn").addEventListener("click",()=>{box.innerHTML="";mktCampEditing=null;mktCampaignsLoad(clientId)});const fromTpl=document.getElementById("mktCampFromTemplateBtn");if(fromTpl)fromTpl.onclick=()=>mktCampUseTemplate();const variantBtn=document.getElementById("mktCampVariantBtn");if(variantBtn)variantBtn.onclick=()=>mktCampAiVariant(clientId,variantBtn);document.getElementById("mktCampSaveBtn").addEventListener("click",()=>mktCampSave(clientId,false));document.getElementById("mktCampScheduleBtn").addEventListener("click",()=>mktCampSave(clientId,true));document.getElementById("mktCampPreviewBtn").addEventListener("click",()=>mktCampPreview({client_id:clientId,...mktCampFields()}));document.getElementById("mktCampTestBtn").addEventListener("click",()=>mktCampTest(clientId));document.getElementById("mktCampAt").addEventListener("focus",()=>{const r=box.querySelector('input[name="mktCampWhen"][value="later"]');if(r)r.checked=true});box.scrollIntoView({behavior:"smooth",block:"start"})}function mktCampFields(){return{name:document.getElementById("mktCampName").value,sender_id:document.getElementById("mktCampSender").value,audience_ids:[...document.querySelectorAll("#mktCampEditor .mkt-camp-aud:checked")].map(x=>x.value),subject:document.getElementById("mktCampSubject").value,preheader:document.getElementById("mktCampPre").value,body:document.getElementById("mktCampBody").value,button_text:document.getElementById("mktCampBtnText").value,button_url:document.getElementById("mktCampBtnUrl").value}}function mktCampUseTemplate(){const msg=document.getElementById("mktCampMsg");const c=mktContent;if(!c||!c.exists||!c.usable){if(msg){msg.style.color="var(--danger)";msg.textContent=c&&c.staff_blocked?"That email is on hold, so it cannot be used.":"This client has not approved an email yet."}return}const set=(id,v)=>{const el=document.getElementById(id);if(el)el.value=v||""};set("mktCampSubject",c.subject);set("mktCampPre",c.preheader);set("mktCampBody",c.body);set("mktCampBtnText",c.button_text);set("mktCampBtnUrl",c.button_url);if(msg){msg.style.color="var(--text-2)";msg.textContent="Filled in from the approved email. Edit it for this campaign if you want."}}async function mktCampAiVariant(clientId,btn){const msg=document.getElementById("mktCampMsg");const c=mktContent;if(!c||!c.exists||!c.usable){if(msg){msg.style.color="var(--danger)";msg.textContent="Approve an email for this client first — a variant is written from it."}return}const name=(document.getElementById("mktCampName")||{}).value||"";const label=btn.textContent;btn.disabled=true;btn.textContent="Writing…";if(msg){msg.style.color="var(--text-2)";msg.textContent="Writing a variant…"}try{const r=await mktCall("content_variant",{client_id:clientId,campaign_name:name,subject:c.subject,body:c.body,preheader:c.preheader||""});const set=(id,v)=>{const el=document.getElementById(id);if(el)el.value=v||""};set("mktCampSubject",r.subject);set("mktCampPre",r.preheader);set("mktCampBody",r.body);if(msg){msg.style.color="var(--good)";msg.textContent="Variant written. Read it before you schedule it — nothing is saved yet."}}catch(e){if(msg){msg.style.color="var(--danger)";msg.textContent=e.message}}finally{btn.disabled=false;btn.textContent=label}}async function mktCampSaveDraft(clientId){const r=await mktCampCall("save",{id:mktCampEditing&&mktCampEditing.id,client_id:clientId,...mktCampFields()});mktCampEditing=r.campaign;return r.campaign}async function mktCampSave(clientId,schedule){const msg=document.getElementById("mktCampEdMsg");const setMsg=(t,c)=>{msg.style.color=c||"var(--text-2)";msg.textContent=t};try{let sendAt=null;if(schedule){const later=document.querySelector('#mktCampEditor input[name="mktCampWhen"]:checked').value==="later";if(later){const v=document.getElementById("mktCampAt").value;if(!v)return setMsg("Pick the date and time to send.","var(--danger)");sendAt=new Date(v).toISOString()}}setMsg("Saving…");const c=await mktCampSaveDraft(clientId);if(!schedule){setMsg("Draft saved.","var(--good)");return}const dry=Boolean(document.getElementById("mktCampDry")&&document.getElementById("mktCampDry").checked);const est=await mktCampCall("estimate",{client_id:clientId,audience_ids:c.audience_ids});const sender=mktSenders.find(s=>s.id===c.sender_id);const question=dry?`Dry run “${c.name}” for ${est.recipients.toLocaleString()} contacts?

Every step runs, but nothing is delivered and nothing is charged.`:`Send “${c.name}” to ${est.recipients.toLocaleString()} people${sendAt?" at "+new Date(sendAt).toLocaleString():" now"}?

From: ${sender?sender.from_name:""}
Subject: ${c.subject}

This uses ${est.recipients.toLocaleString()} of the ${est.remaining.toLocaleString()} emails left this month. Sent email cannot be recalled.`;if(!window.confirm(question)){setMsg("Saved as a draft; not scheduled.");return}setMsg("Scheduling…");await mktCampCall("schedule",{id:c.id,send_at:sendAt,dry_run:dry});document.getElementById("mktCampEditor").innerHTML="";mktCampEditing=null;await mktCampaignsLoad(clientId);const m=document.getElementById("mktCampMsg");if(m){m.style.color="var(--good)";m.textContent=sendAt?"Scheduled.":"Scheduled — sending starts within a minute."}}catch(e){setMsg(e.message,"var(--danger)")}}async function mktCampTest(clientId){const msg=document.getElementById("mktCampEdMsg");const to=document.getElementById("mktCampTestTo").value.trim();if(!to){msg.style.color="var(--danger)";msg.textContent="Enter an address for the test.";return}if(!window.confirm(`Send a real test email to ${to}? It counts toward the client's allowance.`))return;try{msg.style.color="var(--text-2)";msg.textContent="Saving and sending test…";const c=await mktCampSaveDraft(clientId);const r=await mktCampCall("test_send",{id:c.id,to});msg.style.color=r.errors&&r.errors.length?"#E38B2E":"var(--good)";msg.textContent=`Test sent to ${r.sent}.`+(r.errors&&r.errors.length?" Problems: "+r.errors.join("; "):"")}catch(e){msg.style.color="var(--danger)";msg.textContent=e.message}}async function mktCreate(clientId){const msg=document.getElementById("mktMsg"),btn=document.getElementById("mktCreateBtn");btn.disabled=true;msg.style.color="var(--text-2)";msg.textContent="Creating…";try{await mktCall("audience_create",{client_id:clientId,name:document.getElementById("mktNewName").value,source:document.getElementById("mktNewSource").value});await mktLoad()}catch(e){msg.style.color="var(--danger)";msg.textContent=e.message;btn.disabled=false}}async function mktDelete(id,name){if(!window.confirm(`Delete the list “${name}” and all its contacts?`))return;try{await mktCall("audience_delete",{id,confirm:true});await mktLoad()}catch(e){const m=document.getElementById("mktMsg");if(m){m.style.color="var(--danger)";m.textContent=e.message}}}function mktShowUpload(id,name){mktCsv=null;document.getElementById("mktUploadArea").innerHTML=`
    <div style="padding:12px; border:1px solid var(--border); border-radius:8px;">
      <div style="font-size:12.5px; font-weight:700; margin-bottom:8px;">Upload contacts into “${escapeHtml(name)}”</div>
      <div style="font-size:11.5px; color:var(--text-2); margin-bottom:10px;">
        CSV with a header row. Anyone who unsubscribed, bounced or complained is skipped automatically,
        and uploading the same file twice is harmless.</div>
      <input type="file" id="mktCsvFile" accept=".csv,text/csv" style="max-width:320px;">
      <div id="mktMapArea" style="margin-top:10px;"></div>
      <div style="margin-top:12px; display:flex; gap:10px; align-items:center; flex-wrap:wrap;">
        <button class="btn-primary" id="mktImportBtn" data-id="${escapeHtml(id)}" disabled>Import</button>
        <button class="btn-secondary" id="mktCancelBtn">Cancel</button>
        <span id="mktImportMsg" style="font-size:12px; color:var(--text-2);"></span>
      </div>
      <div id="mktResult" style="margin-top:10px;"></div>
    </div>`;document.getElementById("mktCsvFile").addEventListener("change",mktFilePicked);document.getElementById("mktCancelBtn").addEventListener("click",()=>{document.getElementById("mktUploadArea").innerHTML=""});document.getElementById("mktImportBtn").addEventListener("click",e=>mktImport(e.target.dataset.id))}function mktFilePicked(ev){const f=ev.target.files&&ev.target.files[0];if(!f)return;const r=new FileReader;r.onload=()=>{const all=mktParseCsv(r.result);if(all.length<2){document.getElementById("mktMapArea").innerHTML='<div style="font-size:12px; color:var(--danger)">That file has no data rows under its header.</div>';return}mktCsv={headers:all[0],rows:all.slice(1)};const h=mktCsv.headers;document.getElementById("mktMapArea").innerHTML=`
      <div style="font-size:12px; margin-bottom:8px;">${mktCsv.rows.length.toLocaleString()} rows found. Which column is which?</div>
      <div class="filter-row">
        <div><label>Email *</label>${mktSel("mktColEmail",h,mktGuess(h,"email"))}</div>
        <div><label>First name</label>${mktSel("mktColFirst",h,mktGuess(h,"first_name"))}</div>
        <div><label>Last name</label>${mktSel("mktColLast",h,mktGuess(h,"last_name"))}</div>
        <div><label>Company</label>${mktSel("mktColCompany",h,mktGuess(h,"company"))}</div>
      </div>`;document.getElementById("mktImportBtn").disabled=false};r.readAsText(f)}async function mktImport(audienceId){const ec=Number(document.getElementById("mktColEmail").value);const msg=document.getElementById("mktImportMsg"),btn=document.getElementById("mktImportBtn");if(ec<0){msg.style.color="var(--danger)";msg.textContent="Pick the email column.";return}const c={first_name:Number(document.getElementById("mktColFirst").value),last_name:Number(document.getElementById("mktColLast").value),company:Number(document.getElementById("mktColCompany").value)};const rows=mktCsv.rows.map(r=>({email:r[ec],first_name:c.first_name>=0?r[c.first_name]:"",last_name:c.last_name>=0?r[c.last_name]:"",company:c.company>=0?r[c.company]:""}));btn.disabled=true;const t={imported:0,already_on_list:0,duplicates_in_file:0,blank:0,invalid:0,suppressed:0};try{for(let i=0;i<rows.length;i+=2e3){msg.style.color="var(--text-2)";msg.textContent=`Importing ${Math.min(i+2e3,rows.length).toLocaleString()} of ${rows.length.toLocaleString()}…`;const r=await mktCall("contacts_import",{audience_id:audienceId,rows:rows.slice(i,i+2e3)});for(const k of Object.keys(t))t[k]+=Number(r[k]||0)}msg.textContent="";document.getElementById("mktResult").innerHTML=`
      <div style="font-size:13px; line-height:1.7;">
        <b style="color:var(--good)">${t.imported.toLocaleString()} imported.</b><br>
        ${t.already_on_list?`${t.already_on_list.toLocaleString()} were already on this list.<br>`:""}
        ${t.duplicates_in_file?`${t.duplicates_in_file.toLocaleString()} appeared more than once in the file.<br>`:""}
        ${t.suppressed?`<span style="color:#E38B2E">${t.suppressed.toLocaleString()} skipped — they unsubscribed, bounced or complained.</span><br>`:""}
        ${t.invalid?`<span style="color:var(--danger)">${t.invalid.toLocaleString()} were not valid email addresses.</span><br>`:""}
        ${t.blank?`${t.blank.toLocaleString()} rows had no email.<br>`:""}
      </div>`;await mktLoad()}catch(e){msg.style.color="var(--danger)";msg.textContent=e.message;btn.disabled=false}}function activateTab(tabKey){document.querySelectorAll(".tab-btn").forEach(b=>b.classList.toggle("active",b.dataset.tab===tabKey));document.querySelectorAll(".tab-panel").forEach(p=>p.classList.toggle("active",p.id===`tab-${tabKey}`));if(tabKey!=="dialer_management"&&/^#dialer-admin/.test(location.hash||"")){try{history.replaceState(null,"",location.pathname+location.search)}catch(e){}}if(tabKey!=="data_science")stopCsubSkiptracePolling();detailReturnTo=null;if(tabKey==="time_management")initTimeManagementTab();if(tabKey==="leads")initLeadsTab();if(tabKey==="call_activity")initActivityTab();if(tabKey==="client_chat")ciInitTab();if(tabKey==="client_disputes")initClientDisputesTab();if(tabKey==="client_submissions")initClientSubmissionsTab();if(tabKey==="activity")initAuditLogTab();if(tabKey==="ops_health")loadOpsHealth();if(tabKey==="usermanagement")initUserManagementTab();if(tabKey==="finance")initFinanceTab();if(tabKey==="hr")initHrTab();if(tabKey==="dialer_management")initDialerAdminTab();if(tabKey==="data_science")initDataSciTab();if(tabKey==="taxes")initTaxesTab()}async function performSignOut({idleTimeout=false}={}){if(pollHandle)clearInterval(pollHandle);stopPresenceHeartbeat();try{try{if(sb&&myUserId)await sb.from("profiles").update({chat_status:"offline",last_seen_at:new Date().toISOString()}).eq("id",myUserId)}catch(e){}try{teardownChatWidget()}catch(e){console.error("teardownChatWidget failed during sign-out",e)}if(sb)await sb.auth.signOut()}catch(e){console.error("Sign-out encountered an error",e)}finally{try{sessionStorage.removeItem(IDLE_LAST_ACTIVITY_KEY)}catch(e){}staffPageMemoryOff=true;try{sessionStorage.removeItem(STAFF_PAGE_KEY)}catch(e){}if(idleTimeout){try{sessionStorage.setItem(IDLE_SIGNOUT_MESSAGE_KEY,"You were signed out after 4 hours of inactivity — please sign in again.")}catch(e){}}window.location.href=window.location.pathname}}$("signOutBtn").addEventListener("click",()=>performSignOut());function closeDetailPanel(){currentDetailAgent=null;$("detailPanel").style.display="none";if(detailReturnTo&&detailReturnTo.tab==="usermanagement"){const target=detailReturnTo;detailReturnTo=null;document.querySelectorAll(".tab-panel").forEach(p=>p.classList.toggle("active",p.id==="tab-usermanagement"));activateUmSection(target.umSection||"users")}else{detailReturnTo=null;$("teamCard").style.display="block";tmActiveSection="staff";document.querySelectorAll(".tab-panel").forEach(p=>p.classList.toggle("active",p.id==="tab-time_management"));document.querySelectorAll(".tab-btn").forEach(b=>b.classList.toggle("active",b.dataset.tab==="time_management"));document.querySelectorAll(".tm-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.tmSection==="staff"));document.querySelectorAll(".tm-section").forEach(s=>{s.style.display=s.id==="tmSection-staff"?"block":"none"})}}$("backLink").addEventListener("click",closeDetailPanel);let refreshAllLastRun=0;let chatDirectoryLastRun=0;const isOnScreen=id=>{const el=$(id);return!!(el&&el.offsetParent!==null)};async function refreshAll(){if(document.hidden)return;refreshAllLastRun=Date.now();if((myIsOwner||canViewTimeManagementSection("staff"))&&isOnScreen("teamCard"))await loadTeam();if(currentDetailAgent&&isOnScreen("detailPanel")){await renderDetail(currentDetailAgent)}const realtimeUp=!!(chatChannel&&chatChannel.state==="joined");if(!realtimeUp){loadChatOverview();loadClientChatOverview().then(()=>{if(!chatActiveConv)renderChatConvList($("chatSearchInput")?$("chatSearchInput").value:"")})}if(Date.now()-chatDirectoryLastRun>=6e4&&typeof loadChatDirectory==="function"){chatDirectoryLastRun=Date.now();loadChatDirectory().then(()=>{if(typeof renderChatConvList==="function")renderChatConvList($("chatSearchInput")?$("chatSearchInput").value:"");if(chatActiveConv&&typeof renderChatThreadHeaderStatus==="function")renderChatThreadHeaderStatus(chatActiveConv)}).catch(()=>{})}}document.addEventListener("visibilitychange",()=>{if(document.visibilityState==="visible"&&pollHandle&&myUserId&&Date.now()-refreshAllLastRun>2e4)refreshAll()});async function refreshDetailIfViewing(userId){if(currentDetailAgent&&currentDetailAgent.id===userId){await renderDetail(currentDetailAgent)}}let chatDirectory=[];let chatGroupDirectory=[];let chatGroups={};let chatConversations={};let chatLastRead={};let chatReadsIndex={};let chatActiveConv=null;let chatOfflineHint=null;let clientChatThreadsDirectory=[];let clientChatThreadsRefreshTimer=null;let chatActiveGroupInfoId=null;let chatChannel=null;let chatChannelEverConnected=false;const CHAT_FETCH_LIMIT=500;let chatMyTypingChannel=null;let chatTypingOutboundChannel=null;let chatTypingOutboundKey=null;let chatClientTypingChannel=null;let chatClientTypingChannelClientId=null;let chatPeerTypingUntil={};let chatTypingStopTimer=null;let chatLastTypingPingAt=0;function chatIsPeerTyping(key){return!!(chatPeerTypingUntil[key]&&chatPeerTypingUntil[key]>Date.now())}function chatOnPeerTypingChanged(key){if(key.startsWith("client:")&&typeof ciIsViewing==="function"){if(ciIsViewing(key))ciRenderMsgs(false);if(ciTabActive())ciRenderList()}if(chatActiveConv===key){if(key.startsWith("client:"))renderClientChatMessages(key.slice(7));else renderChatThread()}renderChatConvList($("chatSearchInput")?$("chatSearchInput").value:"")}function chatTypingBubbleHtml(){return'<div class="chat-msg-row theirs chat-typing-row"><div class="chat-msg-bubble chat-typing-dots"><span></span><span></span><span></span></div></div>'}const photoViewer={el:null,stage:null,img:null,label:null,url:"",name:"",s:1,fit:1,tx:0,ty:0,pointers:new Map,pinch:null,moved:false,downOnStage:false,prevOverflow:""};function photoViewerApply(){const v=photoViewer;v.img.style.transform=`translate(${v.tx}px, ${v.ty}px) translate(-50%, -50%) scale(${v.s})`;v.label.textContent=Math.round(v.s*100)+"%";v.stage.classList.toggle("zoomed",v.s>v.fit*1.01)}function photoViewerZoomTo(ns,cx,cy){const v=photoViewer;ns=Math.min(Math.max(v.fit*8,4),Math.max(v.fit*.5,ns));const r=v.stage.getBoundingClientRect();const px=(cx==null?r.left+r.width/2:cx)-(r.left+r.width/2);const py=(cy==null?r.top+r.height/2:cy)-(r.top+r.height/2);const k=ns/v.s;v.tx=px-k*(px-v.tx);v.ty=py-k*(py-v.ty);v.s=ns;photoViewerApply()}function photoViewerZoomBy(f,cx,cy){photoViewerZoomTo(photoViewer.s*f,cx,cy)}function photoViewerFit(){const v=photoViewer;const r=v.stage.getBoundingClientRect();const w=v.img.naturalWidth||1,h=v.img.naturalHeight||1;v.fit=Math.max(.01,Math.min((r.width-32)/w,(r.height-32)/h,1));v.s=v.fit;v.tx=0;v.ty=0;photoViewerApply()}function photoViewerBuild(){if(photoViewer.el)return;const v=photoViewer;const el=document.createElement("div");el.className="photo-viewer";el.hidden=true;el.setAttribute("role","dialog");el.setAttribute("aria-modal","true");el.setAttribute("aria-label","Photo");el.innerHTML=`
    <div class="photo-viewer-bar">
      <div class="photo-viewer-name"></div>
      <button type="button" class="photo-viewer-btn" data-pv="out" title="Zoom out (-)" aria-label="Zoom out">&minus;</button>
      <div class="photo-viewer-zoom">100%</div>
      <button type="button" class="photo-viewer-btn" data-pv="in" title="Zoom in (+)" aria-label="Zoom in">+</button>
      <button type="button" class="photo-viewer-btn" data-pv="fit" title="Fit to screen (0)">Fit</button>
      <button type="button" class="photo-viewer-btn" data-pv="download" title="Download" aria-label="Download">&#11015;</button>
      <button type="button" class="photo-viewer-btn" data-pv="close" title="Close (Esc)" aria-label="Close">&times;</button>
    </div>
    <div class="photo-viewer-stage"><img class="photo-viewer-img" alt="" draggable="false"></div>`;document.body.appendChild(el);v.el=el;v.stage=el.querySelector(".photo-viewer-stage");v.img=el.querySelector(".photo-viewer-img");v.label=el.querySelector(".photo-viewer-zoom");el.querySelector(".photo-viewer-bar").addEventListener("click",e=>{const b=e.target.closest("[data-pv]");if(!b)return;const act=b.dataset.pv;if(act==="in")photoViewerZoomBy(1.25);else if(act==="out")photoViewerZoomBy(.8);else if(act==="fit")photoViewerFit();else if(act==="download")downloadFileToDisk(v.url,v.name||"photo");else if(act==="close")closePhotoViewer()});const stage=v.stage;stage.addEventListener("wheel",e=>{e.preventDefault();photoViewerZoomBy(Math.exp(-e.deltaY*.0015),e.clientX,e.clientY)},{passive:false});stage.addEventListener("dblclick",e=>{if(v.s>v.fit*1.05)photoViewerFit();else photoViewerZoomTo(Math.max(v.fit*2.5,1),e.clientX,e.clientY)});stage.addEventListener("pointerdown",e=>{if(!v.pointers.size){v.moved=false;v.downOnStage=e.target===stage}try{stage.setPointerCapture(e.pointerId)}catch(err){}v.pointers.set(e.pointerId,{x:e.clientX,y:e.clientY});if(v.pointers.size===2){const[a,b]=[...v.pointers.values()];v.pinch={d:Math.hypot(a.x-b.x,a.y-b.y)||1,s:v.s}}});stage.addEventListener("pointermove",e=>{const prev=v.pointers.get(e.pointerId);if(!prev)return;const cur={x:e.clientX,y:e.clientY};v.pointers.set(e.pointerId,cur);if(v.pointers.size===2&&v.pinch){const[a,b]=[...v.pointers.values()];photoViewerZoomTo(v.pinch.s*Math.hypot(a.x-b.x,a.y-b.y)/v.pinch.d,(a.x+b.x)/2,(a.y+b.y)/2);v.moved=true;return}const dx=cur.x-prev.x,dy=cur.y-prev.y;if(Math.abs(dx)+Math.abs(dy)>1){v.moved=true;stage.classList.add("dragging")}v.tx+=dx;v.ty+=dy;photoViewerApply()});const release=e=>{v.pointers.delete(e.pointerId);if(v.pointers.size<2)v.pinch=null;if(!v.pointers.size)stage.classList.remove("dragging")};stage.addEventListener("pointerup",e=>{release(e);if(!v.pointers.size&&!v.moved&&v.downOnStage)closePhotoViewer()});stage.addEventListener("pointercancel",release);document.addEventListener("keydown",e=>{if(!v.el||v.el.hidden)return;if(e.key==="Escape"){e.preventDefault();e.stopImmediatePropagation();closePhotoViewer()}else if(e.key==="+"||e.key==="="){e.preventDefault();photoViewerZoomBy(1.25)}else if(e.key==="-"||e.key==="_"){e.preventDefault();photoViewerZoomBy(.8)}else if(e.key==="0"){e.preventDefault();photoViewerFit()}},true);window.addEventListener("resize",()=>{if(!v.el.hidden)photoViewerFit()})}function openPhotoViewer(url,name){photoViewerBuild();const v=photoViewer;v.url=url;v.name=name||"photo";v.el.querySelector(".photo-viewer-name").textContent=v.name;v.img.style.visibility="hidden";v.img.onload=()=>{v.img.style.visibility="";photoViewerFit()};v.img.src=url;v.prevOverflow=document.documentElement.style.overflow;document.documentElement.style.overflow="hidden";v.el.hidden=false;if(v.img.complete&&v.img.naturalWidth){v.img.style.visibility="";photoViewerFit()}v.el.querySelector('[data-pv="close"]').focus({preventScroll:true})}function closePhotoViewer(){const v=photoViewer;if(!v.el||v.el.hidden)return;v.el.hidden=true;v.pointers.clear();v.pinch=null;document.documentElement.style.overflow=v.prevOverflow||""}function chatSwapHtml(box,html){const keep=new Map;box.querySelectorAll('.chat-msg-attachment[data-resolved="1"]').forEach(el=>{if(!keep.has(el.dataset.attachmentPath))keep.set(el.dataset.attachmentPath,el)});box.innerHTML=html;if(!keep.size)return;box.querySelectorAll(".chat-msg-attachment[data-attachment-path]").forEach(el=>{const old=keep.get(el.dataset.attachmentPath);if(old){el.replaceWith(old);keep.delete(el.dataset.attachmentPath)}})}function chatMsgAvatarHtml(kind,id,name){let inner;if(kind==="ai")inner=chatBotAvatarIconHtml(28);else if(kind==="client")inner=`<div class="chat-conv-avatar" style="width:28px; height:28px; font-size:11px; background:${ciAvColor(id)}; color:#fff;">${escapeHtml(ciInitials(name))}</div>`;else inner=chatAvatarHtml(id,name,28,false);return`<div class="chat-msg-av">${inner}</div>`}function initChatTypingSignaling(){if(!myUserId||chatMyTypingChannel)return;chatMyTypingChannel=sb.channel("chat-typing-"+myUserId);chatMyTypingChannel.on("broadcast",{event:"typing"},({payload})=>{if(!payload||!payload.from)return;if(payload.state==="typing")chatPeerTypingUntil[payload.from]=Date.now()+4e3;else delete chatPeerTypingUntil[payload.from];chatOnPeerTypingChanged(payload.from)}).subscribe()}function teardownChatTypingSignaling(){if(chatMyTypingChannel){sb.removeChannel(chatMyTypingChannel);chatMyTypingChannel=null}if(chatTypingOutboundChannel){sb.removeChannel(chatTypingOutboundChannel);chatTypingOutboundChannel=null;chatTypingOutboundKey=null}chatCloseClientTypingChannel();chatPeerTypingUntil={};clearTimeout(chatTypingStopTimer);chatStopTypingSweep()}function chatOpenClientTypingChannel(clientId){if(chatClientTypingChannelClientId===clientId&&chatClientTypingChannel)return;chatCloseClientTypingChannel();chatClientTypingChannelClientId=clientId;const key="client:"+clientId;const ch=sb.channel("chat-typing-client-"+clientId);ch.on("broadcast",{event:"typing"},({payload})=>{if(!payload||payload.from!=="client")return;if(payload.state==="typing")chatPeerTypingUntil[key]=Date.now()+4e3;else delete chatPeerTypingUntil[key];chatOnPeerTypingChanged(key)}).subscribe();chatClientTypingChannel=ch}function chatCloseClientTypingChannel(){if(chatClientTypingChannel){sb.removeChannel(chatClientTypingChannel);chatClientTypingChannel=null}chatClientTypingChannelClientId=null}function chatTypingSend(key,state){if(key.startsWith("client:")){if(chatClientTypingChannel)chatClientTypingChannel.send({type:"broadcast",event:"typing",payload:{from:"staff",state}});return}if(chatTypingOutboundChannel&&chatTypingOutboundKey===key){chatTypingOutboundChannel.send({type:"broadcast",event:"typing",payload:{from:myUserId,state}});return}if(chatTypingOutboundChannel)sb.removeChannel(chatTypingOutboundChannel);chatTypingOutboundKey=key;const ch=sb.channel("chat-typing-"+key);chatTypingOutboundChannel=ch;voiceChannelReady(ch).then(()=>ch.send({type:"broadcast",event:"typing",payload:{from:myUserId,state}}))}let chatTypingSweepTimer=null;function chatStartTypingSweep(){if(chatTypingSweepTimer)return;chatTypingSweepTimer=setInterval(()=>{const now=Date.now();Object.keys(chatPeerTypingUntil).forEach(key=>{if(chatPeerTypingUntil[key]<=now){delete chatPeerTypingUntil[key];chatOnPeerTypingChanged(key)}})},1e3)}function chatStopTypingSweep(){if(chatTypingSweepTimer){clearInterval(chatTypingSweepTimer);chatTypingSweepTimer=null}}function chatContactName(userId){if(userId===myUserId)return myFullName||myEmail||"You";const person=chatDirectory.find(p=>p.id===userId)||chatGroupDirectory.find(p=>p.id===userId);return person&&(person.full_name||person.email)||"Unknown user"}function chatContactRole(userId){const person=userId===myUserId?{title:myTitle,department:myDepartment}:chatDirectory.find(p=>p.id===userId)||chatGroupDirectory.find(p=>p.id===userId);if(!person)return"";return[person.title,person.department].filter(Boolean).join(" · ")}function chatContactAvatar(userId){if(userId===myUserId)return myAvatarUrl;const person=chatDirectory.find(p=>p.id===userId)||chatGroupDirectory.find(p=>p.id===userId);return person&&person.avatar_url||null}function chatContactStatus(userId){if(userId===myUserId){return myChatStatusManual||myChatStatus||"offline"}if(AI_ASSISTANT_BOT_ID&&userId===AI_ASSISTANT_BOT_ID)return"active";const person=chatDirectory.find(p=>p.id===userId)||chatGroupDirectory.find(p=>p.id===userId);return derivePresence(person)}function chatAvatarHtml(userId,name,sizePx,showStatus){const url=chatContactAvatar(userId);const inner=url?`<img src="${escapeHtml(url)}" alt="">`:escapeHtml((name||"?").charAt(0).toUpperCase());const dot=showStatus?`<span class="chat-status-dot ${chatContactStatus(userId)}"></span>`:"";return`<div class="chat-conv-avatar" style="width:${sizePx}px; height:${sizePx}px; font-size:${Math.round(sizePx*.42)}px;">${inner}${dot}</div>`}function chatBotAvatarIconHtml(sizePx){return`<div class="chat-conv-avatar bot-avatar" style="width:${sizePx}px; height:${sizePx}px;">
    <svg viewBox="0 0 24 24" width="${Math.round(sizePx*.8)}" height="${Math.round(sizePx*.8)}" fill="none" xmlns="http://www.w3.org/2000/svg">
      <rect x="1.6" y="9" width="3.2" height="6.4" rx="1.6" fill="#E2B774"/>
      <rect x="19.2" y="9" width="3.2" height="6.4" rx="1.6" fill="#E2B774"/>
      <circle cx="12" cy="12" r="9" fill="#434C60" stroke="#232A37" stroke-width="0.8"/>
      <path d="M8.6 4.3 Q12 3 15.4 4.3" stroke="#E2B774" stroke-width="1.5" stroke-linecap="round"/>
      <rect x="5.4" y="6.8" width="13.2" height="10.2" rx="4.2" fill="#5A3A14" stroke="#E2B774" stroke-width="0.9"/>
      <path d="M8 11.2 Q9.2 9.8 10.4 11.2 M13.6 11.2 Q14.8 9.8 16 11.2" stroke="#FFD38A" stroke-width="1.1" stroke-linecap="round"/>
      <path d="M10.1 13.1 Q12 15.4 13.9 13.1 Z" fill="#FFD38A"/>
    </svg>
  </div>`}function chatConvDisplayName(key){if(key==="team")return"Communication";if(key.startsWith("group:"))return chatGroups[key.slice(6)]&&chatGroups[key.slice(6)].name||"Group";return chatContactName(key)}function chatStatusMeta(status){switch(status){case"active":return{dot:"active",label:"Active"};case"away":return{dot:"away",label:"Away"};case"busy":return{dot:"busy",label:"Busy"};case"dnd":return{dot:"dnd",label:"Do not disturb"};case"brb":return{dot:"brb",label:"Be right back"};case"invisible":return{dot:"offline",label:"Appear offline"};default:return{dot:"offline",label:"Offline"}}}function chatPersonStatusMsg(userId){const p=userId===myUserId?{status_text:myStatusText,status_emoji:myStatusEmoji,status_until:myStatusUntil}:chatDirectory.find(x=>x.id===userId);if(!p||!p.status_text&&!p.status_emoji)return null;if(p.status_until&&Date.now()>new Date(p.status_until).getTime())return null;return{emoji:p.status_emoji||"",text:p.status_text||""}}function chatStatusBadgeHtml(userId){const s=chatPersonStatusMsg(userId);if(!s)return"";return`<span class="chat-status-emoji" title="${escapeHtml(s.text||"")}">${escapeHtml(s.emoji||"💬")}</span>`}function chatLastSeenLabel(userId){const p=chatDirectory.find(x=>x.id===userId)||chatGroupDirectory.find(x=>x.id===userId);if(!p||!p.last_seen_at)return"";const s=(Date.now()-new Date(p.last_seen_at).getTime())/1e3;if(s<90)return"just now";if(s<3600)return Math.floor(s/60)+"m ago";if(s<86400)return Math.floor(s/3600)+"h ago";const tz=nonAgentDisplayTz();return new Date(p.last_seen_at).toLocaleDateString([],{month:"short",day:"numeric",...tz?{timeZone:tz}:{}})}function chatIsMyDnd(){return myChatStatusManual==="dnd"&&!(myStatusUntil&&Date.now()>new Date(myStatusUntil).getTime())}function chatFmtUntil(iso){const tz=nonAgentDisplayTz();return new Date(iso).toLocaleString([],{weekday:"short",hour:"numeric",minute:"2-digit",...tz?{timeZone:tz}:{}})}function renderChatThreadHeaderStatus(key){const el=$("chatThreadTitleStatus");if(!el)return;if(key==="team"){el.style.display="none";el.innerHTML="";return}if(key.startsWith("group:")){const gid=key.slice(6);const g=chatGroups[gid];const memberIds=g&&g.members?g.members.map(m=>m.user_id):[];let activeCount=0;memberIds.forEach(id=>{if(id===myUserId)return;const st2=chatContactStatus(id);if(st2==="active")activeCount++});const others=memberIds.filter(id=>id!==myUserId).length;el.innerHTML=`<span class="chat-status-dot active" style="width:7px;height:7px;display:inline-block;border-radius:50%;margin-right:5px;vertical-align:middle;"></span>${activeCount} of ${others} active`;el.style.display="block";return}const st=chatContactStatus(key);const meta=chatStatusMeta(st);const roleLine=chatContactRole(key);el.innerHTML=`<span class="chat-status-dot ${meta.dot}" style="width:7px;height:7px;display:inline-block;border-radius:50%;margin-right:5px;vertical-align:middle;"></span>${meta.label}`;if(st==="offline"){const seen=chatLastSeenLabel(key);if(seen)el.innerHTML+=` · last seen ${escapeHtml(seen)}`}const sm=chatPersonStatusMsg(key);if(sm)el.innerHTML+=` · ${escapeHtml(((sm.emoji?sm.emoji+" ":"")+(sm.text||"")).trim())}`;if(roleLine)el.innerHTML+=`<span style="opacity:.75;"> · ${escapeHtml(roleLine)}</span>`;el.style.display="block"}function chatCanPostTeam(){return myIsOwner||myRole==="admin"}function isHrRole(roleName){const r=(roleName||"").toLowerCase().replace(/[\s_-]+/g,"");return r==="humanresources"||r==="hr"}function viewerIsHR(){return isHrRole(myRole)}function viewerHasFullUserEdit(){return myIsOwner||myRole==="admin"||myPermissions.canManageUsers}function canOpenUserProfile(targetUserId){if(targetUserId&&targetUserId===myUserId)return true;return viewerHasFullUserEdit()||viewerIsHR()}async function initChatWidget(){if(!myUserId)return;$("chatWidget").style.display="block";await loadChatDirectory();await loadChatOverview(true);await loadChatQueueLabels();await loadClientChatOverview();renderChatConvList($("chatSearchInput")?$("chatSearchInput").value:"");subscribeChatRealtime();subscribeStaffOpsRealtime();subscribeChatReactions();initNotifCenter();handleCallbackHash();initPushToggle();callNotifyGateCheck();initVoiceCallSignaling();setTimeout(()=>voiceResumeSavedCall(),1500);initChatTypingSignaling();chatStartTypingSweep();chatUpdateMyAvatarButton();if(AI_ASSISTANT_BOT_ID&&$("desoukySidebarBtn")){$("desoukySidebarBtn").style.display="flex"}}function teardownChatWidget(){$("chatWidget").style.display="none";$("chatPanel").style.display="none";$("desoukyMascotWrap").style.display="none";if($("desoukySidebarBtn"))$("desoukySidebarBtn").style.display="none";if(chatChannel){sb.removeChannel(chatChannel);chatChannel=null}if(staffOpsChannel){sb.removeChannel(staffOpsChannel);staffOpsChannel=null}teardownVoiceCallSignaling();teardownChatTypingSignaling();chatDirectory=[];chatGroupDirectory=[];chatGroups={};chatConversations={};chatLastRead={};chatActiveConv=null;chatActiveGroupInfoId=null}async function loadChatDirectory(){const{data,error}=await sb.rpc("chat_directory");if(error)console.error("chat_directory failed",error);else chatDirectory=(data||[]).filter(p=>p.id!==myUserId);const{data:groupDirData,error:groupDirErr}=await sb.rpc("chat_group_directory");if(groupDirErr)console.error("chat_group_directory failed",groupDirErr);else chatGroupDirectory=groupDirData||[]}async function loadChatGroups(){const{data:groups,error}=await sb.from("chat_groups").select("id, name, created_by, created_at");if(error){console.error("loadChatGroups failed",error);return}const{data:members}=await sb.from("chat_group_members").select("group_id, user_id, added_by, added_at");chatGroups={};(groups||[]).forEach(g=>{chatGroups[g.id]={...g,members:[]}});(members||[]).forEach(m=>{if(chatGroups[m.group_id])chatGroups[m.group_id].members.push(m)})}async function loadChatOverview(isInitialLoad){await loadChatGroups();const{data:reads}=await sb.from("chat_reads").select("user_id, conversation_key, last_read_at");chatLastRead={};chatReadsIndex={};(reads||[]).forEach(r=>{chatReadsIndex[`${r.user_id}|${r.conversation_key}`]=r.last_read_at;if(r.user_id===myUserId)chatLastRead[r.conversation_key]=r.last_read_at});const myGroupIds=Object.keys(chatGroups);const orParts=[`recipient_id.is.null`,`sender_id.eq.${myUserId}`,`recipient_id.eq.${myUserId}`];if(myGroupIds.length)orParts.push(`group_id.in.(${myGroupIds.join(",")})`);const{data:msgs}=await sb.from("chat_messages").select("id, sender_id, recipient_id, group_id, body, created_at, edited_at, attachment_path, attachment_name, attachment_mime").or(orParts.join(",")).order("created_at",{ascending:false}).limit(CHAT_FETCH_LIMIT);Object.keys(chatConversations).forEach(k=>{if(!k.startsWith("client:"))delete chatConversations[k]});(msgs||[]).slice().reverse().forEach(m=>chatAddMessageToState(m,false));updateChatUnreadBadge();if(chatActiveConv){if(chatActiveConv.startsWith("client:"))renderClientChatMessages(chatActiveConv.slice(7));else renderChatThread()}else{renderChatConvList($("chatSearchInput").value)}if(isInitialLoad){const totalUnread=Object.values(chatConversations).reduce((t,c)=>t+c.unread,0);if(totalUnread>0&&$("chatPanel").style.display==="none"){$("chatPanel").style.display="flex"}}}function chatConvKeyForMessage(m){if(m.group_id)return"group:"+m.group_id;return m.recipient_id===null?"team":m.sender_id===myUserId?m.recipient_id:m.sender_id}const CHAT_EDIT_WINDOW_MS=15*60*1e3;function chatMessageEditable(m){return m.sender_id===myUserId&&Date.now()-new Date(m.created_at).getTime()<CHAT_EDIT_WINDOW_MS}function chatPersonLastSeenAt(userId){const person=chatDirectory.find(p=>p.id===userId)||chatGroupDirectory.find(p=>p.id===userId);return person&&person.last_seen_at?new Date(person.last_seen_at).getTime():0}function chatDmTickState(m,peerId){const sentMs=new Date(m.created_at).getTime();const peerReadAt=chatReadsIndex[`${peerId}|${myUserId}`];if(peerReadAt&&new Date(peerReadAt).getTime()>=sentMs)return"read";if(chatPersonLastSeenAt(peerId)>=sentMs)return"delivered";return"sent"}function chatTicksHtml(state){if(state==="sent")return'<span class="chat-tick sent" title="Sent">&#10003;</span>';const cls=state==="read"?"read":"delivered";const title=state==="read"?"Read":"Delivered";return`<span class="chat-tick ${cls}" title="${title}">&#10003;&#10003;</span>`}function chatSeenByNames(m,key){const sentMs=new Date(m.created_at).getTime();let audienceIds;if(key==="team"){audienceIds=chatGroupDirectory.map(p=>p.id)}else{const gid=key.slice(6);audienceIds=(chatGroups[gid]&&chatGroups[gid].members||[]).map(mm=>mm.user_id)}return audienceIds.filter(id=>id!==m.sender_id).filter(id=>{const readAt=chatReadsIndex[`${id}|${key}`];return readAt&&new Date(readAt).getTime()>=sentMs}).map(id=>chatContactName(id))}function chatSeenByHtml(m,key){const names=chatSeenByNames(m,key);if(!names.length)return'<span class="chat-tick sent" title="Sent">&#10003;</span>';const label=names.length<=3?names.join(", "):`${names.slice(0,3).join(", ")} +${names.length-3} more`;return`<span class="chat-tick read chat-seen-by" title="Seen by: ${escapeHtml(names.join(", "))}">&#10003;&#10003; Seen by ${escapeHtml(label)}</span>`}let chatAudioCtx=null;function chatPlayNotificationSound(){if(chatIsMyDnd())return;try{if(!chatAudioCtx)chatAudioCtx=new(window.AudioContext||window.webkitAudioContext);if(chatAudioCtx.state==="suspended")chatAudioCtx.resume();const now=chatAudioCtx.currentTime;const glide=(fromHz,toHz,startAt,glideEnd,peak,decayEnd)=>{const osc=chatAudioCtx.createOscillator();const gain=chatAudioCtx.createGain();osc.type="sine";osc.frequency.setValueAtTime(fromHz,now+startAt);osc.frequency.exponentialRampToValueAtTime(toHz,now+glideEnd);gain.gain.setValueAtTime(0,now+startAt);gain.gain.linearRampToValueAtTime(peak,now+startAt+.02);gain.gain.exponentialRampToValueAtTime(.001,now+decayEnd);osc.connect(gain).connect(chatAudioCtx.destination);osc.start(now+startAt);osc.stop(now+decayEnd+.01)};glide(880,1318.5,0,.08,.2,.25);glide(1760,1975.5,.1,.18,.25,.45)}catch(e){console.error("Chat notification sound failed",e)}}let chatNotificationPermissionRequested=false;function chatEnsureNotificationPermission(){if(chatNotificationPermissionRequested||!("Notification"in window))return;chatNotificationPermissionRequested=true;if(Notification.permission==="default")Notification.requestPermission()}function chatShowDesktopNotification(key,m){if(!("Notification"in window)||Notification.permission!=="granted")return;if(chatIsMyDnd())return;const title=key==="team"?"Communication":key.startsWith("group:")?`${chatConvDisplayName(key)} · ${chatContactName(m.sender_id)}`:chatContactName(m.sender_id);const body=m.body||(m.attachment_mime&&m.attachment_mime.startsWith("audio/")?"Sent a voice message":m.attachment_name?`Sent a file: ${m.attachment_name}`:"New message");const iconLink=document.querySelector('link[rel="icon"]');let n;try{n=new Notification(title,{body,icon:iconLink?iconLink.href:void 0,tag:`chat-${key}`})}catch(e){return}n.onclick=()=>{window.focus();$("chatPanel").style.display="flex";openChatThread(key);n.close()}}function chatMaybeNotify(key,m){if(m.sender_id===myUserId)return;const activelyViewing=chatActiveConv===key&&$("chatPanel").style.display!=="none"&&document.hasFocus();const mentionsMe=chatMessageMentionsMe(m,key);const where=key==="team"?"Team chat":key.startsWith("group:")?chatConvDisplayName(key):chatContactName(m.sender_id);if(!activelyViewing){pushStaffNotification({icon:mentionsMe?"📣":"💬",priority:mentionsMe,title:mentionsMe?`${chatContactName(m.sender_id)} mentioned you${key==="team"||key.startsWith("group:")?" in "+where:""}`:where,body:m.body||(m.attachment_name?`Sent a file: ${m.attachment_name}`:"New message"),onClick:()=>{$("chatPanel").style.display="flex";openChatThread(key)},silent:!mentionsMe||chatIsMyDnd()})}if(activelyViewing)return;chatPlayNotificationSound();chatShowDesktopNotification(key,m)}function profileMenuInitials(name,email){const src=(name||email||"").trim();if(!src)return"—";const parts=src.split(/\s+/).filter(Boolean);if(parts.length>=2)return(parts[0][0]+parts[1][0]).toUpperCase();return src.slice(0,2).toUpperCase()}function renderProfileMenu(){const name=myFullName||myEmail||"";const initials=profileMenuInitials(myFullName,myEmail);const setText=(id,v)=>{const el=$(id);if(el)el.textContent=v};setText("profileMenuInitials",initials);setText("profileMenuAvatar",initials);if(myAvatarUrl){const img=`<img src="${escapeHtml(myAvatarUrl)}" alt="">`;if($("profileMenuAvatar"))$("profileMenuAvatar").innerHTML=img;if($("profileMenuInitials"))$("profileMenuInitials").innerHTML=img}setText("profileMenuName",name);setText("profileMenuEmail",myEmail||"");const roleEl=$("profileMenuRole");if(roleEl){const line=[myTitle,myDepartment].filter(Boolean).join(" · ");roleEl.textContent=line||(typeof roleDisplayLabel==="function"?roleDisplayLabel(myRole||""):myRole||"")}const btn=$("profileMenuBtn");if(btn)btn.title=[name,myTitle,myDepartment].filter(Boolean).join(" — ")||"Your profile"}function profileMenuSetOpen(open){const panel=$("profileMenuPanel"),btn=$("profileMenuBtn");if(!panel||!btn)return;panel.style.display=open?"block":"none";btn.setAttribute("aria-expanded",open?"true":"false");if(open){renderProfileMenu();pmGoogleRefresh()}}(function wireProfileMenu(){const btn=$("profileMenuBtn");if(!btn)return;btn.addEventListener("click",e=>{e.stopPropagation();profileMenuSetOpen($("profileMenuPanel").style.display==="none")});document.addEventListener("click",e=>{const wrap=$("profileMenuWrap");if(wrap&&!wrap.contains(e.target))profileMenuSetOpen(false)});document.addEventListener("keydown",e=>{if(e.key==="Escape")profileMenuSetOpen(false)})})();async function pmGoogleCall(action){const{data,error}=await sb.functions.invoke("dialer-conversations",{body:{action}});if(error){let msg=error.message||"Request failed";const status=error.context&&error.context.status;try{const b=await error.context.json();if(b&&b.error)msg=b.error}catch(e){}return{ok:false,error:msg,status}}return data||{ok:false,error:"No answer from the server"}}async function pmGoogleRefresh(){const sec=$("pmGoogle");if(!sec)return;const r=await pmGoogleCall("status");if(!r.ok){if(r.status===403){sec.style.display="none";return}sec.style.display="block";$("pmGoogleState").textContent="Could not check your Google connection.";$("pmGoogleConnect").style.display="none";$("pmGoogleDisconnect").style.display="none";return}sec.style.display="block";const partial=r.connected&&(!r.can_read||!r.can_calendar);$("pmGoogleState").innerHTML=r.connected?`Connected as <b>${escapeHtml(r.email||"your Google account")}</b>${partial?" — some permissions are missing":""}`:"Not connected. Use your @proptechnologyai.com Google account.";$("pmGoogleConnect").textContent=partial?"Reconnect for the rest":"Connect Google";$("pmGoogleConnect").style.display=!r.connected||partial?"":"none";$("pmGoogleDisconnect").style.display=r.connected?"":"none"}(function wirePmGoogle(){if(!$("pmGoogleConnect"))return;$("pmGoogleConnect").addEventListener("click",async e=>{e.stopPropagation();const w=window.open("","_blank");const r=await pmGoogleCall("connect_url");if(!r.ok||!r.url){if(w)w.close();$("pmGoogleState").textContent=r.error||"Could not start the Google connection.";return}if(w)w.location=r.url;else location.href=r.url;$("pmGoogleState").textContent="Finish in the Google tab, then come back here."});$("pmGoogleDisconnect").addEventListener("click",async e=>{e.stopPropagation();if(!confirm("Disconnect your Google account? Automatic emails, the email side of Conversations and calendar invitations all stop until you connect it again."))return;$("pmGoogleDisconnect").disabled=true;const r=await pmGoogleCall("disconnect");$("pmGoogleDisconnect").disabled=false;if(!r.ok){$("pmGoogleState").textContent="Could not disconnect: "+(r.error||"unknown error");return}await pmGoogleRefresh()});window.addEventListener("focus",()=>{const panel=$("profileMenuPanel");if(panel&&panel.style.display!=="none")pmGoogleRefresh()})})();let staffNotifications=[];const STAFF_NOTIF_MAX=300;const STAFF_NC_KEEP_MS=30*24*3600*1e3;let staffNotifRestored=false;let staffNotifUnread=0;let staffNotifOpen=false;function pushStaffNotification({icon,title,body,onClick,silent,durableId,at,priority,readAt}){const id=`n${Date.now()}${Math.random().toString(36).slice(2,6)}`;staffNotifications.unshift({id,icon,title,body,onClick,durableId,priority:priority===true,readAt:readAt||null,at:at||Date.now()});staffNotifications.sort((a,b)=>(b.at||0)-(a.at||0));if(staffNotifications.length>STAFF_NOTIF_MAX)staffNotifications.length=STAFF_NOTIF_MAX;if(!staffNotifOpen&&!readAt)staffNotifUnread++;renderNotifCenter();staffNotifSave();if(!durableId)staffNotifPersist(staffNotifications.find(n=>n.id===id));if(!silent)staffPlayOpsNotificationSound();if(!silent&&!document.hasFocus())pushSelfNotification({title,body,tag:id})}function pushSelfNotification({title,body,tag}){if(!myUserId)return;sb.functions.invoke("send-push-notification",{body:{user_id:myUserId,title,body,tag,url:"/"}}).then(({error})=>{if(error)console.error("send-push-notification (self) failed",error)}).catch(err=>console.error("send-push-notification (self) call failed",err))}function staffNotifKey(){return myUserId?"ncHistory:staff:"+myUserId:null}function staffNotifSave(){const key=staffNotifKey();if(!key||!staffNotifRestored)return;const cutoff=Date.now()-STAFF_NC_KEEP_MS;const keep=staffNotifications.filter(n=>!n.durableId&&(n.at||0)>=cutoff).map(n=>({id:n.id,icon:n.icon||null,title:n.title||"",body:n.body||"",priority:n.priority===true,readAt:n.readAt||null,at:n.at||Date.now()}));try{localStorage.setItem(key,JSON.stringify(keep))}catch(e){}}function staffNotifRestore(){const key=staffNotifKey();if(!key||staffNotifRestored)return;let saved=[];try{saved=JSON.parse(localStorage.getItem(key)||"[]")}catch(e){saved=[]}const cutoff=Date.now()-STAFF_NC_KEEP_MS;const have=new Set(staffNotifications.map(n=>n.id));(Array.isArray(saved)?saved:[]).filter(n=>n&&n.id&&(n.at||0)>=cutoff&&!have.has(n.id)).forEach(n=>staffNotifications.push(n));staffNotifications.sort((a,b)=>(b.at||0)-(a.at||0));if(staffNotifications.length>STAFF_NOTIF_MAX)staffNotifications.length=STAFF_NOTIF_MAX;staffNotifRestored=true;staffNotifSave();renderNotifCenter();staffNotifications.filter(n=>!n.durableId).forEach(staffNotifPersist)}function staffNotifPersist(n){if(!n||n.durableId||n.persisting||!myUserId||!(window.crypto&&crypto.randomUUID))return;n.persisting=true;const rowId=crypto.randomUUID();shownDurableNotifIds.add(rowId);sb.from("staff_notifications").insert({id:rowId,user_id:myUserId,kind:"portal",title:String(n.title||"").slice(0,300),body:n.body?String(n.body).slice(0,2e3):null,icon:n.icon||null,priority:n.priority===true,created_at:new Date(n.at||Date.now()).toISOString(),read_at:n.readAt?new Date(n.readAt).toISOString():null}).then(({error})=>{n.persisting=false;if(error){console.warn("notification not saved to the server:",error.message);return}n.durableId=rowId;if(n.readAt)markStaffNotificationsRead([rowId]);staffNotifSave()})}function staffNotifDuplicate(row){const at=row.created_at?new Date(row.created_at).getTime():Date.now();return staffNotifications.find(n=>n.durableId!==row.id&&(n.title||"")===(row.title||"")&&(n.body||"")===(row.body||"")&&Math.abs((n.at||0)-at)<12e4)||null}var NC_ADAPTER={items:function(){return staffNotifications},refreshBadge:function(){staffNotifUnread=(staffNotifications||[]).filter(ncIsUnread).length;var badge=$("notifBellBadge"),bell=$("notifBellBtn");if(badge){badge.textContent=staffNotifUnread>99?"99+":String(staffNotifUnread);badge.style.display=staffNotifUnread>0?"flex":"none"}if(bell)bell.classList.toggle("ringing",staffNotifUnread>0)},onRead:function(n){var ids=[n.durableId].concat(n.dupIds||[]).filter(Boolean);if(ids.length&&typeof markStaffNotificationsRead==="function")markStaffNotificationsRead(ids);staffNotifSave()},onMarkAllRead:function(){var ids=[];(staffNotifications||[]).forEach(function(n){if(n.durableId)ids.push(n.durableId);(n.dupIds||[]).forEach(function(d){ids.push(d)})});if(ids.length&&typeof markStaffNotificationsRead==="function")markStaffNotificationsRead(ids);staffNotifSave()},onClose:function(){staffNotifOpen=false}};var ncTab="priority";var ncUnreadOnly=false;var ncDetailId=null;function ncIsPriority(n){return n&&n.priority===true}function ncIsUnread(n){return!n||!n.readAt}function ncRelTime(ts){var diff=Date.now()-ts;var mins=Math.floor(diff/6e4);if(mins<1)return"Just now";if(mins<60)return mins+(mins===1?" minute ago":" minutes ago");var hours=Math.floor(mins/60);if(hours<24)return hours+(hours===1?" hour ago":" hours ago");var days=Math.floor(hours/24);return days+(days===1?" day ago":" days ago")}function ncVisibleItems(){var all=NC_ADAPTER.items()||[];return all.filter(function(n){if(ncTab==="priority"?!ncIsPriority(n):ncIsPriority(n))return false;if(ncUnreadOnly&&!ncIsUnread(n))return false;return true})}function ncChrome(show){[".nc-controls",".nc-tabs"].forEach(function(sel){var el=document.querySelector(sel);if(el)el.style.display=show?"":"none"})}function ncRenderList(){var list=document.getElementById("notifCenterList");var detail=document.getElementById("ncDetail");if(!list)return;if(ncDetailId){ncRenderDetail();return}if(detail)detail.style.display="none";list.style.display="block";ncChrome(true);document.querySelectorAll(".nc-tab").forEach(function(b){b.classList.toggle("active",b.dataset.ncTab===ncTab)});var sw=document.getElementById("ncUnreadSwitch");if(sw)sw.setAttribute("aria-checked",ncUnreadOnly?"true":"false");var items=ncVisibleItems();if(!items.length){list.innerHTML='<div class="notif-center-empty">No notifications</div>';ncPositionPanel();return}list.innerHTML=items.map(function(n){var readCls=ncIsUnread(n)?"":" is-read";var bodyHtml=n.body?'<div class="notif-center-item-body">'+escapeHtml(n.body)+"</div>":"";return'<div class="notif-center-item'+readCls+'" data-notif-id="'+n.id+'"><span class="nc-dot"></span><div class="notif-center-item-title">'+escapeHtml(n.title||"")+"</div>"+bodyHtml+'<div class="notif-center-item-time">'+ncRelTime(n.at||n.time||Date.now())+"</div></div>"}).join("");list.querySelectorAll(".notif-center-item").forEach(function(el){el.addEventListener("click",function(){ncOpenDetail(el.dataset.notifId)})});ncPositionPanel()}function ncOpenDetail(id){var n=(NC_ADAPTER.items()||[]).find(function(x){return String(x.id)===String(id)});if(!n)return;if(ncIsUnread(n)){n.readAt=Date.now();if(NC_ADAPTER.onRead)NC_ADAPTER.onRead(n)}if(NC_ADAPTER.refreshBadge)NC_ADAPTER.refreshBadge();if(typeof n.onClick==="function"){var panel=document.getElementById("notifCenterPanel");if(panel)panel.style.display="none";ncDetailId=null;if(NC_ADAPTER.onClose)NC_ADAPTER.onClose();try{n.onClick()}catch(e){console.error("notification onClick failed",e)}return}ncDetailId=id;ncRenderDetail()}function ncRenderDetail(){var list=document.getElementById("notifCenterList");var detail=document.getElementById("ncDetail");if(!detail)return;var n=(NC_ADAPTER.items()||[]).find(function(x){return String(x.id)===String(ncDetailId)});if(!n){ncDetailId=null;ncRenderList();return}if(list)list.style.display="none";detail.style.display="block";ncChrome(false);var created=new Date(n.at||n.time||Date.now());var createdLabel=created.toLocaleString([],{month:"long",day:"numeric",year:"numeric",hour:"2-digit",minute:"2-digit"});var bodyHtml=n.body?'<div class="nc-detail-body">'+escapeHtml(n.body)+"</div>":"";var goHtml=n.onClick?'<button type="button" class="nc-markall" id="ncGoBtn" style="margin-top:16px; background:var(--focus); color:#1A1206; justify-content:center; font-weight:700;">Open</button>':"";detail.innerHTML='<button type="button" class="nc-back" id="ncBackBtn">&#8592; All notifications</button><div class="nc-detail-card"><div class="nc-detail-title">'+escapeHtml(n.title||"")+'</div><div class="nc-detail-created">Created at: '+escapeHtml(createdLabel)+"</div>"+bodyHtml+goHtml+"</div>";var back=document.getElementById("ncBackBtn");if(back)back.addEventListener("click",function(){ncDetailId=null;ncRenderList()});var go=document.getElementById("ncGoBtn");if(go)go.addEventListener("click",function(){var panel=document.getElementById("notifCenterPanel");if(panel)panel.style.display="none";ncDetailId=null;if(n.onClick)n.onClick()})}function ncPositionPanel(){var panel=document.getElementById("notifCenterPanel");var wrap=document.querySelector(".notif-center-wrap");if(!panel||!wrap)return;if(getComputedStyle(panel).display==="none")return;var GUTTER=12;panel.style.right="0px";panel.style.left="auto";var r=panel.getBoundingClientRect();if(r.left<GUTTER){panel.style.right=r.left-GUTTER+"px"}else if(r.right>window.innerWidth-GUTTER){panel.style.right=r.right-window.innerWidth+GUTTER+"px"}}window.addEventListener("resize",ncPositionPanel);function ncMarkAllRead(){(NC_ADAPTER.items()||[]).forEach(function(n){if(!n.readAt)n.readAt=Date.now()});if(NC_ADAPTER.onMarkAllRead)NC_ADAPTER.onMarkAllRead();if(NC_ADAPTER.refreshBadge)NC_ADAPTER.refreshBadge();ncRenderList()}(function ncWire(){function on(id,ev,fn){var el=document.getElementById(id);if(el)el.addEventListener(ev,fn)}document.querySelectorAll(".nc-tab").forEach(function(b){b.addEventListener("click",function(){ncTab=b.dataset.ncTab;ncDetailId=null;ncRenderList()})});on("ncUnreadSwitch","click",function(e){e.preventDefault();ncUnreadOnly=!ncUnreadOnly;ncDetailId=null;ncRenderList()});on("ncMoreBtn","click",function(e){e.stopPropagation();var m=document.getElementById("ncMoreMenu");if(!m)return;var open=m.style.display==="none";m.style.display=open?"block":"none";e.currentTarget.setAttribute("aria-expanded",open?"true":"false")});on("ncMarkAllBtn","click",function(){var m=document.getElementById("ncMoreMenu");if(m)m.style.display="none";ncMarkAllRead()});on("ncCloseBtn","click",function(){var p=document.getElementById("notifCenterPanel");if(p)p.style.display="none";ncDetailId=null;if(NC_ADAPTER.onClose)NC_ADAPTER.onClose()});document.addEventListener("click",function(e){var w=document.querySelector(".nc-more-wrap");var m=document.getElementById("ncMoreMenu");if(m&&w&&!w.contains(e.target))m.style.display="none"})})();function renderNotifCenter(){NC_ADAPTER.refreshBadge();ncRenderList()}function staffPlayOpsNotificationSound(){try{if(!chatAudioCtx)chatAudioCtx=new(window.AudioContext||window.webkitAudioContext);if(chatAudioCtx.state==="suspended")chatAudioCtx.resume();const notes=[{freq:587.33,type:"sine",at:0,dur:.11,vol:.13},{freq:739.99,type:"sine",at:.09,dur:.11,vol:.13},{freq:987.77,type:"sine",at:.18,dur:.2,vol:.15}];notes.forEach(n=>{const start=chatAudioCtx.currentTime+n.at;const osc=chatAudioCtx.createOscillator();const gain=chatAudioCtx.createGain();osc.type=n.type;osc.frequency.value=n.freq;gain.gain.setValueAtTime(0,start);gain.gain.linearRampToValueAtTime(n.vol,start+.012);gain.gain.exponentialRampToValueAtTime(1e-4,start+n.dur);osc.connect(gain).connect(chatAudioCtx.destination);osc.start(start);osc.stop(start+n.dur+.02)})}catch(e){console.error("Ops notification sound failed",e)}}["pointerdown","keydown","touchstart"].forEach(ev=>document.addEventListener(ev,()=>{try{if(!chatAudioCtx)chatAudioCtx=new(window.AudioContext||window.webkitAudioContext);if(chatAudioCtx.state==="suspended")chatAudioCtx.resume()}catch(e){}},{capture:true,passive:true}));function chatPlayOfflineHintSound(){try{if(!chatAudioCtx)chatAudioCtx=new(window.AudioContext||window.webkitAudioContext);if(chatAudioCtx.state==="suspended")chatAudioCtx.resume();const start=chatAudioCtx.currentTime;const osc=chatAudioCtx.createOscillator();const gain=chatAudioCtx.createGain();osc.type="sine";osc.frequency.value=392;gain.gain.setValueAtTime(0,start);gain.gain.linearRampToValueAtTime(.08,start+.015);gain.gain.exponentialRampToValueAtTime(1e-4,start+.18);osc.connect(gain).connect(chatAudioCtx.destination);osc.start(start);osc.stop(start+.2)}catch(e){console.error("Offline hint sound failed",e)}}function initNotifCenter(){const bellBtn=$("notifBellBtn");const panel=$("notifCenterPanel");const clearBtn=$("notifClearBtn");if(!bellBtn||bellBtn.dataset.wired)return;bellBtn.dataset.wired="1";bellBtn.addEventListener("click",e=>{e.stopPropagation();staffNotifOpen=panel.style.display==="none";panel.style.display=staffNotifOpen?"block":"none";if(staffNotifOpen){renderNotifCenter()}});document.addEventListener("click",e=>{if(panel.style.display!=="none"&&!panel.contains(e.target)&&e.target!==bellBtn){panel.style.display="none";staffNotifOpen=false}});clearBtn.addEventListener("click",()=>{markStaffNotificationsRead(staffNotifications.map(n=>n.durableId).filter(Boolean));staffNotifications=[];staffNotifUnread=0;renderNotifCenter()});renderNotifCenter()}const STAFF_NOTIF_KINDS=[{key:"ticket_new",label:"New ticket to review"},{key:"lead_submitted",label:"New lead submitted (Leads Queue Review)"},{key:"lead_submitted_primehome",label:"New lead submitted (Leads Queue Review — PrimeHome Buyers)"},{key:"ticket_accepted",label:"Client accepted a ticket"},{key:"ticket_declined",label:"Client declined a ticket"},{key:"dispute_reply",label:"Client reply on a dispute/feedback thread"},{key:"campaign_guide_edit",label:"Campaign Guide edit proposed"},{key:"campaign_requested",label:"New campaign requested"},{key:"billing_activity",label:"Billing activity (new one-time charge)"},{key:"list_order_requested",label:'New "Buy a list" request'},{key:"skiptrace_order_paid",label:"Skip trace order placed"},{key:"client_sms_reply",label:"Client replied via SMS"},{key:"gmail_new_message",label:"New email received (Gmail Inbox)"},{key:"hr_candidate_submitted",label:"New HR candidate submitted for review"},{key:"hr_candidate_approved",label:"HR candidate submission approved"},{key:"hr_candidate_rejected",label:"HR candidate submission rejected"},{key:"hr_candidate_closed",label:"HR candidate account closed (terminated/found other offer)"},{key:"appointment_booked",label:"New appointment booked"},{key:"new_client",label:"New client account created"}];let staffNotifRolesCache={};async function fetchStaffNotifRolesConfig(){const keys=STAFF_NOTIF_KINDS.map(k=>`staff_notif_roles_${k.key}`);const{data:rows}=await sb.from("app_settings").select("key, value").in("key",keys);const byKey={};(rows||[]).forEach(r=>{byKey[r.key]=r.value});const cfg={};STAFF_NOTIF_KINDS.forEach(k=>{const raw=byKey[`staff_notif_roles_${k.key}`];let arr=null;if(raw!=null){try{const parsed=JSON.parse(raw);if(Array.isArray(parsed))arr=parsed}catch{}}cfg[k.key]=arr});staffNotifRolesCache=cfg;return cfg}function staffNotifKindAllowed(kind){const arr=staffNotifRolesCache[kind];if(!arr)return true;return arr.includes(myRole)}const STAFF_NOTIF_KIND_ICONS={dialer_follow_up:"📞",missed_call:"📞",dialer_handoff:"🤝",dialer_appointment:"📅",dialer_task:"✅",client_chat_queue:"💬",leads_import:"📥",call_back_assigned:"↩️"};const STAFF_NOTIF_PRIORITY_KINDS=new Set(["dialer_follow_up","dialer_handoff","dialer_appointment","dialer_task","missed_call","client_chat_queue","call_back_assigned"]);const shownDurableNotifIds=new Set;function showDurableStaffNotification(row,silent){if(!row||shownDurableNotifIds.has(row.id))return;shownDurableNotifIds.add(row.id);const dup=staffNotifDuplicate(row);if(dup){(dup.dupIds=dup.dupIds||[]).push(row.id);if(dup.readAt&&!row.read_at)markStaffNotificationsRead([row.id]);else if(row.read_at&&!dup.readAt){dup.readAt=new Date(row.read_at).getTime();if(dup.durableId)markStaffNotificationsRead([dup.durableId]);renderNotifCenter()}return}pushStaffNotification({icon:row.icon||STAFF_NOTIF_KIND_ICONS[row.kind]||"🔔",title:row.title,body:row.body||"",durableId:row.id,priority:row.priority===true||STAFF_NOTIF_PRIORITY_KINDS.has(row.kind),at:row.created_at?new Date(row.created_at).getTime():Date.now(),readAt:row.read_at?new Date(row.read_at).getTime():null,silent,onClick:row.kind==="portal"?void 0:()=>{markStaffNotificationsRead([row.id]);if(row.kind==="client_chat_queue"&&row.ref_id&&ciCanUseInbox()){ciOpenFromElsewhere(row.ref_id);return}if(row.kind==="ops_alert"&&opsHealthCanView()){activateTab("ops_health");return}if(row.kind==="leads_import"){activateTab("leads");activateLeadsSection("queue_review_ispeedtolead");return}if(row.kind==="call_back_assigned"){leadsActiveSection="call_back";activateTab("leads");activateLeadsSection("call_back");return}if(row.kind==="missed_call"&&row.ref_id){voiceCallBackFromCallId(row.ref_id);return}if(row.url)window.open(row.url,"_blank")}})}async function loadDurableStaffNotifications(){if(!myUserId)return;const{data,error}=await sb.from("staff_notifications").select("id, kind, title, body, url, ref_id, created_at, read_at, icon, priority").eq("user_id",myUserId).gte("created_at",new Date(Date.now()-STAFF_NC_KEEP_MS).toISOString()).order("created_at",{ascending:false}).limit(STAFF_NOTIF_MAX);if(error){console.error("staff_notifications load failed",error);return}(data||[]).forEach(row=>showDurableStaffNotification(row,true))}function markStaffNotificationsRead(ids){if(!ids||!ids.length)return;sb.from("staff_notifications").update({read_at:new Date().toISOString()}).in("id",ids).is("read_at",null).then(({error})=>{if(error)console.error("staff_notifications mark read failed",error)})}let staffOpsChannel=null;async function subscribeStaffOpsRealtime(){if(staffOpsChannel)return;staffNotifRestore();await fetchStaffNotifRolesConfig();loadDurableStaffNotifications();staffOpsChannel=sb.channel("staff-ops-notifications").on("postgres_changes",{event:"INSERT",schema:"public",table:"leads"},payload=>{const lead=payload.new;if(lead.ticket_stage==="staff_review"){if(!staffNotifKindAllowed("ticket_new"))return;pushStaffNotification({priority:true,icon:"🎫",title:"New ticket to review",body:lead.seller_name||lead.address||"A new ticket needs staff review before it goes to the client.",onClick:()=>activateTab("leads")});return}const fromFile=!!(lead.imported||lead.last_import_batch);const prime=!lead.ticket_stage&&isPrimeHomeBuyersLead(lead);if(!lead.ticket_stage&&!fromFile&&!isSalesConversionLead(lead)&&staffNotifKindAllowed(prime?"lead_submitted_primehome":"lead_submitted")){pushStaffNotification({priority:true,icon:"📥",title:prime?"New PrimeHome Buyers lead submitted":"New lead submitted",body:lead.seller_investor_name?`${lead.seller_investor_name} — awaiting review.`:prime?"A new lead needs review in the PrimeHome Buyers queue.":"A new lead needs review in Leads Queue Review.",onClick:()=>{activateTab("leads");activateLeadsSection(prime?"queue_review_ispeedtolead":"queue_review")}})}}).on("postgres_changes",{event:"UPDATE",schema:"public",table:"leads"},payload=>{const lead=payload.new;if(lead.ticket_stage==="recording_pending"){if(!staffNotifKindAllowed("ticket_accepted"))return;pushStaffNotification({icon:"✅",title:"Client accepted a ticket",body:`${lead.seller_name||lead.address||"A ticket"} needs a recording attached.`,onClick:()=>activateTab("leads")})}else if(lead.ticket_stage==="client_rejected"){if(!staffNotifKindAllowed("ticket_declined"))return;pushStaffNotification({icon:"❌",title:"Client declined a ticket",body:`${lead.seller_name||lead.address||"A ticket"} was declined by the client.`,onClick:()=>activateTab("leads")})}}).on("postgres_changes",{event:"INSERT",schema:"public",table:"lead_dispute_messages"},payload=>{const msg=payload.new;if(msg.sender_type!=="client")return;if(!staffNotifKindAllowed("dispute_reply"))return;pushStaffNotification({priority:true,icon:"⚠️",title:"Client reply on a dispute",body:msg.message||"New message on a lead dispute/feedback thread.",onClick:()=>activateTab("client_disputes")})}).on("postgres_changes",{event:"INSERT",schema:"public",table:"campaign_guide_edit_requests"},payload=>{const req=payload.new;if(!staffNotifKindAllowed("campaign_guide_edit"))return;pushStaffNotification({priority:true,icon:"📝",title:"Campaign Guide edit proposed",body:req.campaign_display_name?`${req.campaign_display_name}${req.note?" — "+req.note:""}`:"A client proposed changes to a Campaign Guide.",onClick:()=>{activateTab("client_submissions");activateCsubSection("campaign_guide")}})}).on("postgres_changes",{event:"INSERT",schema:"public",table:"ghl_form_submissions"},payload=>{const sub=payload.new;if(sub.form_name==="Client Portal — New Campaign Request"){if(!staffNotifKindAllowed("campaign_requested"))return;const campaignName=sub.fields&&(sub.fields["Campaign Name (Criteria)"]||sub.fields["Campaign Name"])||null;pushStaffNotification({priority:true,icon:"📣",title:"New campaign requested",body:campaignName?`${campaignName} — submitted by ${sub.contact_name||sub.contact_email||"a client"}.`:`A client submitted a new campaign request.`,onClick:()=>{activateTab("client_submissions");activateCsubSection("campaign_guide")}});return}if(sub.form_name==="Client Portal — Buy a List Request"){if(!staffNotifKindAllowed("list_order_requested"))return;pushStaffNotification({priority:true,icon:"🗂️",title:'New "Buy a list" request',body:sub.fields&&sub.fields["Number of Records Requested"]?`${sub.fields["Number of Records Requested"]} records — submitted by ${sub.contact_name||sub.contact_email||"a client"}.`:`A client submitted a "Buy a list" request.`,onClick:()=>{activateTab("data_science");activateDataSciSection("data")}})}}).on("postgres_changes",{event:"INSERT",schema:"public",table:"client_one_off_charges"},payload=>{const charge=payload.new;if(!staffNotifKindAllowed("billing_activity"))return;pushStaffNotification({icon:"💳",title:"Billing activity — new one-time charge",body:`$${((charge.amount_cents||0)/100).toFixed(2)}${charge.description?" — "+charge.description:""}`,onClick:()=>{activateTab("finance");activateFinanceSection("stripe_payments")}})}).on("postgres_changes",{event:"INSERT",schema:"public",table:"public_skiptrace_orders"},payload=>{const order=payload.new;if(order.status!=="paid")return;if(!staffNotifKindAllowed("skiptrace_order_paid"))return;pushStaffNotification({priority:true,icon:"🔎",title:"Skip trace order placed",body:`${order.record_count} record${order.record_count===1?"":"s"} — ${order.email||"a client"}.`,onClick:()=>{activateTab("data_science");activateDataSciSection("skiptrace")}})}).on("postgres_changes",{event:"UPDATE",schema:"public",table:"public_skiptrace_orders"},payload=>{const order=payload.new;if(order.status!=="paid")return;if(!staffNotifKindAllowed("skiptrace_order_paid"))return;pushStaffNotification({priority:true,icon:"🔎",title:"Skip trace order placed",body:`${order.record_count} record${order.record_count===1?"":"s"} — ${order.email||"a client"}.`,onClick:()=>{activateTab("data_science");activateDataSciSection("skiptrace")}})}).on("postgres_changes",{event:"INSERT",schema:"public",table:"ghl_appointments"},payload=>{const appt=payload.new;if(!staffNotifKindAllowed("appointment_booked"))return;pushStaffNotification({icon:"📅",title:"New appointment booked",body:appt.contact_name?`${appt.contact_name} — ${chatGhlFmtWhen(appt.start_time)}`:appt.title||"A new appointment was booked."})}).on("postgres_changes",{event:"INSERT",schema:"public",table:"hr_candidates"},payload=>{const cand=payload.new;if(cand.submitted_by===myUserId)return;if(!staffNotifKindAllowed("hr_candidate_submitted"))return;pushStaffNotification({priority:true,icon:"🧑‍💼",title:"New candidate submitted for review",body:cand.full_name?`${cand.full_name} is awaiting review.`:"A new candidate submission is awaiting review.",onClick:()=>{activateTab("hr");activateHrSection("review")}})}).on("postgres_changes",{event:"UPDATE",schema:"public",table:"hr_candidates"},payload=>{const cand=payload.new;if(cand.submitted_by!==myUserId)return;if(cand.status==="approved"){if(!staffNotifKindAllowed("hr_candidate_approved"))return;pushStaffNotification({icon:"✅",title:"Candidate submission approved",body:`${cand.full_name||"Your candidate"} was approved.`,onClick:()=>{activateTab("hr");activateHrSection("submission")}})}else if(cand.status==="rejected"){if(!staffNotifKindAllowed("hr_candidate_rejected"))return;pushStaffNotification({icon:"❌",title:"Candidate submission rejected",body:cand.rejection_reason?`${cand.full_name||"Your candidate"} was rejected — ${cand.rejection_reason}`:`${cand.full_name||"Your candidate"} was rejected.`,onClick:()=>{activateTab("hr");activateHrSection("submission")}})}else if(cand.status==="terminated"||cand.status==="found_other_offer"){if(!staffNotifKindAllowed("hr_candidate_closed"))return;const statusLabel=cand.status==="found_other_offer"?"found another offer":"was terminated";pushStaffNotification({icon:"🧑‍💼",title:"Candidate account closed",body:cand.termination_reason?`${cand.full_name||"Your candidate"} ${statusLabel} — ${cand.termination_reason}`:`${cand.full_name||"Your candidate"} ${statusLabel}.`,onClick:()=>{activateTab("hr");activateHrSection("submission")}})}}).on("postgres_changes",{event:"INSERT",schema:"public",table:"client_accounts"},payload=>{const client=payload.new;if(!staffNotifKindAllowed("new_client"))return;pushStaffNotification({icon:"🆕",title:"New client account created",body:client.company_name?`${client.full_name} — ${client.company_name}`:client.full_name||client.email||"A new client account was created.",onClick:()=>{activateTab("usermanagement");activateUmSection("clients")}})}).on("postgres_changes",{event:"INSERT",schema:"public",table:"staff_notifications",filter:`user_id=eq.${myUserId}`},payload=>{showDurableStaffNotification(payload.new,false)}).subscribe()}const VAPID_PUBLIC_KEY="BIR9anFzwzF5xt9h4L7_A2JJq0DDymhYk4HMAjrkEtpTWb8qy0roGCBQHaczOvPMwxqTSoNBc9VvutsW9L5rfFk";function urlBase64ToUint8Array(base64String){const padding="=".repeat((4-base64String.length%4)%4);const base64=(base64String+padding).replace(/-/g,"+").replace(/_/g,"/");const rawData=atob(base64);const outputArray=new Uint8Array(rawData.length);for(let i=0;i<rawData.length;i++)outputArray[i]=rawData.charCodeAt(i);return outputArray}function pushSupported(){return"serviceWorker"in navigator&&"PushManager"in window&&VAPID_PUBLIC_KEY&&VAPID_PUBLIC_KEY.indexOf("REPLACE_WITH")!==0}async function updatePushToggleBtn(){const btn=$("pushToggleBtn");if(!btn)return;if(!pushSupported()){btn.style.display="none";return}let subscribed=false;try{const reg=await navigator.serviceWorker.getRegistration();const sub=reg?await reg.pushManager.getSubscription():null;subscribed=!!sub}catch(e){}btn.textContent=subscribed?"🔔":"🔕";btn.title=subscribed?"Push notifications on — click to turn off":"Enable push notifications (works even when this tab is closed)"}async function pushSubscribeIfNeeded(){if(!pushSupported())return;try{const permission=await Notification.requestPermission();if(permission!=="granted")return;const reg=await navigator.serviceWorker.register("sw.js");await navigator.serviceWorker.ready;let sub=await reg.pushManager.getSubscription();if(!sub){sub=await reg.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:urlBase64ToUint8Array(VAPID_PUBLIC_KEY)})}const json=sub.toJSON();await sb.from("push_subscriptions").upsert({user_id:myUserId,endpoint:json.endpoint,p256dh:json.keys.p256dh,auth:json.keys.auth,user_agent:navigator.userAgent,last_seen_at:new Date().toISOString()},{onConflict:"endpoint"})}catch(e){console.error("Push subscribe failed",e)}finally{updatePushToggleBtn()}}async function pushUnsubscribe(){try{const reg=await navigator.serviceWorker.getRegistration();const sub=reg?await reg.pushManager.getSubscription():null;if(sub){await sb.from("push_subscriptions").delete().eq("endpoint",sub.endpoint);await sub.unsubscribe()}}catch(e){console.error("Push unsubscribe failed",e)}finally{updatePushToggleBtn()}}function initPushToggle(){const btn=$("pushToggleBtn");if(!btn||btn.dataset.wired)return;btn.dataset.wired="1";btn.addEventListener("click",async()=>{const reg=await navigator.serviceWorker.getRegistration().catch(()=>null);const sub=reg?await reg.pushManager.getSubscription().catch(()=>null):null;if(sub){try{localStorage.setItem("pt.pushOff","1")}catch(e){}await pushUnsubscribe();callNotifyGateCheck()}else{try{localStorage.removeItem("pt.pushOff")}catch(e){}await pushSubscribeIfNeeded()}});updatePushToggleBtn();if(!pushSupported()||!("Notification"in window))return;if(staffImpersonating)return;let off=false,asked=false;try{off=localStorage.getItem("pt.pushOff")==="1";asked=localStorage.getItem("pt.pushAsked")==="1"}catch(e){}if(off)return;if(Notification.permission==="granted"){pushSubscribeIfNeeded();return}if(Notification.permission!=="default"||asked)return;const ask=()=>{["pointerup","keydown"].forEach(ev=>document.removeEventListener(ev,ask,true));try{localStorage.setItem("pt.pushAsked","1")}catch(e){}pushSubscribeIfNeeded()};["pointerup","keydown"].forEach(ev=>document.addEventListener(ev,ask,true))}const CALL_NOTIFY_REQUIRED_ROLES=new Set(["agent","sales"]);let callNotifyGateBusy=false;let callNotifyPermWatch=false;let callNotifySkippedUnsupported=false;async function callNotifyHasSubscription(){try{const reg=await navigator.serviceWorker.getRegistration();const sub=reg?await reg.pushManager.getSubscription():null;if(!sub)return false;const{data,error}=await sb.from("push_subscriptions").select("endpoint").eq("user_id",myUserId).eq("endpoint",sub.endpoint).maybeSingle();return!error&&!!data}catch(e){return false}}async function callNotifyGateCheck(){if(callNotifyGateBusy)return;if(staffImpersonating||!myUserId||!CALL_NOTIFY_REQUIRED_ROLES.has(myRole)){callNotifyGateHide();return}callNotifyGateBusy=true;try{if(!callNotifyPermWatch&&navigator.permissions&&navigator.permissions.query){callNotifyPermWatch=true;navigator.permissions.query({name:"notifications"}).then(s=>{s.onchange=()=>callNotifyGateCheck()}).catch(()=>{})}const supported=pushSupported()&&"Notification"in window;if(!supported){if(callNotifySkippedUnsupported)callNotifyGateHide();else callNotifyGateShow("unsupported");return}if(Notification.permission==="granted"){try{localStorage.removeItem("pt.pushOff")}catch(e){}if(!await callNotifyHasSubscription())await pushSubscribeIfNeeded();if(await callNotifyHasSubscription()){callNotifyGateHide();return}callNotifyGateShow("failed");return}callNotifyGateShow(Notification.permission==="denied"?"denied":"default")}finally{callNotifyGateBusy=false}}function callNotifyGateHide(){const el=$("callNotifyGate");if(el)el.remove()}function callNotifyGateShow(state){let el=$("callNotifyGate");if(!el){el=document.createElement("div");el.id="callNotifyGate";el.setAttribute("role","dialog");el.setAttribute("aria-modal","true");el.setAttribute("aria-labelledby","callNotifyGateTitle");el.style.cssText="position:fixed; inset:0; z-index:9980; display:flex; align-items:center; justify-content:center; padding:20px; background:rgba(10,12,16,.72); backdrop-filter:blur(4px); -webkit-backdrop-filter:blur(4px);";document.body.appendChild(el)}const p="margin:0 0 12px; font-size:14px; line-height:1.55; color:var(--text-2);";const primary="width:100%; margin-top:6px;";const copy={default:`<p style="${p}">Your role takes calls. Without notifications, a call only rings while this tab is open and in front of you &mdash; and it is missed otherwise.</p><p style="${p}">Click below, then choose <strong>Allow</strong> when your browser asks.</p><button type="button" class="btn-primary" id="callNotifyAllowBtn" style="${primary}">Allow notifications</button>`,denied:`<p style="${p}">Your browser is <strong>blocking</strong> notifications for this site, so calls cannot reach you when the tab is closed.</p><ol style="${p} padding-left:20px;"><li>Click the <strong>padlock</strong> (or the settings icon) at the left of the address bar.</li><li>Set <strong>Notifications</strong> to <strong>Allow</strong>.</li><li>Click the button below.</li></ol><button type="button" class="btn-primary" id="callNotifyReloadBtn" style="${primary}">I&rsquo;ve allowed it &mdash; reload</button>`,failed:`<p style="${p}">Notifications are allowed, but this device did not finish registering for calls.</p><p style="${p}">Try again. If it keeps failing, tell your admin which browser you are using.</p><button type="button" class="btn-primary" id="callNotifyRetryBtn" style="${primary}">Try again</button>`,unsupported:`<p style="${p}">This browser cannot receive call notifications &mdash; common on an iPhone or inside another app.</p><p style="${p}">To take calls, use <strong>Chrome</strong> or <strong>Edge</strong> on a computer.</p><button type="button" class="btn-primary" id="callNotifyContinueBtn" style="${primary}">Continue without call notifications</button>`}[state]||"";el.innerHTML=`<div style="max-width:440px; width:100%; background:var(--card); border:1px solid var(--border); border-radius:16px; padding:26px 24px 18px; color:var(--text); font-family:var(--sans); box-shadow:0 20px 60px rgba(0,0,0,.35);"><div aria-hidden="true" style="font-size:30px; line-height:1; margin-bottom:12px;">&#128276;</div><h2 id="callNotifyGateTitle" style="margin:0 0 12px; font-size:19px; font-weight:700; color:var(--text);">Turn on call notifications</h2>`+copy+`<div style="margin-top:18px; padding-top:12px; border-top:1px solid var(--border); display:flex; justify-content:space-between; align-items:center; gap:10px;"><span style="font-size:12px; color:var(--text-dim);">Required for everyone who takes calls.</span><button type="button" id="callNotifySignOutBtn" style="background:none; border:none; color:var(--text-2); font-size:12.5px; text-decoration:underline; cursor:pointer; padding:0; width:auto; margin:0;">Sign out</button></div></div>`;const allow=$("callNotifyAllowBtn");if(allow)allow.addEventListener("click",async()=>{await pushSubscribeIfNeeded();callNotifyGateCheck()});const reload=$("callNotifyReloadBtn");if(reload)reload.addEventListener("click",()=>location.reload());const retry=$("callNotifyRetryBtn");if(retry)retry.addEventListener("click",()=>callNotifyGateCheck());const cont=$("callNotifyContinueBtn");if(cont)cont.addEventListener("click",()=>{callNotifySkippedUnsupported=true;callNotifyGateHide()});$("callNotifySignOutBtn").addEventListener("click",()=>performSignOut());const first=allow||reload||retry||cont;if(first)setTimeout(()=>{try{first.focus()}catch(e){}},0)}function notifyPushRecipients(userIds,payload,opts){const always=!!(opts&&opts.always);const targets=(userIds||[]).filter(id=>id&&id!==myUserId&&(always||chatContactStatus(id)!=="active"));targets.forEach(id=>{sb.functions.invoke("send-push-notification",{body:{user_id:id,...payload}}).then(({error})=>{if(error)console.error("send-push-notification failed for",id,error)}).catch(err=>console.error("send-push-notification call failed for",id,err))})}function chatAddMessageToState(m,live){const key=chatConvKeyForMessage(m);if(!chatConversations[key])chatConversations[key]={key,messages:[],unread:0};if(!chatConversations[key].messages.some(x=>x.id===m.id)){chatConversations[key].messages.push(m)}const lastReadMs=chatLastRead[key]?new Date(chatLastRead[key]).getTime():0;chatConversations[key].unread=chatConversations[key].messages.filter(x=>x.sender_id!==myUserId&&new Date(x.created_at).getTime()>lastReadMs).length;if(live){chatMaybeNotify(key,m);updateChatUnreadBadge();if(chatActiveConv===key&&$("chatPanel").style.display!=="none"){chatMarkRead(key);renderChatThread()}else if(!chatActiveConv){renderChatConvList($("chatSearchInput").value)}}}function chatSetBadge(id,count){const el=$(id);if(!el)return;el.textContent=count>99?"99+":String(count);el.style.display=count>0?"flex":"none"}function updateChatUnreadBadge(){const ciOn=typeof ciCanUseInbox==="function"&&ciCanUseInbox();const total=Object.entries(chatConversations).reduce((t,[k,c])=>t+(ciOn&&k.startsWith("client:")?0:c.unread),0);if(typeof ciSetNavBadge==="function")ciSetNavBadge(ciUnreadTotal());const badge=$("chatUnreadBadge");badge.textContent=total>99?"99+":String(total);badge.style.display=total>0?"flex":"none";const fab=$("chatFabBtn");if(fab)fab.classList.toggle("has-unread",total>0);chatSetBadge("chatBottomBadgeChats",total);if(AI_ASSISTANT_BOT_ID){const desoukyUnread=chatConversations[AI_ASSISTANT_BOT_ID]&&chatConversations[AI_ASSISTANT_BOT_ID].unread||0;const desoukyBadge=$("desoukyMascotUnreadBadge");if(desoukyBadge){desoukyBadge.textContent=desoukyUnread>99?"99+":String(desoukyUnread);desoukyBadge.style.display=desoukyUnread>0?"flex":"none"}}}function renderChatConvList(filterText){const search=(filterText||"").trim().toLowerCase();const teamConv=chatConversations["team"];const lastOf=c=>c&&c.messages.length?c.messages[c.messages.length-1]:null;const items=[{key:"team",name:"Communication",isTeam:true,isGroup:false,last:lastOf(teamConv),unread:teamConv?teamConv.unread:0}];chatDirectory.filter(p=>!search||(p.full_name||p.email||"").toLowerCase().includes(search)).forEach(p=>{const conv=chatConversations[p.id];items.push({key:p.id,name:p.full_name||p.email,isTeam:false,isGroup:false,last:lastOf(conv),unread:conv?conv.unread:0,statusBadge:chatStatusBadgeHtml(p.id)})});Object.values(chatGroups).filter(g=>!search||g.name.toLowerCase().includes(search)).forEach(g=>{const conv=chatConversations["group:"+g.id];items.push({key:"group:"+g.id,name:g.name,isTeam:false,isGroup:true,memberCount:g.members.length,last:lastOf(conv),unread:conv?conv.unread:0})});if(!ciCanUseInbox())clientChatThreadsDirectory.filter(t=>!search||chatClientThreadName(t.client_id).toLowerCase().includes(search)).forEach(t=>{const key="client:"+t.client_id;const conv=chatConversations[key];items.push({key,name:chatClientThreadName(t.client_id),isTeam:false,isGroup:false,isClient:true,isUnclaimed:t.unclaimed,last:lastOf(conv),unread:conv?conv.unread:0})});const[team,...rest]=items;rest.sort((a,b)=>{if(a.last&&b.last)return new Date(b.last.created_at)-new Date(a.last.created_at);if(a.last)return-1;if(b.last)return 1;return a.name.localeCompare(b.name)});const sorted=[team,...rest];const convHtml=ciChatLinkRowHtml()+sorted.map(it=>{let previewText;const typingNow=chatIsPeerTyping(it.key);if(typingNow){previewText="typing…"}else if(it.isClient&&it.last){const senderPrefix=it.last.sender==="staff"?"You: ":it.last.sender==="ai"?"AI: ":"";previewText=senderPrefix+(it.last.body||"")}else if(it.last){const senderPrefix=it.last.sender_id===myUserId?"You: ":it.isGroup?`${chatContactName(it.last.sender_id)}: `:"";const body=it.last.body||(it.last.attachment_mime&&it.last.attachment_mime.startsWith("audio/")?"🎤 Voice message":it.last.attachment_name?`📎 ${it.last.attachment_name}`:"");previewText=senderPrefix+body}else{previewText=it.isTeam?"Only Admins/Owner can post here":it.isGroup?`${it.memberCount} member${it.memberCount===1?"":"s"}`:it.isUnclaimed?"Waiting to be accepted":"No messages yet"}const avatarHtml=it.isTeam?`<div class="chat-conv-avatar" style="width:32px; height:32px;">📢</div>`:it.isGroup?`<div class="chat-conv-avatar" style="width:32px; height:32px;">👥</div>`:it.isClient?`<div class="chat-conv-avatar" style="width:32px; height:32px;">🏢</div>`:it.key===AI_ASSISTANT_BOT_ID?chatBotAvatarIconHtml(32):chatAvatarHtml(it.key,it.name,32,true);return`
      <div class="chat-conv-item" data-key="${it.key}">
        ${avatarHtml}
        <div class="chat-conv-info">
          <div class="chat-conv-name">${escapeHtml(it.name)}${it.isUnclaimed?' <span style="font-size:10px; font-weight:600; color:var(--away); text-transform:uppercase; letter-spacing:0.5px;">• Waiting</span>':""}${it.statusBadge||""}</div>
          <div class="chat-conv-preview${typingNow?" chat-conv-preview-typing":""}">${escapeHtml(previewText)}</div>
        </div>
        ${it.unread>0?`<div class="chat-conv-badge">${it.unread>99?"99+":it.unread}</div>`:""}
      </div>
    `}).join("");const convListEl=$("chatConvList");if(convListEl.__lastHtml===convHtml)return;convListEl.__lastHtml=convHtml;convListEl.innerHTML=convHtml;convListEl.querySelectorAll(".chat-conv-item").forEach(el=>{if(el.dataset.ciLink){el.addEventListener("click",()=>ciOpenFromElsewhere(null));return}el.addEventListener("click",()=>openChatThread(el.dataset.key))})}let chatBottomTab="chats";let chatSmsDirectory=[];let chatSmsThreadClient=null;let chatSmsThreadPollTimer=null;async function chatResolveGhlLocationId(){const{data}=await sb.from("app_settings").select("value").eq("key","client_sms_location_id").maybeSingle();if(data?.value)return data.value;const result=await callGhlApi({action:"list_connections"});const rows=result.ok?result.connections||[]:[];return rows[0]?.location_id||null}function chatGhlFmtWhen(iso){if(!iso)return"";const d=new Date(iso);if(isNaN(d.getTime()))return"";const sameDay=d.toDateString()===new Date().toDateString();return sameDay?d.toLocaleTimeString([],{hour:"2-digit",minute:"2-digit"}):d.toLocaleDateString([],{month:"short",day:"numeric"})}async function chatRenderSmsList(){const box=$("chatSmsList");const isFirstRender=!box.querySelector(".chat-ghl-list-item");if(isFirstRender)box.innerHTML='<div class="chat-ghl-list-loading">Loading…</div>';const{data,error}=await sb.from("client_accounts").select("id, full_name, company_name, phone").eq("is_active",true).order("full_name");if(error){if(isFirstRender)box.innerHTML=`<div class="chat-ghl-list-empty">${escapeHtml(error.message)}</div>`;return}chatSmsDirectory=data||[];if(!chatSmsDirectory.length){box.innerHTML='<div class="chat-ghl-list-empty">No active clients yet.</div>';return}box.innerHTML=chatSmsDirectory.map(c=>`
    <div class="chat-ghl-list-item" data-client-id="${c.id}">
      ${chatAvatarHtml(c.id,c.full_name||c.company_name||"Client",32,false)}
      <div class="chat-conv-info">
        <div class="chat-conv-name">${escapeHtml(c.full_name||"Client")}</div>
        <div class="chat-conv-preview">${escapeHtml(c.company_name||c.phone||"No phone on file")}</div>
      </div>
      ${chatSmsPendingByClient[c.id]?'<div class="chat-ghl-list-item-badge" title="New text"></div>':""}
    </div>
  `).join("");box.querySelectorAll(".chat-ghl-list-item").forEach(el=>{el.addEventListener("click",()=>{const c=chatSmsDirectory.find(x=>x.id===el.dataset.clientId);if(c)chatOpenSmsThread(c)})})}async function chatOpenSmsThread(client){clearInterval(chatSmsThreadPollTimer);chatSmsThreadClient=client;chatSmsPendingByClient[client.id]=false;chatUpdateBottomTabBadges();chatMarkRead("sms:"+client.id);$("chatListView").style.display="none";$("chatSmsThreadView").style.display="flex";$("chatSmsThreadTitle").textContent=client.full_name||"Client";const canText=!!myPermissions.canTextClients;$("chatSmsThreadNoPermMsg").style.display=canText?"none":"block";$("chatSmsThreadInput").value="";$("chatSmsThreadBookBtn").style.display=canText&&client.phone?"inline-flex":"none";if(!client.phone){$("chatSmsThreadBody").innerHTML='<div class="chat-ghl-list-empty">This client has no phone number on file — add one under User Management → Clients.</div>';$("chatSmsThreadCompose").style.display="none";return}$("chatSmsThreadCompose").style.display=canText?"flex":"none";delete $("chatSmsThreadBody").dataset.loaded;await chatRefreshSmsThread();chatSmsThreadPollTimer=setInterval(()=>{if(!document.hidden)chatRefreshSmsThread()},8e3)}async function chatRefreshSmsThread(){if(!chatSmsThreadClient||!chatSmsThreadClient.phone)return;const body=$("chatSmsThreadBody");const firstLoad=!body.dataset.loaded;if(firstLoad)body.innerHTML='<div class="chat-ghl-list-loading">Loading…</div>';const locationId=await chatResolveGhlLocationId();if(!locationId){if(firstLoad)body.innerHTML='<div class="chat-ghl-list-empty">No GoHighLevel location connected.</div>';return}const res=await callGhlApi({action:"lookup_contact_activity",locationId,phone:chatSmsThreadClient.phone,clientId:chatSmsThreadClient.id});if(!res.ok){if(firstLoad)body.innerHTML=`<div class="chat-ghl-list-empty">${escapeHtml(res.error||"Lookup failed.")}</div>`;return}if(res.conversationError){if(firstLoad)body.innerHTML=`<div class="chat-ghl-list-empty">${escapeHtml(res.conversationError)}</div>`;return}const filtered=(res.messages||[]).filter(m=>ghlLookupClassifyMessage(m)==="sms").sort((a,b)=>new Date(a.dateAdded||a.dateUpdated)-new Date(b.dateAdded||b.dateUpdated));body.dataset.loaded="1";body.innerHTML=filtered.length?filtered.map(m=>{const mine=(m.direction||"").toLowerCase().includes("outbound");return`
          <div class="chat-msg-row ${mine?"mine":"theirs"}">
            <div class="chat-msg-meta">${ghlLookupFmtWhen(m.dateAdded||m.dateUpdated)}</div>
            <div class="chat-msg-bubble">${escapeHtml(ghlLookupMessageBody(m))}</div>
          </div>
        `}).join(""):'<div class="chat-ghl-list-empty">No text messages yet — send one below.</div>';body.scrollTop=body.scrollHeight}async function chatSendSmsFromThread(){const input=$("chatSmsThreadInput");const text=input.value.trim();if(!text||!chatSmsThreadClient)return;const sendBtn=$("chatSmsThreadSendBtn");input.disabled=true;sendBtn.disabled=true;const res=await callSendClientSms(chatSmsThreadClient.id,"manual",text);input.disabled=false;sendBtn.disabled=false;if(!res.ok){alert(res.error||"Failed to send.");return}input.value="";input.focus();await chatRefreshSmsThread()}$("chatSmsThreadSendBtn").addEventListener("click",chatSendSmsFromThread);$("chatSmsThreadInput").addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();chatSendSmsFromThread()}});$("chatSmsThreadBackBtn").addEventListener("click",()=>{clearInterval(chatSmsThreadPollTimer);chatSmsThreadClient=null;$("chatSmsThreadView").style.display="none";$("chatListView").style.display="flex"});let chatBookApptState={calendarId:null,slotDurationMin:30,chosenIso:null};async function chatOpenBookApptModal(){if(!chatSmsThreadClient||!chatSmsThreadClient.phone)return;chatBookApptState={calendarId:null,slotDurationMin:30,chosenIso:null};$("chatBookApptForClient").textContent=`For ${chatSmsThreadClient.full_name||"this client"} — ${chatSmsThreadClient.phone}`;$("chatBookApptStep1").style.display="block";$("chatBookApptStep2").style.display="none";$("chatBookApptConfirmBtn").style.display="none";$("chatBookApptModalMsg").style.display="none";$("chatBookApptSlots").innerHTML="";$("chatBookApptCalendarSelect").innerHTML='<option value="">Loading calendars…</option>';$("chatBookApptModalOverlay").style.display="flex";const res=await callGhlApi({action:"list_calendars"});if(!res.ok){$("chatBookApptCalendarSelect").innerHTML='<option value="">—</option>';const is401=/\(401\)/.test(res.error||"");$("chatBookApptModalMsg").textContent=is401?`${res.error} — the connected GoHighLevel location needs to be reconnected to grant calendar access. Ask an Owner/Admin to disconnect and reconnect it under User Management → Integrations.`:res.error||"Failed to load calendars.";$("chatBookApptModalMsg").style.display="block";return}const calendars=res.calendars||[];if(!calendars.length){$("chatBookApptCalendarSelect").innerHTML='<option value="">No calendars connected</option>';return}$("chatBookApptCalendarSelect").innerHTML='<option value="">Select a calendar…</option>'+calendars.map(c=>`<option value="${c.id}" data-slot-duration="${c.slotDuration||30}">${escapeHtml(c.name)}</option>`).join("")}async function chatLoadBookApptSlots(calendarId,slotDurationMin){const box=$("chatBookApptSlots");box.innerHTML='<div class="chat-ghl-list-loading">Loading available times…</div>';const start=new Date;const end=new Date(Date.now()+14*24*60*60*1e3);const timezone=Intl.DateTimeFormat().resolvedOptions().timeZone;const res=await callGhlApi({action:"get_calendar_slots",calendarId,startDate:start.toISOString(),endDate:end.toISOString(),timezone});if(!res.ok){box.innerHTML=`<div class="chat-ghl-list-empty">${escapeHtml(res.error||"Failed to load slots.")}</div>`;return}const dates=Object.keys(res.slotsByDate||{}).sort();if(!dates.length){box.innerHTML='<div class="chat-ghl-list-empty">No open slots in the next 14 days.</div>';return}box.innerHTML=dates.map(d=>{const dLabel=new Date(d+"T00:00:00").toLocaleDateString([],{weekday:"short",month:"short",day:"numeric"});const slots=res.slotsByDate[d]||[];return`
      <div class="chat-book-slot-date-heading">${escapeHtml(dLabel)}</div>
      <div class="chat-book-slot-row">
        ${slots.map(iso=>`<button type="button" class="chat-book-slot-btn" data-iso="${iso}">${escapeHtml(new Date(iso).toLocaleTimeString([],{hour:"2-digit",minute:"2-digit"}))}</button>`).join("")}
      </div>
    `}).join("");box.querySelectorAll(".chat-book-slot-btn").forEach(btn=>{btn.addEventListener("click",()=>chatChooseBookApptSlot(btn.dataset.iso,slotDurationMin))})}function chatChooseBookApptSlot(iso,slotDurationMin){chatBookApptState.chosenIso=iso;chatBookApptState.slotDurationMin=slotDurationMin||30;$("chatBookApptStep1").style.display="none";$("chatBookApptStep2").style.display="block";$("chatBookApptChosenSlot").textContent=new Date(iso).toLocaleString([],{weekday:"short",month:"short",day:"numeric",hour:"2-digit",minute:"2-digit"});$("chatBookApptTitleInput").value="";$("chatBookApptConfirmBtn").style.display="inline-block";$("chatBookApptModalMsg").style.display="none"}$("chatBookApptCalendarSelect").addEventListener("change",e=>{const opt=e.target.selectedOptions[0];const calendarId=e.target.value;chatBookApptState.calendarId=calendarId;if(!calendarId){$("chatBookApptSlots").innerHTML="";return}chatLoadBookApptSlots(calendarId,Number(opt?.dataset.slotDuration)||30)});$("chatBookApptBackToSlotsBtn").addEventListener("click",()=>{$("chatBookApptStep2").style.display="none";$("chatBookApptStep1").style.display="block";$("chatBookApptConfirmBtn").style.display="none"});$("chatSmsThreadBookBtn").addEventListener("click",chatOpenBookApptModal);$("chatBookApptCancelBtn").addEventListener("click",()=>{$("chatBookApptModalOverlay").style.display="none"});$("chatBookApptConfirmBtn").addEventListener("click",async()=>{if(!chatBookApptState.calendarId||!chatBookApptState.chosenIso||!chatSmsThreadClient)return;const btn=$("chatBookApptConfirmBtn");btn.disabled=true;btn.textContent="Booking…";$("chatBookApptModalMsg").style.display="none";const startTime=chatBookApptState.chosenIso;const endTime=new Date(new Date(startTime).getTime()+chatBookApptState.slotDurationMin*6e4).toISOString();const title=$("chatBookApptTitleInput").value.trim();const res=await callGhlApi({action:"book_appointment",calendarId:chatBookApptState.calendarId,phone:chatSmsThreadClient.phone,clientId:chatSmsThreadClient.id,startTime,endTime,title:title||void 0,timezone:Intl.DateTimeFormat().resolvedOptions().timeZone});btn.disabled=false;btn.textContent="Confirm booking";if(!res.ok){$("chatBookApptModalMsg").textContent=res.error||"Failed to book appointment.";$("chatBookApptModalMsg").style.display="block";return}$("chatBookApptModalOverlay").style.display="none";alert("Appointment booked.")});async function chatRenderCallsList(){const box=$("chatCallsList");box.innerHTML='<div class="chat-ghl-list-loading">Loading…</div>';const{data:calls,error}=await sb.from("voice_calls").select(`
      id, status, started_at, answered_at, ended_at, caller_id, callee_id, caller_client_id, callee_client_id,
      caller_profile:profiles!voice_calls_caller_id_fkey(full_name),
      callee_profile:profiles!voice_calls_callee_id_fkey(full_name),
      caller_client:client_accounts!voice_calls_caller_client_id_fkey(full_name, company_name),
      callee_client:client_accounts!voice_calls_callee_client_id_fkey(full_name, company_name)
    `).order("started_at",{ascending:false}).or(`caller_id.eq.${myUserId},callee_id.eq.${myUserId}`).limit(50);if(error){box.innerHTML=`<div class="chat-ghl-list-empty">${escapeHtml(error.message)}</div>`;return}const lastReadIso=chatLastRead["calls"];chatCallsPendingCount=(calls||[]).filter(c=>c.caller_id!==myUserId&&(!lastReadIso||new Date(c.started_at)>new Date(lastReadIso))).length;chatUpdateBottomTabBadges();if(!calls||!calls.length){box.innerHTML='<div class="chat-ghl-list-empty">No calls yet.</div>';return}const statusLabel={ringing:"No answer",declined:"Declined",missed:"Missed",failed:"Call failed",ended:"Ended",answered:"Answered"};box.innerHTML=calls.map(c=>{const isMine=c.caller_id===myUserId;const otherName=isMine?c.callee_profile?.full_name||c.callee_client?.full_name||c.callee_client?.company_name||"Unknown":c.caller_profile?.full_name||c.caller_client?.full_name||c.caller_client?.company_name||"Unknown";const durationSec=c.answered_at&&c.ended_at?Math.max(0,Math.round((new Date(c.ended_at)-new Date(c.answered_at))/1e3)):null;const durationLabel=durationSec!=null?`${Math.floor(durationSec/60)}:${String(durationSec%60).padStart(2,"0")}`:statusLabel[c.status]||c.status;const isPending=!isMine&&(!lastReadIso||new Date(c.started_at)>new Date(lastReadIso));const missedByMe=!isMine&&(c.status==="missed"||c.status==="ringing");const otherStaffId=isMine?c.callee_id:c.caller_id;const otherClientId=isMine?c.callee_client_id:c.caller_client_id;const canCallBack=!!otherStaffId||!!otherClientId&&myPermissions.canTextClients;return`
      <div class="chat-ghl-list-item${missedByMe?" chat-call-missed":""}">
        <div class="chat-conv-avatar" style="width:32px; height:32px;">${isMine?"&#8599;&#65039;":missedByMe?"&#128245;":"&#8601;&#65039;"}</div>
        <div class="chat-conv-info">
          <div class="chat-conv-name">${escapeHtml(otherName)}</div>
          <div class="chat-conv-preview">${missedByMe?'<span class="chat-call-missed-label">Missed call</span>':`${isMine?"Outgoing":"Incoming"} &middot; ${escapeHtml(durationLabel)}`}</div>
        </div>
        <div class="mono" style="font-size:10px; color:var(--text-dim); white-space:nowrap;">${chatGhlFmtWhen(c.started_at)}</div>
        ${canCallBack?`<button type="button" class="chat-call-back-btn" data-staff="${escapeHtml(otherStaffId||"")}" data-client="${escapeHtml(otherClientId||"")}" data-name="${escapeHtml(otherName)}" title="Call ${escapeHtml(otherName)} back">&#128222;</button>`:""}
        ${isPending?'<div class="chat-ghl-list-item-badge" title="Not yet seen"></div>':""}
      </div>
    `}).join("");box.querySelectorAll(".chat-call-back-btn").forEach(b=>b.addEventListener("click",e=>{e.stopPropagation();chatCallBack(b.dataset.staff||null,b.dataset.client||null,b.dataset.name)}))}function chatCallBack(staffId,clientId,name){if(voiceCallState){alert("You are already on a call.");return}if(staffId)startVoiceCall(staffId,name||"Colleague");else if(clientId)startVoiceCallToClient(clientId,name||"Client")}async function voiceCallBackFromCallId(callId){const{data:c,error}=await sb.from("voice_calls").select("id, caller_id, caller_client_id, caller_profile:profiles!voice_calls_caller_id_fkey(full_name), caller_client:client_accounts!voice_calls_caller_client_id_fkey(full_name, company_name)").eq("id",callId).maybeSingle();if(error||!c){alert("That call could not be found.");return}const name=c.caller_profile&&c.caller_profile.full_name||c.caller_client&&(c.caller_client.full_name||c.caller_client.company_name)||"them";if(!c.caller_id&&c.caller_client_id&&!myPermissions.canTextClients){alert(`${name} is a client, and your role cannot call clients from the portal.`);return}if(!confirm(`Call ${name} back now?`))return;chatCallBack(c.caller_id||null,c.caller_id?null:c.caller_client_id,name)}function handleCallbackHash(){const m=/^#callback=([0-9a-f-]{36})$/i.exec(location.hash||"");if(!m)return;history.replaceState(null,"",location.pathname+location.search);voiceCallBackFromCallId(m[1])}window.addEventListener("hashchange",()=>{if(myUserId)handleCallbackHash()});let chatSmsNotifyPollTimer=null;let chatSmsNotifySeenAt={};function chatSmsNotifyStorageKey(){return`chatSmsNotifySeenAt:${myUserId}`}function chatSmsNotifyLoadSeen(){try{chatSmsNotifySeenAt=JSON.parse(localStorage.getItem(chatSmsNotifyStorageKey())||"{}")}catch{chatSmsNotifySeenAt={}}}function chatSmsNotifySaveSeen(){try{localStorage.setItem(chatSmsNotifyStorageKey(),JSON.stringify(chatSmsNotifySeenAt))}catch{}}let chatSmsPendingByClient={};let chatCallsPendingCount=0;function chatUpdateBottomTabBadges(){chatSetBadge("chatBottomBadgeSms",Object.values(chatSmsPendingByClient).filter(Boolean).length);chatSetBadge("chatBottomBadgeCalls",chatCallsPendingCount)}async function chatPollSmsForNotifications(){if(!myPermissions.canTextClients)return;await fetchStaffNotifRolesConfig();if(!staffNotifKindAllowed("client_sms_reply"))return;const locationId=await chatResolveGhlLocationId();if(!locationId)return;const{data:clients}=await sb.from("client_accounts").select("id, full_name, company_name, phone").eq("is_active",true).not("phone","is",null);let changed=false;for(const c of clients||[]){if(!c.phone)continue;let res;try{res=await callGhlApi({action:"lookup_contact_activity",locationId,phone:c.phone})}catch{continue}if(!res.ok||res.conversationError)continue;const inbound=(res.messages||[]).filter(m=>ghlLookupClassifyMessage(m)==="sms"&&(m.direction||"").toLowerCase().includes("inbound")).sort((a,b)=>new Date(b.dateAdded||b.dateUpdated)-new Date(a.dateAdded||a.dateUpdated))[0];if(!inbound){chatSmsPendingByClient[c.id]=false;continue}const at=inbound.dateAdded||inbound.dateUpdated;const readAt=chatLastRead["sms:"+c.id];chatSmsPendingByClient[c.id]=!readAt||new Date(at)>new Date(readAt);const seenAt=chatSmsNotifySeenAt[c.id];if(seenAt&&new Date(at)<=new Date(seenAt))continue;const isFirstCheck=!seenAt;chatSmsNotifySeenAt[c.id]=at;changed=true;if(isFirstCheck)continue;pushStaffNotification({icon:"✉️",title:c.full_name||"Client",body:ghlLookupMessageBody(inbound),onClick:()=>{$("chatPanel").style.display="flex";const smsTabBtn=$("chatBottomTabs").querySelector('[data-chat-bottom-tab="sms"]');if(smsTabBtn)smsTabBtn.click();chatOpenSmsThread(c)}})}if(changed)chatSmsNotifySaveSeen();chatUpdateBottomTabBadges();if(chatBottomTab==="sms")chatRenderSmsList()}let gmailNotifyPollTimer=null;let gmailNotifySeenAt=null;function gmailNotifyStorageKey(){return`gmailNotifySeenAt:${myUserId}`}function gmailNotifyLoadSeen(){try{const raw=JSON.parse(localStorage.getItem(gmailNotifyStorageKey())||"null");gmailNotifySeenAt=typeof raw==="string"?raw:null}catch{gmailNotifySeenAt=null}}function gmailNotifySaveSeen(){try{localStorage.setItem(gmailNotifyStorageKey(),JSON.stringify(gmailNotifySeenAt))}catch{}}async function pollGmailReplyNotifications(){if(!myPermissions.canTextClients)return;await fetchStaffNotifRolesConfig();if(!staffNotifKindAllowed("gmail_new_message"))return;let res;try{res=await callGmailApi({action:"list_messages",folder:"inbox"})}catch{return}if(!res.ok){if(/no gmail inbox is connected/i.test(res.error||"")&&gmailNotifyPollTimer){clearInterval(gmailNotifyPollTimer);gmailNotifyPollTimer="stopped"}return}const messages=res.messages||[];const badge=$("actGmailInboxUnreadBadge");if(badge)badge.style.display=messages.some(m=>m.unread)?"":"none";const newest=messages.slice().sort((a,b)=>new Date(b.date||0)-new Date(a.date||0))[0];if(!newest)return;if(gmailNotifySeenAt&&new Date(newest.date||0)<=new Date(gmailNotifySeenAt))return;const isFirstCheck=!gmailNotifySeenAt;gmailNotifySeenAt=newest.date;gmailNotifySaveSeen();if(isFirstCheck)return;pushStaffNotification({icon:"📧",title:newest.from||"New email",body:newest.subject||newest.snippet||""})}$("chatBottomTabs").addEventListener("click",e=>{const btn=e.target.closest(".chat-bottom-tab-btn");if(!btn||btn.dataset.chatBottomTab===chatBottomTab)return;chatBottomTab=btn.dataset.chatBottomTab;$("chatBottomTabs").querySelectorAll(".chat-bottom-tab-btn").forEach(b=>b.classList.toggle("active",b.dataset.chatBottomTab===chatBottomTab));$("chatConvList").style.display=chatBottomTab==="chats"?"block":"none";$("chatSearchInput").style.display=chatBottomTab==="chats"?"":"none";$("chatSmsList").style.display=chatBottomTab==="sms"?"block":"none";$("chatCallsList").style.display=chatBottomTab==="calls"?"block":"none";if(chatBottomTab==="sms")chatRenderSmsList();if(chatBottomTab==="calls")chatRenderCallsList().then(()=>chatMarkRead("calls"))});async function chatMarkRead(key){const nowIso=new Date().toISOString();chatLastRead[key]=nowIso;if(chatConversations[key])chatConversations[key].unread=0;updateChatUnreadBadge();try{await sb.from("chat_reads").upsert({user_id:myUserId,conversation_key:key,last_read_at:nowIso})}catch(e){console.error("Failed to save chat read marker",e)}}async function openChatThread(key){if(chatActiveConv&&chatActiveConv!==key&&chatActiveConv!=="team"&&!chatActiveConv.startsWith("group:")){clearTimeout(chatTypingStopTimer);chatTypingSend(chatActiveConv,"stopped")}if(!key.startsWith("client:"))chatCloseClientTypingChannel();chatActiveConv=key;if($("desoukyMascotWrap")){$("desoukyMascotWrap").style.display=AI_ASSISTANT_BOT_ID&&key===AI_ASSISTANT_BOT_ID?"flex":"none";if(typeof positionDesoukyMascot==="function"){positionDesoukyMascot();setTimeout(positionDesoukyMascot,300)}}if(chatOfflineHint&&chatOfflineHint.key!==key)chatOfflineHint=null;chatClearPendingFile();chatHideMentionBox();chatStopRecording(true);$("chatListView").style.display="none";$("chatMyProfileView").style.display="none";$("chatNewGroupView").style.display="none";$("chatNewClientChatView").style.display="none";$("chatThreadView").style.display="flex";$("chatEndClientChatBtn").style.display="none";$("clientTextComposer").style.display="none";$("chatClientClaimBar").style.display="none";$("chatTextClientBtn").style.display="none";$("chatCallClientInAppBtn").style.display="none";if(key.startsWith("client:")){if(ciCanUseInbox()){chatActiveConv=null;$("chatThreadView").style.display="none";$("chatListView").style.display="flex";ciOpenFromElsewhere(key.slice(7));return}openClientChatThread(key.slice(7));return}$("chatThreadTitleName").textContent=chatConvDisplayName(key);renderChatThreadHeaderStatus(key);if(!chatConversations[key])chatConversations[key]={key,messages:[],unread:0};const isGroup=key.startsWith("group:");$("chatThreadInfoBtn").style.display=isGroup?"inline-block":"none";$("chatThreadInfoBtn").onclick=isGroup?()=>openChatGroupInfo(key.slice(6)):null;const isTeam=key==="team";const canPost=isTeam?chatCanPostTeam():true;$("chatCommunicationDisclaimer").style.display=isTeam&&!canPost?"block":"none";$("chatInputRow").style.display=canPost?"flex":"none";$("chatMeetBtn").style.display=canPost&&chatMeetAllowed()?"inline-block":"none";$("chatCallBtn").style.display=canPost&&!isTeam?"inline-block":"none";$("chatCallBtn").title=isGroup?"Start a conference call — rings everyone in this group":"Voice call";renderChatThread();await chatMarkRead(key);if(canPost)$("chatInput").focus()}let chatQueueRoutesCache={};async function loadChatQueueLabels(){const{data}=await sb.from("client_chat_routes").select("queue_key, label");chatQueueRoutesCache={};(data||[]).forEach(r=>{chatQueueRoutesCache[r.queue_key]=r.label})}function chatQueueLabel(queueKey){return chatQueueRoutesCache[queueKey]||queueKey}let clientChatOverviewSeq=0;async function loadClientChatOverview(){if(!myUserId)return;const mySeq=++clientChatOverviewSeq;const seeAll=myIsOwner||myRole==="admin";let threads;if(seeAll){const{data,error}=await sb.from("client_chat_threads").select("client_id, status, queue_key, assigned_staff_id, updated_at, client_accounts(full_name, company_name)");if(error){console.error("Failed to load client chat threads",error);return}threads=data}else{const{data:myQueues}=await sb.from("client_chat_route_staff").select("queue_key").eq("staff_id",myUserId);const queueKeys=(myQueues||[]).map(q=>q.queue_key);const[{data:mine,error:mineErr},{data:unclaimed,error:unclaimedErr}]=await Promise.all([sb.from("client_chat_threads").select("client_id, status, queue_key, assigned_staff_id, updated_at, client_accounts(full_name, company_name)").eq("assigned_staff_id",myUserId),queueKeys.length?sb.from("client_chat_threads").select("client_id, status, queue_key, assigned_staff_id, updated_at, client_accounts(full_name, company_name)").eq("status","routed").is("assigned_staff_id",null).in("queue_key",queueKeys):Promise.resolve({data:[],error:null})]);if(mineErr||unclaimedErr){console.error("Failed to load client chat threads",mineErr||unclaimedErr);return}const seen=new Set;threads=[...mine||[],...unclaimed||[]].filter(t=>{if(seen.has(t.client_id))return false;seen.add(t.client_id);return true})}if(mySeq!==clientChatOverviewSeq)return;const newDirectory=(threads||[]).map(t=>({client_id:t.client_id,full_name:t.client_accounts&&t.client_accounts.full_name||"Client",company_name:t.client_accounts&&t.client_accounts.company_name||"",status:t.status,queue_key:t.queue_key,assigned_staff_id:t.assigned_staff_id,unclaimed:t.status==="routed"&&!t.assigned_staff_id}));if(!newDirectory.length){if(mySeq!==clientChatOverviewSeq)return;clientChatThreadsDirectory=newDirectory;updateChatUnreadBadge();return}clientChatThreadsDirectory=newDirectory;const currentKeys=new Set(newDirectory.map(t=>"client:"+t.client_id));Object.keys(chatConversations).forEach(k=>{if(k.startsWith("client:")&&!currentKeys.has(k))delete chatConversations[k]});newDirectory.forEach(t=>{const key="client:"+t.client_id;if(!chatConversations[key])chatConversations[key]={key,messages:[],unread:0}});const newClientIds=newDirectory.map(t=>t.client_id).filter(id=>!chatConversations["client:"+id].loadedOnce);if(!newClientIds.length){updateChatUnreadBadge();return}const{data:msgs,error:msgsErr}=await sb.rpc("client_chat_messages_recent",{p_client_ids:newClientIds,p_per_client_limit:100});if(mySeq!==clientChatOverviewSeq)return;if(msgsErr){console.error("Failed to load client chat messages",msgsErr);updateChatUnreadBadge();return}newClientIds.forEach(clientId=>{const key="client:"+clientId;const messages=(msgs||[]).filter(m=>m.client_id===clientId).sort((a,b)=>new Date(a.created_at)-new Date(b.created_at));const lastReadIso=chatLastRead[key];const unread=messages.filter(m=>m.sender!=="staff"&&m.sender!=="system"&&(!lastReadIso||new Date(m.created_at)>new Date(lastReadIso))).length;chatConversations[key]={key,messages,unread,loadedOnce:true}});updateChatUnreadBadge()}function chatClientThreadName(clientId){const t=clientChatThreadsDirectory.find(x=>x.client_id===clientId);if(!t)return"Client";return t.company_name?`${t.company_name} (${t.full_name})`:t.full_name}const CLIENT_TEXT_CAMPAIGN_CONFIRM_DEFAULT="Hi {client_name}, our team wants to connect with you for your campaign ({campaign}) — please log in https://clientportal.proptechnologyai.com/ > click on ask me button > to respond to the team.";let clientTextComposerState=null;async function clientTextComposerOpen(clientId,clientName){const composer=$("clientTextComposer");const msgEl=$("clientTextComposerMsg");const campaignRow=$("clientTextComposerCampaignRow");const campaignSelect=$("clientTextComposerCampaignSelect");const preview=$("clientTextComposerPreview");const sendBtn=$("clientTextComposerSendBtn");msgEl.textContent="";preview.textContent="Loading…";campaignRow.style.display="none";campaignSelect.innerHTML="";sendBtn.disabled=true;composer.style.display="block";const[{data:campaigns},{data:tmplRow}]=await Promise.all([sb.from("client_campaign_access").select("form_id, lead_submission_forms(name)").eq("client_id",clientId),sb.from("app_settings").select("value").eq("key","client_sms_template_campaign_confirm").maybeSingle()]);const template=tmplRow&&tmplRow.value||CLIENT_TEXT_CAMPAIGN_CONFIRM_DEFAULT;const campaignNames=[...new Set((campaigns||[]).map(c=>c.lead_submission_forms&&c.lead_submission_forms.name).filter(Boolean))];clientTextComposerState={clientId,clientName,template,campaignNames};if(!campaignNames.length){preview.textContent="This client has no campaign access granted yet — nothing to send.";return}sendBtn.disabled=false;if(campaignNames.length>1){campaignRow.style.display="block";campaignSelect.innerHTML=campaignNames.map(n=>`<option value="${escapeHtml(n)}">${escapeHtml(n)}</option>`).join("");campaignSelect.onchange=clientTextComposerRenderPreview}clientTextComposerRenderPreview()}function clientTextComposerRenderPreview(){if(!clientTextComposerState)return;const{clientName,template,campaignNames}=clientTextComposerState;const campaign=campaignNames.length>1?$("clientTextComposerCampaignSelect").value:campaignNames[0];$("clientTextComposerPreview").textContent=template.replaceAll("{client_name}",clientName||"there").replaceAll("{campaign}",campaign||"your campaign")}function openClientChatThread(clientId){const key="client:"+clientId;const thread=clientChatThreadsDirectory.find(t=>t.client_id===clientId)||{};chatOpenClientTypingChannel(clientId);$("chatThreadTitleName").textContent=chatClientThreadName(clientId);$("chatThreadTitleStatus").style.display="none";$("chatThreadInfoBtn").style.display="none";$("chatThreadInfoBtn").onclick=null;$("chatCommunicationDisclaimer").style.display="none";$("chatMeetBtn").style.display="none";$("chatCallBtn").style.display="none";const inAppCallBtn=$("chatCallClientInAppBtn");inAppCallBtn.style.display=myPermissions.canTextClients&&!thread.unclaimed?"inline-block":"none";inAppCallBtn.onclick=()=>startVoiceCallToClient(clientId,chatClientThreadName(clientId));const claimBar=$("chatClientClaimBar");if(thread.unclaimed){claimBar.style.display="flex";$("chatClientClaimBarText").textContent="Waiting to be accepted"+(thread.queue_key?` — ${chatQueueLabel(thread.queue_key)}`:"")+".";$("chatClientClaimBtn").onclick=()=>acceptClientChatThread(clientId);$("chatInputRow").style.display="none"}else{claimBar.style.display="none";$("chatInputRow").style.display="flex"}const endBtn=$("chatEndClientChatBtn");const canEndThisChat=!thread.unclaimed&&thread.status!=="closed"&&(myIsOwner||myRole==="admin"||thread.assigned_staff_id===myUserId);endBtn.style.display=canEndThisChat?"inline-block":"none";endBtn.onclick=()=>endClientChatThread(clientId);$("chatEmojiBtn").style.display="none";$("chatAttachBtn").style.display=thread.unclaimed?"none":"";$("chatMicBtn").style.display=thread.unclaimed?"none":"";$("clientTextComposer").style.display="none";$("clientTextComposerMsg").textContent="";const textBtn=$("chatTextClientBtn");textBtn.style.display=myPermissions.canTextClients&&!thread.unclaimed?"inline-block":"none";textBtn.onclick=()=>{const composer=$("clientTextComposer");if(composer.style.display!=="none"){composer.style.display="none";return}clientTextComposerOpen(clientId,chatClientThreadName(clientId))};$("clientTextComposerCancelBtn").onclick=()=>{$("clientTextComposer").style.display="none"};$("clientTextComposerSendBtn").onclick=async()=>{if(!clientTextComposerState||!clientTextComposerState.campaignNames.length)return;const{campaignNames}=clientTextComposerState;const campaign=campaignNames.length>1?$("clientTextComposerCampaignSelect").value:campaignNames[0];const msgEl=$("clientTextComposerMsg");const btn=$("clientTextComposerSendBtn");btn.disabled=true;btn.textContent="Sending…";try{callSendClientEmail(clientId,"campaign_confirm",campaign);const{data,error}=await sb.functions.invoke("send-client-sms",{body:{clientId,kind:"campaign_confirm",details:campaign}});if(error){let msg=error.message;try{const b=await error.context.json();msg=b.error||msg}catch(e){}throw new Error(msg)}if(!data||!data.ok)throw new Error(data&&data.error||"Failed to send.");msgEl.textContent="Text sent.";msgEl.className="msg success";setTimeout(()=>{$("clientTextComposer").style.display="none";msgEl.textContent=""},1200)}catch(e){console.error("Client campaign-confirm text failed:",e);msgEl.textContent=e.message||"Failed to send.";msgEl.className="msg error"}finally{btn.disabled=false;btn.textContent="Send text"}};if(!chatConversations[key])chatConversations[key]={key,messages:[],unread:0};renderClientChatMessages(clientId);chatMarkRead(key);sb.rpc("mark_client_chat_read",{p_client_id:clientId}).then(({error})=>{if(error)console.error("mark_client_chat_read failed",error)});$("chatInput").focus()}function chatClientMessageEditable(m){return m.sender==="staff"&&m.staff_id===myUserId&&Date.now()-new Date(m.created_at).getTime()<CHAT_EDIT_WINDOW_MS}function chatClientTickState(m){if(m.read_at)return"read";if(m.delivered_at)return"delivered";return"sent"}function chatMsgHeadHtml(name,role,detail){const label=role==="client"?detail?"Client · "+escapeHtml(detail):"Client":role==="ai"?"AI":"Staff";const staffDetail=role==="staff"&&detail?escapeHtml(detail):null;return`<div class="chat-msg-head"><span class="chat-msg-head-name">${escapeHtml(name||"Unknown")}</span><span class="chat-role-badge ${role}">${staffDetail||label}</span></div>`}const CHAT_SEQUENTIAL_MS=6e4;function chatIsSequential(prev,senderKey,atMs){if(!prev)return false;return prev.key===senderKey&&atMs-prev.at<CHAT_SEQUENTIAL_MS}function renderClientChatMessages(clientId){const key="client:"+clientId;const conv=chatConversations[key]||{messages:[]};const box=$("chatMessages");const wasAtBottom=chatAtBottom(box);const clientChatTz=nonAgentDisplayTz();let prevSender=null;const clientDisplayName=chatClientThreadName(clientId);chatSwapHtml(box,conv.messages.map(m=>{if(m.sender==="system"){prevSender=null;return`<div class="chat-system-note">${escapeHtml(m.body||"")}</div>`}const mine=m.sender==="staff";const senderLabel="";const role=m.sender==="ai"?"ai":m.sender==="client"?"client":"staff";const atMs=new Date(m.created_at).getTime();const isSeq=chatIsSequential(prevSender,m.sender,atMs);prevSender={key:m.sender,at:atMs};const headHtml=!mine&&!isSeq?chatMsgHeadHtml(role==="ai"?"AI Assistant":clientDisplayName,role,null):"";const time=new Date(m.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...clientChatTz?{timeZone:clientChatTz}:{}});if(chatEditingMessageId===m.id){return`
        <div class="chat-msg-row ${mine?"mine":"theirs"}">
          <div class="chat-msg-meta">${senderLabel}${time}</div>
          <div class="chat-msg-edit-wrap">
            <textarea class="chat-msg-edit-input" data-msg-id="${m.id}">${escapeHtml(m.body||"")}</textarea>
            <div class="chat-msg-edit-actions">
              <button type="button" class="chat-msg-edit-save-btn" data-msg-id="${m.id}" data-client-id="${clientId}">Save</button>
              <button type="button" class="chat-msg-edit-cancel-btn">Cancel</button>
            </div>
          </div>
        </div>`}const editedLabel=m.edited_at?' <span class="chat-edited-label">(edited)</span>':"";const editBtnHtml=mine&&m.body&&chatClientMessageEditable(m)?`<span class="chat-msg-edit-btn" data-msg-id="${m.id}" title="Edit message">&#9998;</span>`:"";const bodyHtml=m.body?`<div class="chat-msg-bubble">${chatLinkify(escapeHtml(m.body))}</div>`:"";const attachmentHtml=m.attachment_path?`<div class="chat-msg-attachment" data-attachment-path="${escapeHtml(m.attachment_path)}" data-attachment-mime="${escapeHtml(m.attachment_mime||"")}" data-attachment-name="${escapeHtml(m.attachment_name||"file")}">Loading attachment…</div>`:"";const ticksHtml=mine?chatTicksHtml(chatClientTickState(m)):"";return`
      <div class="chat-msg-row ${mine?"mine":"theirs with-av"} from-${role}${isSeq?" seq":""}">
        ${!mine&&!isSeq?chatMsgAvatarHtml(role,clientId,clientDisplayName):""}
        ${headHtml}
        ${bodyHtml}
        ${attachmentHtml}
        <div class="chat-msg-meta">${senderLabel}${time}${editedLabel}${editBtnHtml}${ticksHtml?" "+ticksHtml:""}${chatRxAddHtml("client",m.id,clientId)}</div>
        ${chatRxHtml("client",m.id,clientId,mine)}
      </div>
    `}).join("")||'<div class="empty" style="padding:16px 0;">No messages yet.</div>');if(chatIsPeerTyping(key))box.insertAdjacentHTML("beforeend",chatTypingBubbleHtml());if(wasAtBottom)chatPinToBottom(box);Promise.resolve(clientChatResolveVisibleAttachments(box)).then(()=>chatKeepPinned(box,wasAtBottom));chatWireClientEditControls(box,clientId)}function chatWireClientEditControls(box,clientId){box.querySelectorAll(".chat-msg-edit-btn").forEach(el=>{el.addEventListener("click",()=>{chatEditingMessageId=el.dataset.msgId;renderClientChatMessages(clientId)})});const input=box.querySelector(".chat-msg-edit-input");if(input){input.focus();input.setSelectionRange(input.value.length,input.value.length);input.addEventListener("keydown",e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();chatSaveEditedClientMessage(input.dataset.msgId,input.value,clientId)}else if(e.key==="Escape"){chatEditingMessageId=null;renderClientChatMessages(clientId)}})}const saveBtn=box.querySelector(".chat-msg-edit-save-btn");if(saveBtn)saveBtn.addEventListener("click",()=>chatSaveEditedClientMessage(saveBtn.dataset.msgId,input?input.value:"",clientId));const cancelBtn=box.querySelector(".chat-msg-edit-cancel-btn");if(cancelBtn)cancelBtn.addEventListener("click",()=>{chatEditingMessageId=null;renderClientChatMessages(clientId)})}async function chatSaveEditedClientMessage(id,newBody,clientId){const body=(newBody||"").trim();if(!body){alert("Message can't be empty.");return}const{data,error}=await sb.from("client_chat_messages").update({body,edited_at:new Date().toISOString()}).eq("id",id).select().single();chatEditingMessageId=null;if(error){alert("Failed to save edit — edits are only allowed within 15 minutes of sending. ("+error.message+")");renderClientChatMessages(clientId);return}const key="client:"+clientId;const conv=chatConversations[key];if(conv){const idx=conv.messages.findIndex(x=>x.id===id);if(idx!==-1)conv.messages[idx]=data}renderClientChatMessages(clientId)}const clientChatAttachmentUrlCache={};async function clientChatResolveAttachmentUrl(path){if(clientChatAttachmentUrlCache[path])return clientChatAttachmentUrlCache[path];const{data,error}=await sb.storage.from("client-chat-attachments").createSignedUrl(path,3600);if(error){console.error("Failed to sign client chat attachment URL",error);return null}clientChatAttachmentUrlCache[path]=data.signedUrl;return data.signedUrl}async function clientChatResolveVisibleAttachments(container){const els=Array.from((container||$("chatMessages")).querySelectorAll('.chat-msg-attachment:not([data-resolved="1"])'));for(const el of els){const path=el.dataset.attachmentPath;const mime=el.dataset.attachmentMime||"";const name=el.dataset.attachmentName||"file";const url=await clientChatResolveAttachmentUrl(path);if(!url){el.textContent="Attachment unavailable";continue}el.dataset.resolved="1";if(mime.startsWith("image/")){el.innerHTML=`<img src="${url}" alt="${escapeHtml(name)}">`;el.querySelector("img").addEventListener("click",()=>openPhotoViewer(url,name))}else if(mime.startsWith("audio/")){el.className="chat-msg-attachment chat-msg-audio";el.innerHTML=`<audio controls controlsList="nodownload noremoteplayback" src="${url}"></audio><a class="chat-msg-attachment-file chat-msg-download-link" href="${url}" rel="noopener" style="margin-top:4px;">Download ⬇</a>`;el.querySelector(".chat-msg-download-link").addEventListener("click",e=>{e.preventDefault();downloadFileToDisk(url,name)})}else{el.innerHTML=`<a class="chat-msg-attachment-file chat-msg-download-link" href="${url}" rel="noopener">&#128206; ${escapeHtml(name)}</a>`;el.querySelector(".chat-msg-download-link").addEventListener("click",e=>{e.preventDefault();downloadFileToDisk(url,name)})}}}async function acceptClientChatThread(clientId){const btn=$("chatClientClaimBtn");btn.disabled=true;btn.textContent="Accepting…";const{data,error}=await sb.from("client_chat_threads").update({assigned_staff_id:myUserId,status:"routed",updated_at:new Date().toISOString()}).eq("client_id",clientId).is("assigned_staff_id",null).select().maybeSingle();btn.disabled=false;btn.textContent="Accept";if(error){alert("Failed to accept: "+error.message);return}if(!data){alert("Someone else already picked this one up.");await loadClientChatOverview();renderChatConvList($("chatSearchInput").value);$("chatThreadBackBtn").click();return}await loadClientChatOverview();renderChatConvList($("chatSearchInput").value);if(chatActiveConv==="client:"+clientId)openClientChatThread(clientId)}async function endClientChatThread(clientId){if(!confirm("End this conversation? The client will see a note that it ended and can start a new one anytime."))return false;const closingNote=`Chat ended by ${myFullName||"our team"}. Send a new message anytime to start a fresh conversation.`;const{error:msgErr}=await sb.from("client_chat_messages").insert({client_id:clientId,sender:"staff",staff_id:myUserId,body:closingNote});if(msgErr)console.error("Failed to post end-chat note",msgErr);const{error}=await sb.from("client_chat_threads").update({status:"closed",assigned_staff_id:null,updated_at:new Date().toISOString()}).eq("client_id",clientId);if(error){alert("Failed to end chat: "+error.message);return false}delete chatConversations["client:"+clientId];await loadClientChatOverview();renderChatConvList($("chatSearchInput").value);$("chatThreadBackBtn").click();return true}async function sendClientChatReply(clientId,body,attachmentFields){if(!body&&!(attachmentFields&&attachmentFields.attachment_path))return;const{data,error}=await sb.from("client_chat_messages").insert({client_id:clientId,sender:"staff",staff_id:myUserId,body:body||"",...attachmentFields||{}}).select().single();if(error){console.error("Failed to send client chat reply",error);alert("Failed to send: "+error.message);return null}const key="client:"+clientId;if(!chatConversations[key])chatConversations[key]={key,messages:[],unread:0,loadedOnce:true};chatConversations[key].messages.push(data);if(chatActiveConv===key)renderClientChatMessages(clientId);const currentThread=clientChatThreadsDirectory.find(t=>t.client_id===clientId);if(!currentThread||currentThread.assigned_staff_id!==myUserId||currentThread.status!=="routed"){const{error:claimErr}=await sb.from("client_chat_threads").update({assigned_staff_id:myUserId,status:"routed",updated_at:new Date().toISOString()}).eq("client_id",clientId);if(claimErr)console.error("Failed to claim client chat thread on reply",claimErr)}return data}function chatHighlightMentions(escapedBody){const names=chatDirectory.map(p=>p.full_name||p.email).concat(chatGroupDirectory.map(p=>p.full_name||p.email)).concat([myFullName||myEmail]).filter(Boolean);if(chatActiveConv==="team"||chatActiveConv.startsWith("group:"))names.push("everyone");names.sort((a,b)=>b.length-a.length);let out=escapedBody;names.forEach(name=>{const token="@"+escapeHtml(name);if(!out.includes(token))return;const isMe=name===(myFullName||myEmail)||name==="everyone";out=out.split(token).join(`<span class="chat-mention${isMe?" me":""}">${token}</span>`)});return out}function chatMessageMentionsMe(m,key){const body=m&&m.body;if(!body)return false;const mine=myFullName||myEmail;if(mine&&body.includes("@"+mine))return true;if((key==="team"||key&&key.startsWith("group:"))&&body.includes("@everyone"))return true;return false}function chatLinkify(escapedBody){return escapedBody.replace(/(https?:\/\/[^\s<]+)/g,url=>{const trailing=url.match(/[.,;:!?)]+$/);const clean=trailing?url.slice(0,-trailing[0].length):url;const suffix=trailing?trailing[0]:"";return clean?`<a href="${clean}" target="_blank" rel="noopener noreferrer">${clean}</a>${suffix}`:url})}const AI_CHAT_VISIBLE_MS=24*60*60*1e3;function visibleChatThreadMessages(key,messages){if(!AI_ASSISTANT_BOT_ID||key!==AI_ASSISTANT_BOT_ID)return messages;const cutoff=Date.now()-AI_CHAT_VISIBLE_MS;return messages.filter(m=>new Date(m.created_at).getTime()>=cutoff)}let chatEditingMessageId=null;const CHAT_RX_EMOJIS=["👍","❤️","😂","😮","😢","🙏","🎉","✅"];const CHAT_RX_TABLE={staff:"chat_message_reactions",client:"client_chat_message_reactions"};const chatRx={staff:new Map,client:new Map};const chatRxFetched={staff:new Set,client:new Set};const chatRxQueue={staff:new Set,client:new Set};let chatRxTimer=null;function chatRxAttrs(kind,id,clientId){return`data-rx-kind="${kind}" data-rx-id="${escapeHtml(id)}"${clientId?` data-rx-client="${escapeHtml(clientId)}"`:""}`}function chatRxAddHtml(kind,id,clientId){return id?`<span class="chat-rx-add" role="button" tabindex="0" title="React" ${chatRxAttrs(kind,id,clientId)}>&#9786;+</span>`:""}function chatRxHtml(kind,id,clientId,mine){if(!id)return"";if(!chatRxFetched[kind].has(id)){chatRxQueue[kind].add(id);if(!chatRxTimer)chatRxTimer=setTimeout(chatRxFlush,60)}return`<div class="chat-rx${mine?" mine":""}" ${chatRxAttrs(kind,id,clientId)}>${chatRxInner(kind,id,clientId)}</div>`}function chatRxRow(kind,r){return kind==="staff"?{who:r.user_id,kind:"staff",emoji:r.emoji}:{who:r.reactor_id,kind:r.reactor_kind,emoji:r.emoji}}function chatRxAdd(kind,r){const x=chatRxRow(kind,r);const list=chatRx[kind].get(r.message_id)||[];if(!list.some(y=>y.who===x.who&&y.emoji===x.emoji))list.push(x);chatRx[kind].set(r.message_id,list)}function chatRxRemove(kind,r){const x=chatRxRow(kind,r);chatRx[kind].set(r.message_id,(chatRx[kind].get(r.message_id)||[]).filter(y=>!(y.who===x.who&&y.emoji===x.emoji)))}function chatRxName(r,clientId){if(r.who===myUserId)return"You";if(r.kind==="client")return clientId?chatClientThreadName(clientId):"Client";return chatContactName(r.who)||"Someone"}function chatRxInner(kind,id,clientId){const groups=new Map;(chatRx[kind].get(id)||[]).forEach(r=>{if(!groups.has(r.emoji))groups.set(r.emoji,[]);groups.get(r.emoji).push(r)});return[...groups.entries()].map(([emoji,rs])=>{const me=rs.some(r=>r.who===myUserId);return`<button type="button" class="chat-rx-chip${me?" me":""}" data-emoji="${escapeHtml(emoji)}" title="${escapeHtml(rs.map(r=>chatRxName(r,clientId)).join(", "))}">${escapeHtml(emoji)}<span>${rs.length}</span></button>`}).join("")}function chatRxPaint(kind,id){document.querySelectorAll(`.chat-rx[data-rx-kind="${kind}"][data-rx-id="${CSS.escape(id)}"]`).forEach(el=>{el.innerHTML=chatRxInner(kind,id,el.dataset.rxClient||null)})}async function chatRxFlush(){chatRxTimer=null;for(const kind of["staff","client"]){const ids=[...chatRxQueue[kind]];chatRxQueue[kind].clear();if(!ids.length)continue;ids.forEach(id=>chatRxFetched[kind].add(id));const cols=kind==="staff"?"message_id, user_id, emoji":"message_id, reactor_id, reactor_kind, emoji";const{data,error}=await selectInChunks(CHAT_RX_TABLE[kind],cols,"message_id",ids);if(error){ids.forEach(id=>chatRxFetched[kind].delete(id));console.warn("[chat] reactions load failed",error);continue}ids.forEach(id=>chatRx[kind].delete(id));data.forEach(r=>chatRxAdd(kind,r));ids.forEach(id=>chatRxPaint(kind,id))}}async function chatRxToggle(kind,id,clientId,emoji){if(!myUserId||!id)return;const has=(chatRx[kind].get(id)||[]).some(r=>r.who===myUserId&&r.emoji===emoji);const row=kind==="staff"?{message_id:id,user_id:myUserId,emoji}:{message_id:id,client_id:clientId,reactor_id:myUserId,reactor_kind:"staff",emoji};if(has)chatRxRemove(kind,row);else chatRxAdd(kind,row);chatRxPaint(kind,id);const whoCol=kind==="staff"?"user_id":"reactor_id";const res=has?await sb.from(CHAT_RX_TABLE[kind]).delete().eq("message_id",id).eq(whoCol,myUserId).eq("emoji",emoji):await sb.from(CHAT_RX_TABLE[kind]).insert(row);if(res.error&&res.error.code!=="23505"){if(has)chatRxAdd(kind,row);else chatRxRemove(kind,row);chatRxPaint(kind,id);alert("Could not save the reaction: "+res.error.message)}}let chatRxPickerFor=null;function chatRxOpenPicker(btn){let p=document.getElementById("chatRxPicker");if(!p){p=document.createElement("div");p.id="chatRxPicker";p.className="chat-rx-picker";p.hidden=true;p.innerHTML=CHAT_RX_EMOJIS.map(e=>`<button type="button" data-emoji="${e}" title="${e}">${e}</button>`).join("");document.body.appendChild(p)}chatRxPickerFor={kind:btn.dataset.rxKind,id:btn.dataset.rxId,clientId:btn.dataset.rxClient||null};p.hidden=false;const r=btn.getBoundingClientRect();const w=p.offsetWidth,h=p.offsetHeight;p.style.left=Math.max(8,Math.min(window.innerWidth-w-8,r.left+r.width/2-w/2))+"px";p.style.top=(r.top-h-6<8?r.bottom+6:r.top-h-6)+"px"}function chatRxClosePicker(){const p=document.getElementById("chatRxPicker");if(p&&!p.hidden)p.hidden=true;chatRxPickerFor=null}document.addEventListener("click",e=>{const pick=e.target.closest("#chatRxPicker button[data-emoji]");if(pick){e.stopPropagation();const f=chatRxPickerFor;chatRxClosePicker();if(f)chatRxToggle(f.kind,f.id,f.clientId,pick.dataset.emoji);return}const add=e.target.closest(".chat-rx-add");if(add){e.stopPropagation();chatRxOpenPicker(add);return}const chip=e.target.closest(".chat-rx-chip");if(chip){e.stopPropagation();const box=chip.closest(".chat-rx");chatRxToggle(box.dataset.rxKind,box.dataset.rxId,box.dataset.rxClient||null,chip.dataset.emoji);return}chatRxClosePicker()},true);document.addEventListener("keydown",e=>{if(e.key==="Escape")chatRxClosePicker();else if((e.key==="Enter"||e.key===" ")&&e.target&&e.target.classList&&e.target.classList.contains("chat-rx-add")){e.preventDefault();chatRxOpenPicker(e.target)}});document.addEventListener("scroll",chatRxClosePicker,true);let chatRxChannel=null;function subscribeChatReactions(){if(chatRxChannel||!sb)return;const ch=sb.channel("chat-reactions");chatRxChannel=ch;const handle=kind=>payload=>{const r=payload.eventType==="DELETE"?payload.old:payload.new;if(!r||!r.message_id||!chatRxFetched[kind].has(r.message_id))return;if(payload.eventType==="DELETE")chatRxRemove(kind,r);else chatRxAdd(kind,r);chatRxPaint(kind,r.message_id)};ch.on("postgres_changes",{event:"*",schema:"public",table:"chat_message_reactions"},handle("staff")).on("postgres_changes",{event:"*",schema:"public",table:"client_chat_message_reactions"},handle("client")).subscribe(status=>{if(ch!==chatRxChannel)return;if(status==="CHANNEL_ERROR"||status==="TIMED_OUT"){chatRxChannel=null;try{sb.removeChannel(ch)}catch(err){}setTimeout(subscribeChatReactions,5e3)}})}function renderChatThread(){const conv=chatConversations[chatActiveConv]||{messages:[]};const box=$("chatMessages");const wasAtBottom=chatAtBottom(box);const isDm=chatActiveConv!=="team"&&!chatActiveConv.startsWith("group:");const teamChatTz=nonAgentDisplayTz();let prevSender=null;chatSwapHtml(box,visibleChatThreadMessages(chatActiveConv,conv.messages).map(m=>{const mine=m.sender_id===myUserId;const senderLabel="";const atMs=new Date(m.created_at).getTime();const isSeq=chatIsSequential(prevSender,m.sender_id,atMs);prevSender={key:m.sender_id,at:atMs};const headHtml=!mine&&!isSeq?chatMsgHeadHtml(chatContactName(m.sender_id),"staff",chatContactRole(m.sender_id)):"";const time=new Date(m.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...teamChatTz?{timeZone:teamChatTz}:{}});if(chatEditingMessageId===m.id){return`
        <div class="chat-msg-row ${mine?"mine":"theirs"}">
          <div class="chat-msg-meta">${senderLabel}${time}</div>
          <div class="chat-msg-edit-wrap">
            <textarea class="chat-msg-edit-input" data-msg-id="${m.id}">${escapeHtml(m.body||"")}</textarea>
            <div class="chat-msg-edit-actions">
              <button type="button" class="chat-msg-edit-save-btn" data-msg-id="${m.id}">Save</button>
              <button type="button" class="chat-msg-edit-cancel-btn">Cancel</button>
            </div>
          </div>
        </div>`}const editedLabel=m.edited_at?' <span class="chat-edited-label">(edited)</span>':"";const editBtnHtml=mine&&m.body&&chatMessageEditable(m)?`<span class="chat-msg-edit-btn" data-msg-id="${m.id}" title="Edit message">&#9998;</span>`:"";const bodyHtml=m.body?`<div class="chat-msg-bubble">${chatLinkify(chatHighlightMentions(escapeHtml(m.body)))}</div>`:"";const attachmentHtml=m.attachment_path?`<div class="chat-msg-attachment" data-attachment-path="${escapeHtml(m.attachment_path)}" data-attachment-mime="${escapeHtml(m.attachment_mime||"")}" data-attachment-name="${escapeHtml(m.attachment_name||"file")}">Loading attachment…</div>`:"";const ticksHtml=mine?isDm?chatTicksHtml(chatDmTickState(m,chatActiveConv)):chatSeenByHtml(m,chatActiveConv):"";return`
      <div class="chat-msg-row ${mine?"mine":"theirs with-av"} from-staff${isSeq?" seq":""}">
        ${!mine&&!isSeq?chatMsgAvatarHtml(AI_ASSISTANT_BOT_ID&&m.sender_id===AI_ASSISTANT_BOT_ID?"ai":"staff",m.sender_id,chatContactName(m.sender_id)):""}
        ${headHtml}
        ${bodyHtml}
        ${attachmentHtml}
        <div class="chat-msg-meta">${senderLabel}${time}${editedLabel}${editBtnHtml}${myIsOwner?`<span class="chat-msg-del-btn" data-msg-id="${m.id}" title="Delete for everyone (owner only)">&#128465;</span>`:""}${ticksHtml?" "+ticksHtml:""}${chatRxAddHtml("staff",m.id,null)}</div>
        ${chatRxHtml("staff",m.id,null,mine)}
      </div>
    `}).join("")||'<div class="empty" style="padding:16px 0;">No messages yet — say hello!</div>');if(chatOfflineHint&&chatOfflineHint.key===chatActiveConv){const h=chatOfflineHint,nm=escapeHtml(h.name);const line=h.status==="dnd"?`⛔ ${nm} has Do not disturb on — they'll see this, just without a sound or pop-up.`:h.status==="busy"?`🟠 ${nm} is busy — they may take a while to reply.`:h.status==="brb"?`🕒 ${nm} will be right back.`:`📴 ${nm} is offline — they'll see this when they're back.`;const hs=chatPersonStatusMsg(h.key);box.insertAdjacentHTML("beforeend",`<div class="chat-offline-hint">${line}${hs?" Their status: "+escapeHtml(((hs.emoji?hs.emoji+" ":"")+(hs.text||"")).trim()):""}</div>`)}if(chatIsPeerTyping(chatActiveConv))box.insertAdjacentHTML("beforeend",chatTypingBubbleHtml());if(chatActiveConv==="team")box.insertAdjacentHTML("afterbegin",'<div class="chat-offline-hint" style="margin:0 0 8px;">📣 Announcements here clear automatically after 10 days.</div>');if(wasAtBottom)chatPinToBottom(box);Promise.resolve(chatResolveVisibleAttachments(box)).then(()=>chatKeepPinned(box,wasAtBottom));chatWireEditControls(box)}function chatWireEditControls(box){box.querySelectorAll(".chat-msg-del-btn").forEach(el=>{el.addEventListener("click",()=>chatDeleteMessage(el.dataset.msgId))});box.querySelectorAll(".chat-msg-edit-btn").forEach(el=>{el.addEventListener("click",()=>{chatEditingMessageId=el.dataset.msgId;renderChatThread()})});const input=box.querySelector(".chat-msg-edit-input");if(input){input.focus();input.setSelectionRange(input.value.length,input.value.length);input.addEventListener("keydown",e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();chatSaveEditedMessage(input.dataset.msgId,input.value)}else if(e.key==="Escape"){chatEditingMessageId=null;renderChatThread()}})}const saveBtn=box.querySelector(".chat-msg-edit-save-btn");if(saveBtn)saveBtn.addEventListener("click",()=>chatSaveEditedMessage(saveBtn.dataset.msgId,input?input.value:""));const cancelBtn=box.querySelector(".chat-msg-edit-cancel-btn");if(cancelBtn)cancelBtn.addEventListener("click",()=>{chatEditingMessageId=null;renderChatThread()})}async function chatDeleteMessage(id){if(!myIsOwner||!id)return false;if(!confirm("Delete this message for everyone? This cannot be undone."))return false;const{data,error}=await sb.from("chat_messages").delete().eq("id",id).select("id, attachment_path");if(error){alert(error.message);return false}if(!data||!data.length){alert("That message could not be deleted -- it may already be gone.");return false}const path=data[0].attachment_path;if(path){sb.storage.from("chat-attachments").remove([path]).then(({error:e})=>{if(e)console.warn("chat attachment not removed:",e.message)})}chatRemoveMessageFromState(id);logActivity("chat.message_delete","chat_message",id,"Deleted a staff chat message");return true}function chatRemoveMessageFromState(id){let touched=null;Object.entries(chatConversations).forEach(([key,conv])=>{if(touched||key.startsWith("client:")||key.startsWith("sms:"))return;const idx=conv.messages.findIndex(x=>x.id===id);if(idx===-1)return;conv.messages.splice(idx,1);const lastReadMs=chatLastRead[key]?new Date(chatLastRead[key]).getTime():0;conv.unread=conv.messages.filter(x=>x.sender_id!==myUserId&&new Date(x.created_at).getTime()>lastReadMs).length;touched=key});if(!touched)return;updateChatUnreadBadge();if(chatActiveConv===touched&&$("chatPanel").style.display!=="none")renderChatThread();else if(!chatActiveConv)renderChatConvList($("chatSearchInput").value)}async function chatSaveEditedMessage(id,newBody){const body=(newBody||"").trim();if(!body){alert("Message can't be empty.");return}const nowIso=new Date().toISOString();const{data,error}=await sb.from("chat_messages").update({body,edited_at:nowIso}).eq("id",id).select().single();chatEditingMessageId=null;if(error){alert("Failed to save edit — edits are only allowed within 15 minutes of sending. ("+error.message+")");renderChatThread();return}const key=chatConvKeyForMessage(data);const conv=chatConversations[key];if(conv){const idx=conv.messages.findIndex(x=>x.id===id);if(idx!==-1)conv.messages[idx]=data}renderChatThread()}async function downloadFileToDisk(url,filename){try{const resp=await fetch(url);if(!resp.ok)throw new Error("HTTP "+resp.status);const blob=await resp.blob();const blobUrl=URL.createObjectURL(blob);const a=document.createElement("a");a.href=blobUrl;a.download=filename||"download";document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(blobUrl),3e4)}catch(e){console.error("Download failed, falling back to opening it directly:",e);window.open(url,"_blank","noopener")}}const chatAttachmentUrlCache={};async function chatResolveAttachmentUrl(path){if(chatAttachmentUrlCache[path])return chatAttachmentUrlCache[path];const{data,error}=await sb.storage.from("chat-attachments").createSignedUrl(path,3600);if(error){console.error("Failed to sign chat attachment URL",error);return null}chatAttachmentUrlCache[path]=data.signedUrl;return data.signedUrl}function chatAtBottom(box,slack){if(!box)return true;return box.scrollHeight-box.scrollTop-box.clientHeight<=(slack===void 0?80:slack)}function chatPinToBottom(box){if(box)box.scrollTop=box.scrollHeight}function chatKeepPinned(box,wasAtBottom){if(!box||!wasAtBottom)return;chatPinToBottom(box);const imgs=box.querySelectorAll("img");imgs.forEach(img=>{if(img.complete)return;img.addEventListener("load",()=>chatPinToBottom(box),{once:true});img.addEventListener("error",()=>chatPinToBottom(box),{once:true})})}async function chatResolveVisibleAttachments(container){const els=Array.from((container||$("chatMessages")).querySelectorAll('.chat-msg-attachment:not([data-resolved="1"])'));for(const el of els){const path=el.dataset.attachmentPath;const mime=el.dataset.attachmentMime||"";const name=el.dataset.attachmentName||"file";const url=await chatResolveAttachmentUrl(path);if(!url){el.textContent="Attachment unavailable";continue}el.dataset.resolved="1";if(mime.startsWith("image/")){el.innerHTML=`<img src="${url}" alt="${escapeHtml(name)}">`;el.querySelector("img").addEventListener("click",()=>openPhotoViewer(url,name))}else if(mime.startsWith("audio/")){el.className="chat-msg-attachment chat-msg-audio";el.innerHTML=`<audio controls controlsList="nodownload noremoteplayback" src="${url}"></audio><a class="chat-msg-attachment-file chat-msg-download-link" href="${url}" rel="noopener" style="margin-top:4px;">Download ⬇</a>`;el.querySelector(".chat-msg-download-link").addEventListener("click",e=>{e.preventDefault();downloadFileToDisk(url,name)})}else{el.innerHTML=`<a class="chat-msg-attachment-file chat-msg-download-link" href="${url}" rel="noopener">&#128206; ${escapeHtml(name)}</a>`;el.querySelector(".chat-msg-download-link").addEventListener("click",e=>{e.preventDefault();downloadFileToDisk(url,name)})}}}let chatPendingFile=null;function chatClearPendingFile(){if(chatPendingFile&&chatPendingFile.previewUrl)URL.revokeObjectURL(chatPendingFile.previewUrl);chatPendingFile=null;$("chatAttachPreview").style.display="none";$("chatAttachPreview").innerHTML="";$("chatFileInput").value=""}let chatSendInFlight=false;function chatSendFailed(input,body,what,err){console.error("[chat] "+what,err);input.value=body;var detail=err&&(err.message||err.error_description)||"";var NL=String.fromCharCode(10);alert(what+(detail?NL+NL+detail:"")+NL+NL+"Your message has been put back in the box.")}async function sendChatMessage(){const input=$("chatInput");const body=input.value.trim();const pendingFile=chatPendingFile;if(!body&&!pendingFile||!chatActiveConv)return;if(chatSendInFlight)return;chatSendInFlight=true;const sendBtn=$("chatSendBtn");if(sendBtn)sendBtn.disabled=true;try{await sendChatMessageInner(input,body,pendingFile)}finally{chatSendInFlight=false;if(sendBtn)sendBtn.disabled=false}}async function sendChatMessageInner(input,body,pendingFile){if(chatActiveConv!=="team"&&!chatActiveConv.startsWith("group:")){clearTimeout(chatTypingStopTimer);chatTypingSend(chatActiveConv,"stopped");chatLastTypingPingAt=0}if(chatActiveConv.startsWith("client:")){const clientId=chatActiveConv.slice(7);input.value="";let clientAttachmentFields=null;if(pendingFile){const cleanName=pendingFile.file.name.replace(/[^a-zA-Z0-9._-]/g,"_");const path=`${clientId}/${Date.now()}-${cleanName}`;const{error:uploadErr}=await sb.storage.from("client-chat-attachments").upload(path,pendingFile.file,{contentType:pendingFile.file.type||"application/octet-stream"});if(uploadErr){chatSendFailed(input,body,"Couldn't upload that attachment.",uploadErr);return}clientAttachmentFields={attachment_path:path,attachment_name:pendingFile.file.name,attachment_mime:pendingFile.file.type||"application/octet-stream"};chatClearPendingFile()}await sendClientChatReply(clientId,body,clientAttachmentFields);return}input.value="";chatHideMentionBox();const isGroup=chatActiveConv.startsWith("group:");const payload={sender_id:myUserId,recipient_id:chatActiveConv==="team"||isGroup?null:chatActiveConv,group_id:isGroup?chatActiveConv.slice(6):null,body};if(pendingFile){const cleanName=pendingFile.file.name.replace(/[^a-zA-Z0-9._-]/g,"_");const path=`${myUserId}/${Date.now()}-${cleanName}`;const{error:uploadErr}=await sb.storage.from("chat-attachments").upload(path,pendingFile.file,{contentType:pendingFile.file.type||"application/octet-stream"});if(uploadErr){chatSendFailed(input,body,"Couldn't upload that attachment.",uploadErr);return}payload.attachment_path=path;payload.attachment_name=pendingFile.file.name;payload.attachment_mime=pendingFile.file.type||"application/octet-stream";chatClearPendingFile()}const{data,error}=await sb.from("chat_messages").insert(payload).select().single();if(error){chatSendFailed(input,body,"Your message wasn't sent.",error);return}const recipientSt=!isGroup&&chatActiveConv!=="team"?chatContactStatus(chatActiveConv):null;if(["offline","dnd","busy","brb"].includes(recipientSt)){chatOfflineHint={key:chatActiveConv,name:chatContactName(chatActiveConv),status:recipientSt};chatPlayOfflineHintSound()}else{chatOfflineHint=null}if(!isGroup&&chatActiveConv!=="team"){notifyPushRecipients([chatActiveConv],{title:`${myFullName||"A teammate"} sent you a message`,body:body||(pendingFile?"Sent a file":"New message"),tag:`chat-${myUserId<chatActiveConv?myUserId+"_"+chatActiveConv:chatActiveConv+"_"+myUserId}`,url:"/"})}chatAddMessageToState(data,true);if(AI_ASSISTANT_BOT_ID&&payload.recipient_id===AI_ASSISTANT_BOT_ID){sb.functions.invoke("ai-chat-assistant",{body:{message_id:data.id}}).then(({error:aiErr})=>{if(aiErr)console.error("ai-chat-assistant call failed",aiErr)}).catch(aiErr=>console.error("ai-chat-assistant call failed",aiErr))}}async function resyncClientChatMessages(){const ids=Object.keys(chatConversations).filter(k=>k.startsWith("client:")).map(k=>k.slice(7));if(!ids.length)return;const{data:msgs,error}=await sb.rpc("client_chat_messages_recent",{p_client_ids:ids,p_per_client_limit:100});if(error){console.error("resyncClientChatMessages failed",error);return}ids.forEach(clientId=>{const key="client:"+clientId;const conv=chatConversations[key];if(!conv)return;const existingIds=new Set(conv.messages.map(m=>m.id));(msgs||[]).filter(m=>m.client_id===clientId&&!existingIds.has(m.id)).forEach(m=>conv.messages.push(m));conv.messages.sort((a,b)=>new Date(a.created_at)-new Date(b.created_at))});if(chatActiveConv&&chatActiveConv.startsWith("client:"))renderClientChatMessages(chatActiveConv.slice(7));renderChatConvList($("chatSearchInput").value);updateChatUnreadBadge()}function subscribeChatRealtime(){if(chatChannel)return;chatChannel=sb.channel("chat-messages").on("postgres_changes",{event:"INSERT",schema:"public",table:"chat_messages"},payload=>{const m=payload.new;const isMine=m.sender_id===myUserId||m.recipient_id===myUserId;const isGroupImIn=m.group_id&&chatGroups[m.group_id];if(m.recipient_id!==null&&!isMine&&!isGroupImIn)return;chatAddMessageToState(m,true)}).on("postgres_changes",{event:"UPDATE",schema:"public",table:"chat_messages"},payload=>{const m=payload.new;const isMine=m.sender_id===myUserId||m.recipient_id===myUserId;const isGroupImIn=m.group_id&&chatGroups[m.group_id];if(m.recipient_id!==null&&!isMine&&!isGroupImIn)return;const key=chatConvKeyForMessage(m);const conv=chatConversations[key];if(conv){const idx=conv.messages.findIndex(x=>x.id===m.id);if(idx!==-1)conv.messages[idx]=m}if(chatActiveConv===key)renderChatThread()}).on("postgres_changes",{event:"DELETE",schema:"public",table:"chat_messages"},payload=>{const id=payload.old&&payload.old.id;if(id)chatRemoveMessageFromState(id)}).on("postgres_changes",{event:"*",schema:"public",table:"chat_reads"},payload=>{const row=payload.new&&payload.new.user_id?payload.new:payload.old;if(!row)return;chatReadsIndex[`${row.user_id}|${row.conversation_key}`]=payload.new?payload.new.last_read_at:void 0;if(row.user_id===myUserId)chatLastRead[row.conversation_key]=payload.new?payload.new.last_read_at:void 0;if(chatActiveConv&&!chatActiveConv.startsWith("client:"))renderChatThread()}).on("postgres_changes",{event:"INSERT",schema:"public",table:"chat_group_members",filter:`user_id=eq.${myUserId}`},async()=>{await loadChatGroups();if(!chatActiveConv)renderChatConvList($("chatSearchInput").value)}).on("postgres_changes",{event:"DELETE",schema:"public",table:"chat_groups"},payload=>{const deletedId=payload.old&&payload.old.id;if(!deletedId||!chatGroups[deletedId])return;delete chatGroups[deletedId];delete chatConversations["group:"+deletedId];if(chatActiveConv==="group:"+deletedId){alert("This group was deleted.");$("chatThreadBackBtn").click()}else if(!chatActiveConv){renderChatConvList($("chatSearchInput").value)}}).on("postgres_changes",{event:"INSERT",schema:"public",table:"client_chat_messages"},async payload=>{const m=payload.new;const key="client:"+m.client_id;const known=clientChatThreadsDirectory.some(t=>t.client_id===m.client_id);if(!known){await loadClientChatOverview();if(!chatActiveConv)renderChatConvList($("chatSearchInput").value);ciRefresh();if(m.sender==="client"&&clientChatThreadsDirectory.some(t=>t.client_id===m.client_id)&&!ciIsViewing(key))chatPlayNotificationSound();return}if(!chatConversations[key])chatConversations[key]={key,messages:[],unread:0};if(chatConversations[key].messages.some(x=>x.id===m.id))return;chatConversations[key].messages.push(m);if(m.sender==="client"){sb.rpc("mark_client_chat_delivered",{p_client_id:m.client_id}).then(({error})=>{if(error)console.error("mark_client_chat_delivered failed",error)})}const ciViewing=ciIsViewing(key);if(chatActiveConv===key||ciViewing){if(chatActiveConv===key)renderClientChatMessages(m.client_id);chatMarkRead(key);if(m.sender==="client"){sb.rpc("mark_client_chat_read",{p_client_id:m.client_id}).then(({error})=>{if(error)console.error("mark_client_chat_read failed",error)})}}else if(m.sender!=="staff"&&m.sender!=="system"){chatConversations[key].unread++;chatPlayNotificationSound();renderChatConvList($("chatSearchInput").value)}ciRefresh()}).on("postgres_changes",{event:"UPDATE",schema:"public",table:"client_chat_messages"},payload=>{const m=payload.new;const key="client:"+m.client_id;const conv=chatConversations[key];if(conv){const idx=conv.messages.findIndex(x=>x.id===m.id);if(idx!==-1)conv.messages[idx]=m}if(chatActiveConv===key)renderClientChatMessages(m.client_id);ciRefresh()}).on("postgres_changes",{event:"*",schema:"public",table:"client_chat_threads"},()=>{clearTimeout(clientChatThreadsRefreshTimer);clientChatThreadsRefreshTimer=setTimeout(async()=>{await loadClientChatOverview();if(!chatActiveConv)renderChatConvList($("chatSearchInput").value);else if(chatActiveConv.startsWith("client:"))openClientChatThread(chatActiveConv.slice(7));ciRefresh()},250)}).subscribe((status,err)=>{if(status==="CHANNEL_ERROR"||status==="TIMED_OUT"){console.error("Chat realtime channel "+status+(err&&err.message?": "+err.message:""));chatScheduleRealtimeRetry();return}if(status!=="SUBSCRIBED")return;chatRealtimeRetryDelay=2e3;if(chatChannelEverConnected)resyncClientChatMessages();chatChannelEverConnected=true})}var chatRealtimeRetryTimer=null;var chatRealtimeRetryDelay=2e3;function chatScheduleRealtimeRetry(){if(chatRealtimeRetryTimer)return;const delay=chatRealtimeRetryDelay;chatRealtimeRetryDelay=Math.min(chatRealtimeRetryDelay*2,6e4);chatRealtimeRetryTimer=setTimeout(()=>{chatRealtimeRetryTimer=null;if(!chatChannel)return;const old=chatChannel;chatChannel=null;try{sb.removeChannel(old)}catch(e){}subscribeChatRealtime();resyncClientChatMessages();loadClientChatOverview().then(()=>{if(!chatActiveConv)renderChatConvList($("chatSearchInput").value);ciRefresh()})},delay)}document.addEventListener("visibilitychange",()=>{if(document.visibilityState==="visible"&&chatChannel){resyncClientChatMessages();loadClientChatOverview()}});let ciInited=false;let ciInitPromise=null;let ciFilter=null;let ciOpenClientId=null;let ciQuickReplies=[];let ciTargets=null;const ciSummaryCache={};let ciPendingFile=null;let ciTypingAt=0;let ciTypingStopTimer=null;let ciQrEditingId=null;let ciNewDirectory=[];function ciIsAdmin(){return myIsOwner||myRole==="admin"}function ciCanUseInbox(){if(!myUserId)return false;if(ciIsAdmin())return true;const allowed=myPermissions&&myPermissions.allowedTabs||[];if(allowed.includes("client_chat"))return true;return String(myRole||"").toLowerCase().replace(/[\s_-]+/g,"")==="sales"}function ciTabActive(){const p=$("tab-client_chat");return!!(p&&p.classList.contains("active"))}function ciIsViewing(key){return ciTabActive()&&document.visibilityState==="visible"&&!!ciOpenClientId&&key==="client:"+ciOpenClientId}function ciThread(clientId){return clientChatThreadsDirectory.find(t=>t.client_id===clientId)||null}function ciConv(clientId){return chatConversations["client:"+clientId]||{messages:[],unread:0}}function ciLast(clientId){const m=ciConv(clientId).messages;return m.length?m[m.length-1]:null}function ciUnclaimed(t){return!!t&&t.status==="routed"&&!t.assigned_staff_id}function ciStaffName(id){if(!id)return"";if(id===myUserId)return"You";const s=ciTargets&&(ciTargets.staff||[]).find(x=>x.id===id);return s&&s.name||typeof chatContactName==="function"&&chatContactName(id)||"a teammate"}function ciInFilter(t,f){if(f==="unassigned")return ciUnclaimed(t);if(f==="mine")return t.assigned_staff_id===myUserId&&t.status!=="closed";if(f==="closed")return t.status==="closed";return t.status!=="closed"}function ciInitials(name){const p=String(name||"?").trim().split(/\s+/).filter(Boolean);if(!p.length)return"?";return((p[0][0]||"?")+(p.length>1?p[p.length-1][0]:"")).toUpperCase()}const CI_AV_COLORS=["#C9A66B","#4EA8FF","#B388FF","#2DD4BF","#F5A623","#F28B82","#3DD68C"];function ciAvColor(id){let h=0;for(const ch of String(id))h=h*31+ch.charCodeAt(0)>>>0;return CI_AV_COLORS[h%CI_AV_COLORS.length]}function ciTzOpt(){const tz=nonAgentDisplayTz();return tz?{timeZone:tz}:{}}function ciAgo(iso){if(!iso)return"";const d=new Date(iso);const s=(Date.now()-d.getTime())/1e3;if(s<60)return"now";if(s<3600)return Math.floor(s/60)+"m";if(s<86400)return Math.floor(s/3600)+"h";if(s<7*86400)return d.toLocaleDateString([],{weekday:"short",...ciTzOpt()});return d.toLocaleDateString([],{month:"short",day:"numeric",...ciTzOpt()})}function ciDate(iso){return iso?new Date(iso).toLocaleDateString([],{month:"short",day:"numeric",year:"numeric",...ciTzOpt()}):"—"}function ciPreview(m){if(!m)return"";const who=m.sender==="staff"?m.staff_id===myUserId?"You: ":ciStaffName(m.staff_id)+": ":m.sender==="ai"?"AI: ":"";const body=m.body||(m.attachment_mime&&m.attachment_mime.startsWith("audio/")?"🎤 Voice message":m.attachment_name?"📎 "+m.attachment_name:"");return who+body}function ciStatusLine(t){if(!t)return"";if(t.status==="closed")return"Closed";if(t.status==="ai"||!t.status)return"With the AI assistant";const team=t.queue_key?" · "+chatQueueLabel(t.queue_key):"";if(!t.assigned_staff_id)return"Waiting to be accepted"+team;return(t.assigned_staff_id===myUserId?"Assigned to you":"Assigned to "+ciStaffName(t.assigned_staff_id))+team}function ciUnreadTotal(){let n=0;Object.entries(chatConversations).forEach(([k,c])=>{if(k.startsWith("client:"))n+=c.unread||0});return n}function ciSetNavBadge(n){const b=$("ciNavBadge");if(!b)return;b.textContent=n>99?"99+":String(n);b.style.display=n>0?"":"none"}function ciChatLinkRowHtml(){if(!ciCanUseInbox())return"";let waiting=0;clientChatThreadsDirectory.forEach(t=>{if(ciUnclaimed(t))waiting++});const unread=ciUnreadTotal();const sub=unread?`${unread} unread from clients`:waiting?`${waiting} waiting to be accepted`:"Client conversations are on their own page now";return`<div class="chat-conv-item ci-link-row" data-ci-link="1">
      <div class="chat-conv-avatar" style="width:32px; height:32px;">💬</div>
      <div class="chat-conv-info"><div class="chat-conv-name">Client Inbox →</div><div class="chat-conv-preview">${escapeHtml(sub)}</div></div>
      ${unread?`<div class="chat-conv-badge">${unread>99?"99+":unread}</div>`:""}
    </div>`}function ciInitTab(){if(!ciCanUseInbox())return Promise.resolve();if(ciInitPromise)return ciInitPromise;ciInitPromise=(async()=>{if(!ciInited){ciInited=true;ciWire()}if(!ciTargets)ciLoadTargets();ciLoadQuickReplies();await loadClientChatOverview();if(!ciFilter){ciFilter=clientChatThreadsDirectory.some(t=>ciInFilter(t,"unassigned"))?"unassigned":clientChatThreadsDirectory.some(t=>ciInFilter(t,"mine"))?"mine":"all"}ciRefresh()})().finally(()=>{ciInitPromise=null});return ciInitPromise}async function ciLoadTargets(){const{data,error}=await sb.rpc("client_chat_transfer_targets");if(error){console.error("client_chat_transfer_targets failed",error);return}ciTargets=data||{staff:[],teams:[]};if(ciOpenClientId){$("ciDetails").__sig=null;ciRenderDetails()}}async function ciLoadQuickReplies(){const{data,error}=await sb.from("client_chat_quick_replies").select("id, title, body, sort").eq("kind","chat").order("sort").order("title");if(error){console.error("quick replies load failed",error);return}ciQuickReplies=data||[]}function ciRefresh(){ciSetNavBadge(ciUnreadTotal());if(!ciTabActive())return;ciRenderList();if(ciOpenClientId){if(!ciThread(ciOpenClientId)){ciClose();return}ciRenderHead();ciRenderMsgs(false);ciRenderDetails()}}function ciRenderList(){const q=($("ciSearch").value||"").trim().toLowerCase();const counts={unassigned:0,mine:0,all:0,closed:0};clientChatThreadsDirectory.forEach(t=>{Object.keys(counts).forEach(f=>{if(ciInFilter(t,f))counts[f]++})});document.querySelectorAll("#ciFilters .ci-filter").forEach(b=>{const f=b.dataset.f;b.classList.toggle("active",f===ciFilter);b.querySelector("span").textContent=counts[f]?String(counts[f]):"";if(f==="closed")b.style.display=ciIsAdmin()?"":"none"});const rows=clientChatThreadsDirectory.filter(t=>ciInFilter(t,ciFilter)).filter(t=>!q||chatClientThreadName(t.client_id).toLowerCase().includes(q)||ciConv(t.client_id).messages.some(m=>(m.body||"").toLowerCase().includes(q))).map(t=>({t,last:ciLast(t.client_id),unread:ciConv(t.client_id).unread||0})).sort((a,b)=>(b.last?new Date(b.last.created_at).getTime():0)-(a.last?new Date(a.last.created_at).getTime():0));const box=$("ciRows");if(!rows.length){const empty={unassigned:"Nobody is waiting. New requests from clients land here.",mine:"Nothing assigned to you right now.",all:"No open client conversations.",closed:"No closed conversations."};box.innerHTML=`<div class="ci-list-empty">${q?"No conversation matches that search.":empty[ciFilter]||""}</div>`;return}box.innerHTML=rows.map(({t,last,unread})=>{const name=t.full_name||"Client";let tag="";if(ciUnclaimed(t))tag=`<span class="ci-tag wait">Waiting${t.queue_key?" · "+escapeHtml(chatQueueLabel(t.queue_key)):""}</span>`;else if(t.status==="ai"||!t.status)tag='<span class="ci-tag ai">AI assistant</span>';else if(t.status==="closed")tag='<span class="ci-tag">Closed</span>';else if(t.assigned_staff_id===myUserId)tag='<span class="ci-tag me">Yours</span>';else if(t.assigned_staff_id)tag=`<span class="ci-tag">${escapeHtml(ciStaffName(t.assigned_staff_id))}</span>`;const typing=chatIsPeerTyping("client:"+t.client_id);return`<div class="ci-row${t.client_id===ciOpenClientId?" active":""}${unread?" unread":""}" data-id="${escapeHtml(t.client_id)}">
        <div class="ci-av" style="background:${ciAvColor(t.client_id)}">${escapeHtml(ciInitials(name))}</div>
        <div class="ci-row-main">
          <div class="ci-row-top"><span class="ci-row-name">${escapeHtml(name)}</span><span class="ci-row-time">${escapeHtml(ciAgo(last&&last.created_at))}</span></div>
          ${t.company_name?`<div class="ci-row-co">${escapeHtml(t.company_name)}</div>`:""}
          <div class="ci-row-bottom"><span class="ci-row-prev${typing?" typing":""}">${escapeHtml(typing?"typing…":ciPreview(last)||"No messages yet")}</span>${unread?`<span class="ci-unread">${unread>99?"99+":unread}</span>`:""}</div>
          ${tag?`<div class="ci-row-tags">${tag}</div>`:""}
        </div>
      </div>`}).join("")}function ciOpen(clientId){const t=ciThread(clientId);if(!t)return;if(ciOpenClientId!==clientId){ciClearAttachment();$("ciDetails").__sig=null}ciOpenClientId=clientId;chatOpenClientTypingChannel(clientId);$("ciEmpty").classList.add("ci-hide");$("ciThread").classList.remove("ci-hide");$("ciWrap").classList.add("show-thread");$("ciQrMenu").classList.add("ci-hide");const key="client:"+clientId;chatMarkRead(key);sb.rpc("mark_client_chat_read",{p_client_id:clientId}).then(({error})=>{if(error)console.error("mark_client_chat_read failed",error)});ciRenderList();ciRenderHead();ciRenderMsgs(true);ciRenderDetails();ciSetNavBadge(ciUnreadTotal());renderChatConvList($("chatSearchInput")?$("chatSearchInput").value:"");if(!ciUnclaimed(t))$("ciInput").focus()}function ciClose(){ciOpenClientId=null;ciClearAttachment();$("ciThread").classList.add("ci-hide");$("ciEmpty").classList.remove("ci-hide");$("ciWrap").classList.remove("show-thread","show-details");$("ciDetails").innerHTML="";$("ciDetails").__sig=null;ciRenderList()}async function ciOpenFromElsewhere(clientId){if(!ciCanUseInbox())return;activateTab("client_chat");await ciInitTab();if(!clientId)return;if(!ciThread(clientId))await loadClientChatOverview();const t=ciThread(clientId);if(!t){alert("That conversation isn't available to you any more -- it may have been handed to someone else or closed.");return}if(!ciInFilter(t,ciFilter))ciFilter=["unassigned","mine","all","closed"].find(f=>ciInFilter(t,f))||"all";ciOpen(clientId)}function ciRenderHead(){const t=ciThread(ciOpenClientId);if(!t)return;$("ciThreadName").textContent=chatClientThreadName(t.client_id);$("ciThreadSub").textContent=ciStatusLine(t);const unclaimed=ciUnclaimed(t);$("ciClaimBar").classList.toggle("ci-hide",!unclaimed);$("ciClaimText").textContent="Waiting to be accepted"+(t.queue_key?" — "+chatQueueLabel(t.queue_key):"")+". Accept it to reply.";$("ciComposer").classList.toggle("ci-hide",unclaimed);$("ciCallBtn").classList.toggle("ci-hide",!(myPermissions.canTextClients&&!unclaimed))}function ciDayLabel(d){const key=x=>x.toLocaleDateString("en-CA",ciTzOpt());const k=key(d);if(k===key(new Date))return"Today";if(k===key(new Date(Date.now()-864e5)))return"Yesterday";return d.toLocaleDateString([],{weekday:"short",month:"short",day:"numeric",...ciTzOpt()})}function ciRenderMsgs(forceBottom){const clientId=ciOpenClientId;if(!clientId)return;const box=$("ciMsgs");const msgs=ciConv(clientId).messages;const typing=chatIsPeerTyping("client:"+clientId);const sig=clientId+"|"+typing+"|"+msgs.map(m=>m.id+(m.read_at?"r":m.delivered_at?"d":"")+(m.edited_at||"")).join(",");if(!forceBottom&&box.__sig===sig)return;box.__sig=sig;const wasAtBottom=forceBottom||chatAtBottom(box);const clientName=chatClientThreadName(clientId);let prev=null,lastDay=null;const html=msgs.map(m=>{const d=new Date(m.created_at);const day=ciDayLabel(d);let sep="";if(day!==lastDay){sep=`<div class="ci-day"><span>${escapeHtml(day)}</span></div>`;prev=null;lastDay=day}if(m.sender==="system"){prev=null;return`${sep}<div class="chat-system-note">${escapeHtml(m.body||"")}</div>`}const mine=m.sender==="staff";const role=m.sender==="ai"?"ai":m.sender==="client"?"client":"staff";const at=d.getTime();const senderKey=m.sender+":"+(m.staff_id||"");const seq=chatIsSequential(prev,senderKey,at);prev={key:senderKey,at};let head="";if(!seq){if(!mine)head=chatMsgHeadHtml(role==="ai"?"AI Assistant":clientName,role,null);else if(m.staff_id&&m.staff_id!==myUserId)head=`<div class="chat-msg-head"><span class="chat-msg-head-name">${escapeHtml(ciStaffName(m.staff_id))}</span></div>`}const time=d.toLocaleTimeString([],{hour:"numeric",minute:"2-digit",...ciTzOpt()});const body=m.body?`<div class="chat-msg-bubble">${chatLinkify(escapeHtml(m.body))}</div>`:"";const att=m.attachment_path?`<div class="chat-msg-attachment" data-attachment-path="${escapeHtml(m.attachment_path)}" data-attachment-mime="${escapeHtml(m.attachment_mime||"")}" data-attachment-name="${escapeHtml(m.attachment_name||"file")}">Loading attachment…</div>`:"";const ticks=mine?" "+chatTicksHtml(chatClientTickState(m)):"";const edited=m.edited_at?' <span class="chat-edited-label">(edited)</span>':"";return`${sep}<div class="chat-msg-row ${mine?"mine":"theirs with-av"} from-${role}${seq?" seq":""}">${!mine&&!seq?chatMsgAvatarHtml(role,clientId,clientName):""}${head}${body}${att}<div class="chat-msg-meta">${escapeHtml(time)}${edited}${ticks}${chatRxAddHtml("client",m.id,clientId)}</div>${chatRxHtml("client",m.id,clientId,mine)}</div>`}).join("");chatSwapHtml(box,(html||'<div class="ci-list-empty">No messages yet. Say hello.</div>')+(typing?chatTypingBubbleHtml():""));if(wasAtBottom)chatPinToBottom(box);Promise.resolve(clientChatResolveVisibleAttachments(box)).then(()=>chatKeepPinned(box,wasAtBottom))}async function ciRenderDetails(){const clientId=ciOpenClientId;if(!clientId)return;const t=ciThread(clientId);if(!t)return;const box=$("ciDetails");const cached=ciSummaryCache[clientId];if(!cached||Date.now()-cached.at>6e4){if(!cached)box.innerHTML='<div class="ci-list-empty">Loading…</div>';const{data,error}=await sb.rpc("client_inbox_client_summary",{p_client_id:clientId});if(error)console.error("client_inbox_client_summary failed",error);ciSummaryCache[clientId]={at:Date.now(),data:data||null};if(ciOpenClientId!==clientId)return}const s=ciSummaryCache[clientId]&&ciSummaryCache[clientId].data||{};const sig=[clientId,t.status,t.assigned_staff_id,t.queue_key,ciSummaryCache[clientId].at,!!ciTargets].join("|");if(box.__sig===sig)return;box.__sig=sig;const a=s.account||{};const money=c=>"$"+(Number(c||0)/100).toFixed(2);const camps=(s.campaigns||[]).map(c=>`<div class="ci-d-item"><div class="ci-d-item-name">${escapeHtml(c.name||"Campaign")}</div>
      <div class="ci-d-item-sub"><span class="ci-tag ${c.status==="active"?"me":""}">${escapeHtml(c.status||"—")}</span>${c.complimentary?' <span class="ci-tag">Complimentary</span>':""}${c.unpaid?` <span class="ci-tag bad">Unpaid ${money(c.unpaid_cents)}</span>`:""}${c.renews&&c.status==="active"?` renews ${escapeHtml(ciDate(c.renews))}`:""}</div></div>`).join("")||'<div class="ci-d-none">No campaigns yet.</div>';const o=s.orders||{};const orders=o.count?`${o.count} list order${o.count===1?"":"s"}`+(o.latest?` · latest: ${escapeHtml(o.latest.type||"order")} (${escapeHtml(o.latest.status||"new")}), ${escapeHtml(ciDate(o.latest.created_at))}`:""):"No list orders.";const pays=(s.payments||[]).map(p=>`<div class="ci-d-item-sub">${escapeHtml(p.campaign||"Campaign")} — ${escapeHtml(p.status||"—")}${p.paid_at?", paid "+escapeHtml(ciDate(p.paid_at)):""}</div>`).join("")||'<div class="ci-d-none">No payment requests.</div>';const canTransfer=ciIsAdmin()||t.assigned_staff_id===myUserId||ciUnclaimed(t);const staffOpts=(ciTargets&&ciTargets.staff||[]).filter(x=>x.id!==t.assigned_staff_id).map(x=>`<option value="staff:${escapeHtml(x.id)}">${escapeHtml(x.id===myUserId?"Me ("+x.name+")":x.name)}${x.title?" — "+escapeHtml(x.title):""}</option>`).join("");const teamOpts=(ciTargets&&ciTargets.teams||[]).map(x=>`<option value="team:${escapeHtml(x.key)}">${escapeHtml(x.label)}</option>`).join("");const canClose=t.status!=="closed"&&!ciUnclaimed(t)&&(ciIsAdmin()||t.assigned_staff_id===myUserId);box.innerHTML=`
    <div class="ci-d-head">
      <div class="ci-av big" style="background:${ciAvColor(clientId)}">${escapeHtml(ciInitials(a.full_name||t.full_name))}</div>
      <div class="ci-d-name">${escapeHtml(a.full_name||t.full_name||"Client")}</div>
      ${a.company_name||t.company_name?`<div class="ci-d-co">${escapeHtml(a.company_name||t.company_name)}</div>`:""}
      ${a.is_active===false?'<div style="margin-top:6px;"><span class="ci-tag bad">Account disabled</span></div>':""}
    </div>
    <div class="ci-d-sec">
      ${a.email?`<a class="ci-d-line" href="mailto:${escapeHtml(a.email)}">✉ ${escapeHtml(a.email)}</a>`:""}
      ${a.phone?`<a class="ci-d-line" href="tel:${escapeHtml(a.phone)}">☎ ${escapeHtml(a.phone)}</a>`:""}
      <div class="ci-d-line dim">Client since ${escapeHtml(ciDate(a.created_at))}${a.last_login_at?" · last signed in "+escapeHtml(ciDate(a.last_login_at)):""}</div>
    </div>
    <div class="ci-d-sec"><div class="ci-d-label">Campaigns</div>${camps}</div>
    <div class="ci-d-sec"><div class="ci-d-label">List orders</div><div class="ci-d-item-sub">${orders}</div></div>
    <div class="ci-d-sec"><div class="ci-d-label">Payments</div>${pays}</div>
    <div class="ci-d-sec">
      <div class="ci-d-label">Conversation</div>
      <div class="ci-d-item-sub">${escapeHtml(ciStatusLine(t))}</div>
      ${canTransfer?`<div class="ci-transfer">
          <select id="ciTransferSel"><option value="">Transfer to…</option>${staffOpts?`<optgroup label="Teammates">${staffOpts}</optgroup>`:""}${teamOpts?`<optgroup label="Teams — first to accept gets it">${teamOpts}</optgroup>`:""}</select>
          <button type="button" class="ci-btn" id="ciTransferBtn">Transfer</button>
        </div>
        <div class="ci-d-msg" id="ciTransferMsg"></div>`:""}
      ${canClose?'<button type="button" class="ci-btn danger" id="ciCloseBtn">Close conversation</button>':""}
    </div>`;const tb=$("ciTransferBtn");if(tb)tb.onclick=ciDoTransfer;const cb=$("ciCloseBtn");if(cb)cb.onclick=async()=>{const done=await endClientChatThread(clientId);if(done){delete ciSummaryCache[clientId];ciClose();ciRefresh()}}}async function ciDoTransfer(){const clientId=ciOpenClientId;const sel=$("ciTransferSel");const msg=$("ciTransferMsg");if(!clientId||!sel)return;const v=sel.value;if(!v){msg.textContent="Choose a teammate or a team.";msg.className="ci-d-msg err";return}const kind=v.slice(0,v.indexOf(":"));const id=v.slice(v.indexOf(":")+1);const label=sel.selectedOptions[0]?sel.selectedOptions[0].textContent:"";$("ciTransferBtn").disabled=true;const{error}=await sb.rpc("client_chat_transfer",{p_client_id:clientId,p_staff_id:kind==="staff"?id:null,p_queue_key:kind==="team"?id:null});if($("ciTransferBtn"))$("ciTransferBtn").disabled=false;if(error){msg.textContent=error.message;msg.className="ci-d-msg err";return}logActivity("client_chat.transfer","client_chat_thread",clientId,`Transferred ${chatClientThreadName(clientId)} to ${label}`);msg.textContent="Transferred to "+label+".";msg.className="ci-d-msg ok";await loadClientChatOverview();if($("ciDetails"))$("ciDetails").__sig=null;ciRefresh()}function ciAutoGrow(){const i=$("ciInput");i.style.height="auto";i.style.height=Math.min(140,i.scrollHeight)+"px"}function ciTypingPing(){if(!ciOpenClientId)return;const key="client:"+ciOpenClientId;clearTimeout(ciTypingStopTimer);if(!$("ciInput").value.trim()){chatTypingSend(key,"stopped");ciTypingAt=0;return}if(Date.now()-ciTypingAt>2e3){ciTypingAt=Date.now();chatTypingSend(key,"typing")}ciTypingStopTimer=setTimeout(()=>{chatTypingSend(key,"stopped");ciTypingAt=0},2500)}function ciSetAttachment(file){if(!file)return;if(file.size>15*1024*1024){alert("That file is too large — 15MB max.");return}ciPendingFile=file;$("ciAttachName").textContent="📎 "+file.name;$("ciAttachPreview").classList.remove("ci-hide")}function ciClearAttachment(){ciPendingFile=null;if($("ciAttachPreview"))$("ciAttachPreview").classList.add("ci-hide")}async function ciSend(){const clientId=ciOpenClientId;if(!clientId)return;const input=$("ciInput");const btn=$("ciSendBtn");const body=input.value.trim();if(!body&&!ciPendingFile||btn.disabled)return;btn.disabled=true;clearTimeout(ciTypingStopTimer);chatTypingSend("client:"+clientId,"stopped");ciTypingAt=0;try{let fields=null;if(ciPendingFile){const f=ciPendingFile;const path=`${clientId}/${Date.now()}-${f.name.replace(/[^a-zA-Z0-9._-]/g,"_")}`;const{error:upErr}=await sb.storage.from("client-chat-attachments").upload(path,f,{contentType:f.type||"application/octet-stream"});if(upErr){console.error("inbox attachment upload failed",upErr);alert("Couldn't upload that attachment — try again.");return}fields={attachment_path:path,attachment_name:f.name,attachment_mime:f.type||"application/octet-stream"}}input.value="";ciAutoGrow();const sent=await sendClientChatReply(clientId,body,fields);if(!sent){input.value=body;ciAutoGrow();return}ciClearAttachment();ciRenderMsgs(true);ciRenderList()}finally{btn.disabled=false}}function ciFillPlaceholders(text){const t=ciThread(ciOpenClientId)||{};const first=String(t.full_name||"").trim().split(/\s+/)[0]||"there";return String(text).replaceAll("{client_name}",first).replaceAll("{company}",t.company_name||"your company").replaceAll("{my_name}",String(myFullName||"").trim().split(/\s+/)[0]||"our team")}function ciRenderQrMenu(){const menu=$("ciQrMenu");const items=ciQuickReplies.map(q=>`<button type="button" class="ci-qr-item" data-id="${escapeHtml(q.id)}"><span class="ci-qr-title">${escapeHtml(q.title)}</span><span class="ci-qr-body">${escapeHtml(q.body)}</span></button>`).join("");menu.innerHTML=(items||`<div class="ci-list-empty">No quick replies yet.${ciIsAdmin()?"":" An owner or admin can add them."}</div>`)+(ciIsAdmin()?'<button type="button" class="ci-qr-manage" id="ciQrManageBtn">Manage quick replies…</button>':"");menu.querySelectorAll(".ci-qr-item").forEach(b=>{b.onclick=()=>{const q=ciQuickReplies.find(x=>x.id===b.dataset.id);if(!q)return;const input=$("ciInput");const text=ciFillPlaceholders(q.body);const s=input.selectionStart??input.value.length;const e=input.selectionEnd??input.value.length;input.value=input.value.slice(0,s)+text+input.value.slice(e);menu.classList.add("ci-hide");input.focus();input.selectionStart=input.selectionEnd=s+text.length;ciAutoGrow()}});const mb=$("ciQrManageBtn");if(mb)mb.onclick=()=>{menu.classList.add("ci-hide");ciOpenQrModal()}}let ciQrKind="chat";function ciQrList(){return ciQrKind==="dispute"?disputeQuickReplies:ciQuickReplies}async function ciReloadQrKind(){if(ciQrKind==="dispute")await disputeLoadQuickReplies();else await ciLoadQuickReplies()}function ciOpenQrModal(kind){ciQrKind=kind==="dispute"?"dispute":"chat";const modal=$("ciQrModal");if(modal.parentElement!==document.body)document.body.appendChild(modal);modal.querySelector(".ci-modal-head span").textContent=ciQrKind==="dispute"?"Dispute & feedback replies":"Quick replies";modal.querySelector(".ci-modal-note").textContent=ciQrKind==="dispute"?"Saved answers for dispute and feedback threads. Placeholders fill in when used: {client_name} (their first name), {company}, {seller} (the lead’s seller), {my_name}.":"Saved answers for everyone who takes client chats. Placeholders fill in when used: {client_name} (their first name), {company}, {my_name}.";ciQrEditingId=null;$("ciQrTitle").value="";$("ciQrBody").value="";$("ciQrMsg").textContent="";$("ciQrSave").textContent="Add reply";ciRenderQrManageList();$("ciQrModal").classList.remove("ci-hide")}function ciRenderQrManageList(){const box=$("ciQrManageList");box.innerHTML=ciQrList().length?ciQrList().map(q=>`<div class="ci-qr-manage-row">
      <div><b>${escapeHtml(q.title)}</b><span>${escapeHtml(q.body)}</span></div>
      <button type="button" class="ci-btn" data-edit="${escapeHtml(q.id)}">Edit</button>
      <button type="button" class="ci-btn" data-del="${escapeHtml(q.id)}" style="color:var(--danger);">Delete</button>
    </div>`).join(""):'<div class="ci-d-none">None yet — add the first one below.</div>';box.querySelectorAll("[data-edit]").forEach(b=>{b.onclick=()=>{const q=ciQrList().find(x=>x.id===b.dataset.edit);if(!q)return;ciQrEditingId=q.id;$("ciQrTitle").value=q.title;$("ciQrBody").value=q.body;$("ciQrSave").textContent="Save changes";$("ciQrTitle").focus()}});box.querySelectorAll("[data-del]").forEach(b=>{b.onclick=async()=>{const q=ciQrList().find(x=>x.id===b.dataset.del);if(!q||!confirm(`Delete the quick reply "${q.title}"?`))return;const{error}=await sb.from("client_chat_quick_replies").delete().eq("id",q.id);if(error){$("ciQrMsg").textContent=error.message;$("ciQrMsg").className="ci-d-msg err";return}await ciReloadQrKind();ciRenderQrManageList()}})}async function ciSaveQr(){const title=$("ciQrTitle").value.trim();const body=$("ciQrBody").value.trim();const msg=$("ciQrMsg");if(!title||!body){msg.textContent="A title and the reply are both needed.";msg.className="ci-d-msg err";return}$("ciQrSave").disabled=true;const res=ciQrEditingId?await sb.from("client_chat_quick_replies").update({title,body,updated_at:new Date().toISOString()}).eq("id",ciQrEditingId):await sb.from("client_chat_quick_replies").insert({title,body,sort:ciQrList().length,kind:ciQrKind});$("ciQrSave").disabled=false;if(res.error){msg.textContent=res.error.message;msg.className="ci-d-msg err";return}msg.textContent=ciQrEditingId?"Saved.":"Added.";msg.className="ci-d-msg ok";ciQrEditingId=null;$("ciQrTitle").value="";$("ciQrBody").value="";$("ciQrSave").textContent="Add reply";await ciReloadQrKind();ciRenderQrManageList()}async function ciOpenNewModal(){$("ciNewSearch").value="";$("ciNewMsg").textContent="";$("ciNewList").innerHTML='<div class="ci-list-empty">Loading…</div>';$("ciNewModal").classList.remove("ci-hide");const{data,error}=await sb.from("client_accounts").select("id, full_name, company_name, email").eq("is_active",true).order("full_name");if(error){$("ciNewList").innerHTML=`<div class="ci-list-empty">${escapeHtml(error.message)}</div>`;return}ciNewDirectory=data||[];ciRenderNewList();$("ciNewSearch").focus()}function ciRenderNewList(){const q=$("ciNewSearch").value.trim().toLowerCase();const rows=ciNewDirectory.filter(c=>!q||[c.full_name,c.company_name,c.email].some(v=>String(v||"").toLowerCase().includes(q)));$("ciNewList").innerHTML=rows.length?rows.map(c=>`<div class="ci-new-row" data-id="${escapeHtml(c.id)}">
      <div class="ci-av" style="background:${ciAvColor(c.id)}">${escapeHtml(ciInitials(c.full_name||c.company_name))}</div>
      <div style="min-width:0;"><div class="ci-row-name">${escapeHtml(c.full_name||"Client")}</div><div class="ci-row-co">${escapeHtml([c.company_name,c.email].filter(Boolean).join(" · "))}</div></div>
    </div>`).join(""):'<div class="ci-list-empty">No client matches.</div>';$("ciNewList").querySelectorAll(".ci-new-row").forEach(r=>{r.onclick=()=>ciStartConversation(r.dataset.id)})}async function ciStartConversation(clientId){if(!ciThread(clientId)){const{error}=await sb.from("client_chat_threads").insert({client_id:clientId,status:"routed",assigned_staff_id:myUserId,queue_key:null});if(error){$("ciNewMsg").textContent=error.message;$("ciNewMsg").className="ci-d-msg err";return}await loadClientChatOverview()}$("ciNewModal").classList.add("ci-hide");const t=ciThread(clientId);if(t&&!ciInFilter(t,ciFilter))ciFilter=["mine","unassigned","all","closed"].find(f=>ciInFilter(t,f))||"all";ciOpen(clientId)}function ciWire(){$("ciSearch").addEventListener("input",ciRenderList);$("ciFilters").addEventListener("click",e=>{const b=e.target.closest(".ci-filter");if(!b)return;ciFilter=b.dataset.f;ciRenderList()});$("ciRows").addEventListener("click",e=>{const r=e.target.closest(".ci-row");if(r)ciOpen(r.dataset.id)});$("ciBackBtn").addEventListener("click",ciClose);$("ciDetailsBtn").addEventListener("click",()=>$("ciWrap").classList.toggle("show-details"));$("ciCallBtn").addEventListener("click",()=>{if(ciOpenClientId)startVoiceCallToClient(ciOpenClientId,chatClientThreadName(ciOpenClientId))});$("ciClaimBtn").addEventListener("click",async()=>{if(!ciOpenClientId)return;await acceptClientChatThread(ciOpenClientId);if($("ciDetails"))$("ciDetails").__sig=null;ciRefresh();if(ciOpenClientId&&!ciUnclaimed(ciThread(ciOpenClientId)))$("ciInput").focus()});$("ciSendBtn").addEventListener("click",ciSend);$("ciInput").addEventListener("keydown",e=>{if(e.key==="Enter"&&!e.shiftKey&&!e.isComposing){e.preventDefault();ciSend()}});$("ciInput").addEventListener("input",()=>{ciAutoGrow();ciTypingPing()});$("ciInput").addEventListener("paste",e=>{const items=e.clipboardData&&e.clipboardData.items;if(!items)return;for(const item of items){if(item.kind!=="file")continue;const file=item.getAsFile();if(!file)return;e.preventDefault();ciSetAttachment(file.name?file:new File([file],`pasted-${Date.now()}.${file.type.split("/")[1]||"png"}`,{type:file.type}));return}});$("ciAttachBtn").addEventListener("click",()=>$("ciFile").click());$("ciFile").addEventListener("change",e=>{const f=e.target.files&&e.target.files[0];e.target.value="";ciSetAttachment(f)});$("ciAttachRemove").addEventListener("click",ciClearAttachment);$("ciQrBtn").addEventListener("click",e=>{e.stopPropagation();const m=$("ciQrMenu");if(m.classList.contains("ci-hide")){ciRenderQrMenu();m.classList.remove("ci-hide")}else m.classList.add("ci-hide")});document.addEventListener("click",e=>{if(!e.target.closest||!e.target.closest(".ci-qr-wrap"))$("ciQrMenu").classList.add("ci-hide")});$("ciQrModalClose").addEventListener("click",()=>$("ciQrModal").classList.add("ci-hide"));$("ciQrClear").addEventListener("click",()=>{ciQrEditingId=null;$("ciQrTitle").value="";$("ciQrBody").value="";$("ciQrMsg").textContent="";$("ciQrSave").textContent="Add reply"});$("ciQrSave").addEventListener("click",ciSaveQr);$("ciNewBtn").classList.toggle("ci-hide",!ciIsAdmin());$("ciNewBtn").addEventListener("click",ciOpenNewModal);$("ciNewClose").addEventListener("click",()=>$("ciNewModal").classList.add("ci-hide"));$("ciNewSearch").addEventListener("input",ciRenderNewList);[$("ciQrModal"),$("ciNewModal")].forEach(m=>m.addEventListener("click",e=>{if(e.target===m)m.classList.add("ci-hide")}));document.addEventListener("keydown",e=>{if(e.key!=="Escape")return;$("ciQrMenu").classList.add("ci-hide");$("ciQrModal").classList.add("ci-hide");$("ciNewModal").classList.add("ci-hide")});setInterval(()=>{if(ciTabActive())ciRenderList()},6e4)}let voiceCallState=null;let pendingMeetInvite=null;let voiceMyChannel=null;let webrtcTurnConfig=null;let voiceAudioCtx=null;function voicePrimeAudioContext(){if(voiceAudioCtx)return;try{voiceAudioCtx=new(window.AudioContext||window.webkitAudioContext)}catch(e){}}document.addEventListener("click",voicePrimeAudioContext,{once:true});function voicePlayTone(freq,startOffset,duration,volume,waveType){if(!voiceAudioCtx)return;const t0=voiceAudioCtx.currentTime+startOffset;const osc=voiceAudioCtx.createOscillator();osc.type=waveType||"sine";osc.frequency.value=freq;const gain=voiceAudioCtx.createGain();gain.gain.setValueAtTime(0,t0);gain.gain.linearRampToValueAtTime(volume,t0+.02);gain.gain.linearRampToValueAtTime(volume*.65,t0+duration*.6);gain.gain.linearRampToValueAtTime(0,t0+duration);osc.connect(gain);gain.connect(voiceAudioCtx.destination);osc.start(t0);osc.stop(t0+duration+.02)}function voicePlaySequence(notes){if(!voiceAudioCtx)return;if(voiceAudioCtx.state==="suspended")voiceAudioCtx.resume().catch(()=>{});notes.forEach(n=>voicePlayTone(n.freq,n.at,n.dur,n.vol,n.type))}const VOICE_CONNECT_NOTES=[{freq:659.25,at:0,dur:.14,vol:.18},{freq:987.77,at:.09,dur:.4,vol:.2}];const VOICE_HANGUP_NOTES=[{freq:659.25,at:0,dur:.14,vol:.16},{freq:440,at:.09,dur:.4,vol:.18}];const VOICE_INCOMING_ARPEGGIO=[523.25,659.25,783.99,1046.5];const VOICE_RINGBACK_PAIR=[440,480];const VOICE_RING_NOTE_GAIN=.16;const VOICE_RINGBACK_OSC_GAIN=.07;let voiceRingInterval=null;let voiceRingGain=null;function voiceRingPlayIncoming(){const ctx=voiceAudioCtx;if(!ctx||!voiceRingGain)return;VOICE_INCOMING_ARPEGGIO.forEach((freq,idx)=>{const at=ctx.currentTime+idx*.15;const osc=ctx.createOscillator();const g=ctx.createGain();osc.frequency.value=freq;g.gain.setValueAtTime(VOICE_RING_NOTE_GAIN,at);g.gain.exponentialRampToValueAtTime(.001,at+.35);osc.connect(g);g.connect(voiceRingGain);osc.start(at);osc.stop(at+.4)})}function voiceRingPlayRingback(){const ctx=voiceAudioCtx;if(!ctx||!voiceRingGain)return;const oscs=VOICE_RINGBACK_PAIR.map(freq=>{const osc=ctx.createOscillator();const g=ctx.createGain();const t0=ctx.currentTime;osc.frequency.value=freq;g.gain.setValueAtTime(0,t0);g.gain.linearRampToValueAtTime(VOICE_RINGBACK_OSC_GAIN,t0+.02);g.gain.setValueAtTime(VOICE_RINGBACK_OSC_GAIN,t0+1.16);g.gain.linearRampToValueAtTime(0,t0+1.2);osc.connect(g);g.connect(voiceRingGain);osc.start();return osc});setTimeout(()=>{oscs.forEach(o=>{try{o.stop()}catch(e){}})},1200)}function voiceStartRingtone(kind){voiceStopRingtone();if(!voiceAudioCtx)voicePrimeAudioContext();if(!voiceAudioCtx)return;if(voiceAudioCtx.state==="suspended")voiceAudioCtx.resume().catch(()=>{});const incoming=kind==="incoming";voiceRingGain=voiceAudioCtx.createGain();voiceRingGain.gain.value=1;voiceRingGain.connect(voiceAudioCtx.destination);const playCycle=()=>{if(!voiceAudioCtx||voiceAudioCtx.state==="closed"||!voiceRingGain)return;if(incoming)voiceRingPlayIncoming();else voiceRingPlayRingback()};playCycle();voiceRingInterval=setInterval(playCycle,incoming?2500:3500)}function voiceStopRingtone(){voiceIncomingAlertStop();if(voiceRingInterval){clearInterval(voiceRingInterval);voiceRingInterval=null}if(voiceRingGain){try{voiceRingGain.disconnect()}catch(e){}voiceRingGain=null}}function chatMeetAllowed(){return/@proptechnologyai\.com$/i.test(myEmail||"")}function meetPlayRingBurst(){if(!voiceAudioCtx)return;const toneA=950,toneB=1400;const noteDur=.045,gap=.045,trillCount=10;for(let i=0;i<trillCount;i++){voicePlayTone(i%2===0?toneA:toneB,i*(noteDur+gap),noteDur,.13,"square")}}let meetRingtoneInterval=null;function meetStartRingtone(){meetStopRingtone();if(!voiceAudioCtx)voicePrimeAudioContext();if(!voiceAudioCtx)return;if(voiceAudioCtx.state==="suspended")voiceAudioCtx.resume().catch(()=>{});meetPlayRingBurst();meetRingtoneInterval=setInterval(meetPlayRingBurst,1700)}function meetStopRingtone(){if(meetRingtoneInterval){clearInterval(meetRingtoneInterval);meetRingtoneInterval=null}}function voicePlayConnectSound(){if(!voiceAudioCtx)voicePrimeAudioContext();voicePlaySequence(VOICE_CONNECT_NOTES)}function voicePlayHangupSound(){if(!voiceAudioCtx)voicePrimeAudioContext();voicePlaySequence(VOICE_HANGUP_NOTES)}async function loadWebrtcTurnConfig(){const{data}=await sb.from("app_settings").select("key,value").in("key",["webrtc_turn_url","webrtc_turn_username","webrtc_turn_credential"]);const byKey={};(data||[]).forEach(r=>{byKey[r.key]=r.value});webrtcTurnConfig={url:byKey.webrtc_turn_url||null,username:byKey.webrtc_turn_username||null,credential:byKey.webrtc_turn_credential||null}}async function voiceIceServers(){const servers=[{urls:"stun:stun.l.google.com:19302"},{urls:"stun:stun1.l.google.com:19302"}];try{const{data,error}=await sb.functions.invoke("cloudflare-turn-credentials",{body:{}});if(error){console.warn("[call] cloudflare-turn-credentials call failed, falling back:",error)}else if(data&&data.ok&&Array.isArray(data.iceServers)&&data.iceServers.length){console.log("[call] got",data.iceServers.length,"Cloudflare TURN ICE server entries");return servers.concat(data.iceServers)}else if(data&&data.ok){console.log("[call] Cloudflare TURN not configured (no secrets set) — using STUN only unless a static TURN server is set in Settings")}else{console.warn("[call] cloudflare-turn-credentials returned an unexpected response:",data)}}catch(e){console.warn("[call] cloudflare-turn-credentials threw, falling back:",e)}if(webrtcTurnConfig&&webrtcTurnConfig.url){servers.push({urls:webrtcTurnConfig.url,username:webrtcTurnConfig.username||void 0,credential:webrtcTurnConfig.credential||void 0})}return servers}function voiceChannelReady(channel){return new Promise(resolve=>{channel.subscribe(status=>{if(status==="SUBSCRIBED")resolve()})})}function voiceSend(channel,payload){return channel.send({type:"broadcast",event:"signal",payload:{...payload,from:myUserId,fromName:myFullName||myEmail}})}function voiceSendOneOff(toUserId,payload){const ch=sb.channel("call-signal-"+toUserId);voiceChannelReady(ch).then(()=>voiceSend(ch,payload)).then(()=>sb.removeChannel(ch))}function initVoiceCallSignaling(){if(!myUserId||voiceMyChannel)return;voiceMyChannel=sb.channel("call-signal-"+myUserId);voiceMyChannel.on("broadcast",{event:"signal"},({payload})=>handleVoiceSignal(payload)).subscribe(status=>{if(status==="SUBSCRIBED")voiceSignalRetryDelay=2e3;else if(status==="CHANNEL_ERROR"||status==="TIMED_OUT")voiceSignalRetry()});loadWebrtcTurnConfig()}var voiceSignalRetryTimer=null;var voiceSignalRetryDelay=2e3;function voiceSignalRetry(){if(voiceSignalRetryTimer||!voiceMyChannel)return;const delay=voiceSignalRetryDelay;voiceSignalRetryDelay=Math.min(voiceSignalRetryDelay*2,6e4);voiceSignalRetryTimer=setTimeout(()=>{voiceSignalRetryTimer=null;if(!voiceMyChannel)return;const old=voiceMyChannel;voiceMyChannel=null;try{sb.removeChannel(old)}catch(e){}initVoiceCallSignaling()},delay)}function voiceSignalCheck(){if(voiceMyChannel&&voiceMyChannel.state!=="joined")voiceSignalRetry()}window.addEventListener("online",voiceSignalCheck);document.addEventListener("visibilitychange",()=>{if(document.visibilityState==="visible")voiceSignalCheck()});let voiceTitleTimer=null;let voiceTitleOrig=null;let voiceCallNotif=null;function voiceIncomingAlertStart(name,callRowId){voiceIncomingAlertStop();voiceTitleOrig=document.title;let on=false;voiceTitleTimer=setInterval(()=>{on=!on;document.title=on?`📞 Incoming call — ${name}`:voiceTitleOrig},1e3);if(document.visibilityState!=="visible"&&"Notification"in window&&Notification.permission==="granted"){try{const iconLink=document.querySelector('link[rel="icon"]');voiceCallNotif=new Notification(`Incoming call from ${name}`,{body:"Click to answer in the staff portal.",tag:callRowId?`call-${callRowId}`:"staff-call",requireInteraction:true,icon:iconLink?iconLink.href:void 0});voiceCallNotif.onclick=()=>{window.focus();if(voiceCallNotif)voiceCallNotif.close()}}catch(e){voiceCallNotif=null}}}function voiceIncomingAlertStop(){if(voiceTitleTimer){clearInterval(voiceTitleTimer);voiceTitleTimer=null}if(voiceTitleOrig!==null){document.title=voiceTitleOrig;voiceTitleOrig=null}if(voiceCallNotif){try{voiceCallNotif.close()}catch(e){}voiceCallNotif=null}}function teardownVoiceCallSignaling(){if(voiceCallState)endVoiceCall(null,false);if(voiceMyChannel){sb.removeChannel(voiceMyChannel);voiceMyChannel=null}}async function voiceFlushPendingCandidates(state){if(!state||!state.pc||!state.pendingCandidates||!state.pendingCandidates.length)return;const queued=state.pendingCandidates;state.pendingCandidates=[];console.log("[call] flushing",queued.length,"queued ICE candidate(s)");for(const candidate of queued){try{await state.pc.addIceCandidate(new RTCIceCandidate(candidate))}catch(e){console.error("[call] queued addIceCandidate failed",e)}}}function voiceGenId(){return crypto.randomUUID?crypto.randomUUID():`${Date.now()}-${Math.random().toString(16).slice(2)}`}let voiceCallRoster={};let voiceRosterChannel=null;let voiceRosterInviteTimeouts={};function voiceRosterRowUpsertLocal(row){voiceCallRoster[row.user_id]=row;if(row.status!=="ringing"&&voiceRosterInviteTimeouts[row.user_id]){clearTimeout(voiceRosterInviteTimeouts[row.user_id]);delete voiceRosterInviteTimeouts[row.user_id]}renderCallParticipantsPanel();updateCallParticipantsBadge()}async function voiceRosterInit(groupId){voiceCallRoster={};voiceRosterInviteTimeouts={};if(voiceRosterChannel){sb.removeChannel(voiceRosterChannel);voiceRosterChannel=null}const{data}=await sb.from("call_participants").select("*").eq("call_group_id",groupId);(data||[]).forEach(row=>{voiceCallRoster[row.user_id]=row});voiceRosterChannel=sb.channel("call-roster-"+groupId).on("postgres_changes",{event:"INSERT",schema:"public",table:"call_participants",filter:`call_group_id=eq.${groupId}`},payload=>voiceRosterRowUpsertLocal(payload.new)).on("postgres_changes",{event:"UPDATE",schema:"public",table:"call_participants",filter:`call_group_id=eq.${groupId}`},payload=>voiceRosterRowUpsertLocal(payload.new)).subscribe();renderCallParticipantsPanel();updateCallParticipantsBadge()}function voiceRosterTeardown(){if(voiceRosterChannel){sb.removeChannel(voiceRosterChannel);voiceRosterChannel=null}Object.values(voiceRosterInviteTimeouts).forEach(h=>clearTimeout(h));voiceRosterInviteTimeouts={};voiceCallRoster={}}function voiceRosterInsert(groupId,userId,userName,invitedBy,status){return sb.from("call_participants").insert({call_group_id:groupId,user_id:userId,user_name:userName,invited_by:invitedBy,status,connected_at:status==="connected"?new Date().toISOString():null}).select().single().then(({data,error})=>{if(error){console.error("[call] roster insert failed",error);return}if(data)voiceRosterRowUpsertLocal(data)})}function voiceRosterSetStatus(userId,status){if(!voiceCallState||!voiceCallState.groupId)return;const patch={status,updated_at:new Date().toISOString()};if(status==="connected")patch.connected_at=new Date().toISOString();if(status==="declined"||status==="no_answer"||status==="left"||status==="ended")patch.ended_at=new Date().toISOString();sb.from("call_participants").update(patch).eq("call_group_id",voiceCallState.groupId).eq("user_id",userId).then(({error})=>{if(error)console.error("[call] roster status update failed",error)})}function updateCallParticipantsBadge(){const btn=$("callParticipantsBtn");const badge=$("callParticipantsCount");if(!btn||!badge)return;const rows=Object.values(voiceCallRoster);if(!voiceCallState||rows.length<2){btn.style.display="none";return}btn.style.display="";const activeCount=rows.filter(r=>r.status==="connected"||r.status==="ringing").length;badge.textContent=String(activeCount);badge.style.display=activeCount>0?"":"none"}const CALL_ROSTER_STATUS_LABEL={ringing:"Ringing…",connected:"Connected",declined:"Declined",no_answer:"No answer",left:"Left",ended:"Ended"};function renderCallParticipantsPanel(){const list=$("callParticipantsList");if(!list)return;const rows=Object.values(voiceCallRoster).sort((a,b)=>new Date(a.invited_at)-new Date(b.invited_at));if(!rows.length){list.innerHTML='<div class="call-add-panel-empty">No one on this call yet.</div>';return}list.innerHTML=rows.map(r=>{const isMe=r.user_id===myUserId;const canRingAgain=!isMe&&(r.status==="declined"||r.status==="no_answer");return`<div class="call-participants-row">
      <span class="call-participants-name">${escapeHtml(r.user_name||"Someone")}${isMe?" (you)":""}</span>
      <span class="call-participants-status ${escapeHtml(r.status)}">${escapeHtml(CALL_ROSTER_STATUS_LABEL[r.status]||r.status)}</span>
      ${canRingAgain?`<button type="button" class="call-participants-ring-again" data-user-id="${escapeHtml(r.user_id)}" data-user-name="${escapeHtml(r.user_name||"")}">Ring again</button>`:""}
    </div>`}).join("");list.querySelectorAll(".call-participants-ring-again").forEach(btn=>{btn.addEventListener("click",()=>ringAgainParticipant(btn.dataset.userId,btn.dataset.userName))})}if($("callParticipantsBtn"))$("callParticipantsBtn").addEventListener("click",()=>{const panel=$("callParticipantsPanel");const showing=panel.style.display==="block";if(showing){panel.style.display="none";return}$("callAddPanel").style.display="none";renderCallParticipantsPanel();panel.style.display="block"});async function voiceFlushMeshCandidates(peer){if(!peer||!peer.pc||!peer.pendingCandidates||!peer.pendingCandidates.length)return;const queued=peer.pendingCandidates;peer.pendingCandidates=[];for(const candidate of queued){try{await peer.pc.addIceCandidate(new RTCIceCandidate(candidate))}catch(e){console.error("[call] mesh queued addIceCandidate failed",e)}}}function voiceRemoveExtraPeer(peerId){if(!voiceCallState||!voiceCallState.extraPeers[peerId])return;const peer=voiceCallState.extraPeers[peerId];if(peer.pc)peer.pc.close();if(peer.channel)sb.removeChannel(peer.channel);const audioEl=document.getElementById("extraAudio-"+peerId);if(audioEl)audioEl.remove();delete voiceCallState.extraPeers[peerId];renderCallAddPanel()}async function voiceCreateMeshPc(peerId,peerName,channel){const iceServers=await voiceIceServers();const pc=new RTCPeerConnection({iceServers});voiceCallState.localStream.getTracks().forEach(track=>pc.addTrack(track,voiceCallState.localStream));let audioEl=document.getElementById("extraAudio-"+peerId);if(!audioEl){audioEl=document.createElement("audio");audioEl.id="extraAudio-"+peerId;audioEl.autoplay=true;audioEl.style.display="none";document.getElementById("extraCallAudios").appendChild(audioEl)}pc.ontrack=e=>{audioEl.srcObject=e.streams[0]};pc.onicecandidate=e=>{if(e.candidate)voiceSend(channel,{type:"mesh-ice-candidate",candidate:e.candidate,groupId:voiceCallState.groupId})};voiceAttachDiagnostics(pc);return pc}async function meshDialAll(){const state=voiceCallState;if(!state||!state.meshParticipants||!state.meshParticipants.length)return;for(const p of state.meshParticipants){if(p.id===state.peerId||p.id===myUserId)continue;try{const channel=sb.channel("call-signal-"+p.id);await voiceChannelReady(channel);const pc=await voiceCreateMeshPc(p.id,p.name,channel);state.extraPeers[p.id]={pc,channel,name:p.name,remoteDescSet:false,pendingCandidates:[]};const offer=await pc.createOffer();await pc.setLocalDescription(offer);voiceSend(channel,{type:"mesh-offer",sdp:offer,groupId:state.groupId});renderCallAddPanel()}catch(e){console.error("[call] meshDialAll failed for",p.id,e)}}}async function meshAcceptOffer(msg){const state=voiceCallState;const channel=sb.channel("call-signal-"+msg.from);await voiceChannelReady(channel);const pc=await voiceCreateMeshPc(msg.from,msg.fromName,channel);const peer={pc,channel,name:msg.fromName,remoteDescSet:false,pendingCandidates:[]};state.extraPeers[msg.from]=peer;await pc.setRemoteDescription(new RTCSessionDescription(msg.sdp));peer.remoteDescSet=true;await voiceFlushMeshCandidates(peer);const answer=await pc.createAnswer();await pc.setLocalDescription(answer);voiceSend(channel,{type:"mesh-answer",sdp:answer,groupId:state.groupId});renderCallAddPanel()}const CALL_RING_AGAIN_TIMEOUT_MS=3e4;async function inviteToCall(userId,userName){const state=voiceCallState;if(!state||!state.localStream)return;if(state.peerId===userId||state.extraPeers[userId])return;try{const channel=sb.channel("call-signal-"+userId);await voiceChannelReady(channel);const pc=await voiceCreateMeshPc(userId,userName,channel);state.extraPeers[userId]={pc,channel,name:userName,remoteDescSet:false,pendingCandidates:[]};const offer=await pc.createOffer();await pc.setLocalDescription(offer);const currentParticipants=[{id:myUserId,name:myFullName||myEmail},{id:state.peerId,name:state.peerName},...Object.keys(state.extraPeers).filter(id=>id!==userId).map(id=>({id,name:state.extraPeers[id].name}))];voiceSend(channel,{type:"offer",sdp:offer,groupId:state.groupId,participants:currentParticipants});voiceRosterInsert(state.groupId,userId,userName,myUserId,"ringing");voiceRosterInviteTimeouts[userId]=setTimeout(()=>{if(voiceCallState&&voiceCallState.extraPeers[userId]&&!voiceCallState.extraPeers[userId].remoteDescSet){voiceRosterSetStatus(userId,"no_answer");voiceRemoveExtraPeer(userId)}},CALL_RING_AGAIN_TIMEOUT_MS);renderCallAddPanel();$("callAddPanel").style.display="none"}catch(e){console.error("[call] inviteToCall failed",e);alert("Couldn't add them to the call.")}}function ringAgainParticipant(userId,userName){inviteToCall(userId,userName)}function renderCallAddPanel(){const list=$("callAddPanelList");if(!list)return;const state=voiceCallState;if(!state){list.innerHTML="";return}const already=new Set([state.peerId,myUserId,...Object.keys(state.extraPeers||{})]);const candidates=(chatDirectory||[]).filter(p=>!already.has(p.id));if(!candidates.length){list.innerHTML='<div class="call-add-panel-empty">No one else available to add.</div>';return}list.innerHTML=candidates.map(p=>`<div class="call-add-panel-row" data-user-id="${escapeHtml(p.id)}" data-user-name="${escapeHtml(p.full_name||p.email||"Colleague")}"><span class="chat-status-dot ${chatContactStatus(p.id)}" style="position:static; width:8px; height:8px; border:none;"></span> ${escapeHtml(p.full_name||p.email||"Colleague")}</div>`).join("");list.querySelectorAll(".call-add-panel-row").forEach(row=>{row.addEventListener("click",()=>inviteToCall(row.dataset.userId,row.dataset.userName))})}$("callAddBtn").addEventListener("click",()=>{const panel=$("callAddPanel");const showing=panel.style.display==="block";if(showing){panel.style.display="none";return}renderCallAddPanel();panel.style.display="block"});const VOICE_REJOIN_WINDOW_MS=6e4;const VOICE_SAVED_CALL_KEY="pt.activeCall";function voiceSaveCallForRejoin(){const st=voiceCallState;const ringingOut=!!st&&st.status==="ringing"&&st.role==="caller";if(!st||st.status!=="answered"&&!ringingOut||!st.callRowId||!st.peerId)return;try{sessionStorage.setItem(VOICE_SAVED_CALL_KEY,JSON.stringify({callRowId:st.callRowId,peerId:st.peerId,peerName:st.peerName,role:st.role,isClientCall:!!st.isClientCall,startedAt:st.startedAt||null,status:st.status,ringStartedAt:st.ringStartedAt||null,groupId:st.groupId||null,savedAt:Date.now()}))}catch(e){}}function voiceReadSavedCall(){try{const saved=JSON.parse(sessionStorage.getItem(VOICE_SAVED_CALL_KEY)||"null");if(!saved||!saved.callRowId||!saved.peerId)return null;if(Date.now()-(saved.savedAt||0)>VOICE_REJOIN_WINDOW_MS){sessionStorage.removeItem(VOICE_SAVED_CALL_KEY);return null}return saved}catch(e){return null}}function voiceClearSavedCall(){try{sessionStorage.removeItem(VOICE_SAVED_CALL_KEY)}catch(e){}}function voiceMarkLive(state){voiceSaveCallForRejoin();clearInterval(state.saveTimer);state.saveTimer=setInterval(voiceSaveCallForRejoin,5e3)}window.addEventListener("pagehide",voiceSaveCallForRejoin);window.addEventListener("online",()=>{const st=voiceCallState;if(st&&st.reconnecting&&(st.role==="caller"||st.restored))voiceSendRejoin(st)});function voiceOnPcState(pc){const state=voiceCallState;console.log("[call] connection state:",pc.connectionState);if(!state||state.pc!==pc)return;const cs=pc.connectionState;if(cs==="connected"){if(state.reconnecting)voiceRejoinDone(state);return}if(state.status!=="answered"){if(cs==="failed")endVoiceCall("failed",true);return}if(cs==="disconnected"||cs==="failed"){if(state.reconnecting){if(cs==="failed"&&(state.role==="caller"||state.restored)){setTimeout(()=>{if(voiceCallState===state&&state.reconnecting&&state.pc===pc)voiceSendRejoin(state)},2e3)}return}voiceBeginReconnect(state,cs==="failed"?0:3e3)}}function voiceBeginReconnect(state,kickDelay){if(voiceCallState!==state||state.reconnecting)return;const r=state.reconnecting={since:Date.now()};voiceShowActiveBar(state.peerName,"Reconnecting…",true);r.deadline=setTimeout(()=>{if(voiceCallState===state&&state.reconnecting===r)endVoiceCall("ended",true)},VOICE_REJOIN_WINDOW_MS);r.poll=setInterval(async()=>{if(voiceCallState!==state||state.reconnecting!==r||!state.callRowId)return;const{data}=await sb.from("voice_calls").select("status").eq("id",state.callRowId).maybeSingle();if(data&&["ended","failed","missed","declined"].includes(data.status)&&voiceCallState===state){endVoiceCall(state.role==="caller"?"ended":null,false)}},5e3);if(kickDelay!=null&&state.role==="caller"){r.kick=setTimeout(()=>{if(voiceCallState!==state||state.reconnecting!==r||!state.pc)return;if(state.pc.connectionState==="connected")voiceRejoinDone(state);else voiceSendRejoin(state)},kickDelay)}}function voiceClearReconnect(state){if(!state)return;const r=state.reconnecting;if(r){clearTimeout(r.deadline);clearInterval(r.poll);clearTimeout(r.kick)}state.reconnecting=null;clearInterval(state.rejoinRetry)}function voiceRejoinDone(state){voiceClearReconnect(state);state.rejoinId=null;if(!state.durationInterval)voiceStartCallTimer();else{voicePlayConnectSound();voiceShowActiveBar(state.peerName,"",false);$("callScreenShareBtn").style.display="flex"}const track=state.localStream&&state.localStream.getAudioTracks()[0];if(track)$("callMuteBtn").classList.toggle("muted",!track.enabled);voiceMarkLive(state)}async function voiceEnsureChannel(state){if(state.outboundChannel)return;state.outboundChannel=sb.channel("call-signal-"+state.peerId);await voiceChannelReady(state.outboundChannel)}function voiceDropScreenShare(state){if(state.screenStream)state.screenStream.getTracks().forEach(t=>t.stop());state.screenStream=null;state.hasScreenShare=false;state.videoSender=null;state.peerSharing=false;try{voiceUpdateScreenShareUI()}catch(e){}}async function voiceBuildPc(state,tag){const channel=state.outboundChannel;const iceServers=await voiceIceServers();const pc=new RTCPeerConnection({iceServers});state.localStream.getTracks().forEach(track=>pc.addTrack(track,state.localStream));pc.ontrack=e=>{$(e.track.kind==="video"?"remoteCallVideo":"remoteCallAudio").srcObject=e.streams[0]};pc.onicecandidate=e=>{if(e.candidate)voiceSend(channel,{type:"ice-candidate",candidate:e.candidate,tag})};voiceAttachDiagnostics(pc);pc.onconnectionstatechange=()=>voiceOnPcState(pc);return pc}async function voiceSendRejoin(state){if(voiceCallState!==state)return;const rejoinId=voiceGenId();state.rejoinId=rejoinId;state.pcTag=rejoinId;state.remoteDescSet=false;state.pendingCandidates=[];await voiceEnsureChannel(state);const old=state.pc;voiceDropScreenShare(state);const pc=await voiceBuildPc(state,rejoinId);if(voiceCallState!==state||state.rejoinId!==rejoinId){pc.close();return}state.pc=pc;if(old){try{old.close()}catch(e){}}const offer=await pc.createOffer();await pc.setLocalDescription(offer);const payload={type:"rejoin-request",callRowId:state.callRowId,rejoinId,sdp:offer};voiceSend(state.outboundChannel,payload);clearInterval(state.rejoinRetry);state.rejoinRetry=setInterval(()=>{if(voiceCallState!==state||state.rejoinId!==rejoinId||state.remoteDescSet){clearInterval(state.rejoinRetry);return}voiceSend(state.outboundChannel,payload)},3e3)}let voiceRestoring=null;function voiceEnsureRestored(saved){if(voiceCallState)return Promise.resolve(voiceCallState);if(!voiceRestoring)voiceRestoring=voiceRestoreFromSaved(saved).finally(()=>{voiceRestoring=null});return voiceRestoring}async function voiceRestoreFromSaved(saved){let localStream;try{localStream=await navigator.mediaDevices.getUserMedia({audio:true})}catch(e){voiceClearSavedCall();alert("Your call dropped when the page reloaded, and the microphone is blocked, so it could not reconnect. Allow the microphone and call them again.");return null}if(voiceCallState){localStream.getTracks().forEach(t=>t.stop());return voiceCallState}const outboundChannel=sb.channel("call-signal-"+saved.peerId);await voiceChannelReady(outboundChannel);voiceCallState={role:saved.role,isClientCall:!!saved.isClientCall,peerId:saved.peerId,peerName:saved.peerName,pc:null,localStream,outboundChannel,callRowId:saved.callRowId,status:"answered",remoteDescSet:false,pendingCandidates:[],videoSender:null,screenStream:null,hasScreenShare:false,peerSharing:false,videoPanelMinimized:false,videoPanelMaximized:false,groupId:saved.groupId||voiceGenId(),extraPeers:{},startedAt:saved.startedAt||Date.now(),restored:true};voiceBeginReconnect(voiceCallState,null);return voiceCallState}async function voiceResumeSavedCall(){const saved=voiceReadSavedCall();if(!saved||voiceCallState)return;const{data}=await sb.from("voice_calls").select("status").eq("id",saved.callRowId).maybeSingle();const rowStatus=data?data.status:null;if(saved.status==="ringing"){if(rowStatus==="ringing"&&Date.now()-(saved.ringStartedAt||0)<VOICE_RING_MS){voiceResumeRinging(saved);return}if(rowStatus!=="answered"){voiceClearSavedCall();return}}else if(rowStatus&&rowStatus!=="answered"){voiceClearSavedCall();return}const state=await voiceEnsureRestored(saved);if(!state||state.callRowId!==saved.callRowId)return;if(voiceCallState===state&&!state.answeredRejoinId&&!state.rejoinId&&!state.remoteDescSet)voiceSendRejoin(state)}async function voiceResumeRinging(saved){let localStream;try{localStream=await navigator.mediaDevices.getUserMedia({audio:true})}catch(e){voiceClearSavedCall();return}if(voiceCallState){localStream.getTracks().forEach(t=>t.stop());return}const outboundChannel=sb.channel("call-signal-"+saved.peerId);await voiceChannelReady(outboundChannel);if(voiceCallState){localStream.getTracks().forEach(t=>t.stop());sb.removeChannel(outboundChannel);return}const tag=voiceGenId();const state=voiceCallState={role:"caller",isClientCall:!!saved.isClientCall,peerId:saved.peerId,peerName:saved.peerName,pc:null,localStream,outboundChannel,callRowId:saved.callRowId,status:"ringing",remoteDescSet:false,pendingCandidates:[],videoSender:null,screenStream:null,hasScreenShare:false,peerSharing:false,videoPanelMinimized:false,videoPanelMaximized:false,groupId:saved.groupId||voiceGenId(),extraPeers:{},ringStartedAt:saved.ringStartedAt||Date.now(),pcTag:tag,restored:true};const pc=await voiceBuildPc(state,tag);if(voiceCallState!==state){pc.close();return}state.pc=pc;const offer=await pc.createOffer();await pc.setLocalDescription(offer);const payload={type:"offer-retry",sdp:offer,callRowId:state.callRowId,replace:true,tag};if(!state.isClientCall)payload.groupId=state.groupId;voiceSend(outboundChannel,payload);voiceRepeatOffer(outboundChannel,payload);voiceShowActiveBar(state.peerName,"Calling…",true);voiceStartRingtone("ringback");const left=Math.max(5e3,VOICE_RING_MS-(Date.now()-state.ringStartedAt));state.ringTimeout=setTimeout(()=>{if(voiceCallState===state&&state.status==="ringing")endVoiceCall("missed",true)},left);voiceMarkLive(state)}async function voiceHandleRejoinRequest(msg){let state=voiceCallState;if(!state){const saved=voiceReadSavedCall();if(!saved||saved.callRowId!==msg.callRowId||saved.peerId!==msg.from)return;state=await voiceEnsureRestored(saved);if(!state)return}if(state.callRowId!==msg.callRowId||state.peerId!==msg.from||state.status!=="answered")return;if(state.answeredRejoinId===msg.rejoinId){if(state.lastRejoinAnswer&&state.outboundChannel)voiceSend(state.outboundChannel,state.lastRejoinAnswer);return}if(state.rejoinId&&state.rejoinId!==msg.rejoinId&&!state.remoteDescSet&&state.rejoinId<msg.rejoinId)return;clearInterval(state.rejoinRetry);state.rejoinId=null;state.pcTag=msg.rejoinId;state.remoteDescSet=false;state.pendingCandidates=state.candidateStash&&state.candidateStash[msg.rejoinId]||[];if(state.candidateStash)delete state.candidateStash[msg.rejoinId];if(!state.reconnecting)voiceBeginReconnect(state,null);await voiceEnsureChannel(state);const old=state.pc;voiceDropScreenShare(state);const pc=await voiceBuildPc(state,msg.rejoinId);if(voiceCallState!==state||state.pcTag!==msg.rejoinId){pc.close();return}state.pc=pc;if(old){try{old.close()}catch(e){}}await pc.setRemoteDescription(new RTCSessionDescription(msg.sdp));state.remoteDescSet=true;await voiceFlushPendingCandidates(state);const answer=await pc.createAnswer();await pc.setLocalDescription(answer);state.answeredRejoinId=msg.rejoinId;state.lastRejoinAnswer={type:"rejoin-answer",callRowId:state.callRowId,rejoinId:msg.rejoinId,sdp:answer};voiceSend(state.outboundChannel,state.lastRejoinAnswer)}const VOICE_RING_MS=45e3;const VOICE_OFFER_RETRY_MS=4e3;const voiceRangCallIds=new Set;function voiceWatchRingingRow(state){if(!state||!state.callRowId)return;clearInterval(state.rowWatch);state.rowWatch=setInterval(async()=>{if(voiceCallState!==state||state.status!=="ringing"){clearInterval(state.rowWatch);return}const{data}=await sb.from("voice_calls").select("status").eq("id",state.callRowId).maybeSingle();if(voiceCallState!==state||state.status!=="ringing"||!data)return;const s=data.status;if(state.role==="caller"){if(s==="declined"){clearInterval(state.rowWatch);endVoiceCall("declined",false)}else if(s==="missed"||s==="ended"||s==="failed"){clearInterval(state.rowWatch);endVoiceCall(null,false)}}else if(s&&s!=="ringing"){clearInterval(state.rowWatch);endVoiceCall(null,false)}},3e3)}function voiceRepeatOffer(channel,payload){const state=voiceCallState;if(!state||!payload.callRowId)return;state.offerRetry=setInterval(()=>{if(voiceCallState!==state||state.status!=="ringing"){clearInterval(state.offerRetry);return}voiceSend(channel,payload)},VOICE_OFFER_RETRY_MS);voiceWatchRingingRow(state)}async function handleVoiceSignal(msg){if(msg.type==="offer-retry"){if(msg.replace&&voiceCallState&&voiceCallState.role==="callee"&&voiceCallState.callRowId===msg.callRowId){if(voiceCallState.status==="ringing"&&!voiceCallState.pc&&voiceCallState.offerTag!==msg.tag){voiceCallState.offerSdp=msg.sdp;voiceCallState.offerTag=msg.tag;voiceCallState.pcTag=msg.tag;voiceCallState.pendingCandidates=voiceCallState.candidateStash&&voiceCallState.candidateStash[msg.tag]||[]}return}if(voiceCallState||!msg.callRowId||voiceRangCallIds.has(msg.callRowId))return;msg={...msg,type:"offer"}}if(msg.type==="offer"){if(voiceCallState){voiceSendOneOff(msg.from,{type:"busy"});return}if(msg.callRowId)voiceRangCallIds.add(msg.callRowId);voiceCallState={role:"callee",peerId:msg.from,peerName:msg.fromName,callRowId:msg.callRowId,offerSdp:msg.sdp,status:"ringing",remoteDescSet:false,pendingCandidates:[],videoSender:null,screenStream:null,hasScreenShare:false,peerSharing:false,videoPanelMinimized:false,videoPanelMaximized:false,groupId:msg.groupId||voiceGenId(),extraPeers:{},meshParticipants:msg.participants||[]};if(msg.tag){voiceCallState.pcTag=msg.tag;voiceCallState.offerTag=msg.tag}if(voiceCallState.groupId)voiceRosterInit(voiceCallState.groupId);$("incomingCallName").textContent=msg.fromName||"Someone";$("incomingCallSub").textContent=msg.participants&&msg.participants.length?`Adding you to a call with ${msg.participants.map(p=>p.name).join(", ")}…`:"Incoming call…";$("incomingCallBanner").style.display="flex";requestAnimationFrame(voiceSetCallStackOffsets);voiceStartRingtone("incoming");voiceIncomingAlertStart(msg.fromName||"Someone",msg.callRowId);voiceWatchRingingRow(voiceCallState)}else if(msg.type==="answer"){if(!voiceCallState||voiceCallState.role!=="caller"||!voiceCallState.pc)return;if(voiceCallState.status==="answered"||voiceCallState.remoteDescSet)return;clearTimeout(voiceCallState.ringTimeout);voiceStopRingtone();await voiceCallState.pc.setRemoteDescription(new RTCSessionDescription(msg.sdp));voiceCallState.remoteDescSet=true;await voiceFlushPendingCandidates(voiceCallState);voiceCallState.status="answered";voiceCallState.startedAt=Date.now();if(voiceCallState.callRowId)sb.from("voice_calls").update({status:"answered",answered_at:new Date().toISOString()}).eq("id",voiceCallState.callRowId).then(()=>{});voiceStartCallTimer();voiceMarkLive(voiceCallState)}else if(msg.type==="ice-candidate"){if(!voiceCallState||!msg.candidate)return;const tag=msg.tag||"o";if(tag!==(voiceCallState.pcTag||"o")){if(tag!=="o"){const stash=voiceCallState.candidateStash||(voiceCallState.candidateStash={});(stash[tag]||(stash[tag]=[])).push(msg.candidate)}return}if(voiceCallState.pc&&voiceCallState.remoteDescSet){try{await voiceCallState.pc.addIceCandidate(new RTCIceCandidate(msg.candidate))}catch(e){console.error("[call] addIceCandidate failed",e)}}else{console.log("[call] queuing ICE candidate — connection not ready yet");(voiceCallState.pendingCandidates||(voiceCallState.pendingCandidates=[])).push(msg.candidate)}}else if(msg.type==="rejoin-request"){await voiceHandleRejoinRequest(msg)}else if(msg.type==="rejoin-answer"){const st=voiceCallState;if(!st||st.callRowId!==msg.callRowId||st.rejoinId!==msg.rejoinId||!st.pc||st.remoteDescSet)return;clearInterval(st.rejoinRetry);await st.pc.setRemoteDescription(new RTCSessionDescription(msg.sdp));st.remoteDescSet=true;await voiceFlushPendingCandidates(st)}else if(msg.type==="renegotiate-offer"){if(!voiceCallState||!voiceCallState.pc||!voiceCallState.outboundChannel)return;voiceCallState.peerSharing=true;voiceUpdateScreenShareUI();await voiceCallState.pc.setRemoteDescription(new RTCSessionDescription(msg.sdp));const answer=await voiceCallState.pc.createAnswer();await voiceCallState.pc.setLocalDescription(answer);voiceSend(voiceCallState.outboundChannel,{type:"renegotiate-answer",sdp:answer})}else if(msg.type==="renegotiate-answer"){if(!voiceCallState||!voiceCallState.pc)return;await voiceCallState.pc.setRemoteDescription(new RTCSessionDescription(msg.sdp))}else if(msg.type==="screenshare-status"){if(!voiceCallState)return;voiceCallState.peerSharing=!!msg.sharing;voiceUpdateScreenShareUI()}else if(msg.type==="decline"){if(voiceCallState&&voiceCallState.extraPeers[msg.from]){voiceRosterSetStatus(msg.from,"declined");voiceRemoveExtraPeer(msg.from)}else if(voiceCallState&&voiceCallState.role==="caller"){if(voiceCallState.status==="answered")return;voiceRosterSetStatus(msg.from,"declined");endVoiceCall("declined",false)}}else if(msg.type==="hangup"){if(voiceCallState)endVoiceCall(voiceCallState.status==="answered"?"ended":"missed",false)}else if(msg.type==="busy"){if(voiceCallState&&voiceCallState.extraPeers[msg.from]){voiceRosterSetStatus(msg.from,"declined");voiceRemoveExtraPeer(msg.from)}else if(voiceCallState&&voiceCallState.role==="caller"){if(voiceCallState.status==="answered")return;const name=voiceCallState.peerName;voiceRosterSetStatus(msg.from,"declined");endVoiceCall("missed",false);alert(`${name||"They"} are on another call.`)}}else if(msg.type==="claimed"){if(msg.callRowId)voiceRangCallIds.add(msg.callRowId);if(voiceCallState&&voiceCallState.role==="callee"&&voiceCallState.status==="ringing"&&msg.callRowId===voiceCallState.callRowId){voiceStopRingtone();$("incomingCallBanner").style.display="none";requestAnimationFrame(voiceSetCallStackOffsets);voiceCallState=null}}else if(msg.type==="mesh-offer"){if(!voiceCallState||voiceCallState.groupId!==msg.groupId)return;await meshAcceptOffer(msg)}else if(msg.type==="mesh-answer"){const peer=voiceCallState&&voiceCallState.extraPeers[msg.from];if(!peer||!peer.pc)return;await peer.pc.setRemoteDescription(new RTCSessionDescription(msg.sdp));peer.remoteDescSet=true;await voiceFlushMeshCandidates(peer)}else if(msg.type==="mesh-ice-candidate"){const peer=voiceCallState&&voiceCallState.extraPeers[msg.from];if(!peer||!msg.candidate)return;if(peer.pc&&peer.remoteDescSet){try{await peer.pc.addIceCandidate(new RTCIceCandidate(msg.candidate))}catch(e){console.error("[call] mesh addIceCandidate failed",e)}}else{(peer.pendingCandidates||(peer.pendingCandidates=[])).push(msg.candidate)}}else if(msg.type==="mesh-hangup"){voiceRosterSetStatus(msg.from,"left");voiceRemoveExtraPeer(msg.from)}else if(msg.type==="meet-invite"){pendingMeetInvite={link:msg.link,fromId:msg.from,fromName:msg.fromName,groupId:msg.groupId||null};$("incomingMeetName").textContent=msg.fromName||"Someone";$("incomingMeetBanner").style.display="flex";requestAnimationFrame(voiceSetCallStackOffsets);meetStartRingtone();voicePlayConnectSound()}else if(msg.type==="meet-accepted"||msg.type==="meet-declined"){console.log(`${msg.fromName||"Someone"} ${msg.type==="meet-accepted"?"joined":"declined"} the Google Meet you shared.`)}}function postMeetResponseMessage(invite,accepted){const text=accepted?"📹 Meeting accepted — started joining":"📹 Meeting declined";const payload={sender_id:myUserId,recipient_id:invite.groupId?null:invite.fromId,group_id:invite.groupId||null,body:text};sb.from("chat_messages").insert(payload).select().single().then(({data,error})=>{if(error){console.error("Failed to post meet response to chat",error);return}chatAddMessageToState(data,true)})}function acceptMeetInvite(){if(!pendingMeetInvite)return;const invite=pendingMeetInvite;window.open(invite.link,"_blank","noopener");voiceSendOneOff(invite.fromId,{type:"meet-accepted"});postMeetResponseMessage(invite,true);meetStopRingtone();$("incomingMeetBanner").style.display="none";requestAnimationFrame(voiceSetCallStackOffsets);pendingMeetInvite=null}function declineMeetInvite(){if(!pendingMeetInvite)return;const invite=pendingMeetInvite;voiceSendOneOff(invite.fromId,{type:"meet-declined"});postMeetResponseMessage(invite,false);meetStopRingtone();$("incomingMeetBanner").style.display="none";requestAnimationFrame(voiceSetCallStackOffsets);pendingMeetInvite=null}$("acceptMeetBtn").addEventListener("click",acceptMeetInvite);$("declineMeetBtn").addEventListener("click",declineMeetInvite);function voiceLogIceServers(iceServers){console.log("[call] ICE servers offered:",iceServers.map(s=>s.urls))}function voiceAttachDiagnostics(pc){pc.oniceconnectionstatechange=()=>console.log("[call] ICE connection state:",pc.iceConnectionState);pc.onicegatheringstatechange=()=>console.log("[call] ICE gathering state:",pc.iceGatheringState)}async function startVoiceCall(calleeId,calleeName){if(voiceCallState){alert("You are already on a call.");return}if(!navigator.mediaDevices||!navigator.mediaDevices.getUserMedia){alert("Voice calling needs microphone access, which this browser/context does not support.");return}let localStream;try{localStream=await navigator.mediaDevices.getUserMedia({audio:true})}catch(e){alert("Microphone access was blocked or unavailable — allow microphone access to make a call.");return}const outboundChannel=sb.channel("call-signal-"+calleeId);await voiceChannelReady(outboundChannel);const iceServers=await voiceIceServers();voiceLogIceServers(iceServers);const pc=new RTCPeerConnection({iceServers});localStream.getTracks().forEach(track=>pc.addTrack(track,localStream));pc.ontrack=e=>{$(e.track.kind==="video"?"remoteCallVideo":"remoteCallAudio").srcObject=e.streams[0]};pc.onicecandidate=e=>{if(e.candidate){console.log("[call] local ICE candidate:",e.candidate.type,e.candidate.protocol);voiceSend(outboundChannel,{type:"ice-candidate",candidate:e.candidate,tag:voiceCallState&&voiceCallState.pcTag||void 0})}};voiceAttachDiagnostics(pc);pc.onconnectionstatechange=()=>voiceOnPcState(pc);const{data:callRow}=await sb.from("voice_calls").insert({caller_id:myUserId,callee_id:calleeId}).select().single();notifyPushRecipients([calleeId],{title:`Incoming call from ${myFullName||"a teammate"}`,body:"Tap to open the dashboard and answer.",tag:`call-${callRow?callRow.id:calleeId}`,url:"/",require_interaction:true},{always:true});voiceCallState={role:"caller",peerId:calleeId,peerName:calleeName,pc,localStream,outboundChannel,callRowId:callRow?callRow.id:null,status:"ringing",remoteDescSet:false,pendingCandidates:[],videoSender:null,screenStream:null,hasScreenShare:false,peerSharing:false,videoPanelMinimized:false,videoPanelMaximized:false,groupId:voiceGenId(),extraPeers:{}};voiceRosterInit(voiceCallState.groupId);voiceRosterInsert(voiceCallState.groupId,myUserId,myFullName||myEmail,myUserId,"connected").then(()=>voiceRosterInsert(voiceCallState.groupId,calleeId,calleeName,myUserId,"ringing"));const offer=await pc.createOffer();await pc.setLocalDescription(offer);voiceSend(outboundChannel,{type:"offer",sdp:offer,callRowId:voiceCallState.callRowId,groupId:voiceCallState.groupId});voiceRepeatOffer(outboundChannel,{type:"offer-retry",sdp:offer,callRowId:voiceCallState.callRowId,groupId:voiceCallState.groupId});voiceCallState.ringStartedAt=Date.now();voiceMarkLive(voiceCallState);voiceShowActiveBar(calleeName,"Calling…",true);voiceStartRingtone("ringback");voiceCallState.ringTimeout=setTimeout(()=>{if(voiceCallState&&voiceCallState.status==="ringing")endVoiceCall("missed",true)},VOICE_RING_MS)}async function startVoiceCallToClient(clientId,clientName){if(voiceCallState){alert("You are already on a call.");return}if(!navigator.mediaDevices||!navigator.mediaDevices.getUserMedia){alert("Voice calling needs microphone access, which this browser/context does not support.");return}let localStream;try{localStream=await navigator.mediaDevices.getUserMedia({audio:true})}catch(e){alert("Microphone access was blocked or unavailable — allow microphone access to make a call.");return}const outboundChannel=sb.channel("call-signal-"+clientId);await voiceChannelReady(outboundChannel);const iceServers=await voiceIceServers();voiceLogIceServers(iceServers);const pc=new RTCPeerConnection({iceServers});localStream.getTracks().forEach(track=>pc.addTrack(track,localStream));pc.ontrack=e=>{$(e.track.kind==="video"?"remoteCallVideo":"remoteCallAudio").srcObject=e.streams[0]};pc.onicecandidate=e=>{if(e.candidate)voiceSend(outboundChannel,{type:"ice-candidate",candidate:e.candidate})};voiceAttachDiagnostics(pc);pc.onconnectionstatechange=()=>voiceOnPcState(pc);const{data:callRow}=await sb.from("voice_calls").insert({caller_id:myUserId,callee_client_id:clientId}).select().single();sb.functions.invoke("send-push-notification",{body:{client_id:clientId,title:`Incoming call from ${myFullName||"PropTech AI"}`,body:"Open the PropTech AI app or client portal to answer.",tag:`call-${callRow?callRow.id:clientId}`,url:"/",require_interaction:true,category:"voice_call"}}).then(({error})=>{if(error)console.warn("call push to client failed",error)}).catch(()=>{});voiceCallState={role:"caller",isClientCall:true,peerId:clientId,peerName:clientName,pc,localStream,outboundChannel,callRowId:callRow?callRow.id:null,status:"ringing",remoteDescSet:false,pendingCandidates:[],videoSender:null,screenStream:null,hasScreenShare:false,peerSharing:false,videoPanelMinimized:false,videoPanelMaximized:false,groupId:voiceGenId(),extraPeers:{}};const offer=await pc.createOffer();await pc.setLocalDescription(offer);voiceSend(outboundChannel,{type:"offer",sdp:offer,callRowId:voiceCallState.callRowId});voiceRepeatOffer(outboundChannel,{type:"offer-retry",sdp:offer,callRowId:voiceCallState.callRowId});voiceCallState.ringStartedAt=Date.now();voiceMarkLive(voiceCallState);voiceShowActiveBar(clientName,"Calling…",true);voiceStartRingtone("ringback");voiceCallState.ringTimeout=setTimeout(()=>{if(voiceCallState&&voiceCallState.status==="ringing")endVoiceCall("missed",true)},VOICE_RING_MS)}async function acceptIncomingCall(){if(!voiceCallState||voiceCallState.role!=="callee")return;voiceStopRingtone();$("incomingCallBanner").style.display="none";requestAnimationFrame(voiceSetCallStackOffsets);let localStream;try{localStream=await navigator.mediaDevices.getUserMedia({audio:true})}catch(e){alert("Microphone access was blocked or unavailable.");declineIncomingCall();return}const outboundChannel=sb.channel("call-signal-"+voiceCallState.peerId);await voiceChannelReady(outboundChannel);const iceServers=await voiceIceServers();voiceLogIceServers(iceServers);const pc=new RTCPeerConnection({iceServers});localStream.getTracks().forEach(track=>pc.addTrack(track,localStream));pc.ontrack=e=>{$(e.track.kind==="video"?"remoteCallVideo":"remoteCallAudio").srcObject=e.streams[0]};pc.onicecandidate=e=>{if(e.candidate){console.log("[call] local ICE candidate:",e.candidate.type,e.candidate.protocol);voiceSend(outboundChannel,{type:"ice-candidate",candidate:e.candidate,tag:voiceCallState&&voiceCallState.pcTag||void 0})}};voiceAttachDiagnostics(pc);pc.onconnectionstatechange=()=>voiceOnPcState(pc);voiceCallState.pc=pc;await pc.setRemoteDescription(new RTCSessionDescription(voiceCallState.offerSdp));voiceCallState.remoteDescSet=true;await voiceFlushPendingCandidates(voiceCallState);const answer=await pc.createAnswer();await pc.setLocalDescription(answer);voiceCallState.localStream=localStream;voiceCallState.outboundChannel=outboundChannel;voiceCallState.status="answered";voiceCallState.startedAt=Date.now();voiceSend(outboundChannel,{type:"answer",sdp:answer});if(voiceCallState.callRowId)sb.from("voice_calls").update({status:"answered",answered_at:new Date().toISOString()}).eq("id",voiceCallState.callRowId).then(()=>{});if(voiceMyChannel)voiceSend(voiceMyChannel,{type:"claimed",callRowId:voiceCallState.callRowId});voiceRosterSetStatus(myUserId,"connected");voiceShowActiveBar(voiceCallState.peerName,"Connecting…",true);voiceStartCallTimer();voiceMarkLive(voiceCallState);if(voiceCallState.meshParticipants&&voiceCallState.meshParticipants.length)meshDialAll()}function declineIncomingCall(){if(!voiceCallState||voiceCallState.role!=="callee")return;voiceStopRingtone();$("incomingCallBanner").style.display="none";requestAnimationFrame(voiceSetCallStackOffsets);voiceSendOneOff(voiceCallState.peerId,{type:"decline"});if(voiceCallState.callRowId)sb.from("voice_calls").update({status:"declined",ended_at:new Date().toISOString()}).eq("id",voiceCallState.callRowId).then(()=>{});voiceRosterSetStatus(myUserId,"declined");voiceRosterTeardown();voiceCallState=null}function voiceUpdateScreenShareUI(){const state=voiceCallState;if(!state)return;const showing=state.hasScreenShare||state.peerSharing;$("callScreenShareBtn").classList.toggle("active",!!state.hasScreenShare);const minimizeBtn=$("callMinimizeBtn");minimizeBtn.style.display=showing?"flex":"none";if(!showing)state.videoPanelMinimized=false;const minimized=showing&&state.videoPanelMinimized;minimizeBtn.classList.toggle("alt",minimized);minimizeBtn.title=minimized?"Show screen share preview":"Minimize screen share preview";const maximizeBtn=$("callMaximizeBtn");maximizeBtn.style.display=showing&&!minimized?"flex":"none";if(!showing)state.videoPanelMaximized=false;const maximized=showing&&!minimized&&state.videoPanelMaximized;maximizeBtn.classList.toggle("alt",maximized);maximizeBtn.title=maximized?"Restore screen share preview":"Maximize screen share preview";$("callVideoPanel").classList.toggle("maximized",maximized);$("callVideoPanel").style.display=showing&&!minimized?"block":"none";if(!state.hasScreenShare)$("localCallVideo").srcObject=null;if(!state.peerSharing)$("remoteCallVideo").srcObject=null;if(!showing){$("callScreenShareBadge").style.display="none";return}$("callScreenShareBadge").style.display="block";$("callScreenShareBadge").textContent=state.hasScreenShare?"You’re sharing your screen":`${state.peerName||"They"} are sharing their screen`}async function voiceRenegotiate(){const state=voiceCallState;if(!state||!state.pc||!state.outboundChannel)return;const offer=await state.pc.createOffer();await state.pc.setLocalDescription(offer);voiceSend(state.outboundChannel,{type:"renegotiate-offer",sdp:offer})}async function startScreenShare(){if(!voiceCallState||!voiceCallState.pc)return;if(voiceCallState.status!=="answered"){alert("Wait until the call connects before sharing your screen.");return}if(voiceCallState.hasScreenShare)return;if(!navigator.mediaDevices||!navigator.mediaDevices.getDisplayMedia){alert(window.isSecureContext===false?"Screen sharing needs a secure (https) connection, and this page is not on one.":"This browser does not support screen sharing.");return}let screenStream;try{screenStream=await navigator.mediaDevices.getDisplayMedia({video:true,audio:false})}catch(e){console.error("[call] getDisplayMedia failed:",e&&e.name,e&&e.message,e);const name=e&&e.name||"";const msg=String(e&&e.message||"");const policyBlocked=/by system|policy|disallow|blocked|not supported/i.test(msg);if(name==="NotAllowedError"&&!policyBlocked)return;if(name==="NotFoundError"){alert("No screen or window was available to share.")}else if(policyBlocked||name==="NotAllowedError"){alert("Your browser blocked screen sharing.\n\nOn macOS this is usually the operating system: System Settings → Privacy & Security → Screen Recording, tick your browser, then restart it.")}else if(name==="NotSupportedError"||name==="TypeError"){alert("This browser does not support screen sharing.")}else{alert("Screen sharing could not start: "+(msg||name||"unknown error"))}return}if(!screenStream||!screenStream.getVideoTracks().length){console.error("[call] getDisplayMedia returned no video track");alert("Screen sharing started but no video track was produced.");return}const screenTrack=screenStream.getVideoTracks()[0];screenTrack.onended=()=>{if(voiceCallState&&voiceCallState.hasScreenShare)stopScreenShare()};voiceCallState.screenStream=screenStream;voiceCallState.hasScreenShare=true;voiceUpdateScreenShareUI();$("localCallVideo").srcObject=screenStream;if(voiceCallState.videoSender){try{await voiceCallState.videoSender.replaceTrack(screenTrack)}catch(e){console.error("[call] replaceTrack failed",e)}voiceSend(voiceCallState.outboundChannel,{type:"screenshare-status",sharing:true})}else{voiceCallState.videoSender=voiceCallState.pc.addTrack(screenTrack,screenStream);await voiceRenegotiate()}}async function stopScreenShare(){const state=voiceCallState;if(!state||!state.hasScreenShare)return;state.hasScreenShare=false;if(state.screenStream)state.screenStream.getTracks().forEach(t=>t.stop());state.screenStream=null;voiceUpdateScreenShareUI();if(state.videoSender){try{await state.videoSender.replaceTrack(null)}catch(e){console.error("[call] replaceTrack(null) failed",e)}}if(state.outboundChannel)voiceSend(state.outboundChannel,{type:"screenshare-status",sharing:false})}function voiceStartCallTimer(){voicePlayConnectSound();voiceShowActiveBar(voiceCallState.peerName,"00:00",false);$("callScreenShareBtn").style.display="flex";voiceCallState.durationInterval=setInterval(()=>{if(!voiceCallState||!voiceCallState.startedAt||voiceCallState.reconnecting)return;const secs=Math.floor((Date.now()-voiceCallState.startedAt)/1e3);$("activeCallStatus").textContent=`${String(Math.floor(secs/60)).padStart(2,"0")}:${String(secs%60).padStart(2,"0")}`},1e3)}function voiceSetCallStackOffsets(){var TOP=20,GAP=12;var shown=function(el){return!!el&&getComputedStyle(el).display!=="none"};var h=function(el){return el?el.getBoundingClientRect().height:0};var bar=document.getElementById("activeCallBar");var call=document.getElementById("incomingCallBanner");var meet=document.getElementById("incomingMeetBanner");var y=TOP;if(shown(bar))y+=h(bar)+GAP;document.documentElement.style.setProperty("--call-stack-top",y+"px");if(shown(call))y+=h(call)+GAP;document.documentElement.style.setProperty("--meet-stack-top",y+"px")}function voiceShowActiveBar(name,status,connecting){$("activeCallName").textContent=name||"";$("activeCallStatus").textContent=status;$("activeCallDot").className="call-bar-dot"+(connecting?" connecting":"");$("activeCallBar").classList.toggle("connecting",!!connecting);$("activeCallBar").style.display="flex";requestAnimationFrame(voiceSetCallStackOffsets);$("callMuteBtn").classList.remove("muted");$("callScreenShareBtn").style.display="none";$("callScreenShareBtn").classList.remove("active");$("callMinimizeBtn").style.display="none";$("callMaximizeBtn").style.display="none"}function voicePostCallSummary(state,finalStatus){if(state.role!=="caller"||!state.peerId||!finalStatus)return;let text;if(finalStatus==="ended"&&state.startedAt){const secs=Math.floor((Date.now()-state.startedAt)/1e3);text=`📞 Call ended · ${String(Math.floor(secs/60)).padStart(2,"0")}:${String(secs%60).padStart(2,"0")}`}else if(finalStatus==="declined"){text="📞 Call declined"}else if(finalStatus==="missed"){text="📞 No answer"}else if(finalStatus==="failed"){text="📞 Call failed to connect"}else{return}if(state.isClientCall){sb.from("client_chat_messages").insert({client_id:state.peerId,sender:"staff",staff_id:myUserId,body:text}).then(({error})=>{if(error)console.error("Failed to post call summary to client chat",error)});return}sb.from("chat_messages").insert({sender_id:myUserId,recipient_id:state.peerId,body:text}).then(({error})=>{if(error)console.error("Failed to post call summary to chat",error)})}function endVoiceCall(finalStatus,notifyPeer){if(!voiceCallState)return;const state=voiceCallState;clearTimeout(state.ringTimeout);clearInterval(state.offerRetry);clearInterval(state.rowWatch);clearInterval(state.durationInterval);clearInterval(state.saveTimer);voiceClearReconnect(state);voiceClearSavedCall();voiceStopRingtone();if(state.status==="answered")voicePlayHangupSound();if(state.screenStream)state.screenStream.getTracks().forEach(t=>t.stop());if(state.pc)state.pc.close();if(state.localStream)state.localStream.getTracks().forEach(t=>t.stop());Object.keys(state.extraPeers||{}).forEach(peerId=>{const peer=state.extraPeers[peerId];if(notifyPeer&&peer.channel)voiceSend(peer.channel,{type:"mesh-hangup"});if(peer.pc)peer.pc.close();if(peer.channel)sb.removeChannel(peer.channel);const audioEl=document.getElementById("extraAudio-"+peerId);if(audioEl)audioEl.remove()});if(state.groupId){voiceRosterSetStatus(myUserId,finalStatus==="declined"?"declined":state.status==="answered"?"left":"no_answer");voiceRosterTeardown()}$("callAddPanel").style.display="none";$("callParticipantsPanel").style.display="none";$("callParticipantsBtn").style.display="none";$("remoteCallAudio").srcObject=null;$("remoteCallVideo").srcObject=null;$("localCallVideo").srcObject=null;$("callVideoPanel").style.display="none";$("callVideoPanel").classList.remove("maximized");$("callScreenShareBadge").style.display="none";$("callScreenShareBtn").classList.remove("active");$("callScreenShareBtn").style.display="none";$("callMinimizeBtn").style.display="none";$("callMaximizeBtn").style.display="none";if(notifyPeer&&state.peerId){if(state.outboundChannel)voiceSend(state.outboundChannel,{type:"hangup"});else voiceSendOneOff(state.peerId,{type:"hangup"})}if(state.outboundChannel)sb.removeChannel(state.outboundChannel);if(state.callRowId&&finalStatus){const update={status:finalStatus};if(finalStatus==="ended"||finalStatus==="failed"||finalStatus==="missed")update.ended_at=new Date().toISOString();sb.from("voice_calls").update(update).eq("id",state.callRowId).then(()=>{})}voicePostCallSummary(state,finalStatus);$("incomingCallBanner").style.display="none";requestAnimationFrame(voiceSetCallStackOffsets);$("activeCallBar").style.display="none";requestAnimationFrame(voiceSetCallStackOffsets);voiceCallState=null}$("acceptCallBtn").addEventListener("click",acceptIncomingCall);$("declineCallBtn").addEventListener("click",declineIncomingCall);$("callHangupBtn").addEventListener("click",()=>{if(!voiceCallState)return;endVoiceCall(voiceCallState.status==="answered"?"ended":"missed",true)});$("callMuteBtn").addEventListener("click",()=>{if(!voiceCallState||!voiceCallState.localStream)return;const track=voiceCallState.localStream.getAudioTracks()[0];if(!track)return;track.enabled=!track.enabled;$("callMuteBtn").classList.toggle("muted",!track.enabled)});$("callScreenShareBtn").addEventListener("click",()=>{if(!voiceCallState)return;if(voiceCallState.hasScreenShare)stopScreenShare();else startScreenShare()});$("callMinimizeBtn").addEventListener("click",()=>{if(!voiceCallState)return;voiceCallState.videoPanelMinimized=!voiceCallState.videoPanelMinimized;voiceUpdateScreenShareUI()});$("callMaximizeBtn").addEventListener("click",()=>{if(!voiceCallState)return;voiceCallState.videoPanelMaximized=!voiceCallState.videoPanelMaximized;voiceUpdateScreenShareUI()});$("chatCallBtn").addEventListener("click",()=>{if(!chatActiveConv||chatActiveConv==="team"||chatActiveConv.startsWith("client:"))return;if(chatActiveConv.startsWith("group:")){startGroupConferenceCall(chatActiveConv.slice(6));return}startVoiceCall(chatActiveConv,chatConvDisplayName(chatActiveConv))});const CONFERENCE_SOFT_LIMIT=6;async function startGroupConferenceCall(groupId){const group=chatGroups[groupId];if(!group){alert("That group could not be loaded — try reopening the chat.");return}const others=(group.members||[]).map(m=>m.user_id).filter(id=>id&&id!==myUserId);if(!others.length){alert("There's nobody else in this group to call yet.");return}if(others.length+1>CONFERENCE_SOFT_LIMIT){const ok=confirm("This group has "+(others.length+1)+" people."+String.fromCharCode(10)+String.fromCharCode(10)+"Calls here are peer-to-peer, so everyone sends their audio to everyone else. Past about "+CONFERENCE_SOFT_LIMIT+" that gets choppy and eats bandwidth."+String.fromCharCode(10)+String.fromCharCode(10)+"Ring them all anyway?");if(!ok)return}const nameOf=id=>chatContactName(id)||"Colleague";await startVoiceCall(others[0],nameOf(others[0]));if(!voiceCallState||!voiceCallState.localStream)return;for(const id of others.slice(1)){if(!voiceCallState)break;try{await inviteToCall(id,nameOf(id))}catch(e){console.error("[call] conference invite failed for",id,e)}}}const CHAT_EMOJIS=["😀","😂","😊","😉","😍","🥰","😎","🤔","😅","😢","😭","😡","👍","👎","🙏","👏","🎉","🔥","💯","✅","❌","❤️","💪","🚀","☕","📞","⏰","📌","👀","🙌"];function chatBuildEmojiPicker(){$("chatEmojiPicker").innerHTML=CHAT_EMOJIS.map(e=>`<span>${e}</span>`).join("");$("chatEmojiPicker").querySelectorAll("span").forEach(span=>{span.addEventListener("click",()=>{const input=$("chatInput");const pos=input.selectionStart??input.value.length;input.value=input.value.slice(0,pos)+span.textContent+input.value.slice(pos);input.focus();input.setSelectionRange(pos+span.textContent.length,pos+span.textContent.length);$("chatEmojiPicker").style.display="none"})})}$("chatEmojiBtn").addEventListener("click",()=>{if($("chatEmojiPicker").innerHTML==="")chatBuildEmojiPicker();$("chatEmojiPicker").style.display=$("chatEmojiPicker").style.display==="none"?"grid":"none";$("chatMentionBox").style.display="none"});function chatStagePendingFile(file){if(!file)return;if(file.size>15*1024*1024){alert("That file is larger than 15MB — please pick a smaller one.");return}chatClearPendingFile();const isImage=file.type.startsWith("image/");chatPendingFile={file,previewUrl:isImage?URL.createObjectURL(file):null};const preview=$("chatAttachPreview");preview.style.display="flex";preview.innerHTML=`
    ${isImage?`<img src="${chatPendingFile.previewUrl}">`:"📎"}
    <span>${escapeHtml(file.name)}</span>
    <span class="chat-attach-remove" title="Remove">&times;</span>
  `;preview.querySelector(".chat-attach-remove").addEventListener("click",chatClearPendingFile)}$("chatAttachBtn").addEventListener("click",()=>$("chatFileInput").click());$("chatFileInput").addEventListener("change",()=>{const file=$("chatFileInput").files[0];chatStagePendingFile(file);$("chatFileInput").value=""});$("chatInput").addEventListener("paste",e=>{const items=e.clipboardData&&e.clipboardData.items;if(!items)return;for(const item of items){if(item.kind==="file"){const file=item.getAsFile();if(file){e.preventDefault();chatStagePendingFile(file.name?file:new File([file],`pasted-${Date.now()}.${file.type.split("/")[1]||"png"}`,{type:file.type}))}return}}});$("chatInput").addEventListener("input",()=>{const key=chatActiveConv;if(!key||key==="team"||key.startsWith("group:"))return;clearTimeout(chatTypingStopTimer);if($("chatInput").value.trim()===""){chatTypingSend(key,"stopped");chatLastTypingPingAt=0;return}const now=Date.now();if(now-chatLastTypingPingAt>2e3){chatLastTypingPingAt=now;chatTypingSend(key,"typing")}chatTypingStopTimer=setTimeout(()=>{chatTypingSend(key,"stopped");chatLastTypingPingAt=0},2500)});let chatMentionStart=-1;function chatHideMentionBox(){chatMentionStart=-1;$("chatMentionBox").style.display="none"}function chatMentionCandidates(){const names=chatDirectory.map(p=>({id:p.id,name:p.full_name||p.email}));if(chatActiveConv==="team")names.unshift({id:"everyone",name:"everyone"});return names}$("chatInput").addEventListener("input",()=>{const input=$("chatInput");const pos=input.selectionStart??input.value.length;const uptoCursor=input.value.slice(0,pos);const match=uptoCursor.match(/(?:^|\s)@([\w\s]{0,30})$/);if(!match){chatHideMentionBox();return}chatMentionStart=pos-match[1].length-1;const query=match[1].trim().toLowerCase();const candidates=chatMentionCandidates().filter(c=>c.name.toLowerCase().includes(query)).slice(0,6);if(candidates.length===0){chatHideMentionBox();return}$("chatMentionBox").innerHTML=candidates.map(c=>`<div class="chat-mention-item" data-name="${escapeHtml(c.name)}">${escapeHtml(c.name)}</div>`).join("");$("chatMentionBox").querySelectorAll(".chat-mention-item").forEach(el=>{el.addEventListener("click",()=>chatInsertMention(el.dataset.name))});$("chatMentionBox").style.display="block";$("chatEmojiPicker").style.display="none"});function chatInsertMention(name){const input=$("chatInput");const pos=input.selectionStart??input.value.length;input.value=input.value.slice(0,chatMentionStart)+"@"+name+" "+input.value.slice(pos);const newPos=chatMentionStart+name.length+2;input.focus();input.setSelectionRange(newPos,newPos);chatHideMentionBox()}$("chatFabBtn").addEventListener("click",()=>{const opening=$("chatPanel").style.display==="none";$("chatPanel").style.display=opening?"flex":"none";if(opening){chatEnsureNotificationPermission();if(chatActiveConv){chatMarkRead(chatActiveConv)}}});function positionDesoukyMascot(){const wrap=$("desoukyMascotWrap"),panel=$("chatPanel");if(!wrap||!panel||wrap.style.display==="none"||panel.style.display==="none")return;const pr=panel.getBoundingClientRect();const ww=wrap.offsetWidth,wh=wrap.offsetHeight,gap=16,pad=8;let left=pr.left-ww-gap;let top=pr.bottom-wh;let fits=left>=pad;if(!fits){left=Math.min(Math.max(pad,pr.right-ww),window.innerWidth-ww-pad);top=pr.top-wh-gap;fits=top>=pad}top=Math.max(pad,Math.min(top,window.innerHeight-wh-pad));wrap.style.left=Math.round(left)+"px";wrap.style.top=Math.round(top)+"px";wrap.style.right="auto";wrap.style.bottom="auto";wrap.style.visibility=fits?"visible":"hidden"}window.addEventListener("resize",positionDesoukyMascot);function desoukyShowTeaserOnce(){try{if(sessionStorage.getItem("desoukyTeaserShown"))return}catch(e){}setTimeout(()=>{if($("chatPanel").style.display!=="none")return;$("desoukyTeaser").classList.add("show");try{sessionStorage.setItem("desoukyTeaserShown","1")}catch(e){}},2500)}function desoukyDismissTeaser(){$("desoukyTeaser").classList.remove("show")}function desoukyOpenChat(){desoukyDismissTeaser();$("chatPanel").style.display="flex";chatEnsureNotificationPermission();openChatThread(AI_ASSISTANT_BOT_ID)}$("desoukyMascotBtn").addEventListener("click",desoukyOpenChat);if($("desoukySidebarBtn"))$("desoukySidebarBtn").addEventListener("click",desoukyOpenChat);$("desoukyTeaser").addEventListener("click",desoukyOpenChat);$("desoukyTeaserCloseBtn").addEventListener("click",e=>{e.stopPropagation();desoukyDismissTeaser()});const PEND_LEADS_SECTIONS={queue_review:"queue_review",queue_review_ispeedtolead:"primehome_unsent",tickets_review:"tickets_review",call_back:"call_back"};const PEND_LEADS_TOTAL=["queue_review","queue_review_ispeedtolead","tickets_review"];let pendingCounts=null;let pendingCountsStarted=false;let pendingCountsSettle=null;let desoukyRemindDue=false;let desoukyRemindAutoHide=null;function pendCanSeeLeads(section){const tabs=myPermissions&&myPermissions.allowedTabs||[];return(myIsOwner||tabs.includes("leads"))&&canViewLeadsSection(section)}function pendNavVisible(tab){const b=document.querySelector(`.tab-btn[data-tab="${tab}"]`);return!!b&&b.style.display!=="none"}function pendSetBadge(el,n){if(!el)return;el.textContent=n>99?"99+":String(n);el.style.display=n>0?"":"none"}function renderPendingBadges(){if(!pendingCounts)return;const seen=section=>pendCanSeeLeads(section)?Number(pendingCounts[PEND_LEADS_SECTIONS[section]]||0):0;Object.keys(PEND_LEADS_SECTIONS).forEach(section=>{pendSetBadge(document.querySelector(`.leads-subtab-btn[data-leads-section="${section}"] .pend-badge`),seen(section))});pendSetBadge($("leadsNavBadge"),PEND_LEADS_TOTAL.reduce((sum,section)=>sum+seen(section),0))}async function refreshPendingCounts(){if(!sb||!myUserId)return;const{data,error}=await sb.rpc("staff_pending_counts");if(error){console.warn("pending counts:",error.message);return}pendingCounts=data||{};renderPendingBadges();if(desoukyRemindDue){desoukyRemindDue=false;setTimeout(desoukyRemindShow,1200)}else if(!$("desoukyRemind").hidden)desoukyRemindRender()}function pendingCountsSoon(){clearTimeout(pendingCountsSettle);pendingCountsSettle=setTimeout(refreshPendingCounts,1500)}function desoukyRemindFirstOpen(isNewLogin){const key="desoukyRemindShown:"+myUserId;try{if(!isNewLogin&&sessionStorage.getItem(key))return false;sessionStorage.setItem(key,"1")}catch(e){return!!isNewLogin}return true}function startPendingCounts(isNewLogin){if(desoukyRemindKeys().length&&desoukyRemindFirstOpen(isNewLogin))desoukyRemindDue=true;if(pendingCountsStarted){refreshPendingCounts();return}const tabs=myPermissions&&myPermissions.allowedTabs||[];if(!myIsOwner&&!tabs.includes("leads")&&!pendNavVisible("client_chat")&&!pendNavVisible("client_disputes"))return;pendingCountsStarted=true;refreshPendingCounts();setInterval(()=>{if(!document.hidden)refreshPendingCounts()},3e4);document.addEventListener("visibilitychange",()=>{if(!document.hidden)pendingCountsSoon()});const ch=sb.channel("pending-counts");["leads","lead_disputes","client_chat_threads"].forEach(table=>{ch.on("postgres_changes",{event:"*",schema:"public",table},table==="leads"?()=>{pendingCountsSoon();callBackLiveSoon()}:pendingCountsSoon)});ch.subscribe()}const DESOUKY_REMIND_ROLES={"quality":["client_chat","client_disputes","queue_review","queue_review_ispeedtolead","tickets_review"],"team leader":["call_back"],"team lead":["call_back"]};const DESOUKY_REMIND_ITEMS={client_chat:{label:"Client chat",key:"chats_waiting",note:n=>`${n===1?"chat":"chats"} waiting to be accepted`,can:()=>pendNavVisible("client_chat"),go:()=>pendOpenTab("client_chat")},client_disputes:{label:"Dispute/Feedback",key:"disputes_open",note:n=>n===1?"open dispute or feedback":"open disputes and feedback",can:()=>pendNavVisible("client_disputes"),go:()=>pendOpenTab("client_disputes")},queue_review:{label:"Leads Queue Review",key:"queue_review",note:n=>`${n===1?"lead":"leads"} waiting for review`,can:()=>pendCanSeeLeads("queue_review"),go:()=>pendOpenLeads("queue_review")},queue_review_ispeedtolead:{label:"Leads Queue Review — PrimeHome Buyers",key:"primehome_unsent",note:n=>`pending ${n===1?"lead":"leads"} not sent yet`,can:()=>pendCanSeeLeads("queue_review_ispeedtolead"),go:()=>pendOpenLeads("queue_review_ispeedtolead")},tickets_review:{label:"Tickets Review",key:"tickets_review",note:n=>`${n===1?"ticket":"tickets"} waiting for staff review`,can:()=>pendCanSeeLeads("tickets_review"),go:()=>pendOpenLeads("tickets_review")},call_back:{label:"Call Back",key:"call_back",note:(n,c)=>`${n===1?"call back":"call backs"} waiting to be picked up`+(Number(c.call_back_mine||0)?` · ${c.call_back_mine} picked up by you`:""),can:()=>pendCanSeeLeads("call_back"),go:()=>pendOpenLeads("call_back")}};function desoukyRemindKeys(){const role=String(myRole||"").toLowerCase().replace(/[\s_-]+/g," ").trim();return(DESOUKY_REMIND_ROLES[role]||[]).filter(k=>DESOUKY_REMIND_ITEMS[k].can())}function pendOpenTab(tab){const b=document.querySelector(`.tab-btn[data-tab="${tab}"]`);if(b)b.click()}function pendOpenLeads(section){const panel=$("tab-leads");if(panel&&panel.classList.contains("active")){activateLeadsSection(section);return}leadsActiveSection=section;pendOpenTab("leads")}function desoukyRemindRender(){const keys=desoukyRemindKeys();const c=pendingCounts||{};const total=keys.reduce((sum,k)=>sum+Number(c[DESOUKY_REMIND_ITEMS[k].key]||0),0);const first=String(myFullName||"").trim().split(/\s+/)[0]||"there";$("desoukyRemindTitle").textContent=total?`Hi ${first}, here's what's waiting for you`:`Hi ${first}, your queues are clear`;$("desoukyRemindSub").textContent=total?`${total} ${total===1?"item needs":"items need"} you right now. Click one to open it.`:"Nothing is waiting on your queues right now.";$("desoukyRemindRows").innerHTML=keys.map(k=>{const it=DESOUKY_REMIND_ITEMS[k];const n=Number(c[it.key]||0);return`<button type="button" class="desouky-remind-row${n?"":" zero"}" data-k="${k}">
        <div><div class="desouky-remind-label">${escapeHtml(it.label)}</div><div class="desouky-remind-note">${escapeHtml(it.note(n,c))}</div></div>
        <span class="desouky-remind-count">${n>999?"999+":n}</span>
      </button>`}).join("");$("desoukyRemindRows").querySelectorAll(".desouky-remind-row").forEach(b=>{b.addEventListener("click",()=>{desoukyRemindHide();DESOUKY_REMIND_ITEMS[b.dataset.k].go()})});return total}function desoukyRemindShow(){if(!desoukyRemindKeys().length)return;const face=$("desoukyRemindFace");const art=document.querySelector("#desoukyMascotBtn svg");if(face&&!face.firstChild&&art)face.innerHTML=art.outerHTML.replace(/id="dsk/g,'id="dskRm').replace(/#dsk/g,"#dskRm");const total=desoukyRemindRender();$("desoukyRemind").hidden=false;clearTimeout(desoukyRemindAutoHide);if(!total)desoukyRemindAutoHide=setTimeout(desoukyRemindHide,12e3)}function desoukyRemindHide(){clearTimeout(desoukyRemindAutoHide);$("desoukyRemind").hidden=true}$("desoukyRemindClose").addEventListener("click",desoukyRemindHide);$("desoukyRemindOk").addEventListener("click",desoukyRemindHide);$("chatFabBtn").addEventListener("click",desoukyRemindHide);$("chatCloseBtn").addEventListener("click",()=>{$("chatPanel").style.display="none";if($("desoukyMascotWrap"))$("desoukyMascotWrap").style.display="none"});$("chatSearchInput").addEventListener("input",e=>renderChatConvList(e.target.value));$("chatThreadBackBtn").addEventListener("click",()=>{if(chatActiveConv&&chatActiveConv!=="team"&&!chatActiveConv.startsWith("group:")){clearTimeout(chatTypingStopTimer);chatTypingSend(chatActiveConv,"stopped")}chatCloseClientTypingChannel();chatActiveConv=null;chatClearPendingFile();chatHideMentionBox();chatStopRecording(true);$("chatThreadView").style.display="none";$("chatListView").style.display="flex";renderChatConvList($("chatSearchInput").value)});$("chatSendBtn").addEventListener("click",sendChatMessage);$("chatMeetBtn").addEventListener("click",()=>{if(!chatActiveConv)return;if(!chatMeetAllowed())return;window.open("https://meet.google.com/new","_blank","noopener");const link=prompt("Google Meet is opening in a new tab. Paste the meeting link here to invite this conversation to it (Cancel to skip):");if(link===null)return;const trimmed=link.trim();if(!trimmed)return;$("chatInput").value=`📹 Google Meet: ${trimmed}`;sendChatMessage();const isGroup=chatActiveConv.startsWith("group:");let recipientIds=[];let meetGroupId=null;if(isGroup){meetGroupId=chatActiveConv.slice(6);recipientIds=(chatGroups[meetGroupId]&&chatGroups[meetGroupId].members||[]).map(m=>m.user_id).filter(id=>id!==myUserId)}else if(chatActiveConv!=="team"){recipientIds=[chatActiveConv]}recipientIds.forEach(id=>voiceSendOneOff(id,{type:"meet-invite",link:trimmed,groupId:meetGroupId}))});$("chatInput").addEventListener("keydown",e=>{if(e.key==="Enter"&&$("chatMentionBox").style.display==="none"){e.preventDefault();sendChatMessage()}else if(e.key==="Escape"){chatHideMentionBox();$("chatEmojiPicker").style.display="none"}});function chatUpdateMyAvatarButton(){const btn=$("chatMyAvatarBtnInner");btn.innerHTML=myAvatarUrl?`<img src="${escapeHtml(myAvatarUrl)}" alt="">`:escapeHtml((myFullName||myEmail||"?").charAt(0).toUpperCase())}$("chatMyProfileBtn").addEventListener("click",()=>{$("chatListView").style.display="none";$("chatMyProfileView").style.display="flex";$("chatMyAvatarPreview").innerHTML=myAvatarUrl?`<img src="${escapeHtml(myAvatarUrl)}" alt="">`:escapeHtml((myFullName||myEmail||"?").charAt(0).toUpperCase());$("chatMyStatusSelect").value=myChatStatusManual||"auto";$("chatStatusEmoji").value=myStatusEmoji||"";$("chatStatusText").value=myStatusText||"";$("chatStatusUntil").value="";$("chatMyProfileMsg").textContent=myStatusUntil?"Your status clears "+chatFmtUntil(myStatusUntil)+".":"";$("chatMyProfileMsg").className="msg"});$("chatMyProfileBackBtn").addEventListener("click",()=>{$("chatMyProfileView").style.display="none";$("chatListView").style.display="flex"});async function avatarSquareJpeg(blob,size){const bmp=await createImageBitmap(blob);const side=Math.min(bmp.width,bmp.height);let cur=document.createElement("canvas");cur.width=cur.height=side;cur.getContext("2d").drawImage(bmp,(bmp.width-side)/2,(bmp.height-side)/2,side,side,0,0,side,side);if(bmp.close)bmp.close();while(cur.width>size){const next=document.createElement("canvas");next.width=next.height=Math.max(size,Math.round(cur.width/2));const ctx=next.getContext("2d");ctx.imageSmoothingEnabled=true;ctx.imageSmoothingQuality="high";ctx.drawImage(cur,0,0,next.width,next.height);cur=next}return await new Promise((resolve,reject)=>cur.toBlob(b=>b?resolve(b):reject(new Error("Could not encode the photo")),"image/jpeg",.9))}async function avatarAutoShrink(){try{if(!myAvatarUrl||!myUserId||!myAvatarUrl.includes("/storage/v1/object/public/avatars/"))return;const head=await fetch(myAvatarUrl,{method:"HEAD",cache:"no-store"});const len=Number(head.headers.get("content-length")||0);if(!head.ok||!len||len<200*1024)return;const blob=await(await fetch(myAvatarUrl,{cache:"no-store"})).blob();const photo=await avatarSquareJpeg(blob,256);const path=`${myUserId}/avatar.jpg`;const{error:upErr}=await sb.storage.from("avatars").upload(path,photo,{upsert:true,contentType:"image/jpeg"});if(upErr){console.warn("avatar shrink upload failed",upErr);return}const{data:urlData}=sb.storage.from("avatars").getPublicUrl(path);const url=`${urlData.publicUrl}?v=${Date.now()}`;const{error:saveErr}=await sb.from("profiles").update({avatar_url:url}).eq("id",myUserId);if(saveErr){console.warn("avatar shrink save failed",saveErr);return}myAvatarUrl=url;if(typeof renderProfileMenu==="function")renderProfileMenu();try{chatUpdateMyAvatarButton()}catch(e){}}catch(e){console.warn("avatar shrink skipped",e)}}$("pmPhotoBtn").addEventListener("click",e=>{e.stopPropagation();$("pmPhotoInput").click()});$("pmPhotoInput").addEventListener("change",async()=>{const file=$("pmPhotoInput").files[0];$("pmPhotoInput").value="";if(!file)return;const msg=$("pmPhotoMsg");if(!file.type.startsWith("image/")){msg.textContent="Please choose an image file.";msg.className="msg error";return}if(file.size>15*1024*1024){msg.textContent="That image is larger than 15MB — please pick a smaller one.";msg.className="msg error";return}msg.textContent="Uploading…";msg.className="msg";let photo;try{photo=await avatarSquareJpeg(file,256)}catch(e){msg.textContent="Could not read that image — try a JPG or PNG.";msg.className="msg error";return}const path=`${myUserId}/avatar.jpg`;const{error:uploadErr}=await sb.storage.from("avatars").upload(path,photo,{upsert:true,contentType:"image/jpeg"});if(uploadErr){msg.textContent=uploadErr.message||"Upload failed.";msg.className="msg error";return}const{data:urlData}=sb.storage.from("avatars").getPublicUrl(path);const bustedUrl=`${urlData.publicUrl}?v=${Date.now()}`;const{error:updateErr}=await sb.from("profiles").update({avatar_url:bustedUrl}).eq("id",myUserId);if(updateErr){msg.textContent=updateErr.message||"Failed to save.";msg.className="msg error";return}myAvatarUrl=bustedUrl;$("chatMyAvatarPreview").innerHTML=`<img src="${escapeHtml(bustedUrl)}" alt="">`;chatUpdateMyAvatarButton();renderProfileMenu();renderChatConvList($("chatSearchInput").value);msg.textContent="Updated!";msg.className="msg ok"});function chatStatusUntilFrom(choice){const now=new Date;if(choice==="30m")return new Date(now.getTime()+30*6e4);if(choice==="1h")return new Date(now.getTime()+36e5);if(choice==="4h")return new Date(now.getTime()+4*36e5);if(choice==="today"){const d=new Date(now);d.setHours(23,59,59,0);return d}if(choice==="week"){const d=new Date(now);d.setDate(d.getDate()+(7-d.getDay())%7);d.setHours(23,59,59,0);return d}return null}async function chatSaveMyStatus(){const msg=$("chatMyProfileMsg");const choice=$("chatMyStatusSelect").value;const manual=choice==="auto"?null:choice;const text=$("chatStatusText").value.trim().slice(0,80)||null;const emoji=$("chatStatusEmoji").value.trim().slice(0,8)||null;const untilSel=$("chatStatusUntil").value;let untilIso=myStatusUntil;if(untilSel)untilIso=chatStatusUntilFrom(untilSel).toISOString();if(!manual&&!text&&!emoji)untilIso=null;myChatStatusManual=manual;myStatusText=text;myStatusEmoji=emoji;myStatusUntil=untilIso;myChatStatus=manual==="invisible"?"offline":manual||currentAutoStatus();const patch={chat_status_manual:manual,chat_status:myChatStatus,status_text:text,status_emoji:emoji,status_until:untilIso};if(manual!=="invisible")patch.last_seen_at=new Date().toISOString();const{error}=await sb.from("profiles").update(patch).eq("id",myUserId);if(error){msg.textContent=error.message;msg.className="msg error";return}msg.textContent=!manual&&!text&&!emoji?"Your status is automatic.":"Status saved"+(untilIso?" — it clears "+chatFmtUntil(untilIso)+".":".");msg.className="msg ok";$("chatStatusUntil").value="";chatUpdateMyAvatarButton();renderChatConvList($("chatSearchInput")?$("chatSearchInput").value:"")}$("chatMyStatusSelect").addEventListener("change",chatSaveMyStatus);$("chatStatusSaveBtn").addEventListener("click",chatSaveMyStatus);$("chatStatusClearBtn").addEventListener("click",()=>{$("chatMyStatusSelect").value="auto";$("chatStatusText").value="";$("chatStatusEmoji").value="";$("chatStatusUntil").value="";myStatusUntil=null;chatSaveMyStatus()});$("chatStatusPresets").addEventListener("click",e=>{const b=e.target.closest("button[data-t]");if(!b)return;$("chatStatusEmoji").value=b.dataset.e||"";$("chatStatusText").value=b.dataset.t||"";$("chatStatusText").focus()});let chatNewGroupSelected=new Set;function chatRenderGroupPicker(container,directory,selected,cap,excludeIds,onToggle){const search=($("chatNewGroupSearch")&&container===$("chatNewGroupPicker")?$("chatNewGroupSearch").value:$("chatAddMemberSearch")?$("chatAddMemberSearch").value:"").trim().toLowerCase();const list=directory.filter(p=>!excludeIds.has(p.id)&&(!search||(p.full_name||p.email||"").toLowerCase().includes(search)));container.innerHTML=list.map(p=>`
    <label class="chat-picker-checkbox-row">
      <input type="checkbox" data-id="${p.id}" ${selected.has(p.id)?"checked":""}>
      ${chatAvatarHtml(p.id,p.full_name||p.email,26,false)}
      <span>${escapeHtml(p.full_name||p.email)}${p.role?` <span style="color:var(--text-dim);">· ${escapeHtml(roleDisplayLabel(p.role))}</span>`:""}</span>
    </label>
  `).join("")||'<div class="empty" style="padding:8px;">No one matches.</div>';container.querySelectorAll("input[type=checkbox]").forEach(cb=>{cb.addEventListener("change",()=>{if(cb.checked&&selected.size>=cap){cb.checked=false;alert(`A group can have at most ${cap+excludeIds.size} members total.`);return}if(cb.checked)selected.add(cb.dataset.id);else selected.delete(cb.dataset.id);onToggle()})})}function chatIsSenior(){return myIsOwner||myRole==="admin"||["lead","manager","head"].includes(myGrade)}$("chatNewGroupBtn").addEventListener("click",()=>{chatNewGroupSelected=new Set;$("chatNewGroupName").value="";$("chatNewGroupSearch").value="";$("chatNewGroupMsg").textContent="";$("chatNewGroupCount").textContent="(1/10)";chatRenderGroupPicker($("chatNewGroupPicker"),chatGroupDirectory,chatNewGroupSelected,9,new Set,()=>{$("chatNewGroupCount").textContent=`(${chatNewGroupSelected.size+1}/10)`});$("chatListView").style.display="none";$("chatNewGroupView").style.display="flex"});$("chatNewGroupBackBtn").addEventListener("click",()=>{$("chatNewGroupView").style.display="none";$("chatListView").style.display="flex"});$("chatNewGroupSearch").addEventListener("input",()=>{chatRenderGroupPicker($("chatNewGroupPicker"),chatGroupDirectory,chatNewGroupSelected,9,new Set,()=>{$("chatNewGroupCount").textContent=`(${chatNewGroupSelected.size+1}/10)`})});let chatNewClientChatDirectory=[];function chatRenderNewClientChatPicker(){const search=($("chatNewClientChatSearch").value||"").trim().toLowerCase();const list=chatNewClientChatDirectory.filter(c=>{if(!search)return true;return(c.full_name||"").toLowerCase().includes(search)||(c.company_name||"").toLowerCase().includes(search)});$("chatNewClientChatPicker").innerHTML=list.map(c=>`
    <div class="chat-conv-item" data-client-id="${c.id}" style="cursor:pointer;">
      ${chatAvatarHtml(c.id,c.full_name||c.company_name||"Client",32,false)}
      <div class="chat-conv-item-text">
        <div class="chat-conv-item-name">${escapeHtml(c.full_name||"Client")}</div>
        <div class="chat-conv-item-preview">${escapeHtml(c.company_name||c.email||"")}</div>
      </div>
    </div>
  `).join("")||'<div class="empty" style="padding:8px;">No clients match.</div>';$("chatNewClientChatPicker").querySelectorAll("[data-client-id]").forEach(row=>{row.addEventListener("click",()=>chatStartOrOpenClientChat(row.dataset.clientId))})}async function chatStartOrOpenClientChat(clientId){const msg=$("chatNewClientChatMsg");msg.textContent="";if(clientChatThreadsDirectory.some(t=>t.client_id===clientId)){$("chatNewClientChatView").style.display="none";openChatThread("client:"+clientId);return}const{error}=await sb.from("client_chat_threads").insert({client_id:clientId,status:"routed",assigned_staff_id:myUserId,queue_key:null});if(error){msg.textContent=error.message;msg.className="msg error";return}await loadClientChatOverview();$("chatNewClientChatView").style.display="none";openChatThread("client:"+clientId)}$("chatNewClientChatBtn").addEventListener("click",async()=>{$("chatNewClientChatSearch").value="";$("chatNewClientChatMsg").textContent="";$("chatNewClientChatPicker").innerHTML='<div class="empty" style="padding:8px;">Loading…</div>';$("chatListView").style.display="none";$("chatNewClientChatView").style.display="flex";const{data,error}=await sb.from("client_accounts").select("id, full_name, company_name, email").eq("is_active",true).order("full_name");if(error){$("chatNewClientChatMsg").textContent=error.message;$("chatNewClientChatMsg").className="msg error";return}chatNewClientChatDirectory=data||[];chatRenderNewClientChatPicker()});$("chatNewClientChatBackBtn").addEventListener("click",()=>{$("chatNewClientChatView").style.display="none";$("chatListView").style.display="flex"});$("chatNewClientChatSearch").addEventListener("input",chatRenderNewClientChatPicker);$("chatCreateGroupBtn").addEventListener("click",async()=>{const name=$("chatNewGroupName").value.trim();const msg=$("chatNewGroupMsg");if(!chatIsSenior()){msg.textContent="Only seniors (team leads, managers, heads, admins) can create a group.";msg.className="msg error";return}if(!name){msg.textContent="Give the group a name.";msg.className="msg error";return}if(chatNewGroupSelected.size===0){msg.textContent="Add at least one other person.";msg.className="msg error";return}const btn=$("chatCreateGroupBtn");btn.disabled=true;const{data:group,error}=await sb.from("chat_groups").insert({name,created_by:myUserId}).select().single();if(error){msg.textContent=error.message;msg.className="msg error";btn.disabled=false;return}const memberRows=[myUserId,...chatNewGroupSelected].map(uid=>({group_id:group.id,user_id:uid,added_by:myUserId}));const{error:memberErr}=await sb.from("chat_group_members").insert(memberRows);btn.disabled=false;if(memberErr){msg.textContent=memberErr.message;msg.className="msg error";return}logActivity("chat.group_create","chat_group",group.id,`Created group "${name}" with ${chatNewGroupSelected.size+1} members`);await loadChatGroups();$("chatNewGroupView").style.display="none";openChatThread("group:"+group.id)});async function openChatGroupInfo(groupId){chatActiveGroupInfoId=groupId;const group=chatGroups[groupId];if(!group)return;$("chatThreadView").style.display="none";$("chatGroupInfoView").style.display="flex";$("chatGroupInfoMsg").textContent="";chatRenderGroupInfo()}function chatRenderGroupInfo(){const group=chatGroups[chatActiveGroupInfoId];if(!group)return;const isModerator=group.created_by===myUserId;$("chatGroupInfoName").textContent=`${group.name} · ${group.members.length}/10 members`;$("chatGroupInfoMembers").innerHTML=group.members.map(m=>{const name=chatContactName(m.user_id);return`
      <div class="chat-group-member-row">
        <div class="chat-group-member-avatar">${chatContactAvatar(m.user_id)?`<img src="${escapeHtml(chatContactAvatar(m.user_id))}" alt="">`:escapeHtml(name.charAt(0).toUpperCase())}</div>
        <div class="chat-group-member-name">${escapeHtml(name)}${m.user_id===myUserId?" (you)":""}</div>
        ${m.user_id===group.created_by?'<span class="chat-group-member-mod-tag">Moderator</span>':""}
        ${isModerator&&m.user_id!==group.created_by?`<button class="chat-group-member-remove" data-user="${m.user_id}" title="Remove from group">&times;</button>`:""}
      </div>
    `}).join("");$("chatGroupInfoMembers").querySelectorAll(".chat-group-member-remove").forEach(btn=>{btn.addEventListener("click",()=>chatRemoveGroupMember(group.id,btn.dataset.user))});$("chatGroupInfoModeratorActions").style.display=isModerator?"block":"none";$("chatLeaveGroupBtn").style.display=isModerator?"none":"inline-block"}$("chatGroupInfoBackBtn").addEventListener("click",()=>{$("chatGroupInfoView").style.display="none";$("chatThreadView").style.display="flex"});async function chatRemoveGroupMember(groupId,userId){if(!confirm("Remove this person from the group?"))return;const{error}=await sb.from("chat_group_members").delete().eq("group_id",groupId).eq("user_id",userId);if(error){alert(error.message);return}logActivity("chat.group_remove_member","chat_group",groupId,`Removed a member from "${chatGroups[groupId].name}"`);await loadChatGroups();chatRenderGroupInfo()}$("chatLeaveGroupBtn").addEventListener("click",async()=>{const group=chatGroups[chatActiveGroupInfoId];if(!group)return;if(!confirm(`Leave "${group.name}"?`))return;const{error}=await sb.from("chat_group_members").delete().eq("group_id",group.id).eq("user_id",myUserId);if(error){alert(error.message);return}await loadChatGroups();delete chatConversations["group:"+group.id];$("chatGroupInfoView").style.display="none";$("chatListView").style.display="flex";chatActiveConv=null;renderChatConvList($("chatSearchInput").value)});$("chatDeleteGroupBtn").addEventListener("click",async()=>{const group=chatGroups[chatActiveGroupInfoId];if(!group)return;if(!confirm(`Delete "${group.name}" for everyone? This can't be undone.`))return;const{error}=await sb.from("chat_groups").delete().eq("id",group.id);if(error){alert(error.message);return}logActivity("chat.group_delete","chat_group",group.id,`Deleted group "${group.name}"`);delete chatGroups[group.id];delete chatConversations["group:"+group.id];$("chatGroupInfoView").style.display="none";$("chatListView").style.display="flex";chatActiveConv=null;renderChatConvList($("chatSearchInput").value)});let chatAddMemberSelected=new Set;$("chatGroupAddMemberBtn").addEventListener("click",()=>{const group=chatGroups[chatActiveGroupInfoId];if(!group)return;chatAddMemberSelected=new Set;$("chatAddMemberSearch").value="";$("chatAddMemberMsg").textContent="";const existingIds=new Set(group.members.map(m=>m.user_id));const remaining=10-group.members.length;$("chatAddMemberCount").textContent=`(0/${remaining} slots left)`;chatRenderGroupPicker($("chatAddMemberPicker"),chatGroupDirectory,chatAddMemberSelected,remaining,existingIds,()=>{$("chatAddMemberCount").textContent=`(${chatAddMemberSelected.size}/${remaining} slots left)`});$("chatGroupInfoView").style.display="none";$("chatAddMemberView").style.display="flex"});$("chatAddMemberBackBtn").addEventListener("click",()=>{$("chatAddMemberView").style.display="none";$("chatGroupInfoView").style.display="flex"});$("chatAddMemberSearch").addEventListener("input",()=>{const group=chatGroups[chatActiveGroupInfoId];if(!group)return;const existingIds=new Set(group.members.map(m=>m.user_id));const remaining=10-group.members.length;chatRenderGroupPicker($("chatAddMemberPicker"),chatGroupDirectory,chatAddMemberSelected,remaining,existingIds,()=>{$("chatAddMemberCount").textContent=`(${chatAddMemberSelected.size}/${remaining} slots left)`})});$("chatConfirmAddMemberBtn").addEventListener("click",async()=>{const group=chatGroups[chatActiveGroupInfoId];const msg=$("chatAddMemberMsg");if(!group||chatAddMemberSelected.size===0){msg.textContent="Pick at least one person to add.";msg.className="msg error";return}const rows=[...chatAddMemberSelected].map(uid=>({group_id:group.id,user_id:uid,added_by:myUserId}));const{error}=await sb.from("chat_group_members").insert(rows);if(error){msg.textContent=error.message;msg.className="msg error";return}logActivity("chat.group_add_member","chat_group",group.id,`Added ${chatAddMemberSelected.size} member(s) to "${group.name}"`);await loadChatGroups();$("chatAddMemberView").style.display="none";$("chatGroupInfoView").style.display="flex";chatRenderGroupInfo()});let chatMediaRecorder=null;let chatRecordingChunks=[];let chatRecordingTimerHandle=null;let chatRecordingStream=null;function chatUpdateRecordingTimer(startedAt){const secs=Math.floor((Date.now()-startedAt)/1e3);$("chatRecordingTimer").textContent=`${Math.floor(secs/60)}:${String(secs%60).padStart(2,"0")}`}async function chatStartRecording(){if(!navigator.mediaDevices||!window.MediaRecorder){alert("Voice messages aren’t supported in this browser.");return}try{chatRecordingStream=await navigator.mediaDevices.getUserMedia({audio:true})}catch(e){alert("Microphone access was denied or isn’t available.");return}chatRecordingChunks=[];chatMediaRecorder=new MediaRecorder(chatRecordingStream);chatMediaRecorder.ondataavailable=e=>{if(e.data.size>0)chatRecordingChunks.push(e.data)};chatMediaRecorder.start();const startedAt=Date.now();$("chatInputRow").style.display="none";$("chatRecordingBar").style.display="flex";chatUpdateRecordingTimer(startedAt);chatRecordingTimerHandle=setInterval(()=>chatUpdateRecordingTimer(startedAt),1e3)}function chatStopRecording(discard){if(chatRecordingTimerHandle){clearInterval(chatRecordingTimerHandle);chatRecordingTimerHandle=null}$("chatRecordingBar").style.display="none";if(!chatMediaRecorder||chatMediaRecorder.state==="inactive"){if(chatRecordingStream){chatRecordingStream.getTracks().forEach(t=>t.stop());chatRecordingStream=null}$("chatInputRow").style.display="flex";return}chatMediaRecorder.onstop=()=>{if(chatRecordingStream){chatRecordingStream.getTracks().forEach(t=>t.stop());chatRecordingStream=null}$("chatInputRow").style.display="flex";if(discard||chatRecordingChunks.length===0)return;const blob=new Blob(chatRecordingChunks,{type:"audio/webm"});const file=new File([blob],`voice-message-${Date.now()}.webm`,{type:"audio/webm"});chatClearPendingFile();chatPendingFile={file,previewUrl:URL.createObjectURL(blob),isVoice:true};const preview=$("chatAttachPreview");preview.style.display="flex";preview.innerHTML=`
      <audio controls src="${chatPendingFile.previewUrl}" style="height:32px;"></audio>
      <span class="chat-attach-remove" title="Remove">&times;</span>
    `;preview.querySelector(".chat-attach-remove").addEventListener("click",chatClearPendingFile)};chatMediaRecorder.stop()}$("chatMicBtn").addEventListener("click",chatStartRecording);$("chatRecordingStopBtn").addEventListener("click",()=>chatStopRecording(false));$("chatRecordingCancelBtn").addEventListener("click",()=>chatStopRecording(true));let lastTeamRows=[];let teamFlagsByAgent={};const STALE_PAUSE_MS=60*60*1e3;const LIVE_ENTRY_STALE_MS=15*60*1e3;function liveEntryIsStale(e){if(!e||e.end_time||!e.last_heartbeat_at)return false;return Date.now()-new Date(e.last_heartbeat_at).getTime()>LIVE_ENTRY_STALE_MS}function liveEntryEndMs(e){if(e.end_time)return new Date(e.end_time).getTime();if(liveEntryIsStale(e))return new Date(e.last_heartbeat_at).getTime();return Date.now()}let activeFlagPopover=null;function closeFlagPopover(){if(activeFlagPopover){activeFlagPopover.remove();activeFlagPopover=null}document.removeEventListener("click",closeFlagPopoverOnOutsideClick,true);window.removeEventListener("scroll",closeFlagPopover,true)}function closeFlagPopoverOnOutsideClick(e){if(activeFlagPopover&&!activeFlagPopover.contains(e.target))closeFlagPopover()}function showFlagPopover(anchorEl,agentName,flags){const reopeningSame=activeFlagPopover&&activeFlagPopover.dataset.anchorFor===agentName;closeFlagPopover();if(reopeningSame)return;const rect=anchorEl.getBoundingClientRect();const pop=document.createElement("div");pop.className="flag-popover";pop.dataset.anchorFor=agentName;const left=Math.min(rect.left,window.innerWidth-320-12);pop.style.cssText=`position:fixed; top:${rect.bottom+6}px; left:${Math.max(12,left)}px; z-index:1000;`;pop.innerHTML=`
    <div class="flag-popover-header">
      <span>${escapeHtml(agentName)} — today's flags</span>
      <button type="button" class="flag-popover-close" aria-label="Close">&times;</button>
    </div>
    <div class="flag-popover-body">
      ${flags.map(f=>`
        <div class="flag-popover-item">
          <div class="flag-popover-type">${SUSPICION_TYPE_META[f.type].label}</div>
          <div class="flag-popover-detail">${f.detail}</div>
        </div>
      `).join("")||'<div class="flag-popover-detail">No flags.</div>'}
    </div>
  `;document.body.appendChild(pop);activeFlagPopover=pop;pop.querySelector(".flag-popover-close").addEventListener("click",closeFlagPopover);pop.addEventListener("click",e=>e.stopPropagation());setTimeout(()=>{document.addEventListener("click",closeFlagPopoverOnOutsideClick,true);window.addEventListener("scroll",closeFlagPopover,true)},0)}let globalDisplayTzMode="America/Chicago";function resolveDashTz(agentOwnTz){return globalDisplayTzMode==="agent"?agentOwnTz:globalDisplayTzMode}function nonAgentDisplayTz(){return globalDisplayTzMode==="agent"?void 0:globalDisplayTzMode}(function installGlobalTzDefault(){const DATE_KEYS=["year","month","day","weekday","dateStyle","era"];const TIME_KEYS=["hour","minute","second","timeStyle","dayPeriod","fractionalSecondDigits"];function zoneFor(d,opts,kind){if(opts&&opts.timeZone)return opts;let tz;try{tz=nonAgentDisplayTz()}catch(e){return opts}if(!tz||!(d instanceof Date)||isNaN(d))return opts;const o=opts||{};const dateOnly=kind==="date"||kind==="any"&&opts&&TIME_KEYS.every(k=>o[k]===void 0)&&DATE_KEYS.some(k=>o[k]!==void 0);if(dateOnly){if(d.getHours()===0&&d.getMinutes()===0&&d.getSeconds()===0&&d.getMilliseconds()===0)return opts;if(d.getUTCHours()===0&&d.getUTCMinutes()===0&&d.getUTCSeconds()===0&&d.getUTCMilliseconds()===0)return Object.assign({},o,{timeZone:"UTC"})}return Object.assign({},o,{timeZone:tz})}[["toLocaleString","any"],["toLocaleDateString","date"],["toLocaleTimeString","time"]].forEach(([m,kind])=>{const orig=Date.prototype[m];Date.prototype[m]=function(locales,opts){return orig.call(this,locales,zoneFor(this,opts,kind))}})})();document.querySelectorAll(".global-tz-btn").forEach(btn=>{btn.addEventListener("click",()=>{const oldToday=todayDateInputValue();globalDisplayTzMode=btn.dataset.tz;document.querySelectorAll(".global-tz-btn").forEach(b=>b.classList.toggle("active",b===btn));const newToday=todayDateInputValue();if(newToday!==oldToday){[["tsFromDateInput","tsToDateInput"],["callActFromInput","callActToInput"],["leadsFromDateInput","leadsToDateInput"]].forEach(([fromId,toId])=>{const fromEl=$(fromId),toEl=$(toId);if(fromEl&&toEl&&fromEl.value===oldToday&&toEl.value===oldToday){fromEl.value=newToday;toEl.value=newToday}})}const activePanel=document.querySelector(".tab-panel.active");if(!activePanel)return;if(activePanel.id==="tab-time_management"&&tmActiveSection==="staff")loadTeam();else if(activePanel.id==="tab-time_management"&&tmActiveSection==="timesheets")loadTimesheets();else if(activePanel.id==="tab-call_activity")loadCallActivity();else if(activePanel.id==="tab-leads")loadLeads();else if(activePanel.id==="tab-client_submissions")loadClientSubmissions()})});const TEAM_STATIC_TTL_MS=5*60*1e3;let teamStaticCache=null;function invalidateTeamStatic(){teamStaticCache=null}function loadTeamStatic(){if(teamStaticCache&&Date.now()-teamStaticCache.at<TEAM_STATIC_TTL_MS)return teamStaticCache.promise;const promise=Promise.all([sb.from("app_settings").select("value").eq("key","min_extension_version").maybeSingle(),fetchAllRows((from,to)=>sb.from("schedules").select("user_id, timezone").order("user_id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("tasks").select("id").eq("is_dialer_task",true).order("id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("dialer_domains").select("domain").order("id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("tasks").select("id, category_id, task_categories(idle_policy, abuse_check_ratio, abuse_check_min_hours), task_domains(domain)").not("category_id","is",null).order("id",{ascending:true}).range(from,to))]).then(([{data:minVersionRow},scheduleRows,dialerTaskRows,dialerDomainRows,categorizedTaskRows])=>({minVersionRow,scheduleRows,dialerTaskRows,dialerDomainRows,categorizedTaskRows}));teamStaticCache={at:Date.now(),promise};promise.catch(()=>{if(teamStaticCache&&teamStaticCache.promise===promise)teamStaticCache=null});return promise}async function loadTeam(){const TEAM_QUERY_LOOKBACK_MS=30*3600*1e3;const start=new Date(Date.now()-TEAM_QUERY_LOOKBACK_MS).toISOString();const teamStatic=loadTeamStatic();const[agents,{minVersionRow,scheduleRows,dialerTaskRows,dialerDomainRows,categorizedTaskRows}]=await Promise.all([fetchAllRows((from,to)=>sb.from("profiles").select("id, full_name, email, role, extension_version, extension_version_at, is_active, department, title, grade, american_name").order("full_name").range(from,to)),teamStatic]);const minVersion=minVersionRow&&minVersionRow.value&&minVersionRow.value!=="0.0.0"?minVersionRow.value:null;const agentTz={};(scheduleRows||[]).forEach(s=>{if(s.timezone)agentTz[s.user_id]=s.timezone});const overlapEntriesP=fetchAllRows((from,to)=>sb.from("time_entries").select("id, user_id, start_time, end_time, task_id, idle_seconds, break_seconds, is_manual, last_heartbeat_at").or(`end_time.is.null,end_time.gte.${start}`).order("id",{ascending:true}).range(from,to));const openBreaksP=sb.from("breaks").select("user_id, start_time, time_entry_id").is("end_time",null);const openIdlesP=sb.from("idle_periods").select("user_id, start_time, time_entry_id").is("end_time",null);const recentClosedP=sb.from("time_entries").select("user_id, end_time, start_time").not("end_time","is",null).order("end_time",{ascending:false}).limit(2e3);const[todaySamples,todayIdlePeriods,todayUrlVisits,todayShots,todayBreaks,overlapEntries,{data:openBreaks},{data:openIdles},{data:recentClosed}]=await Promise.all([fetchAllRows((from,to)=>sb.from("activity_samples").select("time_entry_id, user_id, sampled_at, mouse_events, keyboard_events").gte("sampled_at",start).order("id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("idle_periods").select("time_entry_id, start_time, end_time").gte("start_time",start).order("id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("url_visits").select("time_entry_id, domain, start_time, end_time").gte("start_time",start).order("id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("screenshots").select("time_entry_id, taken_at").gte("taken_at",start).order("id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("breaks").select("time_entry_id, start_time, end_time").gte("start_time",start).order("id",{ascending:true}).range(from,to)),overlapEntriesP,openBreaksP,openIdlesP,recentClosedP]);const onBreakByEntry={};(openBreaks||[]).forEach(b=>{if(b.time_entry_id)onBreakByEntry[b.time_entry_id]=b.start_time});const idleByEntry={};(openIdles||[]).forEach(i=>{if(i.time_entry_id)idleByEntry[i.time_entry_id]=i.start_time});const shotCountByEntry={};(todayShots||[]).forEach(s=>{if(s.time_entry_id)shotCountByEntry[s.time_entry_id]=(shotCountByEntry[s.time_entry_id]||0)+1});const lastResumeByEntry={};const _trackResume=r=>{if(!r.time_entry_id||!r.end_time)return;const ms=new Date(r.end_time).getTime();if(!(r.time_entry_id in lastResumeByEntry)||ms>lastResumeByEntry[r.time_entry_id]){lastResumeByEntry[r.time_entry_id]=ms}};(todayIdlePeriods||[]).forEach(_trackResume);(todayBreaks||[]).forEach(_trackResume);const teamIdlePeriodsByEntry=groupByEntry(todayIdlePeriods,"start_time");const teamBreaksByEntry=groupByEntry(todayBreaks,"start_time");const suspicionCtx={samplesByEntry:groupSamplesByEntry(todaySamples),idlePeriodsByEntry:teamIdlePeriodsByEntry,urlVisitsByEntry:groupByEntry(todayUrlVisits,"start_time"),dialerTaskIds:new Set((dialerTaskRows||[]).map(t=>t.id)),dialerDomains:(dialerDomainRows||[]).map(d=>d.domain),taskCategoryMap:buildTaskCategoryMap(categorizedTaskRows)};const lastEndMap={};(recentClosed||[]).forEach(e=>{if(!(e.user_id in lastEndMap))lastEndMap[e.user_id]=e.end_time});const body=$("teamBody");lastTeamRows=[];teamFlagsByAgent={};const visibleAgents=(agents||[]).filter(a=>!PROTECTED_ROLES.has(a.role)||myIsOwner&&a.role==="admin"||myIsOwner&&a.id===myUserId).sort((a,b)=>{const aAdmin=a.role==="admin"?0:1,bAdmin=b.role==="admin"?0:1;if(aAdmin!==bAdmin)return aAdmin-bAdmin;const aLabel=roleDisplayLabel(a.role),bLabel=roleDisplayLabel(b.role);if(aLabel!==bLabel)return aLabel.localeCompare(bLabel);const aName=a.full_name||"",bName=b.full_name||"";if(aName!==bName)return aName.localeCompare(bName);return String(a.id).localeCompare(String(b.id))});const existingRowsById=new Map;body.querySelectorAll("tr.agent-row[data-agent-id]").forEach(tr=>existingRowsById.set(tr.dataset.agentId,tr));const desiredNodes=[];let lastGroupLabel=null;visibleAgents.forEach(agent=>{const groupLabel=roleDisplayLabel(agent.role);if(groupLabel!==lastGroupLabel){lastGroupLabel=groupLabel;const groupTr=document.createElement("tr");groupTr.className="team-group-header";groupTr.innerHTML=`<td colspan="8">${groupLabel}</td>`;desiredNodes.push(groupTr)}const myEntriesRaw=(overlapEntries||[]).filter(e=>e.user_id===agent.id);const{startMs:agentDayStartMs}=dayBoundsMsInZone(resolveDashTz(agentTz[agent.id]));const myEntries=myEntriesRaw.filter(e=>{const rawEnd=liveEntryEndMs(e);return rawEnd>agentDayStartMs});const{focusSec,idleSec,breakSec}=computeClippedTotals(myEntries,teamIdlePeriodsByEntry,teamBreaksByEntry,agentDayStartMs);const totalSec=focusSec+idleSec+breakSec;const liveEntry=myEntriesRaw.find(e=>!e.end_time);const myFlags=computeSuspicionFlags(myEntries,suspicionCtx).concat(computeChoppedSessionFlags(myEntries,agentTz)).concat(computeSparseScreenshotFlags(myEntries,shotCountByEntry)).concat(computeMergedSessionFlags(myEntries,agentTz));teamFlagsByAgent[agent.id]=myFlags;const flagBadge=myFlags.length?` <button type="button" class="flag-badge" data-agent-id="${agent.id}">&#9888; ${myFlags.length}</button>`:"";let statusLabel,dotClass,statusSince;const breakSince=liveEntry&&onBreakByEntry[liveEntry.id];const idleSince=liveEntry&&idleByEntry[liveEntry.id];const staleSince=since=>since&&Date.now()-new Date(since).getTime()>STALE_PAUSE_MS;if(liveEntry&&breakSince&&!staleSince(breakSince)){statusLabel="on break";dotClass="status-break";statusSince=breakSince}else if(liveEntry&&idleSince&&!staleSince(idleSince)){statusLabel="idle";dotClass="status-idle";statusSince=idleSince}else if(liveEntry&&(breakSince||idleSince)){statusLabel="not tracking";dotClass="off";statusSince=null}else if(liveEntry&&liveEntryIsStale(liveEntry)){statusLabel="disconnected";dotClass="off";statusSince=liveEntry.last_heartbeat_at}else if(liveEntry){statusLabel="productive";dotClass="on";const resumeMs=lastResumeByEntry[liveEntry.id];const startMs=new Date(liveEntry.start_time).getTime();statusSince=new Date(resumeMs&&resumeMs>startMs?resumeMs:startMs).toISOString()}else{statusLabel="not tracking";dotClass="off";statusSince=null}const statusMinLabel=statusSince?fmtShort(Math.max(0,(Date.now()-new Date(statusSince).getTime())/1e3)):"—";const statusSinceMs=statusSince?new Date(statusSince).getTime():null;const tz=resolveDashTz(agentTz[agent.id]);const statusStartLabel=statusSince?new Date(statusSince).toLocaleTimeString([],{hour:"2-digit",minute:"2-digit",...tz?{timeZone:tz}:{}}):null;const statusTimerClass=dotClass==="on"?"pct-focus":dotClass==="status-idle"?"text-idle":dotClass==="status-break"?"text-break":"";const lastEnd=lastEndMap[agent.id];const lastEndLabel=lastEnd?new Date(lastEnd).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...tz?{timeZone:tz}:{}}):"—";const firstStartTodayMs=myEntries.reduce((min,e)=>{const t=new Date(e.start_time).getTime();return min===null||t<min?t:min},null);const loginStartLabel=firstStartTodayMs!==null?new Date(firstStartTodayMs).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...tz?{timeZone:tz}:{}}):"—";const version=agent.extension_version;const isOutdatedVersion=!!(version&&minVersion&&compareVersionStrings(version,minVersion)<0);const versionLabel=version||"—";const versionTitle=version?`Reported ${agent.extension_version_at?new Date(agent.extension_version_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...tz?{timeZone:tz}:{}}):"time unknown"}`:"Never reported — old build, or never logged in since this was added";const versionCell=isOutdatedVersion?`<span class="text-idle" title="${versionTitle} — below required v${minVersion}">${versionLabel} &#9888;</span>`:`<span style="${version?"":"color:var(--text-dim);"}" title="${versionTitle}">${versionLabel}</span>`;lastTeamRows.push({name:agent.full_name||agent.email,status:`${statusLabel} (${statusMinLabel})`,focus:fmtShort(focusSec),idle:fmtShort(idleSec),brk:fmtShort(breakSec),total:fmtHM(totalSec),loginStart:loginStartLabel,type:roleDisplayLabel(agent.role||"user"),lastEnd:lastEndLabel});let tr=existingRowsById.get(agent.id);const isNewRow=!tr;if(isNewRow){tr=document.createElement("tr");tr.className="agent-row";tr.dataset.agentId=agent.id}const agentTypeLabel=roleDisplayLabel(agent.role||"user");tr.innerHTML=`
      <td>${escapeHtml(agent.full_name||agent.email)}${flagBadge}<div style="font-size:10px; color:var(--text-dim); margin-top:2px;">${agentTypeLabel}</div></td>
      <td><span class="dot ${dotClass}"></span>${statusLabel}${statusSinceMs?` <span class="live-status-timer ${statusTimerClass}" data-since="${statusSinceMs}" style="opacity:0.85;">(${fmtDurationLive((Date.now()-statusSinceMs)/1e3)})</span><span style="opacity:0.55; font-size:11px; margin-left:4px;">since ${statusStartLabel}</span>`:""}</td>
      <td class="mono">${fmtShort(focusSec)}</td>
      <td class="mono text-idle">${fmtShort(idleSec)}</td>
      <td class="mono text-break">${fmtShort(breakSec)}</td>
      <td class="mono">${fmtHM(totalSec)}</td>
      <td class="mono">${loginStartLabel}</td>
      <td class="mono">${lastEndLabel}</td>
    `;tr._agent=agent;const canJumpToTimesheets=()=>myIsOwner||canViewTimeManagementSection("timesheets");const clickable=canJumpToTimesheets();tr.classList.toggle("clickable",clickable);tr.style.cursor=clickable?"pointer":"default";if(isNewRow){tr.addEventListener("click",()=>{if(!canJumpToTimesheets())return;$("tsFromDateInput").value=todayDateInputValue();$("tsToDateInput").value=todayDateInputValue();$("tsAgentFilter").value=tr._agent.id;document.querySelector('.tab-btn[data-tab="time_management"]').click();activateTimeManagementSection("timesheets")})}const flagBtn=tr.querySelector(".flag-badge");if(flagBtn){flagBtn.addEventListener("click",e=>{e.stopPropagation();showFlagPopover(flagBtn,agent.full_name||agent.email,teamFlagsByAgent[agent.id]||[])})}desiredNodes.push(tr)});desiredNodes.forEach((node,i)=>{const current=body.children[i];if(current!==node)body.insertBefore(node,current||null)});while(body.children.length>desiredNodes.length)body.lastChild.remove();if(visibleAgents.length===0){body.innerHTML='<tr><td colspan="8" class="empty">No user accounts yet. Add users in Supabase Auth, they will appear here.</td></tr>'}}$("staffExportCsvBtn").addEventListener("click",()=>{const headers=["User","Type","Status","Focus today","Idle today","Break today","Total time","Login / start time","Last end tracking"];const rows=lastTeamRows.map(r=>[r.name,r.type,r.status,r.focus,r.idle,r.brk,r.total,r.loginStart,r.lastEnd]);exportCSV(`staff-${new Date().toISOString().slice(0,10)}.csv`,headers,rows);logActivity("export.staff_csv",null,null,`Exported staff CSV (${rows.length} agents)`)});$("staffExportPdfBtn").addEventListener("click",()=>{const headers=["User","Type","Status","Focus today","Idle today","Break today","Total time","Login / start time","Last end tracking"];const rows=lastTeamRows.map(r=>[r.name,r.type,r.status,r.focus,r.idle,r.brk,r.total,r.loginStart,r.lastEnd]);exportPDF("Dashboard Report",headers,rows);logActivity("export.staff_pdf",null,null,`Exported staff PDF (${rows.length} agents)`)});async function fetchAgentEntries(agentId,sinceISO){const since=sinceISO||todayRangeISO().start;const entries=await fetchAllRows((from,to)=>sb.from("time_entries").select("id, user_id, start_time, end_time, task_id, idle_seconds, break_seconds, last_heartbeat_at").eq("user_id",agentId).or(`end_time.is.null,end_time.gte.${since}`).order("id",{ascending:true}).range(from,to));const entryIds=entries.map(e=>e.id);let idlePeriodsByEntry={},breaksByEntry={};if(entryIds.length){const[{data:idlePeriods},{data:breaksRows}]=await Promise.all([sb.from("idle_periods").select("time_entry_id, start_time, end_time").in("time_entry_id",entryIds),sb.from("breaks").select("time_entry_id, start_time, end_time").in("time_entry_id",entryIds)]);idlePeriodsByEntry=groupByEntry(idlePeriods,"start_time");breaksByEntry=groupByEntry(breaksRows,"start_time")}return{entries,idlePeriodsByEntry,breaksByEntry}}function sumFocusSeconds(entries){return entries.reduce((total,e)=>{const end=e.end_time?new Date(e.end_time).getTime():Date.now();const gross=(end-new Date(e.start_time).getTime())/1e3;const idle=e.idle_seconds||0;const brk=e.break_seconds||0;return total+Math.max(0,gross-idle-brk)},0)}function computeClippedTotals(entries,idlePeriodsByEntry,breaksByEntry,rangeStartMs){const now=Date.now();let focusSec=0,idleSec=0,breakSec=0;(entries||[]).forEach(e=>{const rawStart=new Date(e.start_time).getTime();const rawEnd=liveEntryEndMs(e);const spanStart=Math.max(rawStart,rangeStartMs);const spanEnd=Math.min(rawEnd,now);if(spanEnd<=spanStart)return;const gross=(spanEnd-spanStart)/1e3;let entryIdle=0,entryBreak=0;if(e.is_manual){entryBreak=e.break_seconds||0;entryIdle=e.idle_seconds||0}else{(idlePeriodsByEntry&&idlePeriodsByEntry[e.id]||[]).forEach(p=>{const s=Math.max(new Date(p.start_time).getTime(),spanStart);const en=Math.min(p.end_time?new Date(p.end_time).getTime():now,spanEnd);if(en>s)entryIdle+=(en-s)/1e3});(breaksByEntry&&breaksByEntry[e.id]||[]).forEach(p=>{const s=Math.max(new Date(p.start_time).getTime(),spanStart);const en=Math.min(p.end_time?new Date(p.end_time).getTime():now,spanEnd);if(en>s)entryBreak+=(en-s)/1e3})}idleSec+=entryIdle;breakSec+=entryBreak;focusSec+=Math.max(0,gross-entryIdle-entryBreak)});return{focusSec,idleSec,breakSec}}function sumFocusSecondsClipped(entries,idlePeriodsByEntry,breaksByEntry,rangeStartMs){return computeClippedTotals(entries,idlePeriodsByEntry,breaksByEntry,rangeStartMs).focusSec}function sumTrackedSecondsClipped(entries,idlePeriodsByEntry,breaksByEntry,rangeStartMs){return computeClippedTotals(entries,idlePeriodsByEntry,breaksByEntry,rangeStartMs).focusSec}const SUSPICION_ZERO_IDLE_MIN_HOURS=4;const SUSPICION_ZERO_IDLE_RATIO=.01;const SUSPICION_NO_BREAK_MIN_HOURS=5;const SUSPICION_NO_BREAK_RATIO=.01;const SUSPICION_ROBOTIC_MIN_RUN=15;const SUSPICION_ROBOTIC_TOLERANCE=1;const SUSPICION_DIALER_ABUSE_MIN_HOURS=1;const SUSPICION_DIALER_ABUSE_MAX_DOMAIN_RATIO=.2;const SUSPICION_CHOPPED_MAX_MINUTES=4;const SUSPICION_CHOPPED_MIN_COUNT=5;const SUSPICION_PINGPONG_MIN_COUNT=5;const SUSPICION_PINGPONG_MAX_AVG_SEC=120;const SUSPICION_SPARSE_SHOTS_MIN_HOURS=.75;const SUSPICION_SPARSE_SHOTS_MAX_RATIO=.5;const SUSPICION_MERGE_GAP_MAX_MIN=15;const SUSPICION_TYPE_META={zero_idle:{label:"Suspiciously perfect activity"},no_break:{label:"Marathon session (no break)"},robotic:{label:"Repetitive / robotic activity"},dialer_abuse:{label:"Dialer-task idle-exemption abuse"},category_abuse:{label:"Task-domain abuse (no evidence of expected work)"},chopped_sessions:{label:"Chopped sessions"},idle_pingpong:{label:"Idle ping-ponging"},sparse_screenshots:{label:"Few or no screenshots"},split_zero_idle:{label:"Suspiciously perfect activity (combined sessions)"},split_no_break:{label:"Marathon session (combined sessions)"}};function entryDurationSec(e){const end=e.end_time?new Date(e.end_time).getTime():Date.now();return Math.max(0,(end-new Date(e.start_time).getTime())/1e3)}function groupByEntry(rows,sortField){sortField=sortField||"sampled_at";const map={};(rows||[]).forEach(r=>{if(!r.time_entry_id)return;(map[r.time_entry_id]||(map[r.time_entry_id]=[])).push(r)});Object.values(map).forEach(arr=>arr.sort((a,b)=>new Date(a[sortField])-new Date(b[sortField])));return map}function groupSamplesByEntry(samples){return groupByEntry(samples,"sampled_at")}function isDialerDomainMatch(domain,dialerDomains){if(!domain)return false;return(dialerDomains||[]).some(d=>domain===d||domain.endsWith("."+d))}function buildTaskCategoryMap(rows){const map={};(rows||[]).forEach(t=>{if(!t.category_id||!t.task_categories)return;map[t.id]={idlePolicy:t.task_categories.idle_policy,abuseCheckRatio:t.task_categories.abuse_check_ratio,abuseCheckMinHours:t.task_categories.abuse_check_min_hours,domains:(t.task_domains||[]).map(d=>d.domain).filter(Boolean)}});return map}async function fetchAllRows(buildQuery,pageSize){pageSize=pageSize||1e3;const rows=[];for(let from=0;;from+=pageSize){const{data,error}=await buildQuery(from,from+pageSize-1);if(error){console.error("fetchAllRows: a page failed to load — results may be incomplete",error);break}rows.push(...data||[]);if(!data||data.length<pageSize)break}return rows}function computeSuspicionFlags(entries,ctx){ctx=ctx||{};const flags=[];(entries||[]).forEach(e=>{if(e.is_manual)return;const durationSec=entryDurationSec(e);if(durationSec<=0)return;const idleSec=e.idle_seconds||0;const breakSec=e.break_seconds||0;const isDialerTask=ctx.dialerTaskIds&&ctx.dialerTaskIds.has(e.task_id);const taskCategoryInfo=ctx.taskCategoryMap&&ctx.taskCategoryMap[e.task_id];const isBoundedGraceTask=taskCategoryInfo&&taskCategoryInfo.idlePolicy==="bounded_grace";if(!isDialerTask&&!isBoundedGraceTask&&durationSec>=SUSPICION_ZERO_IDLE_MIN_HOURS*3600&&idleSec<=durationSec*SUSPICION_ZERO_IDLE_RATIO){flags.push({type:"zero_idle",entryId:e.id,userId:e.user_id,startedAt:e.start_time,detail:`${fmtShort(durationSec)} tracked with only ${fmtShort(idleSec)} idle`})}if(durationSec>=SUSPICION_NO_BREAK_MIN_HOURS*3600&&breakSec<=durationSec*SUSPICION_NO_BREAK_RATIO){flags.push({type:"no_break",entryId:e.id,userId:e.user_id,startedAt:e.start_time,detail:`${fmtShort(durationSec)} straight with no break taken`})}const samples=ctx.samplesByEntry&&ctx.samplesByEntry[e.id]||[];let runLen=0,runAnchorM=null,runAnchorK=null,runStart=null,worstRun=0,worstStart=null;samples.forEach(s=>{const m=s.mouse_events||0,k=s.keyboard_events||0;const events=m+k;const withinTolerance=runAnchorM!==null&&Math.abs(m-runAnchorM)<=SUSPICION_ROBOTIC_TOLERANCE&&Math.abs(k-runAnchorK)<=SUSPICION_ROBOTIC_TOLERANCE;if(events>0&&withinTolerance){runLen++}else if(events>0){runAnchorM=m;runAnchorK=k;runLen=1;runStart=s.sampled_at}else{runAnchorM=null;runAnchorK=null;runLen=0}if(runLen>worstRun){worstRun=runLen;worstStart=runStart}});if(worstRun>=SUSPICION_ROBOTIC_MIN_RUN){flags.push({type:"robotic",entryId:e.id,userId:e.user_id,startedAt:worstStart||e.start_time,detail:`${worstRun} minutes straight with identical mouse/keyboard counts every minute`})}if(ctx.dialerTaskIds&&ctx.dialerTaskIds.has(e.task_id)&&durationSec>=SUSPICION_DIALER_ABUSE_MIN_HOURS*3600){const visits=ctx.urlVisitsByEntry&&ctx.urlVisitsByEntry[e.id]||[];const entryStartMs=new Date(e.start_time).getTime();const entryEndMs=e.end_time?new Date(e.end_time).getTime():Date.now();let dialerSec=0;visits.forEach(v=>{if(!isDialerDomainMatch(v.domain,ctx.dialerDomains))return;const vStart=Math.max(new Date(v.start_time).getTime(),entryStartMs);const vEnd=Math.min(v.end_time?new Date(v.end_time).getTime():Date.now(),entryEndMs);if(vEnd>vStart)dialerSec+=(vEnd-vStart)/1e3});if(dialerSec<=durationSec*SUSPICION_DIALER_ABUSE_MAX_DOMAIN_RATIO){flags.push({type:"dialer_abuse",entryId:e.id,userId:e.user_id,startedAt:e.start_time,detail:`${fmtShort(durationSec)} on a dialer task, only ${fmtShort(dialerSec)} actually on a dialer domain`})}}if(taskCategoryInfo&&taskCategoryInfo.domains.length>0&&durationSec>=taskCategoryInfo.abuseCheckMinHours*3600){const visits=ctx.urlVisitsByEntry&&ctx.urlVisitsByEntry[e.id]||[];const entryStartMs=new Date(e.start_time).getTime();const entryEndMs=e.end_time?new Date(e.end_time).getTime():Date.now();let onTaskSec=0;visits.forEach(v=>{const onDomain=isDialerDomainMatch(v.domain,taskCategoryInfo.domains)||taskCategoryInfo.idlePolicy==="dialer_exempt"&&isDialerDomainMatch(v.domain,ctx.dialerDomains);if(!onDomain)return;const vStart=Math.max(new Date(v.start_time).getTime(),entryStartMs);const vEnd=Math.min(v.end_time?new Date(v.end_time).getTime():Date.now(),entryEndMs);if(vEnd>vStart)onTaskSec+=(vEnd-vStart)/1e3});if(onTaskSec<=durationSec*taskCategoryInfo.abuseCheckRatio){flags.push({type:"category_abuse",entryId:e.id,userId:e.user_id,startedAt:e.start_time,detail:`${fmtShort(durationSec)} tracked, only ${fmtShort(onTaskSec)} on this task's expected sites`})}}const idlePeriods=ctx.idlePeriodsByEntry&&ctx.idlePeriodsByEntry[e.id]||[];if(idlePeriods.length>=SUSPICION_PINGPONG_MIN_COUNT){const durations=idlePeriods.map(p=>{const s=new Date(p.start_time).getTime();const en=p.end_time?new Date(p.end_time).getTime():Date.now();return Math.max(0,(en-s)/1e3)});const avgSec=durations.reduce((a,b)=>a+b,0)/durations.length;if(avgSec<=SUSPICION_PINGPONG_MAX_AVG_SEC){flags.push({type:"idle_pingpong",entryId:e.id,userId:e.user_id,startedAt:e.start_time,detail:`${idlePeriods.length} idle stretches in one session, averaging ${Math.round(avgSec)}s each`})}}});return flags}function computeChoppedSessionFlags(entries,tzByUser){const flags=[];const byAgentDay={};(entries||[]).forEach(e=>{if(e.is_manual)return;if(!e.end_time)return;const durationSec=entryDurationSec(e);if(durationSec>SUSPICION_CHOPPED_MAX_MINUTES*60)return;const localDate=localDateKeyInZone(e.start_time,tzByUser&&tzByUser[e.user_id]);const dayKey=`${e.user_id}|${localDate}`;(byAgentDay[dayKey]||(byAgentDay[dayKey]=[])).push(e)});Object.entries(byAgentDay).forEach(([key,shortEntries])=>{if(shortEntries.length<SUSPICION_CHOPPED_MIN_COUNT)return;const userId=key.split("|")[0];const earliest=shortEntries.reduce((a,b)=>new Date(a.start_time)<new Date(b.start_time)?a:b);flags.push({type:"chopped_sessions",entryId:earliest.id,userId,startedAt:earliest.start_time,detail:`${shortEntries.length} sessions under ${SUSPICION_CHOPPED_MAX_MINUTES} min in one day`})});return flags}function computeSparseScreenshotFlags(entries,shotCountByEntry){const flags=[];(entries||[]).forEach(e=>{if(e.is_manual)return;const durationSec=entryDurationSec(e);if(durationSec<SUSPICION_SPARSE_SHOTS_MIN_HOURS*3600)return;if(!e.end_time)return;const expected=durationSec/600;const actual=shotCountByEntry&&shotCountByEntry[e.id]||0;if(actual<expected*SUSPICION_SPARSE_SHOTS_MAX_RATIO){flags.push({type:"sparse_screenshots",entryId:e.id,userId:e.user_id,startedAt:e.start_time,detail:`${fmtShort(durationSec)} tracked with only ${actual} screenshot${actual===1?"":"s"} (expected around ${Math.round(expected)})`})}});return flags}function computeMergedSessionFlags(entries,tzByUser){const flags=[];const byAgentDay={};(entries||[]).forEach(e=>{if(e.is_manual)return;if(!e.end_time)return;const localDate=localDateKeyInZone(e.start_time,tzByUser&&tzByUser[e.user_id]);const dayKey=`${e.user_id}|${localDate}`;(byAgentDay[dayKey]||(byAgentDay[dayKey]=[])).push(e)});Object.values(byAgentDay).forEach(dayEntries=>{dayEntries.sort((a,b)=>new Date(a.start_time)-new Date(b.start_time));let chain=[];const flushChain=()=>{if(chain.length>=2){const totalDurationSec=chain.reduce((t,e)=>t+entryDurationSec(e),0);const totalIdleSec=chain.reduce((t,e)=>t+(e.idle_seconds||0),0);const totalBreakSec=chain.reduce((t,e)=>t+(e.break_seconds||0),0);const anyEntryAlreadyHits=(minHours,ratioField,ratio)=>chain.some(e=>{const d=entryDurationSec(e);return d>=minHours*3600&&(e[ratioField]||0)<=d*ratio});const first=chain[0];if(totalDurationSec>=SUSPICION_ZERO_IDLE_MIN_HOURS*3600&&totalIdleSec<=totalDurationSec*SUSPICION_ZERO_IDLE_RATIO&&!anyEntryAlreadyHits(SUSPICION_ZERO_IDLE_MIN_HOURS,"idle_seconds",SUSPICION_ZERO_IDLE_RATIO)){flags.push({type:"split_zero_idle",entryId:first.id,userId:first.user_id,startedAt:first.start_time,detail:`${fmtShort(totalDurationSec)} across ${chain.length} sessions (gaps under ${SUSPICION_MERGE_GAP_MAX_MIN}m) with only ${fmtShort(totalIdleSec)} idle combined`})}if(totalDurationSec>=SUSPICION_NO_BREAK_MIN_HOURS*3600&&totalBreakSec<=totalDurationSec*SUSPICION_NO_BREAK_RATIO&&!anyEntryAlreadyHits(SUSPICION_NO_BREAK_MIN_HOURS,"break_seconds",SUSPICION_NO_BREAK_RATIO)){flags.push({type:"split_no_break",entryId:first.id,userId:first.user_id,startedAt:first.start_time,detail:`${fmtShort(totalDurationSec)} across ${chain.length} sessions (gaps under ${SUSPICION_MERGE_GAP_MAX_MIN}m) with no break combined`})}}chain=[]};dayEntries.forEach(e=>{if(chain.length===0){chain.push(e);return}const prevEnd=new Date(chain[chain.length-1].end_time).getTime();const gapMin=(new Date(e.start_time).getTime()-prevEnd)/6e4;if(gapMin>=0&&gapMin<=SUSPICION_MERGE_GAP_MAX_MIN){chain.push(e)}else{flushChain();chain.push(e)}});flushChain()});return flags}let flagsAllAgents=[];let lastFlagsRows=[];function defaultFlagsRange(){const from=new Date;from.setDate(from.getDate()-6);const iso=d=>d.toISOString().slice(0,10);return{from:iso(from),to:endOfLoginMonthDateInputValue()}}async function initFlagsTab(){if(!$("flagsFromInput").value||!$("flagsToInput").value){const{from,to}=defaultFlagsRange();$("flagsFromInput").value=from;$("flagsToInput").value=to}const{data:agents}=await sb.from("profiles").select("id, full_name, email, role").order("full_name");flagsAllAgents=(agents||[]).filter(a=>!PROTECTED_ROLES.has(a.role));const sel=$("flagsAgentFilter");const prev=sel.value;sel.innerHTML='<option value="">All users</option>'+flagsAllAgents.map(a=>`<option value="${a.id}">${escapeHtml(a.full_name||a.email)}</option>`).join("");sel.value=prev}async function runFlagsScan(){const fromVal=$("flagsFromInput").value;const toVal=$("flagsToInput").value;if(!fromVal||!toVal)return;const agentFilter=$("flagsAgentFilter").value;const typeFilter=$("flagsTypeFilter").value;const rangeStart=new Date(fromVal+"T00:00:00").toISOString();const rangeEnd=new Date(toVal+"T23:59:59.999").toISOString();const summary=$("flagsSummaryLabel");summary.textContent="Scanning…";$("flagsBody").innerHTML="";const buildEntryQuery=(from,to)=>{let q=sb.from("time_entries").select("id, user_id, task_id, start_time, end_time, idle_seconds, break_seconds, is_manual").or(`end_time.is.null,end_time.gte.${rangeStart}`).lte("start_time",rangeEnd).order("id",{ascending:true});if(agentFilter)q=q.eq("user_id",agentFilter);return q.range(from,to)};const buildSampleQuery=(from,to)=>{let q=sb.from("activity_samples").select("time_entry_id, user_id, sampled_at, mouse_events, keyboard_events").gte("sampled_at",rangeStart).lte("sampled_at",rangeEnd).order("id",{ascending:true});if(agentFilter)q=q.eq("user_id",agentFilter);return q.range(from,to)};const buildIdleQuery=(from,to)=>{let q=sb.from("idle_periods").select("time_entry_id, user_id, start_time, end_time").or(`end_time.is.null,end_time.gte.${rangeStart}`).lte("start_time",rangeEnd).order("id",{ascending:true});if(agentFilter)q=q.eq("user_id",agentFilter);return q.range(from,to)};const buildVisitQuery=(from,to)=>{let q=sb.from("url_visits").select("time_entry_id, domain, start_time, end_time").or(`end_time.is.null,end_time.gte.${rangeStart}`).lte("start_time",rangeEnd).order("id",{ascending:true});if(agentFilter)q=q.eq("user_id",agentFilter);return q.range(from,to)};const buildShotQuery=(from,to)=>{let q=sb.from("screenshots").select("time_entry_id, taken_at").gte("taken_at",rangeStart).lte("taken_at",rangeEnd).order("id",{ascending:true});if(agentFilter)q=q.eq("user_id",agentFilter);return q.range(from,to)};const[entries,samples,idlePeriods,urlVisits,shots,dialerTaskRows,dialerDomainRows,scheduleRows,categorizedTaskRows]=await Promise.all([fetchAllRows(buildEntryQuery),fetchAllRows(buildSampleQuery),fetchAllRows(buildIdleQuery),fetchAllRows(buildVisitQuery),fetchAllRows(buildShotQuery),fetchAllRows((from,to)=>sb.from("tasks").select("id").eq("is_dialer_task",true).order("id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("dialer_domains").select("domain").order("id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("schedules").select("user_id, timezone").order("user_id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("tasks").select("id, category_id, task_categories(idle_policy, abuse_check_ratio, abuse_check_min_hours), task_domains(domain)").not("category_id","is",null).order("id",{ascending:true}).range(from,to))]);const flagsAgentTz={};(scheduleRows||[]).forEach(s=>{if(s.timezone)flagsAgentTz[s.user_id]=s.timezone});const shotCountByEntry={};(shots||[]).forEach(s=>{if(s.time_entry_id)shotCountByEntry[s.time_entry_id]=(shotCountByEntry[s.time_entry_id]||0)+1});const suspicionCtx={samplesByEntry:groupSamplesByEntry(samples),idlePeriodsByEntry:groupByEntry(idlePeriods,"start_time"),urlVisitsByEntry:groupByEntry(urlVisits,"start_time"),dialerTaskIds:new Set((dialerTaskRows||[]).map(t=>t.id)),dialerDomains:(dialerDomainRows||[]).map(d=>d.domain),taskCategoryMap:buildTaskCategoryMap(categorizedTaskRows)};let flags=computeSuspicionFlags(entries,suspicionCtx).concat(computeChoppedSessionFlags(entries,flagsAgentTz)).concat(computeSparseScreenshotFlags(entries,shotCountByEntry)).concat(computeMergedSessionFlags(entries,flagsAgentTz));if(typeFilter)flags=flags.filter(f=>f.type===typeFilter);flags.sort((a,b)=>new Date(b.startedAt)-new Date(a.startedAt));const agentName=id=>{const a=flagsAllAgents.find(x=>x.id===id);return a?a.full_name||a.email:"—"};summary.textContent=flags.length?`${flags.length} flag${flags.length===1?"":"s"} across ${new Set(flags.map(f=>f.userId)).size} user(s), ${fromVal} to ${toVal}`:`No flags found, ${fromVal} to ${toVal}`;lastFlagsRows=[];const body=$("flagsBody");if(flags.length===0){body.innerHTML='<tr><td colspan="5" class="empty">Nothing flagged in this range.</td></tr>';return}const flagsDisplayTz=globalDisplayTzMode==="agent"?null:globalDisplayTzMode;flags.forEach(f=>{const tz=flagsDisplayTz||flagsAgentTz[f.userId];const dateLabel=new Date(f.startedAt).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...tz?{timeZone:tz}:{}});const typeLabel=SUSPICION_TYPE_META[f.type].label;lastFlagsRows.push({agent:agentName(f.userId),date:dateLabel,type:typeLabel,detail:f.detail});const tr=document.createElement("tr");tr.innerHTML=`
      <td>${escapeHtml(agentName(f.userId))}</td>
      <td class="mono">${dateLabel}</td>
      <td><span class="dot status-break"></span>${typeLabel}</td>
      <td style="color:var(--text-2);">${f.detail}</td>
      <td><button class="flags-view-btn" style="background:transparent; border:1px solid var(--border-light); border-radius:999px; color:var(--focus); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px;">View day</button></td>
    `;tr.querySelector(".flags-view-btn").addEventListener("click",()=>{const agent=flagsAllAgents.find(x=>x.id===f.userId);if(!agent)return;const day=new Date(f.startedAt).toISOString().slice(0,10);$("tsFromDateInput").value=day;$("tsToDateInput").value=day;$("tsAgentFilter").value=agent.id;document.querySelector('.tab-btn[data-tab="time_management"]').click();activateTimeManagementSection("timesheets")});body.appendChild(tr)})}$("flagsGenerateBtn").addEventListener("click",runFlagsScan);$("flagsExportCsvBtn").addEventListener("click",()=>{const headers=["User","Date","Type","Detail"];const rows=lastFlagsRows.map(r=>[r.agent,r.date,r.type,r.detail]);exportCSV(`suspicious-activity-${new Date().toISOString().slice(0,10)}.csv`,headers,rows);logActivity("export.flags_csv",null,null,`Exported suspicious activity CSV (${rows.length} flags)`)});$("flagsExportPdfBtn").addEventListener("click",()=>{const headers=["User","Date","Type","Detail"];const rows=lastFlagsRows.map(r=>[r.agent,r.date,r.type,r.detail]);exportPDF("Suspicious Activity Report",headers,rows);logActivity("export.flags_pdf",null,null,`Exported suspicious activity PDF (${rows.length} flags)`)});function umCanImpersonate(p){if(!(myIsOwner||myRole==="admin"))return false;if(p.id===myUserId)return false;if(!myIsOwner&&(p.role==="owner"||p.role==="admin"))return false;return true}let staffImpersonating=false;function showImpersonationBanner(){const banner=$("staffImpersonationBanner");if(!banner)return;const who=$("staffImpersonationName");if(who)who.textContent=myFullName||myEmail||"a user";banner.style.display="flex";const exit=$("staffImpersonationExitBtn");if(exit)exit.onclick=async()=>{try{await sb.auth.signOut()}catch(e){}try{sessionStorage.clear()}catch(e){}window.close();location.replace(location.pathname)}}async function umImpersonate(btn){const name=btn.dataset.userName||"this user";if(!confirm(`Open the staff portal signed in as ${name}?

It opens in a new tab and your own session stays signed in here. Anything you do in that tab is recorded as ${name}.`))return;btn.disabled=true;const original=btn.textContent;btn.textContent="Opening…";const result=await invokeManageUser({action:"impersonate",userId:btn.dataset.userId});btn.disabled=false;btn.textContent=original;if(!result.ok||!result.tokenHash){alert(result.error||"Could not open the portal as this user.");return}logActivity("user.impersonate","agent",btn.dataset.userId,`Logged in as "${name}" to view the portal as them`);window.open(`${location.origin}${location.pathname}?staff_token=${encodeURIComponent(result.tokenHash)}`,"_blank","noopener")}function umImpersonateCellHtml(p,isActive){if(!umCanImpersonate(p))return"";if(!isActive){return'<span style="color:var(--text-dim); font-size:11px;" title="Reactivate the account first">—</span>'}return`<button class="btn-secondary um-impersonate-btn" data-user-id="${p.id}" data-user-name="${escapeHtml(p.full_name||p.email||"this user")}" title="Open the staff portal signed in as this user, in a new tab" style="width:auto; padding:5px 10px; font-size:11px; margin-top:0; white-space:nowrap;">Log in as user</button>`}let umTwoStepUsers=new Set;async function loadUmTwoStepUsers(){umTwoStepUsers=new Set;if(!(myIsOwner||myRole==="admin"))return;const{data,error}=await sb.rpc("staff_two_step_users");if(error){console.warn("two-step list:",error.message);return}(data||[]).forEach(r=>umTwoStepUsers.add(typeof r==="string"?r:Object.values(r||{})[0]))}function umResetTwoStepBtnHtml(p){if(!umCanImpersonate(p)||!umTwoStepUsers.has(p.id))return"";return`<button class="btn-secondary um-reset2fa-btn" data-user-id="${p.id}" data-user-name="${escapeHtml(p.full_name||p.email||"this user")}" title="Lost or replaced phone: removes their authenticator app and signs them out, so they set up the new phone at their next sign-in" style="width:auto; padding:5px 10px; font-size:11px; margin-top:0; white-space:nowrap;">Reset two-step</button>`}async function umResetTwoStep(btn){const name=btn.dataset.userName||"this user";if(!confirm(`Reset two-step sign-in for ${name}?

Use this when they lost or replaced the phone with their authenticator app. It removes the authenticator from their account and signs them out everywhere. At their next sign-in they set up two-step again with the new phone.`))return;btn.disabled=true;const original=btn.textContent;btn.textContent="Resetting…";const{data,error}=await sb.rpc("staff_reset_two_step",{p_user:btn.dataset.userId});btn.disabled=false;btn.textContent=original;if(error){alert(error.message||"Could not reset two-step sign-in.");return}alert(`Two-step sign-in reset for ${name}. ${data&&data.sessions_ended?"They were signed out and ":""}they will set up the new phone the next time they sign in.`);await loadUmUsersList()}async function loadUmUsersList(){const tbody=$("umUsersTbody");if(!tbody)return;tbody.innerHTML='<tr><td colspan="9" style="color:var(--text-dim);">Loading…</td></tr>';const[profiles,assignments,tasks,scheduleRows]=await Promise.all([fetchAllRows((from,to)=>sb.from("profiles").select("id, full_name, email, role, extension_version, extension_version_at, created_at, is_active, department, title, grade, american_name").order("full_name").range(from,to)),fetchAllRows((from,to)=>sb.from("task_assignments").select("task_id, user_id").order("user_id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("tasks").select("id, name").order("id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("schedules").select("user_id, timezone").order("user_id",{ascending:true}).range(from,to))]);await loadUmTwoStepUsers();const taskName={};(tasks||[]).forEach(t=>{taskName[t.id]=t.name});const byUser={};(assignments||[]).forEach(a=>{(byUser[a.user_id]=byUser[a.user_id]||[]).push(taskName[a.task_id]||"—")});const umAgentTz={};(scheduleRows||[]).forEach(s=>{if(s.timezone)umAgentTz[s.user_id]=s.timezone});const list=(profiles||[]).filter(p=>!PROTECTED_ROLES.has(p.role)||myIsOwner&&p.role==="admin"||myIsOwner&&p.id===myUserId);if(!list.length){tbody.innerHTML='<tr><td colspan="9" style="color:var(--text-dim);">No users.</td></tr>';return}tbody.innerHTML="";list.forEach(p=>{const assigned=byUser[p.id]||[];const assignedLabel=assigned.length?`${assigned.length} — ${assigned.slice(0,3).join(", ")}${assigned.length>3?"…":""}`:"—";const typeLabel=roleDisplayLabel(p.role||"user");const version=p.extension_version||"—";const tz=globalDisplayTzMode==="agent"?umAgentTz[p.id]:globalDisplayTzMode;const createdLabel=p.created_at?new Date(p.created_at).toLocaleString([],{month:"short",day:"numeric",year:"numeric",hour:"2-digit",minute:"2-digit",...tz?{timeZone:tz}:{}}):"—";const isActive=p.is_active!==false;const statusBadge=isActive?'<span class="type-badge" style="color:var(--good);">Active</span>':'<span class="type-badge" style="color:var(--danger);">Deactivated</span>';const tr=document.createElement("tr");tr.style.cursor=canOpenUserProfile(p.id)?"pointer":"default";tr.innerHTML=`
      <td>${escapeHtml(p.full_name||p.email||"—")}</td>
      <td>${p.title?escapeHtml(p.title):'<span style="color:var(--text-dim);">—</span>'}</td>
      <td>${p.department?escapeHtml(p.department):'<span style="color:var(--text-dim);">—</span>'}</td>
      <td title="${assigned.join(", ")}">${assignedLabel}</td>
      <td><span class="type-badge">${typeLabel}</span></td>
      <td>${statusBadge}</td>
      <td class="mono">${version}</td>
      <td class="mono">${createdLabel}</td>
      <td><div style="display:flex; gap:6px; flex-wrap:wrap;">${umImpersonateCellHtml(p,isActive)}${umResetTwoStepBtnHtml(p)}</div></td>`;if(canOpenUserProfile(p.id)){tr.addEventListener("click",()=>{detailReturnTo={tab:"usermanagement",umSection:umActiveSection||"users"};tmActiveSection="staff";document.querySelectorAll(".tab-panel").forEach(panel=>panel.classList.toggle("active",panel.id==="tab-time_management"));document.querySelectorAll(".tm-section").forEach(s=>{s.style.display=s.id==="tmSection-staff"?"block":"none"});openDetail(p,{fromUsersTab:true})})}const impBtn=tr.querySelector(".um-impersonate-btn");if(impBtn)impBtn.addEventListener("click",ev=>{ev.stopPropagation();umImpersonate(impBtn)});const resetBtn=tr.querySelector(".um-reset2fa-btn");if(resetBtn)resetBtn.addEventListener("click",ev=>{ev.stopPropagation();umResetTwoStep(resetBtn)});tbody.appendChild(tr)});applyUmSectionEditGating("users")}async function getOrCreateDefaultProject(){const{data:existing}=await sb.from("projects").select("id").eq("name","General").limit(1).maybeSingle();if(existing)return existing.id;const{data:created,error}=await sb.from("projects").insert({name:"General"}).select("id").single();if(error)throw error;return created.id}let taskCategoriesCache=[];let taskFieldsExpanded=new Set;let taskPolicyEditing=new Set;let taskScopeFieldsCache={};let taskScopeFieldEditingKey=null;async function loadTaskScopeFields(scopeKey,{categoryId,taskId}){let q=sb.from("task_category_fields").select("id, field_key, label, field_type, options, required, sort_order");q=categoryId?q.eq("category_id",categoryId):q.eq("task_id",taskId);const{data}=await q.order("sort_order");taskScopeFieldsCache[scopeKey]=data||[]}function renderTaskScopeFieldsList(scopeKey,container,scope){const fields=(taskScopeFieldsCache[scopeKey]||[]).slice().sort((a,b)=>a.sort_order-b.sort_order);container.innerHTML="";if(!fields.length){const empty=document.createElement("div");empty.className="empty";empty.style.cssText="padding:6px 0;";empty.textContent="No fields yet — the extension shows no lead-submission form here until you add at least one below.";container.appendChild(empty)}fields.forEach((f,i)=>{const fieldKey=`${scopeKey}:${f.id}`;if(taskScopeFieldEditingKey===fieldKey){container.appendChild(buildSfFieldEditRow(f,{onCancel:()=>{taskScopeFieldEditingKey=null;renderTaskScopeFieldsList(scopeKey,container,scope)},onSave:async patch=>{const{error}=await sb.from("task_category_fields").update(patch).eq("id",f.id);if(error)return error.message;logActivity("task_category_field.update",null,null,`Edited field "${patch.label}" (${scopeKey.startsWith("cat:")?"category":"task"} scoped)`);return null},onSaved:async()=>{taskScopeFieldEditingKey=null;await loadTaskScopeFields(scopeKey,scope);renderTaskScopeFieldsList(scopeKey,container,scope)}}));return}const optsNote=(f.field_type==="select"||f.field_type==="multi")&&f.options&&f.options.length?`: ${f.options.slice(0,3).map(escapeHtml).join(", ")}${f.options.length>3?`, +${f.options.length-3} more`:""}`:"";const row=document.createElement("div");row.style.cssText="display:flex; align-items:center; gap:8px; padding:5px 0; border-top:1px solid var(--border);";row.innerHTML=`
      <span class="mono" style="font-size:12px; flex:1;">${escapeHtml(f.label)} <span style="color:var(--text-dim); font-size:10px;">(${fieldTypeLabel(f.field_type)}${f.required?", required":""}${RESERVED_FIELD_KEYS.has(f.field_key)?", maps to a fixed lead column":""}${optsNote})</span></span>
      <button class="edit-only" title="Edit field" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px;">Edit</button>
      <button class="edit-only" title="Move up" ${i===0?"disabled":""} style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px;">▲</button>
      <button class="edit-only" title="Move down" ${i===fields.length-1?"disabled":""} style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px;">▼</button>
      <button class="edit-only" title="Remove field" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px;">×</button>
    `;const[editBtn,upBtn,downBtn,delBtn]=row.querySelectorAll("button");editBtn.addEventListener("click",()=>{taskScopeFieldEditingKey=fieldKey;renderTaskScopeFieldsList(scopeKey,container,scope)});delBtn.addEventListener("click",async()=>{if(!confirm(`Remove the "${f.label}" field? Values already submitted with it stay on those existing leads — this only stops it from being asked for on new submissions.`))return;await sb.from("task_category_fields").delete().eq("id",f.id);logActivity("task_category_field.delete",null,null,`Removed field "${f.label}" (${scopeKey.startsWith("cat:")?"category":"task"} scoped)`);await loadTaskScopeFields(scopeKey,scope);renderTaskScopeFieldsList(scopeKey,container,scope)});const swapWith=async other=>{if(!other)return;await Promise.all([sb.from("task_category_fields").update({sort_order:other.sort_order}).eq("id",f.id),sb.from("task_category_fields").update({sort_order:f.sort_order}).eq("id",other.id)]);await loadTaskScopeFields(scopeKey,scope);renderTaskScopeFieldsList(scopeKey,container,scope)};upBtn.addEventListener("click",()=>swapWith(fields[i-1]));downBtn.addEventListener("click",()=>swapWith(fields[i+1]));container.appendChild(row)});applyEditGating("tasks")}function wireTaskScopeFieldAddForm(container,scopeKey,scope,fieldsContainer){container.querySelectorAll(".sf-form-field-reserved-btn").forEach(btn=>{btn.addEventListener("click",async()=>{const existing=taskScopeFieldsCache[scopeKey]||[];if(existing.some(f=>f.field_key===btn.dataset.key)){alert("This already has that field.");return}await addTaskScopeField(scopeKey,scope,{field_key:btn.dataset.key,label:btn.dataset.label,field_type:btn.dataset.type,required:btn.dataset.required==="true",options:null},fieldsContainer)})});const typeSelect=container.querySelector(".sf-form-field-type-select");const optionsRow=container.querySelector(".sf-form-field-options-row");typeSelect.addEventListener("change",()=>{optionsRow.style.display=typeSelect.value==="select"||typeSelect.value==="multi"?"":"none"});container.querySelector(".sf-form-field-add-btn").addEventListener("click",async()=>{const labelInput=container.querySelector(".sf-form-field-label-input");const requiredInput=container.querySelector(".sf-form-field-required-input");const optionsInput=container.querySelector(".sf-form-field-options-input");const msg=container.querySelector(".sf-form-field-msg");const label=(labelInput.value||"").trim();msg.textContent="";msg.className="msg sf-form-field-msg";if(!label){msg.textContent="Enter a label for the custom field.";msg.className="msg sf-form-field-msg error";return}const existing=taskScopeFieldsCache[scopeKey]||[];const existingKeys=new Set(existing.map(f=>f.field_key));let key=slugifyFieldKey(label);if(!key||RESERVED_FIELD_KEYS.has(key)){msg.textContent="That label produces a reserved or empty field key — try a more specific label.";msg.className="msg sf-form-field-msg error";return}let suffix=1;while(existingKeys.has(key))key=`${slugifyFieldKey(label)}_${++suffix}`;const fieldType=typeSelect.value;let options=null;if(fieldType==="select"||fieldType==="multi"){options=(optionsInput.value||"").split(",").map(s=>s.trim()).filter(Boolean);if(!options.length){msg.textContent="Enter at least one choice, comma-separated.";msg.className="msg sf-form-field-msg error";return}}const err=await addTaskScopeField(scopeKey,scope,{field_key:key,label,field_type:fieldType,required:requiredInput.checked,options},fieldsContainer);if(err){msg.textContent=err;msg.className="msg sf-form-field-msg error";return}labelInput.value="";optionsInput.value="";requiredInput.checked=false})}async function addTaskScopeField(scopeKey,{categoryId,taskId},{field_key,label,field_type,options,required},fieldsContainer){const existing=taskScopeFieldsCache[scopeKey]||[];const nextOrder=existing.length?Math.max(...existing.map(f=>f.sort_order))+10:0;const row={category_id:categoryId||null,task_id:taskId||null,field_key,label,field_type,required,sort_order:nextOrder,options:options&&options.length?options:null};const{error}=await sb.from("task_category_fields").insert(row);if(error)return/duplicate|unique/i.test(error.message)?`This already has a "${label}" field.`:error.message;logActivity("task_category_field.create",null,null,`Added field "${label}" (${scopeKey.startsWith("cat:")?"category":"task"} scoped)`);await loadTaskScopeFields(scopeKey,{categoryId,taskId});renderTaskScopeFieldsList(scopeKey,fieldsContainer,{categoryId,taskId});return null}function idlePolicyLabel(policy){return policy==="dialer_exempt"?"Dialer-exempt":policy==="bounded_grace"?"Bounded grace":"Standard"}async function loadTaskCategoriesList(){invalidateTeamStatic();const{data:categories}=await sb.from("task_categories").select("id, name, idle_policy, grace_seconds").order("name");taskCategoriesCache=categories||[];const list=$("taskCategoriesList");list.innerHTML="";if(!taskCategoriesCache.length){list.innerHTML='<div class="empty">No task categories yet — add one above.</div>';return}taskCategoriesCache.forEach(c=>{const row=document.createElement("div");row.className="task-row";row.style.flexWrap="wrap";const expanded=taskFieldsExpanded.has(`cat:${c.id}`);const policyEditing=taskPolicyEditing.has(c.id);row.innerHTML=`
      <div style="flex:1;"><span class="task-row-name" contenteditable="true" data-id="${c.id}" data-original="${escapeHtml(c.name)}" title="Click to rename">${escapeHtml(c.name)}</span><span class="task-row-count">${idlePolicyLabel(c.idle_policy)}${c.idle_policy==="bounded_grace"&&c.grace_seconds?` · ${Math.round(c.grace_seconds/60)} min grace`:""}</span></div>
      <button class="edit-only task-policy-toggle-btn" data-id="${c.id}" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px;">${policyEditing?"Cancel":"Edit policy"}</button>
      <button class="edit-only task-fields-toggle-btn" data-scope="cat:${c.id}" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px;">${expanded?"Hide fields":"Manage fields"}</button>
      <button class="task-del-btn edit-only" data-id="${c.id}" title="Delete category">×</button>
      <div style="flex-basis:100%;"></div>
    `;const catNameEl=row.querySelector(".task-row-name");catNameEl.addEventListener("blur",()=>saveTaskCategoryRename(catNameEl));catNameEl.addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();catNameEl.blur()}if(e.key==="Escape"){catNameEl.textContent=catNameEl.dataset.original;catNameEl.blur()}});row.querySelector(".task-del-btn").addEventListener("click",()=>deleteTaskCategory(c.id,c.name));row.querySelector(".task-fields-toggle-btn").addEventListener("click",()=>{const key=`cat:${c.id}`;if(taskFieldsExpanded.has(key))taskFieldsExpanded.delete(key);else taskFieldsExpanded.add(key);loadTaskCategoriesList()});row.querySelector(".task-policy-toggle-btn").addEventListener("click",()=>{if(taskPolicyEditing.has(c.id))taskPolicyEditing.delete(c.id);else taskPolicyEditing.add(c.id);loadTaskCategoriesList()});if(policyEditing){const editWrap=document.createElement("div");editWrap.style.cssText="flex-basis:100%; margin-top:8px; padding-top:8px; border-top:1px solid var(--border); display:flex; gap:8px; flex-wrap:wrap; align-items:flex-end;";const graceMin=c.grace_seconds?Math.round(c.grace_seconds/60):8;editWrap.innerHTML=`
        <div style="min-width:150px;"><label>Idle policy</label>
          <select class="task-policy-edit-select">
            <option value="standard"${c.idle_policy==="standard"||!c.idle_policy?" selected":""}>Standard</option>
            <option value="dialer_exempt"${c.idle_policy==="dialer_exempt"?" selected":""}>Dialer-exempt</option>
            <option value="bounded_grace"${c.idle_policy==="bounded_grace"?" selected":""}>Bounded grace</option>
          </select>
        </div>
        <div class="task-policy-edit-grace-wrap" style="min-width:110px; ${c.idle_policy==="bounded_grace"?"":"display:none;"}"><label>Grace (min)</label><input type="number" class="task-policy-edit-grace-input" min="1" max="60" value="${graceMin}"></div>
        <button class="btn-primary task-policy-save-btn" style="width:auto; padding:8px 16px;">Save</button>
        <div class="msg task-policy-edit-msg" style="display:none; flex-basis:100%;"></div>
      `;const editSelect=editWrap.querySelector(".task-policy-edit-select");const editGraceWrap=editWrap.querySelector(".task-policy-edit-grace-wrap");editSelect.addEventListener("change",()=>{editGraceWrap.style.display=editSelect.value==="bounded_grace"?"":"none"});editWrap.querySelector(".task-policy-save-btn").addEventListener("click",async()=>{const msg=editWrap.querySelector(".task-policy-edit-msg");const newPolicy=editSelect.value;const newGraceSeconds=newPolicy==="bounded_grace"?(parseInt(editWrap.querySelector(".task-policy-edit-grace-input").value,10)||8)*60:null;const{error}=await sb.from("task_categories").update({idle_policy:newPolicy,grace_seconds:newGraceSeconds}).eq("id",c.id);if(error){msg.textContent=error.message;msg.className="msg error";msg.style.display="block";return}logActivity("task_category.update",null,null,`Changed "${c.name}" idle policy to ${idlePolicyLabel(newPolicy)}${newPolicy==="bounded_grace"?` (${Math.round(newGraceSeconds/60)} min grace)`:""}`);taskPolicyEditing.delete(c.id);loadTaskCategoriesList()});row.appendChild(editWrap)}if(expanded){const wrap=document.createElement("div");wrap.style.cssText="flex-basis:100%; margin-top:8px; padding-top:8px; border-top:1px solid var(--border);";const hint=document.createElement("div");hint.className="mono";hint.style.cssText="font-size:10px; color:var(--text-dim); margin-bottom:8px;";hint.textContent="These are the fields the extension shows when submitting a lead for a task in this category (unless the specific task has its own fields — those replace these entirely). Applies to every task in this category with no fields of its own.";wrap.appendChild(hint);const fieldsContainer=document.createElement("div");wrap.appendChild(fieldsContainer);const addRow=document.createElement("div");addRow.innerHTML=sfFormFieldAddFormHtml();wrap.appendChild(addRow);row.appendChild(wrap);loadTaskScopeFields(`cat:${c.id}`,{categoryId:c.id}).then(()=>{renderTaskScopeFieldsList(`cat:${c.id}`,fieldsContainer,{categoryId:c.id});wireTaskScopeFieldAddForm(addRow,`cat:${c.id}`,{categoryId:c.id},fieldsContainer)})}list.appendChild(row)});applyEditGating("tasks");populateAllTaskCategorySelects()}const RESERVED_FIELD_KEYS=new Set(["seller_investor_name","seller_phone","count"]);function slugifyFieldKey(label){return label.trim().toLowerCase().replace(/[^a-z0-9]+/g,"_").replace(/^_+|_+$/g,"")}function fieldTypeLabel(type){return type==="tel"?"Phone":type==="number"?"Number":type==="textarea"?"Long text":type==="select"?"Dropdown":type==="multi"?"Checkbox group":"Text"}$("newCategoryPolicySelect")&&$("newCategoryPolicySelect").addEventListener("change",e=>{$("newCategoryGraceWrap").style.display=e.target.value==="bounded_grace"?"":"none"});$("addCategoryBtn")&&$("addCategoryBtn").addEventListener("click",async()=>{const nameInput=$("newCategoryNameInput");const policySelect=$("newCategoryPolicySelect");const graceInput=$("newCategoryGraceInput");const msg=$("taskCategoriesMsg");const name=(nameInput.value||"").trim();if(!name){msg.textContent="Enter a category name.";msg.className="msg error";return}const idle_policy=policySelect.value;const grace_seconds=idle_policy==="bounded_grace"?(parseInt(graceInput.value,10)||8)*60:null;const{error}=await sb.from("task_categories").insert({name,idle_policy,grace_seconds});if(error){msg.textContent=/duplicate|unique/i.test(error.message)?"A category with that name already exists.":error.message;msg.className="msg error";return}nameInput.value="";graceInput.value="";msg.textContent="";loadTaskCategoriesList();logActivity("task_category.create",null,null,`Added task category "${name}" (${idlePolicyLabel(idle_policy)})`)});async function deleteTaskCategory(id,name){if(!confirm(`Delete the "${name}" category? Tasks assigned to it fall back to standard idle detection (or their legacy phone/dialer toggle, if set) — they aren't deleted.`))return;await sb.from("task_categories").delete().eq("id",id);loadTaskCategoriesList();loadTasksList();logActivity("task_category.delete",null,null,`Deleted task category "${name}"`)}async function saveTaskCategoryRename(nameEl){const newName=nameEl.textContent.trim();const categoryId=nameEl.dataset.id;const oldName=nameEl.dataset.original;if(!newName||newName===oldName){nameEl.textContent=oldName;return}const{error}=await sb.from("task_categories").update({name:newName}).eq("id",categoryId);const msg=$("taskCategoriesMsg");if(error){msg.textContent=/duplicate|unique/i.test(error.message)?"A category with that name already exists.":error.message;msg.className="msg error";nameEl.textContent=oldName}else{nameEl.dataset.original=newName;msg.textContent="Renamed.";msg.className="msg";msg.style.color="var(--focus)";logActivity("task_category.rename",null,null,`Renamed task category "${oldName}" to "${newName}"`);const cacheEntry=taskCategoriesCache.find(c=>c.id===categoryId);if(cacheEntry)cacheEntry.name=newName;populateAllTaskCategorySelects()}}function populateAllTaskCategorySelects(){document.querySelectorAll(".task-category-select").forEach(sel=>{const current=sel.value;sel.innerHTML='<option value="">— No category (standard) —</option>'+taskCategoriesCache.map(c=>`<option value="${c.id}">${escapeHtml(c.name)}</option>`).join("");sel.value=current})}async function loadTaskDomainsForRow(taskId,container){const{data:domains}=await sb.from("task_domains").select("id, domain").eq("task_id",taskId).order("domain");container.innerHTML="";(domains||[]).forEach(d=>{const chip=document.createElement("span");chip.className="mono";chip.style.cssText="display:inline-flex; align-items:center; gap:5px; background:var(--inset); border-radius:4px; padding:3px 7px; font-size:10px; margin:2px 4px 2px 0;";chip.innerHTML=`${escapeHtml(d.domain)} <span class="edit-only" data-id="${d.id}" style="cursor:pointer; color:var(--text-dim);" title="Remove">×</span>`;const x=chip.querySelector("span[data-id]");if(x)x.addEventListener("click",async()=>{await sb.from("task_domains").delete().eq("id",d.id);loadTaskDomainsForRow(taskId,container)});container.appendChild(chip)});if(!domains||domains.length===0){const hint=document.createElement("span");hint.className="mono";hint.style.cssText="font-size:10px; color:var(--text-dim);";hint.textContent="No domains set — no off-task warning or abuse check for this task.";container.appendChild(hint)}applyEditGating("tasks")}async function loadTasksList(){invalidateTeamStatic();const{data:allTasks}=await sb.from("tasks").select("id, name, require_chrome_focus, is_dialer_task, category_id, archived_at").order("name");refreshExtensionTaskSections(allTasks||[]);const{data:assignments}=await sb.from("task_assignments").select("task_id");const counts={};(assignments||[]).forEach(a=>{counts[a.task_id]=(counts[a.task_id]||0)+1});const showArchived=$("showArchivedTasksToggle")&&$("showArchivedTasksToggle").checked;const tasks=(allTasks||[]).filter(t=>showArchived||!t.archived_at);const list=$("tasksList");list.innerHTML="";if(!tasks||tasks.length===0){list.innerHTML=`<div class="empty">${showArchived?"No tasks yet — add one above.":'No active tasks — add one above, or check "Show archived tasks" to see retired ones.'}</div>`;return}tasks.forEach(t=>{if(t.archived_at){const row2=document.createElement("div");row2.className="task-row";row2.style.cssText="flex-wrap:wrap; opacity:0.6;";row2.innerHTML=`
        <div style="flex:1; min-width:160px;"><span class="task-row-name">${escapeHtml(t.name)}</span><span class="task-row-count">Archived · ${counts[t.id]||0} historical assignment${counts[t.id]===1?"":"s"}</span></div>
        <button class="btn-secondary edit-only task-unarchive-btn" data-id="${t.id}" style="width:auto; padding:4px 12px; font-size:11px;">Unarchive</button>
      `;row2.querySelector(".task-unarchive-btn").addEventListener("click",()=>unarchiveTask(t.id,t.name));list.appendChild(row2);return}const row=document.createElement("div");row.className="task-row";row.style.flexWrap="wrap";row.innerHTML=`
      <div style="flex:1; min-width:160px;"><span class="task-row-name" contenteditable="true" data-id="${t.id}" data-original="${escapeHtml(t.name)}">${escapeHtml(t.name)}</span><span class="task-row-count">${counts[t.id]||0} assigned</span></div>
      <select class="task-category-select" data-id="${t.id}" title="Task category — sets idle policy (Standard/Dialer-exempt/Bounded grace) and role restriction" style="min-width:170px; margin-right:12px;"></select>
      <label style="display:flex; align-items:center; gap:6px; cursor:pointer; text-transform:none; font-family:var(--sans); font-size:11px; color:var(--text-dim); letter-spacing:0; margin-right:12px;">
        <input type="checkbox" class="require-focus-toggle" data-id="${t.id}" ${t.require_chrome_focus?"checked":""} style="width:14px; height:14px; accent-color:var(--focus);">
        Requires Chrome focus
      </label>
      <label style="display:flex; align-items:center; gap:6px; cursor:pointer; text-transform:none; font-family:var(--sans); font-size:11px; color:var(--text-dim); letter-spacing:0; margin-right:12px;" title="Legacy toggle — superseded by a Dialer-exempt category above, but still honored for tasks with no category assigned">
        <input type="checkbox" class="dialer-toggle" data-id="${t.id}" ${t.is_dialer_task?"checked":""} style="width:14px; height:14px; accent-color:var(--focus);">
        Phone / dialer task (legacy)
      </label>
      <button class="edit-only task-fields-toggle-btn" data-scope="task:${t.id}" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px; margin-right:12px;">${taskFieldsExpanded.has(`task:${t.id}`)?"Hide fields":"Manage fields"}</button>
      <button class="task-del-btn edit-only" data-id="${t.id}" title="Delete task">×</button>
      <div style="flex-basis:100%; margin-top:8px; padding-top:8px; border-top:1px solid var(--border);">
        <div style="display:flex; gap:6px; align-items:center; margin-bottom:6px;">
          <span class="mono" style="font-size:10px; color:var(--text-dim); text-transform:uppercase; letter-spacing:0.5px;">Expected domains</span>
          <input type="text" class="task-domain-input edit-only" data-id="${t.id}" placeholder="e.g. zillow.com" style="flex:0 0 160px; padding:4px 8px; font-size:11px;">
          <button class="btn-secondary task-domain-add-btn edit-only" data-id="${t.id}" style="width:auto; padding:4px 10px; font-size:11px;">Add</button>
        </div>
        <div class="task-domains-chips" data-id="${t.id}"></div>
      </div>
      <div class="task-fields-editor-slot" style="flex-basis:100%;"></div>
    `;const nameEl=row.querySelector(".task-row-name");nameEl.addEventListener("blur",()=>saveTaskRename(nameEl));nameEl.addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();nameEl.blur()}if(e.key==="Escape"){nameEl.textContent=nameEl.dataset.original;nameEl.blur()}});const categorySelect=row.querySelector(".task-category-select");categorySelect.innerHTML='<option value="">— No category (standard) —</option>'+taskCategoriesCache.map(c=>`<option value="${c.id}">${escapeHtml(c.name)}</option>`).join("");categorySelect.value=t.category_id||"";categorySelect.addEventListener("change",async e=>{const value=e.target.value||null;const{error}=await sb.from("tasks").update({category_id:value}).eq("id",t.id);if(!error){const catName=value?(taskCategoriesCache.find(c=>c.id===value)||{}).name:"no category";logActivity("task.update","task",t.id,`Set task "${t.name}" category to "${catName}"`)}});row.querySelector(".require-focus-toggle").addEventListener("change",async e=>{const checked=e.target.checked;const{error}=await sb.from("tasks").update({require_chrome_focus:checked}).eq("id",t.id);if(!error){logActivity("task.update","task",t.id,`${checked?"Enabled":"Disabled"} "requires Chrome focus" for "${t.name}"`)}});row.querySelector(".dialer-toggle").addEventListener("change",async e=>{const checked=e.target.checked;const{error}=await sb.from("tasks").update({is_dialer_task:checked}).eq("id",t.id);if(!error){logActivity("task.update","task",t.id,`${checked?"Enabled":"Disabled"} "phone/dialer task" for "${t.name}"`)}});row.querySelector(".task-del-btn").addEventListener("click",()=>deleteTask(t.id,t.name));row.querySelector(".task-fields-toggle-btn").addEventListener("click",()=>{const key=`task:${t.id}`;if(taskFieldsExpanded.has(key))taskFieldsExpanded.delete(key);else taskFieldsExpanded.add(key);loadTasksList()});if(taskFieldsExpanded.has(`task:${t.id}`)){const slot=row.querySelector(".task-fields-editor-slot");slot.style.cssText="flex-basis:100%; margin-top:8px; padding-top:8px; border-top:1px solid var(--border);";const hint=document.createElement("div");hint.className="mono";hint.style.cssText="font-size:10px; color:var(--text-dim); margin-bottom:8px;";hint.textContent="Fields specific to THIS task — if this task has any fields here, the extension uses these INSTEAD OF its category's fields entirely for this one task.";slot.appendChild(hint);const fieldsContainer=document.createElement("div");slot.appendChild(fieldsContainer);const addRow=document.createElement("div");addRow.innerHTML=sfFormFieldAddFormHtml();slot.appendChild(addRow);loadTaskScopeFields(`task:${t.id}`,{taskId:t.id}).then(()=>{renderTaskScopeFieldsList(`task:${t.id}`,fieldsContainer,{taskId:t.id});wireTaskScopeFieldAddForm(addRow,`task:${t.id}`,{taskId:t.id},fieldsContainer)})}const chipsContainer=row.querySelector(".task-domains-chips");loadTaskDomainsForRow(t.id,chipsContainer);const domainInput=row.querySelector(".task-domain-input");const domainAddBtn=row.querySelector(".task-domain-add-btn");const addTaskDomain=async()=>{const domain=normalizeDomainInput(domainInput.value||"");if(!domain||!domain.includes("."))return;const{error}=await sb.from("task_domains").insert({task_id:t.id,domain});if(!error){domainInput.value="";loadTaskDomainsForRow(t.id,chipsContainer);logActivity("task_domain.create","task",t.id,`Added expected domain "${domain}" to "${t.name}"`)}};domainAddBtn.addEventListener("click",addTaskDomain);domainInput.addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();addTaskDomain()}});list.appendChild(row)});applyEditGating("tasks")}async function loadDialerDomainsList(){invalidateTeamStatic();const{data:domains}=await sb.from("dialer_domains").select("id, domain").order("domain");const list=$("dialerDomainsList");list.innerHTML="";if(!domains||domains.length===0){list.innerHTML='<div class="empty">No dialer domains yet — add one above.</div>';return}domains.forEach(d=>{const row=document.createElement("div");row.className="task-row";row.innerHTML=`
      <div style="flex:1;"><span class="task-row-name">${escapeHtml(d.domain)}</span><span class="task-row-count">+ subdomains</span></div>
      <button class="task-del-btn edit-only" data-id="${d.id}" title="Remove domain">×</button>
    `;row.querySelector(".task-del-btn").addEventListener("click",()=>deleteDialerDomain(d.id,d.domain));list.appendChild(row)});applyEditGating("tasks")}function normalizeDomainInput(raw){let v=raw.trim().toLowerCase();v=v.replace(/^https?:\/\//,"").replace(/\/.*$/,"");return v}$("addDialerDomainBtn").addEventListener("click",async()=>{const input=$("newDialerDomainInput");const msg=$("dialerDomainsMsg");const domain=normalizeDomainInput(input.value||"");if(!domain||!domain.includes(".")){msg.textContent="Enter a valid domain, e.g. readymode.com";msg.className="msg error";return}const{error}=await sb.from("dialer_domains").insert({domain});if(error){msg.textContent=/duplicate|unique/i.test(error.message)?"That domain is already on the list.":error.message;msg.className="msg error";return}input.value="";msg.textContent="";loadDialerDomainsList();logActivity("dialer_domain.create",null,null,`Added dialer/CRM domain "${domain}"`)});$("newDialerDomainInput").addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();$("addDialerDomainBtn").click()}});async function deleteDialerDomain(id,domain){if(!confirm(`Remove "${domain}" from the dialer/CRM domain list?`))return;await sb.from("dialer_domains").delete().eq("id",id);loadDialerDomainsList();logActivity("dialer_domain.delete",null,null,`Removed dialer/CRM domain "${domain}"`)}async function loadMinExtensionVersion(){invalidateTeamStatic();const{data}=await sb.from("app_settings").select("value").eq("key","min_extension_version").maybeSingle();$("minExtensionVersionInput").value=data&&data.value&&data.value!=="0.0.0"?data.value:""}function isValidVersionString(v){return/^\d+(\.\d+){0,3}$/.test(v.trim())}$("saveMinExtensionVersionBtn").addEventListener("click",async()=>{const msg=$("extensionVersionMsg");const raw=$("minExtensionVersionInput").value.trim();const value=raw||"0.0.0";if(!isValidVersionString(value)){msg.textContent="Enter a version like 1.2.0, or leave blank for no minimum.";msg.className="msg error";return}const{error}=await sb.from("app_settings").upsert({key:"min_extension_version",value,updated_at:new Date().toISOString()});if(error){msg.textContent=error.message;msg.className="msg error";return}msg.textContent=raw?`Saved — users below v${raw} will be blocked and prompted to update.`:"Saved — no minimum version enforced.";msg.className="msg ok";logActivity("app_settings.update",null,null,`Set minimum extension version to ${raw||"(none)"}`)});async function loadActivityCeilings(){const{data}=await sb.from("app_settings").select("key,value").in("key",["activity_ceiling_mouse_per_min","activity_ceiling_keyboard_per_min"]);const byKey={};(data||[]).forEach(r=>{byKey[r.key]=r.value});$("activityCeilingMouseInput").value=byKey.activity_ceiling_mouse_per_min||"";$("activityCeilingKeyboardInput").value=byKey.activity_ceiling_keyboard_per_min||""}$("saveActivityCeilingsBtn").addEventListener("click",async()=>{const msg=$("activityCeilingsMsg");const mouseRaw=$("activityCeilingMouseInput").value.trim();const kbRaw=$("activityCeilingKeyboardInput").value.trim();for(const[label,raw]of[["Mouse",mouseRaw],["Keyboard",kbRaw]]){if(raw&&(!Number.isFinite(Number(raw))||Number(raw)<=0)){msg.textContent=`${label} ceiling must be a positive number, or blank to use the default.`;msg.className="msg error";return}}const now=new Date().toISOString();const{error}=await sb.from("app_settings").upsert([{key:"activity_ceiling_mouse_per_min",value:mouseRaw||null,updated_at:now},{key:"activity_ceiling_keyboard_per_min",value:kbRaw||null,updated_at:now}]);if(error){msg.textContent=error.message;msg.className="msg error";return}msg.textContent="Saved — takes effect for agents within about 5 minutes.";msg.className="msg ok";logActivity("app_settings.update",null,null,`Set activity ceilings to mouse=${mouseRaw||"(default)"}, keyboard=${kbRaw||"(default)"}`)});async function loadWebrtcTurnSettings(){const{data}=await sb.from("app_settings").select("key,value").in("key",["webrtc_turn_url","webrtc_turn_username","webrtc_turn_credential"]);const byKey={};(data||[]).forEach(r=>{byKey[r.key]=r.value});$("webrtcTurnUrlInput").value=byKey.webrtc_turn_url||"";$("webrtcTurnUsernameInput").value=byKey.webrtc_turn_username||"";$("webrtcTurnCredentialInput").value=byKey.webrtc_turn_credential||""}$("saveWebrtcTurnBtn").addEventListener("click",async()=>{const msg=$("webrtcTurnMsg");const url=$("webrtcTurnUrlInput").value.trim();const username=$("webrtcTurnUsernameInput").value.trim();const credential=$("webrtcTurnCredentialInput").value.trim();if(url&&!/^turns?:/.test(url)){msg.textContent="TURN URL should start with turn: or turns: (e.g. turn:your-server.example.com:3478).";msg.className="msg error";return}const now=new Date().toISOString();const{error}=await sb.from("app_settings").upsert([{key:"webrtc_turn_url",value:url||null,updated_at:now},{key:"webrtc_turn_username",value:username||null,updated_at:now},{key:"webrtc_turn_credential",value:credential||null,updated_at:now}]);if(error){msg.textContent=error.message;msg.className="msg error";return}msg.textContent=url?"Saved — new calls will use this TURN server.":"Saved — new calls will use STUN only.";msg.className="msg ok";logActivity("app_settings.update",null,null,`Set WebRTC TURN server to ${url||"(none — STUN only)"}`)});const DASHBOARD_TAB_KEYS=[{key:"time_management",label:"Time Management"},{key:"leads",label:"Leads"},{key:"call_activity",label:"Activity"},{key:"dialer",label:"Dialer"},{key:"dialer_admin",label:"Dialer Admin"},{key:"client_submissions",label:"Campaigns"},{key:"usermanagement",label:"User Management"},{key:"client_chat",label:"Client Chat (routing eligibility)"},{key:"finance",label:"Finance"},{key:"hr",label:"Human Resources"},{key:"data_science",label:"Data Science"},{key:"taxes",label:"Taxes"}];const TIME_MANAGEMENT_SECTIONS=[{key:"staff",label:"Dashboard"},{key:"timesheets",label:"Timesheets",editable:true},{key:"reports",label:"Reports",editable:true},{key:"flags",label:"Flags"}];function timeManagementSectionDefault(key){return false}function canViewTimeManagementSection(key){if(myPermissions.isProtected)return true;const s=myPermissions.timeManagementSections[key];return s&&typeof s.view==="boolean"?s.view:timeManagementSectionDefault(key)}function canEditTimeManagementSection(key){if(myPermissions.isProtected)return true;if(key==="staff")return myPermissions.canManageUsers;const s=myPermissions.timeManagementSections[key];return s&&typeof s.edit==="boolean"?s.edit:timeManagementSectionDefault(key)}function applyTimeManagementSectionEditGating(key){const panel=document.getElementById("tmSection-"+key);if(!panel)return;const editable=canEditTimeManagementSection(key);panel.querySelectorAll(".edit-only").forEach(el=>{el.disabled=!editable;el.style.opacity=editable?"":"0.4";el.style.cursor=editable?"":"not-allowed";el.title=editable?"":"Your role has view-only access to this section"})}function timeManagementSubtabBtns(){return Array.from(document.querySelectorAll(".tm-subtab-btn")).filter(b=>canViewTimeManagementSection(b.dataset.tmSection))}let tmActiveSection=null;function initTimeManagementTab(){document.querySelectorAll(".tm-subtab-btn").forEach(b=>{b.style.display=canViewTimeManagementSection(b.dataset.tmSection)?"":"none"});const usable=timeManagementSubtabBtns();const stillValid=usable.find(b=>b.dataset.tmSection===tmActiveSection);const target=stillValid?tmActiveSection:usable[0]?usable[0].dataset.tmSection:null;if(target)activateTimeManagementSection(target)}function activateTimeManagementSection(key){tmActiveSection=key;document.querySelectorAll(".tm-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.tmSection===key));document.querySelectorAll(".tm-section").forEach(s=>{s.style.display=s.id==="tmSection-"+key?"block":"none"});applyTimeManagementSectionEditGating(key);if(key==="staff"){currentDetailAgent=null;$("detailPanel").style.display="none";$("teamCard").style.display="block";loadTeam()}else if(key==="timesheets")loadTimesheets();else if(key==="reports")initReportsTab();else if(key==="flags")initFlagsTab()}document.querySelectorAll(".tm-subtab-btn").forEach(btn=>{btn.addEventListener("click",()=>{if(canViewTimeManagementSection(btn.dataset.tmSection))activateTimeManagementSection(btn.dataset.tmSection)})});const ACTIVITY_GROUPS=[{key:"staff_activity",label:"Staff Activity"}];let ACTIVITY_SECTIONS=[{key:"call",label:"Calls",group:"ghl"},{key:"sms",label:"Texts",group:"ghl"},{key:"appointment",label:"Appointments",group:"ghl"},{key:"rm_call",label:"Call Log",group:"readymode"},{key:"agent_report",label:"Agent Report",group:"readymode"},{key:"productivity",label:"Productivity",group:"readymode"},{key:"gmail_inbox",label:"Inbox",group:"gmail"},{key:"staff_activity",label:"Overview",group:"staff_activity"}];function activitySectionDefault(key){return key!=="staff_activity"}function canViewActivitySection(key){if(myPermissions.isProtected)return true;if(key==="gmail_inbox")return!!myPermissions.canTextClients;const s=myPermissions.activitySections[key];return s&&typeof s.view==="boolean"?s.view:activitySectionDefault(key)}let extensionTaskNameById={};function refreshExtensionTaskSections(tasks){const activeTasks=(tasks||[]).filter(t=>!t.archived_at).slice().sort((a,b)=>a.name.localeCompare(b.name));extensionTaskNameById={};activeTasks.forEach(t=>{extensionTaskNameById[t.id]=t.name});ACTIVITY_SECTIONS=ACTIVITY_SECTIONS.filter(s=>s.group!=="extension").concat(activeTasks.map(t=>({key:`task_${t.id}`,label:t.name,group:"extension"})));const container=$("actExtensionSubtabs");const noTasksMsg=$("actExtensionNoTasksMsg");if(!container)return;container.innerHTML="";activeTasks.forEach(t=>{const btn=document.createElement("button");btn.className="act-subtab-btn";btn.dataset.actKind=`task_${t.id}`;btn.dataset.actGroup="extension";btn.textContent=t.name;btn.addEventListener("click",()=>{if(!btn.disabled)activateActivityKind(btn.dataset.actKind)});container.appendChild(btn)});if(noTasksMsg)noTasksMsg.style.display=activeTasks.length?"none":""}const UM_SECTIONS=[{key:"users",label:"Users"},{key:"tasks",label:"Tasks"},{key:"dialer_domains",label:"Dialer Domains"},{key:"min_version",label:"Extension Version"},{key:"roles",label:"User Types"},{key:"clients",label:"Clients"},{key:"integrations",label:"Integrations"},{key:"notifications_setting",label:"Notifications Setting"},{key:"chat_settings",label:"Chat Settings"}];function umSectionDefault(key){return key!=="roles"&&key!=="notifications_setting"&&key!=="clients"}function canViewUmSection(key){if(key==="clients")return myIsOwner||!!(myPermissions.umSections.clients&&myPermissions.umSections.clients.view);if(myPermissions.isProtected)return true;const s=myPermissions.umSections[key];return s&&typeof s.view==="boolean"?s.view:umSectionDefault(key)}function canEditUmSection(key){if(key==="clients")return myIsOwner||!!(myPermissions.umSections.clients&&myPermissions.umSections.clients.edit);if(myPermissions.isProtected)return true;const s=myPermissions.umSections[key];return s&&typeof s.edit==="boolean"?s.edit:umSectionDefault(key)}const LEADS_SECTIONS=[{key:"dashboard",label:"Leads Dashboard"},{key:"submission_forms",label:"Lead Submission"},{key:"submission_forms_creator",label:"Submission Forms Creator"},{key:"queue_review",label:"Leads Queue Review"},{key:"leads_dispute",label:"Dispute/Feedback"},{key:"queue_review_ispeedtolead",label:"Leads Queue Review — PrimeHome Buyers"},{key:"tickets_review",label:"Tickets Review"},{key:"call_back",label:"Call Back"},{key:"all_queue",label:"All Queue"}];function leadsSectionDefault(key){return key!=="submission_forms_creator"&&key!=="all_queue"}function canViewLeadsSection(key){if(myPermissions.isProtected)return true;const s=myPermissions.leadsSections[key];return s&&typeof s.view==="boolean"?s.view:leadsSectionDefault(key)}function canEditLeadsSection(key){if(myPermissions.isProtected)return true;const s=myPermissions.leadsSections[key];return s&&typeof s.edit==="boolean"?s.edit:leadsSectionDefault(key)}const CSUB_SECTIONS=[{key:"dashboard",label:"Dashboard"},{key:"data",label:"Data Form"},{key:"qualification",label:"Client Qualification Form"},{key:"campaign",label:"Campaign Details"},{key:"campaign_guide",label:"Campaign Guide"},{key:"campaign_progress",label:"Campaign Progress"},{key:"readymode_upload_list",label:"ReadyMode Upload List"},{key:"marketing",label:"Email Marketing"}];function csubSectionDefault(key){return key!=="campaign_guide"&&key!=="marketing"}function csubEditSectionDefault(){return false}function canViewCsubSection(key){if(myPermissions.isProtected)return true;const s=myPermissions.csubSections[key];return s&&typeof s.view==="boolean"?s.view:csubSectionDefault(key)}function canEditCsubSection(key){if(myPermissions.isProtected)return true;const s=myPermissions.csubSections[key];return s&&typeof s.edit==="boolean"?s.edit:csubEditSectionDefault(key)}const DATA_SCIENCE_SECTIONS=[{key:"list_orders",label:"List Orders"},{key:"skiptrace",label:"Skip Trace"},{key:"data_orders",label:"Data Orders"}];function dataSciSectionDefault(){return true}function dataSciEditSectionDefault(key){return key==="data_orders"}function canViewDataSciSection(key){if(key==="list_builder"||key==="overview")return myIsOwner||myRole==="admin";if(myPermissions.isProtected)return true;const s=myPermissions.dataScienceSections[key];return s&&typeof s.view==="boolean"?s.view:dataSciSectionDefault()}function canEditDataSciSection(key){if(key==="list_builder"||key==="overview")return myIsOwner||myRole==="admin";if(myPermissions.isProtected)return true;const s=myPermissions.dataScienceSections[key];return s&&typeof s.edit==="boolean"?s.edit:dataSciEditSectionDefault(key)}function permissionFamilies(){const alwaysEdit=()=>true;const perSectionEdit=sec=>!!sec.editable;return[{tab:"time_management",family:"time_management_sections",prop:"timeManagementSections",cls:"tm-section",sections:TIME_MANAGEMENT_SECTIONS,viewDefault:timeManagementSectionDefault,editDefault:()=>true,hasEdit:perSectionEdit,note:'Dashboard is view-only for every user type — editing there follows "Can manage users", which is Owner/Admin only.'},{tab:"leads",family:"leads_sections",prop:"leadsSections",cls:"leads-section",sections:LEADS_SECTIONS,viewDefault:leadsSectionDefault,editDefault:leadsSectionDefault,hasEdit:alwaysEdit,note:"View/Edit here is the whole story — there is no separate whole-tab Edit toggle. Deleting a lead stays Owner/Admin only."},{tab:"call_activity",family:"activity_sections",prop:"activitySections",cls:"activity-section",sections:typeof ACTIVITY_SECTIONS!=="undefined"?ACTIVITY_SECTIONS.filter(x=>x.group!=="gmail"):[],viewDefault:activitySectionDefault,editDefault:()=>false,hasEdit:()=>false,groups:typeof ACTIVITY_GROUPS!=="undefined"?ACTIVITY_GROUPS.filter(g=>g.key!=="gmail"):null,note:'Every report here is read-only, so there is no Edit column. The Gmail Inbox follows "Can text clients" instead.'},{tab:"dialer_admin",family:"dialer_admin_sections",prop:"dialerAdminSections",cls:"da-section",sections:DIALER_ADMIN_SECTIONS,viewDefault:dialerAdminSectionDefault,editDefault:dialerAdminEditSectionDefault,hasEdit:perSectionEdit,note:"Saving also sets the database’s own dialer rights to match. Ordering phone numbers stays Owner/Admin only."},{tab:"client_submissions",family:"csub_sections",prop:"csubSections",cls:"csub-section",sections:CSUB_SECTIONS,viewDefault:csubSectionDefault,editDefault:csubEditSectionDefault,hasEdit:alwaysEdit,note:"Delete and Export CSV on every subtab stay Owner/Admin only, whatever is ticked here."},{tab:"usermanagement",family:"um_sections",prop:"umSections",cls:"um-section",sections:UM_SECTIONS,viewDefault:umSectionDefault,editDefault:umSectionDefault,hasEdit:alwaysEdit,note:"User Types is the section that hands out further access — whoever has it can only ever grant what they hold themselves."},{tab:"finance",family:"finance_sections",prop:"financeSections",cls:"finance-section",sections:FINANCE_SECTIONS,viewDefault:financeSectionDefault,editDefault:financeEditSectionDefault,hasEdit:perSectionEdit,note:"Edit is what lets a user type actually charge, refund or change pricing. iSpeedToLead Postbacks is Owner-only and is not listed."},{tab:"hr",family:"hr_sections",prop:"hrSections",cls:"hr-section",sections:HR_SECTIONS,viewDefault:hrSectionDefault,editDefault:hrEditSectionDefault,hasEdit:perSectionEdit,note:"Candidate Submission’s Edit is what lets somebody submit a candidate; Candidate Review’s is what lets them approve or reject."},{tab:"data_science",family:"data_science_sections",prop:"dataScienceSections",cls:"datasci-section",sections:DATA_SCIENCE_SECTIONS,viewDefault:()=>dataSciSectionDefault(),editDefault:dataSciEditSectionDefault,hasEdit:alwaysEdit,note:"List Builder and Overview are Owner/Admin only in the database too, so they are not grantable here."},{tab:"taxes",family:"taxes_sections",prop:"taxesSections",cls:"taxes-section",sections:TAXES_SECTIONS,viewDefault:()=>true,editDefault:()=>false,hasEdit:()=>false,note:"All three are read-only reports, so there is no Edit column."}]}function permissionFamilyForTab(tabKey){return permissionFamilies().find(f=>f.tab===tabKey)||null}function ceilingAllowsTab(key){if(!myGrantCeiling)return true;return(myGrantCeiling.allowedTabs||[]).includes(key)}function ceilingAllowsTabEdit(key){if(!myGrantCeiling)return true;if(!ceilingAllowsTab(key))return false;const v=myGrantCeiling.tab_edit?myGrantCeiling.tab_edit[key]:void 0;return typeof v==="boolean"?v:true}function ceilingAllowsSection(fam,key,mode){if(!myGrantCeiling)return true;if(!ceilingAllowsTab(fam.tab))return false;const map=myGrantCeiling[fam.family]||{};const cur=map[key];if(cur&&typeof cur[mode]==="boolean")return cur[mode];return mode==="edit"?!!fam.editDefault(key):!!fam.viewDefault(key)}function ceilingAllowsFlag(name){if(!myGrantCeiling)return true;return!!myGrantCeiling[name]}const CEILING_TITLE="Only the Owner can grant this — it is not part of your own access.";let allRoles=[];let allViewableUsers=[];let editingRoleName=null;function permMatrixEl(rootId){return document.getElementById(rootId)}function permSectionValue(values,fam,key,mode){const cur=(values[fam.family]||{})[key];if(cur&&typeof cur[mode]==="boolean")return cur[mode];return mode==="edit"?!!fam.editDefault(key):!!fam.viewDefault(key)}function renderPermissionMatrix(rootId,values,opts){opts=opts||{};values=values||{};values.allowed_tabs=values.allowed_tabs||[];const root=permMatrixEl(rootId);if(!root)return;const inherited=opts.inherited||null;const cb=(cls,dataAttr,dataVal,checked,allowed,extraCls)=>{const dis=allowed?"":" disabled";const t=allowed?"":` title="${CEILING_TITLE}"`;return`<input type="checkbox" class="perm-cb ${cls}${extraCls?" "+extraCls:""}" ${dataAttr}="${dataVal}" ${checked?"checked":""}${dis}${t}>`};root.innerHTML=DASHBOARD_TAB_KEYS.map(t=>{const fam=permissionFamilyForTab(t.key);const tabOn=values.allowed_tabs.includes(t.key);const tabAllowed=ceilingAllowsTab(t.key);const editOn=tabOn&&values.tab_edit&&values.tab_edit[t.key]!==false;const editAllowed=ceilingAllowsTabEdit(t.key);const sections=fam?fam.sections:[];const granted=sections.filter(s=>permSectionValue(values,fam,s.key,"view")).length;const rowsFor=list=>list.map(s=>{const viewOn=permSectionValue(values,fam,s.key,"view");const viewAllowed=ceilingAllowsSection(fam,s.key,"view");const hasEdit=fam.hasEdit(s);const editVal=hasEdit?permSectionValue(values,fam,s.key,"edit"):false;const editAllowedS=hasEdit?ceilingAllowsSection(fam,s.key,"edit"):false;let custom=false;if(inherited){const inhV=permSectionValue(inherited,fam,s.key,"view");const inhE=hasEdit?permSectionValue(inherited,fam,s.key,"edit"):false;custom=inhV!==viewOn||hasEdit&&inhE!==editVal}const capped=!viewAllowed;return`
        <div class="perm-row${capped?" is-capped":""}">
          <div class="perm-row-label">${escapeHtml(s.label)}${custom?' <span class="perm-custom-tag">Custom</span>':""}${capped?' <span class="perm-capped-tag">Owner only</span>':""}</div>
          <div class="perm-cell">${cb(fam.cls+"-view-cb","data-section",s.key,viewOn,viewAllowed)}</div>
          <div class="perm-cell">${hasEdit?cb(fam.cls+"-edit-cb","data-section",s.key,editVal,editAllowedS):'<span class="perm-cell-note">view only</span>'}</div>
        </div>`}).join("");const body=!fam?"":`
      <div class="perm-body" data-perm-body="${t.key}"${tabOn?"":' style="display:none;"'}>
        ${fam.note?`<div class="perm-note">${fam.note}</div>`:""}
        ${fam.groups?fam.groups.map(g=>{const list=sections.filter(s=>s.group===g.key);return list.length?`<div class="perm-group-label">${escapeHtml(g.label)}</div>${rowsFor(list)}`:""}).join(""):rowsFor(sections)}
      </div>`;return`
      <div class="perm-tab-card${tabOn?"":" is-off"}" data-perm-tab-card="${t.key}">
        <div class="perm-tab-head${tabAllowed?"":" is-capped"}">
          <div class="perm-tab-title">
            ${fam?`<button type="button" class="perm-disclose" data-perm-disclose="${t.key}" aria-label="Show or hide subtabs">${tabOn?"&#9662;":"&#9656;"}</button>`:'<span class="perm-disclose"></span>'}
            <label style="display:flex; align-items:center; gap:8px; cursor:pointer; text-transform:none; font-family:var(--sans); letter-spacing:0; margin:0; min-width:0;">
              ${cb("role-tab-cb","data-tab",t.key,tabOn,tabAllowed)}
              <span class="perm-tab-name">${escapeHtml(t.label)}</span>
            </label>
            ${fam?`<span class="perm-tab-count" data-perm-count="${t.key}">${granted}/${sections.length}</span>`:""}
            ${tabAllowed?"":'<span class="perm-capped-tag">Owner only</span>'}
          </div>
          <div class="perm-col-label">${fam?"View":""}</div>
          <div class="perm-cell">${t.editable?cb("role-tab-edit-cb","data-tab",t.key,editOn,editAllowed):fam?'<span class="perm-col-label">Edit</span>':""}</div>
        </div>
        ${body}
      </div>`}).join("");root._permValues=values;permBindMatrix(root);permRefreshCounts(root)}function permBindMatrix(root){root.querySelectorAll(".perm-disclose[data-perm-disclose]").forEach(btn=>{btn.addEventListener("click",()=>{const body=root.querySelector(`[data-perm-body="${btn.dataset.permDisclose}"]`);if(!body)return;const open=body.style.display!=="none";body.style.display=open?"none":"";btn.innerHTML=open?"&#9656;":"&#9662;"})});root.querySelectorAll(".role-tab-cb").forEach(box=>{box.addEventListener("change",()=>{const key=box.dataset.tab;const card=root.querySelector(`[data-perm-tab-card="${key}"]`);const body=root.querySelector(`[data-perm-body="${key}"]`);const arrow=root.querySelector(`[data-perm-disclose="${key}"]`);if(card)card.classList.toggle("is-off",!box.checked);if(body)body.style.display=box.checked?"":"none";if(arrow)arrow.innerHTML=box.checked?"&#9662;":"&#9656;";const editBox=root.querySelector(`.role-tab-edit-cb[data-tab="${key}"]`);if(editBox&&!editBox.disabled)editBox.checked=box.checked})});root.querySelectorAll('input[class*="-view-cb"], input[class*="-edit-cb"]').forEach(box=>{box.addEventListener("change",()=>permRefreshCounts(root))})}function permRefreshCounts(root){permissionFamilies().forEach(fam=>{const el=root.querySelector(`[data-perm-count="${fam.tab}"]`);if(!el)return;const boxes=Array.from(root.querySelectorAll(`.${fam.cls}-view-cb`));el.textContent=`${boxes.filter(b=>b.checked).length}/${boxes.length}`})}function permApplyBulk(root,action){if(action==="expand"||action==="collapse"){root.querySelectorAll("[data-perm-body]").forEach(b=>{b.style.display=action==="expand"?"":"none"});root.querySelectorAll("[data-perm-disclose]").forEach(a=>{a.innerHTML=action==="expand"?"&#9662;":"&#9656;"});return}const set=(sel,val)=>root.querySelectorAll(sel).forEach(b=>{if(!b.disabled)b.checked=val});if(action==="view-all"){set(".role-tab-cb",true);set('input[class*="-view-cb"]',true)}else if(action==="edit-all"){set(".role-tab-cb",true);set('input[class*="-view-cb"]',true);set(".role-tab-edit-cb",true);set('input[class*="-edit-cb"]',true)}else if(action==="clear"){set(".role-tab-cb",false);set(".role-tab-edit-cb",false);set('input[class*="-view-cb"]',false);set('input[class*="-edit-cb"]',false)}root.querySelectorAll(".role-tab-cb").forEach(b=>b.dispatchEvent(new Event("change")));permRefreshCounts(root)}function permReadMatrix(rootId){const root=permMatrixEl(rootId);const out={allowed_tabs:[],tab_edit:{}};if(!root)return out;const orig=root._permValues||{};root.querySelectorAll(".role-tab-cb").forEach(b=>{const on=b.disabled?(orig.allowed_tabs||[]).includes(b.dataset.tab):b.checked;if(on)out.allowed_tabs.push(b.dataset.tab)});root.querySelectorAll(".role-tab-edit-cb").forEach(b=>{if(b.disabled){const stored=(orig.tab_edit||{})[b.dataset.tab];if(typeof stored==="boolean")out.tab_edit[b.dataset.tab]=stored}else{out.tab_edit[b.dataset.tab]=b.checked}});permissionFamilies().forEach(fam=>{const map={};const origMap=orig[fam.family]||{};fam.sections.forEach(sec=>{const v=root.querySelector(`.${fam.cls}-view-cb[data-section="${sec.key}"]`);const e=root.querySelector(`.${fam.cls}-edit-cb[data-section="${sec.key}"]`);const stored=origMap[sec.key]||{};const entry={};if(v&&!v.disabled)entry.view=v.checked;else if(typeof stored.view==="boolean")entry.view=stored.view;if(fam.hasEdit(sec)){if(e&&!e.disabled)entry.edit=e.checked;else if(typeof stored.edit==="boolean")entry.edit=stored.edit}else if(typeof stored.edit==="boolean")entry.edit=stored.edit;if(Object.keys(entry).length)map[sec.key]=entry});out[fam.family]=map});return out}function renderRoleTabsPicker(selectedTabs,tabEdit,umSections,activitySections,leadsSections,csubSections,financeSections,hrSections,dataScienceSections,taxesSections,timeManagementSections,dialerAdminSections){renderPermissionMatrix("roleTabsPicker",{allowed_tabs:selectedTabs||[],tab_edit:tabEdit||{},um_sections:umSections||{},activity_sections:activitySections||{},leads_sections:leadsSections||{},csub_sections:csubSections||{},finance_sections:financeSections||{},hr_sections:hrSections||{},data_science_sections:dataScienceSections||{},taxes_sections:taxesSections||{},time_management_sections:timeManagementSections||{},dialer_admin_sections:dialerAdminSections||{}})}function renderRoleViewUsersPicker(selectedIds,filterType){selectedIds=selectedIds||[];const picker=$("roleViewUsersPicker");const types=Array.from(new Set(allViewableUsers.map(u=>u.role||"user"))).sort();const shown=filterType?allViewableUsers.filter(u=>(u.role||"user")===filterType):allViewableUsers;const filterBar=`
    <div style="width:100%; margin-bottom:8px; display:flex; align-items:center; gap:8px;">
      <span style="font-size:10px; color:var(--text-dim); text-transform:uppercase; letter-spacing:0.5px;">Filter by type</span>
      <select id="roleViewUsersTypeFilter" style="width:auto; padding:4px 8px; margin-top:0; font-size:12px;">
        <option value="">All types</option>
        ${types.map(t=>`<option value="${t}" ${filterType===t?"selected":""}>${roleDisplayLabel(t)}</option>`).join("")}
      </select>
    </div>`;picker.innerHTML=filterBar+(shown.length?shown.map(u=>`
        <label style="display:flex; align-items:center; gap:6px; cursor:pointer; text-transform:none; font-family:var(--sans); font-size:12px; color:var(--text); letter-spacing:0;">
          <input type="checkbox" class="role-view-user-cb" value="${u.id}" ${selectedIds.includes(u.id)?"checked":""} style="width:14px; height:14px; accent-color:var(--focus);"> ${escapeHtml(u.full_name||u.email)} <span class="type-badge" style="opacity:0.75;">${roleDisplayLabel(u.role||"user")}</span>
        </label>
      `).join(""):'<div class="empty" style="padding:4px 0;">No users of this type.</div>');const filterEl=$("roleViewUsersTypeFilter");if(filterEl){filterEl.addEventListener("change",()=>{const keep=Array.from(document.querySelectorAll(".role-view-user-cb:checked")).map(cb=>cb.value);renderRoleViewUsersPicker(keep,filterEl.value||null)})}}function renderRoleViewSpecificRoleOptions(selected){const sel=$("roleViewSpecificRoleSelect");if(!sel)return;const types=Array.from(new Set(allViewableUsers.map(u=>u.role||"user"))).sort();sel.innerHTML=types.map(t=>`<option value="${t}" ${selected===t?"selected":""}>${roleDisplayLabel(t)}</option>`).join("")}$("roleViewScopeSelf").addEventListener("change",onRoleViewScopeComboChange);$("roleViewScopeSelected").addEventListener("change",onRoleViewScopeComboChange);$("roleViewScopeSpecificRole").addEventListener("change",onRoleViewScopeComboChange);$("roleViewScopeAll").addEventListener("change",onRoleViewScopeAllChange);function onRoleViewScopeAllChange(){if($("roleViewScopeAll").checked){$("roleViewScopeSelf").checked=false;$("roleViewScopeSelected").checked=false;$("roleViewScopeSpecificRole").checked=false}[$("roleViewScopeSelf"),$("roleViewScopeSelected"),$("roleViewScopeSpecificRole")].forEach(cb=>cb.disabled=$("roleViewScopeAll").checked);updateRoleViewScopeVisibility()}function onRoleViewScopeComboChange(){if(this&&this.checked){$("roleViewScopeAll").checked=false}updateRoleViewScopeVisibility()}function updateRoleViewScopeVisibility(){$("roleViewUsersPicker").style.display=$("roleViewScopeSelected").checked?"flex":"none";$("roleViewSpecificRoleWrap").style.display=$("roleViewScopeSpecificRole").checked?"block":"none"}function resetRoleForm(){editingRoleName=null;$("rolesFormTitle").textContent="Add a user type";$("roleNameInput").value="";$("roleNameInput").disabled=false;renderRoleTabsPicker([],{},{},{},{},{},{},{},{},{},{},{});renderRoleTaskCategoriesPicker([]);$("roleCanTextClientsCb").checked=false;$("roleCanEditCompensationCb").checked=false;$("roleCanManualDialCb").checked=false;$("roleCanManualDialCb").disabled=!ceilingAllowsFlag("can_manual_dial");$("roleCanManualDialCb").title="";$("roleViewScopeSelf").checked=true;$("roleViewScopeSelected").checked=false;$("roleViewScopeSpecificRole").checked=false;$("roleViewScopeAll").checked=false;$("roleViewScopeAll").disabled=false;[$("roleViewScopeSelf"),$("roleViewScopeSelected"),$("roleViewScopeSpecificRole")].forEach(cb=>cb.disabled=false);renderRoleViewUsersPicker([]);renderRoleViewSpecificRoleOptions(null);updateRoleViewScopeVisibility();$("roleCancelEditBtn").style.display="none";if($("roleDeleteBtn"))$("roleDeleteBtn").style.display="none";$("rolesMsg").textContent="";if(typeof renderRolesSideList==="function")renderRolesSideList()}function renderRoleTaskCategoriesPicker(selectedIds){const wrap=$("roleTaskCategoriesPicker");if(!taskCategoriesCache.length){wrap.innerHTML='<div style="font-size:11px; color:var(--text-dim);">No task categories set up yet — add some under User Management → Tasks.</div>';return}wrap.innerHTML=taskCategoriesCache.map(c=>`
    <label style="display:flex; align-items:center; gap:6px; cursor:pointer; text-transform:none; font-family:var(--sans); font-size:12px; color:var(--text); letter-spacing:0;">
      <input type="checkbox" class="role-task-category-cb" value="${c.id}" ${selectedIds.includes(c.id)?"checked":""} style="width:14px; height:14px; accent-color:var(--focus);">
      ${escapeHtml(c.name)} <span class="mono" style="font-size:10px; color:var(--text-dim);">(${idlePolicyLabel(c.idle_policy)})</span>
    </label>
  `).join("")}async function initRolesTab(){const{data:users}=await sb.from("profiles").select("id, full_name, email, role").order("full_name");allViewableUsers=(users||[]).filter(u=>!PROTECTED_ROLES.has(u.role));if(!taskCategoriesCache.length){const{data:categories}=await sb.from("task_categories").select("id, name, idle_policy, grace_seconds").order("name");taskCategoriesCache=categories||[]}resetRoleForm();await permLoadOverrides();await loadRolesList();permInitRolesSection()}let umActiveSection=null;function initUserManagementTab(){document.querySelectorAll(".um-subtab-btn").forEach(btn=>{btn.style.display=canViewUmSection(btn.dataset.umSection)?"":"none"});const firstVisible=Array.from(document.querySelectorAll(".um-subtab-btn")).find(b=>b.style.display!=="none");const target=umActiveSection&&canViewUmSection(umActiveSection)?umActiveSection:firstVisible?firstVisible.dataset.umSection:null;if(target)activateUmSection(target)}function activateUmSection(key){umActiveSection=key;document.querySelectorAll(".um-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.umSection===key));document.querySelectorAll(".um-section").forEach(s=>{s.style.display=s.id==="um-section-"+key?"block":"none"});if(key==="users")loadUmUsersList();if(key==="tasks"){loadTaskCategoriesList();loadTasksList()}if(key==="dialer_domains")loadDialerDomainsList();if(key==="min_version"){loadMinExtensionVersion();loadActivityCeilings()}if(key==="roles")initRolesTab();if(key==="clients")loadUmClientsList();if(key==="integrations"){loadGhlConnections();loadGhlClientSmsLocationSetting();loadGhlAgentMap();loadGhlCalls();loadGhlLookupLocations();loadRmConnections();loadRmLeadPostingChannels();loadRmChannelClientOptions();loadRmAgentMap();loadRmEmailImports();loadRmLeadPushes();loadGmailConnections()}if(key==="notifications_setting"){loadStaffNotifRouting();loadClientNotifSettings()}if(key==="chat_settings"){loadWebrtcTurnSettings();const routingWrap=$("clientChatRoutingWrap");if(routingWrap){const canSeeRouting=myIsOwner||myRole==="admin";routingWrap.style.display=canSeeRouting?"":"none";if(canSeeRouting)loadClientChatRoutes()}}applyUmSectionEditGating(key)}document.querySelectorAll(".um-subtab-btn").forEach(btn=>{btn.addEventListener("click",()=>activateUmSection(btn.dataset.umSection))});function activateIntegSection(key){document.querySelectorAll(".integ-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.integSection===key));document.querySelectorAll(".integ-section").forEach(s=>s.classList.toggle("active",s.id==="integ-section-"+key))}document.querySelectorAll(".integ-subtab-btn").forEach(btn=>{btn.addEventListener("click",()=>activateIntegSection(btn.dataset.integSection))});function applyUmSectionEditGating(key){const panel=document.getElementById("um-section-"+key);if(!panel)return;const editable=canEditUmSection(key);panel.querySelectorAll(".edit-only").forEach(el=>{el.disabled=!editable;el.style.opacity=editable?"":"0.4";el.title=editable?"":"Your role has view-only access to this section"})}let chatLogsConversations=[];let chatLogsClientNames={};async function initChatLogsSection(){const sel=$("chatLogsConvSelect");sel.innerHTML='<option value="">Loading…</option>';$("chatLogsMessages").innerHTML="";const[{data:allProfiles},{data:allGroups},{data:clientThreads}]=await Promise.all([sb.from("profiles").select("id, full_name, email, role").order("full_name"),sb.from("chat_groups").select("id, name").order("name"),sb.from("client_chat_threads").select("client_id, status, assigned_staff_id, updated_at, client_accounts(full_name, company_name)").order("updated_at",{ascending:false})]);chatLogsConversations=[{key:"team",label:"Communication"}];const{data:dmPairs}=await sb.from("chat_messages").select("sender_id, recipient_id").not("recipient_id","is",null);const pairKeys=new Set;(dmPairs||[]).forEach(m=>{const pair=[m.sender_id,m.recipient_id].sort();pairKeys.add(pair.join("|"))});const nameOf=id=>{const p=(allProfiles||[]).find(x=>x.id===id);return p?p.full_name||p.email:"Unknown"};pairKeys.forEach(pk=>{const[a,b]=pk.split("|");chatLogsConversations.push({key:`dm:${a}:${b}`,label:`${nameOf(a)} ↔ ${nameOf(b)}`})});(allGroups||[]).forEach(g=>{chatLogsConversations.push({key:`group:${g.id}`,label:`${g.name} (group)`})});chatLogsClientNames={};(clientThreads||[]).forEach(t=>{const clientName=t.client_accounts&&(t.client_accounts.company_name?`${t.client_accounts.company_name} (${t.client_accounts.full_name})`:t.client_accounts.full_name)||"Client";chatLogsClientNames[t.client_id]=clientName;const statusTag=t.status==="closed"?" — closed":t.status==="routed"&&t.assigned_staff_id?` ↔ ${nameOf(t.assigned_staff_id)}`:"";chatLogsConversations.push({key:`client:${t.client_id}`,label:`${clientName}${statusTag}`})});sel.innerHTML='<option value="">Select a conversation…</option>'+chatLogsConversations.map(c=>`<option value="${escapeHtml(c.key)}">${escapeHtml(c.label)}</option>`).join("")}$("chatLogsConvSelect").addEventListener("change",async()=>{const key=$("chatLogsConvSelect").value;const box=$("chatLogsMessages");if(!key){box.innerHTML="";return}box.innerHTML='<div class="empty" style="padding:16px;">Loading…</div>';const openedLabel=(chatLogsConversations.find(c=>c.key===key)||{}).label||key;logActivity("chat_logs.view_conversation",null,null,`Viewed chat log: ${openedLabel}`);if(key.startsWith("client:")){const clientId=key.slice(7);const{data:msgs2,error:error2}=await sb.from("client_chat_messages").select("id, sender, staff_id, body, created_at, attachment_path, attachment_name, attachment_mime").eq("client_id",clientId).order("created_at",{ascending:true});if(error2){box.innerHTML=`<div class="msg error" style="padding:16px;">${escapeHtml(error2.message)}</div>`;return}const chatLogsClientTz=nonAgentDisplayTz();box.innerHTML=(msgs2||[]).map(m=>{const time=new Date(m.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...chatLogsClientTz?{timeZone:chatLogsClientTz}:{}});const sender=m.sender==="client"?chatLogsClientNames[clientId]||"Client":m.sender==="ai"?"AI Assistant":m.sender==="system"?"System note":chatContactName(m.staff_id);const bodyHtml=m.body?`<div class="chat-msg-bubble">${escapeHtml(m.body)}</div>`:"";const attachmentHtml=m.attachment_path?`<div class="chat-msg-attachment" data-attachment-path="${escapeHtml(m.attachment_path)}" data-attachment-mime="${escapeHtml(m.attachment_mime||"")}" data-attachment-name="${escapeHtml(m.attachment_name||"file")}" data-attachment-bucket="client-chat-attachments">Loading attachment…</div>`:"";return`
        <div class="chat-msg-row theirs">
          <div class="chat-msg-meta">${escapeHtml(sender)} · ${time}</div>
          ${bodyHtml}
          ${attachmentHtml}
        </div>
      `}).join("")||'<div class="empty" style="padding:16px;">No messages in this conversation.</div>';clientChatResolveVisibleAttachments(box);return}let query=sb.from("chat_messages").select("id, sender_id, recipient_id, group_id, body, created_at, attachment_path, attachment_name, attachment_mime").order("created_at",{ascending:true});if(key==="team")query=query.is("recipient_id",null).is("group_id",null);else if(key.startsWith("dm:")){const[,a,b]=key.split(":");query=query.or(`and(sender_id.eq.${a},recipient_id.eq.${b}),and(sender_id.eq.${b},recipient_id.eq.${a})`)}else if(key.startsWith("group:")){query=query.eq("group_id",key.slice(6))}const{data:msgs,error}=await query;if(error){box.innerHTML=`<div class="msg error" style="padding:16px;">${escapeHtml(error.message)}</div>`;return}const chatLogsInternalTz=nonAgentDisplayTz();box.innerHTML=(msgs||[]).map(m=>{const time=new Date(m.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...chatLogsInternalTz?{timeZone:chatLogsInternalTz}:{}});const sender=chatContactName(m.sender_id);const bodyHtml=m.body?`<div class="chat-msg-bubble">${escapeHtml(m.body)}</div>`:"";const attachmentHtml=m.attachment_path?`<div class="chat-msg-attachment" data-attachment-path="${escapeHtml(m.attachment_path)}" data-attachment-mime="${escapeHtml(m.attachment_mime||"")}" data-attachment-name="${escapeHtml(m.attachment_name||"file")}">Loading attachment…</div>`:"";return`
      <div class="chat-msg-row theirs">
        <div class="chat-msg-meta">${escapeHtml(sender)} · ${time}${myIsOwner?` <span class="chat-msg-del-btn" data-msg-id="${m.id}" title="Delete for everyone (owner only)">&#128465;</span>`:""}</div>
        ${bodyHtml}
        ${attachmentHtml}
      </div>
    `}).join("")||'<div class="empty" style="padding:16px;">No messages in this conversation.</div>';chatResolveVisibleAttachments(box);box.querySelectorAll(".chat-msg-del-btn").forEach(el=>{el.addEventListener("click",async()=>{if(await chatDeleteMessage(el.dataset.msgId))el.closest(".chat-msg-row").remove()})})});async function callGhlApi(payload){const{data,error}=await sb.functions.invoke("ghl-api",{body:payload});if(error){try{const body=await error.context.json();return{ok:false,error:body.error||error.message}}catch{return{ok:false,error:error.message}}}return data}async function callSendClientSms(clientId,kind,details){if(!clientId)return{ok:false,error:"No client id."};callSendClientEmail(clientId,kind,details);try{const{data,error}=await sb.functions.invoke("send-client-sms",{body:{clientId,kind,details:details||null}});if(error){let msg=error.message;try{const b=await error.context.json();msg=b.error||msg}catch{}console.warn("send-client-sms failed:",msg);return{ok:false,error:msg}}if(data&&!data.ok){console.warn("send-client-sms failed:",data.error);return{ok:false,error:data.error}}return{ok:true}}catch(e){console.warn("send-client-sms error:",e);return{ok:false,error:e.message||String(e)}}}async function callSendClientEmail(clientId,kind,details){if(!clientId||kind==="manual")return{ok:false,error:"Not applicable."};try{const{data,error}=await sb.functions.invoke("send-client-email",{body:{clientId,kind,details:details||null}});if(error){let msg=error.message;try{const b=await error.context.json();msg=b.error||msg}catch{}console.warn("send-client-email failed:",msg);return{ok:false,error:msg}}if(data&&!data.ok){console.warn("send-client-email failed:",data.error);return{ok:false,error:data.error}}return{ok:true}}catch(e){console.warn("send-client-email error:",e);return{ok:false,error:e.message||String(e)}}}async function notifyClientsForFormId(formId,kind,details){if(!formId)return;const{data:grants}=await sb.from("client_campaign_access").select("client_id").eq("form_id",formId);(grants||[]).forEach(g=>callSendClientSms(g.client_id,kind,details))}async function loadGhlConnections(){const list=$("ghlConnectionsList");list.innerHTML='<div class="empty">Loading…</div>';const result=await callGhlApi({action:"list_connections"});if(!result.ok){list.innerHTML=`<div class="msg error">${escapeHtml(result.error||"Failed to load connections.")}</div>`;return}const connections=result.connections||[];if(connections.length===0){list.innerHTML='<div class="empty">No locations connected yet.</div>';return}list.innerHTML=`
    <table>
      <thead><tr><th>Location</th><th>Connected</th><th>Type</th><th>Token status</th><th></th></tr></thead>
      <tbody>
        ${connections.map(c=>`
          <tr>
            <td>${escapeHtml(c.location_name||c.location_id)}</td>
            <td style="color:var(--text-2);">${new Date(c.created_at).toLocaleDateString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{})}</td>
            <td style="color:var(--text-2);">${c.is_private_token?"Private token":"OAuth2"}</td>
            <td style="color:${c.is_expired?"var(--away)":"var(--good)"};">${c.is_private_token?"Static (no expiry)":c.is_expired?"Will refresh on next use":"Active"}</td>
            <td><button class="task-del-btn ghl-disconnect-btn" data-location="${escapeHtml(c.location_id)}" title="Disconnect">&times;</button></td>
          </tr>
        `).join("")}
      </tbody>
    </table>
  `;list.querySelectorAll(".ghl-disconnect-btn").forEach(btn=>{btn.addEventListener("click",()=>disconnectGhlLocation(btn.dataset.location))})}async function loadGmailConnections(){const list=$("gmailConnectionsList");if(!list)return;list.innerHTML='<div class="empty">Loading…</div>';const result=await callGmailApi({action:"list_connections"});if(!result.ok){list.innerHTML=`<div class="msg error">${escapeHtml(result.error||"Failed to load connections.")}</div>`;return}const connections=result.connections||[];if(connections.length===0){list.innerHTML='<div class="empty">No Gmail inbox connected yet.</div>';return}list.innerHTML=`
    <table>
      <thead><tr><th>Inbox</th><th>Connected</th><th></th></tr></thead>
      <tbody>
        ${connections.map(c=>`
          <tr>
            <td>${escapeHtml(c.email_address)}</td>
            <td style="color:var(--text-2);">${new Date(c.created_at).toLocaleDateString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{})}</td>
            <td><button class="task-del-btn gmail-disconnect-btn" data-email="${escapeHtml(c.email_address)}" title="Disconnect">&times;</button></td>
          </tr>
        `).join("")}
      </tbody>
    </table>
  `;list.querySelectorAll(".gmail-disconnect-btn").forEach(btn=>{btn.addEventListener("click",()=>disconnectGmailInbox(btn.dataset.email))})}async function disconnectGmailInbox(emailAddress){if(!confirm(`Disconnect ${emailAddress}? Staff will no longer be able to send or read email through it.`))return;const result=await callGmailApi({action:"disconnect",emailAddress});if(!result.ok){alert(result.error||"Failed to disconnect.");return}logActivity("integration.disconnect","gmail_inbox",null,`Disconnected Gmail inbox ${emailAddress}`);await loadGmailConnections()}if($("gmailOauthConnectBtn"))$("gmailOauthConnectBtn").addEventListener("click",async()=>{const msg=$("gmailOauthMsg");const btn=$("gmailOauthConnectBtn");msg.textContent="";msg.className="msg";if(GMAIL_CLIENT_ID==="YOUR_GMAIL_CLIENT_ID"||GMAIL_REDIRECT_URI.includes("YOUR-PROJECT-REF")){msg.textContent="GMAIL_CLIENT_ID (and/or GMAIL_REDIRECT_URI) hasn’t been filled in in index.html yet — see the comment above ghlConnectUrl() for where.";msg.className="msg error";return}btn.disabled=true;const originalLabel=btn.textContent;btn.textContent="Starting…";const result=await callGmailApi({action:"create_oauth_state"});btn.disabled=false;btn.textContent=originalLabel;if(!result.ok){msg.textContent=result.error||"Failed to start the connect flow.";msg.className="msg error";return}window.open(gmailConnectUrl(result.state),"_blank","noopener")});async function loadGhlClientSmsLocationSetting(){const sel=$("ghlClientSmsLocationSelect");if(!sel)return;const[connResult,{data:setting}]=await Promise.all([callGhlApi({action:"list_connections"}),sb.from("app_settings").select("value").eq("key","client_sms_location_id").maybeSingle()]);const rows=connResult.ok?connResult.connections||[]:[];sel.innerHTML=rows.length?rows.map(c=>`<option value="${escapeHtml(c.location_id)}">${escapeHtml(c.location_name||c.location_id)}</option>`).join(""):`<option value="">${connResult.ok?"No locations connected yet":escapeHtml(connResult.error||"Failed to load connections")}</option>`;if(setting?.value&&rows.some(c=>c.location_id===setting.value))sel.value=setting.value}if($("ghlClientSmsLocationSaveBtn"))$("ghlClientSmsLocationSaveBtn").addEventListener("click",async()=>{const sel=$("ghlClientSmsLocationSelect");const msg=$("ghlClientSmsLocationMsg");const value=sel?sel.value:"";if(!value){msg.textContent="Connect a location first.";msg.className="msg error";return}const{error}=await sb.from("app_settings").upsert({key:"client_sms_location_id",value,updated_at:new Date().toISOString()});if(error){msg.textContent=error.message;msg.className="msg error";return}msg.textContent="Saved.";msg.className="msg";logActivity("app_settings.update",null,null,`Set client SMS location to ${sel.selectedOptions[0]?sel.selectedOptions[0].textContent:value}`)});const SMS_TEMPLATE_META=[{key:"ticket_received",settingKey:"client_sms_template_ticket_received",label:'Ticket sent to client ("Awaiting client Yes/No")',placeholders:"{client_name}, {campaign}",default:"PropTech AI: Hi {client_name}, you have a new lead ticket to review ({campaign}) — please log in to your portal to say Yes or No. Reply STOP to opt out.",emailSubjectDefault:"A new lead ticket needs your review",emailBodyDefault:"Hi {client_name},\n\nYou have a new lead ticket to review for {campaign}. Please log in to your portal to say Yes or No."},{key:"lead_received",settingKey:"client_sms_template_lead_received",label:"Lead accepted (pushed to client)",placeholders:"{client_name}, {campaign}",default:"PropTech AI: Hi {client_name}, a new lead has been added to your campaign ({campaign}). Log in to your portal to view it. Reply STOP to opt out.",emailSubjectDefault:"A new lead was added to your campaign",emailBodyDefault:"Hi {client_name},\n\nA new lead has been added to your campaign ({campaign}). Log in to your portal to view it."},{key:"dispute_response",settingKey:"client_sms_template_dispute_response",label:"Staff replied to a lead dispute",placeholders:"{client_name}",default:"PropTech AI: Hi {client_name}, we've replied to your lead dispute. Log in to your portal to view it. Reply STOP to opt out.",emailSubjectDefault:"We replied to your lead dispute",emailBodyDefault:"Hi {client_name},\n\nWe've replied to your lead dispute. Log in to your portal to view it."},{key:"feedback_response",settingKey:"client_sms_template_feedback_response",label:"Staff replied to feedback",placeholders:"{client_name}",default:"PropTech AI: Hi {client_name}, we've replied to your feedback. Log in to your portal to view it. Reply STOP to opt out.",emailSubjectDefault:"We replied to your feedback",emailBodyDefault:"Hi {client_name},\n\nWe've replied to your feedback. Log in to your portal to view it."},{key:"account_password",settingKey:"client_sms_template_account_password",label:"Account created / password reset (opt-in)",placeholders:"{client_name}, {password}",default:"PropTech AI: Hi {client_name}, your Client Portal password is {password}. Log in and you’ll be asked to set your own. Reply STOP to opt out.",emailSubjectDefault:"Your Client Portal account",emailBodyDefault:"Hi {client_name},\n\nYour Client Portal password is {password}. Log in and you’ll be asked to set your own."},{key:"campaign_confirm",settingKey:"client_sms_template_campaign_confirm",label:'"Text client directly" button (Client Chats)',placeholders:"{client_name}, {campaign}",default:"PropTech AI: Hi {client_name}, our team wants to connect with you for your campaign ({campaign}) — please log in https://clientportal.proptechnologyai.com/ > click on ask me button > to respond to the team. Reply STOP to opt out.",emailSubjectDefault:"Our team wants to connect about your campaign",emailBodyDefault:'Hi {client_name},\n\nOur team wants to connect with you about your campaign ({campaign}). Please log in to your portal and click "Ask me" to respond.'},{key:"portal_signup_welcome",settingKey:"client_sms_template_portal_signup_welcome",label:'Self-serve signup welcome (client portal "Sign up")',placeholders:"{client_name}",default:"PropTech AI: Hi {client_name}, your Client Portal account is ready! Log in anytime to place skip trace orders, buy a list, and track your account. Reply STOP to opt out.",emailSubjectDefault:"Your Client Portal account is ready",emailBodyDefault:"Hi {client_name},\n\nYour Client Portal account is ready! Log in anytime to place skip trace orders, buy a list, and track your account."},{key:"payment_failed",settingKey:"client_sms_template_payment_failed",label:"Subscription payment failed (card declined)",placeholders:"{client_name}, {campaign}",default:"PropTech AI: Hi {client_name}, we weren't able to process your latest payment for {campaign}. Please log in to update your card so your leads keep flowing without interruption: https://clientportal.proptechnologyai.com/ Reply STOP to opt out.",emailSubjectDefault:"Action needed: your payment failed",emailBodyDefault:"Hi {client_name},\n\nWe weren't able to process your latest payment for {campaign}. Please log in to update your card so your leads keep flowing without interruption."},{key:"list_request",settingKey:"client_sms_template_list_request",label:"Your list request (list uploaded by staff)",placeholders:"{client_name}",default:"PropTech AI: Hi {client_name}, your requested list is ready! Log in to your portal to download it. Reply STOP to opt out.",emailSubjectDefault:"Your list is ready",emailBodyDefault:"Hi {client_name},\n\nYour requested list is ready! Log in to your portal to download it."},{key:"skiptrace_request",settingKey:"client_sms_template_skiptrace_request",label:"Your skip trace request (file imported by staff)",placeholders:"{client_name}",default:"PropTech AI: Hi {client_name}, your skip trace results are ready! Log in to your portal to download them. Reply STOP to opt out.",emailSubjectDefault:"Your skip trace results are ready",emailBodyDefault:"Hi {client_name},\n\nYour skip trace order is complete. Results are attached as a CSV, and also available any time by logging in to your portal."}];let staffNotifAllRoleNames=[];async function loadStaffNotifRouting(){const list=$("staffNotifRoutingList");if(!list)return;list.innerHTML='<div class="empty">Loading…</div>';const{data:roles}=await sb.from("roles").select("name").order("name");staffNotifAllRoleNames=(roles||[]).map(r=>r.name);const cfg=await fetchStaffNotifRolesConfig();list.innerHTML=STAFF_NOTIF_KINDS.map(k=>{const selected=cfg[k.key];const chips=staffNotifAllRoleNames.map(name=>`
      <label style="display:inline-flex; align-items:center; gap:4px; margin:0 12px 6px 0; font-weight:400; font-size:11px;">
        <input type="checkbox" class="staff-notif-role-cb edit-only" data-kind="${k.key}" data-role="${escapeHtml(name)}" ${!selected||selected.includes(name)?"checked":""}>
        ${escapeHtml(name)}
      </label>`).join("");return`
      <div style="margin-bottom:16px; padding-bottom:14px; border-bottom:1px solid var(--border);">
        <div style="font-weight:600; font-size:12px; margin-bottom:6px;">${escapeHtml(k.label)}</div>
        <div>${chips||'<span style="color:var(--text-dim); font-size:11px;">No user types defined yet — add one under User Types.</span>'}</div>
      </div>`}).join("")}if($("staffNotifRoutingSaveBtn"))$("staffNotifRoutingSaveBtn").addEventListener("click",async()=>{const msg=$("staffNotifRoutingMsg");msg.textContent="";const btn=$("staffNotifRoutingSaveBtn");btn.disabled=true;const list=$("staffNotifRoutingList");const rows=STAFF_NOTIF_KINDS.map(k=>{const checked=Array.from(list.querySelectorAll(`.staff-notif-role-cb[data-kind="${k.key}"]:checked`)).map(cb=>cb.dataset.role);return{key:`staff_notif_roles_${k.key}`,value:JSON.stringify(checked),updated_at:new Date().toISOString()}});const{error}=await sb.from("app_settings").upsert(rows,{onConflict:"key"});btn.disabled=false;if(error){msg.textContent=error.message;msg.className="msg error";return}await fetchStaffNotifRolesConfig();await loadStaffNotifRouting();msg.textContent="Saved.";msg.className="msg";logActivity("app_settings.update",null,null,"Updated staff notification routing")});function clientNotifEnabledSettingKey(kind){return`client_notif_enabled_${kind}`}function clientNotifEmailSubjectKey(kind){return`client_email_subject_${kind}`}function clientNotifEmailBodyKey(kind){return`client_email_body_${kind}`}function clientNotifChannelSettingKey(kind){return`client_notif_channel_${kind}`}const CLIENT_NOTIF_CHANNELS=[{value:"sms",label:"SMS"},{value:"email",label:"Email"},{value:"notification_center",label:"Notification Center"}];function parseClientNotifChannels(raw){if(!raw)return CLIENT_NOTIF_CHANNELS.map(c=>c.value);const parts=String(raw).split(",").map(s=>s.trim()).filter(Boolean);const valid=parts.filter(p=>CLIENT_NOTIF_CHANNELS.some(c=>c.value===p));return valid.length?valid:CLIENT_NOTIF_CHANNELS.map(c=>c.value)}async function loadClientNotifSettings(){const list=$("clientNotifList");if(!list)return;list.innerHTML='<div class="empty">Loading…</div>';const allKeys=SMS_TEMPLATE_META.flatMap(t=>[t.settingKey,clientNotifEnabledSettingKey(t.key),clientNotifChannelSettingKey(t.key),clientNotifEmailSubjectKey(t.key),clientNotifEmailBodyKey(t.key)]);const{data:rows,error}=await sb.from("app_settings").select("key, value").in("key",allKeys);if(error){list.innerHTML=`<div class="msg error">${escapeHtml(error.message)}</div>`;return}const valueByKey={};(rows||[]).forEach(r=>{valueByKey[r.key]=r.value});sb.from("app_settings").select("value").eq("key","client_sms_from_number").maybeSingle().then(({data})=>{const el=$("clientSmsFromNumber");const d=String(data&&data.value||"").replace(/\D/g,"");if(el&&d.length===11&&d[0]==="1")el.textContent=`+1 (${d.slice(1,4)}) ${d.slice(4,7)}-${d.slice(7)}`;else if(el&&data&&data.value)el.textContent=data.value});list.innerHTML=SMS_TEMPLATE_META.map(t=>{const enabled=valueByKey[clientNotifEnabledSettingKey(t.key)]!=="false";const channels=parseClientNotifChannels(valueByKey[clientNotifChannelSettingKey(t.key)]);return`
    <div class="client-notif-row" data-key="${t.key}" style="margin-bottom:20px; padding-bottom:16px; border-bottom:1px solid var(--border);">
      <label style="display:flex; align-items:center; justify-content:space-between; flex-wrap:wrap; gap:8px;">
        <span style="display:flex; align-items:center; gap:8px;">
          <input type="checkbox" class="client-notif-enabled-cb edit-only" data-key="${t.key}" ${enabled?"checked":""}>
          <strong style="font-weight:600;">${escapeHtml(t.label)}</strong>
        </span>
        <span style="display:flex; align-items:center; gap:12px;">
          <span style="display:flex; align-items:center; gap:10px;">
            ${CLIENT_NOTIF_CHANNELS.map(c=>`
              <label style="display:flex; align-items:center; gap:4px; cursor:pointer; text-transform:none; font-family:var(--sans); font-size:11px; color:var(--text-2); letter-spacing:0; font-weight:400;">
                <input type="checkbox" class="client-notif-channel-cb edit-only" data-key="${t.key}" data-channel="${c.value}" ${channels.includes(c.value)?"checked":""} style="width:13px; height:13px; accent-color:var(--focus);"> ${c.label}
              </label>
            `).join("")}
          </span>
          <button type="button" class="btn-secondary sms-template-reset-btn edit-only" data-key="${t.key}" style="width:auto; padding:3px 10px; font-size:10px; margin-top:0; text-transform:none; letter-spacing:0; font-weight:400;">Reset to default</button>
        </span>
      </label>
      <div style="font-size:10px; color:var(--text-dim); margin:4px 0 6px;">Placeholders: <code>${t.placeholders}</code></div>
      <label style="font-size:10px; color:var(--text-dim); text-transform:uppercase; letter-spacing:0.5px;">SMS text</label>
      <textarea class="sms-template-input" data-key="${t.key}" rows="2" style="width:100%; font-size:13px;">${escapeHtml(valueByKey[t.settingKey]||t.default)}</textarea>
      <label style="font-size:10px; color:var(--text-dim); text-transform:uppercase; letter-spacing:0.5px; margin-top:8px; display:block;">Email subject</label>
      <input type="text" class="email-subject-input" data-key="${t.key}" style="width:100%; font-size:13px;" value="${escapeHtml(valueByKey[clientNotifEmailSubjectKey(t.key)]||t.emailSubjectDefault)}">
      <label style="font-size:10px; color:var(--text-dim); text-transform:uppercase; letter-spacing:0.5px; margin-top:8px; display:block;">Email body</label>
      <textarea class="email-body-input" data-key="${t.key}" rows="3" style="width:100%; font-size:13px;">${escapeHtml(valueByKey[clientNotifEmailBodyKey(t.key)]||t.emailBodyDefault)}</textarea>
    </div>`}).join("");list.querySelectorAll(".sms-template-reset-btn").forEach(btn=>{btn.addEventListener("click",()=>{const meta=SMS_TEMPLATE_META.find(t=>t.key===btn.dataset.key);if(!meta)return;const smsEl=list.querySelector(`.sms-template-input[data-key="${btn.dataset.key}"]`);const subjEl=list.querySelector(`.email-subject-input[data-key="${btn.dataset.key}"]`);const bodyEl=list.querySelector(`.email-body-input[data-key="${btn.dataset.key}"]`);if(smsEl)smsEl.value=meta.default;if(subjEl)subjEl.value=meta.emailSubjectDefault;if(bodyEl)bodyEl.value=meta.emailBodyDefault})})}if($("clientNotifSaveBtn"))$("clientNotifSaveBtn").addEventListener("click",async()=>{const msg=$("clientNotifMsg");msg.textContent="";const btn=$("clientNotifSaveBtn");btn.disabled=true;const list=$("clientNotifList");const rows=SMS_TEMPLATE_META.flatMap(t=>{const smsEl=list.querySelector(`.sms-template-input[data-key="${t.key}"]`);const subjEl=list.querySelector(`.email-subject-input[data-key="${t.key}"]`);const bodyEl=list.querySelector(`.email-body-input[data-key="${t.key}"]`);const enabledCb=list.querySelector(`.client-notif-enabled-cb[data-key="${t.key}"]`);const checkedChannels=Array.from(list.querySelectorAll(`.client-notif-channel-cb[data-key="${t.key}"]:checked`)).map(cb=>cb.dataset.channel);const now=new Date().toISOString();return[{key:t.settingKey,value:(smsEl?smsEl.value.trim():"")||t.default,updated_at:now},{key:clientNotifEmailSubjectKey(t.key),value:(subjEl?subjEl.value.trim():"")||t.emailSubjectDefault,updated_at:now},{key:clientNotifEmailBodyKey(t.key),value:(bodyEl?bodyEl.value.trim():"")||t.emailBodyDefault,updated_at:now},{key:clientNotifEnabledSettingKey(t.key),value:enabledCb&&enabledCb.checked?"true":"false",updated_at:now},{key:clientNotifChannelSettingKey(t.key),value:checkedChannels.length?checkedChannels.join(","):CLIENT_NOTIF_CHANNELS.map(c=>c.value).join(","),updated_at:now}]});const{error}=await sb.from("app_settings").upsert(rows,{onConflict:"key"});btn.disabled=false;if(error){msg.textContent=error.message;msg.className="msg error";return}await loadClientNotifSettings();msg.textContent="Saved.";msg.className="msg";logActivity("app_settings.update",null,null,"Updated client notification settings (SMS/email templates + on/off)")});if($("createPromoCodeBtn"))$("createPromoCodeBtn").addEventListener("click",async()=>{const msg=$("promoCodeMsg");msg.textContent="";msg.className="msg";const btn=$("createPromoCodeBtn");const code=$("promoCodeText").value.trim();const percentOff=$("promoPercentOff").value?Number($("promoPercentOff").value):null;const amountOffDollars=$("promoAmountOff").value?Number($("promoAmountOff").value):null;const maxRedemptions=$("promoMaxRedemptions").value?Number($("promoMaxRedemptions").value):null;const expiresAt=$("promoExpiresAt").value||null;if(!percentOff&&!amountOffDollars){msg.textContent="Enter either a % off or a $ off amount.";msg.className="msg error";return}if(percentOff&&amountOffDollars){msg.textContent="Choose either % off or $ off, not both.";msg.className="msg error";return}if(percentOff!==null&&(!Number.isFinite(percentOff)||percentOff<1||percentOff>100)){msg.textContent="% off must be a number between 1 and 100.";msg.className="msg error";return}if(amountOffDollars!==null&&(!Number.isFinite(amountOffDollars)||amountOffDollars<=0)){msg.textContent="$ off must be a positive number.";msg.className="msg error";return}if(maxRedemptions!==null&&(!Number.isFinite(maxRedemptions)||maxRedemptions<1)){msg.textContent="Max redemptions must be a positive number.";msg.className="msg error";return}btn.disabled=true;btn.textContent="Creating…";const{data,error}=await sb.functions.invoke("create-promo-code",{body:{code:code||void 0,percentOff:percentOff||void 0,amountOffCents:amountOffDollars?Math.round(amountOffDollars*100):void 0,maxRedemptions:maxRedemptions||void 0,expiresAt:expiresAt||void 0}});btn.disabled=false;btn.textContent="Create code";if(error||!data||data.ok===false){msg.textContent=data&&data.error||error&&error.message||"Failed to create code.";msg.className="msg error";return}msg.textContent=`Code created: ${data.code} — share this with the client to enter at checkout.`;msg.className="msg";$("promoCodeText").value="";$("promoPercentOff").value="";$("promoAmountOff").value="";$("promoMaxRedemptions").value="";$("promoExpiresAt").value="";logActivity("promo_code.create",null,null,`Created a Stripe discount code (${data.code})`);loadPromoCodesList()});async function loadPromoCodesList(){const tbody=$("promoCodesTbody");if(!tbody)return;const{data,error}=await sb.functions.invoke("create-promo-code",{body:{action:"list"}});if(error||!data||data.ok===false){tbody.innerHTML=`<tr><td colspan="6" style="color:var(--danger);">${escapeHtml(data&&data.error||error&&error.message||"Failed to load codes.")}</td></tr>`;return}const codes=data.codes||[];if(!codes.length){tbody.innerHTML='<tr><td colspan="6" style="color:var(--text-dim);">No discount codes yet.</td></tr>';return}const canEditFinance=canEditFinanceSection("stripe_discount_codes")&&myIsOwner;tbody.innerHTML=codes.map(c=>{const redeemed=c.maxRedemptions?`${c.timesRedeemed} / ${c.maxRedemptions}`:`${c.timesRedeemed}`;const expires=c.expiresAt?new Date(c.expiresAt).toLocaleDateString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{}):"Never";return`
    <tr>
      <td class="mono">${escapeHtml(c.code)}</td>
      <td>${escapeHtml(c.discount)}</td>
      <td>${redeemed}</td>
      <td style="color:${c.active?"var(--success, #2fa86b)":"var(--text-dim)"}; font-weight:600;">${c.active?"Active":"Deactivated"}</td>
      <td>${expires}</td>
      <td>${c.active&&canEditFinance?`<button class="btn-outline promo-code-deactivate-btn" data-id="${c.id}" style="width:auto; padding:5px 10px; font-size:11px; color:var(--danger);">Deactivate</button>`:""}</td>
    </tr>`}).join("")}if($("promoCodesTbody"))$("promoCodesTbody").addEventListener("click",async e=>{const btn=e.target.closest(".promo-code-deactivate-btn");if(!btn)return;if(!myIsOwner){alert("Only the Owner can deactivate a discount code.");return}if(!confirm("Deactivate this discount code? It will stop working immediately — existing redemptions are unaffected."))return;btn.disabled=true;const{data,error}=await sb.functions.invoke("create-promo-code",{body:{action:"deactivate",promotionCodeId:btn.dataset.id}});if(error||!data||data.ok===false){alert(data&&data.error||error&&error.message||"Failed to deactivate code.");btn.disabled=false;return}logActivity("promo_code.deactivate",null,null,"Deactivated a Stripe discount code");loadPromoCodesList()});let oneOffChargeClientsCache=[];async function loadOneOffChargeClientOptions(){const sel=$("oneOffChargeClientSelect");if(!sel)return;const{data,error}=await sb.from("client_accounts").select("id, full_name, email").eq("is_active",true).order("full_name");if(error){sel.innerHTML=`<option value="">Failed to load clients</option>`;return}oneOffChargeClientsCache=data||[];sel.innerHTML=`<option value="">Select a client…</option>`+oneOffChargeClientsCache.map(c=>`<option value="${c.id}">${escapeHtml(c.full_name)} (${escapeHtml(c.email)})</option>`).join("")}async function loadOneOffChargesList(){const tbody=$("oneOffChargesTbody");if(!tbody)return;const[{data:charges,error},{data:clients}]=await Promise.all([sb.from("client_one_off_charges").select("id, client_id, description, amount_cents, category, status, created_at, last_charge_error, last_charge_attempted_at, refunded_amount_cents").order("created_at",{ascending:false}).limit(200),sb.from("client_accounts").select("id, full_name, email")]);if(error){tbody.innerHTML=`<tr><td colspan="10" style="color:var(--danger);">${escapeHtml(error.message)}</td></tr>`;return}const clientById={};(clients||[]).forEach(c=>{clientById[c.id]=c});if(!charges||!charges.length){tbody.innerHTML=`<tr><td colspan="10" style="color:var(--text-dim);">No one-time charges yet.</td></tr>`;return}const statusColor={pending:"var(--warning, #d9a02c)",paid:"var(--success, #2fa86b)",canceled:"var(--text-dim)"};const categoryLabel={data:"Data",dialer:"Dialer",caller:"Caller",other:"Other"};const canEditFinance=canEditFinanceSection("stripe_payments");tbody.innerHTML=charges.map(c=>{const client=clientById[c.client_id];const errorLine=c.status==="pending"&&c.last_charge_error?`<div style="font-size:10px; color:var(--danger); font-weight:400; text-transform:none; margin-top:2px; max-width:220px;" title="${escapeHtml(c.last_charge_error)}">${escapeHtml(c.last_charge_error.length>70?c.last_charge_error.slice(0,70)+"…":c.last_charge_error)}</div>`:"";const refundedCents=c.refunded_amount_cents||0;const fullyRefunded=c.status==="paid"&&refundedCents>=c.amount_cents;const statusLabel=fullyRefunded?"refunded":c.status;const refundLine=c.status==="paid"&&refundedCents>0&&!fullyRefunded?`<div style="font-size:10px; color:var(--text-dim); font-weight:400; text-transform:none; margin-top:2px;">$${(refundedCents/100).toFixed(2)} refunded</div>`:"";const actionCell=!canEditFinance?"—":c.status==="pending"?`<button class="btn-outline one-off-charge-charge-now-btn" data-id="${c.id}" data-client-id="${c.client_id}" style="width:auto; padding:5px 10px; font-size:11px;">Charge now</button>`:c.status==="paid"&&!fullyRefunded?`<button class="btn-outline one-off-charge-refund-btn" data-id="${c.id}" data-remaining="${c.amount_cents-refundedCents}" style="width:auto; padding:5px 10px; font-size:11px;">Partial refund</button>`:"—";return`
    <tr>
      <td>${client?escapeHtml(client.full_name):"—"}</td>
      <td>${escapeHtml(c.description)}</td>
      <td>${c.category?escapeHtml(categoryLabel[c.category]||c.category):"—"}</td>
      <td>$${(c.amount_cents/100).toFixed(2)}</td>
      <td style="color:${statusColor[c.status]||"inherit"}; font-weight:600; text-transform:capitalize;">${escapeHtml(statusLabel)}${errorLine}${refundLine}</td>
      <td>${new Date(c.created_at).toLocaleDateString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{})}</td>
      <td>${c.status==="pending"&&canEditFinance?`<button class="btn-outline one-off-charge-link-btn" data-id="${c.id}" style="width:auto; padding:5px 10px; font-size:11px;">Get link</button>`:"—"}</td>
      <td>${actionCell}</td>
      <td>${c.status==="pending"&&canEditFinance?`<button class="btn-outline one-off-charge-cancel-btn" data-id="${c.id}" style="width:auto; padding:5px 10px; font-size:11px;">Cancel</button>`:""}</td>
      <td>${myIsOwner?`<button class="btn-outline one-off-charge-delete-btn" data-id="${c.id}" style="width:auto; padding:5px 10px; font-size:11px; color:var(--danger);">Delete</button>`:""}</td>
    </tr>`}).join("")}let oneOffChargeCardModalState=null;async function openOneOffChargeCardModal(chargeId,clientId){oneOffChargeCardModalState={chargeId};const listEl=$("oneOffChargeCardList");$("oneOffChargeCardModalMsg").style.display="none";listEl.innerHTML='<div style="font-size:12px; color:var(--text-dim); padding:10px 0;">Loading saved cards…</div>';$("oneOffChargeCardModalOverlay").classList.add("active");const{data,error}=await sb.functions.invoke("staff-manage-one-off-charge",{body:{action:"list_payment_methods",clientId}});if(!oneOffChargeCardModalState||oneOffChargeCardModalState.chargeId!==chargeId)return;if(error||!data||data.ok===false){listEl.innerHTML=`<div style="font-size:12px; color:var(--danger); padding:10px 0;">${escapeHtml(data&&data.error||error&&error.message||"Failed to load saved cards.")}</div>`;return}const methods=data.paymentMethods||[];if(!methods.length){listEl.innerHTML='<div style="font-size:12px; color:var(--text-dim); padding:10px 0;">This client has no saved card on file.</div>';return}const brandLabel={visa:"Visa",mastercard:"Mastercard",amex:"Amex",discover:"Discover"};listEl.innerHTML=methods.map(m=>`
    <div class="one-off-charge-card-row" data-pm-id="${escapeHtml(m.id)}" style="display:flex; justify-content:space-between; align-items:center; padding:8px 4px; border-bottom:1px solid var(--border); cursor:pointer;">
      <div style="font-size:12px;">${escapeHtml(brandLabel[m.brand]||m.brand)} •••• ${escapeHtml(m.last4)}${m.id===data.defaultPaymentMethodId?' <span style="color:var(--text-dim); font-size:10px;">(default)</span>':""}</div>
      <div class="mono" style="font-size:11px; color:var(--text-dim);">${m.expMonth?String(m.expMonth).padStart(2,"0"):"--"}/${m.expYear||"--"}</div>
    </div>
  `).join("");listEl.querySelectorAll(".one-off-charge-card-row").forEach(row=>{row.addEventListener("click",()=>chargeOneOffChargeWithCard(row.dataset.pmId))})}function closeOneOffChargeCardModal(){$("oneOffChargeCardModalOverlay").classList.remove("active");oneOffChargeCardModalState=null}$("oneOffChargeCardCancelBtn").addEventListener("click",closeOneOffChargeCardModal);$("oneOffChargeCardModalOverlay").addEventListener("click",e=>{if(e.target.id==="oneOffChargeCardModalOverlay")closeOneOffChargeCardModal()});async function chargeOneOffChargeWithCard(paymentMethodId){if(!oneOffChargeCardModalState)return;const{chargeId}=oneOffChargeCardModalState;if(!confirm("Charge this client's selected card now? This debits their card immediately — it is not reversible from here."))return;const msgEl=$("oneOffChargeCardModalMsg");msgEl.style.display="none";const{data,error}=await sb.functions.invoke("staff-manage-one-off-charge",{body:{action:"charge_now",chargeId,paymentMethodId}});if(error||!data||data.ok===false){msgEl.textContent=data&&data.error||error&&error.message||"Failed to charge this client.";msgEl.style.display="block";return}logActivity("one_off_charge.charge_now",null,null,`Charged a one-time charge directly (${chargeId})`);closeOneOffChargeCardModal();loadOneOffChargesList()}const STRIPE_PRICING_KEYS={pricePartTimeCaller:"stripe_price_part_time_caller_cents",priceFullTimeCaller:"stripe_price_full_time_caller_cents",priceDialer:"stripe_price_dialer_cents",priceDataPerRecord:"stripe_price_data_cents_per_record",dataRecordsPartTime:"stripe_data_records_part_time",dataRecordsFullTime:"stripe_data_records_full_time"};async function loadStripePricing(){const msg=$("stripePricingMsg");if(msg){msg.textContent="";msg.className="msg"}const keys=Object.values(STRIPE_PRICING_KEYS);const{data,error}=await sb.from("app_settings").select("key, value, updated_at").in("key",keys);if(error){if(msg){msg.textContent="Failed to load pricing: "+error.message;msg.className="msg error"}return}const byKey={};(data||[]).forEach(r=>{byKey[r.key]=r});const setDollarsInput=(id,settingKey,divisor)=>{const el=$(id);if(!el)return;const raw=byKey[settingKey]?Number(byKey[settingKey].value):null;el.value=raw!=null&&!Number.isNaN(raw)?raw/divisor:""};setDollarsInput("pricePartTimeCaller",STRIPE_PRICING_KEYS.pricePartTimeCaller,100);setDollarsInput("priceFullTimeCaller",STRIPE_PRICING_KEYS.priceFullTimeCaller,100);setDollarsInput("priceDialer",STRIPE_PRICING_KEYS.priceDialer,100);setDollarsInput("priceDataPerRecord",STRIPE_PRICING_KEYS.priceDataPerRecord,100);const setIntInput=(id,settingKey)=>{const el=$(id);if(!el)return;const raw=byKey[settingKey]?Number(byKey[settingKey].value):null;el.value=raw!=null&&!Number.isNaN(raw)?raw:""};setIntInput("dataRecordsPartTime",STRIPE_PRICING_KEYS.dataRecordsPartTime);setIntInput("dataRecordsFullTime",STRIPE_PRICING_KEYS.dataRecordsFullTime);const latest=Object.values(byKey).map(r=>r.updated_at).sort().pop();const updatedEl=$("stripePricingUpdatedAt");if(updatedEl)updatedEl.textContent=latest?`Last updated ${new Date(latest).toLocaleString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{})}`:""}if($("saveStripePricingBtn"))$("saveStripePricingBtn").addEventListener("click",async()=>{const msg=$("stripePricingMsg");const btn=$("saveStripePricingBtn");const dollarsToCents=id=>{const v=$(id).value;if(v===""||v==null)return null;const n=Number(v);return Number.isFinite(n)?Math.round(n*100):null};const intField=id=>{const v=$(id).value;if(v===""||v==null)return null;const n=Number(v);return Number.isFinite(n)?Math.round(n):null};const values={[STRIPE_PRICING_KEYS.pricePartTimeCaller]:dollarsToCents("pricePartTimeCaller"),[STRIPE_PRICING_KEYS.priceFullTimeCaller]:dollarsToCents("priceFullTimeCaller"),[STRIPE_PRICING_KEYS.priceDialer]:dollarsToCents("priceDialer"),[STRIPE_PRICING_KEYS.priceDataPerRecord]:dollarsToCents("priceDataPerRecord"),[STRIPE_PRICING_KEYS.dataRecordsPartTime]:intField("dataRecordsPartTime"),[STRIPE_PRICING_KEYS.dataRecordsFullTime]:intField("dataRecordsFullTime")};if(Object.values(values).some(v=>v==null||v<0)){msg.textContent="All fields are required and must be zero or greater.";msg.className="msg error";return}if(!confirm("Update live campaign pricing? This affects new checkouts and future add-on changes immediately."))return;btn.disabled=true;btn.textContent="Saving…";const{data,error}=await sb.functions.invoke("staff-save-stripe-pricing",{body:{values}});btn.disabled=false;btn.textContent="Save pricing";if(error||!data||data.ok===false){let apiMsg=data&&data.error;if(!apiMsg&&error&&error.context&&typeof error.context.json==="function"){try{const b=await error.context.json();apiMsg=b.error}catch(e){}}msg.textContent="Failed to save: "+(apiMsg||error&&error.message||"Unknown error");msg.className="msg error";return}msg.textContent="Pricing updated.";msg.className="msg";logActivity("app_settings.update",null,null,"Updated Stripe campaign pricing");loadStripePricing()});if($("createOneOffChargeBtn"))$("createOneOffChargeBtn").addEventListener("click",async()=>{const msg=$("oneOffChargeMsg");msg.textContent="";msg.className="msg";const btn=$("createOneOffChargeBtn");const clientId=$("oneOffChargeClientSelect").value;const description=$("oneOffChargeDescription").value.trim();const amountDollars=$("oneOffChargeAmount").value?Number($("oneOffChargeAmount").value):null;const category=$("oneOffChargeCategory").value||null;if(!clientId){msg.textContent="Select a client.";msg.className="msg error";return}if(!description){msg.textContent="Enter a description.";msg.className="msg error";return}if(!amountDollars||amountDollars<=0){msg.textContent="Enter a valid amount.";msg.className="msg error";return}btn.disabled=true;btn.textContent="Creating…";const{data,error}=await sb.functions.invoke("staff-manage-one-off-charge",{body:{action:"create",clientId,description,amountCents:Math.round(amountDollars*100),category}});btn.disabled=false;btn.textContent="Create charge";if(error||!data||data.ok===false){msg.textContent=data&&data.error||error&&error.message||"Failed to create charge.";msg.className="msg error";return}msg.textContent="Charge created — the client will see it on their Billing tab.";msg.className="msg";$("oneOffChargeClientSelect").value="";$("oneOffChargeDescription").value="";$("oneOffChargeAmount").value="";$("oneOffChargeCategory").value="";loadOneOffChargesList();logActivity("one_off_charge.create",null,clientId,`Created a one-time charge (${description}, $${amountDollars.toFixed(2)})`)});if($("oneOffChargesTbody"))$("oneOffChargesTbody").addEventListener("click",async e=>{const chargeNowBtn=e.target.closest(".one-off-charge-charge-now-btn");if(chargeNowBtn){openOneOffChargeCardModal(chargeNowBtn.dataset.id,chargeNowBtn.dataset.clientId);return}const refundBtn=e.target.closest(".one-off-charge-refund-btn");if(refundBtn){const remainingCents=Number(refundBtn.dataset.remaining)||0;const remainingDollars=(remainingCents/100).toFixed(2);const input=prompt(`Refund amount in dollars (up to $${remainingDollars} remaining on this charge):`,remainingDollars);if(input===null)return;const refundDollars=Number(input);if(!refundDollars||refundDollars<=0){alert("Enter a valid refund amount greater than $0.");return}const refundCents=Math.round(refundDollars*100);if(refundCents>remainingCents){alert(`Only $${remainingDollars} is left to refund on this charge.`);return}if(!confirm(`Refund $${refundDollars.toFixed(2)} to this client? This is issued directly through Stripe and cannot be undone from here.`))return;refundBtn.disabled=true;const originalText=refundBtn.textContent;refundBtn.textContent="Refunding…";const{data,error}=await sb.functions.invoke("staff-manage-one-off-charge",{body:{action:"refund",chargeId:refundBtn.dataset.id,amountCents:refundCents}});if(error||!data||data.ok===false){alert(data&&data.error||error&&error.message||"Failed to issue refund.");refundBtn.disabled=false;refundBtn.textContent=originalText;return}logActivity("one_off_charge.refund",null,null,`Refunded $${refundDollars.toFixed(2)} on a one-time charge (${refundBtn.dataset.id})`);loadOneOffChargesList();return}const cancelBtn=e.target.closest(".one-off-charge-cancel-btn");if(cancelBtn){if(!confirm("Cancel this charge? The client will no longer be able to pay it."))return;cancelBtn.disabled=true;const{data,error}=await sb.functions.invoke("staff-manage-one-off-charge",{body:{action:"cancel",chargeId:cancelBtn.dataset.id}});if(error||!data||data.ok===false){alert(data&&data.error||error&&error.message||"Failed to cancel charge.");cancelBtn.disabled=false;return}loadOneOffChargesList();return}const linkBtn=e.target.closest(".one-off-charge-link-btn");if(linkBtn){linkBtn.disabled=true;const originalText=linkBtn.textContent;linkBtn.textContent="Generating…";const{data,error}=await sb.functions.invoke("staff-manage-one-off-charge",{body:{action:"get_payment_link",chargeId:linkBtn.dataset.id}});linkBtn.disabled=false;linkBtn.textContent=originalText;if(error||!data||data.ok===false||!data.checkoutUrl){alert(data&&data.error||error&&error.message||"Failed to generate a payment link.");return}try{await navigator.clipboard.writeText(data.checkoutUrl)}catch{}prompt("Payment link (copied to clipboard if your browser allowed it) — share this with the client:",data.checkoutUrl);return}const deleteBtn=e.target.closest(".one-off-charge-delete-btn");if(deleteBtn){if(!confirm("Delete this charge record permanently? This does not affect Stripe — it only removes it from this list."))return;deleteBtn.disabled=true;const{error}=await sb.from("client_one_off_charges").delete().eq("id",deleteBtn.dataset.id);if(error){alert("Failed to delete: "+error.message);deleteBtn.disabled=false;return}loadOneOffChargesList()}});async function loadGhlAgentMap(){const box=$("ghlAgentMapList");if(!box)return;box.innerHTML='<div class="empty">Loading…</div>';const{data:agents,error}=await sb.from("profiles").select("id, full_name, email, ghl_user_id, role").order("full_name");if(error){box.innerHTML=`<div class="msg error">${escapeHtml(error.message)}</div>`;return}if(!agents||!agents.length){box.innerHTML='<div class="empty">No users found.</div>';return}box.innerHTML=`
    <table>
      <thead><tr><th>Agent</th><th>GoHighLevel user ID</th><th></th></tr></thead>
      <tbody>
        ${agents.map(a=>`
          <tr>
            <td>${escapeHtml(a.full_name||a.email||"—")}</td>
            <td><input type="text" class="ghl-map-input" data-agent="${a.id}" value="${escapeHtml(a.ghl_user_id||"")}" placeholder="GHL user ID" style="width:100%; max-width:280px;"></td>
            <td><button class="btn-secondary ghl-map-save edit-only" data-agent="${a.id}" style="width:auto; padding:5px 12px;">Save</button></td>
          </tr>
        `).join("")}
      </tbody>
    </table>
    <div class="msg" id="ghlMapMsg"></div>`;box.querySelectorAll(".ghl-map-save").forEach(btn=>{btn.addEventListener("click",async()=>{const agentId=btn.dataset.agent;const input=box.querySelector(`.ghl-map-input[data-agent="${agentId}"]`);const val=input?input.value.trim():"";btn.disabled=true;const res=await callGhlApi({action:"set_agent_ghl_id",agentId,ghlUserId:val||null});btn.disabled=false;const msg=$("ghlMapMsg");if(res.ok){msg.textContent="Saved.";msg.className="msg ok"}else{msg.textContent=res.error||"Failed to save.";msg.className="msg error"}})})}async function loadGhlCalls(){const box=$("ghlCallsList");if(!box)return;box.innerHTML='<div class="empty">Loading…</div>';const res=await callGhlApi({action:"list_calls",limit:50});if(!res.ok){box.innerHTML=`<div class="msg error">${escapeHtml(res.error||"Failed to load calls.")}</div>`;return}const calls=res.calls||[];if(!calls.length){box.innerHTML='<div class="empty">No calls received yet. Once agents make calls in GoHighLevel and the webhook is configured, they’ll appear here.</div>';return}const[{data:agents},{data:scheduleRows}]=await Promise.all([sb.from("profiles").select("id, full_name, email"),sb.from("schedules").select("user_id, timezone")]);const nameById={};(agents||[]).forEach(a=>{nameById[a.id]=a.full_name||a.email});const previewAgentTz={};(scheduleRows||[]).forEach(s=>{if(s.timezone)previewAgentTz[s.user_id]=s.timezone});const previewTz=userId=>globalDisplayTzMode==="agent"?previewAgentTz[userId]:globalDisplayTzMode;const previewFmt=(iso,userId)=>{if(!iso)return"—";const tz=previewTz(userId);return new Date(iso).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...tz?{timeZone:tz}:{}})};box.innerHTML=`
    <table>
      <thead><tr><th>When</th><th>Agent</th><th>Direction</th><th>Status</th><th>Duration</th><th>To / From</th></tr></thead>
      <tbody>
        ${calls.map(c=>{const who=c.user_id?escapeHtml(nameById[c.user_id]||"Agent"):`<span style="color:var(--text-dim);">unmapped (${escapeHtml(c.ghl_user_id||"—")})</span>`;const dur=c.duration_seconds!=null?`${Math.floor(c.duration_seconds/60)}m ${c.duration_seconds%60}s`:"—";const num=c.direction==="outbound"?c.to_number||"—":c.from_number||"—";return`<tr>
            <td class="mono" style="color:var(--text-2);">${previewFmt(c.call_at,c.user_id)}</td>
            <td>${who}</td>
            <td>${escapeHtml(c.direction||"—")}</td>
            <td>${escapeHtml(c.status||"—")}</td>
            <td class="mono">${dur}</td>
            <td class="mono" style="color:var(--text-2);">${escapeHtml(num)}</td>
          </tr>`}).join("")}
      </tbody>
    </table>`}async function loadGhlLookupLocations(){const sel=$("ghlLookupLocationSelect");if(!sel)return;const result=await callGhlApi({action:"list_connections"});const connections=result.ok?result.connections||[]:[];if(!connections.length){sel.innerHTML='<option value="">No location connected</option>';return}sel.innerHTML=connections.map(c=>`<option value="${escapeHtml(c.location_id)}">${escapeHtml(c.location_name||c.location_id)}</option>`).join("")}function ghlLookupFmtWhen(iso){if(!iso)return"—";return new Date(iso).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit"})}let ghlLookupMessages=[];let ghlLookupActiveTab="sms";function ghlLookupClassifyMessage(m){const t=String(m.messageType||m.type||"").toUpperCase();if(t.includes("CALL"))return"calls";if(t.includes("SMS"))return"sms";return"chats"}function ghlLookupMessageBody(m){if(ghlLookupClassifyMessage(m)==="calls"){const status=m.meta?.callStatus||m.callStatus||m.status;const duration=m.meta?.callDuration??m.callDuration;const parts=[status,duration!=null?`${duration}s`:null].filter(Boolean);if(parts.length)return parts.join(" · ")}return m.body||m.text||"—"}function ghlLookupTabsHtml(){const counts={chats:0,sms:0,calls:0};ghlLookupMessages.forEach(m=>{counts[ghlLookupClassifyMessage(m)]++});const tabs=[["chats","Chats"],["sms","SMS"],["calls","Calls"]];return`<div class="ghl-lookup-tab-row">${tabs.map(([key,label])=>`<button class="ghl-lookup-tab-btn${ghlLookupActiveTab===key?" active":""}" data-ghl-lookup-tab="${key}">${label} (${counts[key]})</button>`).join("")}</div>`}function ghlLookupRenderMessages(){const box=$("ghlLookupMessagesBox");if(!box)return;const filtered=ghlLookupMessages.filter(m=>ghlLookupClassifyMessage(m)===ghlLookupActiveTab);const label={chats:"chat messages",sms:"text messages",calls:"calls"}[ghlLookupActiveTab];box.innerHTML=filtered.length?`<table><thead><tr><th>When</th><th>Direction</th><th>${ghlLookupActiveTab==="calls"?"Outcome":"Message"}</th></tr></thead><tbody>${filtered.map(m=>`<tr>
          <td class="mono" style="color:var(--text-2); white-space:nowrap;">${ghlLookupFmtWhen(m.dateAdded||m.dateUpdated)}</td>
          <td>${escapeHtml(m.direction||"—")}</td>
          <td>${escapeHtml(ghlLookupMessageBody(m))}</td>
        </tr>`).join("")}</tbody></table>`:`<div class="empty">No ${label} found for this contact.</div>`}$("ghlLookupResults").addEventListener("click",e=>{const btn=e.target.closest(".ghl-lookup-tab-btn");if(!btn)return;ghlLookupActiveTab=btn.dataset.ghlLookupTab;$("ghlLookupResults").querySelectorAll(".ghl-lookup-tab-btn").forEach(b=>b.classList.toggle("active",b.dataset.ghlLookupTab===ghlLookupActiveTab));ghlLookupRenderMessages()});$("ghlLookupBtn").addEventListener("click",async()=>{const locationId=$("ghlLookupLocationSelect").value;const phone=$("ghlLookupPhone").value.trim();const msg=$("ghlLookupMsg");const results=$("ghlLookupResults");if(!locationId){msg.textContent="No GoHighLevel location connected yet.";msg.className="msg error";return}if(!phone){msg.textContent="Enter a phone number.";msg.className="msg error";return}const btn=$("ghlLookupBtn");const originalLabel=btn.textContent;btn.disabled=true;btn.textContent="Looking up…";msg.textContent="";msg.className="msg";results.innerHTML="";const res=await callGhlApi({action:"lookup_contact_activity",locationId,phone});btn.disabled=false;btn.textContent=originalLabel;if(!res.ok){msg.textContent=res.error||"Lookup failed.";msg.className="msg error";return}ghlLookupMessages=res.messages||[];ghlLookupActiveTab="sms";const messagesHtml=res.conversationError?`<div class="msg error" style="margin-top:0;">${escapeHtml(res.conversationError)}</div>`:ghlLookupMessages.length?`${ghlLookupTabsHtml()}<div id="ghlLookupMessagesBox"></div>`:'<div class="empty">No conversation found for this number.</div>';const oppsHtml=res.opportunitiesError?`<div class="msg error" style="margin-top:0;">${escapeHtml(res.opportunitiesError)}</div>`:res.opportunities&&res.opportunities.length?`<table><thead><tr><th>Name</th><th>Stage</th><th>Status</th><th>Value</th></tr></thead><tbody>${res.opportunities.map(o=>`<tr>
            <td>${escapeHtml(o.name||"—")}</td>
            <td>${escapeHtml(o.pipelineStageId||o.pipeline_stage_id||"—")}</td>
            <td>${escapeHtml(o.status||"—")}</td>
            <td class="mono">${o.monetaryValue!=null?"$"+Number(o.monetaryValue).toLocaleString():"—"}</td>
          </tr>`).join("")}</tbody></table>`:'<div class="empty">No opportunities found for this contact.</div>';results.innerHTML=`
    <div style="font-size:11px; color:var(--text-dim); margin-bottom:8px;">
      Contact: <strong style="color:var(--text-2);">${escapeHtml(res.contact?.name||res.contact?.phone||"Unknown")}</strong>
    </div>
    <div style="font-size:11px; color:var(--text-dim); text-transform:uppercase; letter-spacing:1px; margin:12px 0 6px;">Conversation</div>
    ${messagesHtml}
    <div style="font-size:11px; color:var(--text-dim); text-transform:uppercase; letter-spacing:1px; margin:16px 0 6px;">Opportunities</div>
    ${oppsHtml}
  `;if(ghlLookupMessages.length&&!res.conversationError)ghlLookupRenderMessages()});async function disconnectGhlLocation(locationId){if(!confirm("Disconnect this GoHighLevel location? You’ll need to reconnect it to resume lead sync."))return;const result=await callGhlApi({action:"disconnect",locationId});if(!result.ok){alert(result.error||"Failed to disconnect.");return}logActivity("integration.disconnect","ghl_location",null,`Disconnected GoHighLevel location ${locationId}`);await loadGhlConnections()}$("ghlOauthConnectBtn").addEventListener("click",async()=>{const msg=$("ghlOauthMsg");const btn=$("ghlOauthConnectBtn");msg.textContent="";msg.className="msg";btn.disabled=true;const originalLabel=btn.textContent;btn.textContent="Starting…";const result=await callGhlApi({action:"create_oauth_state"});btn.disabled=false;btn.textContent=originalLabel;if(!result.ok||!result.state){msg.textContent=result.error||"Failed to start the connect flow.";msg.className="msg error";return}if(GHL_CLIENT_ID==="YOUR_GHL_CLIENT_ID"||GHL_REDIRECT_URI.includes("YOUR-PROJECT-REF")){msg.textContent="GoHighLevel OAuth isn’t configured yet (GHL_CLIENT_ID/GHL_REDIRECT_URI are still placeholders) — use a Private Integration Token below instead, or ask an admin to finish the OAuth app setup.";msg.className="msg error";return}window.open(ghlConnectUrl(result.state),"_blank","noopener")});$("ghlAddPitBtn").addEventListener("click",async()=>{const locationId=$("ghlPitLocationId").value.trim();const locationName=$("ghlPitLocationName").value.trim();const token=$("ghlPitToken").value.trim();const msg=$("ghlPitMsg");if(!locationId||!token){msg.textContent="Location ID and token are both required.";msg.className="msg error";return}const btn=$("ghlAddPitBtn");const originalLabel=btn.textContent;btn.disabled=true;btn.textContent="Verifying…";const result=await callGhlApi({action:"add_private_token",locationId,locationName,token});btn.disabled=false;btn.textContent=originalLabel;if(!result.ok){msg.textContent=result.error||"Failed to add token.";msg.className="msg error";return}msg.textContent="Connected.";msg.className="msg ok";$("ghlPitLocationId").value="";$("ghlPitLocationName").value="";$("ghlPitToken").value="";logActivity("integration.connect","ghl_location",null,`Connected GoHighLevel location ${locationId} via Private Integration Token`);await loadGhlConnections()});(function(){const rb=document.getElementById("ghlRefreshCallsBtn");if(rb)rb.addEventListener("click",()=>loadGhlCalls())})();async function callRmApi(payload){const{data,error}=await sb.functions.invoke("readymode-api",{body:payload});if(error){try{const body=await error.context.json();return{ok:false,error:body.error||error.message}}catch{return{ok:false,error:error.message}}}return data}async function loadRmConnections(){const list=$("rmConnectionsList");if(!list)return;list.innerHTML='<div class="empty">Loading…</div>';const result=await callRmApi({action:"list_connections"});if(!result.ok){list.innerHTML=`<div class="msg error">${escapeHtml(result.error||"Failed to load connections.")}</div>`;return}const connections=result.connections||[];if(!connections.length){list.innerHTML='<div class="empty">No ReadyMode account connected yet.</div>';return}list.innerHTML=`
    <table>
      <thead><tr><th>Account</th><th>Connected</th><th>Endpoints configured</th><th>Lead posting</th><th></th></tr></thead>
      <tbody>
        ${connections.map(c=>`
          <tr>
            <td>${escapeHtml(c.account_domain)}</td>
            <td style="color:var(--text-2);">${new Date(c.created_at).toLocaleDateString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{})}</td>
            <td style="color:${c.calls_endpoint||c.research_endpoint||c.productivity_endpoint?"var(--focus)":"var(--text-dim)"};">${[c.calls_endpoint&&"Calls",c.research_endpoint&&"Research",c.productivity_endpoint&&"Productivity"].filter(Boolean).join(", ")||"None yet — use CSV import"}</td>
            <td style="color:${c.lead_posting_url?"var(--focus)":"var(--text-dim)"};">${c.lead_posting_url?"Configured":"Not set"}</td>
            <td><button class="task-del-btn rm-disconnect-btn" data-domain="${escapeHtml(c.account_domain)}" title="Disconnect">&times;</button></td>
          </tr>
        `).join("")}
      </tbody>
    </table>
  `;list.querySelectorAll(".rm-disconnect-btn").forEach(btn=>{btn.addEventListener("click",async()=>{if(!confirm("Disconnect this ReadyMode account?"))return;const res=await callRmApi({action:"disconnect",accountDomain:btn.dataset.domain});if(!res.ok){alert(res.error||"Failed to disconnect.");return}logActivity("integration.disconnect","readymode_account",null,`Disconnected ReadyMode account ${btn.dataset.domain}`);await loadRmConnections()})})}async function loadRmAgentMap(){const box=$("rmAgentMapList");if(!box)return;box.innerHTML='<div class="empty">Loading…</div>';const{data:agents,error}=await sb.from("profiles").select("id, full_name, email, readymode_username, role").order("full_name");if(error){box.innerHTML=`<div class="msg error">${escapeHtml(error.message)}</div>`;return}if(!agents||!agents.length){box.innerHTML='<div class="empty">No users found.</div>';return}box.innerHTML=`
    <table>
      <thead><tr><th>Agent</th><th>ReadyMode username</th><th></th></tr></thead>
      <tbody>
        ${agents.map(a=>`
          <tr>
            <td>${escapeHtml(a.full_name||a.email||"—")}</td>
            <td><input type="text" class="rm-map-input" data-agent="${a.id}" value="${escapeHtml(a.readymode_username||"")}" placeholder="ReadyMode username" style="width:100%; max-width:280px;"></td>
            <td><button class="btn-secondary rm-map-save edit-only" data-agent="${a.id}" style="width:auto; padding:5px 12px;">Save</button></td>
          </tr>
        `).join("")}
      </tbody>
    </table>
    <div class="msg" id="rmMapMsg"></div>`;box.querySelectorAll(".rm-map-save").forEach(btn=>{btn.addEventListener("click",async()=>{const agentId=btn.dataset.agent;const input=box.querySelector(`.rm-map-input[data-agent="${agentId}"]`);const val=input?input.value.trim():"";btn.disabled=true;const res=await callRmApi({action:"set_agent_readymode_username",agentId,username:val||null});btn.disabled=false;const msg=$("rmMapMsg");if(res.ok){msg.textContent="Saved.";msg.className="msg ok"}else{msg.textContent=res.error||"Failed to save.";msg.className="msg error"}})})}const rmSaveBtn=document.getElementById("rmSaveConnectionBtn");if(rmSaveBtn)rmSaveBtn.addEventListener("click",async()=>{const accountDomain=$("rmAccountDomain").value.trim();const apiKey=$("rmApiKey").value.trim();const callsEndpoint=$("rmCallsEndpoint").value.trim();const researchEndpoint=$("rmResearchEndpoint").value.trim();const productivityEndpoint=$("rmProductivityEndpoint").value.trim();const msg=$("rmConnectionMsg");if(!accountDomain){msg.textContent="Account domain is required.";msg.className="msg error";return}rmSaveBtn.disabled=true;const result=await callRmApi({action:"save_connection",accountDomain,apiKey,callsEndpoint,researchEndpoint,productivityEndpoint});rmSaveBtn.disabled=false;if(!result.ok){msg.textContent=result.error||"Failed to save.";msg.className="msg error";return}msg.textContent="Saved.";msg.className="msg ok";$("rmApiKey").value="";logActivity("integration.connect","readymode_account",null,`Connected ReadyMode account ${accountDomain}`);await loadRmConnections();await loadRmLeadPostingChannels()});async function loadRmLeadPostingChannels(){const accountDomain=$("rmAccountDomain").value.trim();const list=$("rmChannelsList");const select=$("rmListChannelSelect");if(list)list.innerHTML='<div class="empty">Loading…</div>';if(select)select.innerHTML='<option value="">Loading channels…</option>';const result=await callRmApi({action:"list_lead_posting_channels",accountDomain:accountDomain||void 0});if(!result.ok){if(list)list.innerHTML=`<div class="msg error">${escapeHtml(result.error||"Failed to load channels.")}</div>`;if(select)select.innerHTML='<option value="">Failed to load channels</option>';return}const channels=result.channels||[];if(list){if(!channels.length){list.innerHTML='<div class="empty">No channels saved yet. Add one above for each ReadyMode campaign you push into.</div>'}else{const{data:agentRows}=await sb.from("profiles").select("id, full_name, email").order("full_name");const agentOptionsHtml=selectedId=>`<option value="">Whole team</option>`+(agentRows||[]).map(a=>`<option value="${a.id}" ${a.id===selectedId?"selected":""}>${escapeHtml(a.full_name||a.email)}</option>`).join("");list.innerHTML=`
        <table>
          <thead><tr><th>Account</th><th>Channel/campaign</th><th>Client</th><th>Agent hours from</th><th>Corrections</th><th>Posting URL</th><th></th></tr></thead>
          <tbody>
            ${channels.map(c=>`
              <tr>
                <td>${escapeHtml(c.account_domain)}</td>
                <td>${escapeHtml(c.channel_name)}</td>
                <td>${c.client_name?escapeHtml(c.client_name):'<span style="color:var(--text-dim);">Unassigned</span>'}</td>
                <td><select class="rm-channel-agent-select" data-channel-id="${c.id}" style="width:auto; min-width:140px;">${agentOptionsHtml(c.agent_id)}</select></td>
                <td><button class="btn-outline rm-channel-adjustments-btn" data-channel-id="${c.id}" data-channel-name="${escapeHtml(c.channel_name)}" style="width:auto; padding:4px 10px; font-size:11px;">Corrections (${c.adjustment_count||0})</button></td>
                <td class="mono" style="color:var(--text-2); word-break:break-all;">${escapeHtml(c.posting_url)}</td>
                <td><button class="task-del-btn rm-delete-channel-btn" data-domain="${escapeHtml(c.account_domain)}" data-channel="${escapeHtml(c.channel_name)}" title="Delete">&times;</button></td>
              </tr>
            `).join("")}
          </tbody>
        </table>
      `;list.querySelectorAll(".rm-delete-channel-btn").forEach(btn=>{btn.addEventListener("click",async()=>{if(!confirm(`Delete the "${btn.dataset.channel}" channel? This won't affect leads already pushed.`))return;const res=await callRmApi({action:"delete_lead_posting_channel",accountDomain:btn.dataset.domain,channelName:btn.dataset.channel});if(!res.ok){alert(res.error||"Failed to delete.");return}await loadRmLeadPostingChannels()})});list.querySelectorAll(".rm-channel-agent-select").forEach(sel=>{sel.addEventListener("change",async()=>{sel.disabled=true;const res=await callRmApi({action:"set_channel_agent",channelId:sel.dataset.channelId,agentId:sel.value||null});sel.disabled=false;if(!res.ok){alert(res.error||"Failed to update agent.");await loadRmLeadPostingChannels();return}})});list.querySelectorAll(".rm-channel-adjustments-btn").forEach(btn=>{btn.addEventListener("click",()=>openHoursAdjModal(btn.dataset.channelId,btn.dataset.channelName))})}}if(select){if(!channels.length){select.innerHTML='<option value="">No channels saved — add one above first</option>'}else{select.innerHTML='<option value="">Select a channel/campaign…</option>'+channels.map(c=>`<option value="${escapeHtml(c.channel_name)}" data-domain="${escapeHtml(c.account_domain)}">${escapeHtml(c.channel_name)} (${escapeHtml(c.account_domain)})</option>`).join("")}}}let hoursAdjTargetChannelId=null;function fmtHoursAdjAmount(seconds){const sign=seconds<0?"-":"+";const abs=Math.abs(seconds);const h=Math.floor(abs/3600);const m=Math.round(abs%3600/60);return`${sign}${h}h ${m}m`}async function loadHoursAdjList(){const listEl=$("hoursAdjList");listEl.innerHTML='<div class="empty">Loading…</div>';const result=await callRmApi({action:"list_channel_hours_adjustments",channelId:hoursAdjTargetChannelId});if(!result.ok){listEl.innerHTML=`<div class="msg error">${escapeHtml(result.error||"Failed to load corrections.")}</div>`;return}const adjustments=result.adjustments||[];if(!adjustments.length){listEl.innerHTML='<div class="empty">No corrections logged yet for this campaign.</div>';return}listEl.innerHTML=adjustments.map(a=>`
    <div style="display:flex; justify-content:space-between; align-items:center; gap:8px; padding:6px 0; border-bottom:1px solid var(--border);">
      <div>
        <div style="font-weight:700; color:${a.adjustment_seconds<0?"var(--danger, #E5484D)":"var(--text)"};">${fmtHoursAdjAmount(a.adjustment_seconds)}</div>
        <div style="font-size:11px; color:var(--text-2);">${escapeHtml(a.date_from)} → ${escapeHtml(a.date_to)}${a.note?" — "+escapeHtml(a.note):""}</div>
      </div>
      <button class="task-del-btn hours-adj-delete-btn" data-id="${a.id}" title="Delete">&times;</button>
    </div>
  `).join("");listEl.querySelectorAll(".hours-adj-delete-btn").forEach(btn=>{btn.addEventListener("click",async()=>{if(!confirm("Delete this correction?"))return;const res=await callRmApi({action:"delete_channel_hours_adjustment",id:btn.dataset.id});if(!res.ok){alert(res.error||"Failed to delete.");return}await loadHoursAdjList();await loadRmLeadPostingChannels()})})}function openHoursAdjModal(channelId,channelName){hoursAdjTargetChannelId=channelId;$("hoursAdjChannelName").textContent=channelName;$("hoursAdjDateFrom").value="";$("hoursAdjDateTo").value="";$("hoursAdjSign").value="1";$("hoursAdjHours").value="";$("hoursAdjMinutes").value="";$("hoursAdjNote").value="";$("hoursAdjMsg").textContent="";$("hoursAdjModalOverlay").classList.add("active");loadHoursAdjList()}$("hoursAdjCloseBtn").addEventListener("click",()=>$("hoursAdjModalOverlay").classList.remove("active"));$("hoursAdjModalOverlay").addEventListener("click",e=>{if(e.target.id==="hoursAdjModalOverlay")$("hoursAdjModalOverlay").classList.remove("active")});$("hoursAdjAddBtn").addEventListener("click",async()=>{const msg=$("hoursAdjMsg");msg.textContent="";const dateFrom=$("hoursAdjDateFrom").value;const dateTo=$("hoursAdjDateTo").value;if(!dateFrom||!dateTo){msg.textContent="From date and To date are both required.";return}if(dateTo<dateFrom){msg.textContent="To date must be on or after From date.";return}const sign=Number($("hoursAdjSign").value);const hours=Math.max(0,parseInt($("hoursAdjHours").value,10)||0);const minutes=Math.max(0,Math.min(59,parseInt($("hoursAdjMinutes").value,10)||0));const adjustmentSeconds=sign*(hours*3600+minutes*60);if(!adjustmentSeconds){msg.textContent="Enter a non-zero amount of hours and/or minutes.";return}const btn=$("hoursAdjAddBtn");btn.disabled=true;const res=await callRmApi({action:"add_channel_hours_adjustment",channelId:hoursAdjTargetChannelId,dateFrom,dateTo,adjustmentSeconds,note:$("hoursAdjNote").value.trim()||void 0});btn.disabled=false;if(!res.ok){msg.textContent=res.error||"Failed to add correction.";return}$("hoursAdjDateFrom").value="";$("hoursAdjDateTo").value="";$("hoursAdjHours").value="";$("hoursAdjMinutes").value="";$("hoursAdjNote").value="";await loadHoursAdjList();await loadRmLeadPostingChannels()});async function loadRmChannelClientOptions(){const sel=$("rmChannelClientSelect");if(!sel)return;const{data,error}=await sb.from("client_accounts").select("id, full_name, email").eq("is_active",true).order("full_name");if(error){sel.innerHTML=`<option value="">Failed to load clients</option>`;return}sel.innerHTML=`<option value="">Unassigned (no client)</option>`+(data||[]).map(c=>`<option value="${c.id}">${escapeHtml(c.full_name||c.email||c.id)}</option>`).join("")}const rmSaveChannelBtn=document.getElementById("rmSaveChannelBtn");if(rmSaveChannelBtn)rmSaveChannelBtn.addEventListener("click",async()=>{const accountDomain=$("rmAccountDomain").value.trim();const channelName=$("rmChannelName").value.trim();const postingUrl=$("rmChannelPostingUrl").value.trim();const clientId=$("rmChannelClientSelect").value||null;const msg=$("rmChannelMsg");if(!accountDomain||!channelName||!postingUrl){msg.textContent="Account domain (above), channel name, and posting URL are all required.";msg.className="msg error";return}rmSaveChannelBtn.disabled=true;const result=await callRmApi({action:"save_lead_posting_channel",accountDomain,channelName,postingUrl,clientId});rmSaveChannelBtn.disabled=false;if(!result.ok){msg.textContent=result.error||"Failed to save channel.";msg.className="msg error";return}msg.textContent="Channel saved.";msg.className="msg ok";$("rmChannelName").value="";$("rmChannelPostingUrl").value="";$("rmChannelClientSelect").value="";logActivity("integration.connect","readymode_channel",null,`Saved ReadyMode lead-posting channel "${channelName}" for ${accountDomain}`);await loadRmLeadPostingChannels()});const rmSyncBtn=document.getElementById("rmSyncBtn");if(rmSyncBtn)rmSyncBtn.addEventListener("click",async()=>{const accountDomain=$("rmAccountDomain").value.trim();const msg=$("rmConnectionMsg");if(!accountDomain){msg.textContent="Enter the account domain above first (save the connection, then Sync).";msg.className="msg error";return}rmSyncBtn.disabled=true;const result=await callRmApi({action:"sync_from_api",accountDomain});rmSyncBtn.disabled=false;msg.textContent=result.ok?result.note||"Sync attempted.":result.error||"Sync failed.";msg.className=result.ok?"msg ok":"msg error"});function wireRmImportButton(btnId2,textareaId,action,label){const btn=document.getElementById(btnId2);if(!btn)return;btn.addEventListener("click",async()=>{const csvText=$(textareaId).value;const msg=$("rmImportMsg");if(!csvText.trim()){msg.textContent="Paste some CSV content first.";msg.className="msg error";return}btn.disabled=true;const result=await callRmApi({action,csvText});btn.disabled=false;if(!result.ok){msg.textContent=result.error||`Failed to import ${label}.`;msg.className="msg error";return}msg.textContent=`Imported ${result.imported} row(s) from ${label}.`;msg.className="msg ok";logActivity("integration.import","readymode_csv",null,`Imported ${result.imported} row(s) of ReadyMode ${label}`)})}wireRmImportButton("rmImportCallsBtn","rmCallsCsv","import_calls_csv","Call Logs");wireRmImportButton("rmImportResearchBtn","rmResearchCsv","import_research_csv","Research Calls");wireRmImportButton("rmImportProductivityBtn","rmProductivityCsv","import_productivity_csv","Agent Report");const RM_STATE_ABBR={alabama:"al",alaska:"ak",arizona:"az",arkansas:"ar",california:"ca",colorado:"co",connecticut:"ct",delaware:"de",florida:"fl",georgia:"ga",hawaii:"hi",idaho:"id",illinois:"il",indiana:"in",iowa:"ia",kansas:"ks",kentucky:"ky",louisiana:"la",maine:"me",maryland:"md",massachusetts:"ma",michigan:"mi",minnesota:"mn",mississippi:"ms",missouri:"mo",montana:"mt",nebraska:"ne",nevada:"nv","new hampshire":"nh","new jersey":"nj","new mexico":"nm","new york":"ny","north carolina":"nc","north dakota":"nd",ohio:"oh",oklahoma:"ok",oregon:"or",pennsylvania:"pa","rhode island":"ri","south carolina":"sc","south dakota":"sd",tennessee:"tn",texas:"tx",utah:"ut",vermont:"vt",virginia:"va",washington:"wa","west virginia":"wv",wisconsin:"wi",wyoming:"wy","district of columbia":"dc"};const RM_STREET_TYPE_ABBR={street:"st",avenue:"ave",av:"ave",boulevard:"blvd",drive:"dr",road:"rd",lane:"ln",court:"ct",place:"pl",circle:"cir",terrace:"ter",trail:"trl",parkway:"pkwy",highway:"hwy",square:"sq",crossing:"xing",north:"n",south:"s",east:"e",west:"w"};function rmExtractZip5(address){const m=String(address||"").match(/\b(\d{5})(?:-\d{4})?\b/);return m?m[1]:""}function normalizeAddressForDedupe(address){if(!address)return"";let s=String(address).toLowerCase().replace(/[.#]/g," ").replace(/,/g," ");s=s.replace(/\b(apt|apartment|unit|suite|ste)\s*\S*/g," ");const words=s.split(/\s+/).filter(Boolean);while(words.length&&(words[words.length-1]==="usa"||words[words.length-1]==="us"))words.pop();let stateEnd=words.length-1;if(stateEnd>=0&&/^\d{5}(-\d{4})?$/.test(words[stateEnd]))stateEnd--;let stateStart=-1,stateAbbr="";if(stateEnd>=1){const two=words[stateEnd-1]+" "+words[stateEnd];if(RM_STATE_ABBR[two]){stateAbbr=RM_STATE_ABBR[two];stateStart=stateEnd-1}}if(stateStart===-1&&stateEnd>=0&&RM_STATE_ABBR[words[stateEnd]]){stateAbbr=RM_STATE_ABBR[words[stateEnd]];stateStart=stateEnd}const out=words.map((w,i)=>stateStart!==-1&&i>=stateStart&&i<=stateEnd?null:RM_STREET_TYPE_ABBR[w]||w);if(stateStart!==-1){out[stateStart]=stateAbbr;for(let i=stateStart+1;i<=stateEnd;i++)out[i]=null}return out.filter(w=>w!==null).join(" ").replace(/\s+/g," ").trim()}function rmDedupeKey(address){const zip=rmExtractZip5(address);const full=normalizeAddressForDedupe(address);if(!zip)return full;const streetOnly=full.split(zip)[0].trim()||full;return`${streetOnly}|${zip}`}const LEAD_DUPLICATE_REJECT_CATEGORY="Duplicate address/phone number";function leadCountsAsAccepted(lead){if(!lead)return false;if(lead.status==="accepted")return true;return lead.status==="rejected"&&lead.reject_reason_category===LEAD_DUPLICATE_REJECT_CATEGORY}const LEAD_STREET_TYPE_TOKENS=new Set(Object.values(RM_STREET_TYPE_ABBR));function collapseRepeatedStreetType(key){return key.split(" ").filter((t,i,arr)=>!(i>0&&t===arr[i-1]&&LEAD_STREET_TYPE_TOKENS.has(t))).join(" ")}function leadPropertyKey(address,phoneNorm){const addr=String(address||"").trim();if(addr)return"a:"+collapseRepeatedStreetType(rmDedupeKey(addr));return phoneNorm?"p:"+phoneNorm:""}const CREDITED_LEAD_COLS="id, count, status, reject_reason_category, seller_phone_norm, addr:extra_fields->>form_address, addr_alt:extra_fields->>property_address";const CREDITED_LEAD_STATUSES=["accepted","rejected"];function creditedLeadCount(rows){const seen=new Set;return(rows||[]).reduce((total,r)=>{if(!leadCountsAsAccepted(r))return total;const key=leadPropertyKey(r.addr||r.addr_alt,r.seller_phone_norm);if(key){if(seen.has(key))return total;seen.add(key)}return total+(r.count||0)},0)}async function rmFetchKnownIspeedtoleadAddresses(){const byKey=new Map;const formId=await getIspdDefaultCampaignFormId();if(!formId)return null;const PAGE=1e3;for(let from=0;;from+=PAGE){const{data,error}=await sb.from("leads").select("status, extra_fields").eq("submission_form_id",formId).range(from,from+PAGE-1);if(error)return null;if(!data||!data.length)break;data.forEach(r=>{const addr=(r.extra_fields||{}).form_address;if(!addr)return;const key=rmDedupeKey(addr);if(key&&!byKey.has(key))byKey.set(key,{address:addr,status:r.status})});if(data.length<PAGE)break}return byKey}async function rmFetchPreviouslyPushedAddresses(){const byKey=new Map;const PAGE=1e3;for(let from=0;;from+=PAGE){const{data,error}=await sb.from("readymode_lead_pushes").select("channel_name, pushed_at, address, city, state, zip").eq("success",true).range(from,from+PAGE-1);if(error)return null;if(!data||!data.length)break;data.forEach(r=>{const full=[r.address,r.city,r.state,r.zip].filter(Boolean).join(", ");const key=rmDedupeKey(full);if(!key)return;const prev=byKey.get(key);if(!prev||String(r.pushed_at||"")>String(prev.pushedAt||"")){byKey.set(key,{channel:r.channel_name,pushedAt:r.pushed_at,address:full})}});if(data.length<PAGE)break}return byKey}function rmCsvDownload(filename,headerRow,rows){const esc=v=>`"${String(v==null?"":v).replace(/"/g,'""')}"`;const lines=[headerRow.map(esc).join(",")].concat(rows.map(r=>r.map(esc).join(",")));const blob=new Blob([lines.join("\n")],{type:"text/csv;charset=utf-8;"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=filename;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url)}let rmListCheckResult=null;let rmListParsed=null;const RM_MAP_FIELDS=[{key:"address",label:"Address",required:true,hint:"street, or a full address"},{key:"city",label:"City"},{key:"state",label:"State"},{key:"zip",label:"Zip"},{key:"firstName",label:"First name"},{key:"lastName",label:"Last name"},{key:"fullName",label:"Full name",hint:"split, if no first/last"},{key:"phone",label:"Phone",hint:"required to push"},{key:"phone2",label:"Phone 2"},{key:"email",label:"Email"}];function rmRenderMapGrid(headerRow,detected){const grid=$("rmListMapGrid");if(!grid)return;const options=selectedIdx=>['<option value="-1"'+(selectedIdx===-1?" selected":"")+">-- none --</option>"].concat(headerRow.map((h,i)=>{const label=String(h||"").trim()||"(unnamed column "+(i+1)+")";return'<option value="'+i+'"'+(i===selectedIdx?" selected":"")+">"+escapeHtml(label)+"</option>"})).join("");grid.innerHTML=RM_MAP_FIELDS.map(f=>`
    <div>
      <label style="display:block; margin:0 0 3px; font-size:11px; color:var(--text-dim);">
        ${escapeHtml(f.label)}${f.required?' <span style="color:var(--danger);">*</span>':""}${f.hint?` <span style="opacity:0.75;">(${escapeHtml(f.hint)})</span>`:""}
      </label>
      <select class="rm-map-select" data-map-field="${f.key}" style="width:100%;">${options(typeof detected[f.key]==="number"?detected[f.key]:-1)}</select>
    </div>
  `).join("");grid.querySelectorAll(".rm-map-select").forEach(sel=>sel.addEventListener("change",rmRenderMapSample));rmRenderMapSample()}function rmReadMapping(){const map={};RM_MAP_FIELDS.forEach(f=>{map[f.key]=-1});document.querySelectorAll("#rmListMapGrid .rm-map-select").forEach(sel=>{const n=parseInt(sel.value,10);map[sel.dataset.mapField]=Number.isFinite(n)?n:-1});return map}function rmRenderMapSample(){const el=$("rmListMapSample");if(!el||!rmListParsed||!rmListParsed.dataRows.length)return;const map=rmReadMapping();const row=rmListParsed.dataRows[0];const val=i=>i>-1?String(row[i]||"").trim():"";const addr=[val(map.address),val(map.city),val(map.state),val(map.zip)].filter(Boolean).join(", ");let who=[val(map.firstName),val(map.lastName)].filter(Boolean).join(" ");if(!who&&map.fullName>-1)who=val(map.fullName);el.innerHTML='First row reads as &rarr; address: <span style="color:var(--text-2);">'+(addr?escapeHtml(addr):'<span style="color:var(--danger);">nothing -- pick an Address column</span>')+'</span> &nbsp;|&nbsp; name: <span style="color:var(--text-2);">'+(who?escapeHtml(who):"--")+'</span> &nbsp;|&nbsp; phone: <span style="color:var(--text-2);">'+(val(map.phone)?escapeHtml(val(map.phone)):"--")+"</span>"}const rmListUploadInput=document.getElementById("rmListUploadInput");if(rmListUploadInput)rmListUploadInput.addEventListener("change",async e=>{const file=e.target.files[0];const msg=$("rmListUploadMsg");msg.style.display="none";$("rmListUploadResults").style.display="none";$("rmListMapCard").style.display="none";rmListParsed=null;rmListCheckResult=null;if(!file)return;try{const buf=await file.arrayBuffer();await ensureXlsx();const wb=XLSX.read(buf,{type:"array"});const sheet=wb.Sheets[wb.SheetNames[0]];const rows=XLSX.utils.sheet_to_json(sheet,{header:1,defval:"",raw:false});if(!rows.length){msg.textContent="That file has no rows.";msg.className="msg error";msg.style.display="block";return}const headerRow=rows[0].map(h=>String(h||"").trim());const dataRows=rows.slice(1).filter(r=>r.some(c=>String(c||"").trim()!==""));if(!dataRows.length){msg.textContent="No data rows found below the header.";msg.className="msg error";msg.style.display="block";return}const norm=s=>String(s||"").trim().toLowerCase();const findCol=names=>headerRow.findIndex(h=>names.includes(norm(h)));const ADDR_SOURCES=[{name:"DealMachine",line:["associated_property_address_line_1"],full:["associated_property_address_full"],city:["associated_property_address_city"],state:["associated_property_address_state"],zip:["associated_property_address_zipcode"]},{name:"skip trace (property)",line:["property_street_address"],full:["formated_address","formatted_address"],city:["property_city"],state:["property_state"],zip:["property_zip_code"]},{name:"generic",line:["property address","property_address","address","street address","street_address","address_line1","address_line_1","situs address","situs_address"],full:["property address full","full address"],city:["property city","city"],state:["property state","state"],zip:["property zip","zip","zip code","zip_code","postal code"]},{name:"mailing",mailing:true,line:["mailing address","mailing_address","mailing_street_address"],full:["mailing_formated_address"],city:["mailing_city"],state:["mailing_state"],zip:["mailing_zip_code"]}];const detected={address:-1,city:-1,state:-1,zip:-1};let detectedSource=null;for(const src of ADDR_SOURCES){const lineCol=findCol(src.line),fullCol=findCol(src.full);if(lineCol===-1&&fullCol===-1)continue;detectedSource=src.name;if(lineCol>-1){detected.address=lineCol;detected.city=findCol(src.city);detected.state=findCol(src.state);detected.zip=findCol(src.zip);if(!src.mailing){if(detected.city===-1)detected.city=findCol(["property city","city"]);if(detected.state===-1)detected.state=findCol(["property state","state"]);if(detected.zip===-1)detected.zip=findCol(["property zip","zip","zip code","zip_code","postal code"])}}else{detected.address=fullCol}break}detected.firstName=findCol(["first_name","owner_first_name","owner first name","first name","firstname"]);detected.lastName=findCol(["last_name","owner_last_name","owner last name","last name","lastname"]);detected.fullName=findCol(["owner_name","owner name","seller name","name","full_name","full name"]);detected.email=findCol(["email_address_1","email_1","email 1","email1","email"]);detected.phone=findCol(["phone_1","phone_number_1","phone 1","phone1","phone number 1","phone","phone number","primary phone","mobile"]);detected.phone2=findCol(["phone_2","phone_number_2","phone 2","phone2","phone number 2","secondary phone"]);rmListParsed={headerRow,dataRows,detectedSource};rmRenderMapGrid(headerRow,detected);$("rmListMapCard").style.display="block";msg.textContent=`Read ${dataRows.length} row(s) and ${headerRow.length} column(s).`+(detectedSource?` These headers look like a ${detectedSource} export, so the mapping below is pre-filled -- check it, then run the dedupe check.`:" None of these headers were recognised automatically, so pick the columns below yourself, then run the dedupe check.");msg.className=detectedSource?"msg":"msg error";msg.style.display="block"}catch(err){console.error("rmListUploadInput: failed to parse file:",err);msg.textContent=`Couldn't read that file: ${err instanceof Error?err.message:String(err)}`;msg.className="msg error";msg.style.display="block"}finally{e.target.value=""}});document.getElementById("rmListRunCheckBtn")?.addEventListener("click",async()=>{const msg=$("rmListUploadMsg");if(!rmListParsed){msg.textContent="Choose a file first.";msg.className="msg error";msg.style.display="block";return}const map=rmReadMapping();if(map.address===-1){msg.textContent="Pick which column holds the address -- the dedupe check has nothing to compare without it.";msg.className="msg error";msg.style.display="block";return}const{headerRow,dataRows}=rmListParsed;const btn=$("rmListRunCheckBtn");btn.disabled=true;try{msg.textContent="Checking against iSpeedToLead and previous ReadyMode pushes...";msg.className="msg";msg.style.display="block";const knownByKey=await rmFetchKnownIspeedtoleadAddresses();if(!knownByKey){msg.textContent="Couldn't load the known iSpeedToLead addresses, so the dedupe check cannot run -- nothing has been checked. Open the Leads tab once and try again; if it keeps failing, your role may not have access to the PrimeHome Buyers campaign.";msg.className="msg error";msg.style.display="block";return}const pushedByKey=await rmFetchPreviouslyPushedAddresses();if(!pushedByKey){msg.textContent="Couldn't load the ReadyMode push history, so the dedupe check cannot run -- nothing has been checked. Try again in a moment.";msg.className="msg error";msg.style.display="block";return}const cell=(r,i)=>i>-1?String(r[i]||"").trim():"";const results=dataRows.map(r=>{const rawAddr=cell(r,map.address);const city=cell(r,map.city);const state=cell(r,map.state);const zip=cell(r,map.zip);const fullAddr=[rawAddr,city,state,zip].filter(Boolean).join(", ");const key=rmDedupeKey(fullAddr||rawAddr);const match=key?knownByKey.get(key):null;const pushedMatch=key?pushedByKey.get(key):null;let firstName=cell(r,map.firstName);let lastName=cell(r,map.lastName);if(!firstName&&!lastName&&map.fullName>-1){const parts=cell(r,map.fullName).split(/\s+/);firstName=parts.shift()||"";lastName=parts.join(" ")}const lead={firstName,lastName,phone:cell(r,map.phone),phone2:cell(r,map.phone2),email:cell(r,map.email),address:rawAddr,city,state,zip};return{cells:r,rawAddr:fullAddr||rawAddr,isDup:!!match,pushedBefore:pushedMatch||null,isClean:!match&&!pushedMatch,matchedStatus:match?match.status:null,lead}});rmListCheckResult={headerRow,results};const dupCount=results.filter(r=>r.isDup).length;const pushedCount=results.filter(r=>r.pushedBefore).length;const cleanCount=results.filter(r=>r.isClean).length;const noAddrCount=results.filter(r=>!r.rawAddr).length;$("rmListStatTotal").textContent=String(results.length);$("rmListStatDup").textContent=String(dupCount);if($("rmListStatPushed"))$("rmListStatPushed").textContent=String(pushedCount);$("rmListStatClean").textContent=String(cleanCount);$("rmListPreviewBody").innerHTML=results.slice(0,500).map(r=>`
      <tr style="border-top:1px solid var(--border-light);">
        <td style="padding:5px 8px;">${escapeHtml(r.rawAddr)}</td>
        <td style="padding:5px 8px; ${r.isClean?"color:var(--focus);":"color:var(--danger);"}">${r.isDup?"In iSpeedToLead":r.pushedBefore?"Already pushed":"Clean"}</td>
        <td style="padding:5px 8px; color:var(--text-dim);">${r.isDup?escapeHtml(r.matchedStatus||"known lead"):r.pushedBefore?escapeHtml(`${r.pushedBefore.channel||"ReadyMode"}${r.pushedBefore.pushedAt?" — "+new Date(r.pushedBefore.pushedAt).toLocaleDateString():""}`):"&mdash;"}</td>
      </tr>
    `).join("")+(results.length>500?`<tr><td colspan="3" style="padding:6px 8px; color:var(--text-dim);">... and ${results.length-500} more row(s), not shown here but included in the downloads below.</td></tr>`:"");$("rmListUploadResults").style.display="block";$("rmListPushMsg").style.display="none";$("rmListPushResults").style.display="none";msg.textContent=`Checked ${results.length} row(s) against ${knownByKey.size} known iSpeedToLead address(es) and ${pushedByKey.size} address(es) already pushed to ReadyMode -- ${cleanCount} clean, ${dupCount} already in iSpeedToLead, ${pushedCount} already pushed to a ReadyMode channel.`+(noAddrCount?` ${noAddrCount} row(s) had nothing in the mapped Address column and could not be checked -- they are counted as clean, so re-check the mapping if that number looks wrong.`:"")+(map.phone>-1?"":' Note: no Phone column is mapped -- ReadyMode requires a phone per lead, so "Push clean list to ReadyMode" will reject every row until you map one.');msg.className="msg ok"}catch(err){console.error("rmListRunCheckBtn: dedupe check failed:",err);msg.textContent=`Couldn't run the check: ${err instanceof Error?err.message:String(err)}`;msg.className="msg error";msg.style.display="block"}finally{btn.disabled=false}});document.getElementById("rmListDownloadCleanBtn")?.addEventListener("click",()=>{if(!rmListCheckResult)return;const clean=rmListCheckResult.results.filter(r=>r.isClean).map(r=>r.cells);if(!clean.length){alert("Nothing to download — every row was flagged as a duplicate.");return}rmCsvDownload("readymode-upload-clean.csv",rmListCheckResult.headerRow,clean);logActivity("integration.readymode_list_dedupe",null,null,`Downloaded a cleaned ReadyMode upload list (${clean.length} row(s), duplicates removed)`)});document.getElementById("rmListDownloadDupBtn")?.addEventListener("click",()=>{if(!rmListCheckResult)return;const dups=rmListCheckResult.results.filter(r=>!r.isClean).map(r=>r.cells);if(!dups.length){alert("No duplicates were flagged.");return}rmCsvDownload("readymode-upload-duplicates.csv",rmListCheckResult.headerRow,dups)});document.getElementById("rmListPushBtn")?.addEventListener("click",async()=>{if(!rmListCheckResult)return;const channelSelect=$("rmListChannelSelect");const channelName=channelSelect?channelSelect.value:"";const selectedOption=channelSelect&&channelSelect.selectedIndex>-1?channelSelect.options[channelSelect.selectedIndex]:null;const accountDomain=selectedOption?selectedOption.dataset.domain||"":"";const pushMsg=$("rmListPushMsg");const pushResults=$("rmListPushResults");pushResults.style.display="none";if(!channelName||!accountDomain){pushMsg.textContent='Pick a channel/campaign to push into first — save one in the "Lead-posting channels" card above if the dropdown is empty.';pushMsg.className="msg error";pushMsg.style.display="block";return}const cleanLeads=rmListCheckResult.results.filter(r=>r.isClean).map(r=>r.lead);if(!cleanLeads.length){alert("Nothing to push — every row was flagged as a duplicate.");return}const withPhone=cleanLeads.filter(l=>l.phone);if(!withPhone.length){pushMsg.textContent="None of the clean rows have a callable phone number — ReadyMode requires one per lead, so there's nothing postable here.";pushMsg.className="msg error";pushMsg.style.display="block";return}if(!confirm(`Push ${withPhone.length} lead(s) into ReadyMode's "${channelName}" channel now? This creates real leads in your ReadyMode dialer account.`))return;const btn=document.getElementById("rmListPushBtn");btn.disabled=true;pushMsg.className="msg";pushMsg.style.display="block";const CLIENT_CHUNK=500;let succeeded=0,failed=0;const allFailures=[];let stoppedEarly=null;for(let offset=0;offset<withPhone.length;offset+=CLIENT_CHUNK){const chunk=withPhone.slice(offset,offset+CLIENT_CHUNK);pushMsg.textContent=`Pushing lead(s) ${offset+1}-${offset+chunk.length} of ${withPhone.length} to ReadyMode ("${channelName}")…`;const result=await callRmApi({action:"post_leads_batch",accountDomain,channelName,leads:chunk});if(!result.ok){stoppedEarly=result.error||"Failed to push leads to ReadyMode.";break}succeeded+=result.succeeded;failed+=result.failed;allFailures.push(...(result.results||[]).filter(r=>!r.success))}btn.disabled=false;const attempted=succeeded+failed;if(stoppedEarly){pushMsg.textContent=`Stopped after pushing ${attempted} of ${withPhone.length} lead(s) (${succeeded} succeeded, ${failed} failed) — ${stoppedEarly}. Leads already pushed are recorded in "Lead push history" below; re-run to pick up where this left off (already-pushed rows aren't tracked for skip-on-retry, so remove them from the file first if you don't want duplicates in ReadyMode).`;pushMsg.className="msg error"}else{pushMsg.textContent=`Pushed ${attempted} lead(s) to "${channelName}" — ${succeeded} succeeded, ${failed} failed.`;pushMsg.className=failed?"msg":"msg ok"}logActivity("integration.readymode_list_push",null,null,`Pushed ${attempted} lead(s) to ReadyMode channel "${channelName}" via the Upload list dedupe tool — ${succeeded} succeeded, ${failed} failed${stoppedEarly?" (stopped early: "+stoppedEarly+")":""}`);loadRmLeadPushes();if(allFailures.length){pushResults.innerHTML=`
      <table class="mono" style="width:100%; font-size:11px; border-collapse:collapse;">
        <thead><tr><th style="text-align:left; padding:6px 8px;">Phone</th><th style="text-align:left; padding:6px 8px;">Address</th><th style="text-align:left; padding:6px 8px;">Error</th></tr></thead>
        <tbody>
          ${allFailures.map(f=>`
            <tr style="border-top:1px solid var(--border-light);">
              <td style="padding:5px 8px;">${escapeHtml(f.input?.phone||"—")}</td>
              <td style="padding:5px 8px;">${escapeHtml(f.input?.address||"—")}</td>
              <td style="padding:5px 8px; color:var(--danger);">${escapeHtml(f.error||"Rejected")}</td>
            </tr>
          `).join("")}
        </tbody>
      </table>
    `;pushResults.style.display="block"}});async function loadRmEmailImports(){const box=$("rmEmailImportsList");if(!box)return;box.innerHTML='<div class="empty">Loading…</div>';const result=await callRmApi({action:"list_email_imports",limit:20});if(!result.ok){box.innerHTML=`<div class="msg error">${escapeHtml(result.error||"Failed to load import history.")}</div>`;return}const imports=result.imports||[];if(!imports.length){box.innerHTML=`<div class="empty">No automated imports received yet. Once a ReadyMode Automated Task email arrives via Mailgun, it'll show up here.</div>`;return}const reportLabel={calls:"Call Logs",agent_daily:"Agent Report",unknown:"Unrecognized"};box.innerHTML=`
    <table>
      <thead><tr><th>Received</th><th>Report</th><th>File</th><th>Rows</th><th>Imported</th><th>Status</th></tr></thead>
      <tbody>
        ${imports.map(i=>`
          <tr>
            <td style="color:var(--text-2); white-space:nowrap;">${new Date(i.received_at).toLocaleString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{})}</td>
            <td>${escapeHtml(reportLabel[i.report_type]||i.report_type)}</td>
            <td style="color:var(--text-2);">${escapeHtml(i.filename||"—")}</td>
            <td>${i.row_count}</td>
            <td>${i.imported_count}</td>
            <td>
              ${i.status==="ok"?'<span style="color:var(--focus);">OK</span>':`<span style="color:#e5484d;" title="${escapeHtml(i.error||"")}">Error${i.error?": "+escapeHtml(i.error.slice(0,60)):""}</span>`}
            </td>
          </tr>
        `).join("")}
      </tbody>
    </table>
  `}const rmRefreshEmailImportsBtn=document.getElementById("rmRefreshEmailImportsBtn");if(rmRefreshEmailImportsBtn)rmRefreshEmailImportsBtn.addEventListener("click",()=>loadRmEmailImports());async function loadRmLeadPushes(){const box=$("rmLeadPushesList");if(!box)return;box.innerHTML='<div class="empty">Loading…</div>';const result=await callRmApi({action:"list_lead_pushes",limit:100});if(!result.ok){box.innerHTML=`<div class="msg error">${escapeHtml(result.error||"Failed to load push history.")}</div>`;return}const pushes=result.pushes||[];if(!pushes.length){box.innerHTML='<div class="empty">No leads pushed to ReadyMode yet.</div>';return}box.innerHTML=`
    <table>
      <thead><tr><th>Pushed</th><th>By</th><th>Name</th><th>Phone</th><th>Address</th><th>Result</th></tr></thead>
      <tbody>
        ${pushes.map(p=>`
          <tr>
            <td style="color:var(--text-2); white-space:nowrap;">${new Date(p.pushed_at).toLocaleString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{})}</td>
            <td>${escapeHtml(p.pushed_by_name||"—")}</td>
            <td>${escapeHtml([p.first_name,p.last_name].filter(Boolean).join(" ")||"—")}</td>
            <td class="mono">${escapeHtml(p.phone||"—")}</td>
            <td style="max-width:220px; white-space:normal; color:var(--text-2);">${escapeHtml([p.address,p.city,p.state,p.zip].filter(Boolean).join(", ")||"—")}</td>
            <td>${p.success?`<span style="color:var(--focus);">Accepted${p.readymode_lead_id?` (${escapeHtml(p.readymode_lead_id)})`:""}</span>`:`<span style="color:var(--danger);" title="${escapeHtml(p.error||"")}">Failed${p.error?": "+escapeHtml(p.error.slice(0,60)):""}</span>`}</td>
          </tr>
        `).join("")}
      </tbody>
    </table>
  `}const rmRefreshLeadPushesBtn=document.getElementById("rmRefreshLeadPushesBtn");if(rmRefreshLeadPushesBtn)rmRefreshLeadPushesBtn.addEventListener("click",()=>loadRmLeadPushes());function renderRmCampaignProgressCard(c,opts){opts=opts||{};const dispositionBadges=Object.entries(c.dispositionCounts||{}).sort((a,b)=>b[1]-a[1]).slice(0,6);const reachPct=c.leadsPushed?Math.round(c.leadsReached/c.leadsPushed*100):null;const spokePct=c.leadsPushed&&typeof c.spokeToSomeone==="number"?Math.round(c.spokeToSomeone/c.leadsPushed*100):null;const spokeStr=typeof c.spokeToSomeone==="number"?`${c.spokeToSomeone}${spokePct!==null?` (${spokePct}%)`:""}`:"—";const f=c.file||null;const fileNotReachedPct=f&&f.leads?Math.round(f.notYetReached/f.leads*100):null;const fileOutcomeBadges=f?Object.entries(f.outcomes||{}).filter(([k])=>String(k).trim().toLowerCase()!=="not yet reached").sort((a,b)=>b[1]-a[1]).slice(0,8):[];const statCards=opts.uncategorized?`<div class="act-stat-card"><div class="act-stat-label">Got a call result</div><div class="act-stat-value good">${c.leadsReached}</div></div>
       <div class="act-stat-card"><div class="act-stat-label">Spoke to someone</div><div class="act-stat-value good">${spokeStr}</div></div>
       <div class="act-stat-card"><div class="act-stat-label">Total call events</div><div class="act-stat-value">${c.totalCallEvents}</div></div>
       <div class="act-stat-card"><div class="act-stat-label">Appointments</div><div class="act-stat-value good">${c.appointmentsSet}</div></div>`:f?`<div class="act-stat-card"><div class="act-stat-label">Leads in ReadyMode file</div><div class="act-stat-value">${f.leads}</div></div>
          <div class="act-stat-card"><div class="act-stat-label">Not yet reached</div><div class="act-stat-value">${f.notYetReached}${fileNotReachedPct!==null?` (${fileNotReachedPct}%)`:""}</div></div>
          <div class="act-stat-card"><div class="act-stat-label">Got an outcome</div><div class="act-stat-value good">${f.withOutcome}</div></div>
          <div class="act-stat-card"><div class="act-stat-label">Appointments</div><div class="act-stat-value good">${f.positives}</div></div>`:`<div class="act-stat-card"><div class="act-stat-label">Leads pushed</div><div class="act-stat-value">${c.leadsPushed}</div></div>
          <div class="act-stat-card"><div class="act-stat-label">Got a call result</div><div class="act-stat-value good">${c.leadsReached}${reachPct!==null?` (${reachPct}%)`:""}</div></div>
          <div class="act-stat-card"><div class="act-stat-label">Spoke to someone</div><div class="act-stat-value good">${spokeStr}</div></div>
          <div class="act-stat-card"><div class="act-stat-label">No dispositioned call yet</div><div class="act-stat-value">${c.leadsNotYetReached}</div></div>
          <div class="act-stat-card"><div class="act-stat-label">Appointments</div><div class="act-stat-value good">${c.appointmentsSet}</div></div>`;return`
    <div style="border:1px solid var(--border-light); border-radius:10px; padding:14px; margin-bottom:12px; ${opts.uncategorized?"border-style:dashed;":""}">
      <div style="display:flex; justify-content:space-between; align-items:baseline; flex-wrap:wrap; gap:8px;">
        <div style="font-weight:600;">${opts.uncategorized?"Uncategorized (not pushed via this dashboard)":escapeHtml(c.campaign)}</div>
        <div style="color:var(--text-dim); font-size:11px;">${c.lastCallAt?"Last call "+new Date(c.lastCallAt).toLocaleString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{}):"No calls logged yet"}</div>
      </div>
      ${opts.uncategorized?`<div style="font-size:11px; color:var(--text-dim); margin-top:4px;">Real call activity whose phone number doesn't match anything pushed via "Upload list for ReadyMode" — likely a different campaign uploaded straight into ReadyMode, or leads that predate this tool. Shown here so it isn't silently missing.</div>`:""}
      <div class="act-stats-row" style="margin-top:10px; margin-bottom:0;">
        ${statCards}
      </div>
      ${f?`
        <div style="margin-top:10px; padding-top:10px; border-top:1px dashed var(--border-light); font-size:11px; color:var(--text-dim); line-height:1.6;">
          <div><strong style="color:var(--text-2);">From ReadyMode's file</strong> — ${f.totalDialAttempts.toLocaleString()} dial attempt(s) across ${f.dialledAtLeastOnce.toLocaleString()} lead(s), averaging ${f.avgDialsPerDialledLead} each.${f.neverDialled?` ${f.neverDialled} lead(s) show 0 dials — ReadyMode carried their status over from an existing Lead Profile at upload.`:""}${f.importedAt?` Imported ${new Date(f.importedAt).toLocaleString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{})}.`:""}</div>
          ${fileOutcomeBadges.length?`<div style="margin-top:6px; display:flex; flex-wrap:wrap; gap:5px;">${fileOutcomeBadges.map(([k,n])=>`<span class="mono" style="font-size:10px; padding:2px 7px; border:1px solid var(--border-light); border-radius:999px;">${escapeHtml(k)}: ${n}</span>`).join("")}</div>`:""}
          <div style="margin-top:6px;">Live call events (a different question — who was actually spoken to): ${c.leadsReached} lead(s) produced a call result, ${spokeStr} spoke to someone.</div>
        </div>`:""}
      ${dispositionBadges.length?`
        <div style="margin-top:10px;">
          ${dispositionBadges.map(([d,n])=>`<span class="lead-ispeedtolead-badge" style="margin-right:6px; margin-bottom:4px; display:inline-block;">${escapeHtml(d)}: ${n}</span>`).join("")}
        </div>
      `:""}
    </div>
  `}async function loadRmCampaignProgress(){const box=$("rmCampaignProgressList");if(!box)return;box.innerHTML='<div class="empty">Loading…</div>';const result=await callRmApi({action:"campaign_progress"});if(!result.ok){box.innerHTML=`<div class="msg error">${escapeHtml(result.error||"Failed to load campaign progress.")}</div>`;return}const campaigns=result.campaigns||[];if(!campaigns.length&&!result.uncategorized){box.innerHTML='<div class="empty">No campaigns yet — save a lead-posting channel and push some leads, or wait for ReadyMode call-result webhook events to arrive.</div>';return}box.innerHTML=campaigns.map(c=>renderRmCampaignProgressCard(c)).join("")+(result.uncategorized?renderRmCampaignProgressCard(result.uncategorized,{uncategorized:true}):"")}const rmRefreshCampaignProgressBtn=document.getElementById("rmRefreshCampaignProgressBtn");if(rmRefreshCampaignProgressBtn)rmRefreshCampaignProgressBtn.addEventListener("click",()=>loadRmCampaignProgress());const rmProgressUploadInput=document.getElementById("rmProgressUploadInput");if(rmProgressUploadInput)rmProgressUploadInput.addEventListener("change",async e=>{const file=e.target.files[0];const msg=$("rmProgressUploadMsg");msg.style.display="none";if(!file)return;const input=e.target;input.disabled=true;try{msg.textContent="Reading file…";msg.className="msg";msg.style.display="block";const buf=await file.arrayBuffer();await ensureXlsx();const wb=XLSX.read(buf,{type:"array"});const sheet=wb.Sheets[wb.SheetNames[0]];const rows=XLSX.utils.sheet_to_json(sheet,{header:1,defval:"",raw:false});if(rows.length<2){msg.textContent="That file has no data rows.";msg.className="msg error";return}const headerRow=rows[0].map(h=>String(h||"").trim());const norm=v=>String(v||"").trim().toLowerCase();const col=names=>headerRow.findIndex(h=>names.includes(norm(h)));const iPhone=col(["phone"]);if(iPhone===-1){msg.textContent='No "Phone" column found — this does not look like a ReadyMode progress export.';msg.className="msg error";return}const iStatus=col(["status"]);const iTimes=col(["times called (dialer)","times called"]);if(iStatus===-1||iTimes===-1){msg.textContent=`This file is missing "Status" and/or "Times Called (Dialer)" — those two are the whole point of the progress export, so ReadyMode's numbers could not be reproduced without them.`;msg.className="msg error";return}const iCampaign=col(["lead file campaign","batch name","original file name"]);const iFirst=col(["first name"]),iLast=col(["last name"]);const iAddr=col(["address"]),iCity=col(["city"]),iState=col(["state"]),iZip=col(["zip","zip code"]);const iLastDispo=col(["last dispo"]);const iStatusTime=col(["status (time)"]),iStatusAgent=col(["status (agent)"]);const iUpload=col(["upload date"]),iRemoved=col(["removed date"]);const cell=(r,i)=>i>-1?String(r[i]||"").trim():"";const dataRows=rows.slice(1).filter(r=>r.some(c=>String(c||"").trim()!==""));const campaignCounts={};dataRows.forEach(r=>{const v=cell(r,iCampaign);if(v)campaignCounts[v]=(campaignCounts[v]||0)+1});const campaignName=Object.entries(campaignCounts).sort((a,b)=>b[1]-a[1])[0]?.[0]||"";if(!campaignName){msg.textContent='Could not read a campaign name from this file (looked for "Lead File Campaign", "Batch Name", "Original File Name").';msg.className="msg error";return}const distinctCampaigns=Object.keys(campaignCounts).length;const payload=dataRows.map(r=>({phone:cell(r,iPhone),firstName:cell(r,iFirst),lastName:cell(r,iLast),address:cell(r,iAddr),city:cell(r,iCity),state:cell(r,iState),zip:cell(r,iZip),status:cell(r,iStatus),timesCalled:cell(r,iTimes),lastDispo:cell(r,iLastDispo),statusAgent:cell(r,iStatusAgent),statusTime:cell(r,iStatusTime),uploadDate:cell(r,iUpload),removedDate:cell(r,iRemoved)}));const CHUNK=1e3;let imported=0,deduped=0,skipped=0;for(let off=0;off<payload.length;off+=CHUNK){const chunk=payload.slice(off,off+CHUNK);msg.textContent=`Importing row ${off+1}-${off+chunk.length} of ${payload.length} for "${campaignName}"…`;const res=await callRmApi({action:"import_progress_csv",campaignName,filename:file.name,rows:chunk});if(!res.ok){msg.textContent=`Stopped at row ${off+1}: ${res.error||"import failed."} ${imported} row(s) were imported before this.`;msg.className="msg error";return}imported+=res.imported||0;deduped+=res.deduped||0;skipped+=res.skippedNoPhone||0}msg.textContent=`Imported ${imported} lead(s) for "${campaignName}".`+(deduped?` ${deduped} duplicate phone(s) collapsed — the row carrying a real outcome won.`:"")+(skipped?` ${skipped} row(s) skipped with no usable phone number.`:"")+(distinctCampaigns>1?` Note: this file names ${distinctCampaigns} campaigns; everything was filed under the most common one.`:"");msg.className="msg ok";loadRmCampaignProgress()}catch(err){console.error("rmProgressUploadInput: import failed:",err);msg.textContent=`Couldn't read that file: ${err instanceof Error?err.message:String(err)}`;msg.className="msg error";msg.style.display="block"}finally{input.disabled=false;input.value=""}});function parseViewScopeList(viewScope){return(viewScope||"self").split(",").map(s=>s.trim()).filter(Boolean)}let roleListFilter="";async function loadRolesList(){const{data,error}=await sb.from("roles").select("name, allowed_tabs, can_manage_users, is_protected, tab_edit, view_scope, viewable_user_ids, viewable_role, um_sections, activity_sections, leads_sections, csub_sections, finance_sections, hr_sections, data_science_sections, taxes_sections, time_management_sections, dialer_admin_sections, task_category_ids, can_text_clients, can_edit_compensation, can_manual_dial").order("name");const body=$("rolesListBody");if(!body)return;if(error){allRoles=[];body.innerHTML=`<div class="msg error" style="margin:0;">${escapeHtml(error.message)}</div>`;return}allRoles=data||[];renderRolesSideList();applyUmSectionEditGating("roles");permRenderUserList()}function renderRolesSideList(){const body=$("rolesListBody");if(!body)return;const q=(roleListFilter||"").trim().toLowerCase();const shown=allRoles.filter(r=>!q||r.name.toLowerCase().includes(q)||roleDisplayLabel(r.name).toLowerCase().includes(q));if(!shown.length){body.innerHTML='<div class="perm-side-empty">No user types match that.</div>';return}const counts={};(allViewableUsers||[]).forEach(u=>{counts[u.role||"user"]=(counts[u.role||"user"]||0)+1});body.innerHTML=shown.map(r=>{const tabs=(r.allowed_tabs||[]).length;const scopeList=parseViewScopeList(r.view_scope);const scope=["owner","admin"].includes(r.name)?"all users":scopeList.includes("all")?"all users":scopeList.map(sc=>sc==="self"?"themselves":sc==="selected"?`${(r.viewable_user_ids||[]).length} selected`:sc==="specific_role"?roleDisplayLabel(r.viewable_role||"—"):sc).join(" + ")||"themselves";const n=counts[r.name]||0;const tag=r.is_protected?"protected":r.name==="admin"?"core":"";return`
      <button type="button" class="perm-side-item${editingRoleName===r.name?" active":""}" data-role-pick="${escapeHtml(r.name)}">
        <span class="perm-side-item-main">
          <span class="perm-side-item-name">${escapeHtml(roleDisplayLabel(r.name))}${tag?` <span class="type-badge">${tag}</span>`:""}</span>
          <span class="perm-side-item-sub">${tabs} tab${tabs===1?"":"s"} &middot; sees ${escapeHtml(scope)}${n?` &middot; ${n} account${n===1?"":"s"}`:""}</span>
        </span>
      </button>`}).join("");body.querySelectorAll("[data-role-pick]").forEach(btn=>{btn.addEventListener("click",()=>startEditRole(btn.dataset.rolePick))})}let permUserId=null;let permUserRow=null;let permUserOverride=null;let permUserSearchText="";let permUserTypeFilterValue="";let permOverrideByUser={};function permRoleValues(roleName){const r=allRoles.find(x=>x.name===roleName);if(!r)return{allowed_tabs:[],tab_edit:{}};return{allowed_tabs:r.allowed_tabs||[],tab_edit:r.tab_edit||{},um_sections:r.um_sections||{},activity_sections:r.activity_sections||{},leads_sections:r.leads_sections||{},csub_sections:r.csub_sections||{},finance_sections:r.finance_sections||{},hr_sections:r.hr_sections||{},data_science_sections:r.data_science_sections||{},taxes_sections:r.taxes_sections||{},time_management_sections:r.time_management_sections||{},dialer_admin_sections:r.dialer_admin_sections||{},can_text_clients:!!r.can_text_clients,can_edit_compensation:!!r.can_edit_compensation,can_manual_dial:!!r.can_manual_dial}}function permEffectiveValues(roleName,override){const base=permRoleValues(roleName);if(!override)return base;const fam=permissionFamilies().map(f=>f.family);const out=Object.assign({},base);if(Array.isArray(override.allowed_tabs))out.allowed_tabs=override.allowed_tabs;fam.forEach(f=>{out[f]=Object.assign({},base[f]||{},override[f]||{})});out.tab_edit=Object.assign({},base.tab_edit||{},override.tab_edit||{});["can_text_clients","can_edit_compensation","can_manual_dial"].forEach(k=>{if(typeof override[k]==="boolean")out[k]=override[k]});return out}async function permLoadOverrides(){try{const{data}=await sb.from("user_permission_overrides").select("user_id");permOverrideByUser={};(data||[]).forEach(r=>{permOverrideByUser[r.user_id]=true})}catch(e){permOverrideByUser={}}}function permRenderUserList(){const body=$("permUserListBody");if(!body)return;const sel=$("permUserTypeFilter");if(sel&&!sel.dataset.filled){const types=Array.from(new Set((allViewableUsers||[]).map(u=>u.role||"user"))).sort();sel.innerHTML='<option value="">Every user type</option>'+types.map(t=>`<option value="${escapeHtml(t)}">${escapeHtml(roleDisplayLabel(t))}</option>`).join("");sel.dataset.filled="1"}const q=(permUserSearchText||"").trim().toLowerCase();const shown=(allViewableUsers||[]).filter(u=>{if(permUserTypeFilterValue&&(u.role||"user")!==permUserTypeFilterValue)return false;if(!q)return true;return(u.full_name||"").toLowerCase().includes(q)||(u.email||"").toLowerCase().includes(q)});if(!shown.length){body.innerHTML='<div class="perm-side-empty">Nobody matches that.</div>';return}body.innerHTML=shown.map(u=>`
    <button type="button" class="perm-side-item${permUserId===u.id?" active":""}" data-perm-user="${u.id}">
      <span class="perm-side-item-main">
        <span class="perm-side-item-name">${escapeHtml(u.full_name||u.email)}${permOverrideByUser[u.id]?' <span class="perm-custom-tag">Custom</span>':""}</span>
        <span class="perm-side-item-sub">${escapeHtml(roleDisplayLabel(u.role||"user"))}</span>
      </span>
    </button>`).join("");body.querySelectorAll("[data-perm-user]").forEach(btn=>{btn.addEventListener("click",()=>permOpenUser(btn.dataset.permUser))})}async function permOpenUser(userId){const u=(allViewableUsers||[]).find(x=>x.id===userId);if(!u)return;permUserId=userId;permUserRow=u;$("permUserMsg").textContent="";let override=null;try{const{data}=await sb.from("user_permission_overrides").select("*").eq("user_id",userId).maybeSingle();override=data||null}catch(e){}permUserOverride=override;permOverrideByUser[userId]=!!override;const roleName=u.role||"user";$("permUserFormTitle").textContent=`${u.full_name||u.email} — ${roleDisplayLabel(roleName)}`;$("permUserFormIntro").innerHTML=override?`This person has an exception on top of the <b>${escapeHtml(roleDisplayLabel(roleName))}</b> user type. Boxes marked <span class="perm-custom-tag">Custom</span> are where they differ from it.${override.note?` Reason recorded: ${escapeHtml(override.note)}`:""}`:`This person currently gets exactly what the <b>${escapeHtml(roleDisplayLabel(roleName))}</b> user type gives them. Change anything below and it becomes an exception that follows them, not their type.`;$("permUserEditorBody").style.display="";const values=permEffectiveValues(roleName,override);renderPermissionMatrix("permUserTabsPicker",values,{inherited:permRoleValues(roleName)});$("permUserCanTextClientsCb").checked=!!values.can_text_clients;$("permUserCanTextClientsCb").disabled=!ceilingAllowsFlag("can_text_clients");$("permUserCanEditCompensationCb").checked=!!values.can_edit_compensation;$("permUserCanEditCompensationCb").disabled=!ceilingAllowsFlag("can_edit_compensation");{const cb=$("permUserCanManualDialCb");const always=["owner","admin"].includes(roleName);cb.checked=always||!!values.can_manual_dial;cb.disabled=always||!ceilingAllowsFlag("can_manual_dial");cb.title=always?"Owner and Admin can always manual dial.":ceilingAllowsFlag("can_manual_dial")?"":CEILING_TITLE}$("permUserNote").value=override&&override.note||"";$("permUserResetBtn").style.display=override?"":"none";permRenderUserList();applyUmSectionEditGating("roles")}async function permSaveUser(){const msg=$("permUserMsg");if(!permUserId){return}if(permUserId===myUserId){msg.textContent="You cannot change your own access. Ask the Owner.";msg.className="msg error";return}const matrix=permReadMatrix("permUserTabsPicker");const payload=Object.assign({user_id:permUserId},matrix,{can_text_clients:$("permUserCanTextClientsCb").checked,can_edit_compensation:$("permUserCanEditCompensationCb").checked,note:$("permUserNote").value.trim()||null});{const roleName=permUserRow&&permUserRow.role||"user";const cb=$("permUserCanManualDialCb");payload.can_manual_dial=["owner","admin"].includes(roleName)||cb.checked===!!permRoleValues(roleName).can_manual_dial?null:cb.checked}const btn=$("permUserSaveBtn");btn.disabled=true;msg.textContent="Saving…";msg.className="msg";const{error}=await sb.from("user_permission_overrides").upsert(payload,{onConflict:"user_id"});btn.disabled=false;if(error){msg.textContent=error.message;msg.className="msg error";return}logActivity("permission.user_override","user",permUserId,`Set a per-person access exception for ${permUserRow?permUserRow.full_name||permUserRow.email:permUserId}`);msg.textContent="Saved. It applies the next time they sign in or refresh.";msg.className="msg success";await permLoadOverrides();await permOpenUser(permUserId)}async function permResetUser(){if(!permUserId)return;const who=permUserRow?permUserRow.full_name||permUserRow.email:"this person";if(!confirm(`Remove the exception for ${who}? They go back to exactly what their user type gives them, the next time they sign in or refresh.`))return;const msg=$("permUserMsg");const{error}=await sb.from("user_permission_overrides").delete().eq("user_id",permUserId);if(error){msg.textContent=error.message;msg.className="msg error";return}logActivity("permission.user_override_clear","user",permUserId,`Removed the per-person access exception for ${who}`);await permLoadOverrides();await permOpenUser(permUserId);msg.textContent="Exception removed — they follow their user type again.";msg.className="msg success"}function permSetMode(mode){document.querySelectorAll(".perm-mode-btn").forEach(b=>b.classList.toggle("active",b.dataset.permMode===mode));const g=$("permPaneGroups"),pp=$("permPanePeople");if(g)g.style.display=mode==="groups"?"":"none";if(pp)pp.style.display=mode==="people"?"":"none";if(mode==="people"){permLoadOverrides().then(permRenderUserList)}}function permInitRolesSection(){const banner=$("permCeilingBanner");if(banner){if(myGrantCeiling){$("permCeilingBannerText").innerHTML=`You can only grant access you have yourself. Anything the Owner has not given <b>${escapeHtml(roleDisplayLabel(myRole))}</b> is shown greyed out, and you cannot change your own user type. Ask the Owner to widen your access first.`;banner.style.display=""}else{banner.style.display="none"}}permRenderUserList()}document.querySelectorAll(".perm-mode-btn").forEach(b=>{b.addEventListener("click",()=>permSetMode(b.dataset.permMode))});document.querySelectorAll(".perm-bulk-btn").forEach(b=>{b.addEventListener("click",()=>{const pane=b.closest("#permPanePeople")?"permUserTabsPicker":"roleTabsPicker";const root=permMatrixEl(pane);if(root)permApplyBulk(root,b.dataset.permBulk)})});if($("roleSearchInput"))$("roleSearchInput").addEventListener("input",e=>{roleListFilter=e.target.value;renderRolesSideList()});if($("roleNewBtn"))$("roleNewBtn").addEventListener("click",()=>{resetRoleForm();renderRolesSideList()});if($("permUserSearch"))$("permUserSearch").addEventListener("input",e=>{permUserSearchText=e.target.value;permRenderUserList()});if($("permUserTypeFilter"))$("permUserTypeFilter").addEventListener("change",e=>{permUserTypeFilterValue=e.target.value;permRenderUserList()});if($("permUserSaveBtn"))$("permUserSaveBtn").addEventListener("click",permSaveUser);if($("permUserResetBtn"))$("permUserResetBtn").addEventListener("click",permResetUser);if($("roleDeleteBtn"))$("roleDeleteBtn").addEventListener("click",()=>{if(editingRoleName)deleteRole(editingRoleName)});function startEditRole(name){const role=allRoles.find(r=>r.name===name);if(!role)return;editingRoleName=name;$("rolesFormTitle").textContent=`Editing "${name}"`;$("roleNameInput").value=role.name;const _renameable=!["owner","admin"].includes(role.name);$("roleNameInput").disabled=!_renameable;$("roleNameInput").dataset.original=role.name;$("roleNameInput").title=_renameable?"Change the name and click away to rename this user type":"Core user type — cannot be renamed";if($("roleDeleteBtn"))$("roleDeleteBtn").style.display=role.is_protected||["owner","admin"].includes(role.name)?"none":"";renderRoleTabsPicker(role.allowed_tabs||[],role.tab_edit||{},role.um_sections||{},role.activity_sections||{},role.leads_sections||{},role.csub_sections||{},role.finance_sections||{},role.hr_sections||{},role.data_science_sections||{},role.taxes_sections||{},role.time_management_sections||{},role.dialer_admin_sections||{});renderRoleTaskCategoriesPicker(role.task_category_ids||[]);$("roleCanTextClientsCb").checked=!!role.can_text_clients;$("roleCanEditCompensationCb").checked=!!role.can_edit_compensation;$("roleCanManualDialCb").checked=!!role.can_manual_dial;$("roleCanManualDialCb").disabled=!ceilingAllowsFlag("can_manual_dial");$("roleCanManualDialCb").title=role.name==="admin"?"Admins can always manual dial themselves. This decides whether they may give it to others.":ceilingAllowsFlag("can_manual_dial")?"":CEILING_TITLE;const scopeList=parseViewScopeList(role.view_scope);const isAll=scopeList.includes("all");$("roleViewScopeSelf").checked=!isAll&&scopeList.includes("self");$("roleViewScopeSelected").checked=!isAll&&scopeList.includes("selected");$("roleViewScopeSpecificRole").checked=!isAll&&scopeList.includes("specific_role");$("roleViewScopeAll").checked=isAll;[$("roleViewScopeSelf"),$("roleViewScopeSelected"),$("roleViewScopeSpecificRole")].forEach(cb=>cb.disabled=isAll);renderRoleViewUsersPicker(role.viewable_user_ids||[]);renderRoleViewSpecificRoleOptions(role.viewable_role||null);updateRoleViewScopeVisibility();$("roleCancelEditBtn").style.display="inline-block";$("rolesMsg").textContent="";if(typeof renderRolesSideList==="function")renderRolesSideList()}$("roleCancelEditBtn").addEventListener("click",resetRoleForm);if($("roleNameInput")){$("roleNameInput").addEventListener("blur",()=>{if(editingRoleName&&!$("roleNameInput").disabled)saveRoleRename($("roleNameInput"))});$("roleNameInput").addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();$("roleNameInput").blur()}if(e.key==="Escape"&&editingRoleName){$("roleNameInput").value=$("roleNameInput").dataset.original||"";$("roleNameInput").blur()}})}$("roleSaveBtn").addEventListener("click",async()=>{const msg=$("rolesMsg");const name=$("roleNameInput").value.trim().toLowerCase();const matrix=permReadMatrix("roleTabsPicker");const allowedTabs=matrix.allowed_tabs;const tabEdit=matrix.tab_edit;const checkedScopes=["self","selected","specific_role"].filter(s=>$(`roleViewScope${s==="self"?"Self":s==="selected"?"Selected":"SpecificRole"}`).checked);const viewScopeList=$("roleViewScopeAll").checked?["all"]:checkedScopes.length?checkedScopes:["self"];const viewScope=viewScopeList.join(",");const viewableUserIds=viewScopeList.includes("selected")?Array.from(document.querySelectorAll(".role-view-user-cb:checked")).map(cb=>cb.value):[];const viewableRole=viewScopeList.includes("specific_role")?$("roleViewSpecificRoleSelect").value||null:null;if(!name){msg.textContent="Give the role a name.";msg.className="msg error";return}if(["owner","admin"].includes(name)&&!editingRoleName){msg.textContent='"owner" and "admin" already exist and are protected.';msg.className="msg error";return}if(allowedTabs.length===0){msg.textContent="Pick at least one tab, or this role won’t have any dashboard access at all.";msg.className="msg error";return}if(viewScopeList.includes("selected")&&viewableUserIds.length===0){msg.textContent='Pick at least one user this role can view, or uncheck "Only selected users".';msg.className="msg error";return}if(viewScopeList.includes("specific_role")&&!viewableRole){msg.textContent="Pick which user type this role can view.";msg.className="msg error";return}const taskCategoryIds=Array.from(document.querySelectorAll(".role-task-category-cb:checked")).map(cb=>cb.value);const sensitiveTabs=allowedTabs.filter(t=>["finance","hr","data_science"].includes(t));const grantsAllUsers=viewScopeList.includes("all");if((sensitiveTabs.length||grantsAllUsers)&&!confirm(`This role will have${sensitiveTabs.length?` access to: ${sensitiveTabs.join(", ")}`:""}${sensitiveTabs.length&&grantsAllUsers?" and":""}${grantsAllUsers?" visibility into every user’s data (view scope: All users)":""}. This takes effect immediately for every account with this role. Save anyway?`))return;const payload=Object.assign({},matrix,{view_scope:viewScope,viewable_user_ids:viewableUserIds,viewable_role:viewableRole,task_category_ids:taskCategoryIds,can_text_clients:$("roleCanTextClientsCb").checked,can_edit_compensation:$("roleCanEditCompensationCb").checked,can_manual_dial:$("roleCanManualDialCb").checked});if(editingRoleName!=="admin")payload.can_manage_users=false;{const daTab=allowedTabs.includes("dialer_admin");const vals=Object.values(matrix.dialer_admin_sections||{});payload.can_manage_dialer=daTab&&vals.some(v=>v.edit);payload.can_review_calls=daTab&&vals.some(v=>v.view)}if(editingRoleName){const{data,error}=await sb.from("roles").update(payload).eq("name",editingRoleName).select("name");if(error){msg.textContent=error.message;msg.className="msg error";return}if(!data||data.length===0){msg.textContent="Nothing was saved — you may not be signed in as the Owner account, which is required to edit user types.";msg.className="msg error";return}logActivity("role.update",null,null,`Updated role "${editingRoleName}"`)}else{const{error}=await sb.from("roles").insert({name,...payload});if(error){msg.textContent=/duplicate|unique/i.test(error.message)?"That role name already exists.":error.message;msg.className="msg error";return}logActivity("role.create",null,null,`Created role "${name}" (tabs: ${allowedTabs.join(", ")})`)}resetRoleForm();await loadRolesList();await refreshAddUserRoleOptions()});async function deleteRole(name){if(["owner","admin"].includes(name)){alert(`"${name}" can't be deleted.`);return}if(!confirm(`Delete the "${name}" role? Any account still using it must be reassigned first.`))return;const{error}=await sb.from("roles").delete().eq("name",name);if(error){alert(/foreign key|violat/i.test(error.message)?`Can't delete "${name}" — at least one account still has this role. Reassign them first.`:error.message);return}await loadRolesList();await refreshAddUserRoleOptions();logActivity("role.delete",null,null,`Deleted role "${name}"`)}async function roleRenameInvokeErrorMessage(error,data){if(data&&data.error)return data.error;if(error&&error.context&&typeof error.context.json==="function"){try{const body=await error.context.json();if(body&&body.error)return body.error}catch(e){}}return error&&error.message||"unknown error"}function _roleNameText(el){return(el.value!==void 0?el.value:el.textContent)||""}function _setRoleNameText(el,v){if(el.value!==void 0)el.value=v;else el.textContent=v}async function saveRoleRename(nameEl){const newName=_roleNameText(nameEl).trim().toLowerCase();const oldName=nameEl.dataset.original;if(!newName||newName===oldName){_setRoleNameText(nameEl,oldName);return}const msg=$("rolesMsg");const{data,error}=await sb.functions.invoke("staff-rename-role",{body:{old_name:oldName,new_name:newName}});if(error||!data||data.ok===false){const errMsg=await roleRenameInvokeErrorMessage(error,data);msg.textContent=errMsg;msg.className="msg error";_setRoleNameText(nameEl,oldName);return}nameEl.dataset.original=newName;_setRoleNameText(nameEl,newName);msg.textContent="Renamed.";msg.className="msg";msg.style.color="var(--focus)";logActivity("role.rename",null,null,`Renamed role "${oldName}" to "${newName}"`);if(editingRoleName===oldName){editingRoleName=newName;$("rolesFormTitle").textContent=`Editing "${newName}"`;$("roleNameInput").value=newName}await loadRolesList();await refreshAddUserRoleOptions()}async function saveTaskRename(nameEl){const newName=nameEl.textContent.trim();const taskId=nameEl.dataset.id;const oldName=nameEl.dataset.original;if(!newName||newName===oldName){nameEl.textContent=oldName;return}const{error}=await sb.from("tasks").update({name:newName}).eq("id",taskId);const msg=$("tasksMsg");if(error){msg.textContent=error.message;msg.className="msg error";nameEl.textContent=oldName}else{nameEl.dataset.original=newName;msg.textContent="Renamed.";msg.className="msg";msg.style.color="var(--focus)";logActivity("task.rename","task",taskId,`Renamed task "${oldName}" to "${newName}"`)}}$("showArchivedTasksToggle")&&$("showArchivedTasksToggle").addEventListener("change",loadTasksList);$("addTaskBtn").addEventListener("click",async()=>{const btn=$("addTaskBtn");if(btn.disabled)return;const input=$("newTaskInput");const name=input.value.trim();if(!name)return;const msg=$("tasksMsg");btn.disabled=true;try{const projectId=await getOrCreateDefaultProject();const{data,error}=await sb.from("tasks").insert({project_id:projectId,name}).select("id").single();if(error)throw error;input.value="";msg.textContent="";await loadTasksList();logActivity("task.create","task",data.id,`Created task "${name}"`)}catch(e){msg.textContent=e.message;msg.className="msg error"}finally{btn.disabled=false}});async function deleteTask(taskId,taskName){const{error}=await sb.from("tasks").delete().eq("id",taskId);if(error){if(error.code==="23503"||/foreign key|violates/i.test(error.message)){if(confirm(`"${taskName}" has time entries and/or leads recorded against it, so it can't be permanently deleted without breaking past reports. Archive it instead? Archived tasks disappear from assignment and the extension's task picker, but every existing timesheet/lead still shows "${taskName}" correctly. You can unarchive it later.`)){await archiveTask(taskId,taskName)}return}alert(error.message);return}await loadTasksList();if(currentDetailAgent)await loadAssignedTasks(currentDetailAgent.id);logActivity("task.delete","task",taskId,`Deleted task "${taskName||taskId}"`)}async function archiveTask(taskId,taskName){const{error}=await sb.from("tasks").update({archived_at:new Date().toISOString()}).eq("id",taskId);if(error){alert(error.message);return}await loadTasksList();logActivity("task.archive","task",taskId,`Archived task "${taskName}"`)}async function unarchiveTask(taskId,taskName){const{error}=await sb.from("tasks").update({archived_at:null}).eq("id",taskId);if(error){alert(error.message);return}await loadTasksList();logActivity("task.unarchive","task",taskId,`Unarchived task "${taskName}"`)}async function loadAssignedTasks(agentId){const{data:allTasks}=await sb.from("tasks").select("id, name, archived_at").order("name");const{data:assignments}=await sb.from("task_assignments").select("task_id").eq("user_id",agentId);const assignedIds=new Set((assignments||[]).map(a=>a.task_id));const tasks=(allTasks||[]).filter(t=>!t.archived_at||assignedIds.has(t.id));const list=$("assignedTasksList");list.innerHTML="";if(!tasks||tasks.length===0){list.innerHTML='<div class="empty">No tasks exist yet — add some above.</div>';return}tasks.forEach(t=>{const row=document.createElement("div");row.className="assign-row";const checked=assignedIds.has(t.id);row.innerHTML=`
      <input type="checkbox" id="assign-${t.id}" class="edit-only" ${checked?"checked":""}>
      <label for="assign-${t.id}">${escapeHtml(t.name)}${t.archived_at?' <span class="mono" style="font-size:10px; color:var(--text-dim);">(archived)</span>':""}</label>
    `;row.querySelector("input").addEventListener("change",async e=>{const agentLabel=currentDetailAgent?currentDetailAgent.full_name||currentDetailAgent.email:agentId;if(e.target.checked){await sb.from("task_assignments").insert({task_id:t.id,user_id:agentId});logActivity("assignment.add","agent",agentId,`Assigned "${t.name}" to ${agentLabel}`)}else{await sb.from("task_assignments").delete().eq("task_id",t.id).eq("user_id",agentId);logActivity("assignment.remove","agent",agentId,`Unassigned "${t.name}" from ${agentLabel}`)}loadTasksList()});list.appendChild(row)});applyTimeManagementSectionEditGating("staff")}let lastTimesheetRows=[];let tsAllAgents=[];let tsAllTasks=[];let tsShotsLoaded={};let tsAgentTz={};function todayDateInputValue(){return localDateKeyInZone(new Date,globalDisplayTzMode)}function endOfLoginMonthDateInputValue(){const[y,m]=todayDateInputValue().split("-").map(Number);const lastDay=new Date(y,m,0).getDate();return`${String(y).padStart(4,"0")}-${String(m).padStart(2,"0")}-${String(lastDay).padStart(2,"0")}`}const DEFAULT_DATE_RANGE_FROM="2026-07-01";const DEFAULT_DATE_RANGE_TO="2050-12-31";function tsIsSingleDay(){const fromVal=$("tsFromDateInput").value||todayDateInputValue();const toVal=$("tsToDateInput").value||fromVal;return fromVal===toVal}function tsRangeBounds(){const fromVal=$("tsFromDateInput").value||todayDateInputValue();const toVal=$("tsToDateInput").value||fromVal;const[fy,fm,fd]=fromVal.split("-").map(Number);const[ty,tm,td]=toVal.split("-").map(Number);const tz=tsTzFor($("tsAgentFilter").value)||null;const{startMs}=dayBoundsMsInZone(tz,null,fy,fm,fd);const{endMs}=dayBoundsMsInZone(tz,null,ty,tm,td);return{start:new Date(startMs).toISOString(),end:new Date(endMs).toISOString(),startMs,endMs}}function clipSeconds(rawStart,rawEnd,rangeStartMs,rangeEndMs){const s=Math.max(rawStart,rangeStartMs);const e=Math.min(rawEnd,rangeEndMs);return Math.max(0,(e-s)/1e3)}function buildStatusSegments(entry,idlePeriods,breaksForEntry,windowStartMs,windowEndMs){const rawStart=new Date(entry.start_time).getTime();const rawEnd=entry.end_time?new Date(entry.end_time).getTime():Date.now();const spanStart=Math.max(rawStart,windowStartMs);const spanEnd=Math.min(rawEnd,windowEndMs);if(spanEnd<=spanStart)return[];const pauses=[];(idlePeriods||[]).forEach(p=>{const s=Math.max(new Date(p.start_time).getTime(),spanStart);const en=Math.min(p.end_time?new Date(p.end_time).getTime():Date.now(),spanEnd);if(en>s)pauses.push({status:"idle",start:s,end:en,note:p.agent_note||null})});(breaksForEntry||[]).forEach(p=>{const s=Math.max(new Date(p.start_time).getTime(),spanStart);const en=Math.min(p.end_time?new Date(p.end_time).getTime():Date.now(),spanEnd);if(en>s)pauses.push({status:"break",start:s,end:en})});pauses.sort((a,b)=>a.start-b.start);const segments=[];let cursor=spanStart;pauses.forEach(p=>{if(p.start>cursor)segments.push({status:"tracking",start:cursor,end:p.start});if(p.end>cursor){segments.push({status:p.status,start:Math.max(p.start,cursor),end:p.end,note:p.note||null});cursor=p.end}});if(cursor<spanEnd)segments.push({status:"tracking",start:cursor,end:spanEnd});return segments.filter(s=>s.end>s.start)}$("tsFromDateInput").addEventListener("change",()=>{if($("tsToDateInput").value&&$("tsToDateInput").value<$("tsFromDateInput").value){$("tsToDateInput").value=$("tsFromDateInput").value}loadTimesheets()});$("tsToDateInput").addEventListener("change",()=>{if($("tsFromDateInput").value&&$("tsFromDateInput").value>$("tsToDateInput").value){$("tsFromDateInput").value=$("tsToDateInput").value}loadTimesheets()});$("tsAgentFilter").addEventListener("change",loadTimesheets);function tsTzFor(agentId){if(globalDisplayTzMode==="agent")return agentId&&tsAgentTz[agentId]||void 0;return globalDisplayTzMode||void 0}function tsFmtTime(d,agentId){const tz=tsTzFor(agentId);return new Date(d).toLocaleTimeString([],{hour:"2-digit",minute:"2-digit",...tz?{timeZone:tz}:{}})}function tsFmtDateTime(d,agentId){const tz=tsTzFor(agentId);return new Date(d).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...tz?{timeZone:tz}:{}})}function mergedCoverageSec(rows,rangeStartMs,rangeEndMs,entryBounds){const intervals=(rows||[]).map(r=>{const eb=entryBounds&&entryBounds[r.time_entry_id];const lo=eb?Math.max(rangeStartMs,eb.start):rangeStartMs;const hi=eb?Math.min(rangeEndMs,eb.end):rangeEndMs;const s=Math.max(new Date(r.start_time).getTime(),lo);const e=Math.min(r.end_time?new Date(r.end_time).getTime():Date.now(),hi);return[s,e]}).filter(([s,e])=>e>s).sort((a,b)=>a[0]-b[0]);let sec=0,curStart=null,curEnd=null;intervals.forEach(([s,e])=>{if(curStart===null){curStart=s;curEnd=e;return}if(s<=curEnd){curEnd=Math.max(curEnd,e)}else{sec+=(curEnd-curStart)/1e3;curStart=s;curEnd=e}});if(curStart!==null)sec+=(curEnd-curStart)/1e3;return sec}async function renderDayTimeline(entries,agentId,startMs,endMs){const track=$("tsDayTimeline");const marks=$("tsDayHourMarks");const label=$("tsDayTotalLabel");const singleDay=tsIsSingleDay();if(!agentId){track.style.display="none";marks.style.display="none";$("tsDayLegend").style.display="none";$("tsDayBreakdown").style.display="none";$("tsDayActivity").textContent="—";label.textContent="Select a user to see their totals";return}track.style.display=singleDay?"block":"none";marks.style.display=singleDay?"flex":"none";$("tsDayLegend").style.display=singleDay?"flex":"none";$("tsDayBreakdown").style.display="flex";track.innerHTML="";const dayMs=endMs-startMs;let grossSec=0;entries.forEach(e=>{const rawStart=new Date(e.start_time).getTime();const rawEnd=liveEntryEndMs(e);const sec=clipSeconds(rawStart,rawEnd,startMs,endMs);if(sec<=0)return;grossSec+=sec;if(!singleDay)return;const startPct=Math.max(0,(Math.max(rawStart,startMs)-startMs)/dayMs*100);const endPct=Math.min(100,(Math.min(rawEnd,endMs)-startMs)/dayMs*100);const div=document.createElement("div");div.className="segment";div.style.left=startPct+"%";div.style.width=Math.max(.15,endPct-startPct)+"%";div.style.background="var(--good)";track.appendChild(div)});const entryIds=entries.map(e=>e.id);const[breaksToday,idleToday]=entryIds.length?await Promise.all([fetchAllRows((from,to)=>sb.from("breaks").select("time_entry_id, start_time, end_time").in("time_entry_id",entryIds).order("id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("idle_periods").select("time_entry_id, start_time, end_time").in("time_entry_id",entryIds).order("id",{ascending:true}).range(from,to))]):[[],[]];const entryBounds={};entries.forEach(e=>{entryBounds[e.id]={start:new Date(e.start_time).getTime(),end:e.end_time?new Date(e.end_time).getTime():Date.now()}});const drawOverlay=(rows,color)=>{if(!singleDay)return;(rows||[]).forEach(r=>{const eb=entryBounds[r.time_entry_id];const lo=eb?Math.max(startMs,eb.start):startMs;const hi=eb?Math.min(endMs,eb.end):endMs;const rawStart=new Date(r.start_time).getTime();const rawEnd=r.end_time?new Date(r.end_time).getTime():Date.now();const s=clipSeconds(rawStart,rawEnd,lo,hi);if(s<=0)return;const startPct=Math.max(0,(Math.max(rawStart,lo)-startMs)/dayMs*100);const endPct=Math.min(100,(Math.min(rawEnd,hi)-startMs)/dayMs*100);const div=document.createElement("div");div.className="segment";div.style.left=startPct+"%";div.style.width=Math.max(.15,endPct-startPct)+"%";div.style.background=color;track.appendChild(div)})};drawOverlay(breaksToday,"var(--away)");drawOverlay(idleToday,"var(--danger)");const tableBreakSec=mergedCoverageSec(breaksToday,startMs,endMs,entryBounds);const tableIdleSec=mergedCoverageSec(idleToday,startMs,endMs,entryBounds);let manualBreakSec=0,manualIdleSec=0;entries.forEach(e=>{if(e.is_manual){manualBreakSec+=e.break_seconds||0;manualIdleSec+=e.idle_seconds||0}});const breakSec=tableBreakSec+manualBreakSec;const idleSec=tableIdleSec+manualIdleSec;const productiveSec=Math.max(0,grossSec-breakSec-idleSec);const activitySamples=entryIds.length?await fetchAllRows((from,to)=>sb.from("activity_samples").select("time_entry_id, activity_pct, sampled_at").in("time_entry_id",entryIds).order("id",{ascending:true}).range(from,to)):[];const activityValues=(activitySamples||[]).filter(s=>{const eb=entryBounds[s.time_entry_id];const lo=eb?Math.max(startMs,eb.start):startMs;const hi=eb?Math.min(endMs,eb.end):endMs;const ms=new Date(s.sampled_at).getTime();return ms>=lo&&ms<hi&&s.activity_pct!=null}).map(s=>s.activity_pct);const totalActivityPct=activityValues.length?Math.round(activityValues.reduce((a,v)=>a+v,0)/activityValues.length):null;$("tsDayProductive").textContent=fmtHMSLong(productiveSec);$("tsDayBreak").textContent=fmtHMSLong(breakSec);$("tsDayIdle").textContent=fmtHMSLong(idleSec);$("tsDayActivity").textContent=totalActivityPct!==null?totalActivityPct+"%":"—";const fromVal=$("tsFromDateInput").value||todayDateInputValue();const toVal=$("tsToDateInput").value||fromVal;const fmtDate=v=>new Date(v+"T00:00:00").toLocaleDateString([],{weekday:"short",month:"short",day:"numeric",year:"numeric"});const rangeLabel=singleDay?fmtDate(fromVal):`${fmtDate(fromVal)} – ${fmtDate(toVal)}`;label.textContent=`${rangeLabel}: ${fmtHMSLong(productiveSec)}`}function fmtHMSLong(sec){sec=Math.max(0,Math.round(sec));const h=Math.floor(sec/3600),m=Math.floor(sec%3600/60),s=sec%60;return`${h}:${String(m).padStart(2,"0")}:${String(s).padStart(2,"0")}`}document.addEventListener("click",e=>{if(!e.target.closest(".ts-actions")){document.querySelectorAll(".ts-dropdown-menu.open").forEach(m=>m.classList.remove("open"))}});async function loadTimesheets(){tsShotsLoaded={};if(!$("tsFromDateInput").value)$("tsFromDateInput").value=todayDateInputValue();if(!$("tsToDateInput").value)$("tsToDateInput").value=endOfLoginMonthDateInputValue();const agents=await fetchAllRows((from,to)=>sb.from("profiles").select("id, full_name, email, role").order("full_name").range(from,to));tsAllAgents=(agents||[]).filter(a=>!PROTECTED_ROLES.has(a.role));const tasks=await fetchAllRows((from,to)=>sb.from("tasks").select("id, name").order("name").range(from,to));tsAllTasks=tasks||[];const scheduleRows=await fetchAllRows((from,to)=>sb.from("schedules").select("user_id, timezone").order("user_id",{ascending:true}).range(from,to));tsAgentTz={};(scheduleRows||[]).forEach(s=>{if(s.timezone)tsAgentTz[s.user_id]=s.timezone});const filterSel=$("tsAgentFilter");const prevFilter=filterSel.value;filterSel.innerHTML=tsAllAgents.map(a=>`<option value="${a.id}">${escapeHtml(a.full_name||a.email)}</option>`).join("");filterSel.value=prevFilter&&tsAllAgents.some(a=>a.id===prevFilter)?prevFilter:(tsAllAgents[0]||{}).id||"";$("tsModalAgent").innerHTML=tsAllAgents.map(a=>`<option value="${a.id}">${escapeHtml(a.full_name||a.email)}</option>`).join("");$("tsModalTask").innerHTML='<option value="">No task</option>'+tsAllTasks.map(t=>`<option value="${t.id}">${escapeHtml(t.name)}</option>`).join("");const{start,end,startMs,endMs}=tsRangeBounds();const rawEntries=await fetchAllRows((from,to)=>{let q=sb.from("time_entries").select("id, user_id, task_id, start_time, end_time, idle_seconds, break_seconds, is_manual, is_edited, last_heartbeat_at").lte("start_time",end).or(`end_time.is.null,end_time.gte.${start}`).order("start_time",{ascending:false});if(filterSel.value)q=q.eq("user_id",filterSel.value);return q.range(from,to)});const seenKeys=new Set;const entries=(rawEntries||[]).filter(e=>{const key=`${e.user_id}|${e.task_id||""}|${e.start_time}|${e.end_time||""}`;if(seenKeys.has(key))return false;seenKeys.add(key);return true});const agentName=id=>{const a=tsAllAgents.find(x=>x.id===id);return a?a.full_name||a.email:"—"};const taskName=id=>{const t=tsAllTasks.find(x=>x.id===id);return t?t.name:"—"};await renderDayTimeline(entries||[],filterSel.value,startMs,endMs);const body=$("timesheetBody");body.innerHTML="";lastTimesheetRows=[];if(!entries||entries.length===0){body.innerHTML='<tr><td colspan="7" class="empty">No entries in this range.</td></tr>';return}const entryIds=entries.map(e=>e.id);const shotRows=await fetchAllRows((from,to)=>sb.from("screenshots").select("time_entry_id, taken_at").in("time_entry_id",entryIds).order("id",{ascending:true}).range(from,to));const shotsByEntry={};(shotRows||[]).forEach(s=>{if(!shotsByEntry[s.time_entry_id])shotsByEntry[s.time_entry_id]=[];shotsByEntry[s.time_entry_id].push(s.taken_at)});const sampleRows=await fetchAllRows((from,to)=>sb.from("activity_samples").select("time_entry_id, activity_pct, mouse_pct, keyboard_pct, sampled_at").in("time_entry_id",entryIds).order("id",{ascending:true}).range(from,to));const samplesByEntry={};(sampleRows||[]).forEach(s=>{if(!samplesByEntry[s.time_entry_id])samplesByEntry[s.time_entry_id]=[];samplesByEntry[s.time_entry_id].push(s)});const[idleRows,breakRows]=await Promise.all([fetchAllRows((from,to)=>sb.from("idle_periods").select("time_entry_id, start_time, end_time, agent_note").in("time_entry_id",entryIds).order("id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("breaks").select("time_entry_id, start_time, end_time").in("time_entry_id",entryIds).order("id",{ascending:true}).range(from,to))]);const idleByEntry={};(idleRows||[]).forEach(r=>{if(!idleByEntry[r.time_entry_id])idleByEntry[r.time_entry_id]=[];idleByEntry[r.time_entry_id].push(r)});const breakByEntry={};(breakRows||[]).forEach(r=>{if(!breakByEntry[r.time_entry_id])breakByEntry[r.time_entry_id]=[];breakByEntry[r.time_entry_id].push(r)});const STATUS_META={tracking:{label:"productive",dot:"on",text:"pct-focus"},idle:{label:"idle",dot:"status-idle",text:"text-idle"},break:{label:"break",dot:"status-break",text:"text-break"}};entries.forEach(e=>{const task=tsAllTasks.find(t=>t.id===e.task_id);const taskLabel=task?task.name:"No task";const initial=taskLabel.charAt(0).toUpperCase();const showFullDate=!tsIsSingleDay()||!filterSel.value;const timeFmt=d=>showFullDate?tsFmtDateTime(d,e.user_id):tsFmtTime(d,e.user_id);const segments=buildStatusSegments(e,idleByEntry[e.id],breakByEntry[e.id],startMs,endMs);const mySamples=samplesByEntry[e.id]||[];const myShots=shotsByEntry[e.id]||[];let actionsAttached=false;const segmentsNewestFirst=segments.map((seg,i)=>({seg,segIdx:i})).reverse();segmentsNewestFirst.forEach(({seg,segIdx})=>{const meta=STATUS_META[seg.status];const durSec=(seg.end-seg.start)/1e3;const shotsInSeg=myShots.filter(t=>{const ms=new Date(t).getTime();return ms>=seg.start&&ms<seg.end}).length;let activityPct=null;let activityKb=null,activityMouse=null;if(seg.status==="tracking"){const inSeg=mySamples.filter(s=>{const ms=new Date(s.sampled_at).getTime();return ms>=seg.start&&ms<seg.end});if(inSeg.length){activityPct=Math.round(inSeg.reduce((a,s)=>a+(s.activity_pct||0),0)/inSeg.length);const kbRows=inSeg.filter(s=>s.keyboard_pct!=null);const mRows=inSeg.filter(s=>s.mouse_pct!=null);if(kbRows.length)activityKb=Math.round(kbRows.reduce((a,s)=>a+s.keyboard_pct,0)/kbRows.length);if(mRows.length)activityMouse=Math.round(mRows.reduce((a,s)=>a+s.mouse_pct,0)/mRows.length)}}const activityTitle=activityKb!=null||activityMouse!=null?` title="keyboard ${activityKb!=null?activityKb:0}% · mouse ${activityMouse!=null?activityMouse:0}%"`:"";const timeLabel=`${timeFmt(seg.start)} – ${seg.status==="tracking"&&segIdx===segments.length-1&&!e.end_time?"live":timeFmt(seg.end)}`;const manualTag=seg.status==="tracking"&&e.is_manual?' <span class="task-row-count" style="margin-left:4px;">manual</span>':"";const editedTag=seg.status==="tracking"&&!e.is_manual&&e.is_edited?' <span class="task-row-count" style="margin-left:4px;">manually adjusted</span>':"";const idleNoteTag=seg.status==="idle"&&seg.note?` <span class="task-row-count" style="margin-left:4px;" title="${escapeHtml(seg.note)}">${escapeHtml(seg.note.length>40?seg.note.slice(0,40)+"…":seg.note)}</span>`:"";lastTimesheetRows.push({agent:agentName(e.user_id),task:taskLabel,status:meta.label,activity:activityPct!==null?activityPct+"%":"—",duration:fmtShort(durSec),time:timeLabel,shots:shotsInSeg});const segKey=`${e.id}__${segIdx}`;const tr=document.createElement("tr");tr.innerHTML=`
        <td>
          <div class="task-cell">
            <div class="task-avatar" style="background:${colorFor(e.task_id,tsAllTasks)}">${initial}</div>
            <div class="task-cell-text">
              <div class="task-cell-name">${taskLabel}${manualTag}${editedTag}</div>
              <div class="task-cell-sub">${!filterSel.value?agentName(e.user_id):"General"}</div>
            </div>
          </div>
        </td>
        <td><span class="dot ${meta.dot}"></span><span class="${meta.text}">${meta.label}</span>${idleNoteTag}</td>
        <td class="mono ${activityPct!==null?"pct-focus":""}"${activityTitle}>${activityPct!==null?activityPct+"%":"—"}</td>
        <td class="mono">${fmtShort(durSec)}</td>
        <td class="mono">${timeLabel}</td>
        <td><button class="shots-toggle" data-entry="${e.id}" data-seg="${segIdx}"><span class="caret">▸</span>${shotsInSeg}</button></td>
        <td>${seg.status==="tracking"&&!actionsAttached?`<div class="ts-actions">
                 <button class="ts-actions-btn edit-only" data-id="${e.id}">Actions ▾</button>
                 <div class="ts-dropdown-menu">
                   <button class="ts-menu-edit">Edit</button>
                   ${!e.end_time?'<button class="ts-menu-endnow">End now</button>':""}
                   <button class="ts-menu-danger danger">Delete</button>
                 </div>
               </div>`:""}</td>
      `;if(seg.status==="tracking"&&!actionsAttached){actionsAttached=true;const actionsBtn=tr.querySelector(".ts-actions-btn");const menu=tr.querySelector(".ts-dropdown-menu");actionsBtn.addEventListener("click",ev=>{ev.stopPropagation();document.querySelectorAll(".ts-dropdown-menu.open").forEach(m=>{if(m!==menu)m.classList.remove("open")});menu.classList.toggle("open")});tr.querySelector(".ts-menu-edit").addEventListener("click",()=>{menu.classList.remove("open");openTsModal(e)});const endNowBtn=tr.querySelector(".ts-menu-endnow");if(endNowBtn)endNowBtn.addEventListener("click",()=>{menu.classList.remove("open");if(!confirm(`End ${agentName(e.user_id)}'s live entry now? This stops tracking as of this moment.`))return;endTimesheetEntryNow(e,`${agentName(e.user_id)} — ${taskLabel}`)});tr.querySelector(".ts-menu-danger").addEventListener("click",()=>{menu.classList.remove("open");const confirmMsg=e.end_time?`Permanently delete this time entry for ${agentName(e.user_id)} — ${taskLabel}? This can't be undone.`:"This entry is still live — the agent may be actively tracking against it. Delete anyway?";if(!confirm(confirmMsg))return;deleteTimesheetEntry(e,`${agentName(e.user_id)} — ${taskLabel}`)})}const expandTr=document.createElement("tr");expandTr.className="ts-expand-row";expandTr.style.display="none";expandTr.innerHTML=`
        <td colspan="7">
          <div class="expand-grid">
            <div>
              <h4>Screenshots</h4>
              <div class="shots-grid" id="ts-shots-${segKey}"></div>
            </div>
            <div>
              <h4>Sites visited</h4>
              <div id="ts-sites-${segKey}"></div>
            </div>
          </div>
        </td>
      `;tr.querySelector(".shots-toggle").addEventListener("click",()=>toggleTsExpand(segKey,tr.querySelector(".shots-toggle"),expandTr,e.id,seg.start,seg.end,e.user_id));body.appendChild(tr);body.appendChild(expandTr)})});applyTimeManagementSectionEditGating("timesheets")}async function toggleTsExpand(segKey,btn,expandTr,entryId,segStartMs,segEndMs,agentId){const isOpen=expandTr.style.display!=="none";if(isOpen){expandTr.style.display="none";btn.classList.remove("expanded");return}expandTr.style.display="table-row";btn.classList.add("expanded");if(tsShotsLoaded[segKey])return;tsShotsLoaded[segKey]=true;const shotsGrid=$(`ts-shots-${segKey}`);const sitesDiv=$(`ts-sites-${segKey}`);shotsGrid.innerHTML='<div class="empty">Loading…</div>';sitesDiv.innerHTML='<div class="empty">Loading…</div>';const segStartIso=new Date(segStartMs).toISOString();const segEndIso=new Date(segEndMs).toISOString();const{data:shots}=await sb.from("screenshots").select("id, storage_path, taken_at, activity_pct_at_capture").eq("time_entry_id",entryId).gte("taken_at",segStartIso).lt("taken_at",segEndIso).order("taken_at",{ascending:true});shotsGrid.innerHTML="";if(!shots||shots.length===0){shotsGrid.innerHTML='<div class="empty">No screenshots for this segment.</div>'}else{const resolvedShots=[];for(const shot of shots){const{data:signed}=await sb.storage.from("screenshots").createSignedUrl(shot.storage_path,3600);resolvedShots.push({url:signed?signed.signedUrl:"",taken_at:shot.taken_at,activity_pct_at_capture:shot.activity_pct_at_capture})}resolvedShots.forEach((shot,idx)=>{const div=document.createElement("div");div.className="shot-card";div.innerHTML=`
        <img src="${shot.url}" loading="lazy">
        <div class="shot-meta">${tsFmtTime(shot.taken_at,agentId)} · ${shot.activity_pct_at_capture??"—"}%</div>
      `;div.addEventListener("click",()=>openLightbox(resolvedShots,idx));shotsGrid.appendChild(div)})}const{data:visits}=await sb.from("url_visits").select("domain, start_time, end_time").eq("time_entry_id",entryId).lt("start_time",segEndIso);const totals={};(visits||[]).forEach(v=>{const vStart=new Date(v.start_time).getTime();const vEnd=v.end_time?new Date(v.end_time).getTime():Date.now();const overlapStart=Math.max(vStart,segStartMs);const overlapEnd=Math.min(vEnd,segEndMs);if(overlapEnd<=overlapStart)return;const dur=(overlapEnd-overlapStart)/1e3;const key=v.domain||"(unknown)";totals[key]=(totals[key]||0)+dur});const rows=Object.entries(totals).sort((a,b)=>b[1]-a[1]).slice(0,10);sitesDiv.innerHTML="";if(rows.length===0){sitesDiv.innerHTML='<div class="empty">No site activity logged for this segment.</div>';return}const maxSec=rows[0][1];rows.forEach(([domain,sec])=>{const row=document.createElement("div");row.className="site-row";row.innerHTML=`
      <div class="site-domain" title="${escapeHtml(domain)}">${escapeHtml(domain)}</div>
      <div class="site-bar-track"><div class="site-bar-fill" style="width:${Math.max(2,sec/maxSec*100)}%"></div></div>
      <div class="site-time">${fmtShort(sec)}</div>
    `;sitesDiv.appendChild(row)})}$("tsRefreshBtn").addEventListener("click",async()=>{const btn=$("tsRefreshBtn");if(btn.disabled)return;btn.disabled=true;const original=btn.innerHTML;btn.innerHTML="&#8635; Refreshing…";try{await loadTimesheets()}finally{btn.disabled=false;btn.innerHTML=original}});$("tsExportCsvBtn").addEventListener("click",()=>{const headers=["User","Project/Task","Status","Activity","Duration","Time","Screenshots"];const rows=lastTimesheetRows.map(r=>[r.agent,r.task,r.status,r.activity,r.duration,r.time,r.shots]);exportCSV(`timesheets-${new Date().toISOString().slice(0,10)}.csv`,headers,rows);logActivity("export.timesheets_csv",null,null,`Exported timesheet CSV (${rows.length} rows)`)});$("tsExportPdfBtn").addEventListener("click",()=>{const headers=["User","Project/Task","Status","Activity","Duration","Time","Shots"];const rows=lastTimesheetRows.map(r=>[r.agent,r.task,r.status,r.activity,r.duration,r.time,r.shots]);exportPDF("Timesheet Report",headers,rows);logActivity("export.timesheets_pdf",null,null,`Exported timesheet PDF (${rows.length} rows)`)});const REPORT_COLUMNS={productive:{label:"Productive (hrs)",get:s=>(s.productiveSec/3600).toFixed(2)},break:{label:"Break (hrs, paid)",get:s=>(s.breakSec/3600).toFixed(2)},idle:{label:"Idle (hrs, unpaid)",get:s=>(s.idleSec/3600).toFixed(2)},payable:{label:"Payable (hrs)",get:s=>(s.payableSec/3600).toFixed(2)},rate:{label:()=>`Rate (${curUnit()}/hr)`,get:s=>s.hourlyRate!=null?(toUSD(Number(s.hourlyRate),s.hourlyRateCurrency)*fxMultiplier()).toFixed(2):"—"},basicPay:{label:()=>`Basic pay (${curUnit()})`,get:s=>s.basicPay!=null?(s.basicPay*fxMultiplier()).toFixed(2):"—"},leads:{label:"Accepted leads",get:s=>String(s.leadsCount)},leadsTarget:{label:"Lead target",get:s=>s.leadsTarget!=null?String(s.leadsTarget):"—"},leadBonus:{label:()=>`Lead bonus (${curUnit()})`,get:s=>s.leadBonus!=null?(Number(s.leadBonus)*fxMultiplier()).toFixed(2):"0.00"},extraBonus:{label:()=>`Extra target bonus (${curUnit()})`,get:s=>s.extraBonus!=null?(Number(s.extraBonus)*fxMultiplier()).toFixed(2):"0.00"},pay:{label:()=>`Total pay (${curUnit()})`,get:s=>s.pay!=null?(Number(s.pay)*fxMultiplier()).toFixed(2):s.basicPay!=null?(s.basicPay*fxMultiplier()).toFixed(2):"—"},activity:{label:"Avg activity %",get:s=>s.avgActivity!=null?s.avgActivity+"%":"—"},days:{label:"Days worked",get:s=>String(s.daysWorked)},avgPerDay:{label:"Avg hrs/day",get:s=>s.daysWorked?(s.productiveSec/3600/s.daysWorked).toFixed(2):"0.00"},login:{label:"First login",get:s=>s.firstLogin?reportFmtDT(s.firstLogin,s.agentTz):"—"},logout:{label:"Last logout",get:s=>s.lastLogout?reportFmtDT(s.lastLogout,s.agentTz):s.stillLive?"live":"—"}};const DEFAULT_REPORTS={payroll:{title:"Monthly Payroll Summary",columns:["payable","rate","basicPay","leads","leadsTarget","leadBonus","extraBonus","pay"]},productivity:{title:"User Productivity Report",columns:["productive","idle","break","activity","days","avgPerDay"]},login:{title:"Login / Logout Tracker",columns:["login","logout","productive","activity"],perDay:true}};const CUSTOM_DEFAULT_COLUMNS=["productive","idle","break","payable"];function reportFmtDT(ms,tz){return new Date(ms).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...tz?{timeZone:tz}:{}})}function toUSD(amount,currency){if(amount==null)return null;return currency==="EGP"?amount/(usdToEgpRate||1):amount}function computeSalesPay(agent,stats){if(agent.payModel==="lead_tier_salary"){const tiers=Array.isArray(agent.salaryTiers)?agent.salaryTiers:[];const qualifying=tiers.filter(t=>stats.leadsCount>=Number(t.min_leads||0)).sort((a,b)=>Number(b.min_leads||0)-Number(a.min_leads||0));const basicPay2=qualifying.length?toUSD(Number(qualifying[0].amount||0),qualifying[0].currency)||0:null;return{basicPay:basicPay2,leadBonus:0,extraBonus:0,pay:basicPay2}}const hourlyRateUSD=toUSD(agent.hourlyRate,agent.hourlyRateCurrency);const basicPay=hourlyRateUSD!=null?stats.payableSec/3600*hourlyRateUSD:null;let leadBonus=0;if(agent.targetLeads&&stats.leadsCount>=agent.targetLeads){const leadBonusRateUSD=toUSD(agent.leadBonusRate,agent.leadBonusCurrency)||0;leadBonus=agent.leadBonusType==="bulk"?leadBonusRateUSD:stats.leadsCount*leadBonusRateUSD}let extraBonus=0;if(agent.targetLeads&&Array.isArray(agent.extraTiers)&&agent.extraTiers.length){const extraLeads=stats.leadsCount-agent.targetLeads;if(extraLeads>0){const qualifying=agent.extraTiers.filter(t=>extraLeads>=Number(t.extra_leads||0)).sort((a,b)=>Number(b.extra_leads||0)-Number(a.extra_leads||0));if(qualifying.length){extraBonus=toUSD(Number(qualifying[0].bonus||0),qualifying[0].currency)||0}}}const pay=basicPay!=null||leadBonus||extraBonus?(basicPay||0)+leadBonus+extraBonus:null;return{basicPay,leadBonus,extraBonus,pay}}let reportState={type:"payroll"};let reportAgents=[];let reportTemplates=[];let lastReportRows=[];let reportsInitialized=false;let reportCurrency="USD";let usdToEgpRate=null;const FX_CACHE_KEY="ptai_fx_usd_egp";function curUnit(){return reportCurrency==="EGP"?"EGP":"$"}function fxMultiplier(){return reportCurrency==="EGP"?usdToEgpRate||1:1}function colLabel(key){const def=REPORT_COLUMNS[key];return typeof def.label==="function"?def.label():def.label}function setFxNote(text){const note=$("fxRateNote");if(note)note.textContent=text}async function ensureFxRate(){const today=new Date().toISOString().slice(0,10);try{const cached=JSON.parse(localStorage.getItem(FX_CACHE_KEY)||"null");if(cached&&cached.date===today&&cached.rate){usdToEgpRate=cached.rate;setFxNote(`1 USD ≈ ${cached.rate.toFixed(2)} EGP · rate as of ${cached.date}`);return}}catch(e){}await refreshFxRate()}async function refreshFxRate(){setFxNote("Fetching today’s USD → EGP rate…");try{const res=await fetch("https://open.er-api.com/v6/latest/USD");const data=await res.json();const rate=data&&data.rates&&data.rates.EGP;if(!rate)throw new Error("EGP rate missing from response");if(!Number.isFinite(rate)||rate<=5||rate>=300)throw new Error(`Implausible USD->EGP rate from API: ${rate}`);const today=new Date().toISOString().slice(0,10);usdToEgpRate=rate;localStorage.setItem(FX_CACHE_KEY,JSON.stringify({rate,date:today}));setFxNote(`1 USD ≈ ${rate.toFixed(2)} EGP · updated ${today}`)}catch(err){let cached=null;try{cached=JSON.parse(localStorage.getItem(FX_CACHE_KEY)||"null")}catch(e){}if(cached&&cached.rate){usdToEgpRate=cached.rate;setFxNote(`1 USD ≈ ${cached.rate.toFixed(2)} EGP · couldn't refresh, showing rate cached ${cached.date}`)}else{usdToEgpRate=1;setFxNote("Couldn’t fetch today’s EGP rate — showing 1:1 for now.")}}}function renderReportColumnPicker(){const colPicker=$("reportColumnsPicker");if(!colPicker)return;colPicker.innerHTML=Object.keys(REPORT_COLUMNS).map(key=>`
    <label style="display:flex; align-items:center; gap:6px; cursor:pointer; text-transform:none; font-family:var(--sans); font-size:12px; color:var(--text); letter-spacing:0;">
      <input type="checkbox" class="report-col-cb" value="${key}" ${CUSTOM_DEFAULT_COLUMNS.includes(key)?"checked":""} style="width:14px; height:14px; accent-color:var(--focus);"> ${colLabel(key)}
    </label>
  `).join("");colPicker.querySelectorAll(".report-col-cb").forEach(cb=>{cb.addEventListener("change",()=>{if(reportState.type==="custom")applyReportCurrencyVisibility("custom")})})}async function initReportsTab(){$("reportCurrencySelect").value=reportCurrency;if(!$("reportMonthInput").value){const now=new Date;$("reportMonthInput").value=`${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,"0")}`}const{data:agents}=await sb.from("profiles").select("id, full_name, email, role").order("full_name");const{data:scheduleRows}=await sb.from("schedules").select("user_id, hourly_rate, hourly_rate_currency, timezone, target_leads, lead_bonus_type, lead_bonus_rate, lead_bonus_currency, extra_target_tiers, pay_model, salary_tiers");const rateMap={},rateCurMap={},tzMap={},targetMap={},bonusTypeMap={},bonusRateMap={},bonusCurMap={},tiersMap={},payModelMap={},salaryTiersMap={};(scheduleRows||[]).forEach(s=>{rateMap[s.user_id]=s.hourly_rate;rateCurMap[s.user_id]=s.hourly_rate_currency;tzMap[s.user_id]=s.timezone;targetMap[s.user_id]=s.target_leads;bonusTypeMap[s.user_id]=s.lead_bonus_type;bonusRateMap[s.user_id]=s.lead_bonus_rate;bonusCurMap[s.user_id]=s.lead_bonus_currency;tiersMap[s.user_id]=Array.isArray(s.extra_target_tiers)?s.extra_target_tiers:[];payModelMap[s.user_id]=s.pay_model;salaryTiersMap[s.user_id]=Array.isArray(s.salary_tiers)?s.salary_tiers:[]});reportAgents=(agents||[]).filter(a=>!PROTECTED_ROLES.has(a.role)).map(a=>({id:a.id,name:a.full_name||a.email,hourlyRate:rateMap[a.id]!=null?Number(rateMap[a.id]):null,hourlyRateCurrency:rateCurMap[a.id]||"USD",tz:tzMap[a.id]||null,targetLeads:targetMap[a.id]!=null?Number(targetMap[a.id]):null,leadBonusType:bonusTypeMap[a.id]||"per_lead",leadBonusRate:bonusRateMap[a.id]!=null?Number(bonusRateMap[a.id]):null,leadBonusCurrency:bonusCurMap[a.id]||"USD",extraTiers:tiersMap[a.id]||[],payModel:payModelMap[a.id]||"hourly_bonus",salaryTiers:salaryTiersMap[a.id]||[]}));const picker=$("reportAgentPicker");picker.innerHTML=reportAgents.length?reportAgents.map(a=>`
    <label style="display:flex; align-items:center; gap:6px; cursor:pointer; text-transform:none; font-family:var(--sans); font-size:12px; color:var(--text); letter-spacing:0; margin-bottom:6px;">
      <input type="checkbox" class="report-agent-cb" value="${a.id}" checked style="width:14px; height:14px; accent-color:var(--focus);"> ${escapeHtml(a.name)}
    </label>
  `).join(""):'<div class="empty">No users yet.</div>';renderReportColumnPicker();await ensureFxRate();await loadReportTemplates();if(!reportsInitialized){reportsInitialized=true;document.querySelectorAll(".report-type-btn").forEach(btn=>{btn.addEventListener("click",()=>setReportType(btn.dataset.report))});$("reportGenerateBtn").addEventListener("click",generateReport);$("reportSaveTemplateBtn").addEventListener("click",saveReportTemplate);$("reportTemplateSelect").addEventListener("change",applyReportTemplate);$("reportDeleteTemplateBtn").addEventListener("click",deleteReportTemplate);$("reportExportCsvBtn").addEventListener("click",()=>{const headers=Array.from($("reportTableHead").children).map(th=>th.textContent);exportCSV(`report-${reportState.type}-${reportState.lastMonthVal||$("reportMonthInput").value}-${reportCurrency}.csv`,headers,lastReportRows);logActivity("export.report_csv",null,null,`Exported ${reportState.type} report CSV`)});$("reportExportPdfBtn").addEventListener("click",()=>{const headers=Array.from($("reportTableHead").children).map(th=>th.textContent);exportPDF($("reportResultsTitle").textContent,headers,lastReportRows);logActivity("export.report_pdf",null,null,`Exported ${reportState.type} report PDF`)});$("reportCurrencySelect").addEventListener("change",()=>{reportCurrency=$("reportCurrencySelect").value;renderReportColumnPicker();if(reportState.lastRows){renderReportTable(reportState.lastTitle,reportState.lastColumns,reportState.lastRows,reportState.lastPerDay,reportState.lastMonthVal)}logActivity("report.currency_change",null,null,`Switched report currency to ${reportCurrency}`)});setReportType("payroll")}}function setReportType(type){reportState.type=type;document.querySelectorAll(".report-type-btn").forEach(b=>b.classList.toggle("active",b.dataset.report===type));$("reportCustomColumns").style.display=type==="custom"?"block":"none";$("reportResultsCard").style.display="none";$("payrollPeriodModeWrap").style.display="block";syncPayrollPeriodInputs($("payrollPeriodModeSelect").value);applyReportCurrencyVisibility(type)}function reportHasCurrency(type){if(type==="payroll")return true;if(type==="custom")return customReportHasMoneyColumn();return false}function customReportHasMoneyColumn(){const moneyCols=new Set(["pay","earnings","hourly_rate","bonus","total_pay","gross_pay","amount"]);const chosen=Array.from(document.querySelectorAll(".report-col-cb:checked")).map(cb=>cb.value.toLowerCase());return chosen.some(c=>moneyCols.has(c)||c.includes("pay")||c.includes("rate")||c.includes("bonus")||c.includes("earn"))}function applyReportCurrencyVisibility(type){const wrap=document.getElementById("reportCurrencyWrap");if(wrap)wrap.style.display=reportHasCurrency(type)?"block":"none"}function syncPayrollPeriodInputs(mode){$("reportMonthWrap").style.display=mode==="daily"?"none":"block";$("reportDayWrap").style.display=mode==="daily"?"block":"none";if(mode==="daily"&&!$("reportDayInput").value){$("reportDayInput").value=todayDateInputValue()}}$("payrollPeriodModeSelect").addEventListener("change",()=>syncPayrollPeriodInputs($("payrollPeriodModeSelect").value));async function loadReportTemplates(){const{data}=await sb.from("report_templates").select("id, name, columns").order("name");reportTemplates=data||[];const sel=$("reportTemplateSelect");const prev=sel.value;sel.innerHTML='<option value="">Load saved template…</option>'+reportTemplates.map(t=>`<option value="${t.id}">${escapeHtml(t.name)}</option>`).join("");sel.value=reportTemplates.some(t=>t.id===prev)?prev:"";$("reportDeleteTemplateBtn").style.display=sel.value?"inline-block":"none"}async function saveReportTemplate(){const btn=$("reportSaveTemplateBtn");if(btn.disabled)return;const name=$("reportTemplateName").value.trim();if(!name){alert("Give the template a name first.");return}const columns=Array.from(document.querySelectorAll(".report-col-cb:checked")).map(cb=>cb.value);if(!columns.length){alert("Pick at least one column first.");return}btn.disabled=true;try{const{data:{user}}=await sb.auth.getUser();const{error}=await sb.from("report_templates").insert({name,columns,created_by:user?user.id:null});if(error){alert(error.message);return}$("reportTemplateName").value="";await loadReportTemplates();logActivity("report_template.create",null,null,`Saved report template "${name}" (${columns.length} columns)`)}finally{btn.disabled=false}}function applyReportTemplate(){const id=$("reportTemplateSelect").value;$("reportDeleteTemplateBtn").style.display=id?"inline-block":"none";if(!id)return;const tmpl=reportTemplates.find(t=>t.id===id);if(!tmpl)return;document.querySelectorAll(".report-col-cb").forEach(cb=>{cb.checked=tmpl.columns.includes(cb.value)})}async function deleteReportTemplate(){const id=$("reportTemplateSelect").value;const tmpl=reportTemplates.find(t=>t.id===id);if(!id||!confirm(`Delete template "${tmpl?tmpl.name:""}"?`))return;await sb.from("report_templates").delete().eq("id",id);await loadReportTemplates();logActivity("report_template.delete",null,null,`Deleted report template "${tmpl?tmpl.name:id}"`)}async function computeAgentStats(agentId,rangeStartMs,rangeEndMs){const startISO=new Date(rangeStartMs).toISOString();const endISO=new Date(rangeEndMs).toISOString();const entries=await fetchAllRows((from,to)=>sb.from("time_entries").select("id, start_time, end_time, is_manual, idle_seconds, break_seconds, last_heartbeat_at").eq("user_id",agentId).lte("start_time",endISO).or(`end_time.is.null,end_time.gte.${startISO}`).order("id",{ascending:true}).range(from,to));let grossSec=0,firstLogin=null,lastLogout=null,stillLive=false;const daySet=new Set;entries.forEach(e=>{const rawStart=new Date(e.start_time).getTime();const rawEnd=liveEntryEndMs(e);const sec=clipSeconds(rawStart,rawEnd,rangeStartMs,rangeEndMs);if(sec<=0)return;grossSec+=sec;const clippedStart=Math.max(rawStart,rangeStartMs);const clippedEnd=Math.min(rawEnd,rangeEndMs);if(firstLogin===null||clippedStart<firstLogin)firstLogin=clippedStart;if(lastLogout===null||clippedEnd>lastLogout)lastLogout=clippedEnd;if(!e.end_time&&!liveEntryIsStale(e))stillLive=true;let cur=new Date(clippedStart);cur.setHours(0,0,0,0);const endMarker=new Date(clippedEnd);while(cur.getTime()<=endMarker.getTime()){daySet.add(cur.toDateString());cur.setDate(cur.getDate()+1)}});const entryIds=entries.map(e=>e.id);const entryBounds={};entries.forEach(e=>{entryBounds[e.id]={start:new Date(e.start_time).getTime(),end:liveEntryEndMs(e)}});let idleSec=0,breakSec=0;if(entryIds.length){const[idleRows,breakRows]=await Promise.all([fetchAllRows((from,to)=>sb.from("idle_periods").select("time_entry_id, start_time, end_time").in("time_entry_id",entryIds).order("id",{ascending:true}).range(from,to)),fetchAllRows((from,to)=>sb.from("breaks").select("time_entry_id, start_time, end_time").in("time_entry_id",entryIds).order("id",{ascending:true}).range(from,to))]);idleSec=mergedCoverageSec(idleRows,rangeStartMs,rangeEndMs,entryBounds);breakSec=mergedCoverageSec(breakRows,rangeStartMs,rangeEndMs,entryBounds)}entries.forEach(e=>{if(e.is_manual){idleSec+=e.idle_seconds||0;breakSec+=e.break_seconds||0}});const productiveSec=Math.max(0,grossSec-idleSec-breakSec);const payableSec=productiveSec+breakSec;const sampleRows=await fetchAllRows((from,to)=>sb.from("activity_samples").select("activity_pct").eq("user_id",agentId).gte("sampled_at",startISO).lte("sampled_at",endISO).order("id",{ascending:true}).range(from,to));const avgActivity=sampleRows&&sampleRows.length?Math.round(sampleRows.reduce((a,s)=>a+(s.activity_pct||0),0)/sampleRows.length):null;const leadRows=await fetchAllRows((from,to)=>sb.from("leads").select(CREDITED_LEAD_COLS).eq("user_id",agentId).in("status",CREDITED_LEAD_STATUSES).gte("submitted_at",startISO).lte("submitted_at",endISO).order("id",{ascending:true}).range(from,to));const leadsCount=creditedLeadCount(leadRows);return{grossSec,idleSec,breakSec,productiveSec,payableSec,avgActivity,leadsCount,daysWorked:daySet.size,firstLogin,lastLogout,stillLive}}async function generateReport(){const isPayroll=reportState.type==="payroll";const isDaily=$("payrollPeriodModeSelect").value==="daily";const payrollDaily=isDaily;let monthVal=null,y,m,singleDayVal=null,dy,dm,dd;if(payrollDaily){singleDayVal=$("reportDayInput").value;if(!singleDayVal)return;[dy,dm,dd]=singleDayVal.split("-").map(Number)}else{monthVal=$("reportMonthInput").value;if(!monthVal)return;[y,m]=monthVal.split("-").map(Number)}const selectedIds=Array.from(document.querySelectorAll(".report-agent-cb:checked")).map(cb=>cb.value);const agentsToRun=reportAgents.filter(a=>selectedIds.includes(a.id));if(agentsToRun.length===0){alert("Select at least one user.");return}let columns,title,perDay=false;if(reportState.type==="payroll"){columns=DEFAULT_REPORTS.payroll.columns;title=payrollDaily?"Daily Payroll Summary":DEFAULT_REPORTS.payroll.title}else if(reportState.type==="productivity"){columns=DEFAULT_REPORTS.productivity.columns;title=DEFAULT_REPORTS.productivity.title}else if(reportState.type==="login"){columns=DEFAULT_REPORTS.login.columns;title=DEFAULT_REPORTS.login.title;perDay=true}else{columns=Array.from(document.querySelectorAll(".report-col-cb:checked")).map(cb=>cb.value);title="Custom Report";if(!columns.length){alert("Pick at least one column.");return}}if(payrollDaily)perDay=false;const btn=$("reportGenerateBtn");btn.disabled=true;const origLabel=btn.textContent;btn.textContent="Generating…";try{const rows=[];if(perDay){const{daysInMonth}=monthBoundsMsInZone(y,m,null);for(const agent of agentsToRun){for(let d=1;d<=daysInMonth;d++){const{startMs:dayStartMs,endMs:dayEndRaw}=dayBoundsMsInZone(agent.tz,null,y,m,d);if(dayStartMs>Date.now())break;const dayEndMs=Math.min(dayEndRaw,Date.now());const stats=await computeAgentStats(agent.id,dayStartMs,dayEndMs);if(stats.grossSec<=0)continue;stats.hourlyRate=agent.hourlyRate;stats.hourlyRateCurrency=agent.hourlyRateCurrency;stats.agentTz=agent.tz;rows.push({agent,dateLabel:new Date(dayStartMs).toLocaleDateString([],{month:"short",day:"numeric",year:"numeric",...agent.tz?{timeZone:agent.tz}:{}}),stats})}}}else{for(const agent of agentsToRun){const{startMs:rangeStartMs,endMs:rangeEndRaw}=payrollDaily?dayBoundsMsInZone(agent.tz,null,dy,dm,dd):monthBoundsMsInZone(y,m,agent.tz);const rangeEndMs=Math.min(rangeEndRaw,Date.now());const stats=await computeAgentStats(agent.id,rangeStartMs,rangeEndMs);stats.hourlyRate=agent.hourlyRate;stats.hourlyRateCurrency=agent.hourlyRateCurrency;stats.agentTz=agent.tz;stats.leadsTarget=agent.targetLeads;const payCalc=computeSalesPay(agent,stats);stats.basicPay=payCalc.basicPay;stats.leadBonus=payCalc.leadBonus;stats.extraBonus=payCalc.extraBonus;stats.pay=payCalc.pay;rows.push({agent,stats})}}reportState.lastRows=rows;reportState.lastColumns=columns;reportState.lastTitle=title;reportState.lastPerDay=perDay;reportState.lastMonthVal=payrollDaily?singleDayVal:monthVal;renderReportTable(title,columns,rows,perDay,reportState.lastMonthVal)}finally{btn.disabled=false;btn.textContent=origLabel}}function renderReportTable(title,columns,rows,perDay,monthVal){$("reportResultsCard").style.display="block";$("reportResultsTitle").textContent=`${title} — ${monthVal}`;const head=$("reportTableHead");head.innerHTML="<th>User</th>"+(perDay?"<th>Date</th>":"")+columns.map(c=>`<th>${colLabel(c)}</th>`).join("");const body=$("reportTableBody");body.innerHTML="";lastReportRows=[];if(rows.length===0){body.innerHTML=`<tr><td colspan="${columns.length+(perDay?2:1)}" class="empty">No data for this range.</td></tr>`;$("reportTotalsRow").textContent="";return}rows.forEach(r=>{const rowVals=columns.map(c=>REPORT_COLUMNS[c].get(r.stats));lastReportRows.push([r.agent.name,...perDay?[r.dateLabel]:[],...rowVals]);const tr=document.createElement("tr");tr.innerHTML=`<td>${escapeHtml(r.agent.name)}</td>`+(perDay?`<td class="mono">${r.dateLabel}</td>`:"")+rowVals.map(v=>`<td class="mono">${v}</td>`).join("");body.appendChild(tr)});const SUMMABLE=["productive","break","idle","payable","basicPay","leadBonus","extraBonus","pay","leads"];const totalParts=[];SUMMABLE.forEach(c=>{if(!columns.includes(c))return;let sum=0,any=false;rows.forEach(r=>{const raw=REPORT_COLUMNS[c].get(r.stats);const num=parseFloat(raw);if(!isNaN(num)){sum+=num;any=true}});if(any)totalParts.push(`${colLabel(c)}: ${sum.toFixed(2)}`)});$("reportTotalsRow").textContent=totalParts.length?`Totals — ${totalParts.join("   ·   ")}`:""}let auditActiveSection="log";function opsHealthCanView(){return!!(myIsOwner||myRole==="admin")}let opsHealthData=null;let opsHealthLoading=false;function opsTime(ts){if(!ts)return"—";const tz=typeof globalDisplayTzMode==="string"&&globalDisplayTzMode&&globalDisplayTzMode!=="agent"?globalDisplayTzMode:void 0;try{return new Date(ts).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...tz?{timeZone:tz}:{}})}catch(e){return new Date(ts).toLocaleString()}}function opsAgo(ts){if(!ts)return"";const s=Math.max(0,Math.round((Date.now()-new Date(ts).getTime())/1e3));if(s<90)return"just now";if(s<5400)return Math.round(s/60)+" min ago";if(s<129600)return Math.round(s/3600)+" h ago";return Math.round(s/86400)+" days ago"}function opsSchedule(expr){const e=String(expr||"").trim();let m;if(e==="* * * * *")return"every minute";if(m=e.match(/^\*\/(\d+) \* \* \* \*$/))return`every ${m[1]} min`;if(m=e.match(/^(\d+) \* \* \* \*$/))return`hourly (:${m[1].padStart(2,"0")})`;if(m=e.match(/^(\d+) (\d+) \* \* \*$/))return`daily ${m[2].padStart(2,"0")}:${m[1].padStart(2,"0")} UTC`;if(m=e.match(/^(\d+) \*\/(\d+) \* \* \*$/))return`every ${m[2]} h`;return e}function opsPlain(t){let x=String(t||"");try{const j=JSON.parse(x);if(j&&(j.error||j.message))x=String(j.error||j.message)}catch(e){}return x.replace(/<[^>]*>/g," ").replace(/\s+/g," ").trim()}function opsJobState(j){if(!j.active)return{label:"Off",color:"var(--text-dim)"};if(j.open_failures>0||j.db_failures_24h>0)return{label:"Needs a look",color:"var(--danger)"};if(j.call_failures_24h>0)return{label:"Recovered",color:"var(--away)"};return{label:"OK",color:"var(--good)"}}function opsStat(label,value,color,note){return`<div class="card" style="margin:0; padding:14px 16px;">
    <div style="font-size:11px; color:var(--text-dim); text-transform:uppercase; letter-spacing:.8px;">${escapeHtml(label)}</div>
    <div class="mono" style="font-size:26px; font-weight:700; margin-top:4px; color:${color};">${escapeHtml(String(value))}</div>
    ${note?`<div style="font-size:11.5px; color:var(--text-dim); margin-top:2px;">${escapeHtml(note)}</div>`:""}
  </div>`}function renderOpsHealth(){const d=opsHealthData;if(!d)return;const jobs=d.jobs||[];const active=jobs.filter(j=>j.active);const needs=active.filter(j=>opsJobState(j).label==="Needs a look");const errors=d.errors||[];const unlabelled=Number(d.unlabelled_open||0);$("opsUpdated").textContent="Updated "+opsTime(d.generated_at);$("opsSummary").innerHTML=opsStat("Jobs needing a look",needs.length,needs.length?"var(--danger)":"var(--good)",needs.length?needs.slice(0,3).map(j=>j.job).join(", ")+(needs.length>3?"…":""):"Everything scheduled is working")+opsStat("Jobs running",active.length,"var(--text)",`${jobs.length-active.length} switched off`)+opsStat("Portal errors (24 h)",Number(d.errors_24h||0),Number(d.errors_24h||0)?"var(--away)":"var(--good)",errors.length?"Latest "+opsAgo(errors[0].occurred_at):"None recorded")+(d.retention?opsStat(`Recordings past ${Number(d.retention.months||6)} months`,Number(d.retention.eligible||0),d.retention.error?"var(--danger)":"var(--text)",d.retention.error?"Last check failed: "+d.retention.error:d.retention.delete_enabled?`Deleted automatically · ${Number(d.retention.deleted||0)} removed on the last run`:"Counted daily · automatic deleting is off"):"")+(unlabelled?opsStat("Other failed calls",unlabelled,"var(--away)","Not from a named scheduled job (older alerts, or a database trigger)").replace(/<\/div>\s*$/,`<button class="btn-secondary ops-seen-btn" data-job="unlabelled" style="width:auto; margin-top:8px; padding:5px 10px; font-size:11px;">Mark as seen</button></div>`):"");const showOff=$("opsShowOff").checked;const rows=jobs.filter(j=>showOff||j.active);$("opsJobsBody").innerHTML=rows.length?rows.map(j=>{const st=opsJobState(j);const fail=(d.failures||[]).find(f=>f.job===j.job);const problem=fail?`${fail.status}: ${opsPlain(fail.detail)}`:j.last_status&&j.last_status!=="succeeded"?`${j.last_status}: ${j.last_message||""}`:"";return`<tr>
      <td class="mono" style="white-space:nowrap;">${escapeHtml(j.job)}</td>
      <td style="white-space:nowrap;"><span style="color:${st.color}; font-weight:600;">&#9679; ${st.label}</span></td>
      <td style="white-space:nowrap;" title="${escapeHtml(opsTime(j.last_start))}">${j.last_start?escapeHtml(opsAgo(j.last_start)):"—"}</td>
      <td class="mono">${Number(j.runs_24h||0)}</td>
      <td class="mono" style="color:${j.call_failures_24h||j.db_failures_24h?"var(--danger)":"inherit"};">${Number(j.call_failures_24h||0)+Number(j.db_failures_24h||0)}${j.open_failures?` <span style="color:var(--text-dim); font-size:11px;">(${Number(j.open_failures)} open)</span>`:""}</td>
      <td style="white-space:nowrap; font-size:12px;" title="${escapeHtml(j.schedule)}">${escapeHtml(opsSchedule(j.schedule))}</td>
      <td style="font-size:12px; max-width:420px; word-break:break-word;">${problem?escapeHtml(problem.slice(0,220))+(fail?` <span style="color:var(--text-dim);">(${escapeHtml(opsAgo(fail.occurred_at))})</span>`:""):'<span style="color:var(--text-dim);">—</span>'}</td>
      <td>${j.open_failures?`<button class="btn-secondary ops-seen-btn" data-job="${escapeHtml(j.job)}" style="width:auto; margin-top:0; padding:5px 10px; font-size:11px; white-space:nowrap;">Mark as seen</button>`:""}</td>
    </tr>`}).join(""):'<tr><td colspan="8" style="color:var(--text-dim);">No scheduled jobs.</td></tr>';document.querySelectorAll("#tab-ops_health .ops-seen-btn").forEach(b=>b.addEventListener("click",()=>opsMarkSeen(b)));$("opsErrorsBody").innerHTML=errors.length?errors.map(e=>{const where=[e.page,e.source?`${e.source}${e.line?":"+e.line:""}`:""].filter(Boolean).join(" · ");return`<tr>
      <td style="white-space:nowrap;" title="${escapeHtml(opsTime(e.occurred_at))}">${escapeHtml(opsAgo(e.occurred_at))}</td>
      <td>${escapeHtml(e.portal||"—")}${e.version?` <span style="color:var(--text-dim); font-size:11px;">${escapeHtml(e.version)}</span>`:""}</td>
      <td>${escapeHtml(e.who||"—")}</td>
      <td style="font-size:12px; max-width:420px; word-break:break-word;">${escapeHtml(e.message||"")}</td>
      <td style="font-size:11.5px; color:var(--text-dim); max-width:260px; word-break:break-all;">${escapeHtml(where||"—")}</td>
    </tr>`}).join(""):'<tr><td colspan="5" style="color:var(--text-dim);">No errors recorded. Nobody has hit a crash.</td></tr>';const lat=d.latency||[];const ms=(v,base)=>{if(v==null)return"—";const diff=base==null?0:Math.round(v-base);const tag=base==null||diff===0?"":` <span style="color:${diff<0?"var(--good)":"var(--danger)"}; font-size:11px;">(${diff<0?"":"+"}${diff})</span>`;return`${Math.round(v)} ms${tag}`};$("opsLatencyBody").innerHTML=lat.length?lat.map(r=>`<tr>
      <td>${escapeHtml(r.place||"—")}</td>
      <td>${escapeHtml(r.portal==="client"?"Client portal":r.portal==="staff"?"Staff portal":r.portal||"—")}</td>
      <td class="mono">${Number(r.people||0)} <span style="color:var(--text-dim); font-size:11px;">(${Number(r.samples||0)} tests)</span></td>
      <td class="mono">${ms(r.today_ms,null)}</td>
      <td class="mono">${ms(r.us_east_1_ms,r.today_ms)}</td>
      <td class="mono">${ms(r.us_east_2_ms,r.today_ms)}</td>
    </tr>`).join(""):'<tr><td colspan="6" style="color:var(--text-dim);">No results yet. Browsers test once a day, starting a minute after someone signs in.</td></tr>'}async function loadOpsHealth(){if(!opsHealthCanView()||opsHealthLoading)return;opsHealthLoading=true;const btn=$("opsRefreshBtn");if(btn){btn.disabled=true;btn.textContent="Loading…"}try{const{data,error}=await sb.rpc("ops_health");if(error)throw error;opsHealthData=data||{};renderOpsHealth()}catch(e){$("opsSummary").innerHTML=`<div class="msg error" style="grid-column:1/-1;">Could not load Ops Health: ${escapeHtml(e&&e.message?e.message:String(e))}</div>`}finally{opsHealthLoading=false;if(btn){btn.disabled=false;btn.textContent="Refresh"}}}async function opsMarkSeen(btn){const job=btn.dataset.job;btn.disabled=true;const{error}=await sb.rpc("ops_mark_job_seen",{p_job:job});if(error){btn.disabled=false;alert(error.message||"Could not update.");return}logActivity("ops.mark_seen","ops",null,`Marked the failures of scheduled job "${job}" as seen`);await loadOpsHealth()}$("opsRefreshBtn").addEventListener("click",()=>loadOpsHealth());$("opsShowOff").addEventListener("change",()=>renderOpsHealth());function initAuditLogTab(){activateAuditSection(auditActiveSection||"log")}function activateAuditSection(key){auditActiveSection=key;document.querySelectorAll(".audit-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.auditSection===key));document.querySelectorAll(".audit-section").forEach(s=>{s.style.display=s.id==="auditSection-"+key?"block":"none"});if(key==="log")loadActivityLog();else if(key==="billing_ak")loadBillingAk();else if(key==="chat_logs")initChatLogsSection()}document.querySelectorAll(".audit-subtab-btn").forEach(btn=>{btn.addEventListener("click",()=>activateAuditSection(btn.dataset.auditSection))});let actRangeMode="today";function weekStartISO(){return weekStartCairoISO()}document.querySelectorAll(".act-range-btn").forEach(btn=>{btn.addEventListener("click",()=>{document.querySelectorAll(".act-range-btn").forEach(b=>b.classList.remove("active"));btn.classList.add("active");actRangeMode=btn.dataset.range;loadActivityLog()})});$("actAdminFilter").addEventListener("change",loadActivityLog);async function loadActivityLog(){const[{data:admins},{data:allProfiles}]=await Promise.all([sb.from("profiles").select("id, full_name, email").not("role","eq","agent").order("full_name"),sb.from("profiles").select("id, full_name, email")]);const filterSel=$("actAdminFilter");const prevFilter=filterSel.value;filterSel.innerHTML='<option value="">All users</option>'+(admins||[]).map(a=>`<option value="${a.id}">${escapeHtml(a.full_name||a.email)}</option>`).join("");filterSel.value=prevFilter;const ACTIVITY_LOG_PAGE_SIZE=300;let query=sb.from("activity_log").select("id, admin_id, action, target_type, target_id, details, created_at").order("created_at",{ascending:false}).limit(ACTIVITY_LOG_PAGE_SIZE+1);if(actRangeMode==="today"){query=query.gte("created_at",todayRangeISO().start)}else if(actRangeMode==="week"){query=query.gte("created_at",weekStartISO())}if(filterSel.value)query=query.eq("admin_id",filterSel.value);const{data:rawLogs}=await query;const truncated=(rawLogs||[]).length>ACTIVITY_LOG_PAGE_SIZE;const logs=truncated?rawLogs.slice(0,ACTIVITY_LOG_PAGE_SIZE):rawLogs||[];const truncationNote=$("activityLogTruncationNote");if(truncationNote){truncationNote.style.display=truncated?"":"none";truncationNote.textContent=truncated?`Showing the most recent ${ACTIVITY_LOG_PAGE_SIZE} of more than ${ACTIVITY_LOG_PAGE_SIZE} entries in this range — narrow the range or filter by user to see older ones.`:""}const adminName=id=>{const a=(allProfiles||[]).find(x=>x.id===id);return a?a.full_name||a.email:"—"};const auditDisplayTz=globalDisplayTzMode&&globalDisplayTzMode!=="agent"?globalDisplayTzMode:void 0;const body=$("activityBody");body.innerHTML="";if(!logs||logs.length===0){body.innerHTML='<tr><td colspan="4" class="empty">No activity in this range.</td></tr>';return}logs.forEach(l=>{const tr=document.createElement("tr");tr.innerHTML=`
      <td class="mono">${new Date(l.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...auditDisplayTz?{timeZone:auditDisplayTz}:{}})}</td>
      <td>${escapeHtml(adminName(l.admin_id))}</td>
      <td class="mono">${escapeHtml(l.action)}</td>
      <td>${escapeHtml(l.details||"—")}</td>
    `;body.appendChild(tr)})}function defaultCallActRange(){return{from:todayDateInputValue(),to:endOfLoginMonthDateInputValue()}}function callActRangeISO(){const fromVal=$("callActFromInput").value;const toVal=$("callActToInput").value;if(!fromVal&&!toVal)return{start:null,end:null};const[fy,fm,fd]=(fromVal||toVal).split("-").map(Number);const[ty,tm,td]=(toVal||fromVal).split("-").map(Number);const{startMs}=dayBoundsMsInZone(globalDisplayTzMode,null,fy,fm,fd);const{endMs}=dayBoundsMsInZone(globalDisplayTzMode,null,ty,tm,td);return{start:new Date(startMs).toISOString(),end:new Date(endMs).toISOString()}}$("callActFromInput").addEventListener("change",loadCallActivity);$("callActToInput").addEventListener("change",loadCallActivity);$("callActAgentFilter").addEventListener("change",loadCallActivity);$("callActDirectionFilter").addEventListener("change",loadCallActivity);$("callActRoleFilter").addEventListener("change",loadCallActivity);$("callActSearchInput").addEventListener("input",debounce(loadCallActivity,250));$("callActRefreshBtn").addEventListener("click",loadCallActivity);let activeActivityGroup="ghl";let activeActivityKind="call";function activitySubtabBtns(group){return Array.from(document.querySelectorAll(".act-subtab-btn")).filter(b=>(!group||b.dataset.actGroup===group)&&!b.disabled&&canViewActivitySection(b.dataset.actKind))}async function initActivityTab(){const{data:tasksForExtension}=await sb.from("tasks").select("id, name, archived_at").order("name");refreshExtensionTaskSections(tasksForExtension||[]);document.querySelectorAll(".act-group-btn").forEach(b=>{b.style.display=activitySubtabBtns(b.dataset.actGroup).length>0?"":"none"});const rememberedGroup=ACTIVITY_GROUPS.find(g=>g.key===activeActivityGroup&&activitySubtabBtns(g.key).length>0);const firstUsableGroup=rememberedGroup||ACTIVITY_GROUPS.find(g=>activitySubtabBtns(g.key).length>0);activateActivityGroup(firstUsableGroup?firstUsableGroup.key:activeActivityGroup)}function activateActivityGroup(group){activeActivityGroup=group;document.querySelectorAll(".act-group-btn").forEach(b=>b.classList.toggle("active",b.dataset.actGroup===group));document.querySelectorAll(".act-subtab-btn").forEach(b=>{const inGroup=b.dataset.actGroup===group;const permitted=canViewActivitySection(b.dataset.actKind);b.style.display=inGroup&&permitted?"":"none"});$("actStatsRowGhl").style.display=group==="ghl"?"":"none";$("actStatsRowReadymode").style.display=group==="readymode"?"":"none";$("actStatsRowExtension").style.display=group==="extension"?"":"none";$("actStatsRowStaffActivity").style.display=group==="staff_activity"?"":"none";const usable=activitySubtabBtns(group);const stillValid=usable.find(b=>b.dataset.actKind===activeActivityKind);activateActivityKind(stillValid?activeActivityKind:usable[0]?usable[0].dataset.actKind:null)}document.querySelectorAll(".act-group-btn").forEach(btn=>{btn.addEventListener("click",()=>activateActivityGroup(btn.dataset.actGroup))});function activateActivityKind(kind){if(!kind){document.querySelectorAll(".act-type-table").forEach(t=>t.style.display="none");$("actGmailPanel").style.display="none";$("actFilterCard").style.display="";$("actNoAccessMsg").style.display="";return}$("actNoAccessMsg").style.display="none";activeActivityKind=kind;document.querySelectorAll(".act-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.actKind===kind));const tableIdKind=kind.startsWith("task_")?"staff_work":kind;document.querySelectorAll(".act-type-table").forEach(t=>t.style.display=t.id==="actTable-"+tableIdKind?"":"none");const dirSel=$("callActDirectionFilter");const dirApplies=kind==="call"||kind==="rm_call";dirSel.disabled=!dirApplies;dirSel.title=dirApplies?"":"Direction doesn't apply to this activity type";const roleSel=$("callActRoleFilter");const roleApplies=kind==="staff_activity";roleSel.disabled=!roleApplies;roleSel.title=roleApplies?"":"Role only applies to Staff Activity";const isGmailInbox=kind==="gmail_inbox";$("actFilterCard").style.display=isGmailInbox?"none":"";$("actGmailPanel").style.display=isGmailInbox?"block":"none";if(isGmailInbox)loadActGmailMessageList(true);if(kind!=="email"&&kind!=="gmail_inbox")loadCallActivity()}document.querySelectorAll(".act-subtab-btn").forEach(btn=>{btn.addEventListener("click",()=>{if(!btn.disabled)activateActivityKind(btn.dataset.actKind)})});let actGmailFolder="inbox";let actGmailMessages=[];let actGmailNextPageToken=null;let actGmailInboxAddress="";let actGmailSelectedMessageId=null;let actGmailThreadData=null;let actGmailMode="empty";let actGmailThreadPollTimer=null;function actGmailShowPane(mode){actGmailMode=mode;$("actGmailReadingEmpty").style.display=mode==="empty"?"block":"none";$("actGmailReadingThread").style.display=mode==="thread"?"flex":"none";$("actGmailComposeForm").style.display=mode==="compose"?"flex":"none"}document.querySelectorAll(".actGmailFolderBtn").forEach(btn=>{btn.addEventListener("click",()=>{if(btn.dataset.folder===actGmailFolder)return;document.querySelectorAll(".actGmailFolderBtn").forEach(b=>b.classList.toggle("active",b===btn));actGmailFolder=btn.dataset.folder;actGmailSelectedMessageId=null;clearInterval(actGmailThreadPollTimer);actGmailShowPane("empty");loadActGmailMessageList(true)})});async function loadActGmailMessageList(reset){const listEl=$("actGmailMessageList");if(reset){actGmailMessages=[];actGmailNextPageToken=null;listEl.innerHTML='<div class="chat-ghl-list-loading">Loading…</div>'}const res=await callGmailApi({action:"list_messages",folder:actGmailFolder,pageToken:actGmailNextPageToken||void 0});if(!res.ok){listEl.innerHTML=`<div class="chat-ghl-list-empty">${escapeHtml(res.error||"Failed to load messages.")}</div>`;$("actGmailLoadMoreBtn").style.display="none";return}actGmailInboxAddress=res.inboxAddress||"";$("actGmailInboxAddress").textContent=actGmailInboxAddress;actGmailMessages=actGmailMessages.concat(res.messages||[]);actGmailNextPageToken=res.nextPageToken||null;renderActGmailMessageList();$("actGmailLoadMoreBtn").style.display=actGmailNextPageToken?"":"none"}function actGmailSortedMessages(){return actGmailMessages.slice().sort((a,b)=>{if(!!a.unread!==!!b.unread)return a.unread?-1:1;return new Date(b.date||0)-new Date(a.date||0)})}function renderActGmailMessageList(){const listEl=$("actGmailMessageList");if(!actGmailMessages.length){listEl.innerHTML=`<div class="chat-ghl-list-empty">No ${{sent:"sent mail",updates:"updates",spam:"spam"}[actGmailFolder]||"inbox mail"} yet.</div>`;return}listEl.innerHTML=actGmailSortedMessages().map(m=>`
    <div class="chat-ghl-list-item ${m.id===actGmailSelectedMessageId?"active":""}" data-msg-id="${m.id}">
      <div class="chat-conv-info">
        <div class="chat-conv-name">${escapeHtml(actGmailFolder==="sent"?m.to||"(no recipient)":m.from||"(unknown sender)")}</div>
        <div class="chat-conv-preview">${escapeHtml(m.subject||"(no subject)")} &middot; ${escapeHtml(m.snippet||"")}</div>
      </div>
      <div style="display:flex; flex-direction:column; align-items:flex-end; gap:4px;">
        <div class="mono" style="font-size:10px; color:var(--text-dim); white-space:nowrap;">${chatGhlFmtWhen(m.date)}</div>
        ${m.unread?'<div class="chat-ghl-list-item-badge" title="Unread"></div>':""}
      </div>
    </div>
  `).join("");listEl.querySelectorAll(".chat-ghl-list-item").forEach(el=>{el.addEventListener("click",()=>{const m=actGmailMessages.find(x=>x.id===el.dataset.msgId);if(m)openActGmailMessage(m)})})}$("actGmailLoadMoreBtn").addEventListener("click",()=>loadActGmailMessageList(false));async function openActGmailMessage(msg){clearInterval(actGmailThreadPollTimer);actGmailSelectedMessageId=msg.id;renderActGmailMessageList();actGmailShowPane("thread");$("actGmailReadingTitle").textContent=msg.subject||"(no subject)";$("actGmailReadingBody").innerHTML='<div class="chat-ghl-list-loading">Loading…</div>';$("actGmailReplyBodyInput").value="";const canText=!!myPermissions.canTextClients;$("actGmailReplyNoPermMsg").style.display=canText?"none":"block";$("actGmailReplyCompose").style.display=canText?"flex":"none";await refreshActGmailThread(msg.threadId);actGmailThreadPollTimer=setInterval(()=>{if(!document.hidden)refreshActGmailThread(msg.threadId)},2e4)}async function refreshActGmailThread(threadId){const res=await callGmailApi({action:"get_thread",threadId});if(actGmailMode!=="thread"||!actGmailSelectedMessageId)return;const bodyEl=$("actGmailReadingBody");if(!res.ok){bodyEl.innerHTML=`<div class="chat-ghl-list-empty">${escapeHtml(res.error||"Failed to load thread.")}</div>`;return}actGmailThreadData=res;let markedAnyRead=false;actGmailMessages.forEach(m=>{if(m.threadId===threadId&&m.unread){m.unread=false;markedAnyRead=true}});if(markedAnyRead)renderActGmailMessageList();const messages=res.messages||[];bodyEl.innerHTML=messages.length?messages.map(m=>`
        <div class="chat-msg-row ${m.direction==="outbound"?"mine":"theirs"}">
          <div class="chat-msg-meta">${escapeHtml(m.from)} &middot; ${chatGhlFmtWhen(m.date)}</div>
          <div class="chat-msg-bubble" style="white-space:pre-wrap;">${chatLinkify(escapeHtml(m.bodyText))}</div>
        </div>
      `).join(""):'<div class="chat-ghl-list-empty">No messages in this thread.</div>';bodyEl.scrollTop=bodyEl.scrollHeight}async function sendActGmailReply(){const input=$("actGmailReplyBodyInput");const text=input.value.trim();if(!text||!actGmailThreadData)return;const sendBtn=$("actGmailReplySendBtn");input.disabled=true;sendBtn.disabled=true;const subj=actGmailThreadData.replySubject||"";const res=await callGmailApi({action:"send_message",to:actGmailThreadData.replyTo,subject:/^re:/i.test(subj)?subj:`Re: ${subj}`,body:text,threadId:actGmailThreadData.threadId,inReplyTo:actGmailThreadData.threadLastMessageId,references:actGmailThreadData.threadReferences});input.disabled=false;sendBtn.disabled=false;if(!res.ok){alert(res.error||"Failed to send.");return}input.value="";input.focus();await refreshActGmailThread(actGmailThreadData.threadId)}$("actGmailReplySendBtn").addEventListener("click",sendActGmailReply);$("actGmailReplyBodyInput").addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();sendActGmailReply()}});$("actGmailComposeBtn").addEventListener("click",()=>{clearInterval(actGmailThreadPollTimer);actGmailSelectedMessageId=null;renderActGmailMessageList();actGmailShowPane("compose");$("actGmailComposeTo").value="";$("actGmailComposeCc").value="";$("actGmailComposeSubject").value="";$("actGmailComposeBody").value="";$("actGmailComposeTo").focus()});async function sendActGmailCompose(){const to=$("actGmailComposeTo").value.trim();const cc=$("actGmailComposeCc").value.trim();const subject=$("actGmailComposeSubject").value.trim();const body=$("actGmailComposeBody").value.trim();if(!to){alert('Enter a "To" address.');return}if(!body){alert("Write something to send.");return}const sendBtn=$("actGmailComposeSendBtn");sendBtn.disabled=true;const res=await callGmailApi({action:"send_message",to,cc:cc||void 0,subject,body});sendBtn.disabled=false;if(!res.ok){alert(res.error||"Failed to send.");return}actGmailShowPane("empty");if(actGmailFolder==="sent")loadActGmailMessageList(true)}$("actGmailComposeSendBtn").addEventListener("click",sendActGmailCompose);function chicagoUtcOffsetMinutes(date){const parts=new Intl.DateTimeFormat("en-US",{timeZone:"America/Chicago",timeZoneName:"shortOffset"}).formatToParts(date);const tzPart=(parts.find(p=>p.type==="timeZoneName")||{}).value||"GMT-6";const m=tzPart.match(/GMT([+-]\d+)(?::(\d+))?/);if(!m)return-360;const hours=parseInt(m[1],10);const mins=m[2]?parseInt(m[2],10):0;return hours*60+(hours<0?-mins:mins)}function ghlCallAtAdjMs(date){return Math.abs(chicagoUtcOffsetMinutes(date))*6e4}function ghlCallAtRangeISO(rangeStart,rangeEnd){return{start:rangeStart?new Date(new Date(rangeStart).getTime()-ghlCallAtAdjMs(new Date(rangeStart))).toISOString():null,end:rangeEnd?new Date(new Date(rangeEnd).getTime()-ghlCallAtAdjMs(new Date(rangeEnd))).toISOString():null}}async function loadCallActivity(){const kind=activeActivityKind;if(kind==="email")return;const tableIdKind=kind.startsWith("task_")?"staff_work":kind;const bodyIds={call:"callActivityBody",sms:"smsActivityBody",appointment:"apptActivityBody",rm_call:"rmCallActivityBody",agent_report:"agentReportActivityBody",productivity:"productivityActivityBody",staff_work:"staffWorkActivityBody",staff_activity:"staffActivityBody"};const colspans={call:10,sms:5,appointment:8,rm_call:8,agent_report:11,productivity:8,staff_work:6,staff_activity:8};const body=$(bodyIds[tableIdKind]);body.innerHTML=`<tr><td colspan="${colspans[tableIdKind]}" class="empty">Loading…</td></tr>`;if(!$("callActFromInput").value||!$("callActToInput").value){const{from,to}=defaultCallActRange();$("callActFromInput").value=from;$("callActToInput").value=to}const{data:agents}=await sb.from("profiles").select("id, full_name, email, ghl_user_id, readymode_username").order("full_name");const agentSel=$("callActAgentFilter");const prevAgent=agentSel.value;agentSel.innerHTML='<option value="">All agents</option>'+(agents||[]).filter(a=>a.ghl_user_id||a.readymode_username).map(a=>`<option value="${a.id}">${escapeHtml(a.full_name||a.email||"—")}</option>`).join("");agentSel.value=prevAgent;const agentId=agentSel.value;const dirVal=kind==="call"?$("callActDirectionFilter").value:"";const{start:rangeStart,end:rangeEnd}=callActRangeISO();loadActivityTotals(agentId,dirVal,rangeStart,rangeEnd);const nameById={};(agents||[]).forEach(a=>{nameById[a.id]=a.full_name||a.email});const{data:actScheduleRows}=await sb.from("schedules").select("user_id, timezone");const actAgentTz={};(actScheduleRows||[]).forEach(s=>{if(s.timezone)actAgentTz[s.user_id]=s.timezone});const resolveActTz=userId=>globalDisplayTzMode==="agent"?actAgentTz[userId]:globalDisplayTzMode;const tzShortLabel=tz=>tz==="Africa/Cairo"?"Cairo":tz==="America/Chicago"?"Chicago":tz||null;const fmt=(iso,userId)=>{if(!iso)return"—";const tz=resolveActTz(userId);return new Date(iso).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...tz?{timeZone:tz}:{}})};const fmtCallAdj=(iso,userId)=>{if(!iso)return"—";const adjusted=new Date(new Date(iso).getTime()+ghlCallAtAdjMs(new Date(iso)));const tz=resolveActTz(userId);return adjusted.toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...tz?{timeZone:tz}:{}})};const whoCell=(userId,ghlUserId,firstName)=>userId?escapeHtml(nameById[userId]||"Agent"):`<span style="color:var(--text-dim);">unmapped${firstName?` (${escapeHtml(firstName)})`:` (${escapeHtml(ghlUserId||"—")})`}</span>`;const contactCell=(name,phone,email,id)=>{const bits=[name,phone,email].filter(Boolean).map(escapeHtml);return bits.length?bits.join(" · "):id?escapeHtml(id):"—"};if(kind==="call"){const{start:callRangeStart,end:callRangeEnd}=ghlCallAtRangeISO(rangeStart,rangeEnd);let q=sb.from("ghl_calls").select("id, user_id, ghl_user_id, contact_id, contact_name, contact_phone, contact_email, direction, status, disposition, duration_seconds, from_number, to_number, recording_url, call_at, call_start_time, call_end_time").order("call_at",{ascending:false}).limit(300);if(callRangeStart)q=q.gte("call_at",callRangeStart);if(callRangeEnd)q=q.lte("call_at",callRangeEnd);if(agentId)q=q.eq("user_id",agentId);if(dirVal)q=q.eq("direction",dirVal);const{data,error}=await q;if(error){body.innerHTML=`<tr><td colspan="10" class="msg error">${escapeHtml(error.message)}</td></tr>`;return}if(!data||!data.length){body.innerHTML='<tr><td colspan="10" class="empty">No calls in this range.</td></tr>';return}body.innerHTML="";data.forEach(r=>{const dur=r.duration_seconds!=null?`${Math.floor(r.duration_seconds/60)}m ${r.duration_seconds%60}s`:"—";const num=r.direction==="outbound"?r.to_number||"—":r.from_number||"—";const recording=r.recording_url?`<a href="${escapeHtml(safeHrefUrl(r.recording_url))}" target="_blank" rel="noopener">Play</a>`:"—";const tr=document.createElement("tr");tr.innerHTML=`
        <td class="mono" style="color:var(--text-2);">${fmtCallAdj(r.call_start_time||r.call_at,r.user_id)}</td>
        <td class="mono" style="color:var(--text-2);">${fmtCallAdj(r.call_end_time,r.user_id)}</td>
        <td>${whoCell(r.user_id,r.ghl_user_id)}</td>
        <td>${contactCell(r.contact_name,r.contact_phone,r.contact_email,r.contact_id)}</td>
        <td>${escapeHtml(r.direction||"—")}</td>
        <td>${escapeHtml(r.status||"—")}</td>
        <td>${escapeHtml(r.disposition||"—")}</td>
        <td class="mono">${dur}</td>
        <td class="mono" style="color:var(--text-2);">${escapeHtml(num)}</td>
        <td>${recording}</td>
      `;body.appendChild(tr)})}else if(kind==="sms"){let q=sb.from("client_sms_log").select("id, kind, message, from_number, created_at, sent_by, client_accounts(full_name, phone, email)").neq("kind","manual").order("created_at",{ascending:false}).limit(300);if(rangeStart)q=q.gte("created_at",rangeStart);if(rangeEnd)q=q.lte("created_at",rangeEnd);if(agentId)q=q.eq("sent_by",agentId);const{data,error}=await q;if(error){body.innerHTML=`<tr><td colspan="5" class="msg error">${escapeHtml(error.message)}</td></tr>`;return}if(!data||!data.length){body.innerHTML='<tr><td colspan="5" class="empty">No automated texts in this range.</td></tr>';return}body.innerHTML="";data.forEach(r=>{const client=r.client_accounts||{};const kindLabel=(SMS_TEMPLATE_META.find(t=>t.key===r.kind)||{}).label||r.kind;const tr=document.createElement("tr");tr.innerHTML=`
        <td class="mono" style="color:var(--text-2);">${fmt(r.created_at,r.sent_by)}</td>
        <td>${contactCell(client.full_name,client.phone,client.email,null)}</td>
        <td>${escapeHtml(kindLabel)}</td>
        <td>${escapeHtml(r.message||"—")}</td>
        <td>${whoCell(r.sent_by,null)}</td>
      `;body.appendChild(tr)})}else if(kind==="appointment"){let q=sb.from("ghl_appointments").select("id, user_id, ghl_user_id, contact_id, contact_name, contact_phone, contact_email, title, status, start_time, end_time, received_at, assigned_user_first_name, timezone, meeting_location").limit(300);if(rangeStart&&rangeEnd){q=q.or(`and(start_time.gte.${rangeStart},start_time.lte.${rangeEnd}),and(start_time.is.null,received_at.gte.${rangeStart},received_at.lte.${rangeEnd})`)}else if(rangeStart){q=q.or(`start_time.gte.${rangeStart},and(start_time.is.null,received_at.gte.${rangeStart})`)}else if(rangeEnd){q=q.or(`start_time.lte.${rangeEnd},and(start_time.is.null,received_at.lte.${rangeEnd})`)}if(agentId)q=q.eq("user_id",agentId);const{data,error}=await q;if(error){body.innerHTML=`<tr><td colspan="8" class="msg error">${escapeHtml(error.message)}</td></tr>`;return}if(!data||!data.length){body.innerHTML='<tr><td colspan="8" class="empty">No appointments in this range.</td></tr>';return}const sorted=[...data].sort((a,b)=>{const ta=new Date(a.start_time||a.received_at).getTime();const tb=new Date(b.start_time||b.received_at).getTime();return tb-ta});body.innerHTML="";sorted.forEach(r=>{const usedFallback=!r.start_time;const startLabel=fmt(r.start_time||r.received_at,r.user_id)+(usedFallback?' <span style="color:var(--text-dim);" title="No start time came through on the webhook — showing when it was received instead">(est.)</span>':"");const activeTz=resolveActTz(r.user_id);const tzLabel=tzShortLabel(activeTz)||r.timezone||"—";const tzTitle=r.timezone?`Originally scheduled in ${escapeHtml(r.timezone)}`:"";const tr=document.createElement("tr");tr.innerHTML=`
        <td class="mono" style="color:var(--text-2);">${startLabel}</td>
        <td class="mono" style="color:var(--text-2);">${fmt(r.end_time,r.user_id)}</td>
        <td class="mono" style="color:var(--text-2);"${tzTitle?` title="${tzTitle}"`:""}>${escapeHtml(tzLabel)}</td>
        <td>${whoCell(r.user_id,r.ghl_user_id,r.assigned_user_first_name)}</td>
        <td>${contactCell(r.contact_name,r.contact_phone,r.contact_email,r.contact_id)}</td>
        <td>${escapeHtml(r.status||"—")}</td>
        <td>${escapeHtml(r.title||"—")}</td>
        <td>${escapeHtml(r.meeting_location||"—")}</td>
      `;body.appendChild(tr)})}else if(kind==="rm_call"){let q=sb.from("readymode_calls").select("id, user_id, agent_username, campaign, contact_name, contact_phone, direction, disposition, duration_seconds, call_at").order("call_at",{ascending:false}).limit(300);if(rangeStart)q=q.gte("call_at",rangeStart);if(rangeEnd)q=q.lte("call_at",rangeEnd);if(agentId)q=q.eq("user_id",agentId);if(dirVal)q=q.eq("direction",dirVal);const{data,error}=await q;if(error){body.innerHTML=`<tr><td colspan="8" class="msg error">${escapeHtml(error.message)}</td></tr>`;return}if(!data||!data.length){body.innerHTML='<tr><td colspan="8" class="empty">No ReadyMode calls in this range. Import a Call Logs CSV or connect the API from User Management &rarr; Integrations.</td></tr>';return}body.innerHTML="";data.forEach(r=>{const dur=r.duration_seconds!=null?`${Math.floor(r.duration_seconds/60)}m ${r.duration_seconds%60}s`:"—";const tr=document.createElement("tr");tr.innerHTML=`
        <td class="mono" style="color:var(--text-2);">${fmt(r.call_at,r.user_id)}</td>
        <td>${whoCell(r.user_id,r.agent_username)}</td>
        <td>${escapeHtml(r.contact_name||"—")}</td>
        <td>${escapeHtml(r.campaign||"—")}</td>
        <td>${escapeHtml(r.direction||"—")}</td>
        <td>${escapeHtml(r.disposition||"—")}</td>
        <td class="mono">${dur}</td>
        <td class="mono" style="color:var(--text-2);">${escapeHtml(r.contact_phone||"—")}</td>
      `;body.appendChild(tr)})}else if(kind==="agent_report"){let q=sb.from("readymode_productivity_daily").select("id, user_id, agent_username, login_id, work_date, shift_start, shift_end, logged_in_seconds, ready_seconds, break_plus_lunch_seconds, meeting_seconds, talk_seconds, calls_made").order("work_date",{ascending:false}).limit(300);if(rangeStart)q=q.gte("work_date",rangeStart.slice(0,10));if(rangeEnd)q=q.lte("work_date",rangeEnd.slice(0,10));if(agentId)q=q.eq("user_id",agentId);const{data,error}=await q;if(error){body.innerHTML=`<tr><td colspan="11" class="msg error">${escapeHtml(error.message)}</td></tr>`;return}if(!data||!data.length){body.innerHTML='<tr><td colspan="11" class="empty">No Agent Report data in this range. Import an Agent Report CSV from User Management &rarr; Integrations.</td></tr>';return}body.innerHTML="";const hms=secs=>{if(secs==null)return"—";const h=Math.floor(secs/3600),m=Math.floor(secs%3600/60);return`${h}h ${m}m`};data.forEach(r=>{const tr=document.createElement("tr");tr.innerHTML=`
        <td class="mono" style="color:var(--text-2);">${escapeHtml(r.work_date||"—")}</td>
        <td>${whoCell(r.user_id,r.agent_username)}</td>
        <td class="mono" style="color:var(--text-2);">${escapeHtml(r.login_id||"—")}</td>
        <td class="mono" style="color:var(--text-2);">${fmt(r.shift_start,r.user_id)}</td>
        <td class="mono" style="color:var(--text-2);">${fmt(r.shift_end,r.user_id)}</td>
        <td class="mono">${hms(r.logged_in_seconds)}</td>
        <td class="mono">${hms(r.ready_seconds)}</td>
        <td class="mono">${hms(r.break_plus_lunch_seconds)}</td>
        <td class="mono">${hms(r.meeting_seconds)}</td>
        <td class="mono">${hms(r.talk_seconds)}</td>
        <td class="mono">${r.calls_made??"—"}</td>
      `;body.appendChild(tr)})}else if(kind==="productivity"){let q=sb.from("readymode_productivity_daily").select("id, user_id, agent_username, work_date, ready_pct, talk_pct_of_ready, calls_per_ready_hr, calls_per_logged_hr, calls_made, calls_connected").order("work_date",{ascending:false}).limit(300);if(rangeStart)q=q.gte("work_date",rangeStart.slice(0,10));if(rangeEnd)q=q.lte("work_date",rangeEnd.slice(0,10));if(agentId)q=q.eq("user_id",agentId);const{data,error}=await q;if(error){body.innerHTML=`<tr><td colspan="8" class="msg error">${escapeHtml(error.message)}</td></tr>`;return}if(!data||!data.length){body.innerHTML='<tr><td colspan="8" class="empty">No productivity data in this range. Import an Agent Report CSV from User Management &rarr; Integrations.</td></tr>';return}body.innerHTML="";const pct=v=>v==null?"—":`${v}%`;const perHr=v=>v==null?"—":`${v}/hr`;data.forEach(r=>{const tr=document.createElement("tr");tr.innerHTML=`
        <td class="mono" style="color:var(--text-2);">${escapeHtml(r.work_date||"—")}</td>
        <td>${whoCell(r.user_id,r.agent_username)}</td>
        <td class="mono">${pct(r.ready_pct)}</td>
        <td class="mono">${pct(r.talk_pct_of_ready)}</td>
        <td class="mono">${perHr(r.calls_per_ready_hr)}</td>
        <td class="mono">${perHr(r.calls_per_logged_hr)}</td>
        <td class="mono">${r.calls_made??"—"}</td>
        <td class="mono">${r.calls_connected??"—"}</td>
      `;body.appendChild(tr)})}else if(kind==="staff_work"||kind.startsWith("task_")){const filterTaskId=kind.startsWith("task_")?kind.slice("task_".length):null;let q=sb.from("leads").select("id, user_id, task_id, submitted_at, extra_fields, status, count, review_note").order("submitted_at",{ascending:false}).limit(300);if(filterTaskId)q=q.eq("task_id",filterTaskId);if(agentId)q=q.eq("user_id",agentId);if(rangeStart)q=q.gte("submitted_at",rangeStart);if(rangeEnd)q=q.lte("submitted_at",rangeEnd);const{data,error}=await q;if(error){body.innerHTML=`<tr><td colspan="6" class="msg error">${escapeHtml(error.message)}</td></tr>`;return}const rowsData=data||[];if($("actStatExtensionTotal")){$("actStatExtensionTotal").textContent=rowsData.length;$("actStatExtensionPending").textContent=rowsData.filter(r=>r.status==="pending").length;$("actStatExtensionAccepted").textContent=rowsData.filter(r=>r.status==="accepted").length;$("actStatExtensionRejected").textContent=rowsData.filter(r=>r.status==="rejected").length}if($("actStatExtensionHours")){if(filterTaskId){let hoursQuery=sb.from("time_entries").select("start_time, end_time, idle_seconds, break_seconds").eq("task_id",filterTaskId);if(agentId)hoursQuery=hoursQuery.eq("user_id",agentId);if(rangeStart)hoursQuery=hoursQuery.or(`end_time.is.null,end_time.gte.${rangeStart}`);if(rangeEnd)hoursQuery=hoursQuery.lte("start_time",rangeEnd);const{data:hourRows,error:hoursErr}=await hoursQuery;if(hoursErr){console.error("Failed to load logged hours for task",filterTaskId,hoursErr);$("actStatExtensionHours").textContent="—"}else{const rangeStartMs=rangeStart?new Date(rangeStart).getTime():-Infinity;const rangeEndMs=rangeEnd?new Date(rangeEnd).getTime():Infinity;const totalSec=(hourRows||[]).reduce((sum,e)=>{const startMs=Math.max(new Date(e.start_time).getTime(),rangeStartMs);const endMs=Math.min(e.end_time?new Date(e.end_time).getTime():Date.now(),rangeEndMs);const rawSec=Math.max(0,(endMs-startMs)/1e3-(e.idle_seconds||0)-(e.break_seconds||0));return sum+rawSec},0);$("actStatExtensionHours").textContent=fmtShort(totalSec)}}else{$("actStatExtensionHours").textContent="—"}}if(!rowsData.length){body.innerHTML='<tr><td colspan="6" class="empty">No submissions in this range.</td></tr>';return}const{data:taskRows}=await sb.from("tasks").select("id, name, category_id");const taskNameById={};const categoryIdByTaskId={};(taskRows||[]).forEach(t=>{taskNameById[t.id]=t.name;categoryIdByTaskId[t.id]=t.category_id});const{data:catFieldRows}=await sb.from("task_category_fields").select("category_id, field_key, label");const labelByKeyPerCategory={};(catFieldRows||[]).forEach(f=>{(labelByKeyPerCategory[f.category_id]||={})[f.field_key]=f.label});const globalLabelByKey={};(catFieldRows||[]).forEach(f=>{if(!(f.field_key in globalLabelByKey))globalLabelByKey[f.field_key]=f.label});body.innerHTML="";const canEditLeadsHere=canEditLeadsSection("queue_review")||canEditLeadsSection("tickets_review")||canEditLeadsSection("queue_review_ispeedtolead");const canDeleteLeadsHere=myIsOwner||myRole==="admin";rowsData.forEach(r=>{const catId=categoryIdByTaskId[r.task_id];const defs=catId&&labelByKeyPerCategory[catId]||{};const rowExtraFields=r.extra_fields&&typeof r.extra_fields==="object"?r.extra_fields:{};const fieldsText=Object.keys(rowExtraFields).filter(k=>k!=="recording_url"&&rowExtraFields[k]!==null&&rowExtraFields[k]!=="").map(k=>`${defs[k]||globalLabelByKey[k]||k}: ${rowExtraFields[k]}`).join(", ")||"—";const meta=LEAD_STATUS_META[r.status]||LEAD_STATUS_META.pending;const rejectedLocked=r.status==="rejected"&&!!r.review_note;const agentLabel=nameById[r.user_id]||"Agent";const tr=document.createElement("tr");tr.innerHTML=`
        <td class="mono" style="color:var(--text-2);">${fmt(r.submitted_at,r.user_id)}</td>
        <td>${escapeHtml(agentLabel)}</td>
        <td>${escapeHtml(taskNameById[r.task_id]||"—")}</td>
        <td>${escapeHtml(fieldsText)}</td>
        <td><span class="dot ${meta.dot}"></span><span class="${meta.text}">${meta.label}</span></td>
        <td><div style="display:flex; gap:6px; align-items:center; flex-wrap:wrap;">
          ${r.status!=="accepted"&&!rejectedLocked&&canEditLeadsHere?`<button class="staff-work-accept-btn" style="background:transparent; border:1px solid var(--good); border-radius:6px; color:var(--good); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px;">Accept</button>`:""}
          ${r.status!=="rejected"&&canEditLeadsHere?`<button class="staff-work-reject-btn" style="background:transparent; border:1px solid var(--border-light); border-radius:6px; color:var(--danger); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px;">Reject</button>`:""}
          ${canDeleteLeadsHere?`<button class="staff-work-del-btn" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px; font-family:var(--mono);">Delete</button>`:""}
        </div></td>
      `;const acceptBtn=tr.querySelector(".staff-work-accept-btn");const rejectBtn=tr.querySelector(".staff-work-reject-btn");const delBtn=tr.querySelector(".staff-work-del-btn");if(acceptBtn)guardedClick(acceptBtn,async()=>{await setLeadStatus(r,"accepted",agentLabel);loadCallActivity()});if(rejectBtn)guardedClick(rejectBtn,async()=>{await setLeadStatus(r,"rejected",agentLabel);loadCallActivity()});if(delBtn)delBtn.addEventListener("click",async()=>{if(!confirm(`Permanently delete this submission — ${agentLabel}, ${taskNameById[r.task_id]||"task"}? This cannot be undone.`))return;await deleteLead(r,`${agentLabel} — ${taskNameById[r.task_id]||"Staff Work"}`);loadCallActivity()});body.appendChild(tr)})}else if(kind==="staff_activity"){let propertyKeyFor2=function(r){return leadPropertyKey(r.addr||r.addr_alt,r.seller_phone_norm)},creditOutcome2=function(agent,r){const effectiveStatus=leadCountsAsAccepted(r)?"accepted":r.status;if(agent.creditedLeadIds.has(r.id))return;if(effectiveStatus==="accepted"){const propertyKey=propertyKeyFor2(r);if(propertyKey){if(agent.acceptedPropertyKeys.has(propertyKey))return;agent.acceptedPropertyKeys.add(propertyKey)}agent.creditedLeadIds.add(r.id);creditedAcceptedIds.add(r.id);agent.accepted++}else if(effectiveStatus==="rejected"){agent.creditedLeadIds.add(r.id);creditedRejectedIds.add(r.id);agent.rejected++;agent.rejectedLeads.push(r)}};var propertyKeyFor=propertyKeyFor2,creditOutcome=creditOutcome2;const roleSel=$("callActRoleFilter");const prevRole=roleSel.value;const{data:allProfiles}=await sb.from("profiles").select("id, full_name, email, role");const staffProfiles=(allProfiles||[]).filter(p=>!PROTECTED_ROLES.has(p.role));const allDistinctRoles=[...new Set(staffProfiles.map(p=>p.role).filter(Boolean))].sort();roleSel.innerHTML='<option value="">All roles</option>'+allDistinctRoles.map(r=>`<option value="${escapeHtml(r)}">${escapeHtml(r)}</option>`).join("");roleSel.value=prevRole;const roleFilterVal=roleSel.value;const relevantAgents=staffProfiles.filter(p=>(!roleFilterVal||p.role===roleFilterVal)&&(!agentId||p.id===agentId));const relevantIds=relevantAgents.map(p=>p.id);[$("actStatCardSaPrimehome"),$("actStatCardSaGyhomes"),$("actThSaPrimehome"),$("actThSaGyhomes")].forEach(el=>{if(el)el.style.display=myRole==="agent"?"none":""});if(!relevantIds.length){body.innerHTML=`<tr><td colspan="${myRole==="agent"?6:8}" class="empty">No agents match this role/agent filter.</td></tr>`;["actStatSaAgents","actStatSaSubmitted","actStatSaAccepted","actStatSaRejected","actStatSaPrimehome","actStatSaGyhomes","actStatSaHours"].forEach(id=>{if($(id))$(id).textContent="—"});return}const IDS_CHUNK=200;async function fetchByIds(table,selectCols,idCol,dateCol,isOpenEndedRange){const rows2=[];for(let i=0;i<relevantIds.length;i+=IDS_CHUNK){const chunk=relevantIds.slice(i,i+IDS_CHUNK);let q=sb.from(table).select(selectCols).in(idCol,chunk);if(dateCol){if(isOpenEndedRange){if(rangeEnd)q=q.lte("start_time",rangeEnd);if(rangeStart)q=q.or(`end_time.is.null,end_time.gte.${rangeStart}`)}else{if(rangeStart)q=q.gte(dateCol,rangeStart);if(rangeEnd)q=q.lte(dateCol,rangeEnd)}}const{data:chunkData,error:chunkErr}=await q;if(chunkErr){console.error(`Failed to load a chunk of ${table} for Staff Activity`,chunkErr);continue}rows2.push(...chunkData||[])}return rows2}const[submittedRows,reviewedRows,ispdRows,hourRows]=await Promise.all([fetchByIds("leads","id, user_id, reviewed_by, status, seller_phone_norm, seller_investor_name, review_note, reject_reason_category, reviewed_at, addr:extra_fields->>form_address, addr_alt:extra_fields->>property_address","user_id","submitted_at"),fetchByIds("leads","id, reviewed_by, status, seller_phone_norm, seller_investor_name, review_note, reject_reason_category, reviewed_at, addr:extra_fields->>form_address, addr_alt:extra_fields->>property_address","reviewed_by","reviewed_at"),fetchByIds("ispeedtolead_submissions","submitted_by, channel, success","submitted_by","created_at"),fetchByIds("time_entries","user_id, start_time, end_time, idle_seconds, break_seconds","user_id","start_time",true)]);const perAgent={};relevantAgents.forEach(p=>{perAgent[p.id]={submitted:0,accepted:0,rejected:0,primehome:0,gyhomes:0,hoursSec:0,acceptedPropertyKeys:new Set,creditedLeadIds:new Set,rejectedLeads:[]}});const creditedAcceptedIds=new Set;const creditedRejectedIds=new Set;submittedRows.forEach(r=>{const agent=perAgent[r.user_id];if(!agent)return;agent.submitted++;creditOutcome2(agent,r)});reviewedRows.forEach(r=>{const agent=perAgent[r.reviewed_by];if(!agent)return;creditOutcome2(agent,r)});ispdRows.forEach(r=>{if(!perAgent[r.submitted_by]||!r.success)return;if(r.channel==="gyhomes")perAgent[r.submitted_by].gyhomes++;else perAgent[r.submitted_by].primehome++});const rangeStartMs=rangeStart?new Date(rangeStart).getTime():-Infinity;const rangeEndMs=rangeEnd?new Date(rangeEnd).getTime():Infinity;hourRows.forEach(e=>{if(!perAgent[e.user_id])return;const startMs=Math.max(new Date(e.start_time).getTime(),rangeStartMs);const endMs=Math.min(e.end_time?new Date(e.end_time).getTime():Date.now(),rangeEndMs);const rawSec=Math.max(0,(endMs-startMs)/1e3-(e.idle_seconds||0)-(e.break_seconds||0));perAgent[e.user_id].hoursSec+=rawSec});let totals={submitted:0,accepted:0,rejected:0,primehome:0,gyhomes:0,hoursSec:0};const rows=relevantAgents.map(p=>{const s=perAgent[p.id];totals.submitted+=s.submitted;totals.primehome+=s.primehome;totals.gyhomes+=s.gyhomes;totals.hoursSec+=s.hoursSec;return{agent:p,stats:s}}).sort((a,b)=>(a.agent.full_name||a.agent.email||"").localeCompare(b.agent.full_name||b.agent.email||""));totals.accepted=creditedAcceptedIds.size;totals.rejected=creditedRejectedIds.size;if($("actStatSaAgents")){$("actStatSaAgents").textContent=rows.length;$("actStatSaSubmitted").textContent=totals.submitted;$("actStatSaAccepted").textContent=totals.accepted;$("actStatSaRejected").textContent=totals.rejected;$("actStatSaPrimehome").textContent=totals.primehome;$("actStatSaGyhomes").textContent=totals.gyhomes;$("actStatSaHours").textContent=fmtShort(totals.hoursSec)}const hidePrimehomeGyhomes=myRole==="agent";const colCount=hidePrimehomeGyhomes?6:8;body.innerHTML="";rows.forEach(({agent:p,stats:s})=>{const hasRejections=s.rejectedLeads.length>0;const tr=document.createElement("tr");tr.innerHTML=`
        <td>${escapeHtml(p.full_name||p.email||"—")}</td>
        <td style="color:var(--text-2);">${escapeHtml(p.role||"—")}</td>
        <td class="mono">${s.submitted}</td>
        <td class="mono good">${s.accepted}</td>
        <td class="mono bad">${hasRejections?`<button class="staff-activity-rejected-btn" style="background:none; border:none; color:inherit; font:inherit; cursor:pointer; text-decoration:underline dotted; padding:0;" title="Click to see why each lead was rejected">${s.rejected}</button>`:s.rejected}</td>
        ${hidePrimehomeGyhomes?"":`<td class="mono">${s.primehome}</td><td class="mono">${s.gyhomes}</td>`}
        <td class="mono">${fmtShort(s.hoursSec)}</td>
      `;body.appendChild(tr);if(hasRejections){const detailTr=document.createElement("tr");detailTr.style.display="none";const items=s.rejectedLeads.slice().sort((a,b)=>new Date(b.reviewed_at||0)-new Date(a.reviewed_at||0));detailTr.innerHTML=`<td colspan="${colCount}" style="background:rgba(127,127,127,0.08); padding:10px 16px;">
          <table style="width:100%; font-size:12px; border-collapse:collapse;">
            <thead>
              <tr style="text-align:left; color:var(--text-dim); text-transform:uppercase; letter-spacing:.04em; font-size:10px;">
                <th style="font-weight:600; padding:0 10px 6px 0;">Date</th>
                <th style="font-weight:600; padding:0 10px 6px 0;">Lead</th>
                <th style="font-weight:600; padding:0 10px 6px 0;">Rejected Reason</th>
                <th style="font-weight:600; padding:0 0 6px 0;">Lead Summary</th>
              </tr>
            </thead>
            <tbody>
            ${items.map(l=>`
              <tr>
                <td style="white-space:nowrap; color:var(--text-2); padding:2px 10px 2px 0; vertical-align:top;">${l.reviewed_at?new Date(l.reviewed_at).toLocaleDateString():"—"}</td>
                <td style="white-space:nowrap; padding:2px 10px 2px 0; vertical-align:top;">${escapeHtml(l.seller_investor_name||"—")}</td>
                <td style="white-space:nowrap; padding:2px 10px 2px 0; vertical-align:top;">${escapeHtml(l.reject_reason_category||"—")}</td>
                <td style="padding:2px 0;">${escapeHtml(l.review_note||"No reason recorded")}</td>
              </tr>
            `).join("")}
            </tbody>
          </table>
        </td>`;body.appendChild(detailTr);const toggleBtn=tr.querySelector(".staff-activity-rejected-btn");if(toggleBtn)toggleBtn.addEventListener("click",()=>{detailTr.style.display=detailTr.style.display==="none"?"":"none"})}})}}async function loadActivityTotals(agentId,dirVal,rangeStart,rangeEnd){const setStat=(id,n,isError)=>{$(id).textContent=isError?"—":String(n??0)};const withFilters=(query,col,{direction,hasAgent}={})=>{if(rangeStart)query=query.gte(col,rangeStart);if(rangeEnd)query=query.lte(col,rangeEnd);if(agentId&&hasAgent)query=query.eq("user_id",agentId);if(direction&&dirVal)query=query.eq("direction",dirVal);return query};const withDateFilters=(query,col,{hasAgent}={})=>{if(rangeStart)query=query.gte(col,rangeStart.slice(0,10));if(rangeEnd)query=query.lte(col,rangeEnd.slice(0,10));if(agentId&&hasAgent)query=query.eq("user_id",agentId);return query};let apptsQuery=sb.from("ghl_appointments").select("id",{count:"exact",head:true});if(rangeStart&&rangeEnd){apptsQuery=apptsQuery.or(`and(start_time.gte.${rangeStart},start_time.lte.${rangeEnd}),and(start_time.is.null,received_at.gte.${rangeStart},received_at.lte.${rangeEnd})`)}else if(rangeStart){apptsQuery=apptsQuery.or(`start_time.gte.${rangeStart},and(start_time.is.null,received_at.gte.${rangeStart})`)}else if(rangeEnd){apptsQuery=apptsQuery.or(`start_time.lte.${rangeEnd},and(start_time.is.null,received_at.lte.${rangeEnd})`)}if(agentId)apptsQuery=apptsQuery.eq("user_id",agentId);const{start:callsRangeStart,end:callsRangeEnd}=ghlCallAtRangeISO(rangeStart,rangeEnd);let callsQuery=sb.from("ghl_calls").select("id",{count:"exact",head:true});if(callsRangeStart)callsQuery=callsQuery.gte("call_at",callsRangeStart);if(callsRangeEnd)callsQuery=callsQuery.lte("call_at",callsRangeEnd);if(agentId)callsQuery=callsQuery.eq("user_id",agentId);if(dirVal)callsQuery=callsQuery.eq("direction",dirVal);let textsQuery=sb.from("client_sms_log").select("id",{count:"exact",head:true}).neq("kind","manual");if(rangeStart)textsQuery=textsQuery.gte("created_at",rangeStart);if(rangeEnd)textsQuery=textsQuery.lte("created_at",rangeEnd);if(agentId)textsQuery=textsQuery.eq("sent_by",agentId);const[calls,texts,appts,rmCalls,agentReportRows]=await Promise.all([callsQuery,textsQuery,apptsQuery,withFilters(sb.from("readymode_calls").select("id",{count:"exact",head:true}),"call_at",{direction:true,hasAgent:true}),withDateFilters(sb.from("readymode_productivity_daily").select("id",{count:"exact",head:true}),"work_date",{hasAgent:true})]);setStat("actStatCalls",calls.count,calls.error);setStat("actStatTexts",texts.count,texts.error);setStat("actStatAppointments",appts.count,appts.error);setStat("actStatRmCalls",rmCalls.count,rmCalls.error);setStat("actStatAgentReport",agentReportRows.count,agentReportRows.error);setStat("actStatProductivity",agentReportRows.count,agentReportRows.error)}function toDatetimeLocal(iso){if(!iso)return"";const d=new Date(iso);const pad=n=>String(n).padStart(2,"0");return`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}T${pad(d.getHours())}:${pad(d.getMinutes())}`}function updateTsModalDuration(){const startVal=$("tsModalStart").value;const endVal=$("tsModalEnd").value;const idleMin=parseInt($("tsModalIdle").value,10)||0;const breakMin=parseInt($("tsModalBreak").value,10)||0;const durEl=$("tsModalDuration");if(!startVal){durEl.textContent="00:00";return}const startMs=new Date(startVal).getTime();const endMs=endVal?new Date(endVal).getTime():Date.now();const netSec=Math.max(0,(endMs-startMs)/1e3-idleMin*60-breakMin*60);const h=Math.floor(netSec/3600),m=Math.floor(netSec%3600/60);durEl.textContent=`${String(h).padStart(2,"0")}:${String(m).padStart(2,"0")}`}["tsModalStart","tsModalEnd","tsModalIdle","tsModalBreak"].forEach(id=>{$(id).addEventListener("input",updateTsModalDuration)});function openTsModal(entry){$("tsModalMsg").textContent="";$("tsModalEntryId").value=entry?entry.id:"";$("tsModalTitle").textContent=entry?"Edit entry":"Add manual entry";const excludeSelf=!myPermissions.isProtected&&!(entry&&entry.user_id===myUserId);const agentOptions=excludeSelf?tsAllAgents.filter(a=>a.id!==myUserId):tsAllAgents;$("tsModalAgent").innerHTML=agentOptions.map(a=>`<option value="${a.id}">${escapeHtml(a.full_name||a.email)}</option>`).join("");if(excludeSelf&&!agentOptions.length){$("tsModalMsg").textContent="You don't have any assigned team members to add time for."}const wantedAgentId=entry?entry.user_id:$("tsAgentFilter").value||"";$("tsModalAgent").value=agentOptions.some(a=>a.id===wantedAgentId)?wantedAgentId:(agentOptions[0]||{}).id||"";$("tsModalTask").value=entry?entry.task_id||"":"";$("tsModalStart").value=entry?toDatetimeLocal(entry.start_time):toDatetimeLocal(new Date().toISOString());$("tsModalEnd").value=entry?toDatetimeLocal(entry.end_time):toDatetimeLocal(new Date().toISOString());$("tsModalIdle").value=entry?Math.round((entry.idle_seconds||0)/60):0;$("tsModalBreak").value=entry?Math.round((entry.break_seconds||0)/60):0;updateTsModalDuration();$("tsModalOverlay").classList.add("active")}function closeTsModal(){$("tsModalOverlay").classList.remove("active")}$("tsAddEntryBtn").addEventListener("click",()=>openTsModal(null));$("tsModalCancelBtn").addEventListener("click",closeTsModal);$("tsModalOverlay").addEventListener("click",e=>{if(e.target.id==="tsModalOverlay")closeTsModal()});$("tsModalSaveBtn").addEventListener("click",async()=>{const id=$("tsModalEntryId").value;const userId=$("tsModalAgent").value;const taskId=$("tsModalTask").value;const startVal=$("tsModalStart").value;const endVal=$("tsModalEnd").value;const msg=$("tsModalMsg");if(!userId||!startVal){msg.textContent="User and start time are required.";return}const newStartMs=new Date(startVal).getTime();const newEndMs=endVal?new Date(endVal).getTime():Date.now();if(endVal&&newEndMs<=newStartMs){msg.textContent="End time must be after start time.";return}let overlapQuery=sb.from("time_entries").select("id, start_time, end_time").eq("user_id",userId).or(`end_time.is.null,end_time.gt.${new Date(newStartMs).toISOString()}`).lt("start_time",endVal?new Date(newEndMs).toISOString():new Date(864e13).toISOString()).limit(1);if(id)overlapQuery=overlapQuery.neq("id",id);const{data:overlapping}=await overlapQuery;if(overlapping&&overlapping.length>0){const other=overlapping[0];const fmt=d=>tsFmtDateTime(d,userId);msg.textContent=`Overlaps an existing entry for this agent (${fmt(other.start_time)} – ${other.end_time?fmt(other.end_time):"live"}). Adjust the time range.`;return}const payload={user_id:userId,task_id:taskId||null,start_time:new Date(startVal).toISOString(),end_time:endVal?new Date(endVal).toISOString():null,idle_seconds:(parseInt($("tsModalIdle").value,10)||0)*60,break_seconds:(parseInt($("tsModalBreak").value,10)||0)*60};if(!id)payload.is_manual=true;if(id)payload.is_edited=true;const{error}=id?await sb.from("time_entries").update(payload).eq("id",id):await sb.from("time_entries").insert(payload);if(error){msg.textContent=/exclude|overlap/i.test(error.message)?"This overlaps another time entry for this agent. Adjust the time range and try again.":error.message;return}const agentLabel=(tsAllAgents.find(a=>a.id===userId)||{}).full_name||(tsAllAgents.find(a=>a.id===userId)||{}).email||userId;const taskLabel=(tsAllTasks.find(t=>t.id===taskId)||{}).name||"no task";closeTsModal();loadTimesheets();loadTeam();refreshDetailIfViewing(userId);logActivity(id?"time_entry.update":"time_entry.create","time_entry",id||null,`${id?"Edited":"Added manual"} entry for ${agentLabel} — ${taskLabel}, ${new Date(payload.start_time).toLocaleString()} to ${payload.end_time?new Date(payload.end_time).toLocaleString():"live"}`)});async function endTimesheetEntryNow(entry,label){const nowISO=new Date().toISOString();const{error}=await sb.from("time_entries").update({end_time:nowISO}).eq("id",entry.id).is("end_time",null);if(error){alert(`Couldn't end this entry: ${error.message}`);return}loadTimesheets();loadTeam();await refreshDetailIfViewing(entry.user_id);logActivity("time_entry.end_now","time_entry",entry.id,`Ended live entry now for ${label||entry.id}`)}async function deleteTimesheetEntry(entry,label){const id=typeof entry==="object"?entry.id:entry;const userId=typeof entry==="object"?entry.user_id:null;await sb.from("time_entries").delete().eq("id",id);loadTimesheets();loadTeam();if(userId)await refreshDetailIfViewing(userId);logActivity("time_entry.delete","time_entry",id,`Deleted time entry — ${label||id}`)}let lastLeadsRows=[];let leadsAllAgents=[];let leadsAllProfiles=[];let leadsAllTasks=[];let leadsAllForms=[];let leadsAgentTz={};let leadsIspeedtoleadSendByLeadId={};let leadsGyhomesSendByLeadId={};let leadsPrimehomeSendByLeadId={};let leadsFullData=[];let leadsImportBatchFilter="";let leadsInboundPostbackByLeadId={};let leadsExtraFieldsLabelFn=()=>"";let leadsExtraFieldsEntriesFn=()=>[];let leadsAgentNameFn=()=>"—";let leadsTaskNameFn=()=>"No campaign";let leadsFormNameFn=()=>"No campaign";let leadsPrimeHomeBuyersFormId=null;let leadsTicketFormId=null;let leadsQueueFilters={user:"",seller:"",phone:"",task:"",status:"pending",dateFrom:"",dateTo:""};let leadsTicketsFilters={user:"",seller:"",phone:"",stage:"",dateFrom:"",dateTo:""};let leadsIspdFilters={user:"",seller:"",phone:"",task:"",status:"pending",dateFrom:"",dateTo:"",sent:"not_sent",sentChannel:"",sentBy:"",sentDateFrom:"",sentDateTo:""};function leadsTzFor(userId){if(globalDisplayTzMode==="agent")return userId&&leadsAgentTz[userId]||void 0;return globalDisplayTzMode||void 0}function leadsRangeBounds(){const fromVal=$("leadsFromDateInput").value;const toVal=$("leadsToDateInput").value||fromVal;if(!fromVal&&!toVal)return{start:null,end:null};let startMs=null,endMs=null;if(fromVal){const[fy,fm,fd]=fromVal.split("-").map(Number);({startMs}=dayBoundsMsInZone(globalDisplayTzMode,null,fy,fm,fd))}if(toVal){const[ty,tm,td]=toVal.split("-").map(Number);({endMs}=dayBoundsMsInZone(globalDisplayTzMode,null,ty,tm,td))}return{start:startMs===null?null:new Date(startMs).toISOString(),end:endMs===null?null:new Date(endMs).toISOString()}}function leadsRangeLabel(){const fromVal=$("leadsFromDateInput").value;const toVal=$("leadsToDateInput").value||fromVal;if(!fromVal&&!toVal)return"All time";const fmt=v=>{const[y,m,d]=v.split("-").map(Number);return new Date(y,m-1,d).toLocaleDateString([],{month:"short",day:"numeric",year:"numeric"})};if(fromVal&&toVal)return fromVal===toVal?fmt(fromVal):`${fmt(fromVal)} – ${fmt(toVal)}`;return fromVal?`Since ${fmt(fromVal)}`:`Through ${fmt(toVal)}`}const LEADS_DATE_RANGE_STORAGE_KEY="ptai_leads_date_range_v2";function restoreLeadsDateRange(){try{const saved=JSON.parse(localStorage.getItem(LEADS_DATE_RANGE_STORAGE_KEY)||"null");if(saved&&saved.from)$("leadsFromDateInput").value=saved.from;if(saved&&saved.to)$("leadsToDateInput").value=saved.to}catch(e){}}function saveLeadsDateRange(){try{localStorage.setItem(LEADS_DATE_RANGE_STORAGE_KEY,JSON.stringify({from:$("leadsFromDateInput").value,to:$("leadsToDateInput").value}))}catch(e){}}$("leadsFromDateInput").addEventListener("change",()=>{saveLeadsDateRange();loadLeads()});$("leadsToDateInput").addEventListener("change",()=>{saveLeadsDateRange();loadLeads()});function leadsFetchRangeBounds(){const dash=leadsRangeBounds();if(!dash.start&&!dash.end)return{start:null,end:null};let startMs=dash.start?new Date(dash.start).getTime():null;let endMs=dash.end?new Date(dash.end).getTime():null;const extraFromIds=["leadsQueueDateFromFilter","leadsTicketsDateFromFilter","leadsIspdDateFromFilter","allQueueDateFromFilter"];const extraToIds=["leadsQueueDateToFilter","leadsTicketsDateToFilter","leadsIspdDateToFilter","allQueueDateToFilter"];extraFromIds.forEach(id=>{const el=$(id);if(el&&el.value){const[y,m,d]=el.value.split("-").map(Number);const{startMs:s}=dayBoundsMsInZone(globalDisplayTzMode,null,y,m,d);if(startMs===null||s<startMs)startMs=s}});extraToIds.forEach(id=>{const el=$(id);if(el&&el.value){const[y,m,d]=el.value.split("-").map(Number);const{endMs:e}=dayBoundsMsInZone(globalDisplayTzMode,null,y,m,d);if(endMs===null||e>endMs)endMs=e}});return{start:startMs===null?null:new Date(startMs).toISOString(),end:endMs===null?null:new Date(endMs).toISOString()}}const LEAD_STATUS_META={accepted:{label:"accepted",dot:"on",text:"pct-focus"},pending:{label:"pending",dot:"status-break",text:"text-break"},rejected:{label:"rejected",dot:"status-idle",text:"text-idle"},call_back:{label:"call back",dot:"status-break",text:"text-break"}};function isIspdRefundLead(r,postback){const p=postback!==void 0?postback:leadsInboundPostbackByLeadId[r.id];return r.status==="rejected"&&!!p&&p.mapped_status==="rejected"&&String(p.status_raw||"").toLowerCase().includes("refund")}function ispdManuallyDelivered(r){return!!r.manual_delivery_at}function ispdHasPostbackDelivery(r){const p=leadsInboundPostbackByLeadId[r.id];return!!p&&p.matched_via!=="phone_ineligible"}function ispdPostbackOnlyDelivery(r){return ispdHasPostbackDelivery(r)&&!(leadsIspeedtoleadSendByLeadId[r.id]||{}).success}let leadsActiveSection=null;function initLeadsTab(){document.querySelectorAll(".leads-subtab-btn").forEach(btn=>{btn.style.display=canViewLeadsSection(btn.dataset.leadsSection)?"":"none"});const firstVisible=Array.from(document.querySelectorAll(".leads-subtab-btn")).find(b=>b.style.display!=="none");const target=leadsActiveSection&&canViewLeadsSection(leadsActiveSection)&&document.querySelector(`.leads-subtab-btn[data-leads-section="${leadsActiveSection}"]`)?leadsActiveSection:firstVisible?firstVisible.dataset.leadsSection:null;if(target)activateLeadsSection(target)}function activateLeadsSection(key){leadsActiveSection=key;document.querySelectorAll(".leads-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.leadsSection===key));document.querySelectorAll(".leads-section").forEach(s=>{s.style.display=s.id==="leadsSection-"+key?"block":"none"});if(key==="dashboard"||key==="queue_review"||key==="queue_review_ispeedtolead"||key==="tickets_review"||key==="all_queue")loadLeads();if(key==="queue_review"&&!campaignGuidesLoaded)loadCampaignGuide();if(key==="queue_review"&&!campaignCriteriaKeywordsLoaded)loadCampaignCriteriaKeywords();if(key==="submission_forms")initSubmissionFormsSection();else if(key==="leads_dispute")loadDisputesList();else if(key==="all_queue")loadAllQueue();else if(key==="call_back")loadCallBackQueue();else applyLeadsSectionEditGating(key)}document.querySelectorAll(".leads-subtab-btn").forEach(btn=>{btn.addEventListener("click",()=>activateLeadsSection(btn.dataset.leadsSection))});function refreshLeadsSectionByKey(key){if(key==="submission_forms")initSubmissionFormsSection();else if(key==="leads_dispute")loadDisputesList();else if(key==="all_queue")loadAllQueue();else loadLeads()}document.querySelectorAll(".leads-refresh-btn").forEach(btn=>{btn.addEventListener("click",async()=>{if(btn.disabled)return;btn.disabled=true;const original=btn.innerHTML;btn.innerHTML="&#8635; Refreshing…";await refreshLeadsSectionByKey(btn.dataset.refreshKey);btn.innerHTML=original;btn.disabled=false})});function applyLeadsSectionEditGating(key,scopeEl){const panel=scopeEl||document.getElementById("leadsSection-"+key);if(!panel)return;const editable=canEditLeadsSection(key);panel.querySelectorAll(".edit-only").forEach(el=>{el.disabled=!editable;el.style.opacity=editable?"":"0.4";el.title=editable?"":"Your role has view-only access to this section"})}let csubAllRows=[];let csubActiveSection="dashboard";const CSUB_SERVICE_KEY_PATTERN=/(please\s*select|check)\s*one\s*of\s*the\s*following\s*services/i;const CSUB_DATA_FORM_SERVICES=["niche list (data order form)","skiptracing"];const CSUB_QUALIFICATION_SERVICES=["caller only (criteria) form"];const CSUB_COLD_CALLER_SERVICE="cold caller package (criteria + data) form";function csubServiceValue(row){const fields=row.fields||{};const key=Object.keys(fields).find(k=>CSUB_SERVICE_KEY_PATTERN.test(k));return key?String(fields[key]||"").trim():""}function csubRoutesFor(row){const service=csubServiceValue(row).toLowerCase();if(!service)return["data","qualification"];if(service===CSUB_COLD_CALLER_SERVICE)return["data","qualification"];if(CSUB_DATA_FORM_SERVICES.includes(service))return["data"];if(CSUB_QUALIFICATION_SERVICES.includes(service))return["qualification"];return["data","qualification"]}function csubNormalizeLabel(key){return String(key).replace(/\(([^)]+)\)\s*$/,"").toLowerCase().replace(/[^a-z0-9]+/g," ").trim().replace(/\s+/g," ")}const CSUB_FIELD_SECTION_MAP={[csubNormalizeLabel("Full Name")]:"both",[csubNormalizeLabel("Phone")]:"both",[csubNormalizeLabel("Email")]:"both",[csubNormalizeLabel("Please select one of the following services")]:"both",[csubNormalizeLabel("Check one of the following services")]:"both",[csubNormalizeLabel("Any other details to help us understand your order?")]:"both",[csubNormalizeLabel("Type of Leads (data pulling)")]:"data",[csubNormalizeLabel("File Upload (Skiptracing)")]:"data",[csubNormalizeLabel("Property Type (data pulling)")]:"data",[csubNormalizeLabel("Target Market (data pulling & Criteria)")]:"both",[csubNormalizeLabel("Campaign Name (Criteria)")]:"qualification",[csubNormalizeLabel("Do you want the seller to always have a valid reason for selling or you can accept leads when the seller says that he can sell the property for the right offer ? (Criteria)")]:"qualification",[csubNormalizeLabel("Do you accept listed properties? (Criteria)")]:"qualification",[csubNormalizeLabel("Do you cover Realtor Commission/Fees? (Criteria)")]:"qualification",[csubNormalizeLabel("Do you cover closing fees ? (Criteria)")]:"qualification",[csubNormalizeLabel('Do you need your Cold Caller to ask if the homeowner has an "Asking Price”? (Criteria)')]:"qualification",[csubNormalizeLabel("Do you buy Above or Below Market price or do you accept any motivated seller even if it is Above Market Value? (Criteria)")]:"qualification",[csubNormalizeLabel("Market value Source (Criteria)")]:"qualification",[csubNormalizeLabel("Do you have any property price restrictions? For example, do you only work with properties above or below a specific price range? If so, could you please provide more details? (Criteria)")]:"qualification",[csubNormalizeLabel("What is the maximum closing timeline acceptable? (Criteria)")]:"qualification",[csubNormalizeLabel("Do you accept referrals outside of your targeted market? (Criteria)")]:"qualification",[csubNormalizeLabel("Would you like us to ask about the email address of the seller ? (Criteria)")]:"qualification"};function csubFieldSuffix(key){const m=String(key).match(/\(([^)]+)\)\s*$/);return m?m[1].trim().toLowerCase():null}function csubFieldSectionFor(key){const known=CSUB_FIELD_SECTION_MAP[csubNormalizeLabel(key)];if(known)return known;const suffix=csubFieldSuffix(key);if(!suffix)return"both";const isDataTagged=suffix.includes("data pulling")||suffix.includes("data order form")||suffix.includes("skiptracing");const isCriteriaTagged=suffix.includes("criteria");if(isDataTagged&&isCriteriaTagged)return"both";if(isDataTagged)return"data";if(isCriteriaTagged)return"qualification";return"both"}function csubFieldBelongsToSection(key,section){const sec=csubFieldSectionFor(key);return sec==="both"||sec===section}const CSUB_TABLE_DUPLICATE_FIELDS=new Set([csubNormalizeLabel("Full Name"),csubNormalizeLabel("Phone"),csubNormalizeLabel("Email")]);const CAMPAIGN_GUIDE_SECTION_LABELS={target_market:"Target Market",seller_qualification:"Seller Qualification Criteria",property_criteria:"Property Criteria",pricing_terms:"Pricing & Deal Terms",referral_policy:"Referral Policy",other:"Additional Criteria"};const CAMPAIGN_GUIDE_FIELD_META={[csubNormalizeLabel("Target Market (data pulling & Criteria)")]:{section:"target_market",kind:"geo"},[csubNormalizeLabel("Do you want the seller to always have a valid reason for selling or you can accept leads when the seller says that he can sell the property for the right offer ? (Criteria)")]:{section:"seller_qualification",kind:"valid_reason"},[csubNormalizeLabel("Do you accept listed properties? (Criteria)")]:{section:"property_criteria",kind:"listing_status"},[csubNormalizeLabel("Do you cover Realtor Commission/Fees? (Criteria)")]:{section:"pricing_terms",informational:true},[csubNormalizeLabel("Do you cover closing fees ? (Criteria)")]:{section:"pricing_terms",informational:true},[csubNormalizeLabel('Do you need your Cold Caller to ask if the homeowner has an "Asking Price”? (Criteria)')]:{section:"pricing_terms",kind:"must_capture",checklistText:"Asking Price has been captured"},[csubNormalizeLabel("Do you buy Above or Below Market price or do you accept any motivated seller even if it is Above Market Value? (Criteria)")]:{section:"pricing_terms",kind:"mv_pricing"},[csubNormalizeLabel("Market value Source (Criteria)")]:{section:"pricing_terms",informational:true},[csubNormalizeLabel("Do you have any property price restrictions? For example, do you only work with properties above or below a specific price range? If so, could you please provide more details? (Criteria)")]:{section:"pricing_terms",kind:"price_restriction"},[csubNormalizeLabel("What is the maximum closing timeline acceptable? (Criteria)")]:{section:"pricing_terms",kind:"max_days"},[csubNormalizeLabel("Do you accept referrals outside of your targeted market? (Criteria)")]:{section:"referral_policy",kind:"referral",informational:true},[csubNormalizeLabel("Would you like us to ask about the email address of the seller ? (Criteria)")]:{section:"other",informational:true},[csubNormalizeLabel("Campaign Name (Criteria)")]:{section:"other",informational:true}};function campaignGuideClassifyUnknownField(normLabel){if(/\b(county|counties|state|city|cities|zip|area|market|region)\b/.test(normLabel))return"target_market";if(/\b(reason|motivat|cooperat|qualif)\b/.test(normLabel))return"seller_qualification";if(/\b(property|residential|commercial|land|condo|duplex|triplex|fourplex|single family|multi family|listed|listing|mls)\b/.test(normLabel))return"property_criteria";if(/\b(price|pricing|value|commission|fee|closing|budget|cap|equity|mortgage|lien)\b/.test(normLabel))return"pricing_terms";if(/\breferral/.test(normLabel))return"referral_policy";return"other"}function campaignGuideFieldMeta(normLabel){const known=CAMPAIGN_GUIDE_FIELD_META[normLabel];if(known)return known;return{section:campaignGuideClassifyUnknownField(normLabel)}}function parseMaxDaysFromText(text){const s=String(text||"").toLowerCase();if(!s.trim())return null;const nums=[];const re=/(\d+(?:\.\d+)?)\s*(day|week|month|year)s?/g;let m;while(m=re.exec(s)){const n=parseFloat(m[1]);const unit=m[2];const days=unit==="month"?n*30:unit==="week"?n*7:unit==="year"?n*365:n;nums.push(days)}if(nums.length)return Math.max(...nums);const bare=s.match(/\d+(?:\.\d+)?/g);if(bare&&bare.length)return Math.max(...bare.map(Number));return null}function geoTextOverlap(guideValue,leadValue){const norm=v=>String(v||"").toLowerCase().replace(/[^a-z\s]+/g," ").split(/\s+/).filter(w=>w.length>=3);const guideWords=new Set(norm(guideValue));const leadWords=norm(leadValue);if(!guideWords.size||!leadWords.length)return null;return leadWords.some(w=>guideWords.has(w))}const CAMPAIGN_GUIDE_KIND_EVALUATORS={geo(expected,actual,ctx){const overlap=geoTextOverlap(expected,actual);if(overlap!=null)return overlap?"meets":ctx.referralOpen?null:"does_not_meet";return null},valid_reason(expected,actual){const requiresReason=/valid\s*reason/i.test(String(expected||""))&&!/accept\s*(any|leads)/i.test(String(expected||""));if(!requiresReason)return"meets";return String(actual||"").trim()?"meets":"does_not_meet"},listing_status(expected,actual){const acceptsListed=/^\s*yes\b/i.test(String(expected||""));if(acceptsListed)return"meets";const act=String(actual||"").toLowerCase();if(!act.trim())return null;const soundsOffMarket=/off[\s-]*market|not\s*listed|no\b/.test(act);const soundsListed=/\blisted\b|\bmls\b|\byes\b/.test(act)&&!soundsOffMarket;if(soundsOffMarket)return"meets";if(soundsListed)return"does_not_meet";return null},max_days(expected,actual){const expDays=parseMaxDaysFromText(expected);const actDays=parseMaxDaysFromText(actual);if(expDays==null||actDays==null)return null;return actDays<=expDays?"meets":"does_not_meet"},must_capture(expected,actual){const required=/^\s*yes\b/i.test(String(expected||""));if(!required)return"meets";return String(actual==null?"":actual).trim()?"meets":"does_not_meet"},price_restriction(expected,actual){const exp=String(expected||"").trim();if(!exp||/^none/i.test(exp))return"meets";return null},mv_pricing(){return null},referral(){return null}};function campaignGuideFieldIsAmbiguous(meta,value){if(!meta||!meta.kind)return false;if(meta.kind==="max_days")return parseMaxDaysFromText(value)==null&&String(value||"").trim()!=="";return false}function csubIsAttachmentField(key){return csubNormalizeLabel(key)===csubNormalizeLabel("File Upload (Skiptracing)")}function csubIsCallerScriptField(key){return csubNormalizeLabel(key)===csubNormalizeLabel("Caller Script (attachment)")}document.addEventListener("click",async e=>{const b=e.target&&e.target.closest?e.target.closest(".csub-script-open"):null;if(!b)return;e.preventDefault();const path=b.dataset.path;if(!path)return;const label=b.textContent;b.disabled=true;b.textContent="Opening…";const{data,error}=await sb.storage.from("campaign-attachments").createSignedUrl(path,3600);b.disabled=false;b.textContent=label;if(error||!data||!data.signedUrl){alert("Could not open the caller script: "+(error&&error.message||"unknown error"));return}window.open(data.signedUrl,"_blank","noopener")});function csubFieldValueHtml(key,value,editable,rowId){if(csubIsCallerScriptField(key)){const pth=Array.isArray(value)?value[0]:value;if(pth==null||String(pth).trim()==="")return"—";return`<button type="button" class="btn-secondary csub-script-open" data-path="${escapeHtml(String(pth).trim())}" style="width:auto; padding:4px 10px; font-size:10px; margin:2px 6px 2px 0;">Open caller script</button>`}if(csubIsAttachmentField(key)){if(value==null||value===""||Array.isArray(value)&&!value.length)return"—";const urls=(Array.isArray(value)?value:String(value).split(/[\n,]+/)).map(u=>String(u).trim()).filter(Boolean);if(!urls.length)return"—";return urls.map((u,i)=>`<a href="${escapeHtml(safeHrefUrl(u))}" target="_blank" rel="noopener" download class="btn-secondary owner-admin-export-only" style="display:inline-block; width:auto; padding:4px 10px; font-size:10px; text-decoration:none; margin:2px 6px 2px 0;">Download${urls.length>1?" "+(i+1):""}</a>`).join("")}const display=Array.isArray(value)?value.join(", "):value==null?"":String(value);if(editable&&rowId){return`<span class="csub-editable-cell" contenteditable="true" data-id="${rowId}" data-key="${escapeHtml(key)}" data-original="${escapeHtml(display)}">${escapeHtml(display)}</span>`}return display===""?"—":escapeHtml(display)}let campaignGuides={};let campaignGuidesLoaded=false;if($("campaignGuideFilterInput"))$("campaignGuideFilterInput").addEventListener("input",debounce(()=>{campaignGuideFilterText=$("campaignGuideFilterInput").value.trim();renderCampaignGuideList()},200));async function loadCampaignGuide(){const listEl=$("campaignGuideList");if(listEl&&!campaignGuidesLoaded)listEl.innerHTML='<div class="empty">Loading…</div>';const{data,error}=await sb.from("ghl_form_submissions").select("id, form_name, contact_name, contact_phone, contact_email, submitted_at, fields").order("submitted_at",{ascending:false}).limit(1e3);if(error){console.error("Failed to load Campaign Guide data",error);if(listEl)listEl.innerHTML='<div class="empty">Couldn’t load Client Qualification Form data.</div>';return}const qualRows=(data||[]).filter(r=>csubRoutesFor(r).includes("qualification"));const dataRows=(data||[]).filter(r=>csubRoutesFor(r).includes("data"));const normContact=v=>String(v||"").trim().toLowerCase();const guides={};qualRows.forEach(r=>{const campaignName=csubCampaignNameValue(r);if(!campaignName)return;const normName=csubNormalizeLabel(campaignName);if(guides[normName])return;const excludedNorms=new Set([csubNormalizeLabel("Full Name"),csubNormalizeLabel("Phone"),csubNormalizeLabel("Email"),csubNormalizeLabel("Campaign Name"),csubNormalizeLabel("Any other details to help us understand your order?"),csubNormalizeLabel("Dialer (Criteria)"),csubNormalizeLabel("Data Source (Criteria)"),csubNormalizeLabel("Caller Staffing (Criteria)")]);const fields=Object.entries(r.fields||{}).filter(([k])=>csubFieldBelongsToSection(k,"qualification")).filter(([k])=>!CSUB_SERVICE_KEY_PATTERN.test(k)).filter(([k])=>!excludedNorms.has(csubNormalizeLabel(k))).filter(([,v])=>v!==""&&v!=null&&!(Array.isArray(v)&&!v.length)).map(([k,v])=>({normLabel:csubNormalizeLabel(k),label:k,value:Array.isArray(v)?v.join(", "):v}));const matchedDataRow=dataRows.find(d=>r.contact_email&&normContact(d.contact_email)===normContact(r.contact_email)||r.contact_phone&&normContact(d.contact_phone)===normContact(r.contact_phone));if(matchedDataRow){const existingNormLabels=new Set(fields.map(f=>f.normLabel));Object.entries(matchedDataRow.fields||{}).filter(([k])=>csubFieldBelongsToSection(k,"data")).filter(([k])=>!CSUB_SERVICE_KEY_PATTERN.test(k)).filter(([k])=>!csubIsAttachmentField(k)).filter(([k])=>!excludedNorms.has(csubNormalizeLabel(k))).filter(([k])=>!existingNormLabels.has(csubNormalizeLabel(k))).filter(([,v])=>v!==""&&v!=null&&!(Array.isArray(v)&&!v.length)).forEach(([k,v])=>fields.push({normLabel:csubNormalizeLabel(k),label:k,value:Array.isArray(v)?v.join(", "):v}))}guides[normName]={displayName:campaignName,submittedAt:r.submitted_at,submissionId:r.id,dataSubmissionId:matchedDataRow?matchedDataRow.id:null,fields,overridden:false}});const{data:overrideRows}=await sb.from("campaign_guide_overrides").select("campaign_norm_name, campaign_display_name, fields, updated_at, ai_draft, ai_generated_at, ai_model");(overrideRows||[]).forEach(o=>{guides[o.campaign_norm_name]={displayName:o.campaign_display_name,submittedAt:(guides[o.campaign_norm_name]||{}).submittedAt||o.updated_at,submissionId:(guides[o.campaign_norm_name]||{}).submissionId,dataSubmissionId:(guides[o.campaign_norm_name]||{}).dataSubmissionId,fields:(o.fields||[]).map(f=>({normLabel:f.normLabel||csubNormalizeLabel(f.label),label:f.label,value:f.value})),overridden:true,overrideUpdatedAt:o.updated_at,aiDraft:!!o.ai_draft,aiGeneratedAt:o.ai_generated_at,aiModel:o.ai_model}});campaignGuides=guides;campaignGuidesLoaded=true;renderCampaignGuideList();loadCampaignGuideEditRequests();if($("sfFormsList"))renderSfFormsCreatorList();if(leadsActiveSection==="queue_review"&&leadsFullData.length)renderLeadsQueueTable()}let campaignResourcesInitialized=false;let campaignResourcesSelectedNorm="";let campaignResourcesEditingId=null;function campaignResourcesCanManage(){return myIsOwner||myRole==="admin"}const CAMPAIGN_SUB_STAFF_STATUS_LABEL={active:"Active",past_due:"Payment failed — past due",pause_scheduled:"Pause scheduled",paused:"Paused",cancel_scheduled:"Cancellation scheduled",canceled:"Canceled"};function campaignSubTierBreakdown(s){const parts=[];if(s.part_time_callers)parts.push(`${s.part_time_callers} Part-time`);if(s.full_time_callers)parts.push(`${s.full_time_callers} Full-time`);return parts.join(", ")||"—"}const CAMPAIGN_SUB_BUCKET_KEYS=["part_time_ours_dialer_ours_data","part_time_ours_dialer_own_data","part_time_own_dialer_ours_data","part_time_own_dialer_own_data","full_time_ours_dialer_ours_data","full_time_ours_dialer_own_data","full_time_own_dialer_ours_data","full_time_own_dialer_own_data"];function campaignSubCallerGroups(s){const groups=[];["part_time","full_time"].forEach(tier=>{["ours","own"].forEach(dialerChoice=>{["ours","own"].forEach(dataChoice=>{const count=s[`${tier}_${dialerChoice}_dialer_${dataChoice}_data`];if(count)groups.push({tier,dialerChoice,dataChoice,count})})})});return groups}const CAMPAIGN_SUB_STAFF_DATA_RECORDS_BY_TIER={part_time:7500,full_time:1e4};const campaignSubStaffDataPriceForTier=tier=>((CAMPAIGN_SUB_STAFF_DATA_RECORDS_BY_TIER[tier]||CAMPAIGN_SUB_STAFF_DATA_RECORDS_BY_TIER.full_time)*4/100).toFixed(0);const CAMPAIGN_SUB_STAFF_DIALER_PRICE=200;let campaignSubscriptionsStaffCache=[];let campaignSubStaffCreditBalanceCentsByClientId={};let campaignSubStaffPendingResumeSubIds=new Set;async function loadCampaignSubscriptionsStaffList(){const listEl=$("campaignSubscriptionsStaffList");if(!listEl)return;const{data,error}=await sb.from("client_campaign_subscriptions").select("id, client_id, campaign_display_name, caller_tier, part_time_callers, full_time_callers, dialer_choice, data_choice, status, current_period_end, created_at, has_unpaid_invoice, unpaid_invoice_amount_cents, unpaid_invoice_url, is_complimentary, complimentary_expires_at, "+CAMPAIGN_SUB_BUCKET_KEYS.join(", ")+", client_accounts(full_name, company_name, email)").order("created_at",{ascending:false}).limit(200);if(error){listEl.innerHTML='<div class="empty">Couldn’t load campaign subscriptions.</div>';return}campaignSubscriptionsStaffCache=data||[];if(!data||!data.length){listEl.innerHTML='<div class="empty">No paid campaign subscriptions yet.</div>';return}const canManageStripe=canEditFinanceSection("campaign_subscriptions");campaignSubStaffCreditBalanceCentsByClientId={};if(canManageStripe){const clientIds=[...new Set(data.map(s=>s.client_id).filter(Boolean))];if(clientIds.length){const{data:balData,error:balErr}=await sb.functions.invoke("staff-manage-one-off-charge",{body:{action:"get_credit_balances",clientIds}});if(!balErr&&balData&&balData.ok!==false&&balData.balances){campaignSubStaffCreditBalanceCentsByClientId=balData.balances}else{console.error("Failed to load Stripe credit balances",balErr||balData)}}}campaignSubStaffPendingResumeSubIds=new Set;if(canManageStripe){const canceledSubIds=data.filter(s=>s.status==="canceled").map(s=>s.id);if(canceledSubIds.length){const{data:pendingResumeData,error:pendingResumeErr}=await sb.from("client_campaign_payment_requests").select("converts_subscription_id").eq("status","pending").in("converts_subscription_id",canceledSubIds);if(!pendingResumeErr&&pendingResumeData){pendingResumeData.forEach(r=>{if(r.converts_subscription_id)campaignSubStaffPendingResumeSubIds.add(r.converts_subscription_id)})}else if(pendingResumeErr){console.error("Failed to load pending resume payment requests",pendingResumeErr)}}}listEl.innerHTML=data.map(s=>{const isActiveish=["active","past_due"].includes(s.status);const canScale=canManageStripe&&isActiveish&&!s.is_complimentary;const canPauseCancel=canManageStripe&&isActiveish&&!s.is_complimentary;const canEditExpiry=canManageStripe&&isActiveish&&s.is_complimentary;const canManageComplimentaryCallers=canManageStripe&&isActiveish&&s.is_complimentary;const canResumeCanceled=canManageStripe&&s.status==="canceled";const hasPendingResumePayment=canResumeCanceled&&campaignSubStaffPendingResumeSubIds.has(s.id);const clientName=s.client_accounts&&(s.client_accounts.company_name||s.client_accounts.full_name)||"—";const campaignSubStaffTz=nonAgentDisplayTz();const periodEnd=s.current_period_end?new Date(s.current_period_end).toLocaleDateString([],campaignSubStaffTz?{timeZone:campaignSubStaffTz}:{}):"—";const complimentaryExpiry=s.complimentary_expires_at?new Date(s.complimentary_expires_at).toLocaleDateString([],campaignSubStaffTz?{timeZone:campaignSubStaffTz}:{}):"—";const clientCreditCents=s.client_id&&campaignSubStaffCreditBalanceCentsByClientId.hasOwnProperty(s.client_id)?campaignSubStaffCreditBalanceCentsByClientId[s.client_id]:null;return`
      <div class="card" style="margin-bottom:10px;" data-sub-id="${s.id}">
        <div style="display:flex; justify-content:space-between; align-items:flex-start; gap:10px; flex-wrap:wrap;">
          <div>
            <div style="font-weight:700; font-size:13px;">${escapeHtml(s.campaign_display_name)}</div>
            <div style="font-size:11px; color:var(--text-2); margin-top:2px;">${escapeHtml(clientName)} · ${escapeHtml(campaignSubTierBreakdown(s))} · ${s.data_choice==="ours"?"Our data":"Own data"}${clientCreditCents?` · <span class="mono" style="color:var(--good);" title="This client's Stripe account balance — auto-applied to their next invoice">$${(clientCreditCents/100).toFixed(2)} Stripe credit</span>`:""}</div>
          </div>
          <div style="text-align:right; font-size:11px; color:var(--text-2);">
            <div style="font-weight:700; color:var(--text);">${s.is_complimentary?'<span class="mono" style="color:var(--focus); text-transform:uppercase; font-size:10px;">Comp</span> · ':""}${escapeHtml(CAMPAIGN_SUB_STAFF_STATUS_LABEL[s.status]||s.status)}</div>
            <div style="display:flex; align-items:center; justify-content:flex-end; gap:6px; margin-top:2px;">${s.is_complimentary?`Active until: ${complimentaryExpiry}${canEditExpiry?` <button class="btn-outline campaign-sub-staff-set-expiry-btn" data-id="${s.id}" style="width:auto; padding:3px 10px; font-size:11px;" title="Change this complimentary campaign's active-until date">Edit</button>`:""}`:`Next billing: ${periodEnd}${canScale?` <button class="btn-outline campaign-sub-staff-set-billing-date-btn" data-id="${s.id}" style="width:auto; padding:3px 10px; font-size:11px;" title="Change this subscription's next billing/renewal date">Edit</button>`:""}`}</div>
          </div>
        </div>
        ${s.has_unpaid_invoice?`<div style="font-size:11px; color:var(--danger); font-weight:600; margin-top:8px;">${s.unpaid_invoice_amount_cents?`$${(s.unpaid_invoice_amount_cents/100).toFixed(2)} unpaid`:"Payment issue"}${s.unpaid_invoice_url?` — <a href="${escapeHtml(s.unpaid_invoice_url)}" target="_blank" rel="noopener" style="color:inherit;">Invoice</a>`:" (multiple open invoices — see Stripe)"}</div>`:""}
        ${!canScale&&s.status==="past_due"?`<div style="font-size:11px; color:var(--danger); margin-top:8px;">Pay the unpaid invoice before adding or converting callers — removing one is still available from Manage callers.</div>`:""}
        <div style="display:flex; gap:8px; margin-top:10px; flex-wrap:wrap; align-items:center;">
          ${canScale||canManageComplimentaryCallers?`<button class="btn-outline btn-gold staff-manage-sub-open-btn" data-id="${s.id}" style="width:auto;">Manage callers</button>`:""}
          ${s.is_complimentary&&canManageStripe&&isActiveish?`<button class="btn-outline campaign-sub-staff-convert-to-paid-btn" data-id="${s.id}" style="width:auto;" title="Convert this complimentary campaign to a paid subscription">Convert to paid</button>`:""}
          ${canResumeCanceled?`<button class="btn-outline btn-gold campaign-sub-staff-resume-canceled-btn" data-id="${s.id}" style="width:auto;" title="Bring this canceled campaign back — same campaign name, lead history, and Campaign Guide">Resume campaign</button>`:""}
          ${hasPendingResumePayment?`<span class="mono" style="display:inline-flex; align-items:center; padding:1px 7px; border-radius:10px; background:var(--away-dim); color:var(--away); font-size:10px;" title="A resume payment request for this campaign is already awaiting payment — see Finance → Stripe Pending Payment to get the link">Payment pending</span>`:""}
          ${canPauseCancel?`<button class="btn-outline campaign-sub-staff-pause-btn" data-id="${s.id}" style="width:auto;">Pause now</button>`:""}
          ${canPauseCancel?`<button class="btn-outline campaign-sub-staff-cancel-btn" data-id="${s.id}" style="width:auto; color:var(--danger); border-color:var(--danger);">Cancel now</button>`:""}
          ${myIsOwner?`<button class="btn-outline campaign-sub-delete-btn" data-id="${s.id}" style="width:auto; margin-left:auto; color:var(--danger); border-color:var(--danger);">Delete</button>`:""}
        </div>
      </div>`}).join("")}function renderStaffManageSubBody(s){const groups=campaignSubCallerGroups(s);const manageSubTz=nonAgentDisplayTz();const periodEnd=s.current_period_end?new Date(s.current_period_end).toLocaleDateString([],manageSubTz?{timeZone:manageSubTz}:{}):null;const isPastDue=s.status==="past_due";const clientName=s.client_accounts&&(s.client_accounts.company_name||s.client_accounts.full_name)||"—";const clientCreditCents=s.client_id&&campaignSubStaffCreditBalanceCentsByClientId.hasOwnProperty(s.client_id)?campaignSubStaffCreditBalanceCentsByClientId[s.client_id]:null;if(s.is_complimentary){return`
      <h3>${escapeHtml(s.campaign_display_name)}</h3>
      <p style="font-size:12px; color:var(--text-2); line-height:1.5; margin-top:-8px;">${escapeHtml(clientName)} — <span class="mono" style="color:var(--focus); text-transform:uppercase; font-size:10px;">Comp</span> campaign. Add or remove caller capacity — no Stripe charge, no billing cycle involved, since this campaign isn't being billed at all. Use "Convert to paid" on the card behind this modal if you want real Stripe billing instead.</p>

      <div style="margin-top:16px;">
        <div style="font-size:11px; font-weight:700; color:var(--text-2); margin-bottom:6px;">Callers on this campaign</div>
        ${groups.map((g,idx)=>{const tierLabel=g.tier==="full_time"?"Full-time":"Part-time";const dialerLabel=g.dialerChoice==="ours"?"our dialer":"their own dialer";const dataLabel=g.dataChoice==="ours"?"our data":"their own data";return`
          <div style="display:flex; justify-content:space-between; align-items:center; gap:8px; flex-wrap:wrap; padding:8px 0; ${idx>0?"border-top:1px dashed var(--border-light);":""}">
            <div style="font-size:12px;">${g.count} × ${tierLabel} — ${dialerLabel}, ${dataLabel}</div>
            <button class="btn-outline campaign-sub-staff-comp-caller-btn" data-id="${s.id}" data-tier="${g.tier}" data-dialer="${g.dialerChoice}" data-data="${g.dataChoice}" data-delta="-1" style="width:auto; color:var(--danger); border-color:var(--danger);">Remove one</button>
          </div>`}).join("")||`<div style="font-size:12px; color:var(--text-2);">${escapeHtml(campaignSubTierBreakdown(s))}</div>`}
      </div>

      <div style="margin-top:16px; border-top:1px solid var(--border-light); padding-top:12px;">
        <div style="font-size:11px; font-weight:700; color:var(--text-2); margin-bottom:6px;">Add a caller (free)</div>
        <div style="display:flex; gap:8px; flex-wrap:wrap; align-items:center;">
          <select class="campaign-sub-staff-comp-add-tier" data-id="${s.id}" style="width:auto; padding:8px 12px; font-size:12px; border-radius:10px;">
            <option value="part_time">Part-time</option>
            <option value="full_time">Full-time</option>
          </select>
          <select class="campaign-sub-staff-comp-add-dialer" data-id="${s.id}" style="width:auto; padding:8px 12px; font-size:12px; border-radius:10px;">
            <option value="ours">Our dialer</option>
            <option value="own">Own dialer</option>
          </select>
          <select class="campaign-sub-staff-comp-add-data" data-id="${s.id}" style="width:auto; padding:8px 12px; font-size:12px; border-radius:10px;">
            <option value="own" selected>Own data</option>
            <option value="ours">Our data</option>
          </select>
          <button class="btn-primary campaign-sub-staff-comp-caller-btn" data-id="${s.id}" data-delta="1" style="width:auto;">+ Add caller</button>
        </div>
      </div>

      <div class="modal-actions">
        <button class="btn-secondary" onclick="closeStaffManageSubModal()">Close</button>
      </div>
    `}return`
    <h3>${escapeHtml(s.campaign_display_name)}</h3>
    <p style="font-size:12px; color:var(--text-2); line-height:1.5; margin-top:-8px;">${escapeHtml(clientName)}${clientCreditCents?` · <span class="mono" style="color:var(--good);" title="This client's Stripe account balance — auto-applied to their next invoice">$${(clientCreditCents/100).toFixed(2)} Stripe credit</span>`:""} — convert an existing caller's tier, dialer, or data source, add more callers, or remove one. Changes apply immediately, prorated for the rest of this billing cycle${periodEnd?` (through ${periodEnd})`:""}.</p>
    <!-- v362: added here so changing the billing cycle doesn't require
         leaving this modal and hunting for the small "Edit" link on the
         card behind it (that one's still there too, just easier to see
         now — same campaign-sub-staff-set-billing-date-btn handler, bound
         on this modal's body too, so it works from either place). -->
    <div style="display:flex; align-items:center; gap:8px; margin-top:10px; font-size:12px; color:var(--text-2);">
      <span>Next billing: <strong style="color:var(--text);">${periodEnd||"—"}</strong></span>
      <button class="btn-outline campaign-sub-staff-set-billing-date-btn" data-id="${s.id}" style="width:auto; padding:4px 12px; font-size:11px;" title="Change this subscription's next billing/renewal date">Change billing date</button>
    </div>
    ${isPastDue?`<div class="msg error" style="margin-top:10px;">This subscription has a failed payment. Pay the outstanding invoice before adding or converting callers — removing one is still available below.</div>`:""}

    <div style="margin-top:16px;">
      <div style="font-size:11px; font-weight:700; color:var(--text-2); margin-bottom:6px;">Callers on this campaign</div>
      ${groups.map((g,idx)=>{const tierLabel=g.tier==="full_time"?"Full-time":"Part-time";const otherTier=g.tier==="full_time"?"part_time":"full_time";const otherTierLabel=otherTier==="full_time"?"Full-time":"Part-time";const dialerLabel=g.dialerChoice==="ours"?"our dialer":"their own dialer";const otherDialer=g.dialerChoice==="ours"?"own":"ours";const otherDialerLabel=otherDialer==="ours"?`our dialer (+$${CAMPAIGN_SUB_STAFF_DIALER_PRICE}/mo)`:"their own dialer";const dataLabel=g.dataChoice==="ours"?"our data":"their own data";const otherData=g.dataChoice==="ours"?"own":"ours";const otherDataLabel=otherData==="ours"?`our data ($${campaignSubStaffDataPriceForTier(g.tier)}/mo)`:"their own data";return`
        <div style="display:flex; justify-content:space-between; align-items:center; gap:8px; flex-wrap:wrap; padding:8px 0; ${idx>0?"border-top:1px dashed var(--border-light);":""}">
          <div style="font-size:12px;">${g.count} × ${tierLabel} — ${dialerLabel}, ${dataLabel}</div>
          <div style="display:flex; gap:6px; flex-wrap:wrap;">
            ${isPastDue?"":`<button class="btn-outline campaign-sub-staff-convert-tier-btn" data-id="${s.id}" data-from-tier="${g.tier}" data-from-dialer="${g.dialerChoice}" data-from-data="${g.dataChoice}" data-to-tier="${otherTier}" data-to-dialer="${g.dialerChoice}" data-to-data="${g.dataChoice}" style="width:auto;">Convert to ${otherTierLabel}</button>`}
            ${isPastDue?"":`<button class="btn-outline campaign-sub-staff-convert-dialer-btn" data-id="${s.id}" data-from-tier="${g.tier}" data-from-dialer="${g.dialerChoice}" data-from-data="${g.dataChoice}" data-to-tier="${g.tier}" data-to-dialer="${otherDialer}" data-to-data="${g.dataChoice}" style="width:auto;">Switch to ${otherDialerLabel}</button>`}
            ${isPastDue?"":`<button class="btn-outline campaign-sub-staff-convert-data-btn" data-id="${s.id}" data-from-tier="${g.tier}" data-from-dialer="${g.dialerChoice}" data-from-data="${g.dataChoice}" data-to-tier="${g.tier}" data-to-dialer="${g.dialerChoice}" data-to-data="${otherData}" style="width:auto;">Switch to ${otherDataLabel}</button>`}
            <button class="btn-outline campaign-sub-staff-remove-caller-btn" data-id="${s.id}" data-tier="${g.tier}" data-dialer="${g.dialerChoice}" data-data="${g.dataChoice}" style="width:auto; color:var(--danger); border-color:var(--danger);">Remove one</button>
          </div>
        </div>`}).join("")||`<div style="font-size:12px; color:var(--text-2);">${escapeHtml(campaignSubTierBreakdown(s))}</div>`}
    </div>

    ${isPastDue?"":`
    <div style="margin-top:16px; border-top:1px solid var(--border-light); padding-top:12px;">
      <div style="font-size:11px; font-weight:700; color:var(--text-2); margin-bottom:6px;">Add a caller</div>
      <div style="display:flex; gap:8px; flex-wrap:wrap; align-items:center;">
        <select class="campaign-sub-staff-add-caller-tier" data-id="${s.id}" style="width:auto; padding:8px 12px; font-size:12px; border-radius:10px;">
          <option value="part_time">Part-time</option>
          <option value="full_time">Full-time</option>
        </select>
        <select class="campaign-sub-staff-add-caller-dialer" data-id="${s.id}" style="width:auto; padding:8px 12px; font-size:12px; border-radius:10px;">
          <option value="ours">Our dialer (+$${CAMPAIGN_SUB_STAFF_DIALER_PRICE}/mo)</option>
          <option value="own">Own dialer</option>
        </select>
        <!-- v338: data choice is genuinely per-caller now, same as dialer
             -- this dropdown sets what THIS new caller uses, no longer a
             shared subscription-wide setting. "Our data" price/records
             tracks whichever tier is currently selected next to it, see
             campaignSubStaffAddCallerTierChangeHandler. -->
        <select class="campaign-sub-staff-data-choice" data-id="${s.id}" title="Data source for this caller" style="width:auto; padding:8px 12px; font-size:12px; border-radius:10px;">
          <option value="own" selected>Own data</option>
          <option value="ours" class="campaign-sub-staff-data-choice-ours-option">Our data (${CAMPAIGN_SUB_STAFF_DATA_RECORDS_BY_TIER.part_time.toLocaleString()} records / $${campaignSubStaffDataPriceForTier("part_time")}/mo)</option>
        </select>
        <button class="btn-primary campaign-sub-staff-add-caller-btn" data-id="${s.id}" style="width:auto;">+ Add caller</button>
      </div>
    </div>`}

    <div class="modal-actions">
      <button class="btn-secondary" onclick="closeStaffManageSubModal()">Close</button>
    </div>
  `}function openStaffManageSubModal(subscriptionId){const s=campaignSubscriptionsStaffCache.find(x=>x.id===subscriptionId);if(!s)return;$("staffManageSubModalBody").innerHTML=renderStaffManageSubBody(s);$("staffManageSubModalOverlay").classList.add("active")}function closeStaffManageSubModal(){$("staffManageSubModalOverlay").classList.remove("active");$("staffManageSubModalBody").innerHTML=""}if($("campaignSubscriptionsStaffList"))$("campaignSubscriptionsStaffList").addEventListener("click",e=>{const openBtn=e.target.closest(".staff-manage-sub-open-btn");if(openBtn)openStaffManageSubModal(openBtn.dataset.id)});if($("staffManageSubModalOverlay"))$("staffManageSubModalOverlay").addEventListener("click",e=>{if(e.target.id==="staffManageSubModalOverlay")closeStaffManageSubModal()});function campaignSubStaffAddCallerTierChangeHandler(e){const tierSelect=e.target.closest(".campaign-sub-staff-add-caller-tier");if(!tierSelect)return;const dataSelect=document.querySelector(`.campaign-sub-staff-data-choice[data-id="${tierSelect.dataset.id}"]`);const oursOption=dataSelect?dataSelect.querySelector(".campaign-sub-staff-data-choice-ours-option"):null;if(!oursOption)return;const records=CAMPAIGN_SUB_STAFF_DATA_RECORDS_BY_TIER[tierSelect.value]||CAMPAIGN_SUB_STAFF_DATA_RECORDS_BY_TIER.full_time;const price=campaignSubStaffDataPriceForTier(tierSelect.value);oursOption.textContent=`Our data (${records.toLocaleString()} records / $${price}/mo)`}if($("campaignSubscriptionsStaffList"))$("campaignSubscriptionsStaffList").addEventListener("change",campaignSubStaffAddCallerTierChangeHandler);if($("staffManageSubModalBody"))$("staffManageSubModalBody").addEventListener("change",campaignSubStaffAddCallerTierChangeHandler);function campaignSubStaffAskPromoCode(){const value=window.prompt("Discount code (optional) — leave blank if none:","");const trimmed=(value||"").trim();return trimmed||void 0}async function refreshCampaignSubStaffList(subscriptionIdForModal){await loadCampaignSubscriptionsStaffList();if(subscriptionIdForModal&&$("staffManageSubModalOverlay")&&$("staffManageSubModalOverlay").classList.contains("active")){const s=campaignSubscriptionsStaffCache.find(x=>x.id===subscriptionIdForModal);if(s)$("staffManageSubModalBody").innerHTML=renderStaffManageSubBody(s);else closeStaffManageSubModal()}}async function campaignSubStaffListClickHandler(e){const compCallerBtn=e.target.closest(".campaign-sub-staff-comp-caller-btn");if(compCallerBtn){const delta=Number(compCallerBtn.dataset.delta);let tier,dialerChoice,dataChoice;if(delta>0){const tierSelect=document.querySelector(`.campaign-sub-staff-comp-add-tier[data-id="${compCallerBtn.dataset.id}"]`);const dialerSelect=document.querySelector(`.campaign-sub-staff-comp-add-dialer[data-id="${compCallerBtn.dataset.id}"]`);const dataSelect=document.querySelector(`.campaign-sub-staff-comp-add-data[data-id="${compCallerBtn.dataset.id}"]`);tier=tierSelect?tierSelect.value:"part_time";dialerChoice=dialerSelect?dialerSelect.value:"own";dataChoice=dataSelect?dataSelect.value:"own"}else{tier=compCallerBtn.dataset.tier;dialerChoice=compCallerBtn.dataset.dialer;dataChoice=compCallerBtn.dataset.data}const tierLabel=tier==="full_time"?"Full-time":"Part-time";const dialerLabel=dialerChoice==="ours"?"our dialer":"their own dialer";const dataLabel=dataChoice==="ours"?"our data":"their own data";const confirmMsg2=delta>0?`Add a ${tierLabel} caller on ${dialerLabel}, using ${dataLabel}, to this complimentary campaign? No charge — this campaign isn't billed.`:`Remove a ${tierLabel} caller on ${dialerLabel}, using ${dataLabel}, from this complimentary campaign?`;if(!confirm(confirmMsg2))return;compCallerBtn.disabled=true;const original2=compCallerBtn.textContent;compCallerBtn.textContent="…";const{data:data2,error:error2}=await sb.functions.invoke("staff-manage-campaign-subscription",{body:{action:"set_complimentary_callers",subscriptionId:compCallerBtn.dataset.id,tier,dialerChoice,dataChoice,delta}});if(error2||!data2||data2.ok===false){alert(data2&&data2.error||error2&&error2.message||"Failed to update caller capacity.");compCallerBtn.disabled=false;compCallerBtn.textContent=original2;return}logActivity("campaign_subscription.set_complimentary_callers","client_campaign_subscription",compCallerBtn.dataset.id,`Staff ${delta>0?"added":"removed"} a ${tierLabel.toLowerCase()} caller on ${dialerLabel}, using ${dataLabel}, on a complimentary campaign`);await refreshCampaignSubStaffList(compCallerBtn.dataset.id);return}const addCallerBtn=e.target.closest(".campaign-sub-staff-add-caller-btn");if(addCallerBtn){const tierSelect=document.querySelector(`.campaign-sub-staff-add-caller-tier[data-id="${addCallerBtn.dataset.id}"]`);const dialerSelect=document.querySelector(`.campaign-sub-staff-add-caller-dialer[data-id="${addCallerBtn.dataset.id}"]`);const dataSelect=document.querySelector(`.campaign-sub-staff-data-choice[data-id="${addCallerBtn.dataset.id}"]`);const tier=tierSelect?tierSelect.value:"part_time";const dialerChoice=dialerSelect?dialerSelect.value:"ours";const dataChoice=dataSelect?dataSelect.value:"own";const tierLabel=tier==="full_time"?"Full-time":"Part-time";const dialerLabel=dialerChoice==="ours"?"our dialer":"own dialer";const dataLabel=dataChoice==="ours"?"our data":"own data";if(!confirm(`Add a ${tierLabel} caller on ${dialerLabel}, using ${dataLabel}, to this subscription? Prorated for the rest of this billing cycle.`))return;const promoCode=campaignSubStaffAskPromoCode();addCallerBtn.disabled=true;const original2=addCallerBtn.textContent;addCallerBtn.textContent="Adding…";const{data:data2,error:error2}=await sb.functions.invoke("staff-manage-campaign-subscription",{body:{action:"add_caller",subscriptionId:addCallerBtn.dataset.id,tier,dialerChoice,dataChoice,promoCode}});if(error2||!data2||data2.ok===false){alert(data2&&data2.error||error2&&error2.message||"Failed to add caller.");addCallerBtn.disabled=false;addCallerBtn.textContent=original2;return}if(data2.billingWarning)alert(`Caller added, but the billing charge failed: ${data2.billingWarning}

Added as a pending charge the client can pay from their own Billing → Pending payment tab.`);logActivity("campaign_subscription.add_caller","client_campaign_subscription",addCallerBtn.dataset.id,`Staff added a ${tierLabel.toLowerCase()} caller on ${dialerLabel}, using ${dataLabel}, to a campaign subscription`);await refreshCampaignSubStaffList(addCallerBtn.dataset.id);return}const removeCallerBtn=e.target.closest(".campaign-sub-staff-remove-caller-btn");if(removeCallerBtn){const tier=removeCallerBtn.dataset.tier,dialerChoice=removeCallerBtn.dataset.dialer,dataChoice=removeCallerBtn.dataset.data;const tierLabel=tier==="full_time"?"Full-time":"Part-time";const dialerLabel=dialerChoice==="ours"?"our dialer":"their own dialer";const dataLabel=dataChoice==="ours"?"our data":"their own data";if(!confirm(`Remove a ${tierLabel} caller on ${dialerLabel}, using ${dataLabel}, from this subscription? Billing drops immediately, prorated for the rest of this billing cycle.`))return;removeCallerBtn.disabled=true;const original2=removeCallerBtn.textContent;removeCallerBtn.textContent="…";const{data:data2,error:error2}=await sb.functions.invoke("staff-manage-campaign-subscription",{body:{action:"remove_caller",subscriptionId:removeCallerBtn.dataset.id,tier,dialerChoice,dataChoice}});if(error2||!data2||data2.ok===false){alert(data2&&data2.error||error2&&error2.message||"Failed to remove caller.");removeCallerBtn.disabled=false;removeCallerBtn.textContent=original2;return}if(data2.billingWarning)alert(`Caller removed, but the billing charge failed: ${data2.billingWarning}

Added as a pending charge the client can pay from their own Billing → Pending payment tab.`);logActivity("campaign_subscription.remove_caller","client_campaign_subscription",removeCallerBtn.dataset.id,`Staff removed a ${tierLabel.toLowerCase()} caller on ${dialerLabel}, using ${dataLabel}, from a campaign subscription`);await refreshCampaignSubStaffList(removeCallerBtn.dataset.id);return}const convertBtn=e.target.closest(".campaign-sub-staff-convert-tier-btn")||e.target.closest(".campaign-sub-staff-convert-dialer-btn")||e.target.closest(".campaign-sub-staff-convert-data-btn");if(convertBtn){const d=convertBtn.dataset;const isDialerConvert=convertBtn.classList.contains("campaign-sub-staff-convert-dialer-btn");const isDataConvert=convertBtn.classList.contains("campaign-sub-staff-convert-data-btn");const label=isDataConvert?`Switch one caller to ${d.toData==="ours"?"our data":"their own data"}?`:isDialerConvert?`Switch one caller to ${d.toDialer==="ours"?"our dialer":"their own dialer"}?`:`Convert one caller to ${d.toTier==="full_time"?"Full-time":"Part-time"}?`;if(!confirm(`${label} Billing adjusts immediately, prorated for the rest of this billing cycle.`))return;const promoCode=campaignSubStaffAskPromoCode();convertBtn.disabled=true;const original2=convertBtn.textContent;convertBtn.textContent="…";const{data:data2,error:error2}=await sb.functions.invoke("staff-manage-campaign-subscription",{body:{action:"move_caller",subscriptionId:d.id,fromTier:d.fromTier,fromDialerChoice:d.fromDialer,fromDataChoice:d.fromData,toTier:d.toTier,toDialerChoice:d.toDialer,toDataChoice:d.toData,promoCode}});if(error2||!data2||data2.ok===false){alert(data2&&data2.error||error2&&error2.message||"Failed to update caller.");convertBtn.disabled=false;convertBtn.textContent=original2;return}if(data2.billingWarning)alert(`Caller updated, but the billing charge failed: ${data2.billingWarning}

Added as a pending charge the client can pay from their own Billing → Pending payment tab.`);logActivity("campaign_subscription.move_caller","client_campaign_subscription",d.id,"Staff converted a caller on a campaign subscription");await refreshCampaignSubStaffList(d.id);return}const setBillingDateBtn=e.target.closest(".campaign-sub-staff-set-billing-date-btn");if(setBillingDateBtn){const todayIso=new Date().toISOString().slice(0,10);const input=prompt("New next billing date (YYYY-MM-DD):",todayIso);if(!input)return;const nextBillingDate=input.trim();if(!/^\d{4}-\d{2}-\d{2}$/.test(nextBillingDate)||Number.isNaN(new Date(`${nextBillingDate}T00:00:00Z`).getTime())){alert("Enter a valid date in YYYY-MM-DD format.");return}if(!confirm(`Set this subscription's next billing date to ${nextBillingDate}? This only reschedules -- it does not charge or credit the client anything.`))return;setBillingDateBtn.disabled=true;const original2=setBillingDateBtn.textContent;setBillingDateBtn.textContent="…";const{data:data2,error:error2}=await sb.functions.invoke("staff-manage-campaign-subscription",{body:{action:"set_next_billing_date",subscriptionId:setBillingDateBtn.dataset.id,nextBillingDate}});if(error2||!data2||data2.ok===false){alert(data2&&data2.error||error2&&error2.message||"Failed to update the billing date.");setBillingDateBtn.disabled=false;setBillingDateBtn.textContent=original2;return}logActivity("campaign_subscription.set_next_billing_date","client_campaign_subscription",setBillingDateBtn.dataset.id,`Staff changed a campaign subscription's next billing date to ${nextBillingDate}`);loadCampaignSubscriptionsStaffList();return}const setExpiryBtn=e.target.closest(".campaign-sub-staff-set-expiry-btn");if(setExpiryBtn){const defaultDate=new Date(Date.now()+30*24*60*60*1e3).toISOString().slice(0,10);const input=prompt("New active-until date (YYYY-MM-DD) -- the campaign auto-cancels once this passes:",defaultDate);if(!input)return;const complimentaryExpiresAt=input.trim();if(!/^\d{4}-\d{2}-\d{2}$/.test(complimentaryExpiresAt)||Number.isNaN(new Date(`${complimentaryExpiresAt}T00:00:00Z`).getTime())){alert("Enter a valid date in YYYY-MM-DD format.");return}if(new Date(`${complimentaryExpiresAt}T23:59:59.999Z`).getTime()<=Date.now()){alert("The active-until date must be in the future.");return}if(!confirm(`Set this complimentary campaign's active-until date to ${complimentaryExpiresAt}?`))return;setExpiryBtn.disabled=true;const original2=setExpiryBtn.textContent;setExpiryBtn.textContent="…";const{data:data2,error:error2}=await sb.functions.invoke("staff-manage-campaign-subscription",{body:{action:"set_complimentary_expiry",subscriptionId:setExpiryBtn.dataset.id,complimentaryExpiresAt}});if(error2||!data2||data2.ok===false){alert(data2&&data2.error||error2&&error2.message||"Failed to update the active-until date.");setExpiryBtn.disabled=false;setExpiryBtn.textContent=original2;return}logActivity("campaign_subscription.set_complimentary_expiry","client_campaign_subscription",setExpiryBtn.dataset.id,`Staff changed a complimentary campaign's active-until date to ${complimentaryExpiresAt}`);loadCampaignSubscriptionsStaffList();return}const convertToPaidBtn=e.target.closest(".campaign-sub-staff-convert-to-paid-btn");if(convertToPaidBtn){$("csubConvertToPaidMsg").style.display="none";$("csubConvertToPaidTier").value="part_time";$("csubConvertToPaidDialer").value="ours";$("csubConvertToPaidData").value="own";$("csubConvertToPaidSubmitBtn").dataset.id=convertToPaidBtn.dataset.id;$("csubConvertToPaidModalOverlay").classList.add("active");return}const resumeCanceledBtn=e.target.closest(".campaign-sub-staff-resume-canceled-btn");if(resumeCanceledBtn){$("csubResumeCanceledMsg").style.display="none";$("csubResumeCanceledTier").value="part_time";$("csubResumeCanceledDialer").value="ours";$("csubResumeCanceledData").value="own";$("csubResumeCanceledSubmitBtn").dataset.id=resumeCanceledBtn.dataset.id;$("csubResumeCanceledModalOverlay").classList.add("active");return}const pauseBtn=e.target.closest(".campaign-sub-staff-pause-btn");const cancelBtn=e.target.closest(".campaign-sub-staff-cancel-btn");const btn=pauseBtn||cancelBtn;if(!btn)return;const action=pauseBtn?"immediate_pause":"immediate_cancel";const confirmMsg=pauseBtn?"Pause this subscription immediately? Billing stops on the live Stripe subscription right now — unlike the client-facing Request pause, there is no 14-day delay.":"Cancel this subscription immediately? The live Stripe subscription is canceled right now — unlike the client-facing Request cancellation, there is no 14-day delay.";if(!confirm(confirmMsg))return;btn.disabled=true;const original=btn.textContent;btn.textContent=pauseBtn?"Pausing…":"Cancelling…";const{data,error}=await sb.functions.invoke("staff-manage-campaign-subscription",{body:{action,subscriptionId:btn.dataset.id}});if(error||!data||data.ok===false){alert(data&&data.error||error&&error.message||"Failed to update subscription.");btn.disabled=false;btn.textContent=original;return}logActivity(`campaign_subscription.${action}`,"client_campaign_subscription",btn.dataset.id,pauseBtn?"Staff paused a campaign subscription immediately":"Staff cancelled a campaign subscription immediately");loadCampaignSubscriptionsStaffList()}if($("campaignSubscriptionsStaffList"))$("campaignSubscriptionsStaffList").addEventListener("click",campaignSubStaffListClickHandler);if($("staffManageSubModalBody"))$("staffManageSubModalBody").addEventListener("click",campaignSubStaffListClickHandler);function closeCsubConvertToPaidModal(){$("csubConvertToPaidModalOverlay").classList.remove("active")}if($("csubConvertToPaidCancelBtn"))$("csubConvertToPaidCancelBtn").addEventListener("click",closeCsubConvertToPaidModal);if($("csubConvertToPaidModalOverlay"))$("csubConvertToPaidModalOverlay").addEventListener("click",e=>{if(e.target.id==="csubConvertToPaidModalOverlay")closeCsubConvertToPaidModal()});if($("csubConvertToPaidSubmitBtn"))$("csubConvertToPaidSubmitBtn").addEventListener("click",async()=>{const msg=$("csubConvertToPaidMsg");msg.style.display="none";const subscriptionId=$("csubConvertToPaidSubmitBtn").dataset.id;const tier=$("csubConvertToPaidTier").value;const dialerChoice=$("csubConvertToPaidDialer").value;const dataChoice=$("csubConvertToPaidData").value;const tierLabel=tier==="full_time"?"Full-time":"Part-time";const dialerLabel=dialerChoice==="ours"?"our dialer":"the client's own dialer";const dataLabel=dataChoice==="ours"?"our data":"the client's own data";if(!confirm(`Convert this complimentary campaign to a paid ${tierLabel} caller on ${dialerLabel}, using ${dataLabel}? This creates a pending payment request -- use "Get link" under Finance → Stripe Pending Payment to actually charge the client (it attaches automatically to their existing subscription if they already pay for another campaign).`))return;const btn=$("csubConvertToPaidSubmitBtn");btn.disabled=true;btn.textContent="…";const{data,error}=await sb.functions.invoke("staff-manage-campaign-subscription",{body:{action:"convert_to_paid",subscriptionId,tier,dialerChoice,dataChoice}});btn.disabled=false;btn.textContent="Convert";if(error||!data||data.ok===false){let apiMsg=data&&data.error;if(!apiMsg&&error&&error.context&&typeof error.context.json==="function"){try{const b=await error.context.json();apiMsg=b.error}catch(e){}}msg.textContent=apiMsg||error&&error.message||"Failed to start conversion.";msg.style.display="block";return}closeCsubConvertToPaidModal();alert(data.alreadyPending?'A payment request for this conversion is already pending — find it under Finance → Stripe Pending Payment and click "Get link".':'Payment request created — find it under Finance → Stripe Pending Payment and click "Get link" to charge the client.');logActivity("campaign_subscription.convert_to_paid","client_campaign_subscription",subscriptionId,`Staff started converting a complimentary campaign to a paid ${tierLabel.toLowerCase()} caller on ${dialerLabel}, using ${dataLabel}`);if(typeof loadPendingCampaignPayments==="function")loadPendingCampaignPayments()});function closeCsubResumeCanceledModal(){$("csubResumeCanceledModalOverlay").classList.remove("active")}if($("csubResumeCanceledCancelBtn"))$("csubResumeCanceledCancelBtn").addEventListener("click",closeCsubResumeCanceledModal);if($("csubResumeCanceledModalOverlay"))$("csubResumeCanceledModalOverlay").addEventListener("click",e=>{if(e.target.id==="csubResumeCanceledModalOverlay")closeCsubResumeCanceledModal()});if($("csubResumeCanceledSubmitBtn"))$("csubResumeCanceledSubmitBtn").addEventListener("click",async()=>{const msg=$("csubResumeCanceledMsg");msg.style.display="none";const subscriptionId=$("csubResumeCanceledSubmitBtn").dataset.id;const tier=$("csubResumeCanceledTier").value;const dialerChoice=$("csubResumeCanceledDialer").value;const dataChoice=$("csubResumeCanceledData").value;const tierLabel=tier==="full_time"?"Full-time":"Part-time";const dialerLabel=dialerChoice==="ours"?"our dialer":"the client's own dialer";const dataLabel=dataChoice==="ours"?"our data":"the client's own data";if(!confirm(`Resume this canceled campaign with a paid ${tierLabel} caller on ${dialerLabel}, using ${dataLabel}? This creates a pending payment request -- use "Get link" under Finance → Stripe Pending Payment to actually charge the client. The same campaign name, lead history, and Campaign Guide stay intact.`))return;const btn=$("csubResumeCanceledSubmitBtn");btn.disabled=true;btn.textContent="…";const{data,error}=await sb.functions.invoke("staff-manage-campaign-subscription",{body:{action:"resume_canceled",subscriptionId,tier,dialerChoice,dataChoice}});btn.disabled=false;btn.textContent="Resume";if(error||!data||data.ok===false){let apiMsg=data&&data.error;if(!apiMsg&&error&&error.context&&typeof error.context.json==="function"){try{const b=await error.context.json();apiMsg=b.error}catch(e){}}msg.textContent=apiMsg||error&&error.message||"Failed to start resuming this campaign.";msg.style.display="block";return}closeCsubResumeCanceledModal();alert(data.alreadyPending?'A payment request to resume this campaign is already pending — find it under Finance → Stripe Pending Payment and click "Get link".':'Payment request created — find it under Finance → Stripe Pending Payment and click "Get link" to charge the client.');logActivity("campaign_subscription.resume_canceled","client_campaign_subscription",subscriptionId,`Staff started resuming a canceled campaign with a paid ${tierLabel.toLowerCase()} caller on ${dialerLabel}, using ${dataLabel}`);if(typeof loadPendingCampaignPayments==="function")loadPendingCampaignPayments();loadCampaignSubscriptionsStaffList()});document.addEventListener("click",async e=>{const btn=e.target.closest(".campaign-sub-delete-btn");if(!btn)return;if(!confirm("Delete this subscription record permanently? This does NOT cancel the live Stripe subscription — it only removes this record. To actually stop billing, cancel it via the client's Billing tab or the Stripe Dashboard first."))return;btn.disabled=true;const{error}=await sb.from("client_campaign_subscriptions").delete().eq("id",btn.dataset.id);if(error){alert("Failed to delete: "+error.message);btn.disabled=false;return}loadCampaignSubscriptionsStaffList()});async function loadPendingCampaignPayments(){const tbody=$("pendingCampaignPaymentsTbody");if(!tbody)return;const{data,error}=await sb.from("client_campaign_payment_requests").select("id, campaign_name, caller_tier, dialer_choice, data_choice, created_at, client_accounts(full_name, company_name, email)").eq("status","pending").order("created_at",{ascending:false}).limit(200);if(error){tbody.innerHTML=`<tr><td colspan="8" style="color:var(--danger);">${escapeHtml(error.message)}</td></tr>`;return}if(!data||!data.length){tbody.innerHTML='<tr><td colspan="8" style="color:var(--text-dim);">No pending campaign payments.</td></tr>';return}const canGetLink=canEditFinanceSection("stripe_pending_payment");const pendingPaymentTz=nonAgentDisplayTz();tbody.innerHTML=data.map(r=>`
    <tr>
      <td>${escapeHtml(r.campaign_name)}</td>
      <td>${escapeHtml(r.client_accounts&&(r.client_accounts.company_name||r.client_accounts.full_name)||"—")}</td>
      <td>${r.caller_tier==="full_time"?"Full-time":"Part-time"}</td>
      <td>${r.dialer_choice==="ours"?"Ours":"Own"}</td>
      <td>${r.data_choice==="ours"?"Ours":"Own"}</td>
      <td class="mono">${new Date(r.created_at).toLocaleDateString([],pendingPaymentTz?{timeZone:pendingPaymentTz}:{})}</td>
      <td>${canGetLink?`<button class="btn-outline campaign-payment-link-btn" data-id="${r.id}" style="width:auto; padding:5px 10px; font-size:11px;">Get link</button>`:""}</td>
      <td>${myIsOwner?`<button class="btn-outline campaign-payment-delete-btn" data-id="${r.id}" style="width:auto; padding:5px 10px; font-size:11px; color:var(--danger);">Delete</button>`:""}</td>
    </tr>`).join("")}if($("pendingCampaignPaymentsTbody"))$("pendingCampaignPaymentsTbody").addEventListener("click",async e=>{const linkBtn=e.target.closest(".campaign-payment-link-btn");if(linkBtn){linkBtn.disabled=true;const originalText=linkBtn.textContent;linkBtn.textContent="Generating…";const{data,error}=await sb.functions.invoke("staff-campaign-payment-link",{body:{pendingRequestId:linkBtn.dataset.id}});linkBtn.disabled=false;linkBtn.textContent=originalText;if(error||!data||data.ok===false||!data.checkoutUrl&&!data.attachedToExisting){alert(data&&data.error||error&&error.message||"Failed to generate a payment link.");return}if(data.attachedToExisting){alert(data.billingWarning?`Campaign attached to the client's existing subscription, but the billing charge failed: ${data.billingWarning}

Added as a pending charge the client can pay from their own Billing → Pending payment tab.`:"Campaign attached to the client's existing subscription and charged immediately — no payment link needed.");loadPendingCampaignPayments();return}try{await navigator.clipboard.writeText(data.checkoutUrl)}catch{}prompt("Payment link (copied to clipboard if your browser allowed it) — share this with the client:",data.checkoutUrl);return}const deleteBtn=e.target.closest(".campaign-payment-delete-btn");if(deleteBtn){if(!confirm("Delete this pending campaign request permanently? This only removes the request record — it does not affect Stripe."))return;deleteBtn.disabled=true;const{error}=await sb.from("client_campaign_payment_requests").delete().eq("id",deleteBtn.dataset.id);if(error){alert("Failed to delete: "+error.message);deleteBtn.disabled=false;return}loadPendingCampaignPayments()}});async function initCampaignResourcesSection(){$("campaignResourcesAddWrap").style.display=campaignResourcesCanManage()?"":"none";if(!campaignResourcesInitialized){campaignResourcesInitialized=true;const{data}=await sb.from("lead_submission_forms").select("id, name").order("sort_order").order("name");const sel=$("campaignResourcesSelect");sel.innerHTML='<option value="">Select a campaign…</option>'+(data||[]).filter(f=>f.name!==TICKET_FORM_NAME).map(f=>`<option value="${escapeHtml(csubNormalizeLabel(f.name))}" data-display="${escapeHtml(f.name)}">${escapeHtml(f.name)}</option>`).join("");sel.addEventListener("change",()=>{campaignResourcesSelectedNorm=sel.value;loadCampaignResourcesList()})}loadCampaignResourcesList()}let campaignResourcesCache=[];async function loadCampaignResourcesList(){const listEl=$("campaignResourcesList");if(!campaignResourcesSelectedNorm){listEl.innerHTML="";return}listEl.innerHTML='<div class="empty">Loading…</div>';const{data,error}=await sb.from("campaign_resources").select("*").eq("campaign_norm_name",campaignResourcesSelectedNorm).order("title");if(error){listEl.innerHTML='<div class="empty">Couldn’t load resources.</div>';return}campaignResourcesCache=data||[];renderCampaignResourcesList()}function renderCampaignResourcesList(){const listEl=$("campaignResourcesList");if(!campaignResourcesCache.length){listEl.innerHTML='<div class="empty">No resources yet for this campaign.</div>';return}const canManage=campaignResourcesCanManage();listEl.innerHTML=campaignResourcesCache.map(r=>`
    <div class="card" style="margin-bottom:10px;" data-resource-id="${r.id}">
      <div style="display:flex; justify-content:space-between; align-items:flex-start; gap:10px;">
        <h4 style="margin:0 0 8px;">${escapeHtml(r.title)}</h4>
        ${canManage?`<button class="btn-secondary campaign-resource-edit-btn" style="width:auto; padding:6px 12px; flex-shrink:0;" data-id="${r.id}">Edit</button>`:""}
      </div>
      <div style="white-space:pre-wrap; font-size:12px; color:var(--text-dim); line-height:1.6;">${escapeHtml(r.body)}</div>
    </div>
  `).join("");listEl.querySelectorAll(".campaign-resource-edit-btn").forEach(btn=>{btn.addEventListener("click",()=>openCampaignResourceForm(btn.dataset.id))})}function openCampaignResourceForm(id){campaignResourcesEditingId=id||null;const r=id?campaignResourcesCache.find(x=>x.id===id):null;$("campaignResourceFormTitle").textContent=r?"Edit resource":"Add resource";$("campaignResourceTitleInput").value=r?r.title:"";$("campaignResourceBodyInput").value=r?r.body:"";$("campaignResourceDeleteBtn").style.display=r?"":"none";$("campaignResourceFormMsg").style.display="none";$("campaignResourceFormOverlay").style.display="flex"}function closeCampaignResourceForm(){$("campaignResourceFormOverlay").style.display="none";campaignResourcesEditingId=null}$("campaignResourcesAddBtn").addEventListener("click",()=>openCampaignResourceForm(null));$("campaignResourceCancelBtn").addEventListener("click",closeCampaignResourceForm);$("campaignResourceFormOverlay").addEventListener("click",e=>{if(e.target===$("campaignResourceFormOverlay"))closeCampaignResourceForm()});$("campaignResourceSaveBtn").addEventListener("click",async()=>{const msg=$("campaignResourceFormMsg");const title=$("campaignResourceTitleInput").value.trim();const body=$("campaignResourceBodyInput").value.trim();if(!title||!body){msg.textContent="Title and body are both required.";msg.className="msg error";msg.style.display="block";return}const sel=$("campaignResourcesSelect");const displayName=sel.selectedOptions[0]?sel.selectedOptions[0].dataset.display:campaignResourcesSelectedNorm;const{data:{user}}=await sb.auth.getUser();const payload={campaign_norm_name:campaignResourcesSelectedNorm,campaign_display_name:displayName,title,body,updated_by:user?user.id:null};let error;if(campaignResourcesEditingId){({error}=await sb.from("campaign_resources").update(payload).eq("id",campaignResourcesEditingId))}else{({error}=await sb.from("campaign_resources").insert({...payload,created_by:user?user.id:null}))}if(error){msg.textContent=error.message;msg.className="msg error";msg.style.display="block";return}closeCampaignResourceForm();loadCampaignResourcesList()});$("campaignResourceDeleteBtn").addEventListener("click",async()=>{if(!campaignResourcesEditingId)return;if(!confirm("Delete this resource? This cannot be undone."))return;const{error}=await sb.from("campaign_resources").delete().eq("id",campaignResourcesEditingId);if(error){const msg=$("campaignResourceFormMsg");msg.textContent=error.message;msg.className="msg error";msg.style.display="block";return}closeCampaignResourceForm();loadCampaignResourcesList()});function csubSlugifyFieldKey(label){return csubNormalizeLabel(label).replace(/\s+/g,"_").slice(0,60)||"field"}let cgPendingEditRequests=[];async function loadCampaignGuideEditRequests(){const{data,error}=await sb.from("campaign_guide_edit_requests").select("id, campaign_norm_name, campaign_display_name, client_id, proposed_fields, note, created_at, client_accounts(full_name, company_name)").eq("status","pending").order("created_at",{ascending:false});if(error){console.error("Failed to load Campaign Guide edit requests",error);return}cgPendingEditRequests=data||[];const card=$("cgEditRequestsCard");if(card)card.style.display=cgPendingEditRequests.length?"":"none";renderCampaignGuideEditRequests()}function campaignGuideEditRequestDiff(r){const existing=campaignGuides[r.campaign_norm_name];const existingByNorm=new Map((existing?existing.fields:[]).map(f=>[f.normLabel,f.value]));const proposedNormLabels=new Set((r.proposed_fields||[]).map(f=>f.normLabel));const proposed=(r.proposed_fields||[]).map(f=>{if(!existingByNorm.has(f.normLabel))return{...f,change:"added"};if(String(existingByNorm.get(f.normLabel))!==String(f.value))return{...f,change:"changed",oldValue:existingByNorm.get(f.normLabel)};return{...f,change:"unchanged"}});const removed=(existing?existing.fields:[]).filter(f=>!proposedNormLabels.has(f.normLabel));return{proposed,removed}}function renderCampaignGuideEditRequests(){const listEl=$("cgEditRequestsList");if(!listEl)return;if(!cgPendingEditRequests.length){listEl.innerHTML="";return}listEl.innerHTML="";const CHANGE_META={added:{border:"var(--good)",bg:"var(--good)",label:"New"},changed:{border:"var(--focus)",bg:"var(--focus)",label:"Edited"},unchanged:null};cgPendingEditRequests.forEach(r=>{const client=r.client_accounts||{};const{proposed,removed}=campaignGuideEditRequestDiff(r);const row=document.createElement("div");row.style.cssText="padding:10px 0; border-bottom:1px solid var(--border);";row.innerHTML=`
      <div style="display:flex; align-items:center; gap:10px; flex-wrap:wrap;">
        <div style="flex:1; min-width:200px;">
          <span class="mono" style="font-size:13px; font-weight:700;">${escapeHtml(r.campaign_display_name)}</span>
          <span style="font-size:11px; color:var(--text-dim); margin-left:8px;">${escapeHtml(client.company_name||client.full_name||"Client")} · ${new Date(r.created_at).toLocaleDateString([],{month:"short",day:"numeric",year:"numeric",...nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{}})}</span>
        </div>
        <button class="btn-secondary edit-only cg-req-reject-btn" style="width:auto; padding:6px 14px; font-size:11px;">Reject</button>
        <button class="btn-primary edit-only cg-req-approve-btn" style="width:auto; padding:6px 14px; font-size:11px; margin-top:0;">Approve</button>
      </div>
      <div style="margin-top:6px; display:flex; flex-wrap:wrap; gap:6px;">
        ${proposed.map(f=>{const meta=CHANGE_META[f.change];const title=f.change==="changed"?` title="Was: ${escapeHtml(String(f.oldValue))}"`:"";return meta?`<span class="mono"${title} style="background:color-mix(in srgb, ${meta.bg} 14%, var(--inset)); border:1px solid ${meta.border}; border-radius:4px; padding:3px 8px; font-size:11px;"><span style="color:${meta.border}; font-weight:700; margin-right:4px;">${meta.label}</span>${escapeHtml(f.label)}: ${escapeHtml(String(f.value))}</span>`:`<span class="mono" style="background:var(--inset); border-radius:4px; padding:3px 8px; font-size:11px;">${escapeHtml(f.label)}: ${escapeHtml(String(f.value))}</span>`}).join("")||'<span style="font-size:11px; color:var(--text-dim);">(no criteria left — this proposal clears the guide)</span>'}
        ${removed.map(f=>`<span class="mono" style="background:color-mix(in srgb, var(--danger) 14%, var(--inset)); border:1px solid var(--danger); border-radius:4px; padding:3px 8px; font-size:11px; text-decoration:line-through; opacity:0.8;"><span style="color:var(--danger); font-weight:700; margin-right:4px; text-decoration:none;">Removed</span>${escapeHtml(f.label)}: ${escapeHtml(String(f.value))}</span>`).join("")}
      </div>
    `;row.querySelector(".cg-req-approve-btn").addEventListener("click",()=>resolveCampaignGuideEditRequest(r,"approved",row));row.querySelector(".cg-req-reject-btn").addEventListener("click",()=>resolveCampaignGuideEditRequest(r,"rejected",row));listEl.appendChild(row)});applyCsubSectionEditGating("campaign_guide")}async function resolveCampaignGuideEditRequest(request,outcome,row){const approveBtn=row?row.querySelector(".cg-req-approve-btn"):null;const rejectBtn=row?row.querySelector(".cg-req-reject-btn"):null;if(approveBtn&&approveBtn.disabled)return;if(rejectBtn&&rejectBtn.disabled)return;if(!confirm(outcome==="approved"?`Approve this edit? "${request.campaign_display_name}"'s guide will be updated to exactly what the client proposed.`:"Reject this proposed edit? The guide stays unchanged."))return;if(approveBtn)approveBtn.disabled=true;if(rejectBtn)rejectBtn.disabled=true;if(outcome==="approved"){const{error:upsertErr}=await sb.from("campaign_guide_overrides").upsert({campaign_norm_name:request.campaign_norm_name,campaign_display_name:request.campaign_display_name,fields:request.proposed_fields||[],updated_by:myUserId,updated_at:new Date().toISOString()},{onConflict:"campaign_norm_name"});if(upsertErr){alert(upsertErr.message);if(approveBtn)approveBtn.disabled=false;if(rejectBtn)rejectBtn.disabled=false;return}}const{error}=await sb.from("campaign_guide_edit_requests").update({status:outcome,reviewed_by:myUserId,reviewed_at:new Date().toISOString()}).eq("id",request.id);if(error){alert(error.message);if(approveBtn)approveBtn.disabled=false;if(rejectBtn)rejectBtn.disabled=false;return}logActivity("campaign_guide.edit_request_resolve","campaign_guide_edit_request",request.id,`${outcome==="approved"?"Approved":"Rejected"} a client-proposed Campaign Guide edit — ${request.campaign_display_name}`);await loadCampaignGuide()}async function sfLoadFromCampaignGuide(formId,campaignNormName,fieldsContainer,msgEl){const guide=campaignGuides[campaignNormName];if(!guide){msgEl.textContent="Pick a campaign first.";msgEl.className="msg sf-form-guide-msg error";return}if(!confirm(`Add every criteria field from "${guide.displayName}"’s Campaign Guide to this form, marked required? Fields already on this form (matched by label) are left as-is; duplicates are skipped.`))return;const existing=sfFormFieldsCache[formId]||[];const existingNormLabels=new Set(existing.map(f=>csubNormalizeLabel(f.label)));const existingKeys=new Set(existing.map(f=>f.field_key));let sortOrder=existing.reduce((m,f)=>Math.max(m,f.sort_order),-10)+10;let added=0,skipped=0;for(const gf of guide.fields){if(existingNormLabels.has(gf.normLabel)){skipped++;continue}let key=csubSlugifyFieldKey(gf.label);let suffix=1;while(existingKeys.has(key))key=`${csubSlugifyFieldKey(gf.label)}_${++suffix}`;const{error}=await sb.from("lead_submission_form_fields").insert({form_id:formId,field_key:key,label:gf.label,field_type:"text",required:true,sort_order:sortOrder});sortOrder+=10;if(!error){added++;existingKeys.add(key);existingNormLabels.add(gf.normLabel)}else skipped++}logActivity("lead_submission_form_field.bulk_load_campaign_guide",null,null,`Loaded the "${guide.displayName}" Campaign Guide field set onto a submission form (${added} added, ${skipped} skipped)`);msgEl.textContent=`Added ${added} field(s)${skipped?`, skipped ${skipped} (already present or failed)`:""}.`;msgEl.className="msg";await loadSfFormsAndFields();renderSfFormFieldsList(formId,fieldsContainer);renderFillFormOptions();renderFillFields()}let campaignGuideFilterText="";let campaignGuideEditDrafts={};function renderCampaignGuideList(){const listEl=$("campaignGuideList");if(!listEl)return;let entries=Object.entries(campaignGuides).map(([normName,g])=>({normName,...g})).sort((a,b)=>a.displayName.localeCompare(b.displayName));if(campaignGuideFilterText){entries=entries.filter(g=>g.displayName.toLowerCase().includes(campaignGuideFilterText.toLowerCase()))}if(!entries.length){listEl.innerHTML=campaignGuideFilterText?'<div class="empty">No campaigns match that search.</div>':'<div class="empty">No Client Qualification Form submissions with a Campaign Name yet — once a client submits one, its campaign shows up here automatically.</div>';return}listEl.innerHTML="";entries.forEach(g=>listEl.appendChild(buildCampaignGuideCard(g)));applyCsubSectionEditGating("campaign_guide");applyOwnerAdminExportGating(listEl)}function cgOtherIndustrySource(g){const pick=needle=>{const f=(g.fields||[]).find(x=>csubNormalizeLabel(x.label).indexOf(csubNormalizeLabel(needle))===0);return f?String(f.value==null?"":f.value).trim():""};const industry=pick("Industry");const dataCriteria=pick("Data Criteria");const marketCoverage=pick("Market Coverage");if(!industry&&!dataCriteria&&!marketCoverage)return null;return{industry,dataCriteria,marketCoverage}}function buildCampaignGuideCard(g){const card=document.createElement("div");card.className="card";card.style.marginBottom="12px";const editing=!!campaignGuideEditDrafts[g.normName];if(editing){const draft=campaignGuideEditDrafts[g.normName];const header2=document.createElement("div");header2.style.cssText="display:flex; align-items:center; gap:10px; margin-bottom:10px; flex-wrap:wrap;";header2.innerHTML=`
      <span class="mono" style="font-size:13px; font-weight:700; flex:1;">${escapeHtml(g.displayName)}</span>
      <span style="font-size:10px; color:var(--text-dim);">Editing</span>
    `;card.appendChild(header2);const fieldsWrap=document.createElement("div");draft.fields.forEach((f,i)=>{const row=document.createElement("div");row.style.cssText="display:flex; gap:8px; padding:5px 0; border-top:1px solid var(--border); align-items:center; flex-wrap:wrap;";row.innerHTML=`
        <input type="text" class="cg-field-label" value="${escapeHtml(f.label)}" placeholder="Criterion label" style="flex:1; min-width:180px; padding:5px 8px; font-size:11px;">
        <input type="text" class="cg-field-value" value="${escapeHtml(String(f.value==null?"":f.value))}" placeholder="Expected value" style="flex:1; min-width:180px; padding:5px 8px; font-size:11px;">
        <button class="cg-field-del-btn" title="Remove criterion" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:14px;">×</button>
      `;row.querySelector(".cg-field-label").addEventListener("input",e=>{draft.fields[i].label=e.target.value});row.querySelector(".cg-field-value").addEventListener("input",e=>{draft.fields[i].value=e.target.value});row.querySelector(".cg-field-del-btn").addEventListener("click",()=>{draft.fields.splice(i,1);renderCampaignGuideList()});fieldsWrap.appendChild(row)});if(!draft.fields.length){const empty=document.createElement("div");empty.className="empty";empty.style.padding="8px 0";empty.textContent="No criteria yet — add one below.";fieldsWrap.appendChild(empty)}card.appendChild(fieldsWrap);const addRow=document.createElement("div");addRow.style.cssText="margin-top:10px;";addRow.innerHTML=`<button class="btn-secondary cg-add-field-btn" style="width:auto; padding:6px 14px; font-size:11px;">+ Add criterion</button>`;addRow.querySelector(".cg-add-field-btn").addEventListener("click",()=>{draft.fields.push({label:"",value:""});renderCampaignGuideList()});card.appendChild(addRow);const msg=document.createElement("div");msg.className="msg";card.appendChild(msg);const actions=document.createElement("div");actions.style.cssText="display:flex; gap:8px; margin-top:10px; justify-content:flex-end;";actions.innerHTML=`
      <button class="btn-secondary cg-cancel-btn" style="width:auto; padding:8px 16px;">Cancel</button>
      <button class="btn-primary cg-save-btn" style="width:auto; padding:8px 16px; margin-top:0;">Save</button>
    `;actions.querySelector(".cg-cancel-btn").addEventListener("click",()=>{delete campaignGuideEditDrafts[g.normName];renderCampaignGuideList()});actions.querySelector(".cg-save-btn").addEventListener("click",async()=>{const cleanFields=draft.fields.map(f=>({label:(f.label||"").trim(),value:(f.value==null?"":String(f.value)).trim()})).filter(f=>f.label);if(!cleanFields.length){msg.textContent="Add at least one criterion with a label before saving.";msg.className="msg error";return}const payload={campaign_norm_name:g.normName,campaign_display_name:g.displayName,fields:cleanFields.map(f=>({normLabel:csubNormalizeLabel(f.label),label:f.label,value:f.value})),updated_by:myUserId,updated_at:new Date().toISOString()};const{error}=await sb.from("campaign_guide_overrides").upsert(payload,{onConflict:"campaign_norm_name"});if(error){msg.textContent=error.message;msg.className="msg error";return}logActivity("campaign_guide_override.save",null,null,`Edited the Campaign Guide for "${g.displayName}"`);delete campaignGuideEditDrafts[g.normName];await loadCampaignGuide()});card.appendChild(actions);return card}const header=document.createElement("div");header.style.cssText="display:flex; align-items:center; gap:10px; margin-bottom:10px; flex-wrap:wrap;";header.innerHTML=`
    <span class="mono" style="font-size:13px; font-weight:700; flex:1;">${escapeHtml(g.displayName)}</span>
    ${g.overridden?'<span style="font-size:10px; color:var(--focus);">Manually edited</span>':""}
    ${g.aiDraft?'<span style="font-size:10px; color:var(--away); font-weight:700;">&#9888; AI draft &mdash; not yet reviewed</span>':""}
    <span style="font-size:10px; color:var(--text-dim);">${g.overridden?"Last edited":"From submission"}: ${new Date(g.overridden?g.overrideUpdatedAt:g.submittedAt).toLocaleDateString([],{month:"short",day:"numeric",year:"numeric",...nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{}})}</span>
    <button class="edit-only btn-secondary cg-edit-btn" style="width:auto; padding:6px 14px; font-size:11px;">Edit</button>
    ${cgOtherIndustrySource(g)?'<button class="edit-only btn-secondary cg-ai-btn" style="width:auto; padding:6px 14px; font-size:11px;">'+(g.aiDraft?"Regenerate with AI":"Generate with AI")+"</button>":""}
    ${g.aiDraft?'<button class="edit-only btn-secondary cg-approve-btn" style="width:auto; padding:6px 14px; font-size:11px; color:var(--good); border-color:var(--good);">Approve</button>':""}
    ${g.overridden?'<button class="edit-only owner-admin-export-only btn-secondary cg-reset-btn" style="width:auto; padding:6px 14px; font-size:11px;">Delete (reset to auto)</button>':""}
    ${myIsOwner?'<button class="btn-secondary cg-delete-campaign-btn" style="width:auto; padding:6px 14px; font-size:11px; color:var(--danger); border-color:var(--danger);">Delete campaign</button>':""}
  `;const aiBtn=header.querySelector(".cg-ai-btn");if(aiBtn)aiBtn.addEventListener("click",async()=>{const src=cgOtherIndustrySource(g);if(!src)return;if(g.overridden&&!g.aiDraft&&!confirm(`"${g.displayName}" already has an approved guide. Replacing it with an AI draft puts it back into review until somebody approves it again. Continue?`))return;const label=aiBtn.textContent;aiBtn.disabled=true;aiBtn.textContent="Writing…";const{data,error}=await sb.functions.invoke("campaign-guide-ai-draft",{body:{campaignNormName:g.normName,campaignDisplayName:g.displayName,...src}});aiBtn.disabled=false;aiBtn.textContent=label;if(error||!data||data.ok===false){alert("Could not write the draft: "+(data&&data.error||error&&error.message||"unknown error"));return}await loadCampaignGuide()});const approveBtn=header.querySelector(".cg-approve-btn");if(approveBtn)approveBtn.addEventListener("click",async()=>{approveBtn.disabled=true;const{error}=await sb.from("campaign_guide_overrides").update({ai_draft:false,reviewed_by:myUserId,reviewed_at:new Date().toISOString()}).eq("campaign_norm_name",g.normName);approveBtn.disabled=false;if(error){alert("Could not approve it: "+error.message);return}logActivity("campaign_guide_override.approve",null,null,`Approved the AI-drafted Campaign Guide for "${g.displayName}"`);await loadCampaignGuide()});header.querySelector(".cg-edit-btn").addEventListener("click",()=>{campaignGuideEditDrafts[g.normName]={displayName:g.displayName,fields:g.fields.map(f=>({label:f.label,value:f.value}))};renderCampaignGuideList()});const resetBtn=header.querySelector(".cg-reset-btn");if(resetBtn){resetBtn.addEventListener("click",async()=>{if(!confirm(`Reset "${g.displayName}" back to auto-derived from its Client Qualification Form submission? This discards your manual edits.`))return;await sb.from("campaign_guide_overrides").delete().eq("campaign_norm_name",g.normName);logActivity("campaign_guide_override.delete",null,null,`Reset the Campaign Guide for "${g.displayName}" to auto-derived`);await loadCampaignGuide()})}const deleteCampaignBtn=header.querySelector(".cg-delete-campaign-btn");if(deleteCampaignBtn){deleteCampaignBtn.addEventListener("click",async()=>{const{data:liveSubs}=await sb.from("client_campaign_subscriptions").select("status, is_complimentary").eq("campaign_display_name",g.displayName);const hasLiveStripeSub=(liveSubs||[]).some(s=>!s.is_complimentary&&["active","past_due"].includes(s.status));const stripeWarning=hasLiveStripeSub?"\n\n⚠ This campaign still has a LIVE Stripe subscription. Deleting this record does NOT cancel it — the client will keep being billed for a campaign with no guide/form left behind it. Cancel the subscription first (client's Billing tab, or Finance → Stripe Subscription) unless you mean to leave it running.":"";if(!confirm(`Permanently delete the entire "${g.displayName}" campaign record? This removes its Client Qualification Form / Data Form submission(s) and any manual edits. This can't be undone.${stripeWarning}`))return;const ids=[g.submissionId,g.dataSubmissionId].filter(Boolean);if(ids.length){const{error}=await sb.from("ghl_form_submissions").delete().in("id",ids);if(error){alert(error.message);return}}await sb.from("campaign_guide_overrides").delete().eq("campaign_norm_name",g.normName);await sb.from("client_campaign_guide_access").delete().eq("campaign_norm_name",g.normName);const{data:matchingForms}=await sb.from("lead_submission_forms").select("id").eq("name",g.displayName);if(matchingForms&&matchingForms.length){await sb.from("client_campaign_access").delete().in("form_id",matchingForms.map(f=>f.id))}logActivity("campaign_guide.delete",null,null,`Deleted the entire "${g.displayName}" campaign record`);await loadCampaignGuide()})}card.appendChild(header);card.appendChild(buildCampaignGuideDocument(g));return card}function campaignGuideTalkingPoint(f,meta){const v=String(f.value==null?"":f.value).trim();switch(meta.kind){case"geo":return`Confirm the property is located in the target area: ${v}.`;case"listing_status":return/^\s*yes\b/i.test(v)?"Listed properties are accepted here too — no off-market requirement.":"Confirm the property is off-market (not currently listed with a realtor/on the MLS).";case"valid_reason":return/valid\s*reason/i.test(v)&&!/accept\s*(any|leads)/i.test(v)?"Ask why they're considering selling — a valid reason/motivation is the priority; if none is given, keep going as long as they're cooperative and forthcoming with property details.":"Any motivated seller is acceptable here, even with no stated reason for selling.";case"must_capture":return`Ask for their ${meta.checklistText?meta.checklistText.replace(/ has been captured$/i,""):f.label}.`;case"max_days":return`Let the seller know closing can happen within ${v||"the agreed timeframe"}.`;case"price_restriction":return/^none/i.test(v)||!v?null:`Note the price restriction: ${v}.`;case"mv_pricing":return`Ask about pricing expectations relative to market value: ${v}.`;case"referral":return/^\s*yes\b/i.test(v)?`Referrals from outside the primary target area are accepted (${v}).`:null;default:return meta.informational?null:`Confirm: ${f.label} — expected: ${v}.`}}function campaignGuideChecklistLine(f,meta){const v=String(f.value==null?"":f.value).trim();switch(meta.kind){case"geo":return`Property is in the target area (${v})`;case"listing_status":return/^\s*yes\b/i.test(v)?"Listed or off-market properties both acceptable":"Property is off-market (not listed at all)";case"valid_reason":return/valid\s*reason/i.test(v)&&!/accept\s*(any|leads)/i.test(v)?"Seller has a valid reason to sell OR is cooperative with no reason stated":"Any motivated seller is acceptable";case"max_days":return`Closing timeframe is within ${v||"the agreed limit"}`;case"must_capture":return meta.checklistText||`${f.label} has been captured`;case"price_restriction":return/^none/i.test(v)||!v?null:`Meets stated price restriction: ${v}`;default:return meta.informational?null:`${f.label}: ${v}`}}function buildCampaignGuideDocument(g){const wrap=document.createElement("div");if(!g.fields.length){wrap.innerHTML='<div class="empty">No criteria fields yet — click Edit to add some.</div>';return wrap}const bySection={};const ambiguous=[];g.fields.forEach(f=>{const meta=campaignGuideFieldMeta(f.normLabel);(bySection[meta.section]||=[]).push({f,meta});if(campaignGuideFieldIsAmbiguous(meta,f.value))ambiguous.push(f)});const sectionOrder=["target_market","seller_qualification","property_criteria","pricing_terms","referral_policy","other"];const sectionsHtml=sectionOrder.filter(s=>bySection[s]&&bySection[s].length).map(s=>`
    <div style="margin-top:14px;">
      <h4 class="mono" style="font-size:10px; color:var(--text-2); text-transform:uppercase; letter-spacing:0.5px; margin-bottom:6px;">${CAMPAIGN_GUIDE_SECTION_LABELS[s]}</h4>
      ${bySection[s].map(({f,meta})=>`
        <div style="display:flex; gap:16px; padding:5px 0; border-top:1px solid var(--border); flex-wrap:wrap;">
          <span style="min-width:280px; font-size:11px; color:var(--text-dim);">${escapeHtml(f.label)}${meta.informational?' <span style="opacity:0.7;">(reference)</span>':""}</span>
          <span style="font-size:12px; color:var(--text);">${escapeHtml(String(f.value))}</span>
        </div>
      `).join("")}
    </div>
  `).join("");wrap.innerHTML=sectionsHtml;if(ambiguous.length){const warn=document.createElement("div");warn.className="msg error";warn.style.cssText="margin-top:12px;";warn.textContent=`⚠ Needs confirmation with the client: ${ambiguous.map(f=>`"${f.label}" answered "${f.value}" (expected a number)`).join("; ")}. Auto-evaluation for these falls back to manual review / learned keywords until this is clarified.`;wrap.appendChild(warn)}const talkingPoints=g.fields.map(f=>campaignGuideTalkingPoint(f,campaignGuideFieldMeta(f.normLabel))).filter(Boolean);if(talkingPoints.length){const tp=document.createElement("div");tp.style.cssText="margin-top:14px;";tp.innerHTML=`
      <h4 class="mono" style="font-size:10px; color:var(--text-2); text-transform:uppercase; letter-spacing:0.5px; margin-bottom:6px;">Caller Talking Points</h4>
      <ul style="margin:0; padding-left:18px; font-size:12px; color:var(--text); line-height:1.7;">
        ${talkingPoints.map(t=>`<li>${escapeHtml(t)}</li>`).join("")}
      </ul>
    `;wrap.appendChild(tp)}const checklist=g.fields.map(f=>campaignGuideChecklistLine(f,campaignGuideFieldMeta(f.normLabel))).filter(Boolean);if(checklist.length){const cl=document.createElement("div");cl.style.cssText="margin-top:14px;";cl.innerHTML=`
      <h4 class="mono" style="font-size:10px; color:var(--text-2); text-transform:uppercase; letter-spacing:0.5px; margin-bottom:6px;">Quick-Reference Lead Checklist</h4>
      <ul style="margin:0; padding-left:18px; font-size:12px; color:var(--text); line-height:1.7;">
        ${checklist.map(c=>`<li>${escapeHtml(c)}</li>`).join("")}
      </ul>
    `;wrap.appendChild(cl)}return wrap}let disputesCache=[];let disputeFormNameById={};let disputeMessagesCache={};let disputeExpandedId=null;function initClientDisputesTab(){loadDisputesList();applyLeadsSectionEditGating("leads_dispute");disputeLoadQuickReplies()}let disputeQuickReplies=[];async function disputeLoadQuickReplies(){const{data,error}=await sb.from("client_chat_quick_replies").select("id, title, body, sort").eq("kind","dispute").order("sort").order("title");if(error){console.error("dispute quick replies load failed",error);return}disputeQuickReplies=data||[]}function disputeFillPlaceholders(text,dispute){const client=dispute.client_accounts||{};const lead=dispute.leads||{};const first=String(client.full_name||"").trim().split(/\s+/)[0]||"there";return String(text).replaceAll("{client_name}",first).replaceAll("{company}",client.company_name||"your company").replaceAll("{seller}",lead.seller_investor_name||"this seller").replaceAll("{my_name}",String(myFullName||"").trim().split(/\s+/)[0]||"our team")}function disputeRenderQrMenu(menu,input,dispute){const items=disputeQuickReplies.map(q=>`<button type="button" class="ci-qr-item" data-id="${escapeHtml(q.id)}"><span class="ci-qr-title">${escapeHtml(q.title)}</span><span class="ci-qr-body">${escapeHtml(q.body)}</span></button>`).join("");menu.innerHTML=(items||`<div class="ci-list-empty">No dispute replies yet.${ciIsAdmin()?"":" An owner or admin can add them."}</div>`)+(ciIsAdmin()?'<button type="button" class="ci-qr-manage">Manage dispute replies…</button>':"");menu.querySelectorAll(".ci-qr-item").forEach(b=>{b.onclick=e=>{e.stopPropagation();const q=disputeQuickReplies.find(x=>x.id===b.dataset.id);if(!q)return;const text=disputeFillPlaceholders(q.body,dispute);const st=input.selectionStart??input.value.length;const en=input.selectionEnd??input.value.length;input.value=input.value.slice(0,st)+text+input.value.slice(en);menu.classList.add("ci-hide");input.focus();input.selectionStart=input.selectionEnd=st+text.length}});const mb=menu.querySelector(".ci-qr-manage");if(mb)mb.onclick=e=>{e.stopPropagation();menu.classList.add("ci-hide");ciOpenQrModal("dispute")}}document.addEventListener("click",e=>{document.querySelectorAll(".dispute-qr-menu:not(.ci-hide)").forEach(m=>{if(!m.parentElement.contains(e.target))m.classList.add("ci-hide")})});async function loadDisputesList(){const container=$("disputesList");if(container&&!disputesCache.length)container.innerHTML='<div class="empty">Loading…</div>';const statusFilter=$("disputeStatusFilter")?$("disputeStatusFilter").value:"open";const typeFilter=$("disputeTypeFilter")?$("disputeTypeFilter").value:"";const dateFrom=$("disputeDateFromFilter")?$("disputeDateFromFilter").value:"";const dateTo=$("disputeDateToFilter")?$("disputeDateToFilter").value:"";let q=sb.from("lead_disputes").select("id, lead_id, client_id, disputed_fields, status, type, created_at, updated_at, resolved_at, leads(seller_investor_name, seller_phone, submission_form_id), client_accounts(full_name, company_name)").order("created_at",{ascending:false});if(statusFilter)q=q.eq("status",statusFilter);if(typeFilter)q=q.eq("type",typeFilter);if(dateFrom){const from=new Date(dateFrom);from.setHours(0,0,0,0);q=q.gte("created_at",from.toISOString())}if(dateTo){const to=new Date(dateTo);to.setHours(23,59,59,999);q=q.lte("created_at",to.toISOString())}const{data,error}=await q;if(error){if(container)container.innerHTML=`<div class="empty">${escapeHtml(error.message)}</div>`;return}disputesCache=data||[];const formIds=Array.from(new Set(disputesCache.map(d=>d.leads&&d.leads.submission_form_id).filter(Boolean)));if(formIds.length){const{data:forms}=await sb.from("lead_submission_forms").select("id, name").in("id",formIds);disputeFormNameById={};(forms||[]).forEach(f=>{disputeFormNameById[f.id]=f.name})}renderDisputesList()}function renderDisputesList(){const container=$("disputesList");if(!container)return;if($("disputesStatTotal")){$("disputesStatTotal").textContent=disputesCache.length;$("disputesStatDisputes").textContent=disputesCache.filter(d=>d.type!=="feedback").length;$("disputesStatFeedback").textContent=disputesCache.filter(d=>d.type==="feedback").length;$("disputesStatOpen").textContent=disputesCache.filter(d=>d.status==="open").length;$("disputesStatAccepted").textContent=disputesCache.filter(d=>d.status==="accepted").length;$("disputesStatRejected").textContent=disputesCache.filter(d=>d.status==="rejected").length}if(!disputesCache.length){container.innerHTML='<div class="empty">No disputes here.</div>';return}container.innerHTML="";disputesCache.forEach(d=>container.appendChild(buildDisputeCard(d)))}function buildDisputeCard(d){const card=document.createElement("div");card.className="card";card.style.marginBottom="14px";const lead=d.leads||{};const client=d.client_accounts||{};const isFeedback=d.type==="feedback";const statusMeta=isFeedback?{open:{label:"Open",color:"var(--away)"},accepted:{label:"Closed",color:"var(--text-dim)"},rejected:{label:"Closed",color:"var(--text-dim)"}}[d.status]||{label:d.status,color:"var(--text-dim)"}:{open:{label:"Open",color:"var(--away)"},accepted:{label:"Accepted — lead rejected",color:"var(--danger)"},rejected:{label:"Rejected — lead unchanged",color:"var(--text-dim)"}}[d.status]||{label:d.status,color:"var(--text-dim)"};const expanded=disputeExpandedId===d.id;const typeBadge=isFeedback?`<span class="mono" style="font-size:10px; color:var(--focus); background:var(--focus-dim); border-radius:999px; padding:2px 9px; text-transform:uppercase; letter-spacing:.4px;">Feedback</span>`:`<span class="mono" style="font-size:10px; color:var(--danger); background:var(--danger-dim); border-radius:999px; padding:2px 9px; text-transform:uppercase; letter-spacing:.4px;">Dispute</span>`;const header=document.createElement("div");header.style.cssText="display:flex; align-items:center; gap:12px; flex-wrap:wrap;";header.innerHTML=`
    <div style="flex:1; min-width:200px;">
      <div class="mono" style="font-size:13px; font-weight:700;">${escapeHtml(lead.seller_investor_name||"—")} <span style="color:var(--text-dim); font-size:11px; font-weight:400;">${escapeHtml(lead.seller_phone||"")}</span> ${typeBadge}</div>
      <div style="font-size:11px; color:var(--text-dim); margin-top:2px;">${escapeHtml(disputeFormNameById[lead.submission_form_id]||"No campaign")} · ${escapeHtml(client.company_name||client.full_name||"Client")} · Opened ${new Date(d.created_at).toLocaleDateString([],{month:"short",day:"numeric",year:"numeric",...nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{}})}</div>
    </div>
    <span class="mono" style="font-size:10px; color:${statusMeta.color}; text-transform:uppercase; letter-spacing:0.5px;">${statusMeta.label}</span>
    <button class="btn-secondary dispute-toggle-btn" style="width:auto; padding:6px 14px; font-size:11px;">${expanded?"Hide":"Open conversation"}</button>
  `;header.querySelector(".dispute-toggle-btn").addEventListener("click",()=>{disputeExpandedId=expanded?null:d.id;renderDisputesList()});card.appendChild(header);if(!isFeedback){const fields=d.disputed_fields||[];const fieldsWrap=document.createElement("div");fieldsWrap.style.cssText="margin-top:8px; padding-top:8px; border-top:1px solid var(--border);";fieldsWrap.innerHTML=`<div style="font-size:11px; color:var(--text-dim); margin-bottom:4px;">Dispute reason:</div>`+fields.map(f=>`<span class="mono" style="display:inline-block; background:var(--inset); border-radius:4px; padding:3px 8px; font-size:11px; margin:2px 4px 2px 0;">${escapeHtml(f.label||f.key)}</span>`).join("");card.appendChild(fieldsWrap)}if(expanded){const threadWrap=document.createElement("div");threadWrap.style.cssText="margin-top:12px; padding-top:12px; border-top:1px solid var(--border);";threadWrap.innerHTML='<div class="empty">Loading conversation…</div>';card.appendChild(threadWrap);loadDisputeMessages(d.id).then(msgs=>renderDisputeThread(threadWrap,d,msgs))}return card}async function loadDisputeMessages(disputeId){const{data}=await sb.from("lead_dispute_messages").select("id, sender_type, sender_client_id, sender_staff_id, message, created_at").eq("dispute_id",disputeId).order("created_at");disputeMessagesCache[disputeId]=data||[];return disputeMessagesCache[disputeId]}function renderDisputeThread(container,dispute,messages){container.innerHTML="";const list=document.createElement("div");list.style.cssText="display:flex; flex-direction:column; gap:8px; max-height:280px; overflow-y:auto; margin-bottom:10px;";if(!messages.length){list.innerHTML=`<div class="empty">No messages yet — the client's comment when they filed this is the first one, once they've sent it.</div>`}else{messages.forEach(m=>{const mine=m.sender_type==="staff";const bubble=document.createElement("div");bubble.style.cssText=`max-width:80%; align-self:${mine?"flex-end":"flex-start"}; background:${mine?"var(--focus-dim)":"var(--inset)"}; border-radius:10px; padding:8px 12px; font-size:13px;`;bubble.innerHTML=`<div style="font-size:10px; color:var(--text-dim); margin-bottom:3px;">${mine?"Staff":"Client"} · ${new Date(m.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{}})}</div>${escapeHtml(m.message)}`;list.appendChild(bubble)})}container.appendChild(list);if(dispute.status==="open"){const replyRow=document.createElement("div");replyRow.className="edit-only";replyRow.style.cssText="display:flex; gap:8px; margin-bottom:10px;";replyRow.innerHTML=`
      <div class="ci-qr-wrap" style="align-self:flex-end;">
        <button type="button" class="ci-btn dispute-qr-btn" title="Insert a saved reply">Quick replies &#9662;</button>
        <div class="ci-qr-menu ci-hide dispute-qr-menu"></div>
      </div>
      <textarea class="dispute-reply-input" placeholder="Reply to the client…" style="flex:1; min-height:50px; padding:8px; font-size:12px;"></textarea>
      <button class="btn-secondary dispute-reply-btn" style="width:auto; align-self:flex-end; padding:8px 14px;">Send</button>
    `;const qrMenu=replyRow.querySelector(".dispute-qr-menu");replyRow.querySelector(".dispute-qr-btn").addEventListener("click",async e=>{e.stopPropagation();if(!qrMenu.classList.contains("ci-hide")){qrMenu.classList.add("ci-hide");return}if(!disputeQuickReplies.length)await disputeLoadQuickReplies();disputeRenderQrMenu(qrMenu,replyRow.querySelector(".dispute-reply-input"),dispute);qrMenu.classList.remove("ci-hide")});replyRow.querySelector(".dispute-reply-btn").addEventListener("click",async()=>{const btn=replyRow.querySelector(".dispute-reply-btn");if(btn.disabled)return;if(!canEditLeadsSection("leads_dispute")){alert("Your role has view-only access to this section.");return}const textarea=replyRow.querySelector(".dispute-reply-input");const message=textarea.value.trim();if(!message)return;btn.disabled=true;btn.textContent="Sending…";const{error}=await sb.from("lead_dispute_messages").insert({dispute_id:dispute.id,sender_type:"staff",sender_staff_id:myUserId,message});if(error){alert(error.message);btn.disabled=false;btn.textContent="Send";return}textarea.value="";callSendClientSms(dispute.client_id,dispute.type==="feedback"?"feedback_response":"dispute_response",null);logActivity("lead_dispute.reply","lead",dispute.lead_id,"Replied to a lead dispute");const msgs=await loadDisputeMessages(dispute.id);renderDisputeThread(container,dispute,msgs)});container.appendChild(replyRow);const actionsRow=document.createElement("div");actionsRow.className="edit-only";actionsRow.style.cssText="display:flex; gap:8px; justify-content:flex-end; flex-wrap:wrap;";if(dispute.type==="feedback"){actionsRow.innerHTML=`<button class="btn-primary dispute-accept-btn" style="width:auto; padding:8px 16px; margin-top:0;">Close feedback</button>`;actionsRow.querySelector(".dispute-accept-btn").addEventListener("click",()=>resolveDispute(dispute,"rejected"))}else{actionsRow.innerHTML=`
        <button class="btn-secondary dispute-reject-btn" style="width:auto; padding:8px 16px;">Reject dispute (keep lead accepted)</button>
        <button class="btn-primary dispute-accept-btn" style="width:auto; padding:8px 16px; margin-top:0;">Accept dispute (reject lead)</button>
      `;actionsRow.querySelector(".dispute-reject-btn").addEventListener("click",()=>resolveDispute(dispute,"rejected"));actionsRow.querySelector(".dispute-accept-btn").addEventListener("click",()=>resolveDispute(dispute,"accepted"))}container.appendChild(actionsRow)}else{const resolvedNote=document.createElement("div");resolvedNote.className="mono";resolvedNote.style.cssText="font-size:11px; color:var(--text-dim); margin-bottom:8px;";resolvedNote.textContent=dispute.type==="feedback"?"This feedback thread is closed.":dispute.status==="accepted"?"This dispute was accepted — the lead was marked rejected.":"This dispute was rejected — the lead remains accepted, unchanged.";container.appendChild(resolvedNote);const reopenRow=document.createElement("div");reopenRow.className="edit-only";reopenRow.style.cssText="display:flex; justify-content:flex-end;";reopenRow.innerHTML=`<button class="btn-secondary dispute-reopen-btn" style="width:auto; padding:6px 14px; font-size:12px;">Reopen ${dispute.type==="feedback"?"feedback":"dispute"}</button>`;reopenRow.querySelector(".dispute-reopen-btn").addEventListener("click",()=>reopenDispute(dispute,container));container.appendChild(reopenRow)}applyLeadsSectionEditGating("leads_dispute")}async function reopenDispute(dispute,container){if(!canEditLeadsSection("leads_dispute")){alert("Your role has view-only access to this section.");return}const isFeedback=dispute.type==="feedback";const note=!isFeedback&&dispute.status==="accepted"?"\n\nNote: this does NOT change the lead’s status back to accepted — that lead was already marked rejected and stays that way unless recalled separately.":"";if(!confirm(`Reopen this ${isFeedback?"feedback":"dispute"} thread so you can keep replying?${note}`))return;const{error}=await sb.from("lead_disputes").update({status:"open",resolved_at:null,resolved_by:null}).eq("id",dispute.id);if(error){alert(error.message);return}logActivity("lead_dispute.reopen","lead",dispute.lead_id,isFeedback?"Reopened a lead feedback thread":"Reopened a lead dispute");dispute.status="open";dispute.resolved_at=null;dispute.resolved_by=null;const msgs=await loadDisputeMessages(dispute.id);if(container)renderDisputeThread(container,dispute,msgs);await loadDisputesList()}async function resolveDispute(dispute,outcome){if(!canEditLeadsSection("leads_dispute")){alert("Your role has view-only access to this section.");return}const isFeedback=dispute.type==="feedback";const verb=isFeedback?"close this feedback thread":outcome==="accepted"?"accept this dispute and mark the lead REJECTED (it will disappear from the client's portal)":"reject this dispute and keep the lead accepted, unchanged";if(!confirm(`Are you sure you want to ${verb}? This closes the conversation.`))return;const{error:disputeErr}=await sb.from("lead_disputes").update({status:outcome,resolved_at:new Date().toISOString(),resolved_by:myUserId}).eq("id",dispute.id);if(disputeErr){alert(disputeErr.message);return}if(!isFeedback&&outcome==="accepted"){const{error:leadErr}=await sb.from("leads").update({status:"rejected",review_note:"Rejected via client dispute"}).eq("id",dispute.lead_id);if(leadErr)alert(`Dispute marked accepted, but updating the lead failed: ${leadErr.message}`)}logActivity("lead_dispute.resolve","lead",dispute.lead_id,isFeedback?"Closed a lead feedback thread":`${outcome==="accepted"?"Accepted":"Rejected"} a lead dispute`);disputeExpandedId=null;await loadDisputesList();if(leadsFullData.length)loadLeads()}if($("disputeStatusFilter"))$("disputeStatusFilter").addEventListener("change",loadDisputesList);if($("disputeTypeFilter"))$("disputeTypeFilter").addEventListener("change",loadDisputesList);if($("disputeDateFromFilter"))$("disputeDateFromFilter").addEventListener("change",loadDisputesList);if($("disputeDateToFilter"))$("disputeDateToFilter").addEventListener("change",loadDisputesList);function leadsCampaignNameFor(lead){const entries=leadsExtraFieldsEntriesFn(lead.task_id,lead.submission_form_id,lead.extra_fields)||[];const campaignEntry=entries.find(e=>csubNormalizeLabel(e.label)===csubNormalizeLabel("Campaign Name"));if(campaignEntry&&String(campaignEntry.value||"").trim())return String(campaignEntry.value).trim();const formName=leadsFormNameFn(lead.submission_form_id);return formName&&formName!=="No campaign"?formName:""}function campaignGuideFieldPasses(expectedValue,actualValue,normLabel){const norm=v=>String(v==null?"":v).trim().toLowerCase();const exp=norm(expectedValue),act=norm(actualValue);if(!exp)return true;if(!act)return false;if(exp===act)return true;if(/price|timeline|closing|cap|budget/.test(normLabel)){const expNum=parseFloat(((exp.match(/[\d,.]+/)||[])[0]||"").replace(/,/g,""));const actNum=parseFloat(((act.match(/[\d,.]+/)||[])[0]||"").replace(/,/g,""));if(!isNaN(expNum)&&!isNaN(actNum))return actNum<=expNum}return false}const CAMPAIGN_FIT_STOPWORDS=new Set(["the","and","for","are","but","not","you","your","with","have","has","was","were","this","that","from","they","them","their","will","can","all","any","out","into","than","then","also","been","being","who","what","when","where","why","how","get","got","just","like","one","two","per","via","off","yes","no"]);function campaignFitKeywords(text){return Array.from(new Set(String(text||"").toLowerCase().replace(/[^a-z0-9\s]+/g," ").split(/\s+/).filter(w=>w.length>=3&&!CAMPAIGN_FIT_STOPWORDS.has(w)))).slice(0,40)}function leadsFullTextBlob(lead){const entries=leadsExtraFieldsEntriesFn(lead.task_id,lead.submission_form_id,lead.extra_fields)||[];const parts=[lead.seller_investor_name,...entries.map(e=>e.value)];return parts.filter(v=>v!=null&&v!=="").map(v=>String(v)).join(" \n ")}let campaignCriteriaKeywords={};let campaignCriteriaKeywordsLoaded=false;async function loadCampaignCriteriaKeywords(){const{data,error}=await sb.from("campaign_criteria_keywords").select("campaign_norm_name, criterion_norm_label, keyword, meets_count, not_meets_count");if(error){console.error("Failed to load campaign criteria keywords",error);return}const map={};(data||[]).forEach(r=>{((map[r.campaign_norm_name]||={})[r.criterion_norm_label]||=[]).push(r)});campaignCriteriaKeywords=map;campaignCriteriaKeywordsLoaded=true}let leadCriteriaEvaluationsByLeadId={};async function selectInChunks(table,cols,column,ids,orderCol){const chunks=[];for(let i=0;i<ids.length;i+=200)chunks.push(ids.slice(i,i+200));const results=await Promise.all(chunks.map(c=>{let q=sb.from(table).select(cols).in(column,c);if(orderCol)q=q.order(orderCol,{ascending:false});return q.then(r=>r)}));const failed=results.find(r=>r.error);return{data:results.flatMap(r=>r.data||[]),error:failed?failed.error:null}}async function loadLeadCriteriaEvaluations(leadIds){if(!leadIds||!leadIds.length){leadCriteriaEvaluationsByLeadId={};return}const{data,error}=await selectInChunks("lead_criteria_evaluations","lead_id, campaign_norm_name, criterion_norm_label, criterion_label, expected_value, rating, source","lead_id",leadIds);if(error){console.error("Failed to load lead criteria evaluations",error);return}const map={};(data||[]).forEach(r=>{(map[r.lead_id]||={})[r.criterion_norm_label]=r});leadCriteriaEvaluationsByLeadId=map}let leadAiFitEvaluationsByLeadId={};async function loadLeadAiFitEvaluations(leadIds){if(!leadIds||!leadIds.length){leadAiFitEvaluationsByLeadId={};return}const{data,error}=await selectInChunks("lead_ai_fit_evaluations","lead_id, campaign_norm_name, overall, summary, criteria, model, created_at","lead_id",leadIds);if(error){console.error("Failed to load lead AI fit evaluations",error);return}const map={};(data||[]).forEach(r=>{const existing=(map[r.lead_id]||={})[r.campaign_norm_name];if(!existing||new Date(r.created_at)>new Date(existing.created_at)){map[r.lead_id][r.campaign_norm_name]=r}});leadAiFitEvaluationsByLeadId=map}function campaignFieldDirectVerdict(criterion,leadByNorm,guideByNorm){if(!(criterion.normLabel in leadByNorm))return null;const actual=leadByNorm[criterion.normLabel];const meta=campaignGuideFieldMeta(criterion.normLabel);if(meta.kind&&CAMPAIGN_GUIDE_KIND_EVALUATORS[meta.kind]){const ctx={};if(meta.kind==="geo"){const referralVal=guideByNorm&&guideByNorm[csubNormalizeLabel("Do you accept referrals outside of your targeted market? (Criteria)")];ctx.referralOpen=/^\s*yes\b/i.test(String(referralVal||""))}const kindResult=CAMPAIGN_GUIDE_KIND_EVALUATORS[meta.kind](criterion.value,actual,ctx);if(kindResult!=null)return kindResult}if(!String(actual==null?"":actual).trim())return"unclear";return campaignGuideFieldPasses(criterion.value,actual,criterion.normLabel)?"meets":"does_not_meet"}function campaignFieldKeywordVerdict(campaignNormName,criterion,blobKeywords){const learned=campaignCriteriaKeywords[campaignNormName]&&campaignCriteriaKeywords[campaignNormName][criterion.normLabel]||[];let meetsScore=0,notMeetsScore=0;const blobSet=new Set(blobKeywords);learned.forEach(k=>{if(blobSet.has(k.keyword)){meetsScore+=k.meets_count;notMeetsScore+=k.not_meets_count}});if(meetsScore===0&&notMeetsScore===0){const expectedKeywords=new Set(campaignFitKeywords(criterion.value));const overlap=blobKeywords.filter(w=>expectedKeywords.has(w)).length;return overlap>=2?"meets":"unclear"}if(meetsScore===notMeetsScore)return"unclear";return meetsScore>notMeetsScore?"meets":"does_not_meet"}function leadsCriterionVerdict(lead,campaignNormName,criterion,leadByNorm,blobKeywords,guideByNorm){const stored=leadCriteriaEvaluationsByLeadId[lead.id]&&leadCriteriaEvaluationsByLeadId[lead.id][criterion.normLabel];if(stored&&stored.source==="manual")return{rating:stored.rating,source:"manual"};const meta=campaignGuideFieldMeta(criterion.normLabel);if(meta.informational)return{rating:"unclear",source:"informational"};const direct=campaignFieldDirectVerdict(criterion,leadByNorm,guideByNorm);if(direct!=null)return{rating:direct,source:"auto-field"};return{rating:campaignFieldKeywordVerdict(campaignNormName,criterion,blobKeywords),source:"auto-keyword"}}function leadsCampaignFitFor(lead){const campaignName=leadsCampaignNameFor(lead);if(!campaignName)return null;const normName=csubNormalizeLabel(campaignName);const guide=campaignGuides[normName];if(!guide||!guide.fields.length)return null;const entries=leadsExtraFieldsEntriesFn(lead.task_id,lead.submission_form_id,lead.extra_fields)||[];const leadByNorm={};entries.forEach(e=>{leadByNorm[csubNormalizeLabel(e.label)]=e.value});const guideByNorm={};guide.fields.forEach(f=>{guideByNorm[f.normLabel]=f.value});const blobKeywords=campaignFitKeywords(leadsFullTextBlob(lead));const verdicts=guide.fields.map(f=>({...leadsCriterionVerdict(lead,normName,f,leadByNorm,blobKeywords,guideByNorm),label:f.label,normLabel:f.normLabel,expected:f.value}));const evaluated=verdicts.filter(v=>v.rating!=="unclear");if(!evaluated.length)return{fit:null,verdicts,campaignName:guide.displayName,normName};const meetsCount=evaluated.filter(v=>v.rating==="meets").length;const pct=meetsCount/evaluated.length;const fit=pct>=.8?"strong":pct>=.4?"partial":"not_fit";return{fit,verdicts,campaignName:guide.displayName,normName,meetsCount,evaluatedCount:evaluated.length}}function leadsCampaignFitForBadge(lead){const heuristic=leadsCampaignFitFor(lead);if(!heuristic)return heuristic;const aiEval=leadAiFitEvaluationsByLeadId[lead.id]&&leadAiFitEvaluationsByLeadId[lead.id][heuristic.normName];if(!aiEval)return heuristic;const guideByNorm={};heuristic.verdicts.forEach(v=>{guideByNorm[v.normLabel]=v.expected});const aiVerdicts=(aiEval.criteria||[]).map(c=>{const normLabel=csubNormalizeLabel(c.label);const rating=c.verdict==="does_not_meet"?"does_not_meet":c.verdict==="meets"?"meets":"unclear";return{label:c.label,normLabel,expected:guideByNorm[normLabel]!=null?guideByNorm[normLabel]:null,rating,source:"ai",reasoning:c.reasoning}});const aiEvaluated=aiVerdicts.filter(v=>v.rating!=="unclear");const meetsCount=aiEvaluated.filter(v=>v.rating==="meets").length;return{fit:aiEval.overall,verdicts:aiVerdicts,campaignName:heuristic.campaignName,normName:heuristic.normName,meetsCount,evaluatedCount:aiEvaluated.length,aiSummary:aiEval.summary,aiModel:aiEval.model}}function initClientSubmissionsTab(){if(!$("csubFromDateInput").value){const from=new Date;from.setDate(from.getDate()-30);$("csubFromDateInput").value=from.toISOString().slice(0,10);$("csubToDateInput").value=endOfLoginMonthDateInputValue()}document.querySelectorAll(".csub-subtab-btn").forEach(btn=>{btn.style.display=canViewCsubSection(btn.dataset.csubSection)?"":"none"});const firstVisible=Array.from(document.querySelectorAll(".csub-subtab-btn")).find(b=>b.style.display!=="none");const target=csubActiveSection&&canViewCsubSection(csubActiveSection)?csubActiveSection:firstVisible?firstVisible.dataset.csubSection:null;if(target)activateCsubSection(target)}function activateCsubSection(key){csubActiveSection=key;document.querySelectorAll(".csub-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.csubSection===key));document.querySelectorAll(".csub-section").forEach(s=>{s.style.display=s.id==="csubSection-"+key?"block":"none"});const filterCard=$("csubFormFilterCard");if(filterCard)filterCard.style.display=key==="dashboard"||key==="campaign_guide"||key==="campaign_progress"||key==="readymode_upload_list"||key==="marketing"?"none":"";const onCampaign=key==="campaign";const hideAllFilters=key==="campaign_progress"||key==="readymode_upload_list"||key==="marketing";if($("csubFormFilterWrap"))$("csubFormFilterWrap").style.display=onCampaign||hideAllFilters?"none":"";if($("csubExportCsvWrap"))$("csubExportCsvWrap").style.display=onCampaign||hideAllFilters?"none":"";if($("csubCampaignStatusFilterWrap"))$("csubCampaignStatusFilterWrap").style.display=onCampaign&&!hideAllFilters?"":"none";if($("csubCampaignPackageFilterWrap"))$("csubCampaignPackageFilterWrap").style.display=onCampaign&&!hideAllFilters?"":"none";if($("csubFormFilterHint"))$("csubFormFilterHint").textContent=onCampaign?"Campaign Name filters this list the same way it does Data Form/Client Qualification Form — kept in sync across all three.":"Campaign Name filters both Data Form and Client Qualification Form together — a Cold Caller package submission appears on both, so this keeps them in sync.";if(key!=="marketing")loadClientSubmissions();if(key==="campaign"){loadCsubCampaignSubsForCampaignTab().then(()=>{if(csubActiveSection==="campaign")renderCsubCampaignTable()});loadCsubActiveCampaignForms().then(()=>{if(csubActiveSection==="campaign")renderCsubCampaignTable()});loadCsubCampaignLeadStats().then(()=>{if(csubActiveSection==="campaign")renderCsubCampaignTable()})}else if(key==="campaign_guide"){loadCampaignGuide();initCampaignResourcesSection()}else if(key==="campaign_progress"){loadRmCampaignProgress();if(!(myIsOwner||myRole==="admin"))applyCsubSectionEditGating("campaign_progress")}else if(key==="marketing"){initMarketingStats()}else if(key==="readymode_upload_list"){loadRmLeadPostingChannels();loadRmLeadPushes();if(!(myIsOwner||myRole==="admin"))applyCsubSectionEditGating("readymode_upload_list")}}document.querySelectorAll(".csub-subtab-btn").forEach(btn=>{btn.addEventListener("click",()=>activateCsubSection(btn.dataset.csubSection))});if($("csubRefreshBtn"))$("csubRefreshBtn").addEventListener("click",async()=>{const btn=$("csubRefreshBtn");if(btn.disabled)return;btn.disabled=true;const original=btn.innerHTML;btn.innerHTML="&#8635; Refreshing…";await loadClientSubmissions();btn.innerHTML=original;btn.disabled=false});function refreshCsubSectionByKey(key){if(key==="campaign_guide"){loadCampaignGuide();initCampaignResourcesSection()}else loadClientSubmissions()}document.querySelectorAll(".csub-refresh-btn").forEach(btn=>{btn.addEventListener("click",async()=>{if(btn.disabled)return;btn.disabled=true;const original=btn.innerHTML;btn.innerHTML="&#8635; Refreshing…";await refreshCsubSectionByKey(btn.dataset.refreshKey);btn.innerHTML=original;btn.disabled=false})});function applyCsubSectionEditGating(key,scopeEl){const panel=scopeEl||document.getElementById("csubSection-"+key);if(!panel)return;const editable=canEditCsubSection(key);panel.querySelectorAll(".edit-only").forEach(el=>{el.disabled=!editable;el.style.opacity=editable?"":"0.4";el.title=editable?"":"Your role has view-only access to this section"})}async function loadClientSubmissions(){const from=$("csubFromDateInput").value;const to=$("csubToDateInput").value;let q=sb.from("ghl_form_submissions").select("id, form_id, form_name, contact_name, contact_phone, contact_email, submitted_at, fields",{count:"exact"}).order("submitted_at",{ascending:false}).limit(1e3);if(from)q=q.gte("submitted_at",new Date(from+"T00:00:00").toISOString());if(to)q=q.lte("submitted_at",new Date(to+"T23:59:59.999").toISOString());const{data,error,count}=await q;const warnEl=$("csubTruncatedWarning");if(error){console.error("Failed to load client submissions",error);csubAllRows=[];warnEl.style.display="none"}else{csubAllRows=data||[];if(typeof count==="number"&&count>csubAllRows.length){warnEl.textContent=`Showing the most recent ${csubAllRows.length.toLocaleString()} of ${count.toLocaleString()} submissions in this range — narrow the date range to see the rest.`;warnEl.style.display=""}else{warnEl.style.display="none"}}populateCsubFormFilterOptions();populateCsubCampaignFilterOptions();if(csubActiveSection==="dashboard")renderCsubDashboard();else if(csubActiveSection==="campaign")renderCsubCampaignTable();else renderCsubSectionTable(csubActiveSection)}const CAMPAIGN_SUBTAB_FIELD_LABELS=["Dialer (Criteria)","Data Source (Criteria)","Caller Staffing (Criteria)"];function csubFieldValueByLabel(row,label){const fields=row.fields||{};const key=Object.keys(fields).find(k=>csubNormalizeLabel(k)===csubNormalizeLabel(label));return key?String(fields[key]||"").trim():""}let csubCampaignSubsByNorm={};let csubCampaignClientById={};let csubMarketingByClient={};let csubMarketingLoading=false;async function csubLoadMarketingAccess(ids){const want=[...new Set(ids.filter(Boolean))].filter(id=>!(id in csubMarketingByClient));if(!want.length||csubMarketingLoading)return;csubMarketingLoading=true;try{await Promise.all(want.map(async id=>{const{data,error}=await sb.rpc("marketing_access",{p_client:id});csubMarketingByClient[id]=error||!data?null:data}))}finally{csubMarketingLoading=false}if(csubActiveSection==="campaign")renderCsubCampaignTable()}function csubMarketingCell(clientId){if(!clientId)return'<span style="color:var(--text-dim);">&mdash;</span>';if(!(clientId in csubMarketingByClient))return'<span style="color:var(--text-dim);">Loading&hellip;</span>';const a=csubMarketingByClient[clientId];if(!a)return'<span style="color:var(--text-dim);">&mdash;</span>';const seats=Number(a.seats||0);if(!seats)return'<span style="color:var(--text-dim);">No seat</span>';const paid=Number(a.paid_seats||0),extra=Number(a.extra_seats||0);const origin=paid&&extra?`${paid} with campaign + ${extra} free`:paid?"with campaign":"free (added by staff)";const ready=!!a.setup_complete;return`<span class="mono" style="color:var(--good); font-size:11px;">${seats} seat${seats===1?"":"s"} &middot; ${Number(a.monthly_limit||0).toLocaleString()}/mo</span><div style="font-size:10px; color:var(--text-2);">${escapeHtml(origin)}</div>`+(ready?"":'<div style="font-size:10px; color:var(--away);">setup incomplete</div>')}let csubCampaignRequestByNorm={};let csubCampaignLeadStatsByNorm={};async function fetchAllLeadStatusRows(){const PAGE_SIZE=1e3;const rows=[];for(let from=0;;from+=PAGE_SIZE){const{data,error}=await sb.from("leads").select("submission_form_id, status").range(from,from+PAGE_SIZE-1);if(error)throw error;rows.push(...data||[]);if(!data||data.length<PAGE_SIZE)break}return rows}async function loadCsubCampaignLeadStats(){let forms,formsErr,leadRows,leadsErr;try{[{data:forms,error:formsErr},leadRows]=await Promise.all([sb.from("lead_submission_forms").select("id, name"),fetchAllLeadStatusRows()])}catch(e){leadsErr=e}if(formsErr||leadsErr){console.error("Failed to load campaign lead stats",formsErr||leadsErr);csubCampaignLeadStatsByNorm={};return}const normByFormId={};(forms||[]).forEach(f=>{if(f.name)normByFormId[f.id]=csubNormalizeLabel(f.name)});const stats={};(leadRows||[]).forEach(r=>{const norm=normByFormId[r.submission_form_id];if(!norm)return;const bucket=stats[norm]||={accepted:0,rejected:0,pending:0,total:0};bucket.total++;if(r.status==="accepted")bucket.accepted++;else if(r.status==="rejected")bucket.rejected++;else if(r.status==="pending"||r.status==="call_back")bucket.pending++});csubCampaignLeadStatsByNorm=stats}function csubCampaignAcceptRate(norm){const s=csubCampaignLeadStatsByNorm[norm];if(!s)return null;const decided=s.accepted+s.rejected;return decided>0?s.accepted/decided:null}async function loadCsubCampaignSubsForCampaignTab(){const{data,error}=await sb.from("client_campaign_subscriptions").select("campaign_display_name, client_id, caller_tier, part_time_callers, full_time_callers, dialer_choice, data_choice, created_at, status, current_period_end, is_complimentary, complimentary_expires_at, "+CAMPAIGN_SUB_BUCKET_KEYS.join(", ")).order("created_at",{ascending:false});if(error){console.error("Failed to load campaign subscriptions for Campaign tab",error);csubCampaignSubsByNorm={};return}csubCampaignSubsByNorm={};(data||[]).forEach(s=>{const norm=csubNormalizeLabel(s.campaign_display_name||"");if(norm&&!(norm in csubCampaignSubsByNorm))csubCampaignSubsByNorm[norm]=s});csubCampaignClientById={};const clientIds=Array.from(new Set((data||[]).map(s=>s.client_id).filter(Boolean)));if(clientIds.length){const{data:cl}=await sb.from("client_accounts").select("id, full_name, company_name, email, phone").in("id",clientIds);(cl||[]).forEach(c=>{csubCampaignClientById[c.id]=c})}csubCampaignRequestByNorm={};const{data:reqs}=await sb.from("ghl_form_submissions").select("submitted_at, contact_name, contact_phone, contact_email, fields").order("submitted_at",{ascending:false}).limit(1e3);(reqs||[]).forEach(r=>{const norm=csubNormalizeLabel(csubCampaignNameValue(r));if(norm&&norm in csubCampaignSubsByNorm)csubCampaignRequestByNorm[norm]=r})}function csubCampaignDialerLabel(r){const sub=csubCampaignSubsByNorm[csubNormalizeLabel(csubCampaignNameValue(r))];if(sub){const byTierDialer={};campaignSubCallerGroups(sub).forEach(g=>{const key=`${g.tier}|${g.dialerChoice}`;byTierDialer[key]=(byTierDialer[key]||0)+g.count});const keys=Object.keys(byTierDialer);if(keys.length){return keys.map(key=>{const[tier,dialerChoice]=key.split("|");return`${byTierDialer[key]} × ${tier==="full_time"?"Full-time":"Part-time"} on ${dialerChoice==="ours"?"our dialer":"own dialer"}`}).join(", ")}return sub.dialer_choice==="ours"?"Our dialer (ReadyMode / IQ)":"Client's own dialer"}return csubFieldValueByLabel(r,"Dialer (Criteria)")||"—"}function csubCampaignDataLabel(r){const sub=csubCampaignSubsByNorm[csubNormalizeLabel(csubCampaignNameValue(r))];if(sub){const byTierData={};campaignSubCallerGroups(sub).forEach(g=>{const key=`${g.tier}|${g.dataChoice}`;byTierData[key]=(byTierData[key]||0)+g.count});const keys=Object.keys(byTierData);if(keys.length){return keys.map(key=>{const[tier,dataChoice]=key.split("|");return`${byTierData[key]} × ${tier==="full_time"?"Full-time":"Part-time"} on ${dataChoice==="ours"?"our data":"client's own data"}`}).join(", ")}return sub.data_choice==="ours"?"Our data":"Client's own data"}return csubFieldValueByLabel(r,"Data Source (Criteria)")||"—"}function csubCampaignStaffingLabel(r){const sub=csubCampaignSubsByNorm[csubNormalizeLabel(csubCampaignNameValue(r))];if(sub)return campaignSubTierBreakdown(sub);return csubFieldValueByLabel(r,"Caller Staffing (Criteria)")||"—"}function csubCampaignPackageLabel(r){const sub=csubCampaignSubsByNorm[csubNormalizeLabel(csubCampaignNameValue(r))];if(!sub)return"—";return`${campaignSubTierBreakdown(sub)} · ${sub.is_complimentary?"Complimentary":"Paid"}`}function csubCampaignActiveUntilLabel(r){const sub=csubCampaignSubsByNorm[csubNormalizeLabel(csubCampaignNameValue(r))];if(!sub)return"—";const dateStr=sub.is_complimentary?sub.complimentary_expires_at:sub.current_period_end;const activeUntilTz=nonAgentDisplayTz();return dateStr?new Date(dateStr).toLocaleDateString([],activeUntilTz?{timeZone:activeUntilTz}:{}):"—"}function csubCampaignStatusInfo(r){const sub=csubCampaignSubsByNorm[csubNormalizeLabel(csubCampaignNameValue(r))];if(!sub)return{key:"none",label:"—",color:"var(--text-dim)"};const label=CAMPAIGN_SUB_STAFF_STATUS_LABEL[sub.status]||sub.status;const color=sub.status==="active"?"var(--good)":sub.status==="past_due"||sub.status==="canceled"?"var(--danger)":"var(--text-2)";return{key:sub.status,label,color}}let csubActiveCampaignForms=[];async function loadCsubActiveCampaignForms(){const{data:subs,error:subsErr}=await sb.from("client_campaign_subscriptions").select("form_id");if(subsErr){console.error("Failed to load campaign subscriptions for Campaign tab",subsErr);csubActiveCampaignForms=[];return}const campaignFormIds=Array.from(new Set((subs||[]).map(s=>s.form_id).filter(Boolean)));if(!campaignFormIds.length){csubActiveCampaignForms=[];return}const{data,error}=await sb.from("lead_submission_forms").select("id, name").eq("is_active",true).in("id",campaignFormIds).order("name");if(error){console.error("Failed to load active campaign forms for Campaign tab",error);csubActiveCampaignForms=[];return}csubActiveCampaignForms=data||[]}function renderCsubCampaignTable(){const headEl=$("csubCampaignTableHead");const bodyEl=$("csubCampaignTableBody");if(!bodyEl)return;const requestRows=csubFilteredRowsForSection("qualification").filter(r=>csubCampaignNameValue(r)&&CAMPAIGN_SUBTAB_FIELD_LABELS.some(l=>csubFieldValueByLabel(r,l))).sort((a,b)=>new Date(b.submitted_at)-new Date(a.submitted_at));const namesWithRequests=new Set(requestRows.map(csubCampaignNameValue));const placeholderRows=csubActiveCampaignForms.filter(f=>f.name&&!namesWithRequests.has(f.name)).map(f=>({id:`form-${f.id}`,formId:f.id,submitted_at:null,contact_name:null,contact_phone:null,contact_email:null,fields:{"Campaign Name":f.name},isPlaceholder:true}));const statusFilterVal=$("csubCampaignStatusFilter")?$("csubCampaignStatusFilter").value:"";const packageFilterVal=$("csubCampaignPackageFilter")?$("csubCampaignPackageFilter").value:"";let rows=[...requestRows,...placeholderRows];if(statusFilterVal)rows=rows.filter(r=>csubCampaignStatusInfo(r).key===statusFilterVal);if(packageFilterVal){rows=rows.filter(r=>{const sub=csubCampaignSubsByNorm[csubNormalizeLabel(csubCampaignNameValue(r))];if(!sub)return false;return packageFilterVal==="complimentary"?!!sub.is_complimentary:!sub.is_complimentary})}if(headEl)headEl.innerHTML="<th>Submitted</th><th>Campaign Name</th><th>Client Details</th><th>Dialer</th><th>Data Source</th><th>Caller Staffing</th><th>Package</th><th>Active Until</th><th>Status</th><th>Accepted / Rejected</th><th>Accept Rate</th><th>Email Marketing</th>"+(myIsOwner?"<th>Delete</th>":"");if(!rows.length){bodyEl.innerHTML=`<tr><td colspan="${myIsOwner?13:12}" style="color:var(--text-dim);">No campaigns match this filter.</td></tr>`;return}const csubCampaignTz=nonAgentDisplayTz();csubLoadMarketingAccess(rows.map(r=>(csubCampaignSubsByNorm[csubNormalizeLabel(csubCampaignNameValue(r))]||{}).client_id));bodyEl.innerHTML=rows.map(r=>{const firstReq=r.isPlaceholder?csubCampaignRequestByNorm[csubNormalizeLabel(csubCampaignNameValue(r))]:null;const submittedAt=r.submitted_at||firstReq?.submitted_at;const submitted=submittedAt?new Date(submittedAt).toLocaleString([],{month:"short",day:"numeric",year:"numeric",hour:"2-digit",minute:"2-digit",...csubCampaignTz?{timeZone:csubCampaignTz}:{}}):r.isPlaceholder?"No request submitted":"—";const linkedClient=csubCampaignClientById[(csubCampaignSubsByNorm[csubNormalizeLabel(csubCampaignNameValue(r))]||{}).client_id];const clientDetails=[r.contact_name,r.contact_phone,r.contact_email].filter(Boolean).join(" · ")||(firstReq?[firstReq.contact_name,firstReq.contact_phone,firstReq.contact_email].filter(Boolean).join(" · "):"")||(linkedClient?[linkedClient.full_name||linkedClient.company_name,linkedClient.phone,linkedClient.email].filter(Boolean).join(" · "):"—");const statusInfo=csubCampaignStatusInfo(r);const leadNorm=csubNormalizeLabel(csubCampaignNameValue(r));const leadStats=csubCampaignLeadStatsByNorm[leadNorm];const acceptedRejectedLabel=leadStats?`${leadStats.accepted} / ${leadStats.rejected}`:"—";const acceptRate=csubCampaignAcceptRate(leadNorm);const acceptRateLabel=acceptRate==null?"—":`${Math.round(acceptRate*100)}%`;const acceptRateColor=acceptRate==null?"var(--text-dim)":acceptRate>=.5?"var(--good)":"var(--danger)";const deleteCell=myIsOwner?r.isPlaceholder?`<td><button class="btn-secondary csub-delete-form-btn" data-form-id="${r.formId}" data-name="${escapeHtml(csubCampaignNameValue(r))}" style="width:auto; padding:4px 10px; font-size:10px; color:var(--danger); border-color:var(--danger);">Delete</button></td>`:`<td><button class="btn-secondary csub-delete-btn" data-id="${r.id}" style="width:auto; padding:4px 10px; font-size:10px; color:var(--danger); border-color:var(--danger);">Delete</button></td>`:"";return`<tr${r.isPlaceholder?' style="color:var(--text-dim);"':""}>
      <td class="mono" style="color:var(--text-2);">${submitted}</td>
      <td>${escapeHtml(csubCampaignNameValue(r))}</td>
      <td>${escapeHtml(clientDetails)}</td>
      <td>${escapeHtml(csubCampaignDialerLabel(r))}</td>
      <td>${escapeHtml(csubCampaignDataLabel(r))}</td>
      <td>${escapeHtml(csubCampaignStaffingLabel(r))}</td>
      <td>${escapeHtml(csubCampaignPackageLabel(r))}</td>
      <td class="mono" style="color:var(--text-2);">${escapeHtml(csubCampaignActiveUntilLabel(r))}</td>
      <td><span class="mono" style="color:${statusInfo.color}; font-size:11px;">${escapeHtml(statusInfo.label)}</span></td>
      <td class="mono">${acceptedRejectedLabel}</td>
      <td class="mono" style="color:${acceptRateColor};">${acceptRateLabel}</td>
      <td>${csubMarketingCell((csubCampaignSubsByNorm[csubNormalizeLabel(csubCampaignNameValue(r))]||{}).client_id)}</td>
      ${deleteCell}
    </tr>`}).join("")}if($("csubCampaignStatusFilter"))$("csubCampaignStatusFilter").addEventListener("change",()=>{if(csubActiveSection==="campaign")renderCsubCampaignTable()});if($("csubCampaignPackageFilter"))$("csubCampaignPackageFilter").addEventListener("change",()=>{if(csubActiveSection==="campaign")renderCsubCampaignTable()});let csubListOrders=[];let csubListOrdersSearchQuery="";let csubListOrdersStatusFilterValue="";let csubListOrdersSortKey="created_at";let csubListOrdersSortDir="desc";async function loadCsubListOrders(){const bodyEl=$("csubListOrdersTableBody");const{data,error}=await sb.from("client_list_orders").select("id, client_id, type_of_leads, property_type, target_market, other_details, record_count, price_cents, status, file_name, file_url, file_path, created_at, client_accounts(full_name, email)").in("status",["pending","fulfilled"]).order("created_at",{ascending:false}).limit(200);if(error){console.error("Failed to load list orders",error);if(bodyEl)bodyEl.innerHTML=`<tr><td colspan="8" style="color:var(--danger);">Failed to load: ${escapeHtml(error.message)}</td></tr>`;return}csubListOrders=data||[];renderCsubListOrders()}function csubListOrdersMatchesSearch(o,q){if(!q)return true;const haystack=[o.type_of_leads,o.property_type,o.target_market,o.other_details,o.client_accounts?.full_name,o.client_accounts?.email].map(v=>(v||"").toString().toLowerCase()).join(" ");return haystack.includes(q)}function csubListOrdersSortCompare(a,b){let av=a[csubListOrdersSortKey],bv=b[csubListOrdersSortKey];if(csubListOrdersSortKey==="record_count"){av=Number(av)||0;bv=Number(bv)||0}else{av=av==null?"":String(av);bv=bv==null?"":String(bv)}if(av<bv)return csubListOrdersSortDir==="asc"?-1:1;if(av>bv)return csubListOrdersSortDir==="asc"?1:-1;return 0}function renderCsubListOrdersHead(){const head=$("csubListOrdersHead");if(!head)return;const sortTh=(key,label)=>{const indicator=csubListOrdersSortKey===key?csubListOrdersSortDir==="asc"?" &#9650;":" &#9660;":"";return`<th class="csub-lo-sort-th" data-sort-key="${key}" style="cursor:pointer;">${label}${indicator}</th>`};head.innerHTML=`<tr>
    ${sortTh("created_at","Submitted")}${sortTh("type_of_leads","Type of Leads")}${sortTh("property_type","Property Type")}
    <th>Target Market</th><th>Details</th>${sortTh("record_count","Records")}${sortTh("status","Status")}<th></th>
  </tr>`}function renderCsubListOrders(){const bodyEl=$("csubListOrdersTableBody");if(!bodyEl)return;renderCsubListOrdersHead();const q=csubListOrdersSearchQuery.trim().toLowerCase();const filtered=csubListOrders.filter(o=>csubListOrdersMatchesSearch(o,q)&&(!csubListOrdersStatusFilterValue||o.status===csubListOrdersStatusFilterValue)).sort(csubListOrdersSortCompare);const countEl=$("csubListOrdersResultCount");if(countEl){countEl.textContent=q||csubListOrdersStatusFilterValue?`Showing ${filtered.length.toLocaleString()} of ${csubListOrders.length.toLocaleString()} requests`:""}if(!csubListOrders.length){bodyEl.innerHTML='<tr><td colspan="8" style="color:var(--text-dim);">No list requests yet.</td></tr>';return}if(!filtered.length){bodyEl.innerHTML='<tr><td colspan="8" style="color:var(--text-dim);">No requests match this search.</td></tr>';return}const canFulfill=canEditDataSciSection("list_orders");const listOrdersTz=nonAgentDisplayTz();bodyEl.innerHTML=filtered.map(o=>{const submitted=o.created_at?new Date(o.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...listOrdersTz?{timeZone:listOrdersTz}:{}}):"—";const clientLabel=o.client_accounts?o.client_accounts.full_name||o.client_accounts.email||"":"";const recordsLabel=o.record_count?`${Number(o.record_count).toLocaleString()}${o.price_cents!=null?` ($${(o.price_cents/100).toFixed(2)})`:""}`:"—";const statusHtml=o.status==="fulfilled"?`<span class="mono" style="color:var(--good); text-transform:uppercase; font-size:10px;">Ready</span>`:`<span class="mono" style="color:var(--away); text-transform:uppercase; font-size:10px;">Pending</span>`;let actionHtml="";if(o.status==="fulfilled"&&(o.file_path||o.file_url)){actionHtml+=`<button class="btn-secondary csub-list-order-view-btn" data-order-id="${o.id}" style="width:auto; padding:4px 8px; font-size:10px; margin-right:6px;">View</button>`}if(canFulfill){actionHtml+=`<button class="btn-secondary csub-list-order-upload-btn" data-order-id="${o.id}" style="width:auto; padding:4px 8px; font-size:10px;">${o.status==="fulfilled"?"Replace list":"Upload list"}</button>`}if(o.status!=="fulfilled"){actionHtml+=` <button class="btn-secondary lbm-open-btn owner-admin-export-only" data-order-type="list_order" data-order-id="${o.id}" style="width:auto; padding:4px 8px; font-size:10px;">Match</button>`}if(myIsOwner){actionHtml+=` <button class="btn-secondary csub-list-order-delete-btn" data-order-id="${o.id}" style="width:auto; padding:4px 8px; font-size:10px; color:var(--danger); border-color:var(--danger);">Delete</button>`}return`<tr>
      <td class="mono" style="color:var(--text-2); font-size:11px;">${submitted}${clientLabel?`<br><span style="color:var(--text-dim);">${escapeHtml(clientLabel)}</span>`:""}</td>
      <td>${escapeHtml(o.type_of_leads||"—")}</td>
      <td>${escapeHtml(o.property_type||"—")}</td>
      <td>${escapeHtml(o.target_market||"—")}</td>
      <td style="max-width:220px;">${escapeHtml(o.other_details||"—")}</td>
      <td class="mono" style="font-size:11px;">${escapeHtml(recordsLabel)}</td>
      <td>${statusHtml}</td>
      <td>${actionHtml}</td>
    </tr>`}).join("");applyOwnerAdminExportGating(bodyEl)}$("csubListOrdersSearchInput")?.addEventListener("input",()=>{csubListOrdersSearchQuery=$("csubListOrdersSearchInput").value;renderCsubListOrders()});$("csubListOrdersStatusFilter")?.addEventListener("change",()=>{csubListOrdersStatusFilterValue=$("csubListOrdersStatusFilter").value;renderCsubListOrders()});$("csubListOrdersHead")?.addEventListener("click",e=>{const th=e.target.closest(".csub-lo-sort-th");if(!th)return;const key=th.dataset.sortKey;if(csubListOrdersSortKey===key)csubListOrdersSortDir=csubListOrdersSortDir==="asc"?"desc":"asc";else{csubListOrdersSortKey=key;csubListOrdersSortDir="asc"}renderCsubListOrders()});let csubListOrderUploadTargetId=null;document.addEventListener("click",e=>{const btn=e.target.closest(".csub-list-order-upload-btn");if(!btn)return;csubListOrderUploadTargetId=btn.dataset.orderId;$("csubListOrderUploadInput").click()});document.addEventListener("click",async e=>{const btn=e.target.closest(".csub-list-order-delete-btn");if(!btn||!myIsOwner)return;const order=csubListOrders.find(o=>o.id===btn.dataset.orderId);if(!order)return;const who=order.client_accounts?order.client_accounts.full_name||order.client_accounts.email||"this client":"this client";const what=`${order.target_market||"no market given"}${order.record_count?`, ${Number(order.record_count).toLocaleString()} records`:""}`;if(!confirm(`Delete the list request from ${who} (${what})?${order.status==="fulfilled"?" The client loses access to the delivered list.":""} This can't be undone.`))return;btn.disabled=true;const{data:gone,error}=await sb.from("client_list_orders").delete().eq("id",order.id).select("id");if(error||!gone||!gone.length){alert(error?`Couldn't delete: ${error.message}`:"Nothing was deleted — your role may not be allowed to delete list requests.");btn.disabled=false;return}if(order.file_path)sb.storage.from("client-list-orders").remove([order.file_path]);logActivity("client_list_order.delete","client_list_order",order.id,`Deleted list request (${what})`);loadCsubListOrders();loadOrderReadiness(true)});document.addEventListener("click",async e=>{const btn=e.target.closest(".csub-list-order-view-btn");if(!btn)return;const order=csubListOrders.find(o=>o.id===btn.dataset.orderId);if(!order)return;if(!order.file_path){if(order.file_url)window.open(order.file_url,"_blank","noopener");return}const{data,error}=await sb.storage.from("client-list-orders").createSignedUrl(order.file_path,3600);if(error){if(order.file_url)window.open(order.file_url,"_blank","noopener");else alert("Could not open the file: "+error.message);return}window.open(data.signedUrl,"_blank","noopener")});if($("csubListOrderUploadInput"))$("csubListOrderUploadInput").addEventListener("change",async e=>{const file=e.target.files[0];e.target.value="";if(!file||!csubListOrderUploadTargetId)return;const orderId=csubListOrderUploadTargetId;csubListOrderUploadTargetId=null;const safeName=file.name.replace(/[^a-zA-Z0-9._-]/g,"_");const path=`${orderId}/${Date.now()}-${safeName}`;const{error:uploadErr}=await sb.storage.from("client-list-orders").upload(path,file,{contentType:file.type||"application/octet-stream"});if(uploadErr){alert("Upload failed: "+uploadErr.message);return}const{data:pub}=sb.storage.from("client-list-orders").getPublicUrl(path);const{data:updatedOrder,error:updateErr}=await sb.from("client_list_orders").update({status:"fulfilled",file_name:file.name,file_url:pub.publicUrl,file_path:path,fulfilled_by:myUserId,fulfilled_at:new Date().toISOString()}).eq("id",orderId).select("client_id").maybeSingle();if(updateErr){alert("File uploaded, but marking the order ready failed: "+updateErr.message);return}if(updatedOrder&&updatedOrder.client_id)callSendClientSms(updatedOrder.client_id,"list_request",null);loadCsubListOrders()});function populateCsubFormFilterOptions(){const sel=$("csubFormFilter");const prev=sel.value;const forms=Array.from(new Set(csubAllRows.map(r=>r.form_name||"(untitled form)"))).sort();sel.innerHTML='<option value="">All forms</option>'+forms.map(f=>`<option value="${escapeHtml(f)}">${escapeHtml(f)}</option>`).join("");sel.value=prev}function csubCampaignNameValue(row){const fields=row.fields||{};const key=Object.keys(fields).find(k=>csubNormalizeLabel(k)===csubNormalizeLabel("Campaign Name"));return key?String(fields[key]||"").trim():""}function populateCsubCampaignFilterOptions(){const sel=$("csubCampaignFilter");if(!sel)return;const prev=sel.value;const campaigns=Array.from(new Set(csubAllRows.map(csubCampaignNameValue).filter(Boolean))).sort();sel.innerHTML='<option value="">All campaigns</option>'+campaigns.map(c=>`<option value="${escapeHtml(c)}">${escapeHtml(c)}</option>`).join("");sel.value=prev}function csubFilteredRows(){const formVal=$("csubFormFilter")?$("csubFormFilter").value:"";const campaignVal=$("csubCampaignFilter")?$("csubCampaignFilter").value:"";let rows=csubAllRows;if(formVal)rows=rows.filter(r=>(r.form_name||"(untitled form)")===formVal);if(campaignVal)rows=rows.filter(r=>csubCampaignNameValue(r)===campaignVal);return rows}function csubFilteredRowsForSection(section){return csubFilteredRows().filter(r=>csubRoutesFor(r).includes(section))}function renderCsubDashboard(){const now=new Date;const weekAgo=new Date(now);weekAgo.setDate(weekAgo.getDate()-7);const monthAgo=new Date(now);monthAgo.setDate(monthAgo.getDate()-30);const total=csubAllRows.length;const thisWeek=csubAllRows.filter(r=>new Date(r.submitted_at)>=weekAgo).length;const thisMonth=csubAllRows.filter(r=>new Date(r.submitted_at)>=monthAgo).length;const formSet=new Set(csubAllRows.map(r=>r.form_name||"(untitled form)"));$("csubStatTotal").textContent=total;$("csubStatWeek").textContent=thisWeek;$("csubStatMonth").textContent=thisMonth;$("csubStatForms").textContent=formSet.size;const byForm={};csubAllRows.forEach(r=>{const name=r.form_name||"(untitled form)";byForm[name]=(byForm[name]||0)+1});const formRows=Object.entries(byForm).sort((a,b)=>b[1]-a[1]);const byFormDiv=$("csubByForm");if(!formRows.length){byFormDiv.innerHTML='<div class="empty">No submissions in this range.</div>'}else{const maxCount=formRows[0][1];byFormDiv.innerHTML=formRows.map(([name,count])=>`
      <div class="site-row">
        <div class="site-domain" title="${escapeHtml(name)}">${escapeHtml(name)}</div>
        <div class="site-bar-track"><div class="site-bar-fill" style="width:${Math.max(2,count/maxCount*100)}%"></div></div>
        <div class="site-time">${count}</div>
      </div>`).join("")}const csubDashboardTz=nonAgentDisplayTz()||FALLBACK_TIMEZONE;const days=[];for(let i=13;i>=0;i--){const d=new Date;d.setDate(d.getDate()-i);days.push(localDateKeyInZone(d,csubDashboardTz))}const byDay={};days.forEach(d=>byDay[d]=0);csubAllRows.forEach(r=>{const d=r.submitted_at?localDateKeyInZone(r.submitted_at,csubDashboardTz):"";if(d in byDay)byDay[d]++});const maxDay=Math.max(1,...Object.values(byDay));$("csubByDay").innerHTML=days.map(d=>{const count=byDay[d];const label=new Date(d+"T00:00:00").toLocaleDateString(void 0,{month:"short",day:"numeric"});return`
      <div class="site-row">
        <div class="site-domain">${label}</div>
        <div class="site-bar-track"><div class="site-bar-fill" style="width:${count/maxDay*100}%"></div></div>
        <div class="site-time">${count}</div>
      </div>`}).join("");const fieldFillCounts={};const fieldValueCounts={};csubAllRows.forEach(r=>{Object.entries(r.fields||{}).forEach(([k,v])=>{if(v===null||v===void 0||v==="")return;fieldFillCounts[k]=(fieldFillCounts[k]||0)+1;const valStr=String(v);fieldValueCounts[k]||={};fieldValueCounts[k][valStr]=(fieldValueCounts[k][valStr]||0)+1})});const topFieldsDiv=$("csubTopFields");const candidateFields=Object.entries(fieldFillCounts).filter(([k,fillCount])=>{const distinctCount=Object.keys(fieldValueCounts[k]).length;return fillCount>=3&&distinctCount<=Math.max(5,fillCount*.5)}).sort((a,b)=>b[1]-a[1]).slice(0,4);if(!candidateFields.length){topFieldsDiv.innerHTML='<div class="empty">Not enough repeated-answer fields yet to show a breakdown — check the Data Form / Client Qualification Form tabs for the raw data.</div>'}else{topFieldsDiv.innerHTML=candidateFields.map(([field,fillCount])=>{const values=Object.entries(fieldValueCounts[field]).sort((a,b)=>b[1]-a[1]).slice(0,6);const maxVal=values[0][1];return`
        <div style="margin-bottom:16px;">
          <div class="mono" style="font-size:11px; color:var(--text-2); text-transform:uppercase; letter-spacing:0.5px; margin-bottom:6px;">${escapeHtml(field)} <span style="color:var(--text-dim); text-transform:none;">(${fillCount} answered)</span></div>
          ${values.map(([val,count])=>`
            <div class="site-row">
              <div class="site-domain" title="${escapeHtml(val)}">${escapeHtml(val)}</div>
              <div class="site-bar-track"><div class="site-bar-fill" style="width:${Math.max(2,count/maxVal*100)}%"></div></div>
              <div class="site-time">${count}</div>
            </div>`).join("")}
        </div>`}).join("")}}const CSUB_DATA_ORDER_DISPLAY_NAME="Data Order Request";const CSUB_SKIPTRACING_DISPLAY_NAME="Skiptracing Request";const CSUB_QUALIFICATION_DISPLAY_NAME="Client Qualification Form";function csubDisplayFormName(row,section){if(section==="qualification")return CSUB_QUALIFICATION_DISPLAY_NAME;return csubServiceValue(row).toLowerCase()==="skiptracing"?CSUB_SKIPTRACING_DISPLAY_NAME:CSUB_DATA_ORDER_DISPLAY_NAME}function applyOwnerAdminExportGating(scopeEl){const scope=scopeEl||document;const allowed=myIsOwner||myRole==="admin";scope.querySelectorAll(".owner-admin-export-only").forEach(el=>{el.style.display=allowed?"":"none"})}function renderCsubSectionTable(section){const rows=csubFilteredRowsForSection(section);const dynamicKeys=Array.from(new Set(rows.flatMap(r=>Object.keys(r.fields||{})))).filter(k=>csubFieldBelongsToSection(k,section)).filter(k=>!CSUB_SERVICE_KEY_PATTERN.test(k)).filter(k=>!CSUB_TABLE_DUPLICATE_FIELDS.has(csubNormalizeLabel(k))).sort();const headEl=section==="data"?$("csubDataFormTableHead"):$("csubQualFormTableHead");const bodyEl=section==="data"?$("csubDataFormTableBody"):$("csubQualFormTableBody");const showPhoneEmail=section!=="qualification";headEl.innerHTML="<th>Submitted</th><th>Form</th><th>Name</th>"+(showPhoneEmail?"<th>Phone</th><th>Email</th>":"")+dynamicKeys.map(k=>`<th>${escapeHtml(k)}</th>`).join("")+(myIsOwner?"<th>Delete</th>":"");const fixedColCount=(showPhoneEmail?5:3)+(myIsOwner?1:0);if(!rows.length){bodyEl.innerHTML=`<tr><td colspan="${fixedColCount+dynamicKeys.length}" style="color:var(--text-dim);">No submissions in this range.</td></tr>`;return}const canEditCsub=myIsOwner||myRole==="admin";bodyEl.innerHTML=rows.map(r=>{const csubTz=globalDisplayTzMode==="agent"?void 0:globalDisplayTzMode;const submitted=r.submitted_at?new Date(r.submitted_at).toLocaleString([],{month:"short",day:"numeric",year:"numeric",hour:"2-digit",minute:"2-digit",...csubTz?{timeZone:csubTz}:{}}):"—";const editableCell=(field,value)=>canEditCsub?`<span class="csub-editable-cell" contenteditable="true" data-id="${r.id}" data-field="${field}" data-original="${escapeHtml(value||"")}">${escapeHtml(value||"")}</span>`:escapeHtml(value||"—");const fixedCells=`
      <td class="mono" style="color:var(--text-2);">${submitted}</td>
      <td>${escapeHtml(csubDisplayFormName(r,section))}</td>
      <td>${editableCell("contact_name",r.contact_name)}</td>`+(showPhoneEmail?`
      <td class="mono">${editableCell("contact_phone",r.contact_phone)}</td>
      <td>${editableCell("contact_email",r.contact_email)}</td>`:"");const dynamicCells=dynamicKeys.map(k=>`<td>${csubFieldValueHtml(k,r.fields?r.fields[k]:null,canEditCsub,r.id)}</td>`).join("");const deleteCell=myIsOwner?`<td><button class="btn-secondary csub-delete-btn" data-id="${r.id}" style="width:auto; padding:4px 10px; font-size:10px; color:var(--danger); border-color:var(--danger);">Delete</button></td>`:"";return`<tr>${fixedCells}${dynamicCells}${deleteCell}</tr>`}).join("");applyOwnerAdminExportGating(bodyEl)}["csubDataFormTableBody","csubQualFormTableBody","csubCampaignTableBody"].forEach(id=>{const el=$(id);if(!el)return;el.addEventListener("click",async e=>{const btn=e.target.closest(".csub-delete-btn");if(!btn||!myIsOwner)return;if(!confirm("Permanently delete this submission? This removes it from Data Form, Client Qualification Form, and Campaign Guide alike. This can’t be undone."))return;btn.disabled=true;const{error}=await sb.from("ghl_form_submissions").delete().eq("id",btn.dataset.id);if(error){alert(error.message);btn.disabled=false;return}logActivity("client_submission.delete","ghl_form_submission",btn.dataset.id,"Deleted a client submission record");csubAllRows=csubAllRows.filter(r=>r.id!==btn.dataset.id);if(id==="csubCampaignTableBody")renderCsubCampaignTable();else if(id==="csubQualFormTableBody")renderCsubSectionTable("qualification");else renderCsubSectionTable("data");if(campaignGuidesLoaded)loadCampaignGuide()})});if($("csubCampaignTableBody"))$("csubCampaignTableBody").addEventListener("click",async e=>{const btn=e.target.closest(".csub-delete-form-btn");if(!btn||!myIsOwner)return;const formId=btn.dataset.formId;const name=btn.dataset.name||"this campaign";if(!confirm(`Delete the "${name}" form? Its fields go with it. Leads already submitted through it keep their data — this only removes it from the picker going forward.`))return;btn.disabled=true;const{error}=await sb.from("lead_submission_forms").delete().eq("id",formId);if(error){alert(`Couldn't delete "${name}": ${error.message}`);btn.disabled=false;return}await sb.from("client_campaign_access").delete().eq("form_id",formId);await sb.from("client_campaign_guide_access").delete().eq("campaign_norm_name",csubNormalizeLabel(name));logActivity("lead_submission_form.delete",null,null,`Deleted submission form "${name}" from Campaigns`);csubActiveCampaignForms=csubActiveCampaignForms.filter(f=>f.id!==formId);renderCsubCampaignTable()});const CSUB_YES_NO=["Yes","No"];const CSUB_DATA_RECORDS_BY_TIER={part_time:7500,full_time:1e4};function csubUpdateCampaignDataHint(){const el=$("csubNewCampaignDataHint");if(!el)return;const tier=($("csubNewCampaignTier")||{}).value||"full_time";const data=($("csubNewCampaignData")||{}).value||"own";const records=CSUB_DATA_RECORDS_BY_TIER[tier]||CSUB_DATA_RECORDS_BY_TIER.full_time;if(data!=="ours"){el.innerHTML=`The client supplies roughly <b>${records.toLocaleString()}</b> records a month for the caller to work.`;return}const seat=tier==="full_time"?" &middot; includes <b>Email Marketing</b>: 1 seat, 10,000 emails a month, free":" &middot; no Email Marketing seat (Full-time on our data only)";el.innerHTML=`We supply <b>${records.toLocaleString()}</b> records a month${seat}.`}let csubScriptUpload=null;async function csubEnsureScriptUploaded(clientId){const input=$("csubNewCampaignScript");const file=input&&input.files&&input.files[0];if(!file)return{path:null,name:null};if(csubScriptUpload&&csubScriptUpload.file===file&&csubScriptUpload.clientId===clientId){return{path:csubScriptUpload.path,name:csubScriptUpload.name}}if(file.size>10*1024*1024)return{error:"That caller script is over 10 MB."};const safeName=file.name.replace(/[^\w.\- ]+/g,"_").slice(-120);const path=`${clientId}/${Date.now()}-${safeName}`;const{error}=await sb.storage.from("campaign-attachments").upload(path,file,{upsert:false,contentType:file.type||"application/octet-stream"});if(error)return{error:"Could not upload the caller script: "+error.message};csubScriptUpload={file,path,name:file.name,clientId};return{path,name:file.name}}function csubSelectedCampaignKind(){const c=document.querySelector('input[name="csubNewCampaignQaRadio-campaignKind"]:checked');return c&&c.value==="Other industry"?"other":"real_estate"}function csubApplyCampaignKind(){const kind=csubSelectedCampaignKind();const aiWrap=document.getElementById("csubNewCampaignAiWrap");if(aiWrap)aiWrap.style.display=kind==="other"?"":"none";document.querySelectorAll("#csubNewCampaignQaFields .csub-qa-field").forEach(el=>{const k=el.dataset.qaKind||"real_estate";el.style.display=k==="both"||k===kind?"":"none"})}const CSUB_NEW_CAMPAIGN_QA_FIELDS=[{key:"campaignKind",label:"Campaign type *",type:"radio",options:["Real estate","Other industry"],kind:"both"},{key:"typeOfLeads",label:"Type of Leads *",type:"checkbox",options:["Absentee Owner","High Equity","Tired Landlord","Pre-Probate (Senior Owners)","Pre-foreclosure","Tax Delinquent","Divorce"]},{key:"propertyType",label:"Property Type *",type:"checkbox",options:["Single Family Home","Multi- Family Home 2-4","Townhouse","Condo","Vacant lots","Mobile Homes","Land","Multi- Family Home 5+ (commercial)","Commercial","Agriculture"]},{key:"targetMarket",label:"Target Market *",type:"textarea"},{key:"validReasonForSelling",label:"Valid reason for selling required? *",type:"radio",options:["Valid Reason and Right Offer","Valid Reason Only"]},{key:"acceptListedProperties",label:"Accept listed properties? *",type:"checkbox",options:["Listed by a Realtor/Owner","Only listed by owner","Listed but the seller promised to remove it","NO LISTED AT ALL"]},{key:"coverRealtorCommission",label:"Cover Realtor commission/fees? *",type:"radio",options:CSUB_YES_NO},{key:"coverClosingFees",label:"Cover closing fees? *",type:"radio",options:CSUB_YES_NO},{key:"askAskingPrice",label:'Ask for "Asking Price"? *',type:"radio",options:CSUB_YES_NO},{key:"aboveOrBelowMarket",label:"Above/below market price accepted? *",type:"radio",options:["Above and below","below only"]},{key:"marketValueSource",label:"Market value source *",type:"checkbox",options:["zillow","redfin","realtor","batch"]},{key:"priceRestrictions",label:"Property price restrictions *",type:"textarea"},{key:"maxClosingTimeline",label:"Max closing timeline *",type:"radio",options:["3 months or less","up to 6 months","up to 1 year"]},{key:"acceptOutsideReferrals",label:"Accept referrals outside target market? *",type:"radio",options:CSUB_YES_NO},{key:"askSellerEmail",label:"Ask for seller's email? *",type:"radio",options:CSUB_YES_NO},{key:"otherDetails",label:"Other details *",type:"textarea"},{key:"industry",label:"Industry *",type:"textarea",kind:"other"},{key:"dataCriteria",label:"Data criteria *",type:"textarea",kind:"other"},{key:"marketCoverage",label:"Market coverage *",type:"textarea",kind:"other"}];function csubNewCampaignOptionRowHtml(inputHtml,text){return`<label style="display:flex; align-items:center; gap:5px; cursor:pointer; text-transform:none; font-family:var(--sans); font-size:12px; color:var(--text-2); font-weight:400;">${inputHtml} ${escapeHtml(text)}</label>`}function csubNewCampaignFieldHtml(f){if(f.type==="checkbox"){return`<div class="csub-qa-field" data-qa-kind="${f.kind||"real_estate"}" style="margin-bottom:10px;">
      <label style="font-size:11px; display:block; margin-bottom:4px;">${escapeHtml(f.label)}</label>
      <div style="display:flex; gap:12px; flex-wrap:wrap;">${f.options.map(o=>csubNewCampaignOptionRowHtml(`<input type="checkbox" class="csub-new-campaign-qa-cb" data-key="${f.key}" value="${escapeHtml(o)}" style="width:13px; height:13px; accent-color:var(--focus);">`,o)).join("")}</div>
    </div>`}if(f.type==="radio"){return`<div class="csub-qa-field" data-qa-kind="${f.kind||"real_estate"}" style="margin-bottom:10px;">
      <label style="font-size:11px; display:block; margin-bottom:4px;">${escapeHtml(f.label)}</label>
      <div style="display:flex; gap:12px; flex-wrap:wrap;">${f.options.map(o=>csubNewCampaignOptionRowHtml(`<input type="radio" name="csubNewCampaignQaRadio-${f.key}" value="${escapeHtml(o)}" style="width:13px; height:13px; accent-color:var(--focus);">`,o)).join("")}</div>
    </div>`}return`<div class="csub-qa-field" data-qa-kind="${f.kind||"real_estate"}" style="margin-bottom:10px;">
    <label style="font-size:11px;">${escapeHtml(f.label)}</label>
    <textarea class="csub-new-campaign-qa-text" data-key="${f.key}" rows="2" style="width:100%;"></textarea>
  </div>`}async function openCsubNewCampaignModal(){const msg=$("csubNewCampaignMsg");msg.style.display="none";$("csubNewCampaignName").value="";$("csubNewCampaignTier").value="part_time";$("csubNewCampaignDialer").value="ours";$("csubNewCampaignData").value="own";$("csubNewCampaignComp").checked=false;$("csubNewCampaignCompExpiry").value="";$("csubNewCampaignCompExpiryRow").style.display="none";$("csubNewCampaignQaFields").innerHTML=CSUB_NEW_CAMPAIGN_QA_FIELDS.map(f=>csubNewCampaignFieldHtml(f)).join("");const kindRadios=document.querySelectorAll('input[name="csubNewCampaignQaRadio-campaignKind"]');const firstKind=document.querySelector('input[name="csubNewCampaignQaRadio-campaignKind"][value="Real estate"]');if(firstKind)firstKind.checked=true;kindRadios.forEach(r=>r.addEventListener("change",csubApplyCampaignKind));const CSUB_AI_DRAFT_MAX=2;let csubAiDraftsUsed=0;function csubAiSyncLeft(){const left=CSUB_AI_DRAFT_MAX-csubAiDraftsUsed;const el=$("csubNewCampaignAiLeft");if(el)el.textContent=left>0?`${left} of ${CSUB_AI_DRAFT_MAX} drafts left.`:"No drafts left — edit it under Leads → Campaign Guide instead.";const b=$("csubNewCampaignAiBtn");if(b&&left<=0)b.disabled=true}const aiBtn=$("csubNewCampaignAiBtn");if(aiBtn)aiBtn.onclick=async()=>{if(csubAiDraftsUsed>=CSUB_AI_DRAFT_MAX)return;const aiMsg=$("csubNewCampaignAiMsg");const clientId=$("csubNewCampaignClient").value;const name=$("csubNewCampaignName").value.trim();const q=k=>{const el=document.querySelector(`.csub-new-campaign-qa-text[data-key="${k}"]`);return el?el.value.trim():""};const industry=q("industry"),dataCriteria=q("dataCriteria"),marketCoverage=q("marketCoverage");if(!clientId||!name||!industry||!dataCriteria||!marketCoverage){aiMsg.style.color="var(--danger)";aiMsg.textContent="Pick a client and fill in the campaign name, industry, data criteria and market coverage first.";return}const scriptPicked=(()=>{const i=$("csubNewCampaignScript");return!!(i&&i.files&&i.files[0])})();if(!scriptPicked&&!confirm("If a caller script is attached, our AI reads it and writes a much sharper guide — it can see how this client already qualifies people.\n\nDraft without a script anyway?"))return;aiBtn.disabled=true;aiMsg.style.color="var(--text-2)";aiMsg.textContent="Uploading the script…";const up=await csubEnsureScriptUploaded(clientId);if(up.error){aiBtn.disabled=false;aiMsg.style.color="var(--danger)";aiMsg.textContent=up.error;return}aiMsg.textContent="Writing the guide…";const{data:data2,error:error2}=await sb.functions.invoke("campaign-guide-ai-draft",{body:{campaignDisplayName:name,industry,dataCriteria,marketCoverage,callerScriptPath:up.path,callerScriptName:up.name}});aiBtn.disabled=false;if(error2||!data2||data2.ok===false){aiMsg.style.color="var(--danger)";aiMsg.textContent=data2&&data2.error||error2&&error2.message||"Could not write the guide.";return}aiMsg.style.color="var(--good)";aiMsg.textContent=`Drafted ${data2.criteria} criteria — review and approve it under Leads → Campaign Guide.`;aiBtn.textContent="Redraft with AI";csubAiDraftsUsed+=1;csubAiSyncLeft()};csubApplyCampaignKind();csubUpdateCampaignDataHint();["csubNewCampaignTier","csubNewCampaignData"].forEach(id=>{const el=$(id);if(el)el.addEventListener("change",csubUpdateCampaignDataHint)});const sel=$("csubNewCampaignClient");sel.innerHTML='<option value="">Loading…</option>';const{data,error}=await sb.from("client_accounts").select("id, full_name, email").eq("is_active",true).order("full_name");if(error){sel.innerHTML='<option value="">Failed to load clients</option>'}else{sel.innerHTML='<option value="">Select a client…</option>'+(data||[]).map(c=>`<option value="${c.id}">${escapeHtml(c.full_name)} (${escapeHtml(c.email)})</option>`).join("")}$("csubNewCampaignModalOverlay").classList.add("active")}function closeCsubNewCampaignModal(){$("csubNewCampaignModalOverlay").classList.remove("active")}if($("csubNewCampaignBtn"))$("csubNewCampaignBtn").addEventListener("click",openCsubNewCampaignModal);if($("csubNewCampaignCancelBtn"))$("csubNewCampaignCancelBtn").addEventListener("click",closeCsubNewCampaignModal);if($("csubNewCampaignComp"))$("csubNewCampaignComp").addEventListener("change",()=>{$("csubNewCampaignCompExpiryRow").style.display=$("csubNewCampaignComp").checked?"block":"none"});if($("csubNewCampaignModalOverlay"))$("csubNewCampaignModalOverlay").addEventListener("click",e=>{if(e.target.id==="csubNewCampaignModalOverlay")closeCsubNewCampaignModal()});if($("csubNewCampaignSubmitBtn"))$("csubNewCampaignSubmitBtn").addEventListener("click",async()=>{const msg=$("csubNewCampaignMsg");msg.style.display="none";const clientId=$("csubNewCampaignClient").value;const campaignName=$("csubNewCampaignName").value.trim();const callerTier=$("csubNewCampaignTier").value;const dialerChoice=$("csubNewCampaignDialer").value;const dataChoice=$("csubNewCampaignData").value;const complimentary=$("csubNewCampaignComp").checked;const complimentaryExpiresAt=$("csubNewCampaignCompExpiry").value;if(!clientId){msg.textContent="Choose a client.";msg.style.display="block";return}if(!campaignName){msg.textContent="Campaign name is required.";msg.style.display="block";return}if(complimentary){if(!complimentaryExpiresAt){msg.textContent="Choose the date this complimentary campaign should stay active until.";msg.style.display="block";return}if(new Date(`${complimentaryExpiresAt}T23:59:59.999Z`).getTime()<=Date.now()){msg.textContent="The active-until date must be in the future.";msg.style.display="block";return}}const qaBody={};const missingQaLabels=[];const campaignKind=csubSelectedCampaignKind();CSUB_NEW_CAMPAIGN_QA_FIELDS.forEach(f=>{const fk=f.kind||"real_estate";if(fk!=="both"&&fk!==campaignKind)return;const cleanLabel=f.label.replace(/\s*\*$/,"");if(f.type==="checkbox"){const vals=Array.from(document.querySelectorAll(`.csub-new-campaign-qa-cb[data-key="${f.key}"]:checked`)).map(cb=>cb.value);if(vals.length)qaBody[f.key]=vals.join(", ");else missingQaLabels.push(cleanLabel)}else if(f.type==="radio"){const checked=document.querySelector(`input[name="csubNewCampaignQaRadio-${f.key}"]:checked`);if(checked)qaBody[f.key]=checked.value;else missingQaLabels.push(cleanLabel)}else{const el=document.querySelector(`.csub-new-campaign-qa-text[data-key="${f.key}"]`);if(el&&el.value.trim())qaBody[f.key]=el.value.trim();else missingQaLabels.push(cleanLabel)}});if(missingQaLabels.length){msg.textContent=`Please fill in: ${missingQaLabels.join(", ")}.`;msg.style.display="block";return}if(complimentary&&!confirm(`Create "${campaignName}" as a COMPLIMENTARY campaign? It goes active immediately for this client — no charge, no Stripe checkout ever — and auto-cancels after ${complimentaryExpiresAt} unless extended.`))return;const btn=$("csubNewCampaignSubmitBtn");btn.disabled=true;btn.textContent="Creating…";qaBody.campaignKind=campaignKind;const up=await csubEnsureScriptUploaded(clientId);if(up.error){msg.textContent=up.error;msg.style.display="block";btn.disabled=false;btn.textContent="Create";return}if(up.path){qaBody.callerScriptPath=up.path;qaBody.callerScriptName=up.name}const{data,error}=await sb.functions.invoke("staff-create-campaign",{body:{clientId,campaignName,callerTier,dialerChoice,dataChoice,complimentary,complimentaryExpiresAt:complimentary?complimentaryExpiresAt:void 0,...qaBody}});btn.disabled=false;btn.textContent="Create";if(error||!data||data.ok===false){let apiMsg=data&&data.error;if(!apiMsg&&error&&error.context&&typeof error.context.json==="function"){try{const b=await error.context.json();apiMsg=b.error}catch(e){}}msg.textContent=apiMsg||error&&error.message||"Failed to create campaign.";msg.style.display="block";return}closeCsubNewCampaignModal();logActivity("campaign.staff_create","client",clientId,`Created ${data.complimentary?"a complimentary":"a pending"} campaign request: ${campaignName}`);if(data.complimentary){const warningText=data.warnings&&data.warnings.length?`

⚠ ${data.warnings.join("\n⚠ ")}`:"";alert(`"${campaignName}" created as a complimentary campaign — active immediately, auto-cancels after ${complimentaryExpiresAt}.${warningText}`);await Promise.all([loadClientSubmissions(),loadCsubCampaignSubsForCampaignTab(),loadCsubActiveCampaignForms(),loadCsubCampaignLeadStats()]);renderCsubCampaignTable()}else{alert(`Pending campaign request created for "${campaignName}". Generate a payment link from Finance → Stripe Pending Payment to send to the client.`)}});async function saveCsubField(el){const id=el.dataset.id;const field=el.dataset.field;const key=el.dataset.key;const newVal=el.textContent.trim();const original=el.dataset.original||"";if(newVal===original)return;let update;if(field){update={[field]:newVal||null}}else if(key){const row2=csubAllRows.find(r=>r.id===id);const fields={...row2&&row2.fields?row2.fields:{}};if(newVal)fields[key]=newVal;else delete fields[key];update={fields}}else{return}const{error}=await sb.from("ghl_form_submissions").update(update).eq("id",id);if(error){alert(error.message);el.textContent=original;return}el.dataset.original=newVal;const row=csubAllRows.find(r=>r.id===id);if(row){if(field)row[field]=newVal||null;else if(key){row.fields={...row.fields||{}};if(newVal)row.fields[key]=newVal;else delete row.fields[key]}}logActivity("client_submission.update","ghl_form_submission",id,`Corrected ${field||key} on a client submission`)}["csubDataFormTableBody","csubQualFormTableBody"].forEach(id=>{const el=$(id);if(el)el.addEventListener("focusout",e=>{const target=e.target.closest(".csub-editable-cell");if(target)saveCsubField(target)})});$("csubExportCsvBtn").addEventListener("click",()=>{if(!(myIsOwner||myRole==="admin"))return;const section=csubActiveSection==="data"||csubActiveSection==="qualification"?csubActiveSection:"data";const rows=csubFilteredRowsForSection(section);const dynamicKeys=Array.from(new Set(rows.flatMap(r=>Object.keys(r.fields||{})))).filter(k=>csubFieldBelongsToSection(k,section)).filter(k=>!CSUB_SERVICE_KEY_PATTERN.test(k)).filter(k=>!CSUB_TABLE_DUPLICATE_FIELDS.has(csubNormalizeLabel(k))).sort();const showPhoneEmail=section!=="qualification";const headers=["Submitted","Form","Name",...showPhoneEmail?["Phone","Email"]:[],...dynamicKeys];const csvEscape=v=>{const s=v==null?"":String(v);return/[",\n]/.test(s)?`"${s.replace(/"/g,'""')}"`:s};const lines=[headers.map(csvEscape).join(",")];rows.forEach(r=>{const line=[r.submitted_at?new Date(r.submitted_at).toISOString():"",csubDisplayFormName(r,section),r.contact_name||"",...showPhoneEmail?[r.contact_phone||"",r.contact_email||""]:[],...dynamicKeys.map(k=>r.fields&&r.fields[k]!=null?String(r.fields[k]):"")];lines.push(line.map(csvEscape).join(","))});const blob=new Blob([lines.join("\n")],{type:"text/csv"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=`client-submissions-${section}-${$("csubFromDateInput").value||"all"}-to-${$("csubToDateInput").value||"all"}.csv`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),3e4)});$("csubFromDateInput").addEventListener("change",loadClientSubmissions);$("csubToDateInput").addEventListener("change",loadClientSubmissions);$("csubFormFilter").addEventListener("change",()=>{if(csubActiveSection==="dashboard")renderCsubDashboard();else renderCsubSectionTable(csubActiveSection)});if($("csubCampaignFilter"))$("csubCampaignFilter").addEventListener("change",()=>{if(csubActiveSection==="dashboard")renderCsubDashboard();else renderCsubSectionTable(csubActiveSection)});let csubSkiptraceOrders=[];let csubSkiptracePollTimer=null;const CSUB_SKIPTRACE_PAGE_SIZE=100;let csubSkiptracePageOffset=0;let csubSkiptraceLastTotal=0;let csubSkiptraceSearchQuery="";let csubSkiptraceStatusFilterValue="";let csubSkiptraceSortBy="created_at";let csubSkiptraceSortDir="desc";const CSUB_SKIPTRACE_SORTABLE_KEYS=new Set(["created_at","updated_at","email","record_count","price_cents","status"]);async function skiptraceInvokeErrorMessage(error,data){if(data&&data.error)return data.error;if(error&&error.context&&typeof error.context.json==="function"){try{const body=await error.context.json();if(body&&body.error)return body.error}catch(e){}}return error&&error.message||"unknown error"}async function loadCsubSkipTrace(offset){if(typeof offset==="number")csubSkiptracePageOffset=Math.max(0,offset);const bodyEl=$("csubSkiptraceTableBody");const{data,error}=await sb.functions.invoke("public-skiptrace",{body:{action:"list_orders",limit:CSUB_SKIPTRACE_PAGE_SIZE,offset:csubSkiptracePageOffset,q:csubSkiptraceSearchQuery||void 0,status:csubSkiptraceStatusFilterValue||void 0,sortBy:csubSkiptraceSortBy,sortDir:csubSkiptraceSortDir}});if(error||!data||data.ok===false){const msg=await skiptraceInvokeErrorMessage(error,data);console.error("Failed to load skip-trace orders",msg);if(bodyEl)bodyEl.innerHTML=`<tr><td colspan="8" style="color:var(--danger);">Failed to load: ${escapeHtml(msg)}</td></tr>`;return}csubSkiptraceOrders=data.orders||[];csubSkiptraceLastTotal=typeof data.total==="number"?data.total:csubSkiptraceOrders.length;renderCsubSkiptraceTable();const pagerEl=$("csubSkiptracePagerControls");if(pagerEl){const pageCount=Math.max(1,Math.ceil(csubSkiptraceLastTotal/CSUB_SKIPTRACE_PAGE_SIZE));const pageNum=Math.floor(csubSkiptracePageOffset/CSUB_SKIPTRACE_PAGE_SIZE)+1;pagerEl.style.display=csubSkiptraceLastTotal>CSUB_SKIPTRACE_PAGE_SIZE?"flex":"none";$("csubSkiptracePageIndicator").textContent=`Page ${pageNum} of ${pageCount} (${csubSkiptraceLastTotal.toLocaleString()} total)`;$("csubSkiptracePrevPageBtn").disabled=csubSkiptracePageOffset<=0;$("csubSkiptraceNextPageBtn").disabled=csubSkiptracePageOffset+CSUB_SKIPTRACE_PAGE_SIZE>=csubSkiptraceLastTotal}loadCsubSkiptraceStats()}$("csubSkiptracePrevPageBtn")?.addEventListener("click",()=>loadCsubSkipTrace(Math.max(0,csubSkiptracePageOffset-CSUB_SKIPTRACE_PAGE_SIZE)));$("csubSkiptraceNextPageBtn")?.addEventListener("click",()=>loadCsubSkipTrace(csubSkiptracePageOffset+CSUB_SKIPTRACE_PAGE_SIZE));$("csubSkiptraceSearchInput")?.addEventListener("input",debounce(()=>{csubSkiptraceSearchQuery=$("csubSkiptraceSearchInput").value.trim();loadCsubSkipTrace(0)},250));$("csubSkiptraceStatusFilter")?.addEventListener("change",()=>{csubSkiptraceStatusFilterValue=$("csubSkiptraceStatusFilter").value;loadCsubSkipTrace(0)});$("csubSkiptraceTableHead")?.addEventListener("click",e=>{const th=e.target.closest(".csub-skiptrace-sort-th");if(!th)return;const key=th.dataset.sortKey;if(!CSUB_SKIPTRACE_SORTABLE_KEYS.has(key))return;if(csubSkiptraceSortBy===key)csubSkiptraceSortDir=csubSkiptraceSortDir==="asc"?"desc":"asc";else{csubSkiptraceSortBy=key;csubSkiptraceSortDir="asc"}loadCsubSkipTrace(0)});async function loadCsubSkiptraceStats(){const{data,error}=await sb.functions.invoke("public-skiptrace",{body:{action:"stats"}});if(error||!data||data.ok===false){console.error("Failed to load skip-trace stats",error,data);return}$("csubSkiptraceStatTotal").textContent=data.total.toLocaleString();$("csubSkiptraceStatProcessing").textContent=data.inProgress.toLocaleString();$("csubSkiptraceStatCompleted").textContent=data.completed.toLocaleString();$("csubSkiptraceStatFailed").textContent=data.needsAttention.toLocaleString();$("csubSkiptraceStatRevenue").textContent="$"+(data.revenueCents/100).toFixed(2)}let skiptraceCreditsClientsCache=[];async function loadSkiptraceCreditsClientOptions(){const sel=$("skiptraceCreditsClientSelect");if(!sel)return;const{data,error}=await sb.from("client_accounts").select("id, full_name, email").eq("is_active",true).order("full_name");if(error){sel.innerHTML=`<option value="">Failed to load clients</option>`;return}skiptraceCreditsClientsCache=data||[];sel.innerHTML=`<option value="">Select a client…</option>`+skiptraceCreditsClientsCache.map(c=>`<option value="${c.id}">${escapeHtml(c.full_name)} (${escapeHtml(c.email)})</option>`).join("")}const STAFF_SKIPTRACE_CENTS_PER_CREDIT=3;const SKIPTRACE_CREDIT_KIND_LABEL={topup:"Stripe top-up",order_charge:"Order charge",refund:"Refund",adjustment:"Manual adjustment"};async function loadSkiptraceCreditsForSelectedClient(){const clientId=$("skiptraceCreditsClientSelect").value;const balanceEl=$("skiptraceCreditsBalance");const ledgerEl=$("skiptraceCreditsLedger");if(!clientId){balanceEl.textContent="—";ledgerEl.innerHTML="";return}balanceEl.textContent="…";const[{data:credits},{data:ledger}]=await Promise.all([sb.from("client_skiptrace_credits").select("balance_cents").eq("client_id",clientId).maybeSingle(),sb.from("client_skiptrace_credit_ledger").select("amount_cents, kind, note, created_at").eq("client_id",clientId).order("created_at",{ascending:false}).limit(8)]);const balanceCents=credits?.balance_cents||0;const balanceCredits=Math.round(balanceCents/STAFF_SKIPTRACE_CENTS_PER_CREDIT);balanceEl.textContent=balanceCredits.toLocaleString()+" credits";ledgerEl.innerHTML=ledger&&ledger.length?'<table style="width:100%; border-collapse:collapse;">'+ledger.map(l=>{const creditsDelta=Math.round(l.amount_cents/STAFF_SKIPTRACE_CENTS_PER_CREDIT);const ledgerTz=nonAgentDisplayTz();return`<tr>
          <td style="padding:3px 6px 3px 0; white-space:nowrap;">${new Date(l.created_at).toLocaleDateString([],ledgerTz?{timeZone:ledgerTz}:{})}</td>
          <td style="padding:3px 6px;">${escapeHtml(SKIPTRACE_CREDIT_KIND_LABEL[l.kind]||l.kind)}</td>
          <td style="padding:3px 6px; font-weight:600; color:${creditsDelta>=0?"var(--good)":"var(--danger)"};">${creditsDelta>=0?"+":""}${creditsDelta}</td>
          <td style="padding:3px 0; color:var(--text-dim);">${escapeHtml(l.note||"")}</td>
        </tr>`}).join("")+"</table>":'<span style="color:var(--text-dim);">No credit history yet.</span>'}if($("skiptraceCreditsClientSelect"))$("skiptraceCreditsClientSelect").addEventListener("change",loadSkiptraceCreditsForSelectedClient);let skiptraceCreditsHistoryRows=[];async function loadSkiptraceCreditsHistory(){const tbody=$("skiptraceCreditsHistoryTbody");if(tbody&&!skiptraceCreditsHistoryRows.length)tbody.innerHTML='<tr><td colspan="5" style="color:var(--text-dim);">Loading…</td></tr>';const{data,error}=await sb.from("client_skiptrace_credit_ledger").select("amount_cents, kind, note, created_at, client_accounts(full_name, email)").order("created_at",{ascending:false}).limit(200);if(error){if(tbody)tbody.innerHTML=`<tr><td colspan="5" style="color:var(--danger);">Failed to load: ${escapeHtml(error.message)}</td></tr>`;return}skiptraceCreditsHistoryRows=data||[];renderSkiptraceCreditsHistory()}function renderSkiptraceCreditsHistory(){const tbody=$("skiptraceCreditsHistoryTbody");if(!tbody)return;if(!skiptraceCreditsHistoryRows.length){tbody.innerHTML='<tr><td colspan="5" style="color:var(--text-dim);">No credit history yet.</td></tr>';return}const historyTz=nonAgentDisplayTz();tbody.innerHTML=skiptraceCreditsHistoryRows.map(l=>{const creditsDelta=Math.round(l.amount_cents/STAFF_SKIPTRACE_CENTS_PER_CREDIT);const clientLabel=l.client_accounts?l.client_accounts.full_name||l.client_accounts.email||"—":"—";return`<tr>
      <td class="mono" style="font-size:11px; white-space:nowrap;">${new Date(l.created_at).toLocaleString([],{month:"short",day:"numeric",year:"numeric",hour:"2-digit",minute:"2-digit",...historyTz?{timeZone:historyTz}:{}})}</td>
      <td>${escapeHtml(clientLabel)}</td>
      <td>${escapeHtml(SKIPTRACE_CREDIT_KIND_LABEL[l.kind]||l.kind)}</td>
      <td class="mono" style="font-weight:600; color:${creditsDelta>=0?"var(--good)":"var(--danger)"};">${creditsDelta>=0?"+":""}${creditsDelta}</td>
      <td style="color:var(--text-dim);">${escapeHtml(l.note||"")}</td>
    </tr>`}).join("")}if($("skiptraceCreditsHistoryRefreshBtn"))$("skiptraceCreditsHistoryRefreshBtn").addEventListener("click",loadSkiptraceCreditsHistory);async function applySkiptraceCreditsAdjustment(sign){const clientId=$("skiptraceCreditsClientSelect").value;const amount=Math.round(Number($("skiptraceCreditsAmount").value));const reason=$("skiptraceCreditsReason").value.trim();const msgEl=$("skiptraceCreditsMsg");msgEl.classList.add("hidden");if(!clientId){msgEl.textContent="Select a client first.";msgEl.className="msg error";msgEl.classList.remove("hidden");return}if(!Number.isFinite(amount)||amount<=0){msgEl.textContent="Enter a positive number of credits.";msgEl.className="msg error";msgEl.classList.remove("hidden");return}if(!reason){msgEl.textContent="A reason is required.";msgEl.className="msg error";msgEl.classList.remove("hidden");return}const deltaCredits=sign*amount;const client=skiptraceCreditsClientsCache.find(c=>c.id===clientId);if(sign<0&&!confirm(`Remove ${amount} credits from ${client?client.full_name:"this client"}? This can't be undone.`))return;const addBtn=$("skiptraceCreditsAddBtn"),removeBtn=$("skiptraceCreditsRemoveBtn");addBtn.disabled=true;removeBtn.disabled=true;const{data,error}=await sb.functions.invoke("public-skiptrace",{body:{action:"staff_adjust_credits",clientId,deltaCredits,reason}});addBtn.disabled=false;removeBtn.disabled=false;if(error||!data||data.ok===false){msgEl.textContent=await skiptraceInvokeErrorMessage(error,data);msgEl.className="msg error";msgEl.classList.remove("hidden");return}msgEl.textContent=data.warning?`Done — ${client?client.full_name:"client"} now has ${data.newBalanceCredits.toLocaleString()} credits. ${data.warning}`:`Done — ${client?client.full_name:"client"} now has ${data.newBalanceCredits.toLocaleString()} credits.`;msgEl.className=data.warning?"msg error":"msg success";msgEl.classList.remove("hidden");logActivity(sign>0?"skiptrace_credits.manual_add":"skiptrace_credits.manual_remove","client_account",clientId,`${sign>0?"Added":"Removed"} ${amount} skip-trace credits ${sign>0?"to":"from"} ${client?client.full_name:clientId} — reason: ${reason}`);$("skiptraceCreditsAmount").value="";$("skiptraceCreditsReason").value="";loadSkiptraceCreditsForSelectedClient();loadSkiptraceCreditsHistory()}if($("skiptraceCreditsAddBtn"))$("skiptraceCreditsAddBtn").addEventListener("click",()=>applySkiptraceCreditsAdjustment(1));if($("skiptraceCreditsRemoveBtn"))$("skiptraceCreditsRemoveBtn").addEventListener("click",()=>applySkiptraceCreditsAdjustment(-1));function csubSkiptraceStatusColor(status){switch(status){case"completed":return"var(--good)";case"processing":return"var(--focus)";case"paid":return"var(--away)";case"failed":return"var(--danger)";default:return"var(--text-dim)"}}function csubSkiptraceSortIndicator(key){if(csubSkiptraceSortBy!==key)return"";return csubSkiptraceSortDir==="asc"?" &#9650;":" &#9660;"}function renderCsubSkiptraceTableHead(){const head=$("csubSkiptraceTableHead");if(!head)return;const sortTh=(key,label)=>`<th class="csub-skiptrace-sort-th" data-sort-key="${key}" style="cursor:pointer;">${label}${csubSkiptraceSortIndicator(key)}</th>`;head.innerHTML=`<tr>
    ${sortTh("created_at","Submitted")}${sortTh("email","Email")}${sortTh("record_count","Records")}${sortTh("price_cents","Price")}${sortTh("status","Status")}${sortTh("updated_at","Updated")}
    <th>Results</th><th>Actions</th>
  </tr>`}function renderCsubSkiptraceTable(){renderCsubSkiptraceTableHead();const bodyEl=$("csubSkiptraceTableBody");if(!csubSkiptraceOrders.length){const filtered=!!(csubSkiptraceSearchQuery||csubSkiptraceStatusFilterValue);bodyEl.innerHTML=`<tr><td colspan="8" style="color:var(--text-dim);">${filtered?"No orders match this search.":"No orders yet."}</td></tr>`;return}const canEditSkiptrace=canEditDataSciSection("skiptrace");const canDelete=myIsOwner||myRole==="admin";const skiptraceTz=nonAgentDisplayTz();bodyEl.innerHTML=csubSkiptraceOrders.map(r=>{const submitted=r.created_at?new Date(r.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...skiptraceTz?{timeZone:skiptraceTz}:{}}):"—";const updated=r.updated_at?new Date(r.updated_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...skiptraceTz?{timeZone:skiptraceTz}:{}}):"—";const price="$"+((r.price_cents||0)/100).toFixed(2);const statusHtml=`<span class="mono" style="color:${csubSkiptraceStatusColor(r.status)}; text-transform:uppercase; font-size:10px;">${escapeHtml(r.status)}</span>`;const resultsHtml=r.status==="completed"?`<span class="mono">${r.results_count??0}</span>`:"—";let actionHtml="";if(canEditSkiptrace){actionHtml+=`<button class="btn-secondary csub-skiptrace-export-btn" data-order-id="${r.id}" style="width:auto; padding:4px 8px; font-size:10px;" title="Download this order's records to upload to Ninja">&#8681; Export</button> `;actionHtml+=`<button class="btn-secondary csub-skiptrace-import-btn" data-order-id="${r.id}" style="width:auto; padding:4px 8px; font-size:10px;" title="Upload the results Ninja sends back for this order">&#8682; Import</button> `;actionHtml+=`<button class="btn-secondary csub-skiptrace-email-btn" data-order-id="${r.id}" style="width:auto; padding:4px 8px; font-size:10px;" title="Resend these results to ${escapeHtml(r.email||"the customer")} (sent automatically once already, on Import)">&#9993; Email</button> `}if(canDelete){actionHtml+=`<button class="btn-secondary csub-skiptrace-delete-btn" data-order-id="${r.id}" style="width:auto; padding:4px 8px; font-size:10px; color:var(--danger);" title="Permanently delete this order">&#128465; Delete</button>`}return`<tr>
      <td class="mono" style="color:var(--text-2); font-size:11px;">${submitted}</td>
      <td style="font-size:11px;">${escapeHtml(r.email||"")}</td>
      <td class="mono">${r.record_count}</td>
      <td class="mono">${price}</td>
      <td>${statusHtml}</td>
      <td class="mono" style="color:var(--text-2); font-size:11px;">${updated}</td>
      <td>${resultsHtml}</td>
      <td style="white-space:nowrap;">${actionHtml}</td>
    </tr>`}).join("")}let csubSkiptracePendingImportOrderId=null;document.addEventListener("click",async e=>{const exportBtn=e.target.closest(".csub-skiptrace-export-btn");if(exportBtn){if(exportBtn.disabled)return;const orderId=exportBtn.dataset.orderId;exportBtn.disabled=true;const original=exportBtn.innerHTML;exportBtn.innerHTML="&#8681; Exporting…";const{data,error}=await sb.functions.invoke("public-skiptrace",{body:{action:"export_paid_csv",orderId}});exportBtn.innerHTML=original;exportBtn.disabled=false;if(error||!data||data.ok===false){alert("Export failed: "+await skiptraceInvokeErrorMessage(error,data));return}if(!data.recordCount){alert("This order has no records to export.");return}const blob=new Blob([data.csv],{type:"text/csv"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=`skiptrace-for-ninja-${orderId}.csv`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),3e4);return}const importBtn=e.target.closest(".csub-skiptrace-import-btn");if(importBtn){csubSkiptracePendingImportOrderId=importBtn.dataset.orderId;$("csubSkiptraceImportFile").click();return}const emailBtn=e.target.closest(".csub-skiptrace-email-btn");if(emailBtn){if(emailBtn.disabled)return;const orderId=emailBtn.dataset.orderId;if(!confirm("Email these skip trace results to the customer now?"))return;emailBtn.disabled=true;const original=emailBtn.innerHTML;emailBtn.innerHTML="&#9993; Sending…";const{data,error}=await sb.functions.invoke("public-skiptrace",{body:{action:"send_result_email",orderId}});emailBtn.innerHTML=original;emailBtn.disabled=false;if(error||!data||data.ok===false){alert("Email failed: "+await skiptraceInvokeErrorMessage(error,data));return}logActivity("skiptrace.email_results","public_skiptrace_order",orderId,"Emailed skip-trace results to the customer");alert("Results emailed.");return}const deleteBtn=e.target.closest(".csub-skiptrace-delete-btn");if(deleteBtn){if(deleteBtn.disabled)return;const orderId=deleteBtn.dataset.orderId;if(!confirm("Permanently delete this skip trace order? This cannot be undone."))return;deleteBtn.disabled=true;const original=deleteBtn.innerHTML;deleteBtn.innerHTML="Deleting…";const{data,error}=await sb.functions.invoke("public-skiptrace",{body:{action:"delete_order",orderId}});if(error||!data||data.ok===false){alert("Delete failed: "+await skiptraceInvokeErrorMessage(error,data));deleteBtn.disabled=false;deleteBtn.innerHTML=original;return}logActivity("skiptrace.delete","public_skiptrace_order",orderId,"Deleted a skip-trace order from the staff dashboard");await loadCsubSkipTrace();return}});if($("csubSkiptraceImportFile"))$("csubSkiptraceImportFile").addEventListener("change",async e=>{const file=e.target.files[0];const orderId=csubSkiptracePendingImportOrderId;csubSkiptracePendingImportOrderId=null;if(!file||!orderId){e.target.value="";return}e.target.value="";const importBtn=document.querySelector(`.csub-skiptrace-import-btn[data-order-id="${orderId}"]`);const importBtnOriginal=importBtn?importBtn.innerHTML:null;if(importBtn){importBtn.disabled=true;importBtn.innerHTML="Uploading…"}const safeName=file.name.replace(/[^a-zA-Z0-9._-]/g,"_");const storagePath=`${orderId}/${Date.now()}-${safeName}`;const{error:uploadErr}=await sb.storage.from("skiptrace-imports").upload(storagePath,file,{contentType:file.type||"text/csv"});if(uploadErr){alert("Import failed to upload: "+uploadErr.message);if(importBtn){importBtn.disabled=false;importBtn.innerHTML=importBtnOriginal}return}if(importBtn)importBtn.innerHTML="Importing…";const{data,error}=await sb.functions.invoke("public-skiptrace",{body:{action:"import_ninja_results",storagePath,orderId}});if(importBtn){importBtn.disabled=false;importBtn.innerHTML=importBtnOriginal}if(error||!data||data.ok===false){alert("Import failed: "+await skiptraceInvokeErrorMessage(error,data));return}logActivity("skiptrace.import_ninja_results","public_skiptrace_order",orderId,`Imported Ninja results for one order: ${data.ordersCompleted} completed, ${data.ordersEmailed||0} emailed, ${data.ordersNoMatch} unmatched`);alert(data.ordersCompleted?data.ordersEmailed?"Order marked completed and results emailed to the customer.":"Order marked completed, but the auto-email failed to send — use the Email button to retry.":"No rows in that file matched this order's addresses — check you uploaded the right file.");if(data.ordersCompleted){const{data:stOrder}=await sb.from("public_skiptrace_orders").select("client_account_id").eq("id",orderId).maybeSingle();if(stOrder&&stOrder.client_account_id){sb.functions.invoke("send-client-sms",{body:{clientId:stOrder.client_account_id,kind:"skiptrace_request",details:null}}).catch(e2=>console.warn("send-client-sms (skiptrace_request) error:",e2))}}await loadCsubSkipTrace()});if($("csubSkiptraceRefreshBtn"))$("csubSkiptraceRefreshBtn").addEventListener("click",async()=>{const btn=$("csubSkiptraceRefreshBtn");if(btn.disabled)return;btn.disabled=true;const original=btn.innerHTML;btn.innerHTML="&#8635; Refreshing…";await loadCsubSkipTrace();btn.innerHTML=original;btn.disabled=false});function startCsubSkiptracePolling(){stopCsubSkiptracePolling();csubSkiptracePollTimer=setInterval(()=>{if(dataSciActiveSection==="skiptrace"&&!document.hidden)loadCsubSkipTrace()},1e4)}function stopCsubSkiptracePolling(){if(csubSkiptracePollTimer){clearInterval(csubSkiptracePollTimer);csubSkiptracePollTimer=null}}function activateIspdSection(key){document.querySelectorAll(".ispd-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.ispdSection===key));document.querySelectorAll(".ispd-section").forEach(s=>{s.style.display=s.id==="ispdSection-"+key?"block":"none"});applyLeadsSectionEditGating("queue_review_ispeedtolead")}document.querySelectorAll(".ispd-subtab-btn").forEach(btn=>{btn.addEventListener("click",()=>activateIspdSection(btn.dataset.ispdSection))});function activateSfSection(key){document.querySelectorAll(".sf-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.sfSection===key));document.querySelectorAll(".sf-section").forEach(s=>{s.style.display=s.id==="sfSection-"+key?"block":"none"});applyLeadsSectionEditGating("submission_forms",$("sfSection-submit"));applyLeadsSectionEditGating("submission_forms_creator",$("sfSection-creator"))}document.querySelectorAll(".sf-subtab-btn").forEach(btn=>{btn.addEventListener("click",()=>activateSfSection(btn.dataset.sfSection))});const LEADS_SELECT_COLS="id, user_id, task_id, submission_form_id, count, submitted_at, status, seller_investor_name, seller_phone, extra_fields, review_note, imported, ticket_stage, desouky_chat_message_id, istl_export_status, istl_export_status_at, manual_delivery_at, manual_delivery_note";const LEADS_PAGE_SIZE=1e3;function leadsPageQuery(from,start,end,withCount){let query=sb.from("leads").select(LEADS_SELECT_COLS,withCount?{count:"exact"}:void 0).order("submitted_at",{ascending:false}).order("id",{ascending:true}).range(from,from+LEADS_PAGE_SIZE-1);if(leadsImportBatchFilter){query=query.eq("last_import_batch",leadsImportBatchFilter)}else{if(start)query=query.gte("submitted_at",start);if(end)query=query.lte("submitted_at",end)}return query.then(r=>r)}async function fetchAllLeadsRows(start,end){const first=await leadsPageQuery(0,start,end,true);if(first.error)return{rows:[],error:first.error};const rows=(first.data||[]).slice();let lastLen=rows.length;let next=LEADS_PAGE_SIZE;if(lastLen===LEADS_PAGE_SIZE&&typeof first.count==="number"&&first.count>next){const froms=[];for(let f=next;f<first.count;f+=LEADS_PAGE_SIZE)froms.push(f);const pages=await Promise.all(froms.map(f=>leadsPageQuery(f,start,end,false)));for(const p of pages){if(p.error)return{rows,error:p.error};rows.push(...p.data||[]);lastLen=(p.data||[]).length}next+=froms.length*LEADS_PAGE_SIZE}while(lastLen===LEADS_PAGE_SIZE){const p=await leadsPageQuery(next,start,end,false);if(p.error)return{rows,error:p.error};rows.push(...p.data||[]);lastLen=(p.data||[]).length;next+=LEADS_PAGE_SIZE}return{rows,error:null}}function leadsNoteSendRow(s){if(!leadsIspeedtoleadSendByLeadId[s.lead_id])leadsIspeedtoleadSendByLeadId[s.lead_id]=s;if(s.channel==="gyhomes"){if(!leadsGyhomesSendByLeadId[s.lead_id])leadsGyhomesSendByLeadId[s.lead_id]=s}else{if(!leadsPrimehomeSendByLeadId[s.lead_id])leadsPrimehomeSendByLeadId[s.lead_id]=s}}let leadsLoadInFlight=null;let leadsLoadQueued=null;let leadsLoadedOnce=false;function loadLeads(){if(leadsLoadInFlight){if(!leadsLoadQueued){leadsLoadQueued=leadsLoadInFlight.catch(()=>{}).then(()=>{leadsLoadQueued=null;return loadLeads()})}return leadsLoadQueued}const p=loadLeadsNow();leadsLoadInFlight=p;const clear=()=>{if(leadsLoadInFlight===p)leadsLoadInFlight=null};p.then(clear,clear);return p}async function loadLeadsNow(){if(!$("leadsFromDateInput").value&&!$("leadsToDateInput").value)restoreLeadsDateRange();if(!$("leadsFromDateInput").value&&!$("leadsToDateInput").value){$("leadsFromDateInput").value=DEFAULT_DATE_RANGE_FROM;$("leadsToDateInput").value=DEFAULT_DATE_RANGE_TO}const pAgents=sb.from("profiles").select("id, full_name, email, role").order("full_name").then(r=>r);const pTasks=sb.from("tasks").select("id, name, category_id").order("name").then(r=>r);const pForms=sb.from("lead_submission_forms").select("id, name").then(r=>r);const pSchedules=sb.from("schedules").select("user_id, timezone").then(r=>r);const pCatFields=sb.from("task_category_fields").select("category_id, field_key, label, field_type, options, required, sort_order").then(r=>r);const pFormFields=sb.from("lead_submission_form_fields").select("form_id, field_key, label, field_type, options, required, sort_order").then(r=>r);const{start,end}=leadsFetchRangeBounds();const pLeads=fetchAllLeadsRows(start,end);const{data:agents}=await pAgents;leadsAllAgents=(agents||[]).filter(a=>!PROTECTED_ROLES.has(a.role));leadsAllProfiles=agents||[];const{data:tasks}=await pTasks;leadsAllTasks=tasks||[];const{data:forms}=await pForms;leadsAllForms=forms||[];const{data:leadsScheduleRows}=await pSchedules;leadsAgentTz={};(leadsScheduleRows||[]).forEach(s=>{if(s.timezone)leadsAgentTz[s.user_id]=s.timezone});const{data:catFieldRows}=await pCatFields;const fieldDefsByCategory={};(catFieldRows||[]).forEach(f=>{(fieldDefsByCategory[f.category_id]||={})[f.field_key]={label:f.label,type:f.field_type||"text",options:f.options||[],required:!!f.required,sort_order:f.sort_order||0}});const categoryIdByTaskId={};leadsAllTasks.forEach(t=>{categoryIdByTaskId[t.id]=t.category_id});const{data:formFieldRows}=await pFormFields;const fieldDefsByForm={};(formFieldRows||[]).forEach(f=>{(fieldDefsByForm[f.form_id]||={})[f.field_key]={label:f.label,type:f.field_type||"text",options:f.options||[],required:!!f.required,sort_order:f.sort_order||0}});function fieldDefsFor(taskId,formId){const catId=categoryIdByTaskId[taskId];return formId&&fieldDefsByForm[formId]||catId&&fieldDefsByCategory[catId]||{}}const globalLabelByKey={};(catFieldRows||[]).forEach(f=>{if(!(f.field_key in globalLabelByKey))globalLabelByKey[f.field_key]=f.label});(formFieldRows||[]).forEach(f=>{if(!(f.field_key in globalLabelByKey))globalLabelByKey[f.field_key]=f.label});const globalFieldDefByKey={};(catFieldRows||[]).forEach(f=>{if(!(f.field_key in globalFieldDefByKey))globalFieldDefByKey[f.field_key]={type:f.field_type||"text",options:f.options||[]}});(formFieldRows||[]).forEach(f=>{if(!(f.field_key in globalFieldDefByKey))globalFieldDefByKey[f.field_key]={type:f.field_type||"text",options:f.options||[]}});const extraFieldsLabel=(taskId,formId,extraFields)=>{if(!extraFields||typeof extraFields!=="object")return"";const keys=Object.keys(extraFields).filter(k=>k!=="recording_url"&&extraFields[k]!==null&&extraFields[k]!=="");if(!keys.length)return"";const defs=fieldDefsFor(taskId,formId);return keys.map(k=>`${defs[k]&&defs[k].label||globalLabelByKey[k]||k}: ${extraFields[k]}`).join(", ")};const extraFieldsEntries=(taskId,formId,extraFields)=>{const ef=extraFields&&typeof extraFields==="object"?extraFields:{};const defs=fieldDefsFor(taskId,formId);const defKeys=Object.keys(defs);const leftoverKeys=Object.keys(ef).filter(k=>k!=="recording_url"&&!defKeys.includes(k)&&ef[k]!==null&&ef[k]!=="");const leftoverEntries=leftoverKeys.map(k=>{const fallbackDef=globalFieldDefByKey[k];return{key:k,label:globalLabelByKey[k]||k,value:ef[k],required:false,type:fallbackDef&&fallbackDef.type||"text",options:fallbackDef&&fallbackDef.options||[]}});if(defKeys.length){return defKeys.filter(k=>k!=="recording_url").sort((a,b)=>(defs[a].sort_order||0)-(defs[b].sort_order||0)).map(k=>({key:k,label:defs[k].label,value:ef[k]!=null&&String(ef[k]).trim()!==""?ef[k]:leadSemanticFallback(ef,k,defs[k].label),required:!!defs[k].required,type:defs[k].type||"text",options:defs[k].options||[]})).concat(leftoverEntries)}const keys=Object.keys(ef).filter(k=>k!=="recording_url"&&ef[k]!==null&&ef[k]!=="");return keys.map(k=>{const fallbackDef=globalFieldDefByKey[k];return{key:k,label:globalLabelByKey[k]||k,value:ef[k],required:false,type:fallbackDef&&fallbackDef.type||"text",options:fallbackDef&&fallbackDef.options||[]}})};const userFilterOptionsHtml='<option value="">All users</option>'+leadsAllAgents.map(a=>`<option value="${a.id}">${escapeHtml(a.full_name||a.email)}</option>`).join("");[$("leadsQueueUserFilter"),$("leadsIspdUserFilter"),$("allQueueUserFilter")].forEach(sel=>{if(!sel)return;const prev=sel.value;sel.innerHTML=userFilterOptionsHtml;sel.value=prev});const label=leadsRangeLabel();let{rows,error:leadsFetchError}=await pLeads;if(leadsFetchError){console.error("Failed to load leads (retrying once):",leadsFetchError);({rows,error:leadsFetchError}=await fetchAllLeadsRows(start,end))}if(leadsFetchError&&leadsLoadedOnce){console.error("Failed to load leads:",leadsFetchError);const keepBanner=$("leadsFetchErrorBanner");if(keepBanner){keepBanner.style.display="block";keepBanner.textContent=`Couldn't reload leads just now (${leadsFetchError.message||"unknown error"}) — still showing the last list. Press Refresh to try again.`}return}if(leadsFetchError)console.error("Failed to load leads:",leadsFetchError);const leadsErrorBanner=$("leadsFetchErrorBanner");if(leadsErrorBanner){leadsErrorBanner.style.display=leadsFetchError?"block":"none";leadsErrorBanner.textContent=leadsFetchError?`Couldn't load leads: ${leadsFetchError.message||"unknown error"}. Try refreshing — if this keeps happening, check the browser console (F12) and Network tab for the /rest/v1/leads request.`:""}const leads=rows;const leadIds=leads.map(r=>r.id);async function fetchInChunks(table,selectCols,column,ids,orderCol){const{data,error}=await selectInChunks(table,selectCols,column,ids,orderCol);if(error)console.error(`Failed to load a chunk of ${table}:`,error);return data}const[postbacks,submissions]=await Promise.all([leadIds.length?fetchInChunks("ispeedtolead_postbacks","matched_lead_id, mapped_status, status_raw, reason, amount, received_at, matched_via","matched_lead_id",leadIds,"received_at"):[],leadIds.length?fetchInChunks("ispeedtolead_submissions","lead_id, success, created_at, error, submitted_by, channel","lead_id",leadIds,"created_at"):[],loadLeadCriteriaEvaluations(leadIds),loadLeadAiFitEvaluations(leadIds)]);const ispeedtoleadByLeadId={};postbacks.forEach(p=>{if(!ispeedtoleadByLeadId[p.matched_lead_id])ispeedtoleadByLeadId[p.matched_lead_id]=p});leadsIspeedtoleadSendByLeadId={};leadsGyhomesSendByLeadId={};leadsPrimehomeSendByLeadId={};submissions.forEach(leadsNoteSendRow);const agentName=id=>{const a=leadsAllProfiles.find(x=>x.id===id);return a?a.full_name||a.email:"—"};const taskName=id=>{const t=leadsAllTasks.find(x=>x.id===id);return t?t.name:"No campaign"};const formName=id=>{const f=leadsAllForms.find(x=>x.id===id);return f?f.name:"No campaign"};const accepted=leads.filter(r=>r.status==="accepted");const pendingCount=leads.filter(r=>r.status==="pending").reduce((t,r)=>t+(r.count||0),0);const rejectedCount=leads.filter(r=>r.status==="rejected").reduce((t,r)=>t+(r.count||0),0);const monthTotal=accepted.reduce((t,r)=>t+(r.count||0),0);let totalLabel=`${label}: ${monthTotal} accepted lead${monthTotal===1?"":"s"}`;if(pendingCount)totalLabel+=`  ·  ${pendingCount} pending review`;if(rejectedCount)totalLabel+=`  ·  ${rejectedCount} rejected`;$("leadsMonthTotalLabel").textContent=totalLabel;const byAgent={};accepted.forEach(r=>{byAgent[r.user_id]=(byAgent[r.user_id]||0)+(r.count||0)});const agentRows=Object.entries(byAgent).sort((a,b)=>b[1]-a[1]);const byAgentDiv=$("leadsByAgent");byAgentDiv.innerHTML="";if(agentRows.length===0){byAgentDiv.innerHTML='<div class="empty">No accepted leads this month.</div>'}else{const maxCount=agentRows[0][1];agentRows.forEach(([userId,count])=>{const row=document.createElement("div");row.className="site-row";row.innerHTML=`
        <div class="site-domain" title="${escapeHtml(agentName(userId))}">${escapeHtml(agentName(userId))}</div>
        <div class="site-bar-track"><div class="site-bar-fill" style="width:${Math.max(2,count/maxCount*100)}%"></div></div>
        <div class="site-time">${count}</div>
      `;byAgentDiv.appendChild(row)})}$("leadsInsightTotal").textContent=leads.length;$("leadsInsightAccepted").textContent=accepted.length;$("leadsInsightPending").textContent=leads.filter(r=>r.status==="pending").length;$("leadsInsightRejected").textContent=leads.filter(r=>r.status==="rejected").length;const statusBreakdownDiv=$("leadsStatusBreakdown");if(statusBreakdownDiv){const statusCounts=[{key:"accepted",label:"Accepted",count:leads.filter(r=>r.status==="accepted").length,color:"var(--good)"},{key:"pending",label:"Pending",count:leads.filter(r=>r.status==="pending").length,color:"var(--away)"},{key:"rejected",label:"Rejected",count:leads.filter(r=>r.status==="rejected").length,color:"var(--danger)"}];const maxStatusCount=Math.max(1,...statusCounts.map(s=>s.count));if(!leads.length){statusBreakdownDiv.innerHTML='<div class="empty">No leads submitted in this range.</div>'}else{statusBreakdownDiv.innerHTML=statusCounts.map(s=>`
        <div class="site-row">
          <div class="site-domain">${s.label}</div>
          <div class="site-bar-track"><div class="site-bar-fill" style="width:${Math.max(2,s.count/maxStatusCount*100)}%; background:${s.color};"></div></div>
          <div class="site-time">${s.count}</div>
        </div>
      `).join("")}}const byDayDiv=$("leadsByDay");if(byDayDiv){const byDay={};leads.forEach(r=>{const key=localDateKeyInZone(r.submitted_at,leadsTzFor(r.user_id)||FALLBACK_TIMEZONE);byDay[key]=(byDay[key]||0)+1});const dayEntries=Object.entries(byDay).sort((a,b)=>a[0].localeCompare(b[0]));if(!dayEntries.length){byDayDiv.innerHTML='<div class="empty">No leads submitted in this range.</div>'}else{const maxDayCount=Math.max(1,...dayEntries.map(([,c])=>c));byDayDiv.innerHTML=dayEntries.map(([day,count])=>`
        <div class="site-row">
          <div class="site-domain">${new Date(day+"T00:00:00").toLocaleDateString([],{month:"short",day:"numeric"})}</div>
          <div class="site-bar-track"><div class="site-bar-fill" style="width:${Math.max(2,count/maxDayCount*100)}%"></div></div>
          <div class="site-time">${count}</div>
        </div>
      `).join("")}}const byCampaignDiv=$("leadsByCampaign");if(byCampaignDiv){const byCampaign={};leads.forEach(r=>{byCampaign[r.submission_form_id||"none"]=(byCampaign[r.submission_form_id||"none"]||0)+1});const campaignRows=Object.entries(byCampaign).sort((a,b)=>b[1]-a[1]).slice(0,12);if(!campaignRows.length){byCampaignDiv.innerHTML='<div class="empty">No leads submitted in this range.</div>'}else{const maxCampaignCount=campaignRows[0][1];byCampaignDiv.innerHTML=campaignRows.map(([formId,count])=>`
        <div class="site-row">
          <div class="site-domain" title="${escapeHtml(formName(formId==="none"?null:formId))}">${escapeHtml(formName(formId==="none"?null:formId))}</div>
          <div class="site-bar-track"><div class="site-bar-fill" style="width:${Math.max(2,count/maxCampaignCount*100)}%"></div></div>
          <div class="site-time">${count}</div>
        </div>
      `).join("")}}leadsFullData=leads;leadsInboundPostbackByLeadId=ispeedtoleadByLeadId;leadsExtraFieldsLabelFn=extraFieldsLabel;leadsExtraFieldsEntriesFn=extraFieldsEntries;leadsAgentNameFn=agentName;leadsTaskNameFn=taskName;leadsFormNameFn=formName;leadsPrimeHomeBuyersFormId=(leadsAllForms.find(f=>f.name===ISPD_DEFAULT_CAMPAIGN_NAME)||{}).id||null;leadsTicketFormId=(leadsAllForms.find(f=>f.name===TICKET_FORM_NAME)||{}).id||null;populateLeadsCampaignFilterOptions();populateLeadsIspdSentByFilterOptions(agentName);renderLeadsQueueTable();renderIspdReviewTable();renderTicketsReviewTable();if(leadsActiveSection==="all_queue")renderAllQueueTable();leadsLoadedOnce=true;subscribeLeadsLive()}let leadsLiveChannel=null;let leadsLiveEverSubscribed=false;let leadsLiveRetryTimer=null;let leadsLiveRetryDelay=2e3;let leadsLiveTimer=null;const leadsLivePending=new Set;function subscribeLeadsLive(){if(leadsLiveChannel||!sb)return;const ch=sb.channel("leads-live-queues");leadsLiveChannel=ch;ch.on("postgres_changes",{event:"*",schema:"public",table:"leads"},payload=>{const id=payload.new&&payload.new.id||payload.old&&payload.old.id;if(!id)return;leadsLivePending.add(id);leadsLiveSchedule()}).subscribe(status=>{if(ch!==leadsLiveChannel)return;if(status==="SUBSCRIBED"){leadsLiveRetryDelay=2e3;if(leadsLiveEverSubscribed&&leadsLoadedOnce)loadLeads();leadsLiveEverSubscribed=true}else if(status==="CHANNEL_ERROR"||status==="TIMED_OUT"){leadsLiveScheduleRetry()}})}function leadsLiveScheduleRetry(){if(leadsLiveRetryTimer)return;const delay=leadsLiveRetryDelay;leadsLiveRetryDelay=Math.min(delay*2,6e4);leadsLiveRetryTimer=setTimeout(()=>{leadsLiveRetryTimer=null;const old=leadsLiveChannel;leadsLiveChannel=null;try{if(old)sb.removeChannel(old)}catch(e){}subscribeLeadsLive()},delay)}function leadsLiveSchedule(){if(!leadsLiveTimer)leadsLiveTimer=setTimeout(leadsLiveFlush,700)}document.addEventListener("visibilitychange",()=>{if(!document.hidden&&leadsLivePending.size)leadsLiveSchedule()});function leadsRowInLoadedWindow(r){const{start,end}=leadsFetchRangeBounds();const t=new Date(r.submitted_at).getTime();if(start&&t<new Date(start).getTime())return false;if(end&&t>new Date(end).getTime())return false;return true}function leadsRowOrder(a,b){const ta=new Date(a.submitted_at).getTime(),tb=new Date(b.submitted_at).getTime();if(ta!==tb)return tb-ta;return a.id<b.id?-1:a.id>b.id?1:0}async function leadsLiveFlush(){leadsLiveTimer=null;if(!leadsLoadedOnce){leadsLivePending.clear();return}const panel=$("tab-leads");if(!panel||!panel.classList.contains("active")){leadsLivePending.clear();return}if(document.hidden)return;const ids=[...leadsLivePending];leadsLivePending.clear();if(!ids.length)return;if(leadsLoadInFlight){try{await leadsLoadInFlight}catch(e){}}if(leadsActiveSection==="dashboard"||leadsImportBatchFilter||ids.length>150){loadLeads();return}const[leadRes,subRes,pbRes]=await Promise.all([selectInChunks("leads",LEADS_SELECT_COLS,"id",ids),selectInChunks("ispeedtolead_submissions","lead_id, success, created_at, error, submitted_by, channel","lead_id",ids,"created_at"),selectInChunks("ispeedtolead_postbacks","matched_lead_id, mapped_status, status_raw, reason, amount, received_at, matched_via","matched_lead_id",ids,"received_at")]);if(leadRes.error||subRes.error||pbRes.error){loadLeads();return}if(leadsLoadInFlight)return;const idSet=new Set(ids);const merged=leadsFullData.filter(r=>!idSet.has(r.id));leadRes.data.forEach(r=>{if(leadsRowInLoadedWindow(r))merged.push(r)});merged.sort(leadsRowOrder);leadsFullData=merged;ids.forEach(id=>{delete leadsIspeedtoleadSendByLeadId[id];delete leadsGyhomesSendByLeadId[id];delete leadsPrimehomeSendByLeadId[id];delete leadsInboundPostbackByLeadId[id]});subRes.data.forEach(leadsNoteSendRow);pbRes.data.forEach(p=>{if(!leadsInboundPostbackByLeadId[p.matched_lead_id])leadsInboundPostbackByLeadId[p.matched_lead_id]=p});renderLeadsQueueTable();renderIspdReviewTable();renderTicketsReviewTable();if(leadsActiveSection==="all_queue")renderAllQueueTable()}const LEAD_PHONE_ONLY_RE=/^\s*\+?1?[\s.-]*\(?\d{3}\)?[\s.-]*\d{3}[\s.-]*\d{4}\s*$/;function leadFieldKind(key,label){const t=`${key||""} ${label||""}`;if(/e-?mail/i.test(t))return"email";if(/address/i.test(t))return"address";return null}function leadFieldProblem(key,label,value){const v=String(value||"").trim();if(!v)return null;const kind=leadFieldKind(key,label);if(kind==="address"&&LEAD_PHONE_ONLY_RE.test(v))return`${label||"Property address"} holds a phone number (${v}), not an address.`;if(kind==="email"&&!v.includes("@")&&!/^(n\/?a|none|no email|-)$/i.test(v))return`${label||"Email"} does not look like an email address (${v}).`;return null}function leadSemanticFallback(ef,key,label){const kind=leadFieldKind(key,label);if(!kind||!ef)return"";for(const[k,v]of Object.entries(ef)){if(k===key||v==null||String(v).trim()==="")continue;if(leadFieldKind(k,"")!==kind)continue;if(leadFieldProblem(k,"",v))continue;return String(v)}return""}function isPrimeHomeBuyersLead(r){return!!leadsPrimeHomeBuyersFormId&&r.submission_form_id===leadsPrimeHomeBuyersFormId||!!leadsIspeedtoleadSendByLeadId[r.id]}async function findPrimeHomeBuyersDuplicate(phoneNorm,address){if(!leadsPrimeHomeBuyersFormId)return null;const addressTrimmed=String(address||"").trim();if(addressTrimmed){const{data}=await sb.from("leads").select("id, status, submitted_at, extra_fields").eq("submission_form_id",leadsPrimeHomeBuyersFormId).ilike("extra_fields->>form_address",addressTrimmed).order("submitted_at",{ascending:true}).limit(1);if(data&&data.length)return{...data[0],matched_via:"address"}}if(phoneNorm){const{data}=await sb.from("leads").select("id, status, submitted_at, extra_fields").eq("submission_form_id",leadsPrimeHomeBuyersFormId).eq("seller_phone_norm",phoneNorm).order("submitted_at",{ascending:true}).limit(1);if(data&&data.length)return{...data[0],matched_via:"phone"}}return null}function wireAddressAutocompleteAndDupCheck(container,opts){const addrInput=container.querySelector('input[data-field-key="form_address"]');if(!addrInput)return;let statusEl=addrInput.parentElement.querySelector(".address-dup-check-status");if(!statusEl){statusEl=document.createElement("div");statusEl.className="address-dup-check-status";statusEl.style.cssText="font-size:11px; margin-top:4px; display:none; line-height:1.4;";addrInput.insertAdjacentElement("afterend",statusEl)}async function runDupCheck(){const address=addrInput.value.trim();const phoneNorm=opts.getPhoneNorm?opts.getPhoneNorm():null;if(!address&&!phoneNorm){statusEl.style.display="none";return}const excludeId=opts.getExcludeLeadId?opts.getExcludeLeadId():null;let dup=await findPrimeHomeBuyersDuplicate(phoneNorm,address);if(dup&&excludeId&&dup.id===excludeId)dup=null;if(!dup){statusEl.style.display="none";return}if(dup.matched_via==="address"){statusEl.textContent=`⚠ A PrimeHome Buyers lead for this exact address already exists (status: ${dup.status}, submitted ${new Date(dup.submitted_at).toLocaleDateString()}).`;statusEl.style.color="var(--danger)"}else{const priorAddr=(dup.extra_fields||{}).form_address||"no address on file";statusEl.textContent=`ℹ This phone number already has a PrimeHome Buyers lead on file for a different address (${priorAddr}, ${dup.status}). If this is genuinely a different property, that's fine.`;statusEl.style.color="var(--text-dim)"}statusEl.style.display="block"}if(window.googlePlacesReady&&window.google?.maps?.places){addrInput.dataset.placesWired="1";const ac=new google.maps.places.Autocomplete(addrInput,{types:["address"],componentRestrictions:{country:"us"},fields:["formatted_address","address_components"]});ac.addListener("place_changed",()=>{const place=ac.getPlace();if(place&&place.formatted_address)addrInput.value=place.formatted_address;const comps=place&&place.address_components||[];const comp=type=>comps.find(c=>c.types.includes(type));const city=comp("locality")||comp("sublocality")||comp("postal_town");const state=comp("administrative_area_level_1");const zip=comp("postal_code");const cityInput=container.querySelector('[data-field-key="form_city"]');const stateInput=container.querySelector('[data-field-key="form_state"]');const zipInput=container.querySelector('[data-field-key="form_zip"]');if(cityInput&&city)cityInput.value=city.long_name;if(stateInput&&state)stateInput.value=state.short_name;if(zipInput&&zip)zipInput.value=zip.long_name;runDupCheck()})}addrInput.addEventListener("blur",runDupCheck);if(opts.phoneInput)opts.phoneInput.addEventListener("blur",runDupCheck)}function wireGenericAddressAutocomplete(container){if(!(window.googlePlacesReady&&window.google?.maps?.places)){if(typeof ensureGooglePlaces==="function")ensureGooglePlaces().then(()=>{if(container.isConnected)wireGenericAddressAutocomplete(container)}).catch(()=>{});return}const textInputs=Array.from(container.querySelectorAll("input[data-field-key]"));const labelFor=el=>el.previousElementSibling&&el.previousElementSibling.tagName==="LABEL"?el.previousElementSibling.textContent:"";const looksLike=(el,re)=>re.test(el.dataset.fieldKey||"")||re.test(labelFor(el));const addressInputs=textInputs.filter(el=>!el.dataset.placesWired&&!/email/i.test(el.dataset.fieldKey||"")&&looksLike(el,/address/i));if(!addressInputs.length)return;const cityInput=textInputs.find(el=>looksLike(el,/\bcity\b/i));const stateInput=textInputs.find(el=>looksLike(el,/\bstate\b/i));const zipInput=textInputs.find(el=>looksLike(el,/\bzip\b|postal/i));addressInputs.forEach(addrInput=>{addrInput.dataset.placesWired="1";const ac=new google.maps.places.Autocomplete(addrInput,{types:["address"],componentRestrictions:{country:"us"},fields:["formatted_address","address_components"]});ac.addListener("place_changed",()=>{const place=ac.getPlace();if(place&&place.formatted_address)addrInput.value=place.formatted_address;const comps=place&&place.address_components||[];const comp=type=>comps.find(c=>c.types.includes(type));const city=comp("locality")||comp("sublocality")||comp("postal_town");const state=comp("administrative_area_level_1");const zip=comp("postal_code");if(cityInput&&city)cityInput.value=city.long_name;if(stateInput&&state)stateInput.value=state.short_name;if(zipInput&&zip)zipInput.value=zip.long_name})})}function isTicketInFlight(r){return r.ticket_stage==="staff_review"||r.ticket_stage==="client_review"||r.ticket_stage==="client_rejected"}function isSalesConversionLead(r){return!!(r&&r.extra_fields&&r.extra_fields.dialer_opportunity_id)}function excludedFromGeneralQueue(r){return isPrimeHomeBuyersLead(r)||isTicketInFlight(r)||isSalesConversionLead(r)}function isLeadTicketOrigin(r){return r.ticket_stage==="recording_pending"}function leadsRowMatchesFilters(r,filters){if(filters.user&&r.user_id!==filters.user)return false;if(filters.seller&&!(r.seller_investor_name||"").toLowerCase().includes(filters.seller.toLowerCase()))return false;if(filters.phone&&!(r.seller_phone||"").replace(/\D/g,"").includes(filters.phone.replace(/\D/g,"")))return false;if(filters.task&&r.submission_form_id!==filters.task)return false;if(filters.status&&r.status!==filters.status)return false;if(filters.dateFrom||filters.dateTo){if(!r.submitted_at)return false;const submittedMs=new Date(r.submitted_at).getTime();if(filters.dateFrom){const[fy,fm,fd]=filters.dateFrom.split("-").map(Number);const{startMs}=dayBoundsMsInZone(globalDisplayTzMode,null,fy,fm,fd);if(submittedMs<startMs)return false}if(filters.dateTo){const[ty,tm,td]=filters.dateTo.split("-").map(Number);const{endMs}=dayBoundsMsInZone(globalDisplayTzMode,null,ty,tm,td);if(submittedMs>endMs)return false}}return true}function populateLeadsCampaignFilterOptions(){const generalRows=leadsFullData.filter(r=>!excludedFromGeneralQueue(r));const distinctIds=[...new Set(generalRows.map(r=>r.submission_form_id).filter(Boolean))];const options=distinctIds.map(id=>({id,name:leadsFormNameFn(id)})).sort((a,b)=>a.name.localeCompare(b.name));const optionsHtml='<option value="">All campaigns</option>'+options.map(o=>`<option value="${o.id}">${escapeHtml(o.name)}</option>`).join("");const selGeneral=$("leadsQueueCampaignFilter");if(selGeneral){const prev=selGeneral.value;selGeneral.innerHTML=optionsHtml;if(options.some(o=>o.id===prev))selGeneral.value=prev}const selIspd=$("leadsIspdCampaignFilter");if(selIspd){selIspd.innerHTML=leadsPrimeHomeBuyersFormId?`<option value="${leadsPrimeHomeBuyersFormId}">${escapeHtml(ISPD_DEFAULT_CAMPAIGN_NAME)}</option>`:`<option value="">${escapeHtml(ISPD_DEFAULT_CAMPAIGN_NAME)}</option>`;selIspd.value=leadsPrimeHomeBuyersFormId||"";leadsIspdFilters.task=leadsPrimeHomeBuyersFormId||""}}function populateLeadsIspdSentByFilterOptions(agentName){const sel=$("leadsIspdSentByFilter");if(!sel)return;const senderIds=[...new Set(Object.values(leadsIspeedtoleadSendByLeadId).map(s=>s.submitted_by).filter(Boolean))];const options=senderIds.map(id=>({id,name:agentName(id)})).sort((a,b)=>a.name.localeCompare(b.name));const prev=sel.value;sel.innerHTML='<option value="">Anyone</option>'+options.map(o=>`<option value="${o.id}">${escapeHtml(o.name)}</option>`).join("");if(options.some(o=>o.id===prev))sel.value=prev}function debounce(fn,ms){let t;return(...args)=>{clearTimeout(t);t=setTimeout(()=>fn(...args),ms)}}const LEADS_QUEUE_PAGE_SIZE=50;const leadsPagerState={};function leadsPageRows(key,rows,filters,pagerId){const st=leadsPagerState[key]||(leadsPagerState[key]={page:1,sig:null});const sig=JSON.stringify(filters||{});if(st.sig!==null&&st.sig!==sig)st.page=1;st.sig=sig;const pages=Math.max(1,Math.ceil(rows.length/LEADS_QUEUE_PAGE_SIZE));if(st.page>pages)st.page=pages;if(st.page<1)st.page=1;const from=(st.page-1)*LEADS_QUEUE_PAGE_SIZE;const slice=rows.slice(from,from+LEADS_QUEUE_PAGE_SIZE);const el=$(pagerId);if(el){if(rows.length<=LEADS_QUEUE_PAGE_SIZE){el.innerHTML="";el.style.display="none"}else{el.style.display="flex";el.innerHTML=`
        <button type="button" class="btn-secondary" data-pg="first" ${st.page===1?"disabled":""}>&laquo; First</button>
        <button type="button" class="btn-secondary" data-pg="prev" ${st.page===1?"disabled":""}>&lsaquo; Prev</button>
        <span class="mono leads-pager-label">Page ${st.page} of ${pages} &middot; ${from+1}&ndash;${from+slice.length} of ${rows.length}</span>
        <button type="button" class="btn-secondary" data-pg="next" ${st.page===pages?"disabled":""}>Next &rsaquo;</button>
        <button type="button" class="btn-secondary" data-pg="last" ${st.page===pages?"disabled":""}>Last &raquo;</button>`;el.querySelectorAll("button[data-pg]").forEach(b=>b.addEventListener("click",()=>{const go=b.dataset.pg;st.page=go==="first"?1:go==="prev"?st.page-1:go==="next"?st.page+1:pages;if(key==="ispd")renderIspdReviewTable();else if(key==="tickets")renderTicketsReviewTable();else renderLeadsQueueTable();const table=el.parentElement&&el.parentElement.querySelector("table");if(table)table.scrollIntoView({block:"start",behavior:"smooth"})}))}}return slice}function renderLeadsQueueTable(){const filtered=leadsFullData.filter(r=>!excludedFromGeneralQueue(r)&&r.status!=="call_back"&&leadsRowMatchesFilters(r,leadsQueueFilters));const agentName=leadsAgentNameFn,formName=leadsFormNameFn,extraFieldsLabel=leadsExtraFieldsLabelFn;const ispeedtoleadByLeadId=leadsInboundPostbackByLeadId;$("leadsQueueStatTotal").textContent=filtered.length;$("leadsQueueStatPending").textContent=filtered.filter(r=>r.status==="pending").length;$("leadsQueueStatAccepted").textContent=filtered.filter(r=>r.status==="accepted").length;$("leadsQueueStatRejected").textContent=filtered.filter(r=>r.status==="rejected").length;const dashQueueTotalEl=$("leadsDashboardQueueReviewTotal");if(dashQueueTotalEl)dashQueueTotalEl.textContent=leadsFullData.filter(r=>!excludedFromGeneralQueue(r)).length;lastLeadsRows=leadsFullData.filter(r=>!excludedFromGeneralQueue(r)).map(r=>{const submittedLabel=new Date(r.submitted_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...leadsTzFor(r.user_id)?{timeZone:leadsTzFor(r.user_id)}:{}});const meta=LEAD_STATUS_META[r.status]||LEAD_STATUS_META.pending;const extraFieldsText=extraFieldsLabel(r.task_id,r.submission_form_id,r.extra_fields);const viaIspeedtolead=ispeedtoleadByLeadId[r.id]||null;const sendStatus=leadsIspeedtoleadSendByLeadId[r.id]||null;const sendStatusLabel=sendStatus?sendStatus.success?`Sent ${new Date(sendStatus.created_at).toLocaleDateString([],{month:"short",day:"numeric",...leadsTzFor(r.user_id)?{timeZone:leadsTzFor(r.user_id)}:{}})}`:"Send failed":ispdPostbackOnlyDelivery(r)?"Delivered (postback)":ispdManuallyDelivered(r)?"Closed by staff":"Not sent";return{agent:agentName(r.user_id),submitted:submittedLabel,count:r.count,sellerInvestor:r.seller_investor_name||"—",phone:r.seller_phone||"",extraFieldsText,task:formName(r.submission_form_id),type:isLeadTicketOrigin(r)?"Lead Ticket":"Lead",status:meta.label,rejectReason:r.status==="rejected"&&r.review_note?r.review_note:"",viaIspeedtolead:viaIspeedtolead?"PrimeHome Buyers":"",ispeedtoleadSendStatus:sendStatusLabel}});const body=$("leadsBody");body.innerHTML="";if(filtered.length===0){body.innerHTML=`<tr><td colspan="12" class="empty">${leadsFullData.length===0?"No leads submitted in this range.":"No leads match these filters."}</td></tr>`;leadsPageRows("queue",[],leadsQueueFilters,"leadsQueuePager");applyLeadsSectionEditGating("queue_review");return}leadsPageRows("queue",filtered,leadsQueueFilters,"leadsQueuePager").forEach(r=>{const submittedLabel=new Date(r.submitted_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...leadsTzFor(r.user_id)?{timeZone:leadsTzFor(r.user_id)}:{}});const meta=LEAD_STATUS_META[r.status]||LEAD_STATUS_META.pending;const sellerInvestor=r.seller_investor_name||"—";const phone=r.seller_phone||"";const extraFieldsText=extraFieldsLabel(r.task_id,r.submission_form_id,r.extra_fields);const rejectReason=r.status==="rejected"&&r.review_note?r.review_note:"";const viaIspeedtolead=ispeedtoleadByLeadId[r.id]||null;const fitResult=leadsCampaignFitForBadge(r);const FIT_BADGE_META={strong:{color:"var(--good)",label:"Strong Fit"},partial:{color:"var(--away)",label:"Partial Fit"},not_fit:{color:"var(--danger)",label:"Not a Fit"}};const guideBadge=!fitResult||!fitResult.fit?'<span style="color:var(--text-dim); font-size:11px;">—</span>':(()=>{const meta2=FIT_BADGE_META[fitResult.fit];const isAi=fitResult.verdicts.length&&fitResult.verdicts.every(v=>v.source==="ai");const tooltip=fitResult.verdicts.map(v=>`${v.label}: ${v.rating==="unclear"?"unclear":v.rating==="meets"?"meets":"does not meet"} (${v.source})`).join("\n");const tooltipHead=isAi?`${fitResult.campaignName} — AI Second Opinion (${fitResult.aiModel||"AI"}), ${fitResult.meetsCount}/${fitResult.evaluatedCount} criteria met${fitResult.aiSummary?`
${fitResult.aiSummary}`:""}`:`${fitResult.campaignName} — ${fitResult.meetsCount}/${fitResult.evaluatedCount} criteria met`;const aiTag=isAi?'<span style="opacity:0.7;">✓AI</span> ':"";return`<span style="color:${meta2.color}; font-size:11px; cursor:default;" title="${escapeHtml(`${tooltipHead}
${tooltip}`)}">${aiTag}${meta2.label}</span>`})();const canDeleteLead=myIsOwner||myRole==="admin";const rejectedLocked=r.status==="rejected"&&!!r.review_note;const canEditThisLead=true;const tr=document.createElement("tr");tr.innerHTML=`
      <td>${escapeHtml(agentName(r.user_id))}</td>
      <td class="mono">${submittedLabel}</td>
      <td class="mono">${r.count}</td>
      <td>${escapeHtml(sellerInvestor)}</td>
      <td><span class="lead-phone-cell edit-only" contenteditable="true" data-id="${r.id}" data-original="${escapeHtml(phone)}">${escapeHtml(phone)}</span></td>
      <td>${textPreviewCellHtml(extraFieldsText,"Extra Fields")}</td>
      <td>${escapeHtml(formName(r.submission_form_id))}</td>
      <td>${isLeadTicketOrigin(r)?'<span style="color:var(--focus); font-size:11px;">Lead Ticket</span>':'<span style="color:var(--text-2); font-size:11px;">Lead</span>'}</td>
      <td>${guideBadge}</td>
      <td style="white-space:nowrap;"><span class="dot ${meta.dot}"></span><span class="${meta.text}">${meta.label}</span>${rejectedLocked?' <span class="lead-ispeedtolead-badge" title="A rejection with a reason recorded is final and cannot be re-accepted.">locked</span>':""}${viaIspeedtolead?` <span class="lead-ispeedtolead-badge" title="${viaIspeedtolead.amount!=null?`Amount: ${viaIspeedtolead.amount}`:"Set automatically by a PrimeHome Buyers postback"}">via PrimeHome Buyers</span>`:""}${r.desouky_chat_message_id?' <span class="lead-ispeedtolead-badge" title="Submitted through a chat conversation with Desouky, on behalf of this agent.">via Desouky</span>':""}</td>
      <td>${textPreviewCellHtml(rejectReason,"Rejected reason")}</td>
      <td><div style="display:flex; gap:6px; align-items:center; flex-wrap:nowrap; white-space:nowrap;">
        ${r.status!=="accepted"&&!rejectedLocked&&canEditThisLead?`<button class="lead-review-btn edit-only" style="background:transparent; border:1px solid var(--away); border-radius:6px; color:var(--away); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px;">Review</button>`:""}
        ${r.status==="pending"&&canEditThisLead?`<button class="lead-callback-btn edit-only" data-id="${r.id}" style="background:transparent; border:1px solid var(--border-light); border-radius:6px; color:var(--text-2); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px;">Call Back</button>`:""}
        ${r.status==="accepted"&&canEditThisLead?`<button class="lead-recall-btn edit-only" style="background:transparent; border:1px solid var(--away); border-radius:6px; color:var(--away); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px;">Recall</button>`:""}
        ${r.status!=="rejected"&&canEditThisLead?`<button class="lead-reject-btn edit-only" style="background:transparent; border:1px solid var(--border-light); border-radius:6px; color:var(--danger); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px;">Reject</button>`:""}
        ${canDeleteLead?`<button class="lead-del-btn edit-only" data-id="${r.id}" style="background:transparent; border:1px solid var(--border-light); border-radius:6px; color:var(--text-dim); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px;">Delete</button>`:""}
      </div></td>
    `;const reviewBtn=tr.querySelector(".lead-review-btn");const callBackBtn=tr.querySelector(".lead-callback-btn");const recallBtn=tr.querySelector(".lead-recall-btn");const rejectBtn=tr.querySelector(".lead-reject-btn");const delBtn=tr.querySelector(".lead-del-btn");const phoneEl=tr.querySelector(".lead-phone-cell");if(reviewBtn)reviewBtn.addEventListener("click",()=>openLeadsQueueReviewModal(r,agentName(r.user_id)));if(callBackBtn)callBackBtn.addEventListener("click",()=>openSendToCallBackModal(r,agentName(r.user_id)));if(recallBtn)recallBtn.addEventListener("click",()=>recallAcceptedLeadToQueue(r,agentName(r.user_id)));if(rejectBtn)guardedClick(rejectBtn,()=>setLeadStatus(r,"rejected",agentName(r.user_id)));if(delBtn)delBtn.addEventListener("click",()=>deleteLead(r,`${agentName(r.user_id)} — ${r.count} lead(s)`));if(phoneEl){phoneEl.addEventListener("blur",()=>saveLeadPhone(phoneEl));phoneEl.addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();phoneEl.blur()}if(e.key==="Escape"){phoneEl.textContent=phoneEl.dataset.original;phoneEl.blur()}})}body.appendChild(tr)});applyLeadsSectionEditGating("queue_review")}function renderIspdReviewTable(){const ispdStatusFilterValue=leadsIspdFilters.status;const ispdFiltersForMatch=ispdStatusFilterValue==="refund"?{...leadsIspdFilters,status:"rejected"}:leadsIspdFilters;let filtered=leadsFullData.filter(r=>isPrimeHomeBuyersLead(r)&&r.status!=="call_back"&&leadsRowMatchesFilters(r,ispdFiltersForMatch));if(ispdStatusFilterValue==="refund")filtered=filtered.filter(r=>isIspdRefundLead(r));else if(ispdStatusFilterValue==="rejected")filtered=filtered.filter(r=>!isIspdRefundLead(r));if(leadsIspdFilters.sent==="sent")filtered=filtered.filter(r=>(leadsIspeedtoleadSendByLeadId[r.id]||{}).success||ispdHasPostbackDelivery(r)||ispdManuallyDelivered(r));else if(leadsIspdFilters.sent==="not_sent")filtered=filtered.filter(r=>!(leadsIspeedtoleadSendByLeadId[r.id]||{}).success&&!ispdHasPostbackDelivery(r)&&!ispdManuallyDelivered(r));if(leadsIspdFilters.sentChannel==="gyhomes")filtered=filtered.filter(r=>(leadsGyhomesSendByLeadId[r.id]||{}).success);else if(leadsIspdFilters.sentChannel==="primehome")filtered=filtered.filter(r=>(leadsPrimehomeSendByLeadId[r.id]||{}).success);else if(leadsIspdFilters.sentChannel==="postback")filtered=filtered.filter(r=>ispdPostbackOnlyDelivery(r));else if(leadsIspdFilters.sentChannel==="manual")filtered=filtered.filter(r=>ispdManuallyDelivered(r)&&!(leadsIspeedtoleadSendByLeadId[r.id]||{}).success);if(leadsIspdFilters.sentBy)filtered=filtered.filter(r=>(leadsIspeedtoleadSendByLeadId[r.id]||{}).submitted_by===leadsIspdFilters.sentBy);if(leadsIspdFilters.sentDateFrom||leadsIspdFilters.sentDateTo){filtered=filtered.filter(r=>{const send=leadsIspeedtoleadSendByLeadId[r.id];if(!send||!send.success||!send.created_at)return false;const sentMs=new Date(send.created_at).getTime();if(leadsIspdFilters.sentDateFrom){const[fy,fm,fd]=leadsIspdFilters.sentDateFrom.split("-").map(Number);const{startMs}=dayBoundsMsInZone(globalDisplayTzMode,null,fy,fm,fd);if(sentMs<startMs)return false}if(leadsIspdFilters.sentDateTo){const[ty,tm,td]=leadsIspdFilters.sentDateTo.split("-").map(Number);const{endMs}=dayBoundsMsInZone(globalDisplayTzMode,null,ty,tm,td);if(sentMs>endMs)return false}return true})}const agentName=leadsAgentNameFn,formName=leadsFormNameFn;const ispeedtoleadByLeadId=leadsInboundPostbackByLeadId;$("leadsIspdStatTotal").textContent=filtered.length;$("leadsIspdStatNotSent").textContent=filtered.filter(r=>!leadsIspeedtoleadSendByLeadId[r.id]).length;$("leadsIspdStatSent").textContent=filtered.filter(r=>(leadsIspeedtoleadSendByLeadId[r.id]||{}).success).length;$("leadsIspdStatFailed").textContent=filtered.filter(r=>leadsIspeedtoleadSendByLeadId[r.id]&&!leadsIspeedtoleadSendByLeadId[r.id].success).length;$("leadsIspdStatSentGyhomes").textContent=filtered.filter(r=>(leadsGyhomesSendByLeadId[r.id]||{}).success).length;$("leadsIspdStatSentPrimehome").textContent=filtered.filter(r=>(leadsPrimehomeSendByLeadId[r.id]||{}).success).length;$("leadsIspdStatNotPublished").textContent=filtered.filter(r=>r.status==="pending"&&r.istl_export_status==="not_published").length;const dashIspdTotalEl=$("leadsDashboardIspdTotal");if(dashIspdTotalEl)dashIspdTotalEl.textContent=leadsFullData.filter(r=>isPrimeHomeBuyersLead(r)).length;const bySender={};filtered.forEach(r=>{const send=leadsIspeedtoleadSendByLeadId[r.id];if(send&&send.success&&send.submitted_by)bySender[send.submitted_by]=(bySender[send.submitted_by]||0)+1});const senderRows=Object.entries(bySender).sort((a,b)=>b[1]-a[1]);const bySenderDiv=$("leadsIspdBySender");if(bySenderDiv){if(senderRows.length===0){bySenderDiv.innerHTML='<div class="empty">No successful sends match these filters.</div>'}else{const maxCount=senderRows[0][1];bySenderDiv.innerHTML=senderRows.map(([userId,count])=>`
        <div class="site-row">
          <div class="site-domain" title="${escapeHtml(agentName(userId))}">${escapeHtml(agentName(userId))}</div>
          <div class="site-bar-track"><div class="site-bar-fill" style="width:${Math.max(2,count/maxCount*100)}%"></div></div>
          <div class="site-time">${count}</div>
        </div>
      `).join("")}}const body=$("leadsIspdBody");if(!body)return;const filteredIds=new Set(filtered.map(r=>r.id));Array.from(leadsIspdSelectedIds).forEach(id=>{if(!filteredIds.has(id))leadsIspdSelectedIds.delete(id)});body.innerHTML="";if(!filtered.length){body.innerHTML=`<tr><td colspan="11" class="empty">${leadsFullData.length===0?"No leads submitted in this range.":"No leads match these filters."}</td></tr>`;leadsPageRows("ispd",[],leadsIspdFilters,"leadsIspdPager");applyLeadsSectionEditGating("queue_review_ispeedtolead");updateLeadsIspdBulkToolbar();return}leadsPageRows("ispd",filtered,leadsIspdFilters,"leadsIspdPager").forEach(r=>{const submittedLabel=new Date(r.submitted_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...leadsTzFor(r.user_id)?{timeZone:leadsTzFor(r.user_id)}:{}});const meta=LEAD_STATUS_META[r.status]||LEAD_STATUS_META.pending;const sellerInvestor=r.seller_investor_name||"—";const phone=r.seller_phone||"";const viaIspeedtolead=ispeedtoleadByLeadId[r.id]||null;const isRefund=isIspdRefundLead(r,viaIspeedtolead);const statusLabel=isRefund?"refund":meta.label;const rejectReason=r.status==="rejected"&&r.review_note?r.review_note:"";const sendStatus=leadsIspeedtoleadSendByLeadId[r.id]||null;const sendStatusLabel=sendStatus?sendStatus.success?`Sent ${new Date(sendStatus.created_at).toLocaleDateString([],{month:"short",day:"numeric",...leadsTzFor(r.user_id)?{timeZone:leadsTzFor(r.user_id)}:{}})}`:"Send failed":ispdPostbackOnlyDelivery(r)?"Delivered (postback)":ispdManuallyDelivered(r)?"Closed by staff":"Not sent";const canEditThisLead=true;const rejectedLocked=r.status==="rejected"&&!!r.review_note;const istlExportBadge=r.status==="pending"&&r.istl_export_status?(()=>{const ageDays=r.istl_export_status_at?Math.max(0,Math.floor((Date.now()-new Date(r.istl_export_status_at).getTime())/864e5)):null;const ageText=ageDays!=null?` — ${ageDays}d`:"";return r.istl_export_status==="not_published"?` <span class="lead-ispeedtolead-badge" style="color:var(--text-dim);" title="Per iSpeedToLead's own lead export, this lead is still awaiting an AI publish/reject decision on their side.">awaiting gyhomes decision${ageText}</span>`:` <span class="lead-ispeedtolead-badge" style="color:var(--good);" title="Per iSpeedToLead's own lead export, this lead passed their AI screening and is live for buyers, but not yet purchased.">published, awaiting sale${ageText}</span>`})():"";const tr=document.createElement("tr");tr.innerHTML=`
      <td class="edit-only"><input type="checkbox" class="lead-ispd-select-cb" data-id="${r.id}" ${leadsIspdSelectedIds.has(r.id)?"checked":""}></td>
      <td>${escapeHtml(agentName(r.user_id))}</td>
      <td class="mono">${submittedLabel}</td>
      <td>${escapeHtml(sellerInvestor)}</td>
      <td class="mono">${escapeHtml(phone)||"—"}</td>
      <td>${r.submission_form_id?escapeHtml(formName(r.submission_form_id)):sendStatus?`<span title="Sent to PrimeHome Buyers, but never tagged with the PrimeHome Buyers campaign — likely sent outside the dashboard's own Send button.">PrimeHome Buyers (untagged)</span>`:escapeHtml(formName(r.submission_form_id))}</td>
      <td style="white-space:nowrap;"><span class="dot ${meta.dot}"></span><span class="${meta.text}">${statusLabel}</span>${rejectedLocked?' <span class="lead-ispeedtolead-badge" title="A rejection with a reason recorded is final and cannot be re-accepted.">locked</span>':""}${viaIspeedtolead?` <span class="lead-ispeedtolead-badge" title="${viaIspeedtolead.amount!=null?`Amount: ${viaIspeedtolead.amount}`:"Set automatically by a PrimeHome Buyers postback"}">via PrimeHome Buyers</span>`:""}${istlExportBadge}</td>
      <td>${textPreviewCellHtml(rejectReason,isRefund?"Refund reason":"Rejected reason")}</td>
      <td>
        ${canEditThisLead?`<button class="lead-ispeedtolead-btn edit-only" style="background:transparent; border:1px solid var(--border-light); border-radius:6px; color:${sendStatus&&sendStatus.success?"var(--good)":sendStatus&&!sendStatus.success?"var(--danger)":"var(--text-2)"}; cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px; white-space:nowrap;" title="${sendStatus&&!sendStatus.success?escapeHtml(sendStatus.error||"Last send failed"):""}">${escapeHtml(sendStatusLabel)}</button>`:escapeHtml(sendStatusLabel)}
      </td>
      <td style="color:var(--text-2);">${sendStatus&&sendStatus.submitted_by?escapeHtml(agentName(sendStatus.submitted_by)):"—"}</td>
      <td><div style="display:flex; gap:6px; align-items:center; flex-wrap:nowrap; white-space:nowrap;">
        ${r.status==="pending"&&canEditThisLead?`<button class="lead-callback-btn edit-only" data-id="${r.id}" style="background:transparent; border:1px solid var(--border-light); border-radius:6px; color:var(--text-2); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px;">Call Back</button>`:""}
        ${r.status!=="rejected"&&canEditThisLead?`<button class="lead-ispd-reject-btn edit-only" style="background:transparent; border:1px solid var(--border-light); border-radius:6px; color:var(--danger); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px; white-space:nowrap;">Reject</button>`:""}
      </div></td>
    `;const selectCb=tr.querySelector(".lead-ispd-select-cb");const ispeedtoleadBtn=tr.querySelector(".lead-ispeedtolead-btn");const callBackBtn=tr.querySelector(".lead-callback-btn");const rejectBtn=tr.querySelector(".lead-ispd-reject-btn");if(selectCb)selectCb.addEventListener("change",()=>{if(selectCb.checked)leadsIspdSelectedIds.add(r.id);else leadsIspdSelectedIds.delete(r.id);updateLeadsIspdBulkToolbar()});if(ispeedtoleadBtn)ispeedtoleadBtn.addEventListener("click",()=>openIspeedToLeadModal(r,agentName(r.user_id)));if(callBackBtn)callBackBtn.addEventListener("click",()=>openSendToCallBackModal(r,agentName(r.user_id)));if(rejectBtn)guardedClick(rejectBtn,()=>setLeadStatus(r,"rejected",agentName(r.user_id)));body.appendChild(tr)});applyLeadsSectionEditGating("queue_review_ispeedtolead");updateLeadsIspdBulkToolbar()}let leadsIspdSelectedIds=new Set;function updateLeadsIspdBulkToolbar(){const countEl=$("leadsIspdBulkSelectedCount");const callBackBtn=$("leadsIspdBulkCallBackBtn");const rejectBtn=$("leadsIspdBulkRejectBtn");const deleteBtn=$("leadsIspdBulkDeleteBtn");if(!countEl||!callBackBtn||!rejectBtn)return;const n=leadsIspdSelectedIds.size;countEl.textContent=n===1?"1 selected":`${n} selected`;callBackBtn.disabled=n===0;rejectBtn.disabled=n===0;if(deleteBtn){deleteBtn.style.display=myIsOwner?"":"none";deleteBtn.disabled=n===0}const closeBtn=$("leadsIspdBulkCloseBtn");const reopenBtn=$("leadsIspdBulkReopenBtn");if(closeBtn)closeBtn.disabled=n===0;if(reopenBtn)reopenBtn.disabled=n===0}if($("leadsIspdSelectAllCb"))$("leadsIspdSelectAllCb").addEventListener("change",e=>{document.querySelectorAll(".lead-ispd-select-cb").forEach(cb=>{cb.checked=e.target.checked;const id=cb.dataset.id;if(e.target.checked)leadsIspdSelectedIds.add(id);else leadsIspdSelectedIds.delete(id)});updateLeadsIspdBulkToolbar()});if($("leadsIspdBulkCallBackBtn"))$("leadsIspdBulkCallBackBtn").addEventListener("click",async()=>{const leads=leadsFullData.filter(r=>leadsIspdSelectedIds.has(r.id)&&r.status==="pending");if(!leads.length){alert("None of the selected rows are eligible for Call Back (already sent, rejected, or already in Call Back).");return}const reason=prompt(`Why do these ${leads.length} lead(s) need a call back? (required, applied to all selected)`);if(reason===null)return;const reasonTrim=reason.trim();if(!reasonTrim){alert("A reason is required.");return}const{data:{user}}=await sb.auth.getUser();const ids=leads.map(l=>l.id);const{error}=await sb.from("leads").update({status:"call_back",callback_reason:reasonTrim,callback_fields_to_confirm:[],callback_claimed_by:null,callback_claimed_at:null,reviewed_by:user?user.id:null,reviewed_at:new Date().toISOString()}).in("id",ids);if(error){alert(error.message);return}logActivity("lead.call_back","lead",null,`Bulk sent ${ids.length} lead(s) to Call Back — reason: ${reasonTrim}`);leadsIspdSelectedIds.clear();await loadLeads()});if($("leadsIspdBulkRejectBtn"))$("leadsIspdBulkRejectBtn").addEventListener("click",async()=>{const leads=leadsFullData.filter(r=>leadsIspdSelectedIds.has(r.id)&&r.status!=="rejected");if(!leads.length){alert("None of the selected rows are eligible for Reject (already rejected).");return}const reason=prompt(`Why are these ${leads.length} lead(s) being rejected? (required, applied to all selected)`);if(reason===null)return;const reasonTrim=reason.trim();if(!reasonTrim){alert("A comment is required to reject a lead.");return}const{data:{user}}=await sb.auth.getUser();const ids=leads.map(l=>l.id);const{error}=await sb.from("leads").update({status:"rejected",review_note:reasonTrim,reviewed_by:user?user.id:null,reviewed_at:new Date().toISOString()}).in("id",ids);if(error){alert(error.message);return}logActivity("lead.review","lead",null,`Bulk rejected ${ids.length} lead(s) — reason: ${reasonTrim}`);leadsIspdSelectedIds.clear();await loadLeads()});if($("leadsIspdBulkCloseBtn"))$("leadsIspdBulkCloseBtn").addEventListener("click",async()=>{const leads=leadsFullData.filter(r=>leadsIspdSelectedIds.has(r.id)&&!r.manual_delivery_at);if(!leads.length){alert("None of the selected rows need this — they are already marked closed.");return}const reason=prompt(`Why are these ${leads.length} lead(s) being closed? (required, applied to all selected)

Examples: "already sold, never submitted" or "duplicate of <lead id>".

They will leave the "Not sent" bucket and read "Closed by staff". Nothing about the lead's own data, status or dates changes, and Reopen undoes it.`);if(reason===null)return;const reasonTrim=reason.trim();if(!reasonTrim){alert("A reason is required — it is the only record of why this lead was closed.");return}const{data:{user}}=await sb.auth.getUser();const ids=leads.map(l=>l.id);const{error}=await sb.from("leads").update({manual_delivery_at:new Date().toISOString(),manual_delivery_by:user?user.id:null,manual_delivery_note:reasonTrim}).in("id",ids);if(error){alert(error.message);return}logActivity("lead.manual_close","lead",null,`Marked ${ids.length} lead(s) closed/duplicate — reason: ${reasonTrim}`);leadsIspdSelectedIds.clear();await loadLeads()});if($("leadsIspdBulkReopenBtn"))$("leadsIspdBulkReopenBtn").addEventListener("click",async()=>{const leads=leadsFullData.filter(r=>leadsIspdSelectedIds.has(r.id)&&r.manual_delivery_at);if(!leads.length){alert("None of the selected rows are marked closed.");return}if(!confirm(`Reopen ${leads.length} lead(s)? They go back into the sendable queue and the closed reason is cleared.`))return;const ids=leads.map(l=>l.id);const{error}=await sb.from("leads").update({manual_delivery_at:null,manual_delivery_by:null,manual_delivery_note:null}).in("id",ids);if(error){alert(error.message);return}logActivity("lead.manual_close","lead",null,`Reopened ${ids.length} lead(s) — closed mark removed`);leadsIspdSelectedIds.clear();await loadLeads()});if($("leadsIspdBulkDeleteBtn"))$("leadsIspdBulkDeleteBtn").addEventListener("click",async()=>{if(!myIsOwner)return;const ids=Array.from(leadsIspdSelectedIds);if(!ids.length)return;if(!confirm(`Permanently delete these ${ids.length} lead(s)? This can't be undone.`))return;const{error}=await sb.from("leads").delete().in("id",ids);if(error){alert(error.message);return}logActivity("lead.delete","lead",null,`Bulk deleted ${ids.length} lead(s) from Leads Queue Review — PrimeHome Buyers`);leadsIspdSelectedIds.clear();await loadLeads()});$("leadsIspdUserFilter").addEventListener("change",()=>{leadsIspdFilters.user=$("leadsIspdUserFilter").value;renderIspdReviewTable()});$("leadsIspdSellerFilter").addEventListener("input",debounce(()=>{leadsIspdFilters.seller=$("leadsIspdSellerFilter").value.trim();renderIspdReviewTable()},200));$("leadsIspdPhoneFilter").addEventListener("input",debounce(()=>{leadsIspdFilters.phone=$("leadsIspdPhoneFilter").value.trim();renderIspdReviewTable()},200));$("leadsIspdCampaignFilter").addEventListener("change",()=>{leadsIspdFilters.task=$("leadsIspdCampaignFilter").value;renderIspdReviewTable()});$("leadsIspdStatusFilter").addEventListener("change",()=>{leadsIspdFilters.status=$("leadsIspdStatusFilter").value;renderIspdReviewTable()});$("leadsIspdSentFilter").addEventListener("change",()=>{leadsIspdFilters.sent=$("leadsIspdSentFilter").value;renderIspdReviewTable()});$("leadsIspdSentChannelFilter").addEventListener("change",()=>{leadsIspdFilters.sentChannel=$("leadsIspdSentChannelFilter").value;renderIspdReviewTable()});async function resolveLeadsImportBatch(mode){if(!mode)return"";let q=sb.from("leads").select("last_import_batch, last_imported_at").not("last_import_batch","is",null).order("last_imported_at",{ascending:false}).limit(1);if(mode==="mine_last")q=q.eq("last_imported_by",myUserId);const{data,error}=await q;if(error){console.error("resolveLeadsImportBatch failed:",error);return""}return data&&data[0]&&data[0].last_import_batch||""}$("leadsIspdImportedFilter").addEventListener("change",async()=>{const sel=$("leadsIspdImportedFilter");const mode=sel.value;sel.disabled=true;try{leadsImportBatchFilter=await resolveLeadsImportBatch(mode);if(mode&&!leadsImportBatchFilter){alert(mode==="mine_last"?"You haven't run a CSV/Excel import yet, so there's no last import to show.":"No CSV/Excel import has been run yet.");sel.value=""}await loadLeads()}finally{sel.disabled=false}});$("leadsIspdSentByFilter").addEventListener("change",()=>{leadsIspdFilters.sentBy=$("leadsIspdSentByFilter").value;renderIspdReviewTable()});$("leadsIspdSentDateFromFilter").addEventListener("change",()=>{leadsIspdFilters.sentDateFrom=$("leadsIspdSentDateFromFilter").value;renderIspdReviewTable()});$("leadsIspdSentDateToFilter").addEventListener("change",()=>{leadsIspdFilters.sentDateTo=$("leadsIspdSentDateToFilter").value;renderIspdReviewTable()});$("leadsIspdDateFromFilter").addEventListener("change",()=>{leadsIspdFilters.dateFrom=$("leadsIspdDateFromFilter").value;loadLeads()});$("leadsIspdDateToFilter").addEventListener("change",()=>{leadsIspdFilters.dateTo=$("leadsIspdDateToFilter").value;loadLeads()});$("leadsQueueUserFilter").addEventListener("change",()=>{leadsQueueFilters.user=$("leadsQueueUserFilter").value;renderLeadsQueueTable()});$("leadsQueueSellerFilter").addEventListener("input",debounce(()=>{leadsQueueFilters.seller=$("leadsQueueSellerFilter").value.trim();renderLeadsQueueTable()},200));$("leadsQueuePhoneFilter").addEventListener("input",debounce(()=>{leadsQueueFilters.phone=$("leadsQueuePhoneFilter").value.trim();renderLeadsQueueTable()},200));$("leadsQueueCampaignFilter").addEventListener("change",()=>{leadsQueueFilters.task=$("leadsQueueCampaignFilter").value;renderLeadsQueueTable()});$("leadsQueueStatusFilter").addEventListener("change",()=>{leadsQueueFilters.status=$("leadsQueueStatusFilter").value;renderLeadsQueueTable()});$("leadsQueueDateFromFilter").addEventListener("change",()=>{leadsQueueFilters.dateFrom=$("leadsQueueDateFromFilter").value;loadLeads()});$("leadsQueueDateToFilter").addEventListener("change",()=>{leadsQueueFilters.dateTo=$("leadsQueueDateToFilter").value;loadLeads()});const TICKET_STAGE_META={staff_review:{label:"Awaiting staff review",color:"var(--away)"},client_review:{label:"Awaiting client (Yes/No)",color:"var(--focus)"},recording_pending:{label:"Client said Yes — back in Queue Review",color:"var(--good)"},client_rejected:{label:"Client rejected — pick a new campaign?",color:"var(--danger)"}};function renderTicketsReviewTable(){const filtered=leadsFullData.filter(r=>{if(!r.ticket_stage)return false;if(leadsTicketsFilters.stage&&r.ticket_stage!==leadsTicketsFilters.stage)return false;if(!leadsRowMatchesFilters(r,leadsTicketsFilters))return false;return true});const agentName=leadsAgentNameFn;const body=$("leadsTicketsBody");if(!body)return;if($("leadsTicketsStatTotal")){$("leadsTicketsStatTotal").textContent=filtered.length;$("leadsTicketsStatStaffReview").textContent=filtered.filter(r=>r.ticket_stage==="staff_review").length;$("leadsTicketsStatClientReview").textContent=filtered.filter(r=>r.ticket_stage==="client_review").length;$("leadsTicketsStatRecordingPending").textContent=filtered.filter(r=>r.ticket_stage==="recording_pending").length;$("leadsTicketsStatRejected").textContent=filtered.filter(r=>r.status==="rejected").length}body.innerHTML="";if(!filtered.length){body.innerHTML=`<tr><td colspan="8" class="empty">${leadsFullData.some(r=>r.ticket_stage)?"No tickets match these filters.":"No tickets submitted in this range yet."}</td></tr>`;leadsPageRows("tickets",[],leadsTicketsFilters,"leadsTicketsPager");applyLeadsSectionEditGating("tickets_review");return}leadsPageRows("tickets",filtered,leadsTicketsFilters,"leadsTicketsPager").forEach(r=>{const submittedLabel=new Date(r.submitted_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...leadsTzFor(r.user_id)?{timeZone:leadsTzFor(r.user_id)}:{}});const meta=LEAD_STATUS_META[r.status]||LEAD_STATUS_META.pending;const stageMeta=TICKET_STAGE_META[r.ticket_stage]||{label:r.ticket_stage,color:"var(--text-dim)"};const currentFormName=r.submission_form_id&&r.submission_form_id!==leadsTicketFormId?leadsFormNameFn(r.submission_form_id):"";const currentCampaign=currentFormName&&currentFormName!=="No campaign"?currentFormName:"";const campaignEntries=leadsExtraFieldsEntriesFn(r.task_id,r.submission_form_id,r.extra_fields)||[];const labelledCampaign=campaignEntries.find(e=>csubNormalizeLabel(e.label)===csubNormalizeLabel("Campaign Name"));const submittedCampaignName=String(labelledCampaign&&labelledCampaign.value||r.extra_fields&&r.extra_fields.campaign_name||"").trim();const campaignCell=currentCampaign?escapeHtml(currentCampaign)+(submittedCampaignName&&csubNormalizeLabel(submittedCampaignName)!==csubNormalizeLabel(currentCampaign)?`<div style="font-size:10px; color:var(--text-dim); margin-top:2px;">submitted as: ${escapeHtml(submittedCampaignName)}</div>`:""):submittedCampaignName?escapeHtml(submittedCampaignName):"—";const canEditThisLead=true;const tr=document.createElement("tr");tr.innerHTML=`
      <td>${escapeHtml(agentName(r.user_id))}</td>
      <td class="mono">${submittedLabel}</td>
      <td>${escapeHtml(r.seller_investor_name||"—")}</td>
      <td class="mono">${escapeHtml(r.seller_phone||"—")}</td>
      <td>${campaignCell}</td>
      <td><span style="color:${stageMeta.color};">${escapeHtml(stageMeta.label)}</span></td>
      <td><span class="dot ${meta.dot}"></span><span class="${meta.text}">${meta.label}</span></td>
      <td>
        ${r.ticket_stage==="staff_review"&&canEditThisLead?`
          <button class="ticket-accept-btn edit-only" style="background:transparent; border:1px solid var(--good); border-radius:6px; color:var(--good); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px; white-space:nowrap; margin-right:4px;">Accept</button>
          <button class="ticket-reject-btn edit-only" style="background:transparent; border:1px solid var(--border-light); border-radius:6px; color:var(--danger); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px; white-space:nowrap;">Reject</button>
        `:""}
        ${r.ticket_stage==="client_review"&&canEditThisLead?`
          <button class="ticket-recall-btn edit-only" style="background:transparent; border:1px solid var(--away); border-radius:6px; color:var(--away); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px; white-space:nowrap;">Recall to Queue</button>
        `:""}
        ${r.ticket_stage==="client_rejected"&&canEditThisLead?`
          <button class="ticket-redirect-btn edit-only" style="background:transparent; border:1px solid var(--focus); border-radius:6px; color:var(--focus); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px; white-space:nowrap;">Redirect to another campaign</button>
        `:""}
        ${r.ticket_stage!=="staff_review"&&r.ticket_stage!=="client_review"&&r.ticket_stage!=="client_rejected"||!canEditThisLead?'<span style="color:var(--text-dim); font-size:11px;">—</span>':""}
      </td>
    `;const acceptBtn=tr.querySelector(".ticket-accept-btn");const rejectBtn=tr.querySelector(".ticket-reject-btn");const recallBtn=tr.querySelector(".ticket-recall-btn");const redirectBtn=tr.querySelector(".ticket-redirect-btn");if(acceptBtn)acceptBtn.addEventListener("click",()=>openTicketAcceptModal(r));if(rejectBtn)guardedClick(rejectBtn,()=>setLeadStatus(r,"rejected",agentName(r.user_id),{ticket_stage:null}));if(recallBtn)recallBtn.addEventListener("click",()=>recallTicketToQueue(r,agentName(r.user_id)));if(redirectBtn)redirectBtn.addEventListener("click",()=>openTicketAcceptModal(r));body.appendChild(tr)});applyLeadsSectionEditGating("tickets_review")}async function recallTicketToQueue(lead,agentLabel){if(!confirm(`Recall this ticket back to "Awaiting staff review"?

It will stop being visible to the client immediately.

Agent: ${agentLabel} — ${lead.count} lead(s)`))return;const{error}=await sb.from("leads").update({ticket_stage:"staff_review"}).eq("id",lead.id);if(error){alert(error.message);return}await loadLeads();await refreshDetailIfViewing(lead.user_id);logActivity("lead.ticket_recall","lead",lead.id,`Recalled a ticket from "Awaiting client (Yes/No)" back to "Awaiting staff review" — ${agentLabel} — ${lead.count} lead(s)`)}$("leadsTicketsUserFilter").addEventListener("change",()=>{leadsTicketsFilters.user=$("leadsTicketsUserFilter").value;renderTicketsReviewTable()});$("leadsTicketsDateFromFilter").addEventListener("change",()=>{leadsTicketsFilters.dateFrom=$("leadsTicketsDateFromFilter").value;loadLeads()});$("leadsTicketsDateToFilter").addEventListener("change",()=>{leadsTicketsFilters.dateTo=$("leadsTicketsDateToFilter").value;loadLeads()});$("leadsTicketsSellerFilter").addEventListener("input",debounce(()=>{leadsTicketsFilters.seller=$("leadsTicketsSellerFilter").value.trim();renderTicketsReviewTable()},200));$("leadsTicketsPhoneFilter").addEventListener("input",debounce(()=>{leadsTicketsFilters.phone=$("leadsTicketsPhoneFilter").value.trim();renderTicketsReviewTable()},200));$("leadsTicketsStageFilter").addEventListener("change",()=>{leadsTicketsFilters.stage=$("leadsTicketsStageFilter").value;renderTicketsReviewTable()});let allQueueSubmissions=[];let allQueueDisputes=[];let allQueueFilters={dateFrom:"",dateTo:"",queue:"",search:"",user:"",status:""};let allQueueLoaded=false;let allQueueExpandedKey=null;function allQueueClassifyLead(r){if(r.ticket_stage)return"tickets";if(isPrimeHomeBuyersLead(r))return"primehome";if(isSalesConversionLead(r))return"sales";return"queue_review"}async function loadAllQueue(){const body=$("allQueueBody");if(body&&!allQueueLoaded)body.innerHTML='<tr><td colspan="8" class="empty">Loading…</td></tr>';const[{data:submissions,error:subErr},{data:disputes,error:dispErr}]=await Promise.all([sb.from("ghl_form_submissions").select("id, form_name, contact_name, contact_phone, contact_email, submitted_at, fields").order("submitted_at",{ascending:false}).limit(1e3),sb.from("lead_disputes").select("id, lead_id, type, status, disputed_fields, created_at, leads(seller_investor_name, seller_phone, submission_form_id), client_accounts(full_name, company_name)").order("created_at",{ascending:false}).limit(1e3)]);if(subErr)console.error("Failed to load Client Submissions for All Queue",subErr);if(dispErr)console.error("Failed to load Dispute/Feedback for All Queue",dispErr);allQueueSubmissions=submissions||[];allQueueDisputes=disputes||[];allQueueLoaded=true;renderAllQueueTable()}function renderAllQueueTable(){const body=$("allQueueBody");const emptyEl=$("allQueueEmpty");if(!body)return;const QUEUE_META={queue_review:{label:"Leads Queue Review",color:"var(--text)"},primehome:{label:"PrimeHome Buyers",color:"var(--focus)"},sales:{label:"Sales (converted opportunity)",color:"var(--good)"},tickets:{label:"Tickets Review",color:"var(--away)"},dispute_feedback:{label:"Dispute/Feedback",color:"var(--danger)"},client_submission:{label:"Client Submission",color:"var(--text-dim)"}};let rows=(leadsFullData||[]).map(r=>({source:"lead",id:r.id,queue:allQueueClassifyLead(r),name:r.seller_investor_name||"—",phone:r.seller_phone||"—",campaignOrForm:leadsFormNameFn?leadsFormNameFn(r.submission_form_id):"—",submittedAt:r.submitted_at,status:r.status,userId:r.user_id||"",details:leadsExtraFieldsLabelFn?leadsExtraFieldsLabelFn(r.task_id,r.submission_form_id,r.extra_fields):""}));rows=rows.concat(allQueueSubmissions.map(s=>({source:"submission",id:s.id,queue:"client_submission",name:s.contact_name||s.contact_email||"—",phone:s.contact_phone||"—",campaignOrForm:s.form_name||"(untitled form)",submittedAt:s.submitted_at,status:null,details:Object.entries(s.fields||{}).filter(([,v])=>v!==""&&v!=null).map(([k,v])=>`${k}: ${Array.isArray(v)?v.join(", "):v}`).join("\n")})));rows=rows.concat(allQueueDisputes.map(d=>{const lead=d.leads||{};const client=d.client_accounts||{};const isFeedback=d.type==="feedback";return{source:"dispute",id:d.id,queue:"dispute_feedback",name:lead.seller_investor_name||"—",phone:lead.seller_phone||"—",campaignOrForm:leadsFormNameFn?leadsFormNameFn(lead.submission_form_id):"—",submittedAt:d.created_at,status:`${isFeedback?"Feedback":"Dispute"} — ${d.status}`,details:`Client: ${client.company_name||client.full_name||"—"}`+(isFeedback?"":`
Reason: ${(d.disputed_fields||[]).map(f=>f.label||f.key).join(", ")}`)}}));const search=allQueueFilters.search.trim().toLowerCase();const filtered=rows.filter(r=>{if(allQueueFilters.queue&&r.queue!==allQueueFilters.queue)return false;if(allQueueFilters.user&&r.userId!==allQueueFilters.user)return false;if(allQueueFilters.status&&r.status!==allQueueFilters.status)return false;if(allQueueFilters.dateFrom||allQueueFilters.dateTo){if(!r.submittedAt)return false;const submittedMs=new Date(r.submittedAt).getTime();if(allQueueFilters.dateFrom){const[fy,fm,fd]=allQueueFilters.dateFrom.split("-").map(Number);const{startMs}=dayBoundsMsInZone(globalDisplayTzMode,null,fy,fm,fd);if(submittedMs<startMs)return false}if(allQueueFilters.dateTo){const[ty,tm,td]=allQueueFilters.dateTo.split("-").map(Number);const{endMs}=dayBoundsMsInZone(globalDisplayTzMode,null,ty,tm,td);if(submittedMs>endMs)return false}}if(search){const hay=`${r.name} ${r.phone} ${r.campaignOrForm}`.toLowerCase();if(!hay.includes(search))return false}return true}).sort((a,b)=>new Date(b.submittedAt||0)-new Date(a.submittedAt||0));if(!filtered.length){body.innerHTML="";if(emptyEl)emptyEl.style.display="";return}if(emptyEl)emptyEl.style.display="none";const canDeleteAdminRows=myIsOwner||myRole==="admin";body.innerHTML="";filtered.forEach(r=>{const meta=QUEUE_META[r.queue]||{label:r.queue,color:"var(--text-dim)"};const allQueueTz=r.userId?leadsTzFor(r.userId):nonAgentDisplayTz();const submittedLabel=r.submittedAt?new Date(r.submittedAt).toLocaleString([],{month:"short",day:"numeric",year:"numeric",hour:"2-digit",minute:"2-digit",...allQueueTz?{timeZone:allQueueTz}:{}}):"—";const statusLabel=typeof r.status==="string"&&!r.status.includes("—")?(LEAD_STATUS_META[r.status]||{label:r.status}).label:r.status||"—";const rowKey=`${r.source}-${r.id}`;const expanded=allQueueExpandedKey===rowKey;const canDelete=r.source==="submission"?myIsOwner:canDeleteAdminRows;const tr=document.createElement("tr");tr.innerHTML=`
      <td><span style="color:${meta.color};">${escapeHtml(meta.label)}</span></td>
      <td>${escapeHtml(r.name)}</td>
      <td class="mono">${escapeHtml(r.phone)}</td>
      <td>${escapeHtml(r.campaignOrForm)}</td>
      <td class="mono">${submittedLabel}</td>
      <td>${escapeHtml(statusLabel)}</td>
      <td>${r.details?`<button class="btn-secondary all-queue-details-btn" data-key="${escapeHtml(rowKey)}" style="width:auto; padding:5px 10px; font-size:11px;">${expanded?"Hide":"Show"}</button>`:'<span style="color:var(--text-dim); font-size:11px;">—</span>'}</td>
      <td>${canDelete?`<button class="btn-secondary all-queue-del-btn" style="width:auto; padding:5px 12px; font-size:11px; color:var(--danger); border-color:var(--danger);" data-source="${r.source}" data-id="${r.id}">Delete</button>`:'<span style="color:var(--text-dim); font-size:11px;">—</span>'}</td>
    `;body.appendChild(tr);if(expanded&&r.details){const detailsTr=document.createElement("tr");detailsTr.innerHTML=`<td colspan="8" style="background:var(--inset); white-space:pre-wrap; font-size:12px; padding:10px 14px;">${escapeHtml(r.details)}</td>`;body.appendChild(detailsTr)}})}$("allQueueBody").addEventListener("click",async e=>{const detailsBtn=e.target.closest(".all-queue-details-btn");if(detailsBtn){allQueueExpandedKey=allQueueExpandedKey===detailsBtn.dataset.key?null:detailsBtn.dataset.key;renderAllQueueTable();return}const btn=e.target.closest(".all-queue-del-btn");if(!btn)return;const isSubmissionRow=btn.dataset.source!=="lead"&&btn.dataset.source!=="dispute";if(isSubmissionRow?!myIsOwner:!(myIsOwner||myRole==="admin"))return;if(!confirm("Permanently delete this record? This can’t be undone."))return;btn.disabled=true;const table=btn.dataset.source==="lead"?"leads":btn.dataset.source==="dispute"?"lead_disputes":"ghl_form_submissions";const{error}=await sb.from(table).delete().eq("id",btn.dataset.id);if(error){alert(error.message);btn.disabled=false;return}logActivity("all_queue.delete",table==="leads"?"lead":table==="lead_disputes"?"lead_dispute":"ghl_form_submission",btn.dataset.id,`Deleted a record from All Queue (${table})`);if(table==="leads")await loadLeads();else await loadAllQueue()});$("allQueueDateFromFilter").addEventListener("change",()=>{allQueueFilters.dateFrom=$("allQueueDateFromFilter").value;loadLeads()});$("allQueueDateToFilter").addEventListener("change",()=>{allQueueFilters.dateTo=$("allQueueDateToFilter").value;loadLeads()});$("allQueueQueueFilter").addEventListener("change",()=>{allQueueFilters.queue=$("allQueueQueueFilter").value;renderAllQueueTable()});$("allQueueUserFilter").addEventListener("change",()=>{allQueueFilters.user=$("allQueueUserFilter").value;renderAllQueueTable()});$("allQueueStatusFilter").addEventListener("change",()=>{allQueueFilters.status=$("allQueueStatusFilter").value;renderAllQueueTable()});$("allQueueSearchFilter").addEventListener("input",debounce(()=>{allQueueFilters.search=$("allQueueSearchFilter").value;renderAllQueueTable()},200));function leadsExtraFieldInputHtml(e,cssClass,opts){const val=String(e.value);const requiredAttr=opts&&opts.requiredAttr||"";const extraStyle=opts&&opts.extraStyle||"";if(e.type==="select"&&Array.isArray(e.options)&&e.options.length){const hasVal=val!==""&&e.options.includes(val);const optionsHtml=(val!==""&&!hasVal?[`<option value="${escapeHtml(val)}" selected>${escapeHtml(val)} (not a listed option)</option>`]:[]).concat(e.options.map(o=>`<option value="${escapeHtml(o)}" ${o===val?"selected":""}>${escapeHtml(o)}</option>`));return`<select class="${cssClass}" data-key="${escapeHtml(e.key)}" ${requiredAttr} style="${extraStyle}"><option value="">—</option>${optionsHtml.join("")}</select>`}const isMulti=e.type==="multi"||val.includes("\n");return isMulti?`<textarea class="${cssClass}" data-key="${escapeHtml(e.key)}" ${requiredAttr} rows="3" style="width:100%; font-family:var(--mono); font-size:12px; ${extraStyle}">${escapeHtml(val)}</textarea>`:`<input type="text" class="${cssClass}" data-key="${escapeHtml(e.key)}" ${requiredAttr} value="${escapeHtml(val)}" style="font-family:var(--mono); font-size:12px; ${extraStyle}">`}let ticketAcceptModalLead=null;function renderTicketAcceptExtraFields(lead){const entries=leadsExtraFieldsEntriesFn(lead.task_id,lead.submission_form_id,lead.extra_fields);const wrap=$("ticketAcceptExtraFieldsWrap");const container=$("ticketAcceptExtraFields");if(!entries.length){container.innerHTML="";wrap.style.display="none";return}container.innerHTML=entries.map(e=>{const val=String(e.value);const blankRequired=e.required&&!val.trim();const highlightStyle=blankRequired?"border:1px solid var(--away); background:rgba(255,159,10,0.08);":"";const requiredNote=blankRequired?`<div style="font-size:10px; color:var(--away); margin-top:3px;">Required — this field must be filled in to send to the client.</div>`:"";return`
      <div>
        <label style="font-size:11px; color:var(--text-dim); font-weight:400; display:block; margin-bottom:3px;">${escapeHtml(e.label)}${e.required?' <span style="color:var(--away);">*</span>':""}</label>
        ${leadsExtraFieldInputHtml(e,"ticket-accept-extra-field",{requiredAttr:`data-required="${e.required?"true":"false"}"`,extraStyle:highlightStyle})}
        ${requiredNote}
      </div>`}).join("");wrap.style.display=""}function initSearchableSelect(selectId,searchId,listId){const select=$(selectId),search=$(searchId),list=$(listId);if(!select||!search||!list)return;const options=Array.from(select.options).map(o=>({value:o.value,label:o.textContent,selected:o.selected}));const selected=options.find(o=>o.selected&&o.value);search.value=selected?selected.label:"";function renderList(filterText){const q=(filterText||"").trim().toLowerCase();const matches=q?options.filter(o=>o.value&&o.label.toLowerCase().includes(q)):options.filter(o=>o.value);list.innerHTML=matches.length?matches.map(o=>`<div class="searchable-select-option" data-value="${escapeHtml(o.value)}">${escapeHtml(o.label)}</div>`).join(""):`<div class="searchable-select-option empty">${options.some(o=>!o.value)?escapeHtml(options.find(o=>!o.value).label):"No matches"}</div>`;list.querySelectorAll(".searchable-select-option[data-value]").forEach(el=>{el.addEventListener("mousedown",e=>{e.preventDefault();select.value=el.dataset.value;const opt=options.find(o=>o.value===el.dataset.value);search.value=opt?opt.label:"";select.dispatchEvent(new Event("change",{bubbles:true}));list.classList.remove("show")})})}search.oninput=()=>{renderList(search.value);list.classList.add("show")};search.onfocus=()=>{renderList(search.value===(selected?selected.label:"")?"":search.value);list.classList.add("show")};search.onblur=()=>{setTimeout(()=>list.classList.remove("show"),120)}}async function openTicketAcceptModal(lead){ticketAcceptModalLead=lead;const sel=$("ticketAcceptCampaignSelect");sel.innerHTML='<option value="">Loading campaigns…</option>';$("ticketAcceptSellerName").value=lead.seller_investor_name||"";$("ticketAcceptPhone").value=lead.seller_phone||"";renderTicketAcceptExtraFields(lead);$("ticketAcceptModalMsg").style.display="none";$("ticketAcceptModalOverlay").classList.add("active");const{data:forms}=await sb.from("lead_submission_forms").select("id, name, is_active").eq("is_active",true).order("name");const options=(forms||[]).filter(f=>f.id!==leadsTicketFormId);const submittedName=leadsCampaignNameFor(lead).trim().toLowerCase();const guessedMatch=options.find(f=>f.name.trim().toLowerCase()===submittedName);sel.innerHTML=options.map(f=>`<option value="${f.id}" ${guessedMatch&&f.id===guessedMatch.id?"selected":""}>${escapeHtml(f.name)}</option>`).join("")||'<option value="">No other campaigns exist yet — create one under Submission Forms Creator first.</option>';initSearchableSelect("ticketAcceptCampaignSelect","ticketAcceptCampaignSearch","ticketAcceptCampaignList")}function closeTicketAcceptModal(){$("ticketAcceptModalOverlay").classList.remove("active");ticketAcceptModalLead=null}$("ticketAcceptCancelBtn").addEventListener("click",closeTicketAcceptModal);$("ticketAcceptModalOverlay").addEventListener("click",e=>{if(e.target.id==="ticketAcceptModalOverlay")closeTicketAcceptModal()});$("ticketAcceptConfirmBtn").addEventListener("click",async()=>{const lead=ticketAcceptModalLead;const msgEl=$("ticketAcceptModalMsg");if(!lead)return;const targetFormId=$("ticketAcceptCampaignSelect").value;if(!targetFormId){msgEl.textContent="Pick a campaign first.";msgEl.style.display="";return}const sellerName=$("ticketAcceptSellerName").value.trim();const phone=$("ticketAcceptPhone").value.trim();if(!sellerName){msgEl.textContent="Seller / Investor Name is required.";msgEl.style.display="";return}if(!phone){msgEl.textContent="Phone is required.";msgEl.style.display="";return}const missingRequired=[];document.querySelectorAll("#ticketAcceptExtraFields .ticket-accept-extra-field").forEach(el=>{if(el.dataset.required==="true"&&!el.value.trim()){const label=el.closest("div")?.querySelector("label")?.textContent.replace(/[\s*]+$/,"")||el.dataset.key;missingRequired.push(label)}});if(missingRequired.length){msgEl.textContent=`Required fields missing: ${missingRequired.join(", ")}.`;msgEl.style.display="";return}const badValues=[];document.querySelectorAll("#ticketAcceptExtraFields .ticket-accept-extra-field").forEach(el=>{const label=el.closest("div")?.querySelector("label")?.textContent.replace(/[\s*]+$/,"")||el.dataset.key;const problem=leadFieldProblem(el.dataset.key,label,el.value);if(problem)badValues.push(problem)});if(badValues.length){msgEl.textContent=badValues.join(" ");msgEl.style.display="";return}const extraFields={...lead.extra_fields||{}};document.querySelectorAll("#ticketAcceptExtraFields .ticket-accept-extra-field").forEach(el=>{const key=el.dataset.key;const val=el.value.trim();if(val)extraFields[key]=val;else delete extraFields[key]});const{data:{user}}=await sb.auth.getUser();const targetFormLabel=$("ticketAcceptCampaignSelect").selectedOptions[0]?$("ticketAcceptCampaignSelect").selectedOptions[0].textContent:targetFormId;const{error}=await sb.from("leads").update({submission_form_id:targetFormId,seller_investor_name:sellerName,seller_phone:phone,extra_fields:extraFields,ticket_stage:"client_review",status:"pending",review_note:null,reviewed_by:user?user.id:null,reviewed_at:new Date().toISOString()}).eq("id",lead.id);if(error){msgEl.textContent=error.message;msgEl.style.display="";return}notifyClientsForFormId(targetFormId,"ticket_received",targetFormLabel);logActivity("lead.ticket_accept","lead",lead.id,`Ticket accepted and sent to client for review — campaign: ${targetFormLabel}`);closeTicketAcceptModal();await loadLeads()});let sfAllForms=[];let sfFormFieldsCache={};let sfFormRolesCache={};let sfAssignableRoles=[];let sfAllAgentProfiles=[];let sfFormAgentsCache={};let sfAllTemplates=[];async function initSubmissionFormsSection(){await loadSfFormsAndFields();renderSfFormsCreatorList();renderSfTemplatesList();renderFillFormOptions();renderFillFields();if(!campaignGuidesLoaded)loadCampaignGuide();document.querySelectorAll(".sf-subtab-btn").forEach(btn=>{btn.style.display=canViewLeadsSection(btn.dataset.sfSection==="creator"?"submission_forms_creator":"submission_forms")?"":"none"});const activeBtn=document.querySelector(".sf-subtab-btn.active");const activeKey=activeBtn&&activeBtn.style.display!=="none"?activeBtn.dataset.sfSection:null;const firstVisible=Array.from(document.querySelectorAll(".sf-subtab-btn")).find(b=>b.style.display!=="none");activateSfSection(activeKey||(firstVisible?firstVisible.dataset.sfSection:"submit"));const hint=$("lfFillPermissionHint");if(hint)hint.style.display=canEditLeadsSection("submission_forms")?"none":"block";const creatorHint=$("sfCreatorPermHint");if(creatorHint)creatorHint.style.display=canEditLeadsSection("submission_forms_creator")?"none":""}async function loadSfFormsAndFields(){const{data:forms}=await sb.from("lead_submission_forms").select("id, name, is_active, sort_order").order("sort_order");sfAllForms=forms||[];const{data:fields}=await sb.from("lead_submission_form_fields").select("id, form_id, field_key, label, field_type, options, required, placeholder, sort_order").order("sort_order");sfFormFieldsCache={};(fields||[]).forEach(f=>{(sfFormFieldsCache[f.form_id]=sfFormFieldsCache[f.form_id]||[]).push(f)});const{data:formRoles}=await sb.from("lead_submission_form_roles").select("form_id, role");sfFormRolesCache={};(formRoles||[]).forEach(r=>{(sfFormRolesCache[r.form_id]=sfFormRolesCache[r.form_id]||new Set).add(r.role)});const{data:roles}=await sb.from("roles").select("name").order("name");sfAssignableRoles=(roles||[]).filter(r=>!PROTECTED_ROLES.has(r.name)&&r.name!=="agent");const{data:agentProfiles}=await sb.from("profiles").select("id, full_name, email").eq("role","agent").order("full_name");sfAllAgentProfiles=agentProfiles||[];const{data:formAgents}=await sb.from("lead_submission_form_agents").select("form_id, user_id");sfFormAgentsCache={};(formAgents||[]).forEach(a=>{(sfFormAgentsCache[a.form_id]=sfFormAgentsCache[a.form_id]||new Set).add(a.user_id)});const{data:templates}=await sb.from("lead_submission_field_templates").select("id, name, fields").order("name");sfAllTemplates=templates||[]}function sfFormVisibleToMe(formId){if(myIsOwner||myRole==="admin")return true;const allowedAgents=sfFormAgentsCache[formId];if(allowedAgents&&allowedAgents.size>0)return allowedAgents.has(myUserId);const allowedRoles=sfFormRolesCache[formId];return!allowedRoles||allowedRoles.size===0||allowedRoles.has(myRole)}$("sfAddFormBtn").addEventListener("click",async()=>{const input=$("sfNewFormNameInput");const msg=$("sfNewFormMsg");const name=(input.value||"").trim();msg.textContent="";msg.className="msg";if(!name){msg.textContent="Enter a name for the form.";msg.className="msg error";return}const nextOrder=sfAllForms.reduce((m,f)=>Math.max(m,f.sort_order),-10)+10;const{data:{user}}=await sb.auth.getUser();const{data:createdForm,error}=await sb.from("lead_submission_forms").insert({name,sort_order:nextOrder,created_by:user?user.id:null}).select("id").maybeSingle();if(error){msg.textContent=/duplicate|unique/i.test(error.message)?"A form with that name already exists.":error.message;msg.className="msg error";return}input.value="";logActivity("lead_submission_form.create",null,null,`Created submission form "${name}"`);if(createdForm&&createdForm.id){const template=sfFindDefaultTemplateFinal();if(template&&(template.fields||[]).length){let sortOrder=0;let added=0;for(const tf of template.fields){const{error:fErr}=await sb.from("lead_submission_form_fields").insert({form_id:createdForm.id,field_key:tf.field_key,label:tf.label,field_type:tf.field_type,options:tf.options||null,required:!!tf.required,placeholder:tf.placeholder||null,sort_order:sortOrder});sortOrder+=10;if(!fErr)added++}if(added)logActivity("lead_submission_form_field.apply_template",null,null,`Applied template "${template.name}" to new submission form "${name}" (${added} field(s) added)`)}}await loadSfFormsAndFields();renderSfFormsCreatorList();renderFillFormOptions()});function sfFormFieldAddFormHtml(){return`
    <div class="edit-only" style="display:flex; gap:6px; align-items:center; margin-top:8px; flex-wrap:wrap;">
      <button class="btn-secondary sf-form-field-reserved-btn" data-key="seller_investor_name" data-label="Seller Name" data-type="text" data-required="true" style="width:auto; padding:4px 10px; font-size:11px;">+ Seller Name</button>
      <button class="btn-secondary sf-form-field-reserved-btn" data-key="seller_phone" data-label="Seller phone" data-type="tel" data-required="true" style="width:auto; padding:4px 10px; font-size:11px;">+ Seller phone</button>
      <button class="btn-secondary sf-form-field-reserved-btn" data-key="count" data-label="Count" data-type="number" data-required="true" style="width:auto; padding:4px 10px; font-size:11px;">+ Count</button>
    </div>
    <div class="edit-only" style="display:flex; gap:6px; align-items:center; margin-top:8px; flex-wrap:wrap;">
      <input type="text" class="sf-form-field-label-input" placeholder="Custom field label, e.g. Property address" style="flex:1; min-width:160px; padding:4px 8px; font-size:11px;">
      <select class="sf-form-field-type-select" style="padding:4px 8px; font-size:11px;">
        <option value="text">Text</option>
        <option value="tel">Phone</option>
        <option value="number">Number</option>
        <option value="textarea">Long text</option>
        <option value="select">Dropdown (single choice)</option>
        <option value="multi">Checkbox group (multiple choice)</option>
      </select>
      <label style="display:flex; align-items:center; gap:4px; cursor:pointer; text-transform:none; font-family:var(--sans); font-size:11px; color:var(--text-dim); letter-spacing:0;">
        <input type="checkbox" class="sf-form-field-required-input" style="width:14px; height:14px; accent-color:var(--focus);">
        Required
      </label>
      <button class="btn-secondary sf-form-field-add-btn" style="width:auto; padding:4px 10px; font-size:11px;">Add field</button>
    </div>
    <div class="sf-form-field-options-row edit-only" style="display:none; margin-top:6px;">
      <input type="text" class="sf-form-field-options-input" placeholder="Choices, comma-separated — e.g. Yes, No, Not sure">
    </div>
    <div class="msg sf-form-field-msg"></div>
  `}async function addSfFormField(formId,fieldsContainer,{field_key,label,field_type,options,required},onSuccess){const existing=sfFormFieldsCache[formId]||[];const nextOrder=existing.length?Math.max(...existing.map(f=>f.sort_order))+10:0;const row={form_id:formId,field_key,label,field_type,required,sort_order:nextOrder,options:options&&options.length?options:null};const{error}=await sb.from("lead_submission_form_fields").insert(row);if(error){alert(/duplicate|unique/i.test(error.message)?`This form already has a "${label}" field.`:error.message);return}logActivity("lead_submission_form_field.create",null,null,`Added field "${label}" (${fieldTypeLabel(field_type)}) to a submission form`);await loadSfFormsAndFields();renderSfFormFieldsList(formId,fieldsContainer);renderFillFormOptions();renderFillFields();if(onSuccess)onSuccess()}function renderSfFormFieldsList(formId,container){const fields=(sfFormFieldsCache[formId]||[]).slice().sort((a,b)=>a.sort_order-b.sort_order);container.innerHTML="";if(!fields.length){const hint=document.createElement("div");hint.className="empty";hint.style.cssText="padding:8px 0;";hint.textContent="No fields yet — this form has nothing to submit.";container.appendChild(hint);applyLeadsSectionEditGating("submission_forms_creator",$("sfSection-creator"));return}fields.forEach((f,i)=>{if(sfFieldEditingId===f.id){container.appendChild(buildSfFieldEditRow(f,{onSaved:async()=>{sfFieldEditingId=null;await loadSfFormsAndFields();renderSfFormFieldsList(formId,container);renderFillFormOptions();renderFillFields()},onCancel:()=>{sfFieldEditingId=null;renderSfFormFieldsList(formId,container)}}));return}const row=document.createElement("div");row.style.cssText="display:flex; align-items:center; gap:8px; padding:5px 0; border-bottom:1px solid var(--border);";const optsNote=(f.field_type==="select"||f.field_type==="multi")&&f.options&&f.options.length?`: ${f.options.slice(0,3).map(escapeHtml).join(", ")}${f.options.length>3?`, +${f.options.length-3} more`:""}`:"";row.innerHTML=`
      <span class="mono" style="font-size:12px; flex:1;">${escapeHtml(f.label)} <span style="color:var(--text-dim); font-size:10px;">(${fieldTypeLabel(f.field_type)}${f.required?", required":""}${RESERVED_FIELD_KEYS.has(f.field_key)?", maps to a fixed lead column":""}${optsNote})</span></span>
      <button class="edit-only sf-form-field-edit-btn" title="Edit field" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px;">Edit</button>
      <button class="edit-only sf-form-field-up-btn" title="Move up" ${i===0?"disabled":""} style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px;">▲</button>
      <button class="edit-only sf-form-field-down-btn" title="Move down" ${i===fields.length-1?"disabled":""} style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px;">▼</button>
      <button class="edit-only sf-form-field-del-btn" title="Remove field" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px;">×</button>
    `;row.querySelector(".sf-form-field-edit-btn").addEventListener("click",()=>{sfFieldEditingId=f.id;renderSfFormFieldsList(formId,container)});row.querySelector(".sf-form-field-del-btn").addEventListener("click",async()=>{if(!confirm(`Remove the "${f.label}" field? Values already submitted with it stay on those existing leads — this only stops it from being asked for on new submissions.`))return;await sb.from("lead_submission_form_fields").delete().eq("id",f.id);logActivity("lead_submission_form_field.delete",null,null,`Removed field "${f.label}" from a submission form`);await loadSfFormsAndFields();renderSfFormFieldsList(formId,container);renderFillFormOptions();renderFillFields()});const swapWith=async other=>{if(!other)return;await Promise.all([sb.from("lead_submission_form_fields").update({sort_order:other.sort_order}).eq("id",f.id),sb.from("lead_submission_form_fields").update({sort_order:f.sort_order}).eq("id",other.id)]);await loadSfFormsAndFields();renderSfFormFieldsList(formId,container)};row.querySelector(".sf-form-field-up-btn").addEventListener("click",()=>swapWith(fields[i-1]));row.querySelector(".sf-form-field-down-btn").addEventListener("click",()=>swapWith(fields[i+1]));container.appendChild(row)});applyLeadsSectionEditGating("submission_forms_creator",$("sfSection-creator"))}let sfFieldEditingId=null;function buildSfFieldEditRow(f,{onSaved,onCancel,onSave}){const row=document.createElement("div");row.style.cssText="display:flex; gap:6px; align-items:center; padding:6px 0; border-bottom:1px solid var(--border); flex-wrap:wrap; background:var(--inset);";row.innerHTML=`
    <input type="text" class="sf-field-edit-label" value="${escapeHtml(f.label)}" placeholder="Label" style="flex:1; min-width:140px; padding:5px 8px; font-size:11px;">
    <select class="sf-field-edit-type" style="padding:5px 8px; font-size:11px;">
      <option value="text">Text</option>
      <option value="tel">Phone</option>
      <option value="number">Number</option>
      <option value="textarea">Long text</option>
      <option value="select">Dropdown (single choice)</option>
      <option value="multi">Checkbox group (multiple choice)</option>
    </select>
    <input type="text" class="sf-field-edit-options" placeholder="Choices, comma-separated" style="flex:1; min-width:160px; padding:5px 8px; font-size:11px; display:none;">
    <label style="display:flex; align-items:center; gap:4px; cursor:pointer; text-transform:none; font-family:var(--sans); font-size:11px; color:var(--text-dim); letter-spacing:0;">
      <input type="checkbox" class="sf-field-edit-required" style="width:14px; height:14px; accent-color:var(--focus);"> Required
    </label>
    <button class="btn-secondary sf-field-edit-cancel-btn" style="width:auto; padding:5px 10px; font-size:11px;">Cancel</button>
    <button class="btn-primary sf-field-edit-save-btn" style="width:auto; padding:5px 10px; font-size:11px; margin-top:0;">Save</button>
    <div class="msg sf-field-edit-msg" style="width:100%; margin:0;"></div>
  `;const typeSel=row.querySelector(".sf-field-edit-type");const optsInput=row.querySelector(".sf-field-edit-options");typeSel.value=f.field_type;optsInput.style.display=f.field_type==="select"||f.field_type==="multi"?"":"none";optsInput.value=(f.options||[]).join(", ");row.querySelector(".sf-field-edit-required").checked=!!f.required;if(f.field_key&&RESERVED_FIELD_KEYS.has(f.field_key)){typeSel.disabled=true;typeSel.title="This field maps to a fixed lead column — its type can’t change, but its label and Required setting can."}typeSel.addEventListener("change",()=>{optsInput.style.display=typeSel.value==="select"||typeSel.value==="multi"?"":"none"});row.querySelector(".sf-field-edit-cancel-btn").addEventListener("click",()=>onCancel());row.querySelector(".sf-field-edit-save-btn").addEventListener("click",async()=>{const msg=row.querySelector(".sf-field-edit-msg");const label=row.querySelector(".sf-field-edit-label").value.trim();if(!label){msg.textContent="Label is required.";msg.className="msg sf-field-edit-msg error";return}const field_type=typeSel.value;let options=null;if(field_type==="select"||field_type==="multi"){options=optsInput.value.split(",").map(s=>s.trim()).filter(Boolean);if(!options.length){msg.textContent="Enter at least one choice, comma-separated.";msg.className="msg sf-field-edit-msg error";return}}const required=row.querySelector(".sf-field-edit-required").checked;const patch={label,field_type,options,required};if(onSave){const err=await onSave(patch);if(err){msg.textContent=err;msg.className="msg sf-field-edit-msg error";return}}else{const{error}=await sb.from("lead_submission_form_fields").update(patch).eq("id",f.id);if(error){msg.textContent=error.message;msg.className="msg sf-field-edit-msg error";return}logActivity("lead_submission_form_field.update",null,null,`Edited field "${label}" on a submission form`)}onSaved()});return row}function wireSfFormFieldAddForm(container,formId,fieldsContainer){container.querySelectorAll(".sf-form-field-reserved-btn").forEach(btn=>{btn.addEventListener("click",()=>addSfFormField(formId,fieldsContainer,{field_key:btn.dataset.key,label:btn.dataset.label,field_type:btn.dataset.type,required:btn.dataset.required==="true"}))});const typeSelect=container.querySelector(".sf-form-field-type-select");const optionsRow=container.querySelector(".sf-form-field-options-row");typeSelect.addEventListener("change",()=>{optionsRow.style.display=typeSelect.value==="select"||typeSelect.value==="multi"?"":"none"});container.querySelector(".sf-form-field-add-btn").addEventListener("click",()=>{const labelInput=container.querySelector(".sf-form-field-label-input");const requiredInput=container.querySelector(".sf-form-field-required-input");const optionsInput=container.querySelector(".sf-form-field-options-input");const msg=container.querySelector(".sf-form-field-msg");const label=(labelInput.value||"").trim();msg.textContent="";msg.className="msg sf-form-field-msg";if(!label){msg.textContent="Enter a label for the custom field.";msg.className="msg sf-form-field-msg error";return}const key=slugifyFieldKey(label);if(!key||RESERVED_FIELD_KEYS.has(key)){msg.textContent="That label produces a reserved or empty field key — try a more specific label.";msg.className="msg sf-form-field-msg error";return}const fieldType=typeSelect.value;let options=null;if(fieldType==="select"||fieldType==="multi"){options=(optionsInput.value||"").split(",").map(s=>s.trim()).filter(Boolean);if(!options.length){msg.textContent="Enter at least one choice, comma-separated.";msg.className="msg sf-form-field-msg error";return}}addSfFormField(formId,fieldsContainer,{field_key:key,label,field_type:fieldType,options,required:requiredInput.checked},()=>{labelInput.value="";requiredInput.checked=false;optionsInput.value=""})})}const DEFAULT_TEMPLATE_FINAL_NAME="Default Template Final";function sfFindDefaultTemplateFinal(){return sfAllTemplates.find(t=>(t.name||"").trim().toLowerCase()===DEFAULT_TEMPLATE_FINAL_NAME.toLowerCase())||null}async function sfLoadDefaultTemplateFinal(formId,fieldsContainer,msgEl){const template=sfFindDefaultTemplateFinal();if(!template){msgEl.textContent=`No template named "${DEFAULT_TEMPLATE_FINAL_NAME}" exists yet — save one with that exact name first (any form's "Save this form's fields as a template" button below), then this button will load it.`;msgEl.className="msg sf-form-ispd-msg error";return}if(!confirm(`Add every field from the "${DEFAULT_TEMPLATE_FINAL_NAME}" template to this form (each field's Required setting comes from however the template itself has it set)? Fields already on this form are left as-is; duplicates are skipped.`))return;await sfApplyTemplate(formId,template.id,fieldsContainer,msgEl)}let sfFormsFilters={name:"",status:""};if($("sfFormsFilterInput"))$("sfFormsFilterInput").addEventListener("input",debounce(()=>{sfFormsFilters.name=$("sfFormsFilterInput").value.trim();renderSfFormsCreatorList()},200));if($("sfFormsStatusFilter"))$("sfFormsStatusFilter").addEventListener("change",()=>{sfFormsFilters.status=$("sfFormsStatusFilter").value;renderSfFormsCreatorList()});function renderSfFormsCreatorList(){const list=$("sfFormsList");const filtered=sfAllForms.filter(f=>{if(sfFormsFilters.name&&!f.name.toLowerCase().includes(sfFormsFilters.name.toLowerCase()))return false;if(sfFormsFilters.status==="active"&&!f.is_active)return false;if(sfFormsFilters.status==="inactive"&&f.is_active)return false;return true});if(!sfAllForms.length){list.innerHTML='<div class="empty">No submission forms yet — add one above.</div>';return}if(!filtered.length){list.innerHTML='<div class="empty">No forms match these filters.</div>';return}list.innerHTML="";filtered.forEach(f=>{const row=document.createElement("div");row.className="card";row.style.cssText="margin-bottom:12px;"+(f.is_active?"":" opacity:0.55;");row.innerHTML=`
      <div style="display:flex; align-items:center; gap:10px; flex-wrap:wrap;">
        <span class="mono" style="font-size:13px; font-weight:700; flex:1;">${escapeHtml(f.name)}${!f.is_active?' <span style="font-size:10px; color:var(--text-dim); font-weight:400;">(inactive — hidden from Lead Submission)</span>':""}</span>
        <button class="btn-secondary edit-only sf-form-ispd-btn" style="width:auto; padding:6px 12px; font-size:11px;">Load from Default Template Final</button>
        <button class="btn-secondary edit-only sf-form-toggle-btn" style="width:auto; padding:6px 12px; font-size:11px;">${f.is_active?"Deactivate":"Activate"}</button>
        <button class="edit-only sf-form-del-btn" title="Delete form" style="background:transparent; border:none; color:var(--danger); cursor:pointer; font-size:12px;">Delete</button>
      </div>
      <div style="margin-top:10px; padding-top:10px; border-top:1px solid var(--border);">
        <div style="font-size:10px; color:var(--text-dim); text-transform:uppercase; letter-spacing:0.5px; margin-bottom:6px;">Visible to — user types</div>
        <div class="sf-form-roles-list" style="display:flex; gap:14px; flex-wrap:wrap; margin-bottom:4px;"></div>
        <div style="font-size:10px; color:var(--text-dim); margin-bottom:12px;">No types checked = visible to every type that can submit a lead at all (Agent isn't listed here — assigned individually below instead). Owner/Admin can always see every form regardless.</div>
        <div style="font-size:10px; color:var(--text-dim); text-transform:uppercase; letter-spacing:0.5px; margin-bottom:6px;">Visible to — agents</div>
        <div class="sf-form-agents-list" style="display:flex; flex-direction:column; gap:4px; max-height:200px; overflow-y:auto; padding:8px 10px; border:1px solid var(--border-light); border-radius:8px; background:var(--inset); margin-bottom:4px;"></div>
        <div style="font-size:10px; color:var(--text-dim);">No agents checked = visible to every agent. Check specific agents to limit this form to just them.</div>
      </div>
      <div style="margin-top:10px; padding-top:10px; border-top:1px solid var(--border);">
        <div style="display:flex; gap:8px; align-items:center; flex-wrap:wrap; margin-bottom:10px;">
          <select class="sf-form-template-select edit-only" style="max-width:220px;"><option value="">Apply a template…</option>${sfAllTemplates.map(t=>`<option value="${t.id}">${escapeHtml(t.name)}</option>`).join("")}</select>
          <button class="btn-secondary edit-only sf-form-template-apply-btn" style="width:auto; padding:6px 12px; font-size:11px;">Apply</button>
          <button class="btn-secondary edit-only sf-form-template-save-btn" style="width:auto; padding:6px 12px; font-size:11px;">Save this form's fields as a template</button>
        </div>
        <div class="msg sf-form-template-msg"></div>
        <div class="cat-fields-list sf-form-fields-list"></div>
        <div class="sf-form-field-add-wrap"></div>
        <div class="msg sf-form-ispd-msg"></div>
      </div>
    `;list.appendChild(row);renderSfFormRolesList(f,row.querySelector(".sf-form-roles-list"));renderSfFormAgentsList(f,row.querySelector(".sf-form-agents-list"));const fieldsContainer=row.querySelector(".sf-form-fields-list");renderSfFormFieldsList(f.id,fieldsContainer);const addWrap=row.querySelector(".sf-form-field-add-wrap");addWrap.innerHTML=sfFormFieldAddFormHtml();wireSfFormFieldAddForm(addWrap,f.id,fieldsContainer);row.querySelector(".sf-form-ispd-btn").addEventListener("click",()=>sfLoadDefaultTemplateFinal(f.id,fieldsContainer,row.querySelector(".sf-form-ispd-msg")));const templateMsg=row.querySelector(".sf-form-template-msg");row.querySelector(".sf-form-template-apply-btn").addEventListener("click",()=>{const templateId=row.querySelector(".sf-form-template-select").value;if(!templateId){templateMsg.textContent="Pick a template first.";templateMsg.className="msg sf-form-template-msg error";return}sfApplyTemplate(f.id,templateId,fieldsContainer,templateMsg)});row.querySelector(".sf-form-template-save-btn").addEventListener("click",()=>sfSaveAsTemplate(f.id,f.name,templateMsg));row.querySelector(".sf-form-toggle-btn").addEventListener("click",async()=>{await sb.from("lead_submission_forms").update({is_active:!f.is_active}).eq("id",f.id);logActivity("lead_submission_form.update",null,null,`${f.is_active?"Deactivated":"Activated"} submission form "${f.name}"`);await loadSfFormsAndFields();renderSfFormsCreatorList();renderFillFormOptions();renderFillFields()});row.querySelector(".sf-form-del-btn").addEventListener("click",async()=>{if(!confirm(`Delete the "${f.name}" form? Its fields go with it. Leads already submitted through it keep their data — this only removes it from the picker going forward.`))return;const{error}=await sb.from("lead_submission_forms").delete().eq("id",f.id);if(error){alert(`Couldn't delete "${f.name}": ${error.message}`);return}logActivity("lead_submission_form.delete",null,null,`Deleted submission form "${f.name}"`);await loadSfFormsAndFields();renderSfFormsCreatorList();renderFillFormOptions();renderFillFields()})});applyLeadsSectionEditGating("submission_forms_creator",$("sfSection-creator"))}function renderSfFormRolesList(f,container){const allowed=sfFormRolesCache[f.id]||new Set;if(!sfAssignableRoles.length){container.innerHTML='<span class="empty" style="font-size:11px;">No user types to list yet — add one under User Management → User Types.</span>';return}container.innerHTML=sfAssignableRoles.map(r=>`
    <label class="edit-only" style="display:flex; align-items:center; gap:5px; cursor:pointer; text-transform:none; font-family:var(--sans); font-size:12px; color:var(--text-2); letter-spacing:0;">
      <input type="checkbox" class="sf-form-role-cb" data-role="${escapeHtml(r.name)}" ${allowed.has(r.name)?"checked":""} style="width:13px; height:13px; accent-color:var(--focus);"> ${escapeHtml(r.name)}
    </label>
  `).join("");container.querySelectorAll(".sf-form-role-cb").forEach(cb=>{cb.addEventListener("change",async()=>{const role=cb.dataset.role;if(cb.checked){const{error}=await sb.from("lead_submission_form_roles").insert({form_id:f.id,role});if(error){alert(error.message);cb.checked=false;return}logActivity("lead_submission_form.update",null,null,`Made submission form "${f.name}" visible to role "${role}"`)}else{await sb.from("lead_submission_form_roles").delete().eq("form_id",f.id).eq("role",role);logActivity("lead_submission_form.update",null,null,`Removed role "${role}" from submission form "${f.name}"'s allow-list`)}await loadSfFormsAndFields();renderFillFormOptions();renderFillFields()})});applyLeadsSectionEditGating("submission_forms_creator",$("sfSection-creator"))}async function sfApplyTemplate(formId,templateId,fieldsContainer,msgEl){const template=sfAllTemplates.find(t=>t.id===templateId);if(!template)return;const existing=sfFormFieldsCache[formId]||[];const existingKeys=new Set(existing.map(f=>f.field_key));let sortOrder=existing.reduce((m,f)=>Math.max(m,f.sort_order),-10)+10;let added=0,skipped=0;for(const tf of template.fields||[]){if(existingKeys.has(tf.field_key)){skipped++;continue}const{error}=await sb.from("lead_submission_form_fields").insert({form_id:formId,field_key:tf.field_key,label:tf.label,field_type:tf.field_type,options:tf.options||null,required:!!tf.required,placeholder:tf.placeholder||null,sort_order:sortOrder});sortOrder+=10;if(!error)added++;else skipped++}logActivity("lead_submission_form_field.apply_template",null,null,`Applied template "${template.name}" to a submission form (${added} added, ${skipped} skipped)`);msgEl.textContent=`Applied "${template.name}": ${added} field(s) added${skipped?`, ${skipped} skipped (already present or failed)`:""}.`;msgEl.className="msg";await loadSfFormsAndFields();renderSfFormFieldsList(formId,fieldsContainer);renderFillFormOptions();renderFillFields()}async function sfSaveAsTemplate(formId,formName,msgEl){const fields=sfFormFieldsCache[formId]||[];if(!fields.length){msgEl.textContent="This form has no fields yet — nothing to save as a template.";msgEl.className="msg sf-form-template-msg error";return}const name=prompt(`Save "${formName}"'s ${fields.length} field(s) as a template — name it:`,`${formName} template`);if(!name||!name.trim())return;const snapshot=fields.map(f=>({field_key:f.field_key,label:f.label,field_type:f.field_type,options:f.options||null,required:!!f.required,placeholder:f.placeholder||null}));const{error}=await sb.from("lead_submission_field_templates").insert({name:name.trim(),fields:snapshot});if(error){msgEl.textContent=/duplicate|unique/i.test(error.message)?"A template with that name already exists — pick a different name.":error.message;msgEl.className="msg sf-form-template-msg error";return}logActivity("lead_submission_field_template.create",null,null,`Saved template "${name.trim()}" (${snapshot.length} fields) from submission form "${formName}"`);msgEl.textContent=`Saved as template "${name.trim()}".`;msgEl.className="msg sf-form-template-msg";await loadSfFormsAndFields();renderSfFormsCreatorList();renderSfTemplatesList()}let sfTemplateExpandedIds=new Set;let sfTemplateFieldEditingKey=null;async function sfSaveTemplateFields(template,newFields){const{error}=await sb.from("lead_submission_field_templates").update({fields:newFields}).eq("id",template.id);if(error)return error.message;template.fields=newFields;logActivity("lead_submission_field_template.update",null,null,`Edited fields on template "${template.name}"`);return null}function renderSfTemplatesList(){const list=$("sfTemplatesList");if(!list)return;if(!sfAllTemplates.length){list.innerHTML='<div class="empty">No templates saved yet — save one from any form below.</div>';return}list.innerHTML="";sfAllTemplates.forEach(t=>{const wrap=document.createElement("div");wrap.style.cssText="padding:6px 0; border-bottom:1px solid var(--border);";const expanded=sfTemplateExpandedIds.has(t.id);const header=document.createElement("div");header.style.cssText="display:flex; align-items:center; gap:10px;";header.innerHTML=`
      <span class="mono" style="font-size:12px; flex:1;">${escapeHtml(t.name)} <span style="color:var(--text-dim); font-size:10px;">(${(t.fields||[]).length} field${(t.fields||[]).length===1?"":"s"})</span></span>
      <button class="edit-only sf-template-manage-btn" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px;">${expanded?"Hide fields":"Manage fields"}</button>
      <button class="edit-only sf-template-del-btn" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px;">Delete</button>
    `;header.querySelector(".sf-template-manage-btn").addEventListener("click",()=>{if(expanded)sfTemplateExpandedIds.delete(t.id);else sfTemplateExpandedIds.add(t.id);renderSfTemplatesList()});header.querySelector(".sf-template-del-btn").addEventListener("click",async()=>{if(!confirm(`Delete the "${t.name}" template? Forms it's already been applied to keep their fields — this only removes it from the "Apply a template…" list going forward.`))return;const{error}=await sb.from("lead_submission_field_templates").delete().eq("id",t.id);if(error){alert(`Couldn't delete "${t.name}": ${error.message}`);return}logActivity("lead_submission_field_template.delete",null,null,`Deleted template "${t.name}"`);await loadSfFormsAndFields();renderSfFormsCreatorList();renderSfTemplatesList()});wrap.appendChild(header);if(expanded){const fieldsWrap=document.createElement("div");fieldsWrap.style.cssText="margin:8px 0 0 0; padding-left:4px;";const fields=t.fields||[];fields.forEach((f,i)=>{const key=`${t.id}:${i}`;if(sfTemplateFieldEditingKey===key){fieldsWrap.appendChild(buildSfFieldEditRow(f,{onCancel:()=>{sfTemplateFieldEditingKey=null;renderSfTemplatesList()},onSave:async patch=>{const newFields=fields.slice();newFields[i]={...f,...patch};return await sfSaveTemplateFields(t,newFields)},onSaved:()=>{sfTemplateFieldEditingKey=null;renderSfTemplatesList()}}));return}const optsNote=(f.field_type==="select"||f.field_type==="multi")&&f.options&&f.options.length?`: ${f.options.slice(0,3).map(escapeHtml).join(", ")}${f.options.length>3?`, +${f.options.length-3} more`:""}`:"";const row=document.createElement("div");row.style.cssText="display:flex; align-items:center; gap:8px; padding:4px 0; border-top:1px solid var(--border);";row.innerHTML=`
          <span class="mono" style="font-size:12px; flex:1;">${escapeHtml(f.label)} <span style="color:var(--text-dim); font-size:10px;">(${fieldTypeLabel(f.field_type)}${f.required?", required":""}${optsNote})</span></span>
          <button class="edit-only sf-template-field-edit-btn" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px;">Edit</button>
          <button class="edit-only sf-template-field-del-btn" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:12px;">×</button>
        `;row.querySelector(".sf-template-field-edit-btn").addEventListener("click",()=>{sfTemplateFieldEditingKey=key;renderSfTemplatesList()});row.querySelector(".sf-template-field-del-btn").addEventListener("click",async()=>{if(!confirm(`Remove "${f.label}" from the "${t.name}" template? This only changes the template — forms it's already been applied to are unaffected.`))return;const newFields=fields.filter((_,idx)=>idx!==i);const err=await sfSaveTemplateFields(t,newFields);if(err)alert(err);renderSfFormsCreatorList();renderSfTemplatesList()});fieldsWrap.appendChild(row)});if(!fields.length){const empty=document.createElement("div");empty.className="empty";empty.style.padding="4px 0";empty.textContent="No fields on this template yet — add one below.";fieldsWrap.appendChild(empty)}const addRow=document.createElement("div");addRow.className="edit-only";addRow.style.cssText="margin-top:8px;";addRow.appendChild((()=>{const el=document.createElement("div");el.innerHTML=sfFormFieldAddFormHtml();return el})());fieldsWrap.appendChild(addRow);wireSfTemplateFieldAddForm(addRow,t,fieldsWrap);wrap.appendChild(fieldsWrap)}list.appendChild(wrap)});applyLeadsSectionEditGating("submission_forms_creator",$("sfSection-creator"))}function wireSfTemplateFieldAddForm(container,template,fieldsWrapForRerender){container.querySelectorAll(".sf-form-field-reserved-btn").forEach(btn=>{btn.addEventListener("click",async()=>{const newFields=(template.fields||[]).slice();if(newFields.some(f2=>f2.field_key===btn.dataset.key)){alert("This template already has that field.");return}newFields.push({field_key:btn.dataset.key,label:btn.dataset.label,field_type:btn.dataset.type,required:btn.dataset.required==="true",options:null});const err=await sfSaveTemplateFields(template,newFields);if(err){alert(err);return}renderSfFormsCreatorList();renderSfTemplatesList()})});const typeSelect=container.querySelector(".sf-form-field-type-select");const optionsRow=container.querySelector(".sf-form-field-options-row");typeSelect.addEventListener("change",()=>{optionsRow.style.display=typeSelect.value==="select"||typeSelect.value==="multi"?"":"none"});container.querySelector(".sf-form-field-add-btn").addEventListener("click",async()=>{const labelInput=container.querySelector(".sf-form-field-label-input");const requiredInput=container.querySelector(".sf-form-field-required-input");const optionsInput=container.querySelector(".sf-form-field-options-input");const msg=container.querySelector(".sf-form-field-msg");const label=(labelInput.value||"").trim();msg.textContent="";msg.className="msg sf-form-field-msg";if(!label){msg.textContent="Enter a label for the custom field.";msg.className="msg sf-form-field-msg error";return}const existingKeys=new Set((template.fields||[]).map(f=>f.field_key));let key=slugifyFieldKey(label);if(!key||RESERVED_FIELD_KEYS.has(key)){msg.textContent="That label produces a reserved or empty field key — try a more specific label.";msg.className="msg sf-form-field-msg error";return}let suffix=1;while(existingKeys.has(key))key=`${slugifyFieldKey(label)}_${++suffix}`;const fieldType=typeSelect.value;let options=null;if(fieldType==="select"||fieldType==="multi"){options=(optionsInput.value||"").split(",").map(s=>s.trim()).filter(Boolean);if(!options.length){msg.textContent="Enter at least one choice, comma-separated.";msg.className="msg sf-form-field-msg error";return}}const newFields=(template.fields||[]).slice();newFields.push({field_key:key,label,field_type:fieldType,required:requiredInput.checked,options});const err=await sfSaveTemplateFields(template,newFields);if(err){msg.textContent=err;msg.className="msg sf-form-field-msg error";return}logActivity("lead_submission_field_template.update",null,null,`Added field "${label}" to template "${template.name}"`);renderSfFormsCreatorList();renderSfTemplatesList()})}function renderSfFormAgentsList(f,container){const allowed=sfFormAgentsCache[f.id]||new Set;if(!sfAllAgentProfiles.length){container.innerHTML='<span class="empty" style="font-size:11px;">No agents to list yet.</span>';return}container.innerHTML=sfAllAgentProfiles.map(a=>`
    <label class="edit-only" style="display:flex; align-items:center; gap:6px; cursor:pointer; text-transform:none; font-family:var(--sans); font-size:12px; color:var(--text-2); letter-spacing:0;">
      <input type="checkbox" class="sf-form-agent-cb" data-user-id="${a.id}" ${allowed.has(a.id)?"checked":""} style="width:13px; height:13px; accent-color:var(--focus);"> ${escapeHtml(a.full_name||a.email)}
    </label>
  `).join("");container.querySelectorAll(".sf-form-agent-cb").forEach(cb=>{cb.addEventListener("change",async()=>{const userId=cb.dataset.userId;const agentLabel=(sfAllAgentProfiles.find(a=>a.id===userId)||{}).full_name||userId;if(cb.checked){const{error}=await sb.from("lead_submission_form_agents").insert({form_id:f.id,user_id:userId});if(error){alert(error.message);cb.checked=false;return}logActivity("lead_submission_form.update",null,null,`Made submission form "${f.name}" visible to agent "${agentLabel}"`)}else{await sb.from("lead_submission_form_agents").delete().eq("form_id",f.id).eq("user_id",userId);logActivity("lead_submission_form.update",null,null,`Removed agent "${agentLabel}" from submission form "${f.name}"'s allow-list`)}await loadSfFormsAndFields();renderFillFormOptions();renderFillFields()})});applyLeadsSectionEditGating("submission_forms_creator",$("sfSection-creator"))}function isCampaignNameField(f){return f.field_type==="text"&&csubNormalizeLabel(f.label)===csubNormalizeLabel("Campaign Name")}function fillFieldCriteriaText(f){if(f.field_type==="select")return`Choose one: ${(f.options||[]).join(", ")}`;if(f.field_type==="multi")return`Select at least one: ${(f.options||[]).join(", ")}`;if(f.field_type==="tel")return"Phone number";if(f.field_type==="number")return"Number";if(f.field_type==="textarea")return"Free text";if(isCampaignNameField(f))return"Must be an existing campaign — search and pick one";return f.placeholder?`e.g. ${f.placeholder}`:"Free text"}function fillFieldInputHtml(f){const req=f.required?" *":"";const labelHtml=`<label style="display:block; font-size:11px; color:var(--text-dim); margin-bottom:4px;">${escapeHtml(f.label)}${req}</label>`;const criteriaHtml=`<div style="font-size:10px; color:var(--text-dim); margin-top:3px;">${escapeHtml(fillFieldCriteriaText(f))}</div>`;if(f.field_type==="select"){const opts=(f.options||[]).map(o=>`<option value="${escapeHtml(o)}">${escapeHtml(o)}</option>`).join("");return`${labelHtml}<select data-field-key="${f.field_key}" data-required="${f.required?"1":""}" data-field-label="${escapeHtml(f.label)}"><option value="">—</option>${opts}</select>${criteriaHtml}`}if(f.field_type==="textarea"){return`${labelHtml}<textarea rows="2" data-field-key="${f.field_key}" data-required="${f.required?"1":""}" data-field-label="${escapeHtml(f.label)}" placeholder="${escapeHtml(f.placeholder||"")}"></textarea>`}if(f.field_type==="multi"){const boxes=(f.options||[]).map(o=>`
      <label style="display:flex; align-items:center; gap:6px; text-transform:none; font-weight:400; font-size:12px; margin:2px 0; cursor:pointer;">
        <input type="checkbox" value="${escapeHtml(o)}" style="width:auto;"> ${escapeHtml(o)}
      </label>`).join("");return`${labelHtml}<div data-field-key="${f.field_key}" data-field-type="multi" data-required="${f.required?"1":""}" data-field-label="${escapeHtml(f.label)}" style="border:1px solid var(--border-light); border-radius:8px; padding:8px 10px; max-height:160px; overflow-y:auto; background:var(--inset);">${boxes}</div>`}if(isCampaignNameField(f)){return`${labelHtml}<div class="searchable-select">
      <input type="text" data-field-key="${f.field_key}" data-required="${f.required?"1":""}" data-field-label="${escapeHtml(f.label)}" data-campaign-autocomplete="1" autocomplete="off" placeholder="${escapeHtml(f.placeholder||"Search campaigns…")}">
      <div class="searchable-select-list" data-campaign-list-for="${escapeHtml(f.field_key)}"></div>
    </div>${criteriaHtml}`}const inputType=f.field_type==="tel"?"tel":f.field_type==="number"?"number":"text";return`${labelHtml}<input type="${inputType}" data-field-key="${f.field_key}" data-required="${f.required?"1":""}" data-field-label="${escapeHtml(f.label)}" placeholder="${escapeHtml(f.placeholder||"")}">${criteriaHtml}`}async function wireCampaignNameAutocompletes(container){const inputs=Array.from(container.querySelectorAll('input[data-campaign-autocomplete="1"]'));if(!inputs.length)return;const{data}=await sb.from("lead_submission_forms").select("name, is_active").eq("is_active",true).order("name");const names=(data||[]).filter(f=>f.name!=="Ticket").map(f=>f.name);inputs.forEach(input=>{const list=container.querySelector(`[data-campaign-list-for="${CSS.escape(input.dataset.fieldKey)}"]`);if(!list)return;input.dataset.campaignValidNames=JSON.stringify(names);function renderList(filterText){const q=(filterText||"").trim().toLowerCase();const matches=q?names.filter(n=>n.toLowerCase().includes(q)):names;list.innerHTML=matches.length?matches.map(n=>`<div class="searchable-select-option" data-value="${escapeHtml(n)}">${escapeHtml(n)}</div>`).join(""):`<div class="searchable-select-option empty">No matching campaigns.</div>`;list.querySelectorAll(".searchable-select-option[data-value]").forEach(el=>{el.addEventListener("mousedown",e=>{e.preventDefault();input.value=el.dataset.value;list.classList.remove("show")})})}input.addEventListener("input",()=>{renderList(input.value);list.classList.add("show")});input.addEventListener("focus",()=>{renderList(input.value);list.classList.add("show")});input.addEventListener("blur",()=>{setTimeout(()=>list.classList.remove("show"),120)})})}function campaignNameFieldIsValid(el){const names=JSON.parse(el.dataset.campaignValidNames||"[]");const v=el.value.trim().toLowerCase();return names.some(n=>n.toLowerCase()===v)}function renderFillFormOptions(){const sel=$("lfFillFormSelect");const prev=sel.value;const visible=sfAllForms.filter(f=>f.is_active&&sfFormVisibleToMe(f.id));sel.innerHTML='<option value="">Select a form…</option>'+visible.map(f=>`<option value="${f.id}">${escapeHtml(f.name)}</option>`).join("");if(visible.some(f=>f.id===prev))sel.value=prev}$("lfFillFormSelect").addEventListener("change",renderFillFields);let sfFillFieldDefByKey={};function renderFillFields(){const sel=$("lfFillFormSelect");const container=$("lfFillFields");const formId=sel.value;const fields=(sfFormFieldsCache[formId]||[]).slice().sort((a,b)=>a.sort_order-b.sort_order);sfFillFieldDefByKey={};fields.forEach(f=>{sfFillFieldDefByKey[f.field_key]=f});container.innerHTML="";$("lfFillMsg").style.display="none";if(!formId){container.innerHTML='<div class="empty">Pick a form above to see its fields.</div>';return}if(!fields.length){container.innerHTML='<div class="empty">This form has no fields configured yet — an authorized role can add some under Submission Forms Creator.</div>';return}container.innerHTML=fields.map(f=>`<div style="margin-bottom:12px;">${fillFieldInputHtml(f)}</div>`).join("");wireCampaignNameAutocompletes(container);if(formId===leadsPrimeHomeBuyersFormId){const phoneInput=container.querySelector('input[data-field-key="seller_phone"]');wireAddressAutocompleteAndDupCheck(container,{getPhoneNorm:()=>(phoneInput?phoneInput.value:"").replace(/\D/g,"").slice(-10),phoneInput})}wireGenericAddressAutocomplete(container)}$("lfFillSubmitBtn").addEventListener("click",async()=>{const msg=$("lfFillMsg");msg.style.display="none";msg.innerHTML="";try{if($("lfFillSubmitBtn").disabled){msg.textContent="You don’t have permission to submit through this form — see the note above the button.";msg.className="msg error";msg.style.display="block";return}const formId=$("lfFillFormSelect").value;if(!formId){msg.textContent="Select a form first.";msg.className="msg error";msg.style.display="block";return}const fieldEls=Array.from($("lfFillFields").querySelectorAll("[data-field-key]"));const problems=[];fieldEls.forEach(el=>{if(!el.dataset.required)return;const f=sfFillFieldDefByKey[el.dataset.fieldKey]||{field_type:el.dataset.fieldType||"text"};const label=el.dataset.fieldLabel||el.dataset.fieldKey;const criteria=fillFieldCriteriaText(f);if(el.dataset.fieldType==="multi"){if(el.querySelectorAll('input[type="checkbox"]:checked').length===0)problems.push(`"${label}" is required — ${criteria}.`)}else if(!el.value.trim()){problems.push(`"${label}" is required — ${criteria}.`)}});fieldEls.forEach(el=>{if(el.dataset.campaignAutocomplete!=="1"||!el.value.trim())return;if(!campaignNameFieldIsValid(el)){const label=el.dataset.fieldLabel||el.dataset.fieldKey;problems.push(`"${label}" must be an existing campaign — pick one from the list instead of typing a new name.`)}});if(problems.length){msg.innerHTML=`<div style="margin-bottom:4px; color:var(--danger);">Fix ${problems.length===1?"this":`these ${problems.length}`} before submitting:</div><ul style="margin:0; padding-left:18px; color:var(--danger);">${problems.map(p=>`<li>${escapeHtml(p)}</li>`).join("")}</ul>`;msg.className="msg";msg.style.display="block";return}let count=1,sellerInvestor="",sellerPhone="";const extraFields={};fieldEls.forEach(el=>{const key=el.dataset.fieldKey;const value=el.dataset.fieldType==="multi"?Array.from(el.querySelectorAll('input[type="checkbox"]:checked')).map(cb=>cb.value).join("\n"):el.value.trim();if(key==="count"){const n=parseInt(value,10);if(n>0)count=n}else if(key==="seller_investor_name")sellerInvestor=value;else if(key==="seller_phone")sellerPhone=value;else if(value)extraFields[key]=value});const{data:{user},error:userErr}=await sb.auth.getUser();if(userErr||!user){msg.textContent="Not signed in — refresh the page and sign in again.";msg.className="msg error";msg.style.display="block";return}if(formId===leadsPrimeHomeBuyersFormId){const phoneNorm=(sellerPhone||"").replace(/\D/g,"").slice(-10);const dup=await findPrimeHomeBuyersDuplicate(phoneNorm,extraFields.form_address);if(dup&&dup.matched_via==="address"){msg.textContent=`Not submitted — this looks like a duplicate. A PrimeHome Buyers lead for this exact address already exists (status: ${dup.status}, submitted ${new Date(dup.submitted_at).toLocaleDateString()}).`;msg.className="msg error";msg.style.display="block";return}if(dup&&dup.matched_via==="phone"){const priorAddress=(dup.extra_fields||{}).form_address||"no address on file for that one";const proceed=confirm(`This phone number already has a PrimeHome Buyers lead on file for a DIFFERENT address:

${priorAddress}
(status: ${dup.status}, submitted ${new Date(dup.submitted_at).toLocaleDateString()})

If this is genuinely a different property, click OK to submit anyway. If it's the same property, click Cancel.`);if(!proceed){msg.textContent="Not submitted.";msg.className="msg error";msg.style.display="block";return}}}const{error}=await sb.from("leads").insert({user_id:user.id,submission_form_id:formId,count,seller_investor_name:sellerInvestor||null,seller_phone:sellerPhone||null,extra_fields:Object.keys(extraFields).length?extraFields:null});if(error){msg.textContent=`Couldn't submit: ${error.message}`;msg.className="msg error";msg.style.display="block";return}fieldEls.forEach(el=>{if(el.dataset.fieldType==="multi")el.querySelectorAll('input[type="checkbox"]').forEach(cb=>{cb.checked=false});else el.value=""});msg.textContent="Lead submitted.";msg.className="msg";msg.style.display="block";const formLabel=$("lfFillFormSelect").selectedOptions[0]?$("lfFillFormSelect").selectedOptions[0].textContent:formId;logActivity("lead.create","lead",null,`Submitted a lead via the dashboard — form: ${formLabel}`)}catch(e){msg.textContent=`Something went wrong: ${e&&e.message?e.message:e}`;msg.className="msg error";msg.style.display="block"}});const ISPD_UPLOAD_SPECIAL_COLUMNS=[{header:"Phone",key:"__phone"},{header:"Seller Name (new leads only)",key:"__seller_name"},{header:"Count (new leads only)",key:"__count"}];function ispdUploadFieldColumns(){const cols=[];ISPEEDTOLEAD_FIELD_GROUPS.forEach(group=>group.fields.forEach(f=>cols.push({header:f.label,key:f.key})));return cols}function ispdAllUploadColumns(){return[...ISPD_UPLOAD_SPECIAL_COLUMNS,...ispdUploadFieldColumns()]}const ISPD_DEFAULT_CAMPAIGN_NAME="PrimeHome Buyers";const TICKET_FORM_NAME="Ticket";let ispdDefaultCampaignFormIdCache=null;async function getIspdDefaultCampaignFormId(){if(ispdDefaultCampaignFormIdCache)return ispdDefaultCampaignFormIdCache;const cached=(leadsAllForms||[]).find(f=>f.name===ISPD_DEFAULT_CAMPAIGN_NAME);if(cached){ispdDefaultCampaignFormIdCache=cached.id;return cached.id}const{data:existing}=await sb.from("lead_submission_forms").select("id").eq("name",ISPD_DEFAULT_CAMPAIGN_NAME).limit(1).maybeSingle();if(existing){ispdDefaultCampaignFormIdCache=existing.id;return existing.id}const{data:created,error}=await sb.from("lead_submission_forms").insert({name:ISPD_DEFAULT_CAMPAIGN_NAME}).select("id").maybeSingle();if(error||!created)return null;ispdDefaultCampaignFormIdCache=created.id;return created.id}const ISPD_RAW_FORMAT_HEADERS=["Timestamp","Seller Name","Phone Number","Property Address","Condition","Occupancy","Asking Price","Market Value","Reason","Closing","Additional Notes","Email","Call Recording"];function ispdNormHeader(s){return String(s||"").trim().toLowerCase().replace(/\s+/g," ")}function ispdDetectRawFormat(headerRow){const norm=headerRow.map(ispdNormHeader);const hasPhoneColumn=norm.includes("phone number")||norm.includes("phone");const otherMatches=ISPD_RAW_FORMAT_HEADERS.filter(h=>h!=="Phone Number"&&norm.includes(ispdNormHeader(h))).length;return otherMatches+(hasPhoneColumn?1:0)>=6}const ISPD_RAW_DEFAULT_OWNER="Yes, i own this property";const ISPD_RAW_OCCUPANCY_MAP={owner:"Owner Occupied",rented:"Tenant Occupied",vacant:"No its Vacant"};const ISPD_RAW_REPAIRS_RULES=[[["total rehab","major repair","full repair","gut","alot of work","a lot of work"],"Full Gut Renovation - Everything"],[["some repair","minor repair","need repair","needs repair","need some work","needs some work","needs work","fair condition","fair codnition","some repars","needs total rehab"],"Remodel - Kitchen, Bathroom, Roof"],[["cosmetic","needs to be cleand","needs a new paint"],"Cosmetic - Flooring, Paint"],[["good condition","good shape","good nothing major","no need to repair","it's a good condition","in a good shape","goood","good"],"Fully renovated and updated in past 2 years"]];const ISPD_RAW_GOAL_RULES=[[["preforeclos","foreclos"],"Preforeclosure"],[["behind on tax","behind on mortgage","back taxes"],"Behind on taxes/mortgage"],[["emergency"],"Emergency Reasons"],[["financial"],"Financial Reasons"],[["vacant"],"Selling a vacant/non-occupied property"],[["rent instead","sell and rent"],"Sell and rent instead"],[["death","passed away","she passed","he passed","deceased"],"Death in the family"],[["without showing"],"Sell without showings"],[["inherit"],"Inherited Property"],[["downsiz"],"Downsizing"],[["tired of being","landlord"],"Tired of being a landlord"],[["closer to family","close to family"],"Moving closer to family"],[["relocat","moving out","moving"],"Relocating"],[["retire","retirment","retirement"],"Retirement elsewhere"],[["upsiz","upgrad"],"Upgrading"],[["leaving the us","leaving the country","moving from the us"],"Moving from United States"],[["old age","elder","getting old"],"Old age"],[["divorce"],"Divorce"],[["health","medical"],"Health issues"],[["job loss","lost job","laid off"],"Job loss"]];const ISPD_RAW_GOAL_FALLBACK="Other personal reason";function ispdRawMapSellFast(raw){const s=String(raw||"").trim().toLowerCase();if(!s)return"";if(/asap|today|immediat/.test(s))return"ASAP";const m=s.match(/(\d+(?:\.\d+)?)\s*(day|week|month)/);if(m){const n=parseFloat(m[1]);const unit=m[2];const months=unit==="day"?n/30:unit==="week"?n/4.33:n;if(months<=1.4)return"1 month";if(months<=3.5)return"2-3 months";if(months<=5.5)return"4-5 months";return"6+ months"}return""}function ispdRawMapListed(notes){const m=String(notes||"").match(/listed\s*[:\-]?\s*([^\n]{0,40})/i);if(!m)return"";const val=m[1].toLowerCase();if(/bed|condition|sqft|bath/.test(val))return"";if(/not|off market|off/.test(val)||val.trim()==="no")return"No it's not listed";if(/yes|active|on market|currently listed/.test(val))return"Yes it's listed";return""}function ispdRawMapRepairs(condition){const s=String(condition||"").trim().toLowerCase();for(const[keywords,value]of ISPD_RAW_REPAIRS_RULES)if(keywords.some(k=>s.includes(k)))return value;return""}function ispdRawMapGoal(reason){const s=String(reason||"").trim().toLowerCase();if(!s||s==="n/a"||s==="na"||s==="refused to disclose")return"";for(const[keywords,value]of ISPD_RAW_GOAL_RULES)if(keywords.some(k=>s.includes(k)))return value;return ISPD_RAW_GOAL_FALLBACK}function ispdRawParseAddress(raw){const re=/^(.+?),\s*([A-Za-z .'-]+?),\s*([A-Z]{2})\s*(\d{5})?\s*$/;let a=String(raw||"").trim().replace(/^"+|"+$/g,"").trim();if(a.includes("\n")){const firstLine=a.split("\n").map(x=>x.trim()).filter(Boolean)[0]||a;const m2=firstLine.match(re);if(m2)return{street:m2[1],city:m2[2],state:m2[3],zip:m2[4]||"",ok:false};return{street:a,city:"",state:"",zip:"",ok:false}}const m=a.match(re);if(m)return{street:m[1],city:m[2],state:m[3],zip:m[4]||"",ok:true};return{street:a,city:"",state:"",zip:"",ok:false}}const ISPD_MULTI_ADDR_SEP_RE=/\s*(?:\/{2,}|;|\s\|\s|,{3,})\s*/;function ispdSplitMultiAddress(raw){const s=String(raw||"").trim();if(!s)return[];return s.split(ISPD_MULTI_ADDR_SEP_RE).map(x=>x.replace(/^[\s,]+|[\s,]+$/g,"").trim()).filter(Boolean)}function ispdRawNormalizePhone(raw){return String(raw||"").replace(/\s*\[dnc\]\s*/ig,"").trim()}function ispdRawIsDnc(raw){return/\[dnc\]/i.test(String(raw||""))}function ispdRawLooksShifted(sellerNameCell){return/^\d{1,2}\/\d{1,2}\/\d{4}/.test(String(sellerNameCell||"").trim())}function ispdConvertRawImport(headerRow,dataRows){const norm=headerRow.map(ispdNormHeader);const idx=label=>norm.indexOf(ispdNormHeader(label));const iPhone=idx("Phone Number")>=0?idx("Phone Number"):idx("Phone");const iName=idx("Seller Name"),iAddr=idx("Property Address"),iCond=idx("Condition"),iOcc=idx("Occupancy"),iAsk=idx("Asking Price"),iMkt=idx("Market Value"),iReason=idx("Reason"),iClose=idx("Closing"),iNotes=idx("Additional Notes"),iEmail=idx("Email"),iRec=idx("Call Recording");const templateHeader=ispdAllUploadColumns().map(c=>c.header);const outRows=[];let dncCount=0,corruptCount=0;dataRows.forEach(r=>{const cell=i=>i>=0?String(r[i]||"").trim():"";const sellerName=cell(iName),phoneRaw=cell(iPhone);if(ispdRawLooksShifted(sellerName)){corruptCount++;return}if(ispdRawIsDnc(phoneRaw)){dncCount++;return}const phone=ispdRawNormalizePhone(phoneRaw);const addr=ispdRawParseAddress(cell(iAddr));const condition=cell(iCond),occupancy=cell(iOcc),reason=cell(iReason),closing=cell(iClose),notes=cell(iNotes),email=cell(iEmail),askingPrice=cell(iAsk),marketValue=cell(iMkt),recordingRaw=cell(iRec);const repairs=ispdRawMapRepairs(condition);const occupied=ISPD_RAW_OCCUPANCY_MAP[occupancy.toLowerCase()]||"";const sellFast=ispdRawMapSellFast(closing);const goal=ispdRawMapGoal(reason);const listed=ispdRawMapListed(notes);const recLinks=recordingRaw.split(/[,\s]+/).map(u=>u.trim()).filter(u=>u.startsWith("http"));const recordLink="";const extraLinksNote=recLinks.length?`Recording link(s) from the import file: ${recLinks.join(", ")}

`:"";const emailClean=["n/a","na",""].includes(email.toLowerCase())?"":email;let pricePrefix="";if(askingPrice&&!["n/a","na"].includes(askingPrice.toLowerCase()))pricePrefix+=`Asking Price: ${askingPrice}
`;if(marketValue&&!["n/a","na"].includes(marketValue.toLowerCase()))pricePrefix+=`Market Value: ${marketValue}
`;const conversationNotes=`${pricePrefix}${extraLinksNote}${notes}`.trim();const byHeader={"Phone":phone,"Seller Name (new leads only)":sellerName,"Count (new leads only)":"1","Email":emailClean,"Property address":addr.street,"City":addr.city,"State":addr.state,"ZIP code":addr.zip,"Repairs needed":repairs,"Occupied":occupied,"When do they want to sell":sellFast,"Why they want to sell":goal,"Reason for selling — in their own words":reason,"Owner or agent/wholesaler":ISPD_RAW_DEFAULT_OWNER,"Currently listed with an agent":listed,"Conversation notes":conversationNotes,"Call recording link":recordLink};outRows.push(templateHeader.map(h=>byHeader[h]!=null?byHeader[h]:""))});return{headerRow:templateHeader,dataRows:outRows,summary:{sourceRows:dataRows.length,converted:outRows.length,dncExcluded:dncCount,corruptExcluded:corruptCount}}}$("ispdDownloadTemplateBtn").addEventListener("click",()=>{if(!(myIsOwner||myRole==="admin"))return;const headerRow=ispdAllUploadColumns().map(c=>c.header);const csv=headerRow.map(h=>`"${h.replace(/"/g,'""')}"`).join(",")+"\n";const blob=new Blob([csv],{type:"text/csv"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download="primehome-buyers-upload-template.csv";document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url)});let ispdParsedRows=[];$("ispdUploadInput").addEventListener("change",async e=>{const file=e.target.files[0];const msg=$("ispdUploadMsg");msg.style.display="none";$("ispdUploadPreviewWrap").style.display="none";if(!file)return;try{const buf=await file.arrayBuffer();await ensureXlsx();const wb=XLSX.read(buf,{type:"array"});const sheet=wb.Sheets[wb.SheetNames[0]];const rows=XLSX.utils.sheet_to_json(sheet,{header:1,defval:"",raw:false});if(!rows.length){msg.textContent="That file has no rows.";msg.className="msg error";msg.style.display="block";return}let headerRow=rows[0].map(h=>String(h||"").trim());let dataRows=rows.slice(1).filter(r=>r.some(c=>String(c||"").trim()!==""));if(!dataRows.length){msg.textContent="No data rows found below the header.";msg.className="msg error";msg.style.display="block";return}let rawImportSummary=null;if(ispdDetectRawFormat(headerRow)){const converted=ispdConvertRawImport(headerRow,dataRows);headerRow=converted.headerRow;dataRows=converted.dataRows;rawImportSummary=converted.summary;if(!dataRows.length){msg.textContent=`Detected a raw call-intake export, but every row was excluded (${converted.dncExcluded} DNC-tagged, ${converted.corruptExcluded} corrupted/unreadable) — nothing to import.`;msg.className="msg error";msg.style.display="block";return}}const allCols=ispdAllUploadColumns();const norm=s=>String(s||"").trim().toLowerCase().replace(/\s+/g," ");const colByHeaderIndex=headerRow.map(h=>allCols.find(c=>norm(c.header)===norm(h)||norm(c.key)===norm(h))||null);if(!colByHeaderIndex.some(c=>c&&c.key==="__phone")){msg.textContent='No "Phone" column found — required to match or create leads.';msg.className="msg error";msg.style.display="block";return}const parsed=dataRows.map(r=>{const cells={};colByHeaderIndex.forEach((c,i)=>{if(c)cells[c.key]=String(r[i]||"").trim()});return{__cells:cells,phoneNorm:(cells.__phone||"").replace(/\D/g,"").slice(-10)}}).filter(r=>r.phoneNorm);if(!parsed.length){msg.textContent="No rows had a usable phone number.";msg.className="msg error";msg.style.display="block";return}const phones=[...new Set(parsed.map(r=>r.phoneNorm))];const{data:existingLeads}=await sb.from("leads").select("id, seller_phone_norm, submitted_at").in("seller_phone_norm",phones).order("submitted_at",{ascending:false});const latestByPhone={};(existingLeads||[]).forEach(l=>{if(!latestByPhone[l.seller_phone_norm])latestByPhone[l.seller_phone_norm]=l});const requiredKeys=ISPEEDTOLEAD_FIELD_GROUPS.flatMap(g=>g.fields).filter(f=>f.required).map(f=>f.key);const ispdAddrKey=(street,city,zip)=>{const n=v=>String(v||"").toLowerCase().replace(/[^a-z0-9]/g,"");const st=n(street);if(!st)return"";const loc=n(city)||n(zip);return loc?`${st}|${loc}`:st};const dedupeFormId=await getIspdDefaultCampaignFormId();const existingByAddrKey={};let dedupeReady=false;if(dedupeFormId){const PAGE=1e3;let fetchOk=true;for(let from=0;;from+=PAGE){const{data:addrLeads,error:addrErr}=await sb.from("leads").select("id, extra_fields, status, submitted_at").eq("submission_form_id",dedupeFormId).order("submitted_at",{ascending:false}).range(from,from+PAGE-1);if(addrErr){console.error("address-dedupe fetch failed:",addrErr);fetchOk=false;break}if(!addrLeads||!addrLeads.length)break;addrLeads.forEach(l=>{const ef=l.extra_fields||{};const k=ispdAddrKey(ef.form_address,ef.form_city,ef.form_zip);if(k&&!existingByAddrKey[k])existingByAddrKey[k]=l});if(addrLeads.length<PAGE)break}dedupeReady=fetchOk}if(!dedupeReady){msg.innerHTML='<div><strong>Import blocked — the duplicate check could not run.</strong></div><div style="margin-top:6px;">Nothing was imported. Without it every row would be created as a new lead, even the ones already on file.</div><div style="margin-top:6px;">Open Leads Queue Review → PrimeHome Buyers once so the campaign loads, then try the upload again. If it keeps failing, check the browser console.</div>';msg.className="msg error";msg.style.display="block";$("ispdUploadPreviewWrap").style.display="none";ispdParsedRows=[];return}parsed.forEach(r=>{const parts=ispdSplitMultiAddress(r.__cells.form_address);if(parts.length>1){r.multiAddress=parts;const one=ispdRawParseAddress(parts[0]);r.__cells.form_address=one.ok?one.street:parts[0];if(one.ok){if(!r.__cells.form_city)r.__cells.form_city=one.city;if(!r.__cells.form_state)r.__cells.form_state=one.state;if(!r.__cells.form_zip&&one.zip)r.__cells.form_zip=one.zip}const note=`⚠ The source row listed ${parts.length} addresses in one cell. Imported as the first: "${parts[0]}". Also mentioned: ${parts.slice(1).join(" | ")}`;r.__cells.form_conversation_notes=r.__cells.form_conversation_notes?`${note}

${r.__cells.form_conversation_notes}`:note}});parsed.forEach(r=>{r.missing=requiredKeys.filter(k=>!r.__cells[k]);r.addrKey=ispdAddrKey(r.__cells.form_address,r.__cells.form_city,r.__cells.form_zip);const addrHit=r.addrKey?existingByAddrKey[r.addrKey]:null;if(addrHit){r.matchedLeadId=addrHit.id;r.matchedOn="address"}else if(!r.addrKey&&latestByPhone[r.phoneNorm]){r.matchedLeadId=latestByPhone[r.phoneNorm].id;r.matchedOn="phone (row has no address)"}else{r.matchedLeadId=null;r.matchedOn=null}});const seenAddrKeys=new Set;parsed.forEach(r=>{if(r.matchedLeadId||!r.addrKey)return;if(seenAddrKeys.has(r.addrKey))r.duplicateInFile=true;else seenAddrKeys.add(r.addrKey)});const matchedIds=[...new Set(parsed.map(r=>r.matchedLeadId).filter(Boolean))];const sentToPrimehome=new Set;const sentToGyhomes=new Set;if(matchedIds.length){for(let i=0;i<matchedIds.length;i+=200){const{data:sentRows}=await sb.from("ispeedtolead_submissions").select("lead_id, channel").eq("success",true).in("lead_id",matchedIds.slice(i,i+200));(sentRows||[]).forEach(s=>{if(s.channel==="primehome")sentToPrimehome.add(s.lead_id);else if(s.channel==="gyhomes")sentToGyhomes.add(s.lead_id)})}}const deliveredByPostback=new Set;if(matchedIds.length){for(let i=0;i<matchedIds.length;i+=200){const{data:pbRows}=await sb.from("ispeedtolead_postbacks").select("matched_lead_id, matched_via, received_at").in("matched_lead_id",matchedIds.slice(i,i+200)).order("received_at",{ascending:false});const seen=new Set;(pbRows||[]).forEach(pb=>{if(seen.has(pb.matched_lead_id))return;seen.add(pb.matched_lead_id);if(pb.matched_via!=="phone_ineligible")deliveredByPostback.add(pb.matched_lead_id)})}}const leadStateById={};if(matchedIds.length){for(let i=0;i<matchedIds.length;i+=200){const{data:stateRows}=await sb.from("leads").select("id, status, manual_delivery_at").in("id",matchedIds.slice(i,i+200));(stateRows||[]).forEach(x=>{leadStateById[x.id]=x})}}parsed.forEach(r=>{const st=r.matchedLeadId?leadStateById[r.matchedLeadId]:null;r.closedByStaff=!!(st&&st.manual_delivery_at);r.onCallBack=!!(st&&st.status==="call_back");r.alreadySentPrimehome=!!(r.matchedLeadId&&sentToPrimehome.has(r.matchedLeadId));r.alreadySentGyhomes=!!(r.matchedLeadId&&sentToGyhomes.has(r.matchedLeadId)&&!sentToPrimehome.has(r.matchedLeadId));r.deliveredByPostbackOnly=!!(r.matchedLeadId&&deliveredByPostback.has(r.matchedLeadId)&&!sentToPrimehome.has(r.matchedLeadId)&&!sentToGyhomes.has(r.matchedLeadId))});if(rawImportSummary){const s=rawImportSummary;const skipped=s.dncExcluded+s.corruptExcluded;msg.textContent=`Detected a raw call-intake export — converted ${s.converted} of ${s.sourceRows} row(s)`+(skipped?` (${s.dncExcluded} skipped as DNC-tagged, ${s.corruptExcluded} skipped as unreadable/corrupted).`:".")+` Type of house, Years owned, and Ping ID have no source data and are always left blank — review the ⚠ marks below before importing.`;msg.className="msg ok";msg.style.display="block"}ispdParsedRows=parsed;renderIspdPreview(allCols.filter(c=>colByHeaderIndex.some(mc=>mc&&mc.key===c.key)))}catch(err){msg.textContent=`Couldn't read that file: ${err.message}`;msg.className="msg error";msg.style.display="block"}});function renderIspdPreview(usedCols){$("ispdPreviewHead").innerHTML=`<tr><th>Match</th>${usedCols.map(c=>`<th>${escapeHtml(c.header)}</th>`).join("")}</tr>`;$("ispdPreviewBody").innerHTML=ispdParsedRows.map(r=>`
    <tr>
      <td class="mono" style="white-space:nowrap; color:${r.duplicateInFile||r.alreadySentPrimehome||r.matchedLeadId?"var(--danger)":"var(--text-2)"};" title="${r.alreadySentPrimehome?"This property was already delivered to PrimeHome Buyers — skipped so the sent record is not overwritten.":r.matchedLeadId?`This PROPERTY is already on file (matched on ${escapeHtml(r.matchedOn||"address")})${r.alreadySentGyhomes?" and was already sent to gyhomes":r.deliveredByPostbackOnly?" and iSpeedToLead already confirmed delivery by postback (channel unknown)":" and has never been sent anywhere"}. Nothing on the lead will be changed — the import only creates new leads. It is still tagged into this import so you can find it under Imported → My last import.`:r.duplicateInFile?"The same property appears earlier in this file — only the first row creates the lead.":""}">${r.alreadySentPrimehome?"Sent to PrimeHome — will skip":r.matchedLeadId?r.alreadySentGyhomes?"On file, sent to gyhomes":r.deliveredByPostbackOnly?"On file, delivered (postback)":"On file, never sent — for resend":r.duplicateInFile?"Duplicate in file — will skip":"New lead"}${r.missing.length?` <span style="color:var(--danger);" title="Missing: ${escapeHtml(r.missing.join(", "))}">⚠ ${r.missing.length} missing</span>`:""}${r.multiAddress?` <span style="color:var(--danger); font-weight:700;" title="This row's Property Address cell contained ${r.multiAddress.length} addresses: ${escapeHtml(r.multiAddress.join(" | "))}&#10;&#10;Importing as the first one. The others are saved into Conversation notes — check whether they are separate leads.">⚠ ${r.multiAddress.length} addresses</span>`:""}</td>
      ${usedCols.map(c=>`<td>${escapeHtml((r.__cells[c.key]||"").slice(0,40))}</td>`).join("")}
    </tr>
  `).join("");$("ispdImportCount").textContent=ispdParsedRows.length;$("ispdUploadPreviewWrap").style.display=""}$("ispdCancelImportBtn").addEventListener("click",()=>{ispdParsedRows=[];$("ispdUploadPreviewWrap").style.display="none";$("ispdUploadInput").value=""});$("ispdConfirmImportBtn").addEventListener("click",async()=>{const msg=$("ispdUploadMsg");msg.style.display="none";const{data:{user}}=await sb.auth.getUser();if(!user){msg.textContent="Not signed in.";msg.className="msg error";msg.style.display="block";return}const btn=$("ispdConfirmImportBtn");btn.disabled=true;btn.textContent="Importing…";const defaultCampaignFormId=await getIspdDefaultCampaignFormId();if(!defaultCampaignFormId)console.error("getIspdDefaultCampaignFormId() returned null — this import batch will not auto-tag any lead with the PrimeHome Buyers campaign.");const matchedLeadIdByAddr={};ispdParsedRows.forEach(r=>{if(r.matchedLeadId&&r.addrKey)matchedLeadIdByAddr[r.addrKey]=r.matchedLeadId});const importBatchId=crypto&&crypto.randomUUID?crypto.randomUUID():String(Date.now());const importStamp={last_import_batch:importBatchId,last_imported_at:new Date().toISOString(),last_imported_by:user.id};let created=0,failed=0,skippedDuplicate=0,skippedAlreadySent=0,refreshedGyhomes=0,refreshedNeverSent=0,refreshedPostback=0,refreshedClosed=0,refreshedCallBack=0;for(const r of ispdParsedRows){const matchedLeadId=r.matchedLeadId||(r.addrKey?matchedLeadIdByAddr[r.addrKey]||null:null);if(r.alreadySentPrimehome){skippedAlreadySent++;continue}if(matchedLeadId){const{error}=await sb.from("leads").update(importStamp).eq("id",matchedLeadId);if(error){failed++;console.error("import: batch stamp failed for lead",matchedLeadId,error)}else if(r.alreadySentGyhomes)refreshedGyhomes++;else if(r.deliveredByPostbackOnly)refreshedPostback++;else if(r.closedByStaff)refreshedClosed++;else if(r.onCallBack)refreshedCallBack++;else refreshedNeverSent++;continue}if(r.duplicateInFile){skippedDuplicate++;continue}const extraFields={};Object.entries(r.__cells).forEach(([k,v])=>{if(!k.startsWith("__")&&v)extraFields[k]=v});{const count=parseInt(r.__cells.__count,10)||1;const{data:newLead,error}=await sb.from("leads").insert({user_id:user.id,count,imported:true,submission_form_id:defaultCampaignFormId||null,...importStamp,seller_investor_name:r.__cells.__seller_name||null,seller_phone:r.__cells.__phone||null,extra_fields:Object.keys(extraFields).length?extraFields:{}}).select("id").single();if(error)failed++;else{created++;if(r.addrKey)matchedLeadIdByAddr[r.addrKey]=newLead.id}}}logActivity("lead.csv_import",null,null,`CSV/Excel import (create-only): ${created} created, ${refreshedGyhomes} already sent to gyhomes, ${refreshedNeverSent} never sent (workable), ${refreshedPostback} delivered by postback, ${refreshedClosed} closed by staff, ${refreshedCallBack} on call-back, ${skippedAlreadySent} skipped as already sent to PrimeHome Buyers${skippedDuplicate?`, ${skippedDuplicate} skipped as duplicate address`:""}${failed?`, ${failed} failed`:""} (batch ${importBatchId})${!defaultCampaignFormId?" — WARNING: PrimeHome Buyers campaign could not be resolved, leads went in untagged":""}`);const inWorklist=created+refreshedGyhomes+refreshedNeverSent+refreshedPostback+refreshedClosed+refreshedCallBack;const importSummaryLines=[`New leads created: ${created}`,`Already sent to gyhomes: ${refreshedGyhomes}`,`Never sent anywhere: ${refreshedNeverSent} — refreshed and ready to work`,`Skipped as already sent to PrimeHome Buyers: ${skippedAlreadySent}`];if(refreshedPostback)importSummaryLines.push(`Delivered via iSpeedToLead postback (channel unknown): ${refreshedPostback}`);if(refreshedClosed)importSummaryLines.push(`Closed by staff, not sendable: ${refreshedClosed}`);if(refreshedCallBack)importSummaryLines.push(`On call-back, not ready to send: ${refreshedCallBack}`);const multiAddrCount=ispdParsedRows.filter(r=>r.multiAddress).length;if(multiAddrCount)importSummaryLines.push(`Rows with more than one address in the cell: ${multiAddrCount} — imported the first, the rest are in Conversation notes`);if(skippedDuplicate)importSummaryLines.push(`Skipped as a repeat of the same property in this file: ${skippedDuplicate}`);if(failed)importSummaryLines.push(`Failed: ${failed}`);msg.innerHTML=importSummaryLines.map(t=>`<div>${escapeHtml(t)}</div>`).join("")+(inWorklist?`<div style="margin-top:6px;">Find all ${inWorklist} with the Imported filter &rarr; &quot;My last import&quot;.</div>`:`<div style="margin-top:6px;">Nothing to work from this file &mdash; every row was already delivered.</div>`)+(!defaultCampaignFormId?`<div style="margin-top:6px;">Warning: could not resolve the PrimeHome Buyers campaign &mdash; these leads were NOT tagged and may not show under Lead Reviewing. Try the import again, or check Submission Forms Creator.</div>`:"");msg.className=failed||!defaultCampaignFormId?"msg error":"msg";msg.style.display="block";sb.rpc("leads_import_notify",{p_title:`PrimeHome Buyers file imported: ${created} new lead${created===1?"":"s"}${failed?`, ${failed} failed`:""}`,p_body:importSummaryLines.join("\n")}).then(({error})=>{if(error)console.warn("import summary notification failed:",error.message)});btn.disabled=false;btn.textContent="Import";ispdParsedRows=[];$("ispdUploadPreviewWrap").style.display="none";$("ispdUploadInput").value="";await loadLeads()});async function saveLeadPhone(phoneEl){const newPhone=phoneEl.textContent.trim();const leadId=phoneEl.dataset.id;const oldPhone=phoneEl.dataset.original;if(newPhone===oldPhone)return;const{error}=await sb.from("leads").update({seller_phone:newPhone||null}).eq("id",leadId);if(error){alert(error.message);phoneEl.textContent=oldPhone}else{phoneEl.dataset.original=newPhone;logActivity("lead.update","lead",leadId,`Set phone to "${newPhone}"`)}}function notifyIfLeadAccepted(lead,status){if(status!=="accepted")return;if(lead&&lead.status==="accepted")return;notifyClientsForFormId(lead.submission_form_id,"lead_received",leadsFormNameFn?leadsFormNameFn(lead.submission_form_id):null)}async function setLeadStatus(lead,status,agentLabel,extraUpdate){let reviewNote=null;if(status==="rejected"){reviewNote=prompt(`Why is this lead being rejected? (required)

Agent: ${agentLabel} — ${lead.count} lead(s)`);if(reviewNote===null)return;reviewNote=reviewNote.trim();if(!reviewNote){alert("A comment is required to reject a lead.");return}}const{data:{user}}=await sb.auth.getUser();const update={status,reviewed_by:user?user.id:null,reviewed_at:new Date().toISOString(),...extraUpdate||{}};if(status==="rejected")update.review_note=reviewNote;else if(status==="accepted")update.review_note=null;const{error}=await sb.from("leads").update(update).eq("id",lead.id);if(error){alert(error.message);return}notifyIfLeadAccepted(lead,status);await loadLeads();await refreshDetailIfViewing(lead.user_id);logActivity("lead.review","lead",lead.id,`Marked ${status} — ${agentLabel} — ${lead.count} lead(s)${status==="rejected"?` — reason: ${reviewNote}`:""}`)}async function recallAcceptedLeadToQueue(lead,agentLabel){const{data:soldPostbacks}=await sb.from("ispeedtolead_postbacks").select("id, amount, received_at").eq("matched_lead_id",lead.id).eq("mapped_status","accepted");const soldRows=soldPostbacks||[];const soldWarning=soldRows.length?`

⚠ iSpeedToLead already reported this lead ACCEPTED/PURCHASED (${soldRows.length} postback${soldRows.length>1?"s":""}, ${soldRows.map(r=>r.amount!=null?`$${r.amount}`:"—").join(", ")}). Recalling it will NOT undo that sale or its $ total in Finance — it'll be flagged there as "Needs reconciliation" so you can follow up manually.`:"";if(!confirm(`Recall this accepted lead back to Queue Review?

It will stop being visible to the client immediately.${soldWarning}

Agent: ${agentLabel} — ${lead.count} lead(s)`))return;const{error}=await sb.from("leads").update({status:"pending",review_note:null,reviewed_by:null,reviewed_at:null}).eq("id",lead.id);if(error){alert(error.message);return}if(soldRows.length){await sb.from("ispeedtolead_postbacks").update({needs_reconciliation:true}).in("id",soldRows.map(r=>r.id))}await loadLeads();await refreshDetailIfViewing(lead.user_id);logActivity("lead.recall","lead",lead.id,`Recalled an accepted lead back to Queue Review — ${agentLabel} — ${lead.count} lead(s)${soldRows.length?` — flagged ${soldRows.length} iSpeedToLead postback(s) for reconciliation`:""}`)}async function deleteLead(lead,label){const id=typeof lead==="object"?lead.id:lead;const userId=typeof lead==="object"?lead.user_id:null;const{error}=await sb.from("leads").delete().eq("id",id);if(error){alert(`Couldn't delete this lead: ${error.message}`);return}loadLeads();if(userId)await refreshDetailIfViewing(userId);logActivity("lead.delete","lead",id,`Deleted lead submission — ${label||id}`)}const ISPEEDTOLEAD_FIELD_GROUPS=[{title:"Contact",fields:[{key:"form_first_name",label:"First name",type:"text",placeholder:"Auto-split from Seller Name unless set here"},{key:"form_last_name",label:"Last name",type:"text",placeholder:"Auto-split from Seller Name unless set here"},{key:"form_email",label:"Email",type:"text",placeholder:"seller@example.com"}]},{title:"Property",fields:[{key:"form_address",label:"Property address",type:"text",required:true,placeholder:"243103 Doric Ave, Cranston, RI 02910"},{key:"form_unit",label:"Unit",type:"text"},{key:"form_city",label:"City",type:"text",required:true},{key:"form_state",label:"State",type:"text",required:true,placeholder:"RI or Rhode Island"},{key:"form_zip",label:"ZIP code",type:"text"},{key:"form_multifamily",label:"Type of house",type:"select",required:true,options:["Ranch","2-story","Mobile home owned land","Mobile home rented land","Multifamily","Single family","Bungalow","Cottage","Townhouse","Condominium","Duplex","Farmhouse","Split-level home","Land"]},{key:"form_square",label:"Square footage",type:"select",options:["0 - 500","500 - 1000","1000 - 2000","2000 - 3000","3000 - 4000","4000 - 5000","5000+"]},{key:"form_year",label:"Year of construction",type:"select",options:["1800-1900","1900-1950","1950-1970","1970-1980","1980-1990","1990-2000","2000-2010","2010-2022"]},{key:"form_garage",label:"Garage",type:"select",options:["None","1 Car Attached","2 Car Attached","1 Car Detached","2 Car Detached","Carport","Other"]},{key:"form_bedrooms",label:"Bedrooms",type:"select",options:["1 Bedroom","2 Bedroom","3 Bedroom","4 Bedroom","5 Bedroom","More than 5"]},{key:"form_bathrooms",label:"Bathrooms",type:"select",options:["None","1 Bathroom","1.5 Bathroom","2 Bathroom","2.5 Bathroom","3 Bathroom","More than 3"]},{key:"form_repairs",label:"Repairs needed",type:"multi",required:true,options:["Full Gut Renovation - Everything","Remodel - Kitchen, Bathroom, Roof","Cosmetic - Flooring, Paint","Fully renovated and updated in past 2 years"]},{key:"form_occupied",label:"Occupied",type:"select",required:true,options:["Owner Occupied","Tenant Occupied","No its Vacant"]}]},{title:"Seller situation",fields:[{key:"form_sell_fast",label:"When do they want to sell",type:"select",required:true,options:["ASAP","1 month","2-3 months","4-5 months","6+ months"]},{key:"form_goal",label:"Why they want to sell",type:"multi",required:true,options:["Preforeclosure","Emergency Reasons","Financial Reasons","Selling a vacant/non-occupied property","Sell and rent instead","Death in the family","Sell without showings","Inherited Property","Downsizing","Tired of being a landlord","Moving closer to family","Relocating","Retirement elsewhere","Upgrading","Moving from United States","Behind on taxes/mortgage","Old age","Divorce","Health issues","Job loss","Other personal reason"]},{key:"form_goal2",label:"Reason for selling — in their own words",type:"textarea"},{key:"form_owner",label:"Owner or agent/wholesaler",type:"select",required:true,options:["Yes, i own this property","Agent/wholesaler"]},{key:"form_owned_years",label:"Years owned",type:"select",required:true,options:["0-1","2-5","6-9","10-14","15-19","20-29","30-50","50+"]},{key:"form_listed",label:"Currently listed with an agent",type:"select",required:true,options:["Yes it's listed","No it's not listed"]},{key:"form_scenario",label:"Perfect scenario after selling",type:"multi",options:["Rent instead of owning","1031 Exchange","Nothing, I'm just cashing out an asset","Upgrade to another property","Buy in another location","Downsize","Travel/RV"]},{key:"form_call_time",label:"Best time to call",type:"select",options:["Anytime","Morning","Afternoon","Evening"]},{key:"form_conversation_notes",label:"Conversation notes",type:"textarea"}]},{title:"Metadata",fields:[{key:"form_source",label:"Lead source",type:"select",options:["facebook","discovery","google","pre-qualified-call","bing","local-facebook-groups","sms-marketing","call-marketing","video"],default:"call-marketing"},{key:"record_link",label:"Call recording link",type:"text",required:true,placeholder:"https://.../recording.mp3"},{key:"ping_id",label:"Ping ID",type:"text",required:true,placeholder:"Paste an ID, or upload a file below"},{key:"activeprospect_certificate_url",label:"TrustedForm certificate URL (rarely needed)",type:"text"}]}];const ISPEEDTOLEAD_PING_ID_ACCEPT="image/*";const ISPEEDTOLEAD_ATTACHMENT_FIELDS={record_link:{accept:"audio/*,video/*"},ping_id:{accept:ISPEEDTOLEAD_PING_ID_ACCEPT}};const ISPEEDTOLEAD_IMAGE_EXT_RE=/\.(png|jpe?g|gif|webp|bmp|heic|heif)(\?|#|$)/i;function ispdPingIdLooksLikeImage(value){const v=String(value||"").trim();if(!v)return true;if(!/^https?:\/\//i.test(v))return true;return ISPEEDTOLEAD_IMAGE_EXT_RE.test(v)}function ispeedtoleadFieldInputHtml(f,existingValue){const req=f.required?' <span style="color:var(--danger);">*</span>':"";if(f.type==="select"){const opts=['<option value="">—</option>'].concat(f.options.map(o=>`<option value="${escapeHtml(o)}"${existingValue===o?" selected":""}>${escapeHtml(o)}</option>`));return`<label>${escapeHtml(f.label)}${req}</label><select data-field-key="${f.key}">${opts.join("")}</select>`}if(f.type==="textarea"){return`<label>${escapeHtml(f.label)}${req}</label><textarea data-field-key="${f.key}" rows="2">${escapeHtml(existingValue||"")}</textarea>`}if(f.type==="multi"){const selected=new Set((existingValue||"").split("\n").filter(Boolean));const boxes=f.options.map(o=>`
      <label style="display:flex; align-items:center; gap:6px; text-transform:none; letter-spacing:0; font-size:12px; font-weight:400; margin:2px 0;">
        <input type="checkbox" data-multi-key="${f.key}" value="${escapeHtml(o)}" ${selected.has(o)?"checked":""} style="width:auto;"> ${escapeHtml(o)}
      </label>`).join("");return`<label>${escapeHtml(f.label)}${req}</label><div style="border:1px solid var(--border-light); border-radius:6px; padding:8px 10px; max-height:140px; overflow-y:auto;">${boxes}</div>`}if(ISPEEDTOLEAD_ATTACHMENT_FIELDS[f.key]){const accept=ISPEEDTOLEAD_ATTACHMENT_FIELDS[f.key].accept;return`<label>${escapeHtml(f.label)}${req}</label>
      <input type="text" data-field-key="${f.key}" placeholder="${escapeHtml(f.placeholder||"")}" value="${escapeHtml(existingValue||"")}">
      <div style="display:flex; align-items:center; gap:8px; margin-top:6px;">
        <input type="file" id="ispeedtoleadUpload_${f.key}_file" accept="${accept}" style="flex:1; font-size:11px; padding:0;">
        <button type="button" class="btn-secondary ispeedtolead-attach-upload-btn" data-attach-field="${f.key}" id="ispeedtoleadUpload_${f.key}_btn" style="width:auto; padding:6px 12px; font-size:11px; margin-top:0;">Upload</button>
      </div>
      <div class="msg" id="ispeedtoleadUpload_${f.key}_msg" style="margin-top:4px; margin-bottom:0;"></div>
      <div id="ispeedtoleadUpload_${f.key}_list"></div>`}return`<label>${escapeHtml(f.label)}${req}</label><input type="text" data-field-key="${f.key}" placeholder="${escapeHtml(f.placeholder||"")}" value="${escapeHtml(existingValue||"")}">`}function ispeedtoleadSplitName(full){const trimmed=(full||"").trim().replace(/\s+/g," ");if(!trimmed)return{first:"",last:""};const parts=trimmed.split(" ");if(parts.length===1)return{first:parts[0],last:""};return{first:parts.slice(0,-1).join(" "),last:parts[parts.length-1]}}function renderIspeedToLeadFields(extraFields,sellerName){const ef=extraFields||{};const nameDefaults=ispeedtoleadSplitName(sellerName);return ISPEEDTOLEAD_FIELD_GROUPS.map(group=>`
    <h4 style="margin:16px 0 8px; font-size:12px; color:var(--text-2); text-transform:uppercase; letter-spacing:0.04em;">${escapeHtml(group.title)}</h4>
    ${group.fields.map(f=>{let existingValue=ef[f.key]!=null?ef[f.key]:leadSemanticFallback(ef,f.key,f.label)||f.default||"";if(!existingValue&&f.key==="form_first_name")existingValue=nameDefaults.first;if(!existingValue&&f.key==="form_last_name")existingValue=nameDefaults.last;return`<div style="margin-bottom:10px;">${ispeedtoleadFieldInputHtml(f,existingValue)}</div>`}).join("")}
  `).join("")}function ispdContactFieldProblem(){const container=$("ispeedtoleadModalFields");if(!container)return null;for(const key of["form_address","form_email"]){const el=container.querySelector(`[data-field-key="${key}"]`);if(!el)continue;const problem=leadFieldProblem(key,key==="form_address"?"Property address":"Email",el.value);if(problem)return problem}return null}function collectIspeedToLeadFields(existingExtraFields){const result={...existingExtraFields||{}};const container=$("ispeedtoleadModalFields");ISPEEDTOLEAD_FIELD_GROUPS.forEach(group=>group.fields.forEach(f=>{if(f.type==="multi"){const checked=Array.from(container.querySelectorAll(`[data-multi-key="${f.key}"]:checked`)).map(el=>el.value);if(checked.length)result[f.key]=checked.join("\n");else delete result[f.key]}else{const el=container.querySelector(`[data-field-key="${f.key}"]`);const val=el?el.value.trim():"";if(val)result[f.key]=val;else delete result[f.key]}}));return result}let ispeedtoleadModalLead=null;async function uploadIspeedtoleadAttachment(file,fieldKey){if(!ispeedtoleadModalLead||!file)return;const msg=$(`ispeedtoleadUpload_${fieldKey}_msg`);const btn=$(`ispeedtoleadUpload_${fieldKey}_btn`);if(msg){msg.textContent="Uploading…";msg.className="msg"}if(btn)btn.disabled=true;try{const ext=(file.name.split(".").pop()||"dat").toLowerCase().replace(/[^a-z0-9]/g,"")||"dat";const path=`${ispeedtoleadModalLead.id}/${fieldKey}-${Date.now()}.${ext}`;const{error:uploadErr}=await sb.storage.from("lead-recordings").upload(path,file,{upsert:false,contentType:file.type||"application/octet-stream"});if(uploadErr){if(msg){msg.textContent=`Upload failed: ${uploadErr.message}`;msg.className="msg error"}return}const{data:pub}=sb.storage.from("lead-recordings").getPublicUrl(path);const input=document.querySelector(`#ispeedtoleadModalFields [data-field-key="${fieldKey}"]`);if(input)input.value=pub.publicUrl;if(msg){msg.textContent='Uploaded — link filled in above. Click "Save details" or "Send to PrimeHome Buyers" to keep it.';msg.className="msg"}await refreshIspeedtoleadAttachmentList(ispeedtoleadModalLead.id,fieldKey)}catch(e){if(msg){msg.textContent=`Upload failed: ${e&&e.message?e.message:e}`;msg.className="msg error"}}finally{if(btn)btn.disabled=false}}function ispeedtoleadIndexItemHtml(f){const ext=(f.url.split("?")[0].split(".").pop()||"").toLowerCase();const AUDIO_EXT=["mp3","wav","m4a","ogg","aac","flac","webm"];const IMAGE_EXT=["png","jpg","jpeg","gif","webp"];const preview=AUDIO_EXT.includes(ext)?`<audio controls style="width:100%;" src="${escapeHtml(f.url)}"></audio>`:IMAGE_EXT.includes(ext)?`<img src="${escapeHtml(f.url)}" style="max-width:100%; border-radius:6px; display:block;">`:"";return`
    <div style="margin-bottom:18px; padding:14px; border:1px solid #2a2f2c; border-radius:10px; background:#121714;">
      <div style="font-size:12px; color:#8a938d; margin-bottom:8px;">${escapeHtml(f.when)}</div>
      ${preview}
      <div style="margin-top:${preview?"8px":"0"}; font-size:12px;"><a href="${escapeHtml(f.url)}" style="color:#fd6262;">Open / download</a></div>
    </div>`}async function regenerateIspeedtoleadRecordingsIndexPage(leadId,fieldKey,files){const title=fieldKey==="record_link"||fieldKey==="recording_url"?"Call Recordings":"Ping ID Attachments";const html=`<!DOCTYPE html>
<html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title>${escapeHtml(title)}</title>
<style>body{font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif; max-width:680px; margin:40px auto; padding:0 20px; background:#0b0f0d; color:#e8e8e8;} h1{font-size:18px; font-weight:600;} a{color:#fd6262;}</style>
</head><body>
<h1>${escapeHtml(title)} (${files.length})</h1>
${files.map(ispeedtoleadIndexItemHtml).join("")}
</body></html>`;const path=`${leadId}/${fieldKey}-index.html`;const blob=new Blob([html],{type:"text/html"});await sb.storage.from("lead-recordings").upload(path,blob,{upsert:true,contentType:"text/html"});const{data:pub}=sb.storage.from("lead-recordings").getPublicUrl(path);return pub.publicUrl}async function refreshIspeedtoleadAttachmentList(leadId,fieldKey){const listEl=$(`ispeedtoleadUpload_${fieldKey}_list`);if(!listEl)return;listEl.innerHTML='<span style="font-size:10px; color:var(--text-dim);">Loading uploads…</span>';const{data,error}=await sb.storage.from("lead-recordings").list(leadId,{sortBy:{column:"name",order:"desc"}});if(error||!data){listEl.innerHTML="";return}const indexFileName=`${fieldKey}-index.html`;const files=data.filter(f=>f.name.startsWith(`${fieldKey}-`)&&f.name!==indexFileName);if(!files.length){listEl.innerHTML="";return}const currentInput=document.querySelector(`#ispeedtoleadModalFields [data-field-key="${fieldKey}"]`);const currentUrl=currentInput?currentInput.value.trim():"";const attachListTz=nonAgentDisplayTz();let combinedRowHtml="";if(files.length>1){const withUrls=files.map(f=>{const{data:pub}=sb.storage.from("lead-recordings").getPublicUrl(`${leadId}/${f.name}`);const when=f.created_at?new Date(f.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...attachListTz?{timeZone:attachListTz}:{}}):f.name;return{url:pub.publicUrl,when}});const combinedUrl=await regenerateIspeedtoleadRecordingsIndexPage(leadId,fieldKey,withUrls);const isCombinedActive=combinedUrl===currentUrl;combinedRowHtml=`
      <div style="display:flex; align-items:center; gap:6px; font-size:11px; padding:5px 6px; border-bottom:1px solid var(--border); background:var(--inset); margin-bottom:2px; border-radius:4px;">
        <a href="${escapeHtml(combinedUrl)}" target="_blank" rel="noopener" style="flex:1; color:var(--text); text-decoration:underline;">Combined page — all ${files.length} files</a>
        ${isCombinedActive?'<span class="mono" style="font-size:9px; color:var(--focus);">IN USE</span>':`<button type="button" class="btn-secondary ispeedtolead-attach-combine-btn" data-attach-field="${fieldKey}" data-attach-url="${escapeHtml(combinedUrl)}" style="width:auto; padding:2px 8px; font-size:10px; margin-top:0;">Use combined</button>`}
      </div>`}else{sb.storage.from("lead-recordings").remove([`${leadId}/${indexFileName}`]).catch(()=>{})}listEl.innerHTML=`<div style="font-size:10px; color:var(--text-dim); margin:6px 0 3px;">${files.length} file${files.length===1?"":"s"} uploaded for this lead:</div>`+combinedRowHtml+files.map(f=>{const path=`${leadId}/${f.name}`;const{data:pub}=sb.storage.from("lead-recordings").getPublicUrl(path);const isActive=pub.publicUrl===currentUrl;const when=f.created_at?new Date(f.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...attachListTz?{timeZone:attachListTz}:{}}):f.name;return`
        <div style="display:flex; align-items:center; gap:6px; font-size:11px; padding:3px 0; border-bottom:1px solid var(--border);">
          <a href="${pub.publicUrl}" target="_blank" rel="noopener" style="flex:1; color:var(--text-2); text-decoration:underline; overflow:hidden; text-overflow:ellipsis; white-space:nowrap;" title="Open in a new tab">${escapeHtml(when)}</a>
          ${isActive?'<span class="mono" style="font-size:9px; color:var(--focus);">IN USE</span>':`<button type="button" class="btn-secondary ispeedtolead-attach-use-btn" data-attach-field="${fieldKey}" data-attach-url="${escapeHtml(pub.publicUrl)}" style="width:auto; padding:2px 8px; font-size:10px; margin-top:0;">Use</button>`}
          <button type="button" class="ispeedtolead-attach-del-btn" data-attach-path="${escapeHtml(path)}" data-attach-field="${fieldKey}" data-attach-url="${escapeHtml(pub.publicUrl)}" title="Delete this file permanently" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:13px;">×</button>
        </div>`}).join("");applyLeadsSectionEditGating("queue_review_ispeedtolead")}$("ispeedtoleadModalFields").addEventListener("click",async e=>{const useBtn=e.target.closest(".ispeedtolead-attach-use-btn");if(useBtn){const input=document.querySelector(`#ispeedtoleadModalFields [data-field-key="${useBtn.dataset.attachField}"]`);if(input)input.value=useBtn.dataset.attachUrl;if(ispeedtoleadModalLead)refreshIspeedtoleadAttachmentList(ispeedtoleadModalLead.id,useBtn.dataset.attachField);return}const combineBtn=e.target.closest(".ispeedtolead-attach-combine-btn");if(combineBtn){const input=document.querySelector(`#ispeedtoleadModalFields [data-field-key="${combineBtn.dataset.attachField}"]`);if(input)input.value=combineBtn.dataset.attachUrl;if(ispeedtoleadModalLead)refreshIspeedtoleadAttachmentList(ispeedtoleadModalLead.id,combineBtn.dataset.attachField);return}const delBtn=e.target.closest(".ispeedtolead-attach-del-btn");if(delBtn){if(!canEditLeadsSection("queue_review_ispeedtolead")){alert("Your role has view-only access to this section.");return}if(!confirm("Delete this uploaded file permanently? This can’t be undone, and if it’s the one currently in use (or part of the combined page) you’ll need to pick another before sending."))return;const{error}=await sb.storage.from("lead-recordings").remove([delBtn.dataset.attachPath]);if(error){alert(error.message);return}const fieldKey2=delBtn.dataset.attachField;const deletedInput=document.querySelector(`#ispeedtoleadModalFields [data-field-key="${fieldKey2}"]`);if(deletedInput&&deletedInput.value.trim()===delBtn.dataset.attachUrl&&ispeedtoleadModalLead){deletedInput.value="";const cleared=collectIspeedToLeadFields(ispeedtoleadModalLead.extra_fields);const{error:clearErr}=await sb.from("leads").update({extra_fields:cleared}).eq("id",ispeedtoleadModalLead.id);if(!clearErr)ispeedtoleadModalLead.extra_fields=cleared}if(ispeedtoleadModalLead)refreshIspeedtoleadAttachmentList(ispeedtoleadModalLead.id,fieldKey2);return}const uploadBtn=e.target.closest(".ispeedtolead-attach-upload-btn");if(!uploadBtn)return;const fieldKey=uploadBtn.dataset.attachField;const fileInput=$(`ispeedtoleadUpload_${fieldKey}_file`);const file=fileInput&&fileInput.files&&fileInput.files[0];if(!file){const msg=$(`ispeedtoleadUpload_${fieldKey}_msg`);if(msg){msg.textContent="Choose a file first.";msg.className="msg error"}return}uploadIspeedtoleadAttachment(file,fieldKey)});const LEADS_QUEUE_RECORDING_FIELD_KEY="recording_url";let leadsQueueReviewModalLead=null;async function refreshLeadsQueueRecordingList(leadId){const listEl=$("leadsQueueReviewRecordingList");if(!listEl)return;listEl.innerHTML='<span style="font-size:10px; color:var(--text-dim);">Loading uploads…</span>';const{data,error}=await sb.storage.from("lead-recordings").list(leadId,{sortBy:{column:"name",order:"desc"}});if(error||!data){listEl.innerHTML="";return}const indexFileName=`${LEADS_QUEUE_RECORDING_FIELD_KEY}-index.html`;const files=data.filter(f=>f.name.startsWith(`${LEADS_QUEUE_RECORDING_FIELD_KEY}-`)&&f.name!==indexFileName);if(!files.length){listEl.innerHTML="";return}const currentInput=$("leadsQueueReviewRecordingUrl");const currentUrl=currentInput?currentInput.value.trim():"";const recordingListTz=nonAgentDisplayTz();let combinedRowHtml="";if(files.length>1){const withUrls=files.map(f=>{const{data:pub}=sb.storage.from("lead-recordings").getPublicUrl(`${leadId}/${f.name}`);const when=f.created_at?new Date(f.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...recordingListTz?{timeZone:recordingListTz}:{}}):f.name;return{url:pub.publicUrl,when}});const combinedUrl=await regenerateIspeedtoleadRecordingsIndexPage(leadId,LEADS_QUEUE_RECORDING_FIELD_KEY,withUrls);const isCombinedActive=combinedUrl===currentUrl;combinedRowHtml=`
      <div style="display:flex; align-items:center; gap:6px; font-size:11px; padding:5px 6px; border-bottom:1px solid var(--border); background:var(--inset); margin-bottom:2px; border-radius:4px;">
        <a href="${escapeHtml(combinedUrl)}" target="_blank" rel="noopener" style="flex:1; color:var(--text); text-decoration:underline;">Combined page — all ${files.length} files</a>
        ${isCombinedActive?'<span class="mono" style="font-size:9px; color:var(--focus);">IN USE</span>':`<button type="button" class="btn-secondary leads-queue-recording-combine-btn" data-attach-url="${escapeHtml(combinedUrl)}" style="width:auto; padding:2px 8px; font-size:10px; margin-top:0;">Use combined</button>`}
      </div>`}else{sb.storage.from("lead-recordings").remove([`${leadId}/${indexFileName}`]).catch(()=>{})}listEl.innerHTML=`<div style="font-size:10px; color:var(--text-dim); margin:6px 0 3px;">${files.length} file${files.length===1?"":"s"} uploaded for this lead:</div>`+combinedRowHtml+files.map(f=>{const path=`${leadId}/${f.name}`;const{data:pub}=sb.storage.from("lead-recordings").getPublicUrl(path);const isActive=pub.publicUrl===currentUrl;const when=f.created_at?new Date(f.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...recordingListTz?{timeZone:recordingListTz}:{}}):f.name;return`
        <div style="display:flex; align-items:center; gap:6px; font-size:11px; padding:3px 0; border-bottom:1px solid var(--border);">
          <a href="${pub.publicUrl}" target="_blank" rel="noopener" style="flex:1; color:var(--text-2); text-decoration:underline; overflow:hidden; text-overflow:ellipsis; white-space:nowrap;" title="Open in a new tab">${escapeHtml(when)}</a>
          ${isActive?'<span class="mono" style="font-size:9px; color:var(--focus);">IN USE</span>':`<button type="button" class="btn-secondary leads-queue-recording-use-btn" data-attach-url="${escapeHtml(pub.publicUrl)}" style="width:auto; padding:2px 8px; font-size:10px; margin-top:0;">Use</button>`}
          <button type="button" class="leads-queue-recording-del-btn" data-attach-path="${escapeHtml(path)}" data-attach-url="${escapeHtml(pub.publicUrl)}" title="Delete this file permanently" style="background:transparent; border:none; color:var(--text-dim); cursor:pointer; font-size:13px;">×</button>
        </div>`}).join("")}async function uploadLeadsQueueRecordings(fileList){if(!leadsQueueReviewModalLead||!fileList||!fileList.length)return;const lead=leadsQueueReviewModalLead;const msg=$("leadsQueueReviewRecordingMsg");const btn=$("leadsQueueReviewRecordingUploadBtn");const files=Array.from(fileList);if(msg){msg.textContent=`Uploading ${files.length} file${files.length>1?"s":""}…`;msg.className="msg"}if(btn)btn.disabled=true;let uploaded=0,lastUrl=null;const failures=[];for(const file of files){try{const ext=(file.name.split(".").pop()||"dat").toLowerCase().replace(/[^a-z0-9]/g,"")||"dat";const path=`${lead.id}/${LEADS_QUEUE_RECORDING_FIELD_KEY}-${Date.now()}-${Math.random().toString(36).slice(2,8)}.${ext}`;const{error:uploadErr}=await sb.storage.from("lead-recordings").upload(path,file,{upsert:false,contentType:file.type||"application/octet-stream"});if(uploadErr){failures.push(`${file.name}: ${uploadErr.message}`);continue}const{data:pub}=sb.storage.from("lead-recordings").getPublicUrl(path);lastUrl=pub.publicUrl;uploaded++}catch(e){failures.push(`${file.name}: ${e&&e.message?e.message:e}`)}}const input=$("leadsQueueReviewRecordingUrl");if(input&&lastUrl)input.value=lastUrl;if(msg){if(failures.length){msg.textContent=`Uploaded ${uploaded} of ${files.length}. Failed: ${failures.join("; ")}`;msg.className="msg error"}else{msg.textContent=`Uploaded ${uploaded} file${uploaded===1?"":"s"} — link filled in above.`;msg.className="msg"}}if(btn)btn.disabled=false;const fileInput=$("leadsQueueReviewRecordingFile");if(fileInput)fileInput.value="";await refreshLeadsQueueRecordingList(lead.id)}$("leadsQueueReviewRecordingUploadBtn").addEventListener("click",()=>{const fileInput=$("leadsQueueReviewRecordingFile");const files=fileInput&&fileInput.files;if(!files||!files.length){const msg=$("leadsQueueReviewRecordingMsg");if(msg){msg.textContent="Choose one or more files first.";msg.className="msg error"}return}uploadLeadsQueueRecordings(files)});$("leadsQueueReviewRecordingList").addEventListener("click",async e=>{const useBtn=e.target.closest(".leads-queue-recording-use-btn")||e.target.closest(".leads-queue-recording-combine-btn");if(useBtn){const input=$("leadsQueueReviewRecordingUrl");if(input)input.value=useBtn.dataset.attachUrl;if(leadsQueueReviewModalLead)refreshLeadsQueueRecordingList(leadsQueueReviewModalLead.id);return}const delBtn=e.target.closest(".leads-queue-recording-del-btn");if(delBtn){if(!canEditLeadsSection("queue_review")){alert("Your role has view-only access to this section.");return}if(!confirm("Delete this uploaded file permanently? This can’t be undone."))return;const{error}=await sb.storage.from("lead-recordings").remove([delBtn.dataset.attachPath]);if(error){alert(error.message);return}const recInput=$("leadsQueueReviewRecordingUrl");if(recInput&&recInput.value.trim()===delBtn.dataset.attachUrl)recInput.value="";if(leadsQueueReviewModalLead)refreshLeadsQueueRecordingList(leadsQueueReviewModalLead.id)}});function renderLeadsQueueReviewExtraFields(lead){const entries=leadsExtraFieldsEntriesFn(lead.task_id,lead.submission_form_id,lead.extra_fields);const wrap=$("leadsQueueReviewExtraFieldsWrap");const container=$("leadsQueueReviewExtraFields");if(!entries.length){container.innerHTML="";wrap.style.display="none";return}container.innerHTML=entries.map(e=>{const val=String(e.value);const blankRequired=e.required&&!val.trim();const highlightStyle=blankRequired?"border:1px solid var(--away); background:rgba(255,159,10,0.08);":"";const requiredNote=blankRequired?`<div style="font-size:10px; color:var(--away); margin-top:3px;">Required — this field must be filled in to accept.</div>`:"";return`
      <div style="margin-bottom:8px;">
        <label style="font-size:11px; color:var(--text-dim); font-weight:400; display:block; margin-bottom:3px;">${escapeHtml(e.label)}${e.required?' <span style="color:var(--away);">*</span>':""}</label>
        ${leadsExtraFieldInputHtml(e,"leads-queue-review-extra-field",{requiredAttr:`data-required="${e.required?"true":"false"}"`,extraStyle:highlightStyle})}
        ${requiredNote}
      </div>`}).join("");wrap.style.display=""}let leadsQueueReviewAIFitByLeadId={};async function loadLeadsQueueReviewAIFit(lead){const wrap=$("leadsQueueReviewAIFitWrap");const container=$("leadsQueueReviewAIFit");if(!wrap||!container)return;leadsAiFitTranscriptShown=false;const fitResult=leadsCampaignFitFor(lead);if(!fitResult||!fitResult.verdicts.length){wrap.style.display="none";return}wrap.style.display="";const{data,error}=await sb.from("lead_ai_fit_evaluations").select("overall, summary, criteria, created_at, model, used_transcript, transcript_note").eq("lead_id",lead.id).eq("campaign_norm_name",fitResult.normName).maybeSingle();if(error){console.error("Failed to load AI fit evaluation",error)}leadsQueueReviewAIFitByLeadId[lead.id]=data||null;renderLeadsQueueReviewAIFit(lead,fitResult,data)}const AI_FIT_META={strong:{color:"var(--good)",label:"Strong Fit"},partial:{color:"var(--away)",label:"Partial Fit"},not_fit:{color:"var(--danger)",label:"Not a Fit"}};function renderLeadsQueueReviewAIFit(lead,fitResult,saved){const container=$("leadsQueueReviewAIFit");if(!container)return;if(!saved){container.innerHTML=`<div style="padding:6px 0;">No AI second opinion run yet for this lead.</div>`;return}const aiFitTz=leadsTzFor(lead.user_id);const when=new Date(saved.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...aiFitTz?{timeZone:aiFitTz}:{}});const meta=AI_FIT_META[saved.overall]||{color:"var(--text-dim)",label:saved.overall};const transcriptNote=saved.used_transcript?`<span style="color:var(--good);">🎤 Includes call recording transcript</span> <button type="button" class="btn-secondary leads-ai-fit-view-transcript-btn" data-lead-id="${lead.id}" style="width:auto; padding:2px 8px; font-size:10px; margin-left:6px;">View transcript</button>`:saved.transcript_note?`<span style="color:var(--text-dim);">${escapeHtml(saved.transcript_note)}</span>`:"";container.innerHTML=`
    <div style="margin:6px 0; padding:8px 10px; border-radius:6px; background:rgba(255,255,255,0.04); color:${meta.color};">
      ${meta.label} — ${escapeHtml(saved.summary||"")}
      <div style="color:var(--text-dim); font-size:10px; margin-top:2px;">Checked ${when} (${escapeHtml(saved.model||"AI")})</div>
      ${transcriptNote?`<div style="font-size:10px; margin-top:4px;">${transcriptNote}</div>`:""}
      <div id="leadsAiFitTranscriptBox" style="display:none; margin-top:8px; padding:8px; background:var(--inset); border-radius:6px; font-size:11px; white-space:pre-wrap; max-height:220px; overflow-y:auto;"></div>
    </div>
    ${(saved.criteria||[]).map(c=>{const hasConfidence=typeof c.confidence==="number";const lowConfidence=hasConfidence&&c.confidence<70;const needsFollowUp=c.verdict==="unclear"||lowConfidence;const confBadge=hasConfidence?`<span style="opacity:0.75; font-size:10px;"> · ${c.confidence}% confident</span>`:"";return`
      <div style="padding:6px 0; border-top:1px solid var(--border);">
        <div style="display:flex; justify-content:space-between; gap:8px;">
          <div style="flex:1;">
            <div style="color:var(--text);">${escapeHtml(c.label)}</div>
            <div style="color:var(--text-dim); font-size:10px;">${escapeHtml(c.reasoning||"")}</div>
          </div>
          <div style="white-space:nowrap; color:${c.verdict==="meets"?"var(--good)":c.verdict==="does_not_meet"?"var(--danger)":"var(--text-dim)"};">
            ${c.verdict==="meets"?"Meets":c.verdict==="does_not_meet"?"Doesn't Meet":"Unclear"}${confBadge}
          </div>
        </div>
        ${c.conflict?`<div style="margin-top:4px; padding:5px 7px; border-radius:5px; background:rgba(255,159,10,0.08); color:var(--away); font-size:10px;">⚠ Call vs. form conflict: ${escapeHtml(c.conflict)} (weighted toward what was said on the call)</div>`:""}
        ${needsFollowUp&&(c.missing_info||c.suggested_question)?`
        <div style="margin-top:4px; padding:5px 7px; border-radius:5px; background:rgba(255,255,255,0.04); font-size:10px;">
          ${c.missing_info?`<div style="color:var(--text-dim);">Missing: ${escapeHtml(c.missing_info)}</div>`:""}
          ${c.suggested_question?`<div style="color:var(--text); margin-top:2px;">Ask on follow-up: “${escapeHtml(c.suggested_question)}”</div>`:""}
        </div>`:""}
      </div>`}).join("")}
  `;const viewBtn=container.querySelector(".leads-ai-fit-view-transcript-btn");if(viewBtn)viewBtn.addEventListener("click",()=>toggleLeadAiFitTranscript(viewBtn.dataset.leadId))}let leadsAiFitTranscriptShown=false;async function toggleLeadAiFitTranscript(leadId){const box=$("leadsAiFitTranscriptBox");if(!box)return;leadsAiFitTranscriptShown=!leadsAiFitTranscriptShown;if(!leadsAiFitTranscriptShown){box.style.display="none";return}box.style.display="";box.textContent="Loading transcript…";const{data,error}=await sb.from("lead_recording_transcripts").select("transcript, status, source_url, created_at").eq("lead_id",leadId).eq("status","done").order("created_at");if(error){box.textContent=`Couldn't load transcript: ${error.message}`;return}const parts=(data||[]).filter(r=>r.transcript);box.textContent=parts.length?parts.length===1?parts[0].transcript:parts.map((p,i)=>`Recording ${i+1}:
${p.transcript}`).join("\n\n"):"No cached transcript found."}async function runCampaignFitAIValidator(lead){const btn=$("leadsQueueReviewAIFitBtn");const container=$("leadsQueueReviewAIFit");const fitResult=leadsCampaignFitFor(lead);if(!fitResult||!fitResult.verdicts.length)return;btn.disabled=true;btn.textContent="Checking…";container.innerHTML=`<div style="padding:6px 0;">Asking AI for a second opinion…</div>`;try{const criteria=fitResult.verdicts.map(v=>({label:v.label,expected:v.expected}));const{data,error}=await sb.functions.invoke("campaign-fit-ai-validator",{body:{lead_id:lead.id,campaign_norm_name:fitResult.normName,campaign_display_name:fitResult.campaignName,criteria}});if(error||!data||!data.ok)throw new Error(data&&data.error||error&&error.message||"AI validator failed");renderLeadsQueueReviewAIFit(lead,fitResult,{...data.result,model:void 0,created_at:new Date().toISOString()});await loadLeadsQueueReviewAIFit(lead);const savedRow=leadsQueueReviewAIFitByLeadId[lead.id];if(savedRow){(leadAiFitEvaluationsByLeadId[lead.id]||={})[fitResult.normName]={...savedRow,campaign_norm_name:fitResult.normName};renderLeadsQueueTable()}}catch(e){console.error("AI fit validator failed",e);container.innerHTML=`<div style="padding:6px 0; color:var(--danger);">Couldn't get an AI second opinion: ${escapeHtml(e.message||String(e))}</div>`}finally{btn.disabled=false;btn.textContent="Get AI Second Opinion"}}if($("leadsQueueReviewAIFitBtn"))$("leadsQueueReviewAIFitBtn").addEventListener("click",()=>{if(leadsQueueReviewModalLead)runCampaignFitAIValidator(leadsQueueReviewModalLead)});async function openLeadsQueueReviewModal(lead,agentLabel){leadsQueueReviewModalLead=lead;$("leadsQueueReviewLeadId").value=lead.id;const reviewModalTz=leadsTzFor(lead.user_id);$("leadsQueueReviewSellerLabel").textContent=`Agent: ${agentLabel} — ${lead.count} lead(s) — submitted ${new Date(lead.submitted_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...reviewModalTz?{timeZone:reviewModalTz}:{}})}`;$("leadsQueueReviewSellerName").value=lead.seller_investor_name||"";$("leadsQueueReviewPhone").value=lead.seller_phone||"";$("leadsQueueReviewTask").textContent=leadsFormNameFn(lead.submission_form_id);renderLeadsQueueReviewExtraFields(lead);loadLeadsQueueReviewAIFit(lead);$("leadsQueueReviewRecordingUrl").value=lead.extra_fields&&lead.extra_fields[LEADS_QUEUE_RECORDING_FIELD_KEY]||"";$("leadsQueueReviewRecordingMsg").textContent="";$("leadsQueueReviewModalMsg").style.display="none";$("leadsQueueReviewRecordingFile").value="";await refreshLeadsQueueRecordingList(lead.id);resetLeadsQueueReviewTicketNotes();const sendTicketBtn=$("leadsQueueReviewSendTicketBtn");if(sendTicketBtn)sendTicketBtn.style.display=lead.ticket_stage?"none":"";$("leadsQueueReviewModalOverlay").classList.add("active")}function closeLeadsQueueReviewModal(){$("leadsQueueReviewModalOverlay").classList.remove("active");leadsQueueReviewModalLead=null}$("leadsQueueReviewCancelBtn").addEventListener("click",closeLeadsQueueReviewModal);$("leadsQueueReviewModalOverlay").addEventListener("click",e=>{if(e.target.id==="leadsQueueReviewModalOverlay")closeLeadsQueueReviewModal()});function guardedClick(btnOrId,handler){const btn=typeof btnOrId==="string"?document.getElementById(btnOrId):btnOrId;if(!btn)return;btn.addEventListener("click",async ev=>{if(btn.dataset.busy==="1")return;btn.dataset.busy="1";btn.disabled=true;try{await handler(ev)}catch(err){console.error(`guardedClick(${btnId}) failed:`,err)}finally{delete btn.dataset.busy;btn.disabled=false}})}async function saveLeadsQueueReviewFieldsAndSetStatus(status){const lead=leadsQueueReviewModalLead;if(!lead)return;const msgEl=$("leadsQueueReviewModalMsg");msgEl.style.display="none";let reviewNote=null;if(status==="rejected"){reviewNote=prompt(`Why is this lead being rejected? (required)

Agent — ${lead.count} lead(s)`);if(reviewNote===null)return;reviewNote=reviewNote.trim();if(!reviewNote){alert("A comment is required to reject a lead.");return}}const sellerName=$("leadsQueueReviewSellerName").value.trim();const phone=$("leadsQueueReviewPhone").value.trim();const recordingUrl=$("leadsQueueReviewRecordingUrl").value.trim();if(status==="accepted"){const missing=[];if(!sellerName)missing.push("Seller / Investor Name");if(!phone)missing.push("Phone");if(!recordingUrl)missing.push("Recording (Attach Recording(s) — paste a URL or upload a file)");document.querySelectorAll("#leadsQueueReviewExtraFields .leads-queue-review-extra-field").forEach(el=>{if(el.dataset.required==="true"&&!el.value.trim()){const label=el.closest("div")?.querySelector("label")?.textContent.replace(/[\s*\u26a0]+$/,"")||el.dataset.key;missing.push(label)}});if(missing.length){msgEl.textContent=`Required fields missing: ${missing.join(", ")}.`;msgEl.style.display="";return}}const extraFields={...lead.extra_fields||{}};if(recordingUrl)extraFields[LEADS_QUEUE_RECORDING_FIELD_KEY]=recordingUrl;else delete extraFields[LEADS_QUEUE_RECORDING_FIELD_KEY];document.querySelectorAll("#leadsQueueReviewExtraFields .leads-queue-review-extra-field").forEach(el=>{const key=el.dataset.key;const val=el.value.trim();if(val)extraFields[key]=val;else delete extraFields[key]});const{data:{user}}=await sb.auth.getUser();const update={seller_investor_name:sellerName||null,seller_phone:phone||null,extra_fields:extraFields,status,reviewed_by:user?user.id:null,reviewed_at:new Date().toISOString()};if(status==="rejected")update.review_note=reviewNote;else if(status==="accepted")update.review_note=null;const{error}=await sb.from("leads").update(update).eq("id",lead.id);if(error){msgEl.textContent=error.message;msgEl.style.display="";return}notifyIfLeadAccepted(lead,status);await loadLeads();await refreshDetailIfViewing(lead.user_id);logActivity("lead.review","lead",lead.id,`Marked ${status} — ${lead.count} lead(s)${status==="rejected"?` — reason: ${reviewNote}`:""}`);closeLeadsQueueReviewModal()}guardedClick("leadsQueueReviewAcceptBtn",()=>saveLeadsQueueReviewFieldsAndSetStatus("accepted"));guardedClick("leadsQueueReviewRejectBtn",()=>saveLeadsQueueReviewFieldsAndSetStatus("rejected"));function resetLeadsQueueReviewTicketNotes(){const wrap=$("leadsQueueReviewTicketNotesWrap");const btn=$("leadsQueueReviewSendTicketBtn");if(wrap)wrap.style.display="none";if($("leadsQueueReviewTicketNotes"))$("leadsQueueReviewTicketNotes").value="";if(btn)btn.textContent="Send as Ticket"}async function sendLeadAsTicket(lead,notes){const msgEl=$("leadsQueueReviewModalMsg");const{data:{user}}=await sb.auth.getUser();const extraFields={...lead.extra_fields||{},ticket_notes:notes};const{data:updatedRows,error}=await sb.from("leads").update({ticket_stage:"client_review",status:"pending",review_note:null,reviewed_by:user?user.id:null,reviewed_at:new Date().toISOString(),extra_fields:extraFields}).eq("id",lead.id).or("ticket_stage.is.null,ticket_stage.neq.client_review").select("id");if(error){msgEl.textContent=error.message;msgEl.style.display="";return}if(!updatedRows||!updatedRows.length){msgEl.textContent="This lead is already waiting on the client — no second ticket was sent.";msgEl.style.display="";await loadLeads();return}notifyClientsForFormId(lead.submission_form_id,"ticket_received",leadsFormNameFn?leadsFormNameFn(lead.submission_form_id):null);await loadLeads();await refreshDetailIfViewing(lead.user_id);logActivity("lead.sent_as_ticket","lead",lead.id,`Sent as ticket for client Yes/No — ${lead.count} lead(s)`);closeLeadsQueueReviewModal()}guardedClick("leadsQueueReviewSendTicketBtn",async()=>{const lead=leadsQueueReviewModalLead;if(!lead)return;const wrap=$("leadsQueueReviewTicketNotesWrap");const btn=$("leadsQueueReviewSendTicketBtn");const msgEl=$("leadsQueueReviewModalMsg");if(wrap.style.display==="none"){wrap.style.display="";btn.textContent="Confirm Send as Ticket";$("leadsQueueReviewTicketNotes").focus();return}const notes=$("leadsQueueReviewTicketNotes").value.trim();if(!notes){msgEl.textContent="Ticket Notes is required to send this as a ticket.";msgEl.style.display="";return}msgEl.style.display="none";await sendLeadAsTicket(lead,notes)});function callBackFieldOptions(lead){const options=[{key:"seller_investor_name",label:"Seller / Investor Name",value:lead.seller_investor_name||""},{key:"seller_phone",label:"Phone",value:lead.seller_phone||""}];leadsExtraFieldsEntriesFn(lead.task_id,lead.submission_form_id,lead.extra_fields).filter(e=>!RESERVED_FIELD_KEYS.has(e.key)).forEach(e=>options.push({key:e.key,label:e.label,value:e.value}));return options}let sendToCallBackLead=null;function openSendToCallBackModal(lead,agentLabel){sendToCallBackLead=lead;$("sendToCallBackLeadId").value=lead.id;$("sendToCallBackLeadLabel").textContent=`Agent: ${agentLabel} — ${lead.count} lead(s) — ${lead.seller_investor_name||"no name yet"}`;$("sendToCallBackReason").value="";const options=callBackFieldOptions(lead);$("sendToCallBackFields").innerHTML=options.map(o=>`
    <label style="display:flex; align-items:center; gap:8px; font-size:13px; font-weight:400; text-transform:none; cursor:pointer;">
      <input type="checkbox" class="callback-field-cb" value="${escapeHtml(o.key)}" style="width:15px; height:15px; accent-color:var(--focus);">
      ${escapeHtml(o.label)} <span style="color:var(--text-dim); font-size:11px;">${o.value?"— currently: "+escapeHtml(String(o.value).slice(0,40)):"(blank)"}</span>
    </label>
  `).join("");$("sendToCallBackMsg").style.display="none";$("sendToCallBackModalOverlay").classList.add("active")}function closeSendToCallBackModal(){$("sendToCallBackModalOverlay").classList.remove("active");sendToCallBackLead=null}$("sendToCallBackCancelBtn").addEventListener("click",closeSendToCallBackModal);$("sendToCallBackModalOverlay").addEventListener("click",e=>{if(e.target.id==="sendToCallBackModalOverlay")closeSendToCallBackModal()});guardedClick("sendToCallBackSubmitBtn",async()=>{const lead=sendToCallBackLead;if(!lead)return;const msg=$("sendToCallBackMsg");const reason=$("sendToCallBackReason").value.trim();const fields=Array.from(document.querySelectorAll(".callback-field-cb:checked")).map(cb=>cb.value);if(!reason){msg.textContent="A reason is required.";msg.style.display="";return}if(!fields.length){msg.textContent="Check at least one field that needs confirming.";msg.style.display="";return}msg.style.display="none";const{data:{user}}=await sb.auth.getUser();const{error}=await sb.from("leads").update({status:"call_back",callback_reason:reason,callback_fields_to_confirm:fields,callback_claimed_by:null,callback_claimed_at:null,reviewed_by:user?user.id:null,reviewed_at:new Date().toISOString()}).eq("id",lead.id);if(error){msg.textContent=error.message;msg.style.display="";return}await loadLeads();logActivity("lead.call_back","lead",lead.id,`Sent to Call Back — ${lead.count} lead(s) — reason: ${reason} — fields to confirm: ${fields.join(", ")}`);closeSendToCallBackModal()});let leadsCallBackRows=[];async function loadCallBackQueue(){const{data,error}=await sb.from("leads").select("id, user_id, task_id, submission_form_id, count, submitted_at, seller_investor_name, seller_phone, extra_fields, callback_reason, callback_fields_to_confirm, callback_claimed_by, callback_claimed_at").eq("status","call_back").order("submitted_at",{ascending:false});if(error){console.error("Failed to load Call Back queue",error);return}leadsCallBackRows=data||[];if(callBackIsManager()&&!callBackAssignees)await loadCallBackAssignees();renderCallBackTables()}function callBackIsManager(){return myIsOwner||myRole==="admin"||String(myRole||"").toLowerCase().replace(/[\s_-]+/g," ").trim()==="team leader"}let callBackAssignees=null;async function loadCallBackAssignees(){const{data,error}=await sb.rpc("leads_callback_assignees");if(error){console.warn("call back assignees:",error.message);callBackAssignees=[];return}callBackAssignees=data||[]}function callBackPersonName(id){if(!id)return"—";if(id===myUserId)return"You";const a=(callBackAssignees||[]).find(x=>x.id===id);if(a)return a.full_name;const n=leadsAgentNameFn(id);return n&&n!=="—"?n:"Someone no longer on the list"}function callBackAssignSelectHtml(leadId,currentId){const opts=(callBackAssignees||[]).filter(a=>a.id!==currentId).map(a=>`<option value="${a.id}">${escapeHtml(a.full_name+(a.id===myUserId?" (you)":""))}</option>`).join("");return`<select class="callback-assign-sel" data-id="${r_esc(leadId)}" style="width:auto; min-width:150px; padding:5px 8px; font-size:11px;"><option value="">${currentId?"Reassign to…":"Assign to…"}</option>${opts}</select>`}function r_esc(v){return escapeHtml(String(v||""))}async function assignCallBackLead(leadId,toId){const lead=leadsCallBackRows.find(r=>r.id===leadId);const label=lead&&lead.seller_investor_name||"this lead";const who=toId?callBackPersonName(toId):"";const ok=confirm(toId?`Give the call back for ${label} to ${who}?`:`Pull the call back for ${label} back to "Available to claim"?`);if(!ok){renderCallBackTables();return}const{data,error}=await sb.rpc("leads_callback_assign",{p_lead:leadId,p_to:toId||null});if(error){alert(error.message);await loadCallBackQueue();return}const from=data&&data.from?callBackPersonName(data.from):"nobody";logActivity(toId?"lead.call_back_assign":"lead.call_back_pull","lead",leadId,toId?`Call back for ${label} reassigned from ${from} to ${who}`:`Call back for ${label} pulled back from ${from}`);await loadCallBackQueue()}let callBackLiveTimer=null;function callBackLiveSoon(){clearTimeout(callBackLiveTimer);callBackLiveTimer=setTimeout(()=>{const panel=$("tab-leads");if(!document.hidden&&panel&&panel.classList.contains("active")&&leadsActiveSection==="call_back")loadCallBackQueue()},1500)}function callBackFieldsSummary(lead){const byKey={};callBackFieldOptions(lead).forEach(o=>{byKey[o.key]=o.label});return(lead.callback_fields_to_confirm||[]).map(k=>byKey[k]||k).join(", ")||"—"}let callBackAvailSelectedIds=new Set;let callBackMineSelectedIds=new Set;function updateCallBackBulkToolbar(scope){const canDelete=myIsOwner||myRole==="admin";const ids=scope==="avail"?callBackAvailSelectedIds:callBackMineSelectedIds;const toolbar=$(scope==="avail"?"callBackAvailBulkToolbar":"callBackMineBulkToolbar");const selectAllTh=$(scope==="avail"?"callBackAvailSelectAllTh":"callBackMineSelectAllTh");const countEl=$(scope==="avail"?"callBackAvailBulkSelectedCount":"callBackMineBulkSelectedCount");const deleteBtn=$(scope==="avail"?"callBackAvailBulkDeleteBtn":"callBackMineBulkDeleteBtn");toolbar.style.display=canDelete?"flex":"none";selectAllTh.style.display=canDelete?"":"none";if(!canDelete)return;const n=ids.size;countEl.textContent=n===1?"1 selected":`${n} selected`;deleteBtn.disabled=n===0}async function bulkDeleteCallBackLeads(scope){const canDelete=myIsOwner||myRole==="admin";if(!canDelete)return;const ids=Array.from(scope==="avail"?callBackAvailSelectedIds:callBackMineSelectedIds);if(!ids.length)return;if(!confirm(`Permanently delete these ${ids.length} lead(s)? This can't be undone.`))return;const{error}=await sb.from("leads").delete().in("id",ids);if(error){alert(error.message);return}logActivity("lead.delete","lead",null,`Bulk deleted ${ids.length} lead(s) from Call Back — ${scope==="avail"?"Available to claim":"My claimed call backs"}`);(scope==="avail"?callBackAvailSelectedIds:callBackMineSelectedIds).clear();await loadCallBackQueue()}function renderCallBackTables(){const agentName=leadsAgentNameFn,formName=leadsFormNameFn;const canDeleteLead=myIsOwner||myRole==="admin";const available=leadsCallBackRows.filter(r=>!r.callback_claimed_by);const mine=leadsCallBackRows.filter(r=>r.callback_claimed_by===myUserId);const availIds=new Set(available.map(r=>r.id));Array.from(callBackAvailSelectedIds).forEach(id=>{if(!availIds.has(id))callBackAvailSelectedIds.delete(id)});const mineIds=new Set(mine.map(r=>r.id));Array.from(callBackMineSelectedIds).forEach(id=>{if(!mineIds.has(id))callBackMineSelectedIds.delete(id)});const availBody=$("callBackAvailableBody");$("callBackAvailableEmpty").style.display=available.length?"none":"block";availBody.innerHTML=available.map(r=>`
    <tr>
      ${canDeleteLead?`<td><input type="checkbox" class="callback-avail-select-cb" data-id="${r.id}" ${callBackAvailSelectedIds.has(r.id)?"checked":""}></td>`:""}
      <td>${escapeHtml(r.seller_investor_name||"—")}</td>
      <td class="mono">${escapeHtml(r.seller_phone||"—")}</td>
      <td>${escapeHtml(formName(r.submission_form_id))}</td>
      <td>${textPreviewCellHtml(r.callback_reason,"Call back reason")}</td>
      <td style="max-width:200px;">${escapeHtml(callBackFieldsSummary(r))}</td>
      <td style="color:var(--text-2);">${new Date(r.submitted_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...leadsTzFor(r.user_id)?{timeZone:leadsTzFor(r.user_id)}:{}})}</td>
      <td style="white-space:nowrap;"><button class="btn-secondary callback-claim-btn" data-id="${r.id}" style="width:auto; padding:5px 10px; font-size:11px;">Pick up</button>${callBackIsManager()?" "+callBackAssignSelectHtml(r.id,null):""}</td>
    </tr>
  `).join("");availBody.querySelectorAll(".callback-claim-btn").forEach(btn=>{btn.addEventListener("click",()=>claimCallBackLead(btn.dataset.id))});availBody.querySelectorAll(".callback-avail-select-cb").forEach(cb=>{cb.addEventListener("change",()=>{if(cb.checked)callBackAvailSelectedIds.add(cb.dataset.id);else callBackAvailSelectedIds.delete(cb.dataset.id);updateCallBackBulkToolbar("avail")})});updateCallBackBulkToolbar("avail");const mineBody=$("callBackMineBody");$("callBackMineEmpty").style.display=mine.length?"none":"block";mineBody.innerHTML=mine.map(r=>`
    <tr>
      ${canDeleteLead?`<td><input type="checkbox" class="callback-mine-select-cb" data-id="${r.id}" ${callBackMineSelectedIds.has(r.id)?"checked":""}></td>`:""}
      <td>${escapeHtml(r.seller_investor_name||"—")}</td>
      <td class="mono">${escapeHtml(r.seller_phone||"—")}</td>
      <td>${escapeHtml(formName(r.submission_form_id))}</td>
      <td>${textPreviewCellHtml(r.callback_reason,"Call back reason")}</td>
      <td style="max-width:200px;">${escapeHtml(callBackFieldsSummary(r))}</td>
      <td style="color:var(--text-2);">${r.callback_claimed_at?new Date(r.callback_claimed_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...leadsTzFor(r.user_id)?{timeZone:leadsTzFor(r.user_id)}:{}}):"—"}</td>
      <td><button class="btn-primary callback-confirm-btn" data-id="${r.id}" style="width:auto; padding:5px 10px; font-size:11px; margin-top:0;">Confirm details</button></td>
    </tr>
  `).join("");mineBody.querySelectorAll(".callback-confirm-btn").forEach(btn=>{btn.addEventListener("click",()=>openCallBackConfirmModal(btn.dataset.id))});mineBody.querySelectorAll(".callback-mine-select-cb").forEach(cb=>{cb.addEventListener("change",()=>{if(cb.checked)callBackMineSelectedIds.add(cb.dataset.id);else callBackMineSelectedIds.delete(cb.dataset.id);updateCallBackBulkToolbar("mine")})});updateCallBackBulkToolbar("mine");const teamCard=$("callBackTeamCard");const manager=callBackIsManager();if(teamCard)teamCard.style.display=manager?"":"none";if(teamCard&&!manager)$("callBackTeamBody").innerHTML="";if(teamCard&&manager){const claimed=leadsCallBackRows.filter(r=>r.callback_claimed_by).sort((a,b)=>String(b.callback_claimed_at||"").localeCompare(String(a.callback_claimed_at||"")));$("callBackTeamEmpty").style.display=claimed.length?"none":"block";$("callBackTeamBody").innerHTML=claimed.map(r=>`
      <tr>
        <td>${escapeHtml(r.seller_investor_name||"—")}</td>
        <td class="mono">${escapeHtml(r.seller_phone||"—")}</td>
        <td>${escapeHtml(formName(r.submission_form_id))}</td>
        <td>${textPreviewCellHtml(r.callback_reason,"Call back reason")}</td>
        <td style="font-weight:600;">${escapeHtml(callBackPersonName(r.callback_claimed_by))}</td>
        <td style="color:var(--text-2);">${r.callback_claimed_at?new Date(r.callback_claimed_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...leadsTzFor(r.user_id)?{timeZone:leadsTzFor(r.user_id)}:{}}):"—"}</td>
        <td>${callBackAssignSelectHtml(r.id,r.callback_claimed_by)}</td>
        <td><button class="btn-secondary callback-pull-btn" data-id="${r.id}" style="width:auto; padding:5px 10px; font-size:11px;">Pull back</button></td>
      </tr>
    `).join("");$("callBackTeamBody").querySelectorAll(".callback-pull-btn").forEach(btn=>{btn.addEventListener("click",()=>assignCallBackLead(btn.dataset.id,null))})}if(manager){document.querySelectorAll("#leadsSection-call_back .callback-assign-sel").forEach(sel=>{sel.addEventListener("change",()=>{if(sel.value)assignCallBackLead(sel.dataset.id,sel.value)})})}}$("callBackRefreshBtn").addEventListener("click",loadCallBackQueue);$("callBackAvailSelectAllCb").addEventListener("change",e=>{document.querySelectorAll(".callback-avail-select-cb").forEach(cb=>{cb.checked=e.target.checked;if(e.target.checked)callBackAvailSelectedIds.add(cb.dataset.id);else callBackAvailSelectedIds.delete(cb.dataset.id)});updateCallBackBulkToolbar("avail")});$("callBackMineSelectAllCb").addEventListener("change",e=>{document.querySelectorAll(".callback-mine-select-cb").forEach(cb=>{cb.checked=e.target.checked;if(e.target.checked)callBackMineSelectedIds.add(cb.dataset.id);else callBackMineSelectedIds.delete(cb.dataset.id)});updateCallBackBulkToolbar("mine")});$("callBackAvailBulkDeleteBtn").addEventListener("click",()=>bulkDeleteCallBackLeads("avail"));$("callBackMineBulkDeleteBtn").addEventListener("click",()=>bulkDeleteCallBackLeads("mine"));async function claimCallBackLead(leadId){const{data,error}=await sb.from("leads").update({callback_claimed_by:myUserId,callback_claimed_at:new Date().toISOString()}).eq("id",leadId).eq("status","call_back").is("callback_claimed_by",null).select("id");if(error){alert(error.message);return}if(!data||!data.length){alert("Someone else just claimed this one — refreshing the list.");await loadCallBackQueue();return}logActivity("lead.call_back_claim","lead",leadId,"Claimed a call back lead");await loadCallBackQueue()}let callBackConfirmLead=null;async function openCallBackConfirmModal(leadId){const lead=leadsCallBackRows.find(r=>r.id===leadId);if(!lead)return;callBackConfirmLead=lead;$("callBackConfirmLeadId").value=lead.id;$("callBackConfirmLeadLabel").textContent=`${lead.seller_investor_name||"No name yet"} — ${lead.count} lead(s) — ${leadsFormNameFn(lead.submission_form_id)}`;$("callBackConfirmReason").textContent=lead.callback_reason||"—";let formFieldRows=[];if(lead.submission_form_id){const{data}=await sb.from("lead_submission_form_fields").select("form_id, field_key, label, field_type, options, required, placeholder, sort_order").eq("form_id",lead.submission_form_id);formFieldRows=data||[]}const byKey={};formFieldRows.forEach(f=>{byKey[f.field_key]=f});const optionLabels={};callBackFieldOptions(lead).forEach(o=>{optionLabels[o.key]=o.label});const keys=lead.callback_fields_to_confirm||[];const container=$("callBackConfirmFields");container.innerHTML=keys.map(key=>{const def=byKey[key]||{field_key:key,label:optionLabels[key]||key,field_type:key==="seller_phone"?"tel":"text"};return`<div style="margin-bottom:12px;">${fillFieldInputHtml({...def,required:true})}</div>`}).join("");wireCampaignNameAutocompletes(container);wireGenericAddressAutocomplete(container);const valueByKey={};callBackFieldOptions(lead).forEach(o=>{valueByKey[o.key]=o.value});keys.forEach(key=>{const el=container.querySelector(`[data-field-key="${CSS.escape(key)}"]`);if(!el)return;const current=valueByKey[key]||"";if(el.dataset.fieldType==="multi"){String(current).split("\n").forEach(v=>{const cb=el.querySelector(`input[type="checkbox"][value="${CSS.escape(v)}"]`);if(cb)cb.checked=true})}else{el.value=current}});$("callBackConfirmMsg").style.display="none";$("callBackConfirmModalOverlay").classList.add("active")}function closeCallBackConfirmModal(){$("callBackConfirmModalOverlay").classList.remove("active");callBackConfirmLead=null}$("callBackConfirmCancelBtn").addEventListener("click",closeCallBackConfirmModal);$("callBackConfirmModalOverlay").addEventListener("click",e=>{if(e.target.id==="callBackConfirmModalOverlay")closeCallBackConfirmModal()});$("callBackConfirmSubmitBtn").addEventListener("click",async()=>{const lead=callBackConfirmLead;if(!lead)return;const msg=$("callBackConfirmMsg");const fieldEls=Array.from($("callBackConfirmFields").querySelectorAll("[data-field-key]"));const missing=[];fieldEls.forEach(el=>{const label=el.dataset.fieldLabel||el.dataset.fieldKey;if(el.dataset.fieldType==="multi"){if(el.querySelectorAll('input[type="checkbox"]:checked').length===0)missing.push(label)}else if(!el.value.trim()){missing.push(label)}});if(missing.length){msg.textContent=`Required: ${missing.join(", ")}.`;msg.style.display="";return}const invalidCampaignField=fieldEls.find(el=>el.dataset.campaignAutocomplete==="1"&&el.value.trim()&&!campaignNameFieldIsValid(el));if(invalidCampaignField){const label=invalidCampaignField.dataset.fieldLabel||invalidCampaignField.dataset.fieldKey;msg.textContent=`"${label}" must be an existing campaign — pick one from the list instead of typing a new name.`;msg.style.display="";return}msg.style.display="none";let sellerName=lead.seller_investor_name||null;let phone=lead.seller_phone||null;const extraFields={...lead.extra_fields||{}};fieldEls.forEach(el=>{const key=el.dataset.fieldKey;const value=el.dataset.fieldType==="multi"?Array.from(el.querySelectorAll('input[type="checkbox"]:checked')).map(cb=>cb.value).join("\n"):el.value.trim();if(key==="seller_investor_name")sellerName=value||null;else if(key==="seller_phone")phone=value||null;else if(value)extraFields[key]=value;else delete extraFields[key]});const{error}=await sb.from("leads").update({seller_investor_name:sellerName,seller_phone:phone,extra_fields:extraFields,status:"pending",callback_reason:null,callback_fields_to_confirm:[],callback_claimed_by:null,callback_claimed_at:null,reviewed_by:null,reviewed_at:null}).eq("id",lead.id).eq("callback_claimed_by",myUserId);if(error){msg.textContent=error.message;msg.style.display="";return}logActivity("lead.call_back_confirm","lead",lead.id,`Confirmed call back details — ${lead.count} lead(s) — back to Pending`);await loadCallBackQueue();closeCallBackConfirmModal()});function openTextDetailModal(title,text){$("textDetailModalTitle").textContent=title;$("textDetailModalBody").textContent=text||"—";$("textDetailModalOverlay").classList.add("active")}$("textDetailModalCloseBtn").addEventListener("click",()=>$("textDetailModalOverlay").classList.remove("active"));$("textDetailModalOverlay").addEventListener("click",e=>{if(e.target.id==="textDetailModalOverlay")$("textDetailModalOverlay").classList.remove("active")});function textPreviewCellHtml(text,label){const full=(text||"").trim();if(!full)return"—";const preview=full.length>40?full.slice(0,40)+"…":full;const needsView=full.length>40;return`<span class="text-preview-cell" data-full="${escapeHtml(full)}" data-label="${escapeHtml(label||"Details")}" style="cursor:pointer; display:inline-block; max-width:100%; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; vertical-align:bottom;">${escapeHtml(preview)}${needsView?' <span style="color:var(--focus); font-size:11px;">View</span>':""}</span>`}document.addEventListener("click",e=>{const cell=e.target.closest(".text-preview-cell");if(!cell)return;openTextDetailModal(cell.dataset.label||"Details",cell.dataset.full)});async function refreshIspeedToLeadModalStatus(leadId){const{data:history2}=await sb.from("ispeedtolead_submissions").select("id, success, created_at, error, response_status, submitted_by").eq("lead_id",leadId).order("created_at",{ascending:false}).limit(20);const rows=history2||[];const latest=rows[0];const badge=$("ispeedtoleadModalSentBadge");if(!latest){badge.textContent="Never sent"}else{const when=new Date(latest.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{}});badge.textContent=latest.success?`Sent successfully — ${when}`:`Last attempt failed — ${when}`;badge.style.color=latest.success?"var(--good)":"var(--danger)"}await renderIspeedToLeadHistory(rows);return latest}async function renderIspeedToLeadHistory(rows){const box=$("ispeedtoleadModalHistory");if(!box)return;if(!rows.length){box.innerHTML='<div style="font-size:11px; color:var(--text-dim);">No send attempts yet.</div>';return}const submitterIds=[...new Set(rows.map(r=>r.submitted_by).filter(Boolean))];let nameById={};if(submitterIds.length){const{data:submitters}=await sb.from("profiles").select("id, full_name, email").in("id",submitterIds);(submitters||[]).forEach(p=>{nameById[p.id]=p.full_name||p.email||"Unknown"})}const historyTz=nonAgentDisplayTz();box.innerHTML=rows.map(r=>{const when=new Date(r.created_at).toLocaleString([],{month:"short",day:"numeric",year:"numeric",hour:"2-digit",minute:"2-digit",...historyTz?{timeZone:historyTz}:{}});const who=r.submitted_by?nameById[r.submitted_by]||"Unknown":null;const statusLabel=r.success?'<span style="color:var(--good);">Success</span>':`<span style="color:var(--danger);">Failed${r.response_status?" ("+r.response_status+")":""}</span>`;const errText=!r.success&&r.error?typeof r.error==="string"?r.error:JSON.stringify(r.error):"";const errLine=errText?`<div style="font-size:10px; color:var(--text-dim); margin-top:2px;">${escapeHtml(errText)}</div>`:"";return`
      <div style="display:flex; justify-content:space-between; gap:10px; padding:6px 0; border-bottom:1px solid var(--border); font-size:11px;">
        <div>
          <div>${statusLabel}${who?" · "+escapeHtml(who):""}</div>
          ${errLine}
        </div>
        <div class="mono" style="color:var(--text-dim); white-space:nowrap;">${when}</div>
      </div>
    `}).join("")}let ispeedtoleadModalAgentLabel=null;async function openIspeedToLeadModal(lead,agentLabel){ispeedtoleadModalLead=lead;ispeedtoleadModalAgentLabel=agentLabel;$("ispeedtoleadModalLeadId").value=lead.id;$("ispeedtoleadModalSellerLabel").textContent=`${lead.seller_investor_name||"Unnamed seller"} — ${lead.seller_phone||"no phone on file"} (agent: ${agentLabel})`;$("ispeedtoleadModalFields").innerHTML=renderIspeedToLeadFields(lead.extra_fields,lead.seller_investor_name);wireAddressAutocompleteAndDupCheck($("ispeedtoleadModalFields"),{getPhoneNorm:()=>(lead.seller_phone||"").replace(/\D/g,"").slice(-10),getExcludeLeadId:()=>lead.id});$("ispeedtoleadModalMsg").style.display="none";$("ispeedtoleadModalStatus").style.display="none";Object.keys(ISPEEDTOLEAD_ATTACHMENT_FIELDS).forEach(fieldKey=>refreshIspeedtoleadAttachmentList(lead.id,fieldKey));await refreshIspeedToLeadModalStatus(lead.id);$("ispeedtoleadModalOverlay").classList.add("active")}function closeIspeedToLeadModal(){$("ispeedtoleadModalOverlay").classList.remove("active");ispeedtoleadModalLead=null;ispeedtoleadModalAgentLabel=null}$("ispeedtoleadModalCancelBtn").addEventListener("click",closeIspeedToLeadModal);$("ispeedtoleadModalOverlay").addEventListener("click",e=>{if(e.target.id==="ispeedtoleadModalOverlay")closeIspeedToLeadModal()});$("ispeedtoleadModalGyhomesBtn").addEventListener("click",async()=>{if(!ispeedtoleadModalLead)return;const msg=$("ispeedtoleadModalMsg");msg.style.display="none";const ispdProblem=ispdContactFieldProblem();if(ispdProblem){msg.textContent=ispdProblem;msg.style.display="block";return}const updated=collectIspeedToLeadFields(ispeedtoleadModalLead.extra_fields);const{error}=await sb.from("leads").update({extra_fields:updated}).eq("id",ispeedtoleadModalLead.id);if(error){msg.textContent=error.message;msg.style.display="block";return}ispeedtoleadModalLead.extra_fields=updated;const lead=ispeedtoleadModalLead;const agentLabel=ispeedtoleadModalAgentLabel;closeIspeedToLeadModal();openGyhomesModal(lead,agentLabel)});$("ispeedtoleadModalSaveBtn").addEventListener("click",async()=>{if(!ispeedtoleadModalLead)return;const msg=$("ispeedtoleadModalMsg");msg.style.display="none";const ispdProblem=ispdContactFieldProblem();if(ispdProblem){msg.textContent=ispdProblem;msg.style.display="block";return}const updated=collectIspeedToLeadFields(ispeedtoleadModalLead.extra_fields);const{error}=await sb.from("leads").update({extra_fields:updated}).eq("id",ispeedtoleadModalLead.id);if(error){msg.textContent=error.message;msg.style.display="block";return}ispeedtoleadModalLead.extra_fields=updated;logActivity("lead.update","lead",ispeedtoleadModalLead.id,"Updated PrimeHome Buyers details");await loadLeads();const statusEl=$("ispeedtoleadModalStatus");statusEl.textContent="Details saved.";statusEl.style.display="block"});$("ispeedtoleadModalSendBtn").addEventListener("click",async()=>{if(!ispeedtoleadModalLead)return;const msg=$("ispeedtoleadModalMsg");const statusEl=$("ispeedtoleadModalStatus");msg.style.display="none";statusEl.style.display="none";const ispdSendProblem=ispdContactFieldProblem();if(ispdSendProblem){msg.textContent=ispdSendProblem;msg.style.display="block";return}const updated=collectIspeedToLeadFields(ispeedtoleadModalLead.extra_fields);const{error:saveErr}=await sb.from("leads").update({extra_fields:updated}).eq("id",ispeedtoleadModalLead.id);if(saveErr){msg.textContent=saveErr.message;msg.style.display="block";return}ispeedtoleadModalLead.extra_fields=updated;const sendBtn=$("ispeedtoleadModalSendBtn");sendBtn.disabled=true;sendBtn.textContent="Sending…";try{let{data,error}=await sb.functions.invoke("ispeedtolead-submit",{body:{lead_id:ispeedtoleadModalLead.id}});if(error&&error.context&&typeof error.context.json==="function"){try{data=await error.context.json()}catch(_e){}}if(data&&data.already_sent){const resend=confirm("This lead was already sent successfully to PrimeHome Buyers. Send it again anyway?");if(!resend){statusEl.textContent="Not resent.";statusEl.style.display="block";return}sendBtn.textContent="Sending…";({data,error}=await sb.functions.invoke("ispeedtolead-submit",{body:{lead_id:ispeedtoleadModalLead.id,force:true}}));if(error&&error.context&&typeof error.context.json==="function"){try{data=await error.context.json()}catch(_e){}}}if(data&&data.duplicate_phone){const when=data.previous?.created_at?new Date(data.previous.created_at).toLocaleString():"a previous date";const via=data.previous?.channel==="gyhomes"?"gyhomes":"PrimeHome Buyers";const priorAddr=data.previous?.address;const addrNote=data.same_address?`

Same address on file for both — this looks like the same property.`:priorAddr?`

That prior lead's address: ${priorAddr}
This lead's address: ${data.this_address||"not set"}

If that's a different property, this is probably fine to send.`:"";const resend=confirm(`This seller's phone number was already sent to iSpeedToLead as a DIFFERENT lead (via ${via}) on ${when}. This is the #1 cause of rejections.${addrNote}

Send anyway?`);if(!resend){statusEl.textContent="Not sent.";statusEl.style.display="block";return}sendBtn.textContent="Sending…";({data,error}=await sb.functions.invoke("ispeedtolead-submit",{body:{lead_id:ispeedtoleadModalLead.id,force:true}}));if(error&&error.context&&typeof error.context.json==="function"){try{data=await error.context.json()}catch(_e){}}}if(data&&data.missing_fields&&data.missing_fields.length){msg.textContent=`Fill in before sending: ${data.missing_fields.join(", ")}`;msg.style.display="block";return}if(data&&data.ok){statusEl.textContent="Sent to PrimeHome Buyers successfully.";statusEl.style.color="var(--focus)";statusEl.style.display="block";logActivity("lead.ispeedtolead_send","lead",ispeedtoleadModalLead.id,`Sent to PrimeHome Buyers — ${ispeedtoleadModalLead.seller_investor_name||ispeedtoleadModalLead.id}`);if(!ispeedtoleadModalLead.submission_form_id){const defaultFormId=await getIspdDefaultCampaignFormId();if(defaultFormId){const{error:tagErr}=await sb.from("leads").update({submission_form_id:defaultFormId}).eq("id",ispeedtoleadModalLead.id);if(tagErr)console.error("Failed to auto-tag lead with PrimeHome Buyers campaign after send",tagErr);else ispeedtoleadModalLead.submission_form_id=defaultFormId}}await refreshIspeedToLeadModalStatus(ispeedtoleadModalLead.id);const optimisticSend={lead_id:ispeedtoleadModalLead.id,success:true,created_at:new Date().toISOString(),submitted_by:myUserId,error:null,channel:"primehome"};leadsIspeedtoleadSendByLeadId[ispeedtoleadModalLead.id]=optimisticSend;leadsPrimehomeSendByLeadId[ispeedtoleadModalLead.id]=optimisticSend;renderIspdReviewTable();loadLeads()}else{msg.textContent=data&&data.error||error&&error.message||"Failed to send — check the function logs.";msg.style.display="block"}}catch(e){msg.textContent=e.message||"Failed to send.";msg.style.display="block"}finally{sendBtn.disabled=false;sendBtn.textContent="Send to PrimeHome Buyers"}});let gyhomesModalLead=null;let gyhomesScreenshotUrl=null;let gyhomesRecordLinkUrl=null;async function openGyhomesModal(lead,agentLabel){gyhomesModalLead=lead;$("gyhomesModalLeadId").value=lead.id;$("gyhomesModalSellerLabel").textContent=`${lead.seller_investor_name||"Unnamed seller"} — ${lead.seller_phone||"no phone on file"} (agent: ${agentLabel})`;gyhomesScreenshotUrl=lead.extra_fields&&lead.extra_fields.ping_id?String(lead.extra_fields.ping_id).trim()||null:null;const statusEl=$("gyhomesScreenshotStatus");if(gyhomesScreenshotUrl){statusEl.textContent=gyhomesScreenshotUrl;statusEl.style.color="var(--text-2)"}else{statusEl.textContent="No Ping ID attachment on this lead yet — add one in Send to PrimeHome Buyers first, then try again.";statusEl.style.color="var(--danger)"}gyhomesRecordLinkUrl=lead.extra_fields&&lead.extra_fields.record_link?String(lead.extra_fields.record_link).trim()||null:null;const recordingStatusEl=$("gyhomesRecordingStatus");if(gyhomesRecordLinkUrl){recordingStatusEl.textContent=gyhomesRecordLinkUrl;recordingStatusEl.style.color="var(--text-2)"}else{recordingStatusEl.textContent="No call recording attached to this lead yet — add one in Send to PrimeHome Buyers first, then try again.";recordingStatusEl.style.color="var(--danger)"}$("gyhomesModalMsg").style.display="none";$("gyhomesModalStatus").style.display="none";$("gyhomesCheckAiBtn").style.display="none";$("gyhomesFeedbackReview").style.display="none";$("gyhomesAiWarning").style.display="none";$("gyhomesAiMsg").style.display="none";$("gyhomesAiResults").style.display="none";$("gyhomesAiMatchFlags").innerHTML="";$("gyhomesAiOnlyFields").innerHTML="";gyhomesMandatoryState={};GYHOMES_REVIEW_FIELDS.forEach(({field})=>{gyhomesMandatoryState[field]={value:null,note:"",known:null,ai:null}});renderGyhomesMandatoryChecklist();await refreshGyhomesModalHistory(lead.id);await loadGyhomesFeedbackCache(lead.id);await loadGyhomesComparison(lead.id);$("gyhomesModalOverlay").classList.add("active")}function closeGyhomesModal(){$("gyhomesModalOverlay").classList.remove("active");gyhomesModalLead=null;gyhomesScreenshotUrl=null;gyhomesRecordLinkUrl=null}$("gyhomesModalCancelBtn").addEventListener("click",closeGyhomesModal);$("gyhomesModalOverlay").addEventListener("click",e=>{if(e.target.id==="gyhomesModalOverlay")closeGyhomesModal()});const GYHOMES_REVIEW_FIELDS=[{field:"form_owner",label:"Owner status",comparable:true,options:["Yes, i own this property","Agent/wholesaler"]},{field:"form_occupied",label:"Occupancy",comparable:true,options:["Owner Occupied","Tenant Occupied","No its Vacant"]},{field:"form_repairs",label:"Repairs needed",comparable:true,options:["Full Gut Renovation - Everything","Remodel - Kitchen, Bathroom, Roof","Cosmetic - Flooring, Paint","Fully renovated and updated in past 2 years"]},{field:"form_owned_years",label:"Years owned",comparable:true,options:["0-1","2-5","6-9","10-14","15-19","20-29","30-50","50+"]},{field:"form_listed",label:"Currently listed",comparable:true,options:["Yes it's listed","No it's not listed"]},{field:"form_multifamily",label:"Property type",comparable:true,options:["Ranch","2-story","Mobile home owned land","Mobile home rented land","Multifamily","Single family","Bungalow","Cottage","Townhouse","Condominium","Duplex","Farmhouse","Split-level home","Land"]},{field:"form_goal",label:"Goal",comparable:true,options:["Preforeclosure","Emergency Reasons","Financial Reasons","Selling a vacant/non-occupied property","Sell and rent instead","Death in the family","Sell without showings","Inherited Property","Downsizing","Tired of being a landlord","Moving closer to family","Relocating","Retirement elsewhere","Upgrading","Moving from United States","Behind on taxes/mortgage","Old age","Divorce","Health issues","Job loss","Other personal reason"]},{field:"form_sell_fast",label:"Timeline to sell",comparable:true,options:["ASAP","1 month","2-3 months","4-5 months","6+ months"]},{field:"form_decision_authority",label:"Decision authority",comparable:false,options:["Sole owner - full authority","Multiple owners - need consensus","Not the owner - no authority","Power of attorney / estate representative","Unclear / not discussed"]},{field:"form_mortgage_status",label:"Mortgage status",comparable:false,options:["No mortgage - owned free and clear","Has a mortgage - current on payments","Has a mortgage - behind on payments","Unclear / not discussed"]},{field:"form_previous_listing",label:"Previous listing",comparable:false,options:["Never listed before","Previously listed, expired/withdrawn","Currently listed elsewhere","Unclear / not discussed"]},{field:"form_motivation_indicator",label:"Motivation",comparable:false,options:["High - urgent need to sell","Medium - open but not urgent","Low - just curious/testing the market","None - not motivated to sell","Unclear / not discussed"]},{field:"form_price_expectation",label:"Price expectation",comparable:false,options:["At or below market value","Near market value","Above market value","Firm/non-negotiable price","No price given"]},{field:"form_cash_offer_interest",label:"Cash offer interest",comparable:false,options:["Open to a cash offer","Wants full retail/financed offer only","Undecided","Not discussed"]},{field:"form_showing_preference",label:"Showing preference",comparable:false,options:["Open to showings","Wants to sell without showings","Limited availability for showings","Not discussed"]},{field:"form_seller_flexibility",label:"Seller flexibility",comparable:false,options:["Flexible on price/terms","Somewhat flexible","Firm/inflexible","Not discussed"]},{field:"form_recording_disclosure",label:"Recording disclosure",comparable:false,options:["Disclosed and consented","Disclosed, no clear consent","No disclosure given","Not discussed"]}];const GYHOMES_REVIEW_FIELD_BY_KEY=Object.fromEntries(GYHOMES_REVIEW_FIELDS.map(f=>[f.field,f]));const GYHOMES_AI_ONLY_FIELD_LABELS={form_best_callback_time:"Best callback time",form_followup_date:"Follow-up date"};let gyhomesMandatoryState={};function gyhomesAllFieldsAddressed(){return GYHOMES_REVIEW_FIELDS.every(f=>gyhomesMandatoryState[f.field]&&gyhomesMandatoryState[f.field].value)}function updateGyhomesSubmitVisibility(){$("gyhomesCheckAiBtn").style.display=gyhomesAllFieldsAddressed()?"inline-block":"none"}function renderGyhomesMandatoryChecklist(){const container=$("gyhomesMandatoryChecklist");container.innerHTML=GYHOMES_REVIEW_FIELDS.map(({field,label,comparable,options})=>{const s=gyhomesMandatoryState[field]||(gyhomesMandatoryState[field]={value:null,note:"",known:null,ai:null});const contextBits=[];if(comparable)contextBits.push(`Known: ${s.known?escapeHtml(s.known):'<span style="color:var(--text-dim);">—</span>'}`);contextBits.push(`AI heard: ${s.ai?escapeHtml(s.ai):'<span style="color:var(--text-dim);">—</span>'}`);const optionList=s.ai&&!options.includes(s.ai)?[...options,s.ai]:options;const optionsHtml=optionList.map(o=>`<option value="${escapeHtml(o)}"${s.value===o?" selected":""}>${escapeHtml(o)}</option>`).join("");return`
      <div class="gyhomes-mandatory-row" style="border:1px solid var(--border-light); border-radius:6px; padding:8px 10px; margin-bottom:8px;">
        <div style="font-size:12px; font-weight:600;">${escapeHtml(label)}</div>
        <div style="font-size:10px; color:var(--text-dim); margin:2px 0 6px;">${contextBits.join(" &nbsp;·&nbsp; ")}</div>
        <select class="gyhomes-value-select" data-field="${field}" style="width:100%; font-size:11px;">
          <option value=""${s.value?"":" selected"}>— Select what the call actually showed —</option>
          ${optionsHtml}
        </select>
        <textarea class="gyhomes-note-input" data-field="${field}" rows="1" placeholder="Optional note — anything that needs explaining beyond the selected option" style="margin-top:6px; font-size:11px; width:100%;">${escapeHtml(s.note||"")}</textarea>
      </div>
    `}).join("");container.querySelectorAll(".gyhomes-value-select").forEach(sel=>{sel.addEventListener("change",()=>{gyhomesMandatoryState[sel.dataset.field].value=sel.value||null;updateGyhomesSubmitVisibility()})});container.querySelectorAll(".gyhomes-note-input").forEach(ta=>{ta.addEventListener("input",()=>{gyhomesMandatoryState[ta.dataset.field].note=ta.value})})}function applyGyhomesComparisonResult(result){(result.comparisons||[]).forEach(c=>{if(!gyhomesMandatoryState[c.field])return;gyhomesMandatoryState[c.field].known=c.known||null;gyhomesMandatoryState[c.field].ai=c.ai||null});GYHOMES_REVIEW_FIELDS.filter(f=>!f.comparable).forEach(({field})=>{if(!gyhomesMandatoryState[field])return;const raw=result.ai&&result.ai[field];gyhomesMandatoryState[field].ai=raw===void 0||raw===null||raw===false?null:Array.isArray(raw)?raw.join(", "):String(raw)});renderGyhomesMandatoryChecklist();$("gyhomesAiMatchFlags").innerHTML=`
    <span style="color:${result.address_match?"var(--good)":"var(--danger)"};">Address match: ${result.address_match?"yes":"no"}</span>
    &nbsp;·&nbsp;
    <span style="color:${result.phone_match?"var(--good)":"var(--danger)"};">Phone match: ${result.phone_match?"yes":"no"}</span>
    ${result.updated?`&nbsp;·&nbsp;<span style="color:var(--text-dim);">Analysis updated: ${escapeHtml(result.updated)}</span>`:""}
  `;const aiOnlyEl=$("gyhomesAiOnlyFields");const aiOnlyLines=Object.entries(GYHOMES_AI_ONLY_FIELD_LABELS).filter(([key])=>result.ai&&result.ai[key]!==void 0&&result.ai[key]!==null&&result.ai[key]!==false).map(([key,label])=>{const v=Array.isArray(result.ai[key])?result.ai[key].join(", "):String(result.ai[key]);return`<div>${escapeHtml(label)}: ${escapeHtml(v)}</div>`});aiOnlyEl.innerHTML=aiOnlyLines.join("")||'<div style="color:var(--text-dim);">None returned.</div>';$("gyhomesAiWarning").style.display="block"}async function loadGyhomesComparison(leadId){const{data,error}=await sb.functions.invoke("gyhomes-ai-analysis",{body:{lead_id:leadId}});if(error||!data||!data.ok)return;applyGyhomesComparisonResult(data)}function renderGyhomesFeedbackReview(fr){const reviewEl=$("gyhomesFeedbackReview");if(!fr){reviewEl.style.display="none";return}const missedItems=(fr.missed_in_feedback||[]).map(item=>`<li>${escapeHtml(item)}</li>`).join("");const answerRows=(fr.mandatory_check||[]).map(m=>`
    <div style="margin-bottom:2px;">
      <span style="color:var(--text-2);">${escapeHtml(m.label)}:</span> ${escapeHtml(m.value||"—")}${m.note?` — ${escapeHtml(m.note)}`:""}
    </div>
  `).join("");reviewEl.innerHTML=`
    <div class="mono" style="font-size:10px; color:var(--text-dim); text-transform:uppercase; letter-spacing:0.5px; margin-bottom:6px;">Review</div>
    ${fr.summary?`<div style="margin-bottom:8px;">${escapeHtml(fr.summary)}</div>`:""}
    ${answerRows?`<div style="margin-bottom:8px;"><b>Confirmed answers:</b>${answerRows}</div>`:""}
    ${missedItems?`<div><b>Possible gaps vs. the call recording:</b><ul style="margin:4px 0 0; padding-left:18px;">${missedItems}</ul></div>`:'<div style="color:var(--text-dim);">Nothing flagged — your answers line up with the call recording/AI analysis.</div>'}
    ${fr.note?`<div style="margin-top:8px; color:var(--text-dim);">${escapeHtml(fr.note)}</div>`:""}
  `;reviewEl.style.display="block"}async function loadGyhomesFeedbackCache(leadId){const{data,error}=await sb.from("gyhomes_feedback_reviews").select("review, created_at").eq("lead_id",leadId).order("created_at",{ascending:false}).limit(1).maybeSingle();if(error||!data||!data.review)return;(data.review.mandatory_check||[]).forEach(m=>{if(!gyhomesMandatoryState[m.field])return;gyhomesMandatoryState[m.field].value=m.value||null;gyhomesMandatoryState[m.field].note=m.note||""});renderGyhomesMandatoryChecklist();updateGyhomesSubmitVisibility();$("gyhomesAiResults").style.display="block";renderGyhomesFeedbackReview(data.review)}$("gyhomesCheckAiBtn").addEventListener("click",async()=>{if(!gyhomesModalLead)return;const btn=$("gyhomesCheckAiBtn");const msg=$("gyhomesAiMsg");const results=$("gyhomesAiResults");msg.style.display="none";results.style.display="none";btn.disabled=true;btn.textContent="Submitting…";try{const fieldFeedback=GYHOMES_REVIEW_FIELDS.map(({field,label})=>({field,label,value:gyhomesMandatoryState[field].value||null,note:(gyhomesMandatoryState[field].note||"").trim()||null}));const{data,error}=await sb.functions.invoke("gyhomes-ai-analysis",{body:{lead_id:gyhomesModalLead.id,field_feedback:fieldFeedback}});let result=data;if(error&&error.context&&typeof error.context.json==="function"){try{result=await error.context.json()}catch(_e){}}if(!result||!result.ok){msg.textContent=result&&result.error||error&&error.message||"Failed to submit review.";msg.style.display="block";return}applyGyhomesComparisonResult(result);renderGyhomesFeedbackReview(result.feedback_review);results.style.display="block"}catch(e){msg.textContent=e.message||"Failed to submit review.";msg.style.display="block"}finally{btn.disabled=false;btn.textContent="Submit Review"}});async function refreshGyhomesModalHistory(leadId){const{data:history2}=await sb.from("ispeedtolead_submissions").select("id, success, created_at, error, response_status, submitted_by").eq("lead_id",leadId).eq("channel","gyhomes").order("created_at",{ascending:false}).limit(20);await renderGyhomesHistory(history2||[])}async function renderGyhomesHistory(rows){const box=$("gyhomesModalHistory");if(!box)return;if(!rows.length){box.innerHTML='<div style="font-size:11px; color:var(--text-dim);">No send attempts yet.</div>';return}const submitterIds=[...new Set(rows.map(r=>r.submitted_by).filter(Boolean))];let nameById={};if(submitterIds.length){const{data:submitters}=await sb.from("profiles").select("id, full_name, email").in("id",submitterIds);(submitters||[]).forEach(p=>{nameById[p.id]=p.full_name||p.email||"Unknown"})}const historyTz=nonAgentDisplayTz();box.innerHTML=rows.map(r=>{const when=new Date(r.created_at).toLocaleString([],{month:"short",day:"numeric",year:"numeric",hour:"2-digit",minute:"2-digit",...historyTz?{timeZone:historyTz}:{}});const who=r.submitted_by?nameById[r.submitted_by]||"Unknown":null;const statusLabel=r.success?'<span style="color:var(--good);">Success</span>':`<span style="color:var(--danger);">Failed${r.response_status?" ("+r.response_status+")":""}</span>`;const errText=!r.success&&r.error?typeof r.error==="string"?r.error:JSON.stringify(r.error):"";const errLine=errText?`<div style="font-size:10px; color:var(--text-dim); margin-top:2px;">${escapeHtml(errText)}</div>`:"";return`
      <div style="display:flex; justify-content:space-between; gap:10px; padding:6px 0; border-bottom:1px solid var(--border); font-size:11px;">
        <div>
          <div>${statusLabel}${who?" · "+escapeHtml(who):""}</div>
          ${errLine}
        </div>
        <div class="mono" style="color:var(--text-dim); white-space:nowrap;">${when}</div>
      </div>
    `}).join("")}$("gyhomesModalSendBtn").addEventListener("click",async()=>{if(!gyhomesModalLead)return;const msg=$("gyhomesModalMsg");const statusEl=$("gyhomesModalStatus");msg.style.display="none";statusEl.style.display="none";const missingAttachments=[];if(!gyhomesRecordLinkUrl)missingAttachments.push("call recording");if(!gyhomesScreenshotUrl)missingAttachments.push("Ping ID attachment");if(missingAttachments.length){msg.textContent=`This lead is missing its ${missingAttachments.join(" and ")} — add ${missingAttachments.length>1?"them":"it"} in Send to PrimeHome Buyers first, then try again.`;msg.style.display="block";return}if(!ispdPingIdLooksLikeImage(gyhomesScreenshotUrl)){msg.textContent="This lead's Ping ID attachment is not an image — gyhomes only accepts a screenshot there, so the send would be rejected. It looks like the call recording was attached twice. Replace the Ping ID with the dialer screenshot in Send to PrimeHome Buyers, then try again.";msg.style.display="block";return}const sendBtn=$("gyhomesModalSendBtn");sendBtn.disabled=true;sendBtn.textContent="Sending…";try{let{data,error}=await sb.functions.invoke("ispeedtolead-gyhomes-submit",{body:{lead_id:gyhomesModalLead.id,screenshot_url:gyhomesScreenshotUrl}});if(error&&error.context&&typeof error.context.json==="function"){try{data=await error.context.json()}catch(_e){}}if(data&&data.already_sent){const resend=confirm("This lead was already sent successfully to gyhomes. Send it again anyway?");if(!resend){statusEl.textContent="Not resent.";statusEl.style.display="block";return}sendBtn.textContent="Sending…";({data,error}=await sb.functions.invoke("ispeedtolead-gyhomes-submit",{body:{lead_id:gyhomesModalLead.id,screenshot_url:gyhomesScreenshotUrl,force:true}}));if(error&&error.context&&typeof error.context.json==="function"){try{data=await error.context.json()}catch(_e){}}}if(data&&data.duplicate_phone){const when=data.previous?.created_at?new Date(data.previous.created_at).toLocaleString():"a previous date";const via=data.previous?.channel==="gyhomes"?"gyhomes":"PrimeHome Buyers";const priorAddr=data.previous?.address;const addrNote=data.same_address?`

Same address on file for both — this looks like the same property.`:priorAddr?`

That prior lead's address: ${priorAddr}
This lead's address: ${data.this_address||"not set"}

If that's a different property, this is probably fine to send.`:"";const resend=confirm(`This seller's phone number was already sent to iSpeedToLead as a DIFFERENT lead (via ${via}) on ${when}. This is the #1 cause of rejections.${addrNote}

Send anyway?`);if(!resend){statusEl.textContent="Not sent.";statusEl.style.display="block";return}sendBtn.textContent="Sending…";({data,error}=await sb.functions.invoke("ispeedtolead-gyhomes-submit",{body:{lead_id:gyhomesModalLead.id,screenshot_url:gyhomesScreenshotUrl,force:true}}));if(error&&error.context&&typeof error.context.json==="function"){try{data=await error.context.json()}catch(_e){}}}if(data&&data.missing_fields&&data.missing_fields.length){msg.textContent=`Fill in (via Send to PrimeHome Buyers' details panel) before sending: ${data.missing_fields.join(", ")}`;msg.style.display="block";return}if(data&&data.ok){statusEl.textContent=data.warning?`Sent to gyhomes successfully. ${data.warning}`:"Sent to gyhomes successfully.";statusEl.style.color="var(--focus)";statusEl.style.display="block";logActivity("lead.gyhomes_send","lead",gyhomesModalLead.id,`Sent to gyhomes — ${gyhomesModalLead.seller_investor_name||gyhomesModalLead.id}${data.warning?" (record_link was Combined page, auto-resolved)":""}`);await refreshGyhomesModalHistory(gyhomesModalLead.id);const optimisticSend={lead_id:gyhomesModalLead.id,success:true,created_at:new Date().toISOString(),submitted_by:myUserId,error:null,channel:"gyhomes"};leadsIspeedtoleadSendByLeadId[gyhomesModalLead.id]=optimisticSend;leadsGyhomesSendByLeadId[gyhomesModalLead.id]=optimisticSend;renderIspdReviewTable();loadLeads()}else{const rawMsg=data&&data.error||error&&error.message||"Failed to send — check the function logs.";msg.textContent=typeof rawMsg==="string"?rawMsg:JSON.stringify(rawMsg);msg.style.display="block";await refreshGyhomesModalHistory(gyhomesModalLead.id)}}catch(e){msg.textContent=e.message||"Failed to send.";msg.style.display="block"}finally{sendBtn.disabled=false;sendBtn.textContent="Send to gyhomes"}});$("leadsExportCsvBtn").addEventListener("click",()=>{if(!(myIsOwner||myRole==="admin"))return;const headers=["User","Submitted at","Leads","Seller Name","Phone","Extra fields","Campaign Name","Type","Status","Rejected reason","Set via","PrimeHome Buyers send status"];const rows=lastLeadsRows.map(r=>[r.agent,r.submitted,r.count,r.sellerInvestor,r.phone||"",r.extraFieldsText||"",r.task,r.type,r.status,r.rejectReason||"",r.viaIspeedtolead||"",r.ispeedtoleadSendStatus||""]);const fromVal=$("leadsFromDateInput").value;const toVal=$("leadsToDateInput").value||fromVal;const rangeSuffix=!fromVal&&!toVal?"all-time":fromVal===toVal?fromVal:`${fromVal||"start"}_to_${toVal||"now"}`;exportCSV(`leads-${rangeSuffix}.csv`,headers,rows);logActivity("export.leads_csv",null,null,`Exported leads CSV (${rows.length} entries)`)});function renderSites(visits){const totals={};visits.forEach(v=>{const end=v.end_time?new Date(v.end_time).getTime():Date.now();const dur=(end-new Date(v.start_time).getTime())/1e3;const key=v.domain||"(unknown)";totals[key]=(totals[key]||0)+dur});const rows=Object.entries(totals).sort((a,b)=>b[1]-a[1]).slice(0,12);const list=$("sitesList");list.innerHTML="";if(rows.length===0){list.innerHTML='<div class="empty">No site activity logged yet today.</div>';return}const maxSec=rows[0][1];rows.forEach(([domain,sec])=>{const row=document.createElement("div");row.className="site-row";row.innerHTML=`
      <div class="site-domain" title="${escapeHtml(domain)}">${escapeHtml(domain)}</div>
      <div class="site-bar-track"><div class="site-bar-fill" style="width:${Math.max(2,sec/maxSec*100)}%"></div></div>
      <div class="site-time">${fmtShort(sec)}</div>
    `;list.appendChild(row)})}function detailSetTitleDept(agent){const el=$("detailTitleDept");if(!el)return;const line=[agent&&agent.title,agent&&agent.department].filter(Boolean).join(" · ");el.textContent=line;el.style.display=line?"block":"none"}async function openDetail(agent,{fromUsersTab=false}={}){currentDetailAgent=agent;$("teamCard").style.display="none";$("detailPanel").style.display="block";$("detailName").textContent=agent.full_name||agent.email;detailSetTitleDept(agent);$("acctFullName").value=agent.full_name||"";await refreshAcctRoleOptions(agent.role);$("acctEmailInput").value=agent.email||"";$("acctTitle").value=agent.title||"";$("acctGrade").value=agent.grade||"";$("acctAmericanName").value=agent.american_name||"";await fillDepartmentSelect("acctDepartment",agent.department);$("acctNewPassword").value="";$("acctMsg").textContent="";$("acctPasswordMsg").textContent="";$("acctStatusMsg").textContent="";renderAcctStatus(agent);const canEdit=fromUsersTab&&myPermissions.canManageUsers;$("acctAmericanName").disabled=!(canEdit&&(myIsOwner||myRole==="admin"));["acctFullName","acctRole","acctEmailInput","acctSaveBtn","acctNewPassword","acctSetPasswordBtn","acctDeleteBtn","acctToggleActiveBtn","acctDepartment","acctTitle","acctGrade"].forEach(id=>{$(id).disabled=!canEdit});const isSelf=agent.id===myUserId;if(agent.role==="owner")$("acctRole").disabled=true;$("acctDeleteBtn").style.display=canEdit&&!isSelf?"inline-block":"none";$("acctToggleActiveBtn").style.display=canEdit&&!isSelf?"inline-block":"none";await loadSchedule(agent.id);await loadAssignedTasks(agent.id);await renderDetail(agent);const hrOnly=viewerIsHR()&&!viewerHasFullUserEdit();const accountCard=document.getElementById("detailAccountCard");const statsCard=document.getElementById("detailStatsCard");const scheduleCard=document.getElementById("detailScheduleCard");const _dp=document.getElementById("detailPanel")||document.getElementById("tmSection-staff");if(_dp){_dp.querySelectorAll("#detailAccountCard input, #detailAccountCard select, #detailAccountCard button, #detailScheduleCard input, #detailScheduleCard select, #detailScheduleCard button, #assignedTasksList input, #assignedTasksList select, #assignedTasksList button").forEach(el=>{el.disabled=false})}if(accountCard)accountCard.style.display=hrOnly?"none":"";if(statsCard)statsCard.style.display=hrOnly?"none":"";const assignedWrap=document.getElementById("assignedTasksList");if(scheduleCard){const saveBtn=document.getElementById("saveScheduleBtn");if(saveBtn&&hrOnly)saveBtn.disabled=isSelf;const scheduleMsg=document.getElementById("scheduleMsg");if(scheduleMsg){if(hrOnly&&isSelf){scheduleMsg.textContent="You can’t edit your own schedule/limits from here — ask an Owner or Admin.";scheduleMsg.className="msg"}else if(scheduleMsg.textContent.startsWith("You can’t edit your own schedule")){scheduleMsg.textContent=""}}}const selfViewOnly=agent.id===myUserId&&!viewerHasFullUserEdit()&&!viewerIsHR();const detailPanel=document.getElementById("detailPanel")||document.getElementById("tmSection-staff");if(selfViewOnly&&detailPanel){if(accountCard)accountCard.style.display="";if(statsCard)statsCard.style.display="";detailPanel.querySelectorAll("#detailAccountCard input, #detailAccountCard select, #detailAccountCard button, #detailScheduleCard input, #detailScheduleCard select, #detailScheduleCard button, #assignedTasksList input, #assignedTasksList select, #assignedTasksList button").forEach(el=>{el.disabled=true});let banner=document.getElementById("selfViewOnlyBanner");if(!banner&&accountCard){banner=document.createElement("div");banner.id="selfViewOnlyBanner";banner.style.cssText="background:var(--inset); border:1px solid var(--border); border-radius:8px; padding:8px 12px; margin-bottom:14px; font-size:12px; color:var(--text-2);";banner.textContent="You’re viewing your own profile. This is read-only — contact an admin to change anything.";accountCard.parentNode.insertBefore(banner,accountCard)}else if(banner){banner.style.display=""}}else{const banner=document.getElementById("selfViewOnlyBanner");if(banner)banner.style.display="none"}const trackingEls=[document.getElementById("detailScheduleCard"),document.getElementById("detailTimeline"),document.getElementById("sitesList"),document.getElementById("shotsGrid"),assignedWrap];const hourMarks=detailPanel?detailPanel.querySelector(".hour-marks"):null;let viewedHasDashboardAccess=false;if(fromUsersTab){[...trackingEls,hourMarks].forEach(el=>{if(el)el.style.display=""})}else{viewedHasDashboardAccess=agent.role==="owner";if(!viewedHasDashboardAccess){const{data:viewedRoleRow}=await sb.from("roles").select("allowed_tabs").eq("name",agent.role||"").maybeSingle();viewedHasDashboardAccess=Array.isArray(viewedRoleRow?.allowed_tabs)&&viewedRoleRow.allowed_tabs.length>0}const alwaysOnEls=new Set([scheduleCard,assignedWrap,document.getElementById("detailTimeline"),hourMarks]);[...trackingEls,hourMarks].forEach(el=>{if(el)el.style.display=viewedHasDashboardAccess?"none":alwaysOnEls.has(el)?"":el.style.display})}if(assignedWrap&&hrOnly)assignedWrap.style.display="none";if(!fromUsersTab&&detailPanel){detailPanel.querySelectorAll("#detailAccountCard input, #detailAccountCard select, #detailAccountCard button, #detailScheduleCard input, #detailScheduleCard select, #detailScheduleCard button, #assignedTasksList input, #assignedTasksList select, #assignedTasksList button").forEach(el=>{el.disabled=true});let reviewBanner=document.getElementById("dashboardReviewOnlyBanner");if(!reviewBanner&&accountCard){reviewBanner=document.createElement("div");reviewBanner.id="dashboardReviewOnlyBanner";reviewBanner.style.cssText="background:var(--inset); border:1px solid var(--border); border-radius:8px; padding:8px 12px; margin-bottom:14px; font-size:12px; color:var(--text-2);";reviewBanner.textContent="You’re reviewing this user from the Dashboard — this view is read-only. To make changes, open them from User Management → Users.";accountCard.parentNode.insertBefore(reviewBanner,accountCard)}else if(reviewBanner){reviewBanner.style.display=""}}else{const reviewBanner=document.getElementById("dashboardReviewOnlyBanner");if(reviewBanner)reviewBanner.style.display="none"}}function roleDisplayLabel(name){return name.charAt(0).toUpperCase()+name.slice(1)}async function refreshAcctRoleOptions(currentRole){if(currentRole==="owner"){$("acctRole").innerHTML=`<option value="owner">Owner</option>`;$("acctRole").value="owner";return}const{data}=await sb.from("roles").select("name").order("name");const roles=(data||[]).filter(r=>r.name!=="owner"&&(myIsOwner||r.name!=="admin"));$("acctRole").innerHTML=roles.map(r=>`<option value="${escapeHtml(r.name)}">${escapeHtml(roleDisplayLabel(r.name))}</option>`).join("");$("acctRole").value=roles.some(r=>r.name===currentRole)?currentRole:(roles[0]||{}).name||""}let lbFacetsLoadPromise=null;let lbResultRows=[];let lbLastSearchParams={};let lbLastTotal=0;async function listBuilderCall(payload){const{data,error}=await sb.functions.invoke("staff-list-builder",{body:payload});if(error)return{ok:false,error:error.message||String(error)};return data}const LB_FULL_CSV_HEADERS=["Address","City","State","Zip","County","APN / Parcel ID","Owner Name","Owner First Name","Owner Last Name","Owner Mailing Address","Owner Mailing City","Owner Mailing State","Absentee Owner","Properties Owned","Corporate/Entity Owner","Ownership Type","Probate/Estate","Building Code Lawsuit","Building Code Lawsuit Case Number","Lawsuit Filed Date","Vacant Building","Vacant Building Violation Code","Vacant Flagged Date","Pre-foreclosure","Pre-foreclosure Sale Date","Pre-foreclosure Trustee","Pre-foreclosure Case Number","Pre-foreclosure Status","Pre-foreclosure Legal Description","Last Sale Date","Last Sale Type","Inherited","Beds","Baths","Sqft","Year Built","Lot Sqft","Assessed Value","Assessed Land Value","Assessed Building Value","Property Use Type","Num Stories","Garage","Attic","Basement","MFH 2-4","MFH 5+","Class","Data Source","First Synced","Excluded"];function lbFullCsvRow(r){return[r.address_line1,r.city,r.state,r.zip,r.county,r.apn??"",r.owner_name,r.owner_first_name||"",r.owner_last_name||"",r.owner_mailing_address,r.owner_mailing_city||"",r.owner_mailing_state||"",r.is_absentee_owner?"Yes":"No",r.owner_portfolio_size??"",r.is_corporate_owner?"Yes":"No",r.ownership_type||"",r.is_probate_estate?"Yes":"No",r.has_building_code_lawsuit?"Yes":"No",r.building_code_lawsuit_case_number||"",r.building_code_lawsuit_date||"",r.is_vacant_building?"Yes":"No",r.vacant_building_violation_code||"",r.vacant_building_flagged_date||"",r.is_preforeclosure?"Yes":"No",r.preforeclosure_sale_date||"",r.preforeclosure_trustee||"",r.preforeclosure_case_number||"",r.preforeclosure_status||"",r.preforeclosure_legal_description||"",r.last_sale_date||"",r.last_sale_type||"",r.is_inherited?"Yes":"No",r.beds??"",r.baths??"",r.sqft??"",r.year_built??"",r.lot_sqft??"",r.assessed_value??"",r.assessed_land_value??"",r.assessed_building_value??"",r.property_use_type||"",r.num_stories??"",r.has_garage?"Yes":"No",r.has_attic?"Yes":"No",r.has_basement?"Yes":"No",r.is_mfh_2_4?"Yes":"No",r.is_mfh_5_plus?"Yes":"No",r.extra_fields&&r.extra_fields.class||"",r.source||"",r.created_at?String(r.created_at).slice(0,10):"",r.is_excluded?"Yes":"No"]}async function initListBuilderSection(){if(!lbFacetsLoadPromise)lbFacetsLoadPromise=loadListBuilderFacets();await lbFacetsLoadPromise;loadListBuilderBackfillStatus();loadListBuilderSavedFilters()}async function loadListBuilderBackfillStatus(){const grid=$("lbBackfillStatusGrid");if(!grid)return;const result=await listBuilderCall({action:"backfill_status"});if(!result||result.ok===false){grid.innerHTML=`<div style="font-size:11px; color:var(--danger);">Couldn't load backfill status: ${escapeHtml(result&&result.error||"Unknown error")}</div>`;return}const countyCard=(label,s)=>{if(!s)return"";const pct=s.areasTotal?Math.round(s.areasDone/s.areasTotal*100):0;return`
      <div style="border:1px solid var(--border); border-radius:8px; padding:10px 12px;">
        <div style="display:flex; align-items:center; justify-content:space-between; margin-bottom:6px;">
          <span class="mono" style="font-size:11px; text-transform:uppercase; letter-spacing:0.5px; color:var(--text-2);">${escapeHtml(label)}</span>
          <span class="mono" style="font-size:11px; color:${s.complete?"var(--good)":"var(--text-dim)"};">${s.complete?"Complete":"In progress"}</span>
        </div>
        <div style="height:6px; background:var(--card-hover); border-radius:3px; overflow:hidden; margin-bottom:8px;">
          <div style="height:100%; width:${pct}%; background:${s.complete?"var(--good)":"var(--focus)"};"></div>
        </div>
        <div style="font-size:11px; color:var(--text-dim); line-height:1.5;">
          ${s.areasDone||0} / ${s.areasTotal||0} areas done &middot; ${(s.totalUpserted||0).toLocaleString()} properties pulled so far<br>
          ${s.lastRunAt?"Last ran "+new Date(s.lastRunAt).toLocaleString():"Not started yet"}
          ${s.currentError?`<br><span style="color:var(--danger);">${escapeHtml(s.currentError)}</span>`:""}
        </div>
      </div>`};grid.innerHTML=countyCard("Cook, IL",result.cook)+countyCard("DuPage, IL",result.dupage)}document.getElementById("lbBackfillRefreshBtn")?.addEventListener("click",loadListBuilderBackfillStatus);async function loadListBuilderFacets(){const result=await listBuilderCall({action:"facets"});if(!result.ok){console.error("Failed to load List Builder facets",result.error);return}const countySel=$("lbFilterCounty");const prevCounty=countySel.value;countySel.innerHTML='<option value="">Any</option>'+(result.counties||[]).map(c=>`<option value="${escapeHtml(c)}">${escapeHtml(c)}</option>`).join("");countySel.value=(result.counties||[]).includes(prevCounty)?prevCounty:"";lbTownshipsByCounty=result.townshipsByCounty||{};populateListBuilderTownshipOptions()}let lbTownshipsByCounty={};function populateListBuilderTownshipOptions(){const county=$("lbFilterCounty").value;const townships=county?lbTownshipsByCounty[county]||[]:Array.from(new Set(Object.values(lbTownshipsByCounty).flat())).sort();const sel=$("lbFilterTownship");const prev=sel.value;sel.innerHTML='<option value="">Any</option>'+townships.map(t=>`<option value="${escapeHtml(t)}">${escapeHtml(t)}</option>`).join("");sel.value=townships.includes(prev)?prev:""}$("lbFilterCounty").addEventListener("change",populateListBuilderTownshipOptions);function lbNumOrUndef(id){const raw=$(id).value.trim();if(!raw)return void 0;const n=Number(raw);return Number.isFinite(n)?n:void 0}function lbCollectFilterParams(){return{q:$("lbFilterSearch").value.trim()||void 0,county:$("lbFilterCounty").value||void 0,township:$("lbFilterTownship").value||void 0,city:$("lbFilterCity").value.trim()||void 0,zip:$("lbFilterZip").value.trim()||void 0,absenteeOnly:$("lbFilterAbsenteeOnly").checked||void 0,tiredLandlordOnly:$("lbFilterTiredLandlordOnly").checked||void 0,corporateOwnerOnly:$("lbFilterCorporateOwnerOnly").checked||void 0,individualOwnerOnly:$("lbFilterIndividualOwnerOnly").checked||void 0,isProbateEstateOnly:$("lbFilterProbateEstateOnly").checked||void 0,isVacantBuildingOnly:$("lbFilterVacantBuildingOnly").checked||void 0,isPreforeclosureOnly:$("lbFilterPreforeclosureOnly").checked||void 0,longTermOwnerOnly:$("lbFilterLongTermOwnerOnly").checked||void 0,minPortfolioSize:lbNumOrUndef("lbFilterMinPortfolioSize"),minOwnershipYears:lbNumOrUndef("lbFilterMinOwnershipYears"),hasBuildingCodeLawsuitOnly:$("lbFilterBuildingCodeLawsuitOnly").checked||void 0,isInherited:$("lbFilterInheritedOnly").checked||void 0,ownershipType:$("lbFilterOwnershipType").value||void 0,ownerMailingState:$("lbFilterOwnerMailingState").value.trim().toUpperCase()||void 0,minBeds:lbNumOrUndef("lbFilterMinBeds"),minBaths:lbNumOrUndef("lbFilterMinBaths"),minSqft:lbNumOrUndef("lbFilterMinSqft"),maxSqft:lbNumOrUndef("lbFilterMaxSqft"),minYearBuilt:lbNumOrUndef("lbFilterMinYearBuilt"),maxYearBuilt:lbNumOrUndef("lbFilterMaxYearBuilt"),minLotSqft:lbNumOrUndef("lbFilterMinLotSqft"),maxLotSqft:lbNumOrUndef("lbFilterMaxLotSqft"),minNumStories:lbNumOrUndef("lbFilterMinNumStories"),propertyUseType:$("lbFilterPropertyUseType").value||void 0,isMfh2to4:$("lbFilterMfh2to4").checked||void 0,isMfh5Plus:$("lbFilterMfh5Plus").checked||void 0,hasGarage:$("lbFilterHasGarage").checked||void 0,hasAttic:$("lbFilterHasAttic").checked||void 0,hasBasement:$("lbFilterHasBasement").checked||void 0,minAssessedValue:lbNumOrUndef("lbFilterMinAssessedValue"),maxAssessedValue:lbNumOrUndef("lbFilterMaxAssessedValue"),lastSaleType:$("lbFilterLastSaleType").value||void 0,excludedOnly:$("lbFilterExcludedOnly").checked||void 0}}function lbApplyFilterParams(params){params=params||{};$("lbFilterSearch").value=params.q||"";$("lbFilterCounty").value=params.county||"";populateListBuilderTownshipOptions();$("lbFilterTownship").value=params.township||"";$("lbFilterCity").value=params.city||"";$("lbFilterZip").value=params.zip||"";$("lbFilterAbsenteeOnly").checked=!!params.absenteeOnly;$("lbFilterTiredLandlordOnly").checked=!!params.tiredLandlordOnly;$("lbFilterCorporateOwnerOnly").checked=!!params.corporateOwnerOnly;$("lbFilterIndividualOwnerOnly").checked=!!params.individualOwnerOnly;$("lbFilterProbateEstateOnly").checked=!!params.isProbateEstateOnly;$("lbFilterVacantBuildingOnly").checked=!!params.isVacantBuildingOnly;$("lbFilterPreforeclosureOnly").checked=!!params.isPreforeclosureOnly;$("lbFilterLongTermOwnerOnly").checked=!!params.longTermOwnerOnly;$("lbFilterMinPortfolioSize").value=params.minPortfolioSize??"";$("lbFilterMinOwnershipYears").value=params.minOwnershipYears??"";$("lbFilterBuildingCodeLawsuitOnly").checked=!!params.hasBuildingCodeLawsuitOnly;$("lbFilterInheritedOnly").checked=!!params.isInherited;$("lbFilterOwnershipType").value=params.ownershipType||"";$("lbFilterOwnerMailingState").value=params.ownerMailingState||"";$("lbFilterMinBeds").value=params.minBeds??"";$("lbFilterMinBaths").value=params.minBaths??"";$("lbFilterMinSqft").value=params.minSqft??"";$("lbFilterMaxSqft").value=params.maxSqft??"";$("lbFilterMinYearBuilt").value=params.minYearBuilt??"";$("lbFilterMaxYearBuilt").value=params.maxYearBuilt??"";$("lbFilterMinLotSqft").value=params.minLotSqft??"";$("lbFilterMaxLotSqft").value=params.maxLotSqft??"";$("lbFilterMinNumStories").value=params.minNumStories??"";$("lbFilterPropertyUseType").value=params.propertyUseType||"";$("lbFilterMfh2to4").checked=!!params.isMfh2to4;$("lbFilterMfh5Plus").checked=!!params.isMfh5Plus;$("lbFilterHasGarage").checked=!!params.hasGarage;$("lbFilterHasAttic").checked=!!params.hasAttic;$("lbFilterHasBasement").checked=!!params.hasBasement;$("lbFilterMinAssessedValue").value=params.minAssessedValue??"";$("lbFilterMaxAssessedValue").value=params.maxAssessedValue??"";$("lbFilterLastSaleType").value=params.lastSaleType||"";$("lbFilterExcludedOnly").checked=!!params.excludedOnly}const LB_PAGE_SIZE=1e3;let lbPageOffset=0;async function runListBuilderSearch(){lbLastSearchParams=lbCollectFilterParams();await lbFetchPage(0)}let lbSortBy=null;let lbSortDir="asc";let lbFetchSeq=0;async function lbFetchPage(offset){const seq=++lbFetchSeq;const btn=$("lbSearchBtn");btn.disabled=true;const original=btn.textContent;btn.textContent="Searching…";const result=await listBuilderCall({action:"search",...lbLastSearchParams,limit:LB_PAGE_SIZE,offset,sortBy:lbSortBy||void 0,sortDir:lbSortBy?lbSortDir:void 0});btn.disabled=false;btn.textContent=original;if(seq!==lbFetchSeq)return;if(!result.ok){alert(result.error||"Search failed");return}lbPageOffset=offset;lbResultRows=result.rows||[];lbLastTotal=result.total||lbResultRows.length;renderListBuilderResults(lbLastTotal)}const LB_SORTABLE_KEYS=new Set(["address_line1","city","state","zip","county","owner_name","owner_last_name","is_absentee_owner","owner_portfolio_size","assessed_value","last_sale_date","year_built","created_at"]);function lbSortIndicator(key){if(lbSortBy!==key)return"";return lbSortDir==="asc"?" &#9650;":" &#9660;"}function lbHandleSortClick(key){if(!LB_SORTABLE_KEYS.has(key))return;if(lbSortBy===key)lbSortDir=lbSortDir==="asc"?"desc":"asc";else{lbSortBy=key;lbSortDir="asc"}lbFetchPage(0)}const LB_COLUMN_DEFS=[{key:"city",label:"City",group:"Location",default:true,sortable:true,get:r=>r.city},{key:"state",label:"State",group:"Location",default:false,sortable:true,get:r=>r.state},{key:"zip",label:"Zip",group:"Location",default:true,mono:true,sortable:true,get:r=>r.zip},{key:"county",label:"County",group:"Location",default:false,sortable:true,get:r=>r.county},{key:"apn",label:"APN / Parcel ID",group:"Location",default:false,mono:true,get:r=>r.apn},{key:"owner_name",label:"Owner Name",group:"Owner",default:true,sortable:true,get:r=>r.owner_name},{key:"owner_first_name",label:"Owner First Name",group:"Owner",default:true,get:r=>r.owner_first_name},{key:"owner_last_name",label:"Owner Last Name",group:"Owner",default:true,sortable:true,get:r=>r.owner_last_name},{key:"owner_mailing_address",label:"Owner Mailing Address",group:"Owner",default:true,get:r=>r.owner_mailing_address},{key:"owner_mailing_city",label:"Owner Mailing City",group:"Owner",default:false,get:r=>r.owner_mailing_city},{key:"owner_mailing_state",label:"Owner Mailing State",group:"Owner",default:false,get:r=>r.owner_mailing_state},{key:"is_absentee_owner",label:"Absentee?",group:"Owner",default:true,sortable:true,get:r=>r.is_absentee_owner?"Yes":"No"},{key:"owner_portfolio_size",label:"Properties Owned",group:"Owner",default:false,sortable:true,get:r=>r.owner_portfolio_size},{key:"is_corporate_owner",label:"Corporate/Entity Owner",group:"Owner",default:false,get:r=>r.is_corporate_owner?"Yes":"No"},{key:"ownership_type",label:"Ownership Type",group:"Owner",default:false,get:r=>r.ownership_type},{key:"is_probate_estate",label:"Probate/Estate",group:"Legal status",default:false,get:r=>r.is_probate_estate?"Yes":"No"},{key:"has_building_code_lawsuit",label:"Building Code Lawsuit",group:"Legal status",default:false,get:r=>r.has_building_code_lawsuit?"Yes":"No"},{key:"building_code_lawsuit_case_number",label:"Lawsuit Case #",group:"Legal status",default:false,get:r=>r.building_code_lawsuit_case_number},{key:"building_code_lawsuit_date",label:"Lawsuit Filed Date",group:"Legal status",default:false,get:r=>r.building_code_lawsuit_date},{key:"is_vacant_building",label:"Vacant Building",group:"Legal status",default:false,get:r=>r.is_vacant_building?"Yes":"No"},{key:"vacant_building_violation_code",label:"Vacant Violation Code",group:"Legal status",default:false,get:r=>r.vacant_building_violation_code},{key:"vacant_building_flagged_date",label:"Vacant Flagged Date",group:"Legal status",default:false,get:r=>r.vacant_building_flagged_date},{key:"is_preforeclosure",label:"Pre-foreclosure",group:"Legal status",default:true,get:r=>r.is_preforeclosure?"Yes":"No"},{key:"preforeclosure_sale_date",label:"Pre-foreclosure Sale Date",group:"Legal status",default:true,sortable:false,get:r=>r.preforeclosure_sale_date},{key:"preforeclosure_trustee",label:"Pre-foreclosure Trustee",group:"Legal status",default:false,get:r=>r.preforeclosure_trustee},{key:"preforeclosure_case_number",label:"Pre-foreclosure Case #",group:"Legal status",default:false,get:r=>r.preforeclosure_case_number},{key:"preforeclosure_status",label:"Pre-foreclosure Status",group:"Legal status",default:false,get:r=>r.preforeclosure_status},{key:"preforeclosure_legal_description",label:"Pre-foreclosure Legal Description",group:"Legal status",default:false,get:r=>r.preforeclosure_legal_description},{key:"is_inherited",label:"Inherited (Estate Sale)",group:"Legal status",default:false,get:r=>r.is_inherited?"Yes":"No"},{key:"last_sale_date",label:"Last Sale Date",group:"Sale info",default:false,sortable:true,get:r=>r.last_sale_date},{key:"last_sale_type",label:"Last Sale Type",group:"Sale info",default:false,get:r=>r.last_sale_type},{key:"beds",label:"Beds",group:"Property characteristics (Cook only)",default:false,get:r=>r.beds},{key:"baths",label:"Baths",group:"Property characteristics (Cook only)",default:false,get:r=>r.baths},{key:"sqft",label:"Sqft",group:"Property characteristics (Cook only)",default:false,get:r=>r.sqft},{key:"year_built",label:"Year Built",group:"Property characteristics (Cook only)",default:false,sortable:true,get:r=>r.year_built},{key:"lot_sqft",label:"Lot Sqft",group:"Property characteristics (Cook only)",default:false,get:r=>r.lot_sqft},{key:"property_use_type",label:"Property Use Type",group:"Property characteristics (Cook only)",default:false,get:r=>r.property_use_type},{key:"num_stories",label:"Num Stories",group:"Property characteristics (Cook only)",default:false,get:r=>r.num_stories},{key:"has_garage",label:"Garage",group:"Property characteristics (Cook only)",default:false,get:r=>r.has_garage?"Yes":"No"},{key:"has_attic",label:"Attic",group:"Property characteristics (Cook only)",default:false,get:r=>r.has_attic?"Yes":"No"},{key:"has_basement",label:"Basement",group:"Property characteristics (Cook only)",default:false,get:r=>r.has_basement?"Yes":"No"},{key:"is_mfh_2_4",label:"MFH 2-4",group:"Property characteristics (Cook only)",default:false,get:r=>r.is_mfh_2_4?"Yes":"No"},{key:"is_mfh_5_plus",label:"MFH 5+",group:"Property characteristics (Cook only)",default:false,get:r=>r.is_mfh_5_plus?"Yes":"No"},{key:"assessed_value",label:"Assessed Value",group:"Financial",default:false,sortable:true,get:r=>r.assessed_value},{key:"assessed_land_value",label:"Assessed Land Value",group:"Financial",default:false,get:r=>r.assessed_land_value},{key:"assessed_building_value",label:"Assessed Building Value",group:"Financial",default:false,get:r=>r.assessed_building_value},{key:"class",label:"Class",group:"Metadata",default:true,mono:true,get:r=>r.extra_fields&&r.extra_fields.class||""},{key:"source",label:"Data Source",group:"Metadata",default:false,get:r=>r.source},{key:"created_at",label:"First Synced",group:"Metadata",default:false,sortable:true,get:r=>r.created_at?new Date(r.created_at).toLocaleDateString():""},{key:"is_excluded",label:"Excluded",group:"Metadata",default:false,get:r=>r.is_excluded?"Yes":"No"}];const LB_COLUMNS_STORAGE_KEY="lbVisibleColumns";function lbDefaultColumnKeys(){return LB_COLUMN_DEFS.filter(c=>c.default).map(c=>c.key)}let lbVisibleColumns=new Set(lbDefaultColumnKeys());(function loadLbVisibleColumns(){try{const raw=localStorage.getItem(LB_COLUMNS_STORAGE_KEY);if(!raw)return;const keys=JSON.parse(raw);if(!Array.isArray(keys))return;const validKeys=new Set(LB_COLUMN_DEFS.map(c=>c.key));lbVisibleColumns=new Set(keys.filter(k=>validKeys.has(k)))}catch(e){}})();function saveLbVisibleColumns(){try{localStorage.setItem(LB_COLUMNS_STORAGE_KEY,JSON.stringify(Array.from(lbVisibleColumns)))}catch(e){}}const LB_COLUMN_ORDER_STORAGE_KEY="lbColumnOrder";let lbColumnOrder=LB_COLUMN_DEFS.map(c=>c.key);(function loadLbColumnOrder(){try{const raw=localStorage.getItem(LB_COLUMN_ORDER_STORAGE_KEY);if(!raw)return;const keys=JSON.parse(raw);if(!Array.isArray(keys))return;const validKeys=new Set(LB_COLUMN_DEFS.map(c=>c.key));const known=keys.filter(k=>validKeys.has(k));const missing=LB_COLUMN_DEFS.map(c=>c.key).filter(k=>!known.includes(k));lbColumnOrder=[...known,...missing]}catch(e){}})();function saveLbColumnOrder(){try{localStorage.setItem(LB_COLUMN_ORDER_STORAGE_KEY,JSON.stringify(lbColumnOrder))}catch(e){}}function lbOrderedActiveCols(){return lbColumnOrder.map(k=>LB_COLUMN_DEFS.find(c=>c.key===k)).filter(c=>c&&lbVisibleColumns.has(c.key))}function lbMoveColumn(key,delta){const visible=lbOrderedActiveCols().map(c=>c.key);const idx=visible.indexOf(key);if(idx<0)return;const newIdx=idx+delta;if(newIdx<0||newIdx>=visible.length)return;[visible[idx],visible[newIdx]]=[visible[newIdx],visible[idx]];let vi=0;lbColumnOrder=lbColumnOrder.map(k=>lbVisibleColumns.has(k)?visible[vi++]:k);saveLbColumnOrder();lbOnVisibleColumnsChanged()}function renderColumnPickerBody(el){if(!el)return;const groups=[];LB_COLUMN_DEFS.forEach(c=>{if(!groups.includes(c.group))groups.push(c.group)});const orderedVisible=lbOrderedActiveCols();const orderHtml=`
    <div style="margin-bottom:14px; padding-bottom:14px; border-bottom:1px solid var(--border);">
      <div style="font-size:10px; color:var(--text-dim); text-transform:uppercase; letter-spacing:0.5px; margin-bottom:6px;">Column order (Address is always first)</div>
      ${orderedVisible.length?`<div style="display:flex; flex-direction:column; gap:4px; max-width:360px;">
        ${orderedVisible.map((c,i)=>`
          <div style="display:flex; align-items:center; justify-content:space-between; gap:8px; padding:4px 8px; background:var(--card); border-radius:6px; font-size:12px; color:var(--text);">
            <span>${escapeHtml(c.label)}</span>
            <span style="display:flex; gap:4px;">
              <button type="button" class="btn-secondary lb-col-order-up" data-key="${c.key}" ${i===0?"disabled":""} title="Move up" style="width:auto; padding:1px 8px; font-size:11px;">&uarr;</button>
              <button type="button" class="btn-secondary lb-col-order-down" data-key="${c.key}" ${i===orderedVisible.length-1?"disabled":""} title="Move down" style="width:auto; padding:1px 8px; font-size:11px;">&darr;</button>
            </span>
          </div>
        `).join("")}
      </div>`:`<div style="font-size:11px; color:var(--text-dim);">No optional columns visible yet — check some below.</div>`}
    </div>`;el.innerHTML=orderHtml+groups.map(group=>`
    <div style="margin-bottom:12px;">
      <div style="font-size:10px; color:var(--text-dim); text-transform:uppercase; letter-spacing:0.5px; margin-bottom:6px;">${escapeHtml(group)}</div>
      <div style="display:flex; flex-wrap:wrap; gap:12px;">
        ${LB_COLUMN_DEFS.filter(c=>c.group===group).map(c=>`
          <label style="display:flex; align-items:center; gap:6px; cursor:pointer; font-size:12px; color:var(--text);">
            <input type="checkbox" class="lb-col-cb" value="${c.key}" ${lbVisibleColumns.has(c.key)?"checked":""} style="width:13px; height:13px; accent-color:var(--focus);"> ${escapeHtml(c.label)}
          </label>
        `).join("")}
      </div>
    </div>
  `).join("");el.querySelectorAll(".lb-col-cb").forEach(cb=>{cb.addEventListener("change",()=>{if(cb.checked)lbVisibleColumns.add(cb.value);else lbVisibleColumns.delete(cb.value);lbOnVisibleColumnsChanged()})});el.querySelectorAll(".lb-col-order-up").forEach(btn=>btn.addEventListener("click",()=>lbMoveColumn(btn.dataset.key,-1)));el.querySelectorAll(".lb-col-order-down").forEach(btn=>btn.addEventListener("click",()=>lbMoveColumn(btn.dataset.key,1)))}function lbOnVisibleColumnsChanged(){saveLbVisibleColumns();renderColumnPickerBody($("lbColumnPickerBody"));renderColumnPickerBody($("lbmColumnPickerBody"));if($("lbResultsHead"))renderListBuilderResults(lbLastTotal);if($("lbmResultsHead"))renderMatchResults()}function lbWireColumnPickerButtons(btnId2,panelId,bodyId,selectAllId,defaultsId,noneId){$(btnId2)?.addEventListener("click",()=>{const panel=$(panelId);if(!panel)return;const showing=panel.style.display!=="none";panel.style.display=showing?"none":"block";if(!showing)renderColumnPickerBody($(bodyId))});$(selectAllId)?.addEventListener("click",()=>{lbVisibleColumns=new Set(LB_COLUMN_DEFS.map(c=>c.key));lbOnVisibleColumnsChanged()});$(defaultsId)?.addEventListener("click",()=>{lbVisibleColumns=new Set(lbDefaultColumnKeys());lbColumnOrder=LB_COLUMN_DEFS.map(c=>c.key);saveLbColumnOrder();lbOnVisibleColumnsChanged()});$(noneId)?.addEventListener("click",()=>{lbVisibleColumns=new Set;lbOnVisibleColumnsChanged()})}lbWireColumnPickerButtons("lbColumnsBtn","lbColumnPickerPanel","lbColumnPickerBody","lbColumnsSelectAllBtn","lbColumnsDefaultsBtn","lbColumnsNoneBtn");lbWireColumnPickerButtons("lbmColumnsBtn","lbmColumnPickerPanel","lbmColumnPickerBody","lbmColumnsSelectAllBtn","lbmColumnsDefaultsBtn","lbmColumnsNoneBtn");function renderListBuilderResults(total){$("lbResultCount").textContent=String(total);const body=$("lbResultsBody");$("lbResultsEmpty").style.display=lbResultRows.length?"none":"block";const activeCols=lbOrderedActiveCols();const head=$("lbResultsHead");if(head){const addressTh=`<th class="lb-sort-th" data-sort-key="address_line1" style="cursor:pointer;">Address${lbSortIndicator("address_line1")}</th>`;const colTh=activeCols.map(c=>c.sortable?`<th class="lb-sort-th" data-sort-key="${c.key}" style="cursor:pointer;">${escapeHtml(c.label)}${lbSortIndicator(c.key)}</th>`:`<th>${escapeHtml(c.label)}</th>`).join("");head.innerHTML=`<tr><th style="width:30px;"><input type="checkbox" id="lbSelectAllCb" style="width:13px; height:13px; accent-color:var(--focus);"></th>${addressTh}${colTh}</tr>`}body.innerHTML=lbResultRows.map(r=>`
    <tr>
      <td><input type="checkbox" class="lb-row-cb" data-id="${r.id}" style="width:13px; height:13px; accent-color:var(--focus);"></td>
      <td>${escapeHtml(r.address_line1||"—")}</td>
      ${activeCols.map(c=>{const raw=c.get(r);const val=raw===null||raw===void 0||raw===""?"—":String(raw);return`<td${c.mono?' class="mono"':""}>${escapeHtml(val)}</td>`}).join("")}
    </tr>
  `).join("");const selectAllCb=$("lbSelectAllCb");if(selectAllCb)selectAllCb.checked=false;const noteEl=$("lbMoreResultsNote");if(noteEl){noteEl.textContent=total>lbResultRows.length?`Showing ${lbResultRows.length.toLocaleString()} of ${total.toLocaleString()} matching properties — check "select all" and Export to include all ${total.toLocaleString()}.`:""}const pagerEl=$("lbPagerControls");if(pagerEl){const pageCount=Math.max(1,Math.ceil(total/LB_PAGE_SIZE));const pageNum=Math.floor(lbPageOffset/LB_PAGE_SIZE)+1;pagerEl.style.display=total>LB_PAGE_SIZE?"flex":"none";$("lbPageIndicator").textContent=`Page ${pageNum} of ${pageCount}`;$("lbPrevPageBtn").disabled=lbPageOffset<=0;$("lbNextPageBtn").disabled=lbPageOffset+LB_PAGE_SIZE>=total}}renderListBuilderResults(0);$("lbResultsHead")?.addEventListener("change",e=>{if(e.target&&e.target.id==="lbSelectAllCb"){document.querySelectorAll(".lb-row-cb").forEach(cb=>{cb.checked=e.target.checked})}});$("lbResultsHead")?.addEventListener("click",e=>{const th=e.target.closest(".lb-sort-th");if(th)lbHandleSortClick(th.dataset.sortKey)});$("lbSearchBtn").addEventListener("click",runListBuilderSearch);$("lbPrevPageBtn")?.addEventListener("click",()=>lbFetchPage(Math.max(0,lbPageOffset-LB_PAGE_SIZE)));$("lbNextPageBtn")?.addEventListener("click",()=>lbFetchPage(lbPageOffset+LB_PAGE_SIZE));let lbSavedFilters=[];async function loadListBuilderSavedFilters(){const result=await listBuilderCall({action:"list_saved_filters"});const sel=$("lbSavedFilterSelect");if(!sel)return;if(!result.ok){console.error("Failed to load saved filters",result.error);return}lbSavedFilters=result.savedFilters||[];const prev=sel.value;sel.innerHTML='<option value="">Load a saved filter…</option>'+lbSavedFilters.map(f=>`<option value="${escapeHtml(f.id)}">${escapeHtml(f.name)}</option>`).join("");if(lbSavedFilters.some(f=>f.id===prev))sel.value=prev}$("lbSavedFilterSelect")?.addEventListener("change",()=>{const id=$("lbSavedFilterSelect").value;if(!id)return;const found=lbSavedFilters.find(f=>f.id===id);if(found)lbApplyFilterParams(found.filters)});$("lbSaveFilterBtn")?.addEventListener("click",async()=>{const name=prompt('Name this filter (e.g. "Cook absentee tired landlords"):');if(!name||!name.trim())return;const btn=$("lbSaveFilterBtn");btn.disabled=true;const result=await listBuilderCall({action:"save_filter",name:name.trim(),filters:lbCollectFilterParams()});btn.disabled=false;if(!result.ok){alert(result.error||"Failed to save filter");return}await loadListBuilderSavedFilters()});$("lbDeleteFilterBtn")?.addEventListener("click",async()=>{const id=$("lbSavedFilterSelect")?.value;if(!id){alert("Select a saved filter to delete first.");return}const found=lbSavedFilters.find(f=>f.id===id);if(!confirm(`Delete saved filter "${found?found.name:id}"? This can't be undone.`))return;const result=await listBuilderCall({action:"delete_saved_filter",id});if(!result.ok){alert(result.error||"Failed to delete filter");return}await loadListBuilderSavedFilters()});$("lbRenameFilterBtn")?.addEventListener("click",async()=>{const id=$("lbSavedFilterSelect")?.value;if(!id){alert("Select a saved filter to rename first.");return}const found=lbSavedFilters.find(f=>f.id===id);const name=prompt("New name for this saved filter:",found?found.name:"");if(!name||!name.trim())return;const btn=$("lbRenameFilterBtn");btn.disabled=true;const result=await listBuilderCall({action:"rename_saved_filter",id,name:name.trim()});btn.disabled=false;if(!result.ok){alert(result.error||"Failed to rename filter");return}await loadListBuilderSavedFilters();$("lbSavedFilterSelect").value=id});async function lbBulkSetExcluded(excluded){const ids=Array.from(document.querySelectorAll(".lb-row-cb:checked")).map(cb=>cb.dataset.id);if(!ids.length){alert("Select at least one row first.");return}const verb=excluded?"Exclude":"Un-exclude";if(!confirm(`${verb} ${ids.length} propert${ids.length===1?"y":"ies"} ${excluded?"from":"back into"} future exports?`))return;const btn=excluded?$("lbExcludeBtn"):$("lbUnexcludeBtn");btn.disabled=true;const result=await listBuilderCall({action:"set_excluded",propertyIds:ids,excluded});btn.disabled=false;if(!result.ok){alert(result.error||`Failed to ${verb.toLowerCase()} selected properties`);return}logActivity(`list_builder.${excluded?"exclude":"unexclude"}_properties`,"properties",null,`${verb}d ${ids.length} propert${ids.length===1?"y":"ies"} ${excluded?"from":"back into"} future exports`);await lbFetchPage(lbPageOffset)}$("lbExcludeBtn")?.addEventListener("click",()=>lbBulkSetExcluded(true));$("lbUnexcludeBtn")?.addEventListener("click",()=>lbBulkSetExcluded(false));$("lbExportBtn").addEventListener("click",async()=>{const allChecked=$("lbSelectAllCb").checked;const selectedIds=new Set(Array.from(document.querySelectorAll(".lb-row-cb:checked")).map(cb=>cb.dataset.id));let rows;if(allChecked&&lbLastTotal>lbResultRows.length){const btn=$("lbExportBtn");btn.disabled=true;const original=btn.textContent;const PAGE=2e3;const allRows=lbResultRows.slice();let offset=allRows.length;try{while(offset<lbLastTotal){btn.textContent=`Fetching ${Math.min(offset+PAGE,lbLastTotal).toLocaleString()} / ${lbLastTotal.toLocaleString()}…`;const page=await listBuilderCall({action:"search",...lbLastSearchParams,limit:PAGE,offset,sortBy:lbSortBy||void 0,sortDir:lbSortBy?lbSortDir:void 0});if(!page.ok){alert(page.error||"Export failed while fetching the rest of the matching properties.");btn.disabled=false;btn.textContent=original;return}if(!page.rows||!page.rows.length)break;allRows.push(...page.rows);offset+=page.rows.length}}finally{btn.disabled=false;btn.textContent=original}rows=allRows}else{rows=lbResultRows.filter(r=>selectedIds.has(String(r.id)))}if(!rows.length){alert("Select at least one row to export.");return}exportCSV(`property-list-${new Date().toISOString().slice(0,10)}.csv`,LB_FULL_CSV_HEADERS,rows.map(lbFullCsvRow));logActivity("list_builder.export_csv","properties",null,`Exported ${rows.length} propert${rows.length===1?"y":"ies"} as CSV`)});const LB_SYNC_COUNTY_META={cook:{action:"sync_cook_county",param:"township",label:"Township (optional)",placeholder:"e.g. Evanston — blank = county-wide",countyName:"Cook County"},dupage:{action:"sync_dupage_county",param:"municipality",label:"Municipality (optional)",placeholder:"e.g. Wheaton — blank = county-wide",countyName:"DuPage County"}};$("lbSyncCounty").addEventListener("change",()=>{const meta=LB_SYNC_COUNTY_META[$("lbSyncCounty").value]||LB_SYNC_COUNTY_META.cook;$("lbSyncTownshipLabel").textContent=meta.label;$("lbSyncTownship").placeholder=meta.placeholder});$("lbSyncBtn").addEventListener("click",async()=>{const btn=$("lbSyncBtn");const msg=$("lbSyncMsg");const meta=LB_SYNC_COUNTY_META[$("lbSyncCounty").value]||LB_SYNC_COUNTY_META.cook;btn.disabled=true;msg.textContent="Syncing… this can take a few seconds.";msg.className="msg";const result=await listBuilderCall({action:meta.action,[meta.param]:$("lbSyncTownship").value.trim()||void 0,limit:Number($("lbSyncLimit").value)||500,residentialOnly:$("lbSyncResidentialOnly").checked});btn.disabled=false;if(!result.ok){msg.textContent=result.error||"Sync failed";msg.className="msg error";return}msg.textContent=`Fetched ${result.fetched}, upserted ${result.upserted}${result.dedupedAsSharedAddress?` (${result.dedupedAsSharedAddress} shared a building address with another parcel)`:""} — ${result.absenteeCount} flagged absentee.`;msg.className="msg ok";logActivity(`list_builder.${meta.action}`,"properties",null,`Synced ${meta.countyName} data: ${result.upserted} upserted, ${result.absenteeCount} absentee`);lbFacetsLoadPromise=null;await loadListBuilderFacets()});let lbmOrderType=null;let lbmOrderId=null;let lbmResultRows=[];let lbmSortKey=null;let lbmSortDir="asc";let lbmPageOffset=0;const LBM_PAGE_SIZE=200;let lbmDeselectedIds=new Set;const LBM_SORT_ACCESSORS={address_line1:r=>r.address_line1,city:r=>r.city,state:r=>r.state,zip:r=>r.zip,county:r=>r.county,owner_name:r=>r.owner_name,owner_last_name:r=>r.owner_last_name,is_absentee_owner:r=>r.is_absentee_owner?1:0,owner_portfolio_size:r=>r.owner_portfolio_size,assessed_value:r=>r.assessed_value,last_sale_date:r=>r.last_sale_date,year_built:r=>r.year_built};function lbmSortIndicator(key){if(lbmSortKey!==key)return"";return lbmSortDir==="asc"?" &#9650;":" &#9660;"}function lbmHandleSortClick(key){if(!LBM_SORT_ACCESSORS[key])return;if(lbmSortKey===key)lbmSortDir=lbmSortDir==="asc"?"desc":"asc";else{lbmSortKey=key;lbmSortDir="asc"}lbmPageOffset=0;renderMatchResults()}function lbmSortedRows(){if(!lbmSortKey)return lbmResultRows;const get=LBM_SORT_ACCESSORS[lbmSortKey];const dir=lbmSortDir==="asc"?1:-1;return[...lbmResultRows].sort((a,b)=>{const av=get(a),bv=get(b);const aNull=av===null||av===void 0||av==="";const bNull=bv===null||bv===void 0||bv==="";if(aNull&&bNull)return 0;if(aNull)return 1;if(bNull)return-1;if(av<bv)return-1*dir;if(av>bv)return 1*dir;return 0})}function openMatchModal(orderType,orderId){lbmOrderType=orderType;lbmOrderId=orderId;lbmResultRows=[];lbmSortKey=null;lbmSortDir="asc";lbmPageOffset=0;lbmDeselectedIds=new Set;$("lbmSummary").textContent="Searching…";$("lbmWarnings").innerHTML="";$("lbmMsg").textContent="";$("lbmMsg").className="msg";$("lbmResultsBody").innerHTML="";$("lbmResultCount").textContent="0";$("lbmResultsEmpty").style.display="none";if($("lbmDesiredCount"))$("lbmDesiredCount").value="";if($("lbmIncludeExportedCb"))$("lbmIncludeExportedCb").checked=false;$("lbMatchModalOverlay").classList.add("active");runMatchOrder()}let lbmMatchSeq=0;async function runMatchOrder(){const seq=++lbmMatchSeq;const desiredCountRaw=$("lbmDesiredCount")?$("lbmDesiredCount").value:"";const desiredCountInput=desiredCountRaw?Number(desiredCountRaw):null;const includePreviouslyExported=$("lbmIncludeExportedCb")?$("lbmIncludeExportedCb").checked:false;const MATCH_PAGE_CAP=2e3;$("lbmSummary").textContent="Searching…";$("lbmWarnings").innerHTML="";let allRows=[];let afterId=null;let firstResult=null;let pageNum=0;while(true){pageNum++;const remaining=desiredCountInput?desiredCountInput-allRows.length:MATCH_PAGE_CAP;if(remaining<=0)break;const pageSize=Math.min(MATCH_PAGE_CAP,remaining);if(pageNum>1){const soFar=Math.min(allRows.length+pageSize,desiredCountInput);$("lbmSummary").textContent=`Fetching ${soFar.toLocaleString()} / ${desiredCountInput.toLocaleString()}…`}const result=await listBuilderCall({action:"match_order",orderType:lbmOrderType,orderId:lbmOrderId,desiredCount:pageSize,includePreviouslyExported,afterId});if(seq!==lbmMatchSeq)return;if(!result.ok){$("lbmSummary").textContent="Search failed.";$("lbmWarnings").innerHTML=`<div class="msg error">${escapeHtml(result.error||"Unknown error")}</div>`;return}if(pageNum===1)firstResult=result;allRows.push(...result.rows||[]);afterId=result.nextAfterId;if(!desiredCountInput||allRows.length>=desiredCountInput||!result.hasMore||!afterId)break}lbmResultRows=allRows;const c=firstResult.criteria||{};if($("lbmDesiredCount"))$("lbmDesiredCount").value=desiredCountInput||c.quantity||"";const criteriaParts=[];if(c.states&&c.states.length)criteriaParts.push(`State: ${c.states.join(", ")}`);if(c.counties&&c.counties.length)criteriaParts.push(`County: ${c.counties.join(", ")}`);if(c.cities&&c.cities.length)criteriaParts.push(`City: ${c.cities.join(", ")}`);if(c.absenteeOnly)criteriaParts.push("Absentee owners only");if(c.tiredLandlordOnly)criteriaParts.push("Tired landlord (3+ properties)");if(c.corporateOwnerOnly)criteriaParts.push("Corporate/entity owner");if(c.longTermOwnerOnly)criteriaParts.push("Long-term owner (10+ years)");if(c.isProbateEstateOnly)criteriaParts.push("Probate/estate-held");if(c.hasBuildingCodeLawsuitOnly)criteriaParts.push("Building code lawsuit");if(c.isVacantBuildingOnly)criteriaParts.push("Vacant building");if(c.isPreforeclosureOnly)criteriaParts.push("Pre-foreclosure");criteriaParts.push(`Target: ${desiredCountInput||c.quantity||lbmResultRows.length}`);$("lbmSummary").textContent=criteriaParts.length?`Searched by — ${criteriaParts.join(" · ")}`:"No filters could be parsed from this request — showing recent properties.";const structuralWarnings=(firstResult.warnings||[]).filter(w=>!w.startsWith("Only ")&&!w.startsWith("No matching properties")&&!/already (delivered|exported)/.test(w));const finalWarnings=structuralWarnings.slice();const deliveredNote=(firstResult.warnings||[]).find(w=>/already (delivered|exported)/.test(w));if(deliveredNote)finalWarnings.push(deliveredNote);if(!allRows.length){finalWarnings.push("No matching properties in List Builder's current data. Try syncing more data for this area, or search manually.")}else if(desiredCountInput&&allRows.length<desiredCountInput){finalWarnings.push(`Only ${allRows.length.toLocaleString()} of the ${desiredCountInput.toLocaleString()} requested properties matched the current filters — that's all the matching data List Builder has for this area right now. Try syncing more data, or widening the location/criteria.`)}$("lbmWarnings").innerHTML=finalWarnings.map(w=>`<div class="msg" style="color:var(--away);">${escapeHtml(w)}</div>`).join("");renderMatchResults()}if($("lbmRematchBtn"))$("lbmRematchBtn").addEventListener("click",runMatchOrder);function lbmFormatExportDate(iso){if(!iso)return"";const d=new Date(iso);if(isNaN(d.getTime()))return"";return`${d.getDate()}-${d.getMonth()+1}-${d.getFullYear()}`}function renderMatchResults(){const sorted=lbmSortedRows();const total=sorted.length;$("lbmResultCount").textContent=String(total);const body=$("lbmResultsBody");$("lbmResultsEmpty").style.display=total?"none":"block";const activeCols=lbOrderedActiveCols();const pageRows=sorted.slice(lbmPageOffset,lbmPageOffset+LBM_PAGE_SIZE);const hasExportedTags=sorted.some(r=>r.previouslyExportedAt);const head=$("lbmResultsHead");if(head){const addressTh=`<th class="lbm-sort-th" data-sort-key="address_line1" style="cursor:pointer;">Address${lbmSortIndicator("address_line1")}</th>`;const exportedTh=hasExportedTags?"<th>Exported</th>":"";const colTh=activeCols.map(c=>LBM_SORT_ACCESSORS[c.key]?`<th class="lbm-sort-th" data-sort-key="${c.key}" style="cursor:pointer;">${escapeHtml(c.label)}${lbmSortIndicator(c.key)}</th>`:`<th>${escapeHtml(c.label)}</th>`).join("");head.innerHTML=`<tr><th style="width:30px;"><input type="checkbox" id="lbmSelectAllCb" style="width:13px; height:13px; accent-color:var(--focus);"></th>${addressTh}${exportedTh}${colTh}</tr>`}body.innerHTML=pageRows.map(r=>`
    <tr>
      <td><input type="checkbox" class="lbm-row-cb" data-id="${r.id}" ${lbmDeselectedIds.has(String(r.id))?"":"checked"} style="width:13px; height:13px; accent-color:var(--focus);"></td>
      <td>${escapeHtml(r.address_line1||"—")}</td>
      ${hasExportedTags?`<td>${r.previouslyExportedAt?`<span style="color:var(--away); font-size:10px; font-weight:600; white-space:nowrap;">Exported: ${escapeHtml(lbmFormatExportDate(r.previouslyExportedAt))}</span>`:"—"}</td>`:""}
      ${activeCols.map(c=>{const raw=c.get(r);const val=raw===null||raw===void 0||raw===""?"—":String(raw);return`<td${c.mono?' class="mono"':""}>${escapeHtml(val)}</td>`}).join("")}
    </tr>
  `).join("");const selectAllCb=$("lbmSelectAllCb");if(selectAllCb)selectAllCb.checked=pageRows.length>0&&pageRows.every(r=>!lbmDeselectedIds.has(String(r.id)));const pager=$("lbmPagerControls");if(pager){pager.style.display=total>LBM_PAGE_SIZE?"flex":"none";const indicator=$("lbmPageIndicator");if(indicator)indicator.textContent=total?`${(lbmPageOffset+1).toLocaleString()}–${Math.min(lbmPageOffset+LBM_PAGE_SIZE,total).toLocaleString()} of ${total.toLocaleString()}`:"";const prevBtn=$("lbmPrevPageBtn");if(prevBtn)prevBtn.disabled=lbmPageOffset<=0;const nextBtn=$("lbmNextPageBtn");if(nextBtn)nextBtn.disabled=lbmPageOffset+LBM_PAGE_SIZE>=total}}$("lbmResultsHead")?.addEventListener("change",e=>{if(e.target&&e.target.id==="lbmSelectAllCb"){if(e.target.checked)lbmDeselectedIds.clear();else lbmResultRows.forEach(r=>lbmDeselectedIds.add(String(r.id)));renderMatchResults()}});$("lbmResultsHead")?.addEventListener("click",e=>{const th=e.target.closest(".lbm-sort-th");if(th)lbmHandleSortClick(th.dataset.sortKey)});$("lbmResultsBody")?.addEventListener("change",e=>{const cb=e.target.closest(".lbm-row-cb");if(!cb)return;const id=cb.dataset.id;if(cb.checked)lbmDeselectedIds.delete(id);else lbmDeselectedIds.add(id);const selectAllCb=$("lbmSelectAllCb");if(selectAllCb){const pageIds=Array.from(document.querySelectorAll(".lbm-row-cb")).map(el=>el.dataset.id);selectAllCb.checked=pageIds.every(pid=>!lbmDeselectedIds.has(pid))}});$("lbmPrevPageBtn")?.addEventListener("click",()=>{lbmPageOffset=Math.max(0,lbmPageOffset-LBM_PAGE_SIZE);renderMatchResults()});$("lbmNextPageBtn")?.addEventListener("click",()=>{lbmPageOffset+=LBM_PAGE_SIZE;renderMatchResults()});$("lbmCloseBtn").addEventListener("click",()=>{$("lbMatchModalOverlay").classList.remove("active")});$("lbMatchModalOverlay").addEventListener("click",e=>{if(e.target.id==="lbMatchModalOverlay")$("lbMatchModalOverlay").classList.remove("active")});document.addEventListener("click",e=>{const btn=e.target.closest(".lbm-open-btn");if(!btn)return;openMatchModal(btn.dataset.orderType,btn.dataset.orderId)});$("lbmExportBtn").addEventListener("click",async()=>{const rows=lbmResultRows.filter(r=>!lbmDeselectedIds.has(String(r.id)));if(!rows.length){alert("Select at least one row.");return}const btn=$("lbmExportBtn");btn.disabled=true;const msg=$("lbmMsg");msg.textContent="Recording delivery…";msg.className="msg";exportCSV(`${lbmOrderType}-match-${new Date().toISOString().slice(0,10)}.csv`,LB_FULL_CSV_HEADERS,rows.map(lbFullCsvRow));const result=await listBuilderCall({action:"record_delivery",orderType:lbmOrderType,orderId:lbmOrderId,propertyIds:rows.map(r=>r.id)});btn.disabled=false;if(!result.ok){msg.textContent="CSV downloaded, but recording delivery failed: "+(result.error||"unknown error");msg.className="msg error";return}msg.textContent=result.warning||`Marked ${rows.length} propert${rows.length===1?"y":"ies"} delivered and the order fulfilled.`;msg.className=result.warning?"msg error":"msg ok";logActivity(`${lbmOrderType}.list_builder_match_delivered`,lbmOrderType,lbmOrderId,`Delivered ${rows.length} propert${rows.length===1?"y":"ies"} via List Builder match`);if(lbmOrderType==="data_order")await loadDataOrders();else await loadCsubListOrders()});let dataOrdersCache=[];const DATA_ORDER_STATUSES=["pending","in_progress","fulfilled","cancelled"];let doSearchQuery="";let doStatusFilterValue="";let doSortKey="submitted_at";let doSortDir="desc";(function initDataOrdersFilterUI(){const sel=$("doStatusFilter");if(sel)sel.innerHTML='<option value="">Any</option>'+DATA_ORDER_STATUSES.map(s=>`<option value="${s}">${s.replace("_"," ")}</option>`).join("")})();async function loadDataOrders(){const body=$("doOrdersBody");if(body&&!dataOrdersCache.length)body.innerHTML='<tr><td colspan="15" class="empty">Loading…</td></tr>';const{data,error}=await sb.from("client_data_orders").select("id, submitted_at, full_name, email, phone, quantity_requested, targeted_states, targeted_counties, targeted_cities, lead_type, property_type, price_restrictions, preferred_equity_pct, more_details, amount, order_status").order("submitted_at",{ascending:false}).limit(1e3);if(error){if(body)body.innerHTML=`<tr><td colspan="15" class="empty">${escapeHtml(error.message)}</td></tr>`;return}dataOrdersCache=data||[];renderDataOrdersTable()}function doMatchesSearch(o,q){if(!q)return true;const haystack=[o.full_name,o.email,o.phone,o.targeted_states,o.targeted_counties,o.targeted_cities,o.lead_type,o.property_type,o.price_restrictions,o.more_details].map(v=>(v||"").toString().toLowerCase()).join(" ");return haystack.includes(q)}function doSortCompare(a,b){let av=a[doSortKey],bv=b[doSortKey];if(doSortKey==="amount"){av=Number(av)||0;bv=Number(bv)||0}else{av=av==null?"":String(av);bv=bv==null?"":String(bv)}if(av<bv)return doSortDir==="asc"?-1:1;if(av>bv)return doSortDir==="asc"?1:-1;return 0}function renderDataOrdersHead(){const head=$("doOrdersHead");if(!head)return;const sortTh=(key,label)=>{const indicator=doSortKey===key?doSortDir==="asc"?" &#9650;":" &#9660;":"";return`<th class="do-sort-th" data-sort-key="${key}" style="cursor:pointer;">${label}${indicator}</th>`};head.innerHTML=`<tr>
    ${sortTh("submitted_at","Submitted")}${sortTh("full_name","Name")}<th>Contact</th><th>Quantity</th><th>States</th><th>Counties</th><th>Cities</th>
    ${sortTh("lead_type","Lead Type")}${sortTh("property_type","Property Type")}<th>Price Restrictions</th><th>Equity %</th><th>Notes</th>
    ${sortTh("amount","Amount")}${sortTh("order_status","Status")}<th>Match</th>
  </tr>`}function renderDataOrdersTable(){const body=$("doOrdersBody");const emptyEl=$("doOrdersEmpty");if(!body)return;renderDataOrdersHead();const q=doSearchQuery.trim().toLowerCase();const filtered=dataOrdersCache.filter(o=>doMatchesSearch(o,q)&&(!doStatusFilterValue||o.order_status===doStatusFilterValue)).sort(doSortCompare);const countEl=$("doResultCount");if(countEl){countEl.textContent=q||doStatusFilterValue?`Showing ${filtered.length.toLocaleString()} of ${dataOrdersCache.length.toLocaleString()} orders`:`${dataOrdersCache.length.toLocaleString()} order${dataOrdersCache.length===1?"":"s"}`}if(!dataOrdersCache.length){body.innerHTML="";if(emptyEl){emptyEl.style.display="";emptyEl.textContent="No data order requests yet."}return}if(!filtered.length){body.innerHTML="";if(emptyEl){emptyEl.style.display="";emptyEl.textContent="No orders match this search."}return}if(emptyEl)emptyEl.style.display="none";const dataOrdersTz=nonAgentDisplayTz();body.innerHTML=filtered.map(o=>`
    <tr>
      <td class="mono" style="font-size:11px; white-space:nowrap;">${o.submitted_at?new Date(o.submitted_at).toLocaleString([],{month:"short",day:"numeric",year:"numeric",hour:"2-digit",minute:"2-digit",...dataOrdersTz?{timeZone:dataOrdersTz}:{}}):"—"}</td>
      <td>${escapeHtml(o.full_name||"—")}</td>
      <td style="font-size:11px;">${escapeHtml(o.email||"")}${o.email&&o.phone?"<br>":""}${escapeHtml(o.phone||"")}</td>
      <td>${escapeHtml(o.quantity_requested||"—")}</td>
      <td>${escapeHtml(o.targeted_states||"—")}</td>
      <td>${escapeHtml(o.targeted_counties||"—")}</td>
      <td>${escapeHtml(o.targeted_cities||"—")}</td>
      <td>${escapeHtml(o.lead_type||"—")}</td>
      <td>${escapeHtml(o.property_type||"—")}</td>
      <td style="max-width:200px; white-space:pre-wrap;">${escapeHtml(o.price_restrictions||"—")}</td>
      <td>${escapeHtml(o.preferred_equity_pct||"—")}</td>
      <td style="max-width:220px; white-space:pre-wrap;">${escapeHtml(o.more_details||"—")}</td>
      <td>${o.amount!=null?"$"+Number(o.amount).toLocaleString():"—"}</td>
      <td><select class="do-status-select" data-order-id="${o.id}" style="font-size:11px;">
        ${DATA_ORDER_STATUSES.map(s=>`<option value="${s}" ${o.order_status===s?"selected":""}>${s.replace("_"," ")}</option>`).join("")}
      </select></td>
      <td><button class="btn-secondary lbm-open-btn owner-admin-export-only" data-order-type="data_order" data-order-id="${o.id}" style="width:auto; padding:5px 10px; font-size:10px;">Match</button></td>
    </tr>
  `).join("");body.querySelectorAll(".do-status-select").forEach(sel=>{sel.addEventListener("change",()=>updateDataOrderStatus(sel.dataset.orderId,sel.value))});applyOwnerAdminExportGating(body)}async function updateDataOrderStatus(id,status){const{error}=await sb.from("client_data_orders").update({order_status:status}).eq("id",id);if(error){alert("Failed to update status: "+error.message);return}const row=dataOrdersCache.find(o=>o.id===id);if(row)row.order_status=status;logActivity("client_data_order.status_changed","client_data_order",id,`Status set to ${status}`);renderDataOrdersTable();loadOrderReadiness(true)}$("doSearchInput")?.addEventListener("input",()=>{doSearchQuery=$("doSearchInput").value;renderDataOrdersTable()});$("doStatusFilter")?.addEventListener("change",()=>{doStatusFilterValue=$("doStatusFilter").value;renderDataOrdersTable()});$("doOrdersHead")?.addEventListener("click",e=>{const th=e.target.closest(".do-sort-th");if(!th)return;const key=th.dataset.sortKey;if(doSortKey===key)doSortDir=doSortDir==="asc"?"desc":"asc";else{doSortKey=key;doSortDir="asc"}renderDataOrdersTable()});function extractFunctionErrorMessage(body,fallback){const candidate=body&&(body.error||body.message||body.msg)||fallback;return typeof candidate==="string"&&candidate?candidate:"Unknown error"}async function invokeManageUser(payload){const{data,error}=await sb.functions.invoke("manage-user",{body:payload});if(error){try{const body=await error.context.json();return{ok:false,error:extractFunctionErrorMessage(body,error.message)}}catch{return{ok:false,error:error.message||"Unknown error"}}}return data}async function callManageUser(action,extra){return invokeManageUser({action,userId:currentDetailAgent.id,...extra})}function generatePassword(){const chars="ABCDEFGHJKMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz23456789";let out="";const bytes=new Uint8Array(14);crypto.getRandomValues(bytes);for(let i=0;i<bytes.length;i++)out+=chars[bytes[i]%chars.length];return out}var staffDepartments=null;async function loadStaffDepartments(){if(staffDepartments)return staffDepartments;const{data,error}=await sb.from("staff_departments").select("name, sort_order").order("sort_order").order("name");if(error){console.error("loadStaffDepartments failed",error);return[]}staffDepartments=data||[];return staffDepartments}async function fillDepartmentSelect(selectId,current){const sel=$(selectId);if(!sel)return;const rows=await loadStaffDepartments();sel.innerHTML='<option value="">&mdash; not set &mdash;</option>'+rows.map(r=>`<option value="${escapeHtml(r.name)}">${escapeHtml(r.name)}</option>`).join("");sel.value=current||""}async function saveStaffOrgInfo(userId,department,title,grade){const{error}=await sb.rpc("set_staff_org_info",{target:userId,p_department:department||null,p_title:title||null,p_grade:grade||null});return error?{ok:false,error:error.message}:{ok:true}}async function refreshAddUserRoleOptions(selectId="newUserRole"){const{data}=await sb.from("roles").select("name").order("name");const roles=(data||[]).filter(r=>r.name!=="owner"&&(myIsOwner||r.name!=="admin"));const sel=$(selectId);const prev=sel.value;sel.innerHTML=roles.map(r=>`<option value="${escapeHtml(r.name)}">${escapeHtml(roleDisplayLabel(r.name))}</option>`).join("");sel.value=roles.some(r=>r.name===prev)?prev:roles.some(r=>r.name==="agent")?"agent":(roles[0]||{}).name||""}async function openAddUserModal(){$("newUserFullName").value="";$("newUserEmail").value="";await refreshAddUserRoleOptions();$("newUserTitle").value="";$("newUserGrade").value="staff";await fillDepartmentSelect("newUserDepartment","");$("newUserPassword").value=generatePassword();$("addUserMsg").textContent="";$("addUserFormView").style.display="block";$("addUserSuccessView").style.display="none";$("addUserModalOverlay").classList.add("active")}function closeAddUserModal(){$("addUserModalOverlay").classList.remove("active")}const _addUserBtn=document.getElementById("addUserBtn");if(_addUserBtn)_addUserBtn.addEventListener("click",openAddUserModal);$("addUserCancelBtn").addEventListener("click",closeAddUserModal);$("addUserModalOverlay").addEventListener("click",e=>{if(e.target.id==="addUserModalOverlay")closeAddUserModal()});$("newUserGenPasswordBtn").addEventListener("click",()=>{$("newUserPassword").value=generatePassword()});$("addUserCreateBtn").addEventListener("click",async()=>{const msg=$("addUserMsg");msg.textContent="";const full_name=$("newUserFullName").value.trim();const email=$("newUserEmail").value.trim();const role=$("newUserRole").value;const password=$("newUserPassword").value;const department=$("newUserDepartment").value;const title=$("newUserTitle").value.trim();const grade=$("newUserGrade").value;if(!email){msg.textContent="Email is required.";return}if(!password||password.length<8){msg.textContent="Password must be at least 8 characters.";return}const btn=$("addUserCreateBtn");btn.disabled=true;const result=await invokeManageUser({action:"create_user",email,password,full_name,role});btn.disabled=false;if(!result.ok){msg.textContent=result.error||"Failed to create user.";return}let orgWarning="";if(department||title||grade){const org=await saveStaffOrgInfo(result.userId,department,title,grade);if(!org.ok){orgWarning=` Account created, but the department/title could not be saved (${org.error}). Set them from User Management > Users.`;console.error("set_staff_org_info failed after create_user",org.error)}}logActivity("user.create","agent",result.userId,`Created ${role} account for ${email}${full_name?" ("+full_name+")":""}${department||title?` — ${[title,department].filter(Boolean).join(" · ")}`:""}`);loadTeam();if(umActiveSection==="users")loadUmUsersList();$("addUserSuccessEmail").textContent=email;$("addUserSuccessPassword").textContent=password;$("addUserOrgWarning").textContent=orgWarning;$("addUserOrgWarning").style.display=orgWarning?"block":"none";$("addUserFormView").style.display="none";$("addUserSuccessView").style.display="block"});$("addUserCopyBtn").addEventListener("click",async()=>{const text=`Email: ${$("addUserSuccessEmail").textContent}
Password: ${$("addUserSuccessPassword").textContent}`;try{await navigator.clipboard.writeText(text);const btn=$("addUserCopyBtn");const original=btn.textContent;btn.textContent="Copied!";setTimeout(()=>{btn.textContent=original},1500)}catch{alert(text)}});$("addUserDoneBtn").addEventListener("click",closeAddUserModal);async function invokeManageClient(payload){const{data,error}=await sb.functions.invoke("manage-client",{body:payload});if(error){try{const body=await error.context.json();return{ok:false,error:extractFunctionErrorMessage(body,error.message)}}catch{return{ok:false,error:error.message||"Unknown error"}}}return data}async function loadUmClientsList(){clientPushInit();const tbody=$("umClientsTbody");const[{data:clients,error:clientsErr},{data:access}]=await Promise.all([sb.from("client_accounts").select("id, email, full_name, company_name, phone, must_change_password, is_active, created_at, last_login_at").order("created_at",{ascending:false}),sb.from("client_campaign_access").select("client_id, form_id, lead_submission_forms(name)")]);if(clientsErr){tbody.innerHTML=`<tr><td colspan="7" style="color:var(--danger);">${escapeHtml(clientsErr.message)}</td></tr>`;return}const accessByClient={};(access||[]).forEach(a=>{(accessByClient[a.client_id]||=[]).push(a.lead_submission_forms&&a.lead_submission_forms.name||"—")});if(!clients||!clients.length){tbody.innerHTML=`<tr><td colspan="7" style="color:var(--text-dim);">No client accounts yet.</td></tr>`;return}const clientsEditable=canEditUmSection("clients");tbody.innerHTML=clients.map(c=>`
    <tr>
      <td>
        <div style="color:var(--text);">${escapeHtml(c.full_name)}</div>
        <div style="font-size:11px; color:var(--text-dim);">${escapeHtml(c.email)}</div>
      </td>
      <td>${escapeHtml(c.company_name||"—")}</td>
      <td>
        <!-- v153: destination for the client-facing SMS notifications
             (new ticket / new lead / dispute-feedback reply) — see
             supabase/functions/send-client-sms. Blank = that client never
             gets texted, no error anywhere, just silently skipped. -->
        <span class="client-phone-cell" contenteditable="${clientsEditable}" data-id="${c.id}" data-original="${escapeHtml(c.phone||"")}" style="display:inline-block; min-width:110px;">${escapeHtml(c.phone||"")}</span>
      </td>
      <td style="max-width:260px;">${escapeHtml((accessByClient[c.id]||[]).join(", ")||"None")}</td>
      <td>
        ${c.is_active?'<span style="color:var(--good);">Active</span>':'<span style="color:var(--danger);">Deactivated</span>'}
        ${c.must_change_password?'<div style="font-size:10px; color:var(--text-dim);">pending first login</div>':""}
      </td>
      <td style="font-size:11px; color:var(--text-dim);">${c.last_login_at?new Date(c.last_login_at).toLocaleString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{}):"Never"}</td>
      <td style="white-space:nowrap;">
        <button class="btn-secondary edit-only client-access-btn" data-client-id="${c.id}" data-client-name="${escapeHtml(c.full_name)}" style="width:auto; padding:5px 10px; font-size:11px; margin-top:0;">Edit client</button>
        <button class="btn-secondary edit-only client-reset-btn" data-client-id="${c.id}" data-client-email="${escapeHtml(c.email)}" data-client-phone="${escapeHtml(c.phone||"")}" style="width:auto; padding:5px 10px; font-size:11px; margin-top:0;">Reset password</button>
        <button class="btn-secondary edit-only client-toggle-btn" data-client-id="${c.id}" data-active="${c.is_active}" style="width:auto; padding:5px 10px; font-size:11px; margin-top:0;">${c.is_active?"Deactivate":"Reactivate"}</button>
        ${c.is_active?`<button class="btn-secondary edit-only client-impersonate-btn" data-client-id="${c.id}" data-client-name="${escapeHtml(c.full_name)}" title="Open the client portal, signed in as this client, in a new tab" style="width:auto; padding:5px 10px; font-size:11px; margin-top:0;">Log in as client</button>`:""}
        ${myIsOwner?`<button class="btn-secondary client-delete-btn" data-client-id="${c.id}" data-client-email="${escapeHtml(c.email)}" title="Permanently delete this client account — Owner only" style="width:auto; padding:5px 10px; font-size:11px; margin-top:0; color:var(--danger); border-color:var(--danger);">Delete</button>`:""}
      </td>
    </tr>`).join("");applyUmSectionEditGating("clients")}let clientPushSelected=new Set;function clientPushCanSend(){return canEditUmSection("clients")}async function clientPushCall(payload){const{data,error}=await sb.functions.invoke("client-push-broadcast",{body:payload});if(error||!data||data.ok===false){throw new Error(data&&data.error||error&&error.message||"Something went wrong.")}return data}function clientPushRenderPickList(clients){const box=$("clientPushPickList");if(!box)return;box.innerHTML=(clients||[]).map(c=>`
    <label class="perm-side-item" style="display:flex; align-items:center; gap:8px; cursor:pointer;">
      <input type="checkbox" class="perm-cb client-push-pick" value="${c.id}" ${clientPushSelected.has(c.id)?"checked":""} style="width:auto;">
      <span class="perm-side-item-main">
        <span class="perm-side-item-name">${escapeHtml(c.name)}</span>
        <span class="perm-side-item-sub">${c.hasApp?"has the app":"no app yet"}</span>
      </span>
    </label>`).join("")||'<div class="perm-side-empty">No active clients.</div>';box.querySelectorAll(".client-push-pick").forEach(cb=>{cb.addEventListener("change",()=>{if(cb.checked)clientPushSelected.add(cb.value);else clientPushSelected.delete(cb.value)})})}async function clientPushPreview(showMessage){const msg=$("clientPushMsg");const audience=$("clientPushAudience").value;try{const data=await clientPushCall({action:"preview",audience,clientIds:[...clientPushSelected]});if(audience==="selected"||!$("clientPushPickList").innerHTML.trim())clientPushRenderPickList(data.clients);if(showMessage){msg.style.color="var(--text-2)";msg.textContent=`${data.clientsTargeted} client${data.clientsTargeted===1?"":"s"} match — ${data.clientsWithApp} ${data.clientsWithApp===1?"has":"have"} the app installed.`}return data}catch(e){if(showMessage){msg.style.color="var(--danger)";msg.textContent=e.message}return null}}async function clientPushLoadHistory(){const box=$("clientPushHistory");if(!box)return;try{const data=await clientPushCall({action:"history",limit:10});const rows=data.broadcasts||[];if(!rows.length){box.textContent="Nothing sent yet.";return}box.innerHTML=rows.map(b=>`
      <div style="padding:8px 0; border-top:1px solid var(--border);">
        <div style="color:var(--text); font-weight:600;">${escapeHtml(b.title)}</div>
        <div style="margin-top:2px;">${escapeHtml(b.body)}</div>
        <div class="mono" style="margin-top:4px; font-size:11px;">
          ${new Date(b.created_at).toLocaleString()} · ${escapeHtml(b.sent_by_name||"staff")} ·
          ${b.clients_reached} of ${b.clients_targeted} clients · ${b.devices_sent} phone${b.devices_sent===1?"":"s"}
        </div>
      </div>`).join("")}catch(e){box.textContent=e.message}}function clientPushInit(){const card=$("clientPushCard");if(!card)return;card.style.display=clientPushCanSend()?"":"none";if(!clientPushCanSend()||card.dataset.wired==="1")return;card.dataset.wired="1";$("clientPushAudience").addEventListener("change",()=>{const picking=$("clientPushAudience").value==="selected";$("clientPushPickWrap").style.display=picking?"":"none";if(picking)clientPushPreview(false)});$("clientPushPreviewBtn").addEventListener("click",()=>clientPushPreview(true));$("clientPushSendBtn").addEventListener("click",async()=>{const msg=$("clientPushMsg");const btn=$("clientPushSendBtn");const title=$("clientPushTitle").value.trim();const body=$("clientPushBody").value.trim();const url=$("clientPushUrl").value.trim();if(!title||!body){msg.style.color="var(--danger)";msg.textContent="A title and a message are both needed.";return}const preview=await clientPushPreview(false);if(!preview)return;if(!confirm(`Send "${title}" to ${preview.clientsWithApp} client${preview.clientsWithApp===1?"":"s"} with the app installed?

This reaches their phones straight away and cannot be undone.`))return;btn.disabled=true;msg.style.color="var(--text-2)";msg.textContent="Sending…";try{const data=await clientPushCall({action:"send",audience:$("clientPushAudience").value,clientIds:[...clientPushSelected],title,body,url:url||void 0});msg.style.color="var(--good, var(--focus))";msg.textContent=data.warning?`Sent to ${data.devicesSent} phone(s). ${data.warning}`:`Sent to ${data.devicesSent} phone${data.devicesSent===1?"":"s"} across ${data.clientsReached} client${data.clientsReached===1?"":"s"}.`;$("clientPushTitle").value="";$("clientPushBody").value="";$("clientPushUrl").value="";clientPushLoadHistory()}catch(e){msg.style.color="var(--danger)";msg.textContent=e.message}btn.disabled=false});clientPushLoadHistory()}if($("umClientsTbody"))$("umClientsTbody").addEventListener("focusout",async e=>{const el=e.target.closest(".client-phone-cell");if(!el)return;const newVal=el.textContent.trim();const original=el.dataset.original||"";if(newVal===original)return;const{error}=await sb.from("client_accounts").update({phone:newVal||null}).eq("id",el.dataset.id);if(error){alert(error.message);el.textContent=original;return}el.dataset.original=newVal;logActivity("client.update","client",el.dataset.id,`Set phone to "${newVal}"`)});let clientChatRoutesCache=[];let clientChatRoutesStaffCache=[];let clientChatRouteStaffByQueueKey={};let editingClientChatRouteKey=null;async function loadClientChatRoutes(){const tbody=$("clientChatRoutesTbody");const[{data:routes,error},{data:staff},{data:allRoles2},{data:routeStaff}]=await Promise.all([sb.from("client_chat_routes").select("queue_key, label, description").order("label"),sb.from("profiles").select("id, full_name, email, is_active, role, chat_status, last_seen_at").order("full_name"),sb.from("roles").select("name, allowed_tabs"),sb.from("client_chat_route_staff").select("queue_key, staff_id")]);if(error){tbody.innerHTML=`<tr><td colspan="4" style="color:var(--danger);">${escapeHtml(error.message)}</td></tr>`;return}clientChatRoutesCache=routes||[];clientChatRoutesStaffCache=staff||[];clientChatRouteStaffByQueueKey={};(routeStaff||[]).forEach(rs=>{(clientChatRouteStaffByQueueKey[rs.queue_key]=clientChatRouteStaffByQueueKey[rs.queue_key]||[]).push(rs.staff_id)});const allowedTabsByRole={};(allRoles2||[]).forEach(r=>{allowedTabsByRole[r.name]=Array.isArray(r.allowed_tabs)?r.allowed_tabs:[]});const canHandleClientChat=p=>{const role=(p.role||"").toLowerCase();if(role==="owner"||role==="admin")return true;if(role.replace(/[\s_-]+/g,"")==="sales")return true;return(allowedTabsByRole[p.role]||[]).includes("client_chat")};const eligibleStaff=clientChatRoutesStaffCache.filter(p=>p.is_active&&canHandleClientChat(p));const staffHint=$("clientChatRouteStaffHint");if(staffHint&&!eligibleStaff.length)staffHint.textContent='No one is eligible yet — grant a role the "Client Chat" permission under User Management → User Types, or assign someone the Sales role.';const staffName=id=>{const p=clientChatRoutesStaffCache.find(x=>x.id===id);return p?p.full_name||p.email:"—"};const staffPresenceLabel=p=>{const stale=!p.last_seen_at||Date.now()-new Date(p.last_seen_at).getTime()>=9e4;if(p.chat_status==="active"&&!stale)return{label:"Active",color:"var(--good)"};if(p.chat_status==="away"||stale)return{label:"Away",color:"var(--away)"};return{label:"Offline",color:"var(--text-dim)"}};if(!clientChatRoutesCache.length){tbody.innerHTML=`<tr><td colspan="4" style="color:var(--text-dim);">No teams configured yet — clients won't be offered a "Connect me to..." option (the AI will just tell them someone will follow up) until at least one exists.</td></tr>`;return}tbody.innerHTML=clientChatRoutesCache.map(r=>{const staffIds=clientChatRouteStaffByQueueKey[r.queue_key]||[];const assignedHtml=staffIds.length?staffIds.map(id=>{const p=clientChatRoutesStaffCache.find(x=>x.id===id);const presence=p?staffPresenceLabel(p):{label:"",color:"var(--text-dim)"};return`${escapeHtml(staffName(id))} <span style="color:${presence.color}; font-size:10px;">(${presence.label})</span>`}).join("<br>"):'<span style="color:var(--text-dim);">Nobody assigned</span>';return`
    <tr>
      <td><div style="color:var(--text);">${escapeHtml(r.label)}</div><div style="font-size:10px; color:var(--text-dim);" class="mono">${escapeHtml(r.queue_key)}</div></td>
      <td style="max-width:320px; font-size:12px; color:var(--text-2);">${escapeHtml(r.description)}</td>
      <td style="font-size:12px;">${assignedHtml}</td>
      <td style="white-space:nowrap;">
        <button class="btn-secondary client-chat-route-edit-btn" data-key="${escapeHtml(r.queue_key)}" style="width:auto; padding:5px 10px; font-size:11px; margin-top:0;">Edit</button>
        <button class="btn-secondary client-chat-route-delete-btn" data-key="${escapeHtml(r.queue_key)}" style="width:auto; padding:5px 10px; font-size:11px; margin-top:0; color:var(--danger);">Delete</button>
      </td>
    </tr>`}).join("");renderClientChatRouteStaffList(eligibleStaff,clientChatRouteStaffByQueueKey[editingClientChatRouteKey]||[])}function renderClientChatRouteStaffList(eligibleStaff,checkedIds){const container=$("clientChatRouteStaffList");if(!container)return;const checked=new Set(checkedIds||[]);container.innerHTML=eligibleStaff.map(p=>`
    <label style="display:flex; align-items:center; gap:8px; font-weight:400; margin:4px 0; cursor:pointer;">
      <input type="checkbox" class="client-chat-route-staff-cb" value="${p.id}" ${checked.has(p.id)?"checked":""} style="width:14px; height:14px; accent-color:var(--focus);">
      ${escapeHtml(p.full_name||p.email)}
    </label>`).join("")||'<div style="color:var(--text-dim);">No one is eligible yet.</div>'}function resetClientChatRouteForm(){editingClientChatRouteKey=null;$("clientChatRouteFormTitle").textContent="Add a team";$("clientChatRouteKeyInput").value="";$("clientChatRouteKeyInput").disabled=false;$("clientChatRouteLabelInput").value="";$("clientChatRouteDescInput").value="";document.querySelectorAll("#clientChatRouteStaffList .client-chat-route-staff-cb").forEach(cb=>{cb.checked=false});$("saveClientChatRouteBtn").textContent="Save team";$("cancelClientChatRouteEditBtn").style.display="none";$("clientChatRouteMsg").textContent=""}$("clientChatRoutesTbody").addEventListener("click",e=>{const editBtn=e.target.closest(".client-chat-route-edit-btn");if(editBtn){const route=clientChatRoutesCache.find(r=>r.queue_key===editBtn.dataset.key);if(!route)return;editingClientChatRouteKey=route.queue_key;$("clientChatRouteFormTitle").textContent=`Edit "${route.label}"`;$("clientChatRouteKeyInput").value=route.queue_key;$("clientChatRouteKeyInput").disabled=true;$("clientChatRouteLabelInput").value=route.label;$("clientChatRouteDescInput").value=route.description;document.querySelectorAll("#clientChatRouteStaffList .client-chat-route-staff-cb").forEach(cb=>{cb.checked=(clientChatRouteStaffByQueueKey[route.queue_key]||[]).includes(cb.value)});$("saveClientChatRouteBtn").textContent="Save changes";$("cancelClientChatRouteEditBtn").style.display="inline-block";$("clientChatRouteMsg").textContent="";return}const delBtn=e.target.closest(".client-chat-route-delete-btn");if(delBtn){const route=clientChatRoutesCache.find(r=>r.queue_key===delBtn.dataset.key);if(!route)return;if(!confirm(`Delete the "${route.label}" team? Any client currently routed there stays assigned (existing conversations aren't affected), but it won't be offered as an option going forward.`))return;sb.from("client_chat_routes").delete().eq("queue_key",route.queue_key).then(({error})=>{if(error)alert("Failed to delete: "+error.message);if(editingClientChatRouteKey===route.queue_key)resetClientChatRouteForm();loadClientChatRoutes()})}});$("cancelClientChatRouteEditBtn").addEventListener("click",resetClientChatRouteForm);$("saveClientChatRouteBtn").addEventListener("click",async()=>{const msg=$("clientChatRouteMsg");msg.textContent="";const queueKey=$("clientChatRouteKeyInput").value.trim().toLowerCase().replace(/[^a-z0-9_]/g,"_");const label=$("clientChatRouteLabelInput").value.trim();const description=$("clientChatRouteDescInput").value.trim();const staffIds=Array.from(document.querySelectorAll("#clientChatRouteStaffList .client-chat-route-staff-cb:checked")).map(cb=>cb.value);if(!queueKey||!label||!description||!staffIds.length){msg.textContent="Key, label, description, and at least one assigned staff member are all required.";msg.className="msg error";return}const btn=$("saveClientChatRouteBtn");btn.disabled=true;const{error}=await sb.from("client_chat_routes").upsert({queue_key:queueKey,label,description},{onConflict:"queue_key"});if(error){btn.disabled=false;msg.textContent=error.message;msg.className="msg error";return}const{data:currentStaffRows,error:currentStaffErr}=await sb.from("client_chat_route_staff").select("staff_id").eq("queue_key",queueKey);if(currentStaffErr){btn.disabled=false;msg.textContent=`Team saved, but couldn't read the current staff list to update it: ${currentStaffErr.message}`;msg.className="msg error";return}const currentStaffIds=new Set((currentStaffRows||[]).map(r=>r.staff_id));const newStaffIds=new Set(staffIds);const staffIdsToAdd=staffIds.filter(id=>!currentStaffIds.has(id));const staffIdsToRemove=[...currentStaffIds].filter(id=>!newStaffIds.has(id));if(staffIdsToAdd.length){const{error:addErr}=await sb.from("client_chat_route_staff").insert(staffIdsToAdd.map(staff_id=>({queue_key:queueKey,staff_id})));if(addErr){btn.disabled=false;msg.textContent=`Team saved, but assigning staff failed: ${addErr.message}`;msg.className="msg error";return}}if(staffIdsToRemove.length){const{error:removeErr}=await sb.from("client_chat_route_staff").delete().eq("queue_key",queueKey).in("staff_id",staffIdsToRemove);btn.disabled=false;if(removeErr){msg.textContent=`Team saved and new staff added, but removing unchecked staff failed: ${removeErr.message}`;msg.className="msg error";return}}else{btn.disabled=false}const staffLabels=staffIds.map(id=>{const p=clientChatRoutesStaffCache.find(x=>x.id===id);return p?p.full_name||p.email:id}).join(", ");resetClientChatRouteForm();loadClientChatRoutes();logActivity("client_chat_route.upsert",null,null,`Set "${label}" -> ${staffLabels}`)});async function getRealCampaignFormIds(){const{data}=await sb.from("client_campaign_subscriptions").select("form_id");return new Set((data||[]).map(s=>s.form_id).filter(Boolean))}async function renderClientCampaignCheckboxes(container,checkedIds){const[{data},realCampaignFormIds]=await Promise.all([sb.from("lead_submission_forms").select("id, name").eq("is_active",true).order("sort_order").order("name"),getRealCampaignFormIds()]);const checked=new Set(checkedIds||[]);const forms=(data||[]).filter(f=>f.name!==ISPD_DEFAULT_CAMPAIGN_NAME).filter(f=>realCampaignFormIds.has(f.id)||checked.has(f.id));container.innerHTML=forms.map(f=>{const isRealCampaign=realCampaignFormIds.has(f.id);return`
    <label style="display:flex; align-items:center; gap:8px; font-weight:400; text-transform:none; letter-spacing:0; margin:4px 0; font-family:var(--sans); ${isRealCampaign?"":"color:var(--text-dim);"}">
      <input type="checkbox" class="client-campaign-cb" value="${f.id}" ${checked.has(f.id)?"checked":""} style="width:14px; height:14px; accent-color:var(--focus);">
      ${escapeHtml(f.name)}${isRealCampaign?"":' <span style="font-size:10px;">(legacy — not a campaign)</span>'}
    </label>`}).join("")||'<div style="color:var(--text-dim);">No campaigns exist yet — create one under Campaigns → Campaign Management first.</div>'}async function renderClientGuideCheckboxes(container,checkedNormNames){const[{data},realCampaignFormIds]=await Promise.all([sb.from("lead_submission_forms").select("id, name").eq("is_active",true).order("sort_order").order("name"),getRealCampaignFormIds()]);const checked=new Set(checkedNormNames||[]);const forms=(data||[]).filter(f=>f.name!==ISPD_DEFAULT_CAMPAIGN_NAME).filter(f=>realCampaignFormIds.has(f.id)||checked.has(csubNormalizeLabel(f.name)));container.innerHTML=forms.map(f=>{const normName=csubNormalizeLabel(f.name);const isRealCampaign=realCampaignFormIds.has(f.id);return`
    <label style="display:flex; align-items:center; gap:8px; font-weight:400; text-transform:none; letter-spacing:0; margin:4px 0; font-family:var(--sans); ${isRealCampaign?"":"color:var(--text-dim);"}">
      <input type="checkbox" class="client-guide-cb" value="${escapeHtml(normName)}" data-display-name="${escapeHtml(f.name)}" ${checked.has(normName)?"checked":""} style="width:14px; height:14px; accent-color:var(--focus);">
      ${escapeHtml(f.name)}${isRealCampaign?"":' <span style="font-size:10px;">(legacy — not a campaign)</span>'}
    </label>`}).join("")||'<div style="color:var(--text-dim);">No campaigns exist yet — create one under Campaigns → Campaign Management first.</div>'}async function renderClientReadyModeChannelCheckboxes(container,clientId){const result=await callRmApi({action:"list_lead_posting_channels"});if(!result.ok){container.innerHTML=`<div class="msg error">${escapeHtml(result.error||"Failed to load channels.")}</div>`;return}const channels=result.channels||[];if(!channels.length){container.innerHTML='<div style="color:var(--text-dim);">No ReadyMode channels saved yet — add one under User Management → Integrations → ReadyMode first.</div>';return}container.innerHTML=channels.map(c=>{const mine=c.client_id===clientId;const takenByOther=c.client_id&&!mine;if(takenByOther){return`
      <label style="display:flex; align-items:center; gap:8px; font-weight:400; text-transform:none; letter-spacing:0; margin:4px 0; font-family:var(--sans); color:var(--text-dim);">
        <input type="checkbox" disabled style="width:14px; height:14px;">
        ${escapeHtml(c.channel_name)} <span style="font-size:10px;">(assigned to ${escapeHtml(c.client_name||"another client")})</span>
      </label>`}return`
    <label style="display:flex; align-items:center; gap:8px; font-weight:400; text-transform:none; letter-spacing:0; margin:4px 0; font-family:var(--sans);">
      <input type="checkbox" class="client-channel-cb" value="${c.id}" ${mine?"checked":""} style="width:14px; height:14px; accent-color:var(--focus);">
      ${escapeHtml(c.channel_name)} <span style="font-size:10px; color:var(--text-dim);">(${escapeHtml(c.account_domain)})</span>
    </label>`}).join("")}async function openAddClientModal(){$("newClientFullName").value="";$("newClientEmail").value="";$("newClientCompany").value="";$("newClientPhone").value="";$("newClientTextPassword").checked=false;$("newClientPassword").value=generatePassword();$("addClientMsg").textContent="";$("addClientSmsStatus").style.display="none";await renderClientCampaignCheckboxes($("newClientCampaignList"),[]);await renderClientGuideCheckboxes($("newClientGuideList"),[]);$("addClientFormView").style.display="block";$("addClientSuccessView").style.display="none";$("addClientModalOverlay").classList.add("active")}function closeAddClientModal(){$("addClientModalOverlay").classList.remove("active")}$("addClientBtn").addEventListener("click",openAddClientModal);$("addClientCancelBtn").addEventListener("click",closeAddClientModal);$("addClientModalOverlay").addEventListener("click",e=>{if(e.target.id==="addClientModalOverlay")closeAddClientModal()});$("newClientGenPasswordBtn").addEventListener("click",()=>{$("newClientPassword").value=generatePassword()});$("addClientCreateBtn").addEventListener("click",async()=>{const msg=$("addClientMsg");msg.textContent="";const full_name=$("newClientFullName").value.trim();const email=$("newClientEmail").value.trim();const company_name=$("newClientCompany").value.trim();const phone=$("newClientPhone").value.trim();const password=$("newClientPassword").value;const form_ids=Array.from(document.querySelectorAll("#newClientCampaignList .client-campaign-cb:checked")).map(cb=>cb.value);const guides=Array.from(document.querySelectorAll("#newClientGuideList .client-guide-cb:checked")).map(cb=>({norm_name:cb.value,display_name:cb.dataset.displayName}));if(!full_name){msg.textContent="Full name is required.";return}if(!email){msg.textContent="Email is required.";return}if(!password||password.length<8){msg.textContent="Password must be at least 8 characters.";return}const btn=$("addClientCreateBtn");btn.disabled=true;btn.textContent="Creating…";const result=await invokeManageClient({action:"create_client",email,password,full_name,company_name,form_ids});btn.disabled=false;btn.textContent="Create client";if(!result.ok){msg.textContent=result.error||"Failed to create client.";return}if(phone)await sb.from("client_accounts").update({phone}).eq("id",result.clientId);let guideError=null;if(guides.length){const guideResult=await invokeManageClient({action:"update_guide_access",clientId:result.clientId,guides});if(!guideResult.ok)guideError=guideResult.error||"Failed to grant Campaign Guide access."}let smsResult=null;if(phone&&$("newClientTextPassword").checked){smsResult=await callSendClientSms(result.clientId,"account_password",password)}logActivity("client.create","client",result.clientId,`Created client account for ${email}${full_name?" ("+full_name+")":""}${guides.length?`, granted ${guides.length} Campaign Guide(s)`:""}`);loadUmClientsList();$("addClientSuccessEmail").textContent=email;$("addClientSuccessPassword").textContent=password;const smsStatusEl=$("addClientSmsStatus");const statusLines=[];if(smsResult){statusLines.push(smsResult.ok?"Texted to the client.":`Couldn't text the client (${smsResult.error||"unknown error"}) — share the credentials below with them directly.`)}if(guideError)statusLines.push(`Campaign Guide access failed to save (${guideError}) — set it from Edit client.`);if(statusLines.length){smsStatusEl.style.display="";smsStatusEl.className=smsResult&&!smsResult.ok||guideError?"msg error":"msg ok";smsStatusEl.textContent=statusLines.join(" ")}else{smsStatusEl.style.display="none"}$("addClientFormView").style.display="none";$("addClientSuccessView").style.display="block"});$("addClientCopyBtn").addEventListener("click",async()=>{const text=`Email: ${$("addClientSuccessEmail").textContent}
Password: ${$("addClientSuccessPassword").textContent}`;try{await navigator.clipboard.writeText(text);const btn=$("addClientCopyBtn");const original=btn.textContent;btn.textContent="Copied!";setTimeout(()=>{btn.textContent=original},1500)}catch{alert(text)}});$("addClientDoneBtn").addEventListener("click",closeAddClientModal);const CLIENT_PORTAL_SECTIONS=[{key:"dashboard",label:"Dashboard",note:"Hidden automatically for skip-trace-only accounts."},{key:"leads",label:"Opportunities",note:"Hidden automatically for skip-trace-only accounts.",subs:[{key:"my-leads",label:"My Opportunities"},{key:"campaign-guide",label:"Campaign Guide"},{key:"leads-comments",label:"Opportunity Comments"}]},{key:"tickets",label:"Tickets",note:"Hidden automatically for skip-trace-only accounts."},{key:"reports",label:"Reports",note:'Also needs "Reports tab enabled" above and a ReadyMode campaign feeding it.'},{key:"buylist",label:"Buy a list",subs:[{key:"request",label:"Request a list"},{key:"your-requests",label:"Your list requests"}]},{key:"skiptrace",label:"Skip Tracing",subs:[{key:"place-order",label:"Place Order"},{key:"order-history",label:"Order History"}]},{key:"marketing",label:"Email Marketing",note:"Also needs an Email Marketing seat — unticking here cannot grant one, and ticking it cannot either.",subs:[{key:"setup",label:"Setup"},{key:"lists",label:"Lists"},{key:"campaigns",label:"Campaigns"},{key:"inbox",label:"Email Inbox"}]},{key:"billing",label:"Billing",warn:"With Billing off this client cannot pay an invoice or see what they owe — leave it on unless you are handling their payments for them.",subs:[{key:"subscriptions",label:"Subscriptions"},{key:"invoices",label:"Invoices"},{key:"pending-payment",label:"Pending payment"},{key:"buy-credits",label:"Buy Skip Tracing Credits"},{key:"transactions",label:"Transaction History"},{key:"payment-method",label:"Payment Method"}]},{key:"profile",label:"Profile Details",warn:"With this off they cannot update their own contact details or password from the portal."}];function clientPortalTabOn(sections,tabKey){const cur=(sections||{})[tabKey];return cur&&typeof cur.view==="boolean"?cur.view:true}function clientPortalSubOn(sections,tabKey,subKey){const cur=((sections||{})[tabKey]||{}).subtabs||{};return typeof cur[subKey]==="boolean"?cur[subKey]:true}function renderClientPortalSections(sections){const root=$("clientAccessPortalSections");if(!root)return;root.innerHTML=CLIENT_PORTAL_SECTIONS.map(t=>{const on=clientPortalTabOn(sections,t.key);const subs=t.subs||[];return`
      <div class="perm-tab-card${on?"":" is-off"}" data-cp-card="${t.key}">
        <div class="perm-tab-head perm-head-2">
          <div class="perm-tab-title">
            ${subs.length?`<button type="button" class="perm-disclose" data-cp-disclose="${t.key}" aria-label="Show or hide subtabs">${on?"&#9662;":"&#9656;"}</button>`:'<span class="perm-disclose"></span>'}
            <label style="display:flex; align-items:center; gap:8px; cursor:pointer; text-transform:none; font-family:var(--sans); letter-spacing:0; margin:0; min-width:0;">
              <input type="checkbox" class="perm-cb cp-tab-cb" data-cp-tab="${t.key}" ${on?"checked":""}>
              <span class="perm-tab-name">${escapeHtml(t.label)}</span>
            </label>
          </div>
          <div class="perm-col-label">Show</div>
        </div>
        ${t.note||t.warn||subs.length?`
          <div class="perm-body" data-cp-body="${t.key}"${on?"":' style="display:none;"'}>
            ${t.note?`<div class="perm-note">${t.note}</div>`:""}
            ${t.warn?`<div class="perm-note" style="color:var(--danger);">${t.warn}</div>`:""}
            ${subs.map(sub=>`
              <div class="perm-row perm-row-2">
                <div class="perm-row-label">${escapeHtml(sub.label)}</div>
                <div class="perm-cell"><input type="checkbox" class="perm-cb cp-sub-cb" data-cp-tab="${t.key}" data-cp-sub="${sub.key}" ${clientPortalSubOn(sections,t.key,sub.key)?"checked":""}></div>
              </div>`).join("")}
          </div>`:""}
      </div>`}).join("");root.querySelectorAll("[data-cp-disclose]").forEach(btn=>{btn.addEventListener("click",()=>{const body=root.querySelector(`[data-cp-body="${btn.dataset.cpDisclose}"]`);if(!body)return;const open=body.style.display!=="none";body.style.display=open?"none":"";btn.innerHTML=open?"&#9656;":"&#9662;"})});root.querySelectorAll(".cp-tab-cb").forEach(box=>{box.addEventListener("change",()=>{const key=box.dataset.cpTab;const card=root.querySelector(`[data-cp-card="${key}"]`);const body=root.querySelector(`[data-cp-body="${key}"]`);const arrow=root.querySelector(`[data-cp-disclose="${key}"]`);if(card)card.classList.toggle("is-off",!box.checked);if(body)body.style.display=box.checked?"":"none";if(arrow)arrow.innerHTML=box.checked?"&#9662;":"&#9656;"})})}function readClientPortalSections(){const root=$("clientAccessPortalSections");const out={};if(!root)return out;CLIENT_PORTAL_SECTIONS.forEach(t=>{const tabBox=root.querySelector(`.cp-tab-cb[data-cp-tab="${t.key}"]`);const entry={};if(tabBox&&!tabBox.checked)entry.view=false;const subs={};(t.subs||[]).forEach(sub=>{const b=root.querySelector(`.cp-sub-cb[data-cp-tab="${t.key}"][data-cp-sub="${sub.key}"]`);if(b&&!b.checked)subs[sub.key]=false});if(Object.keys(subs).length)entry.subtabs=subs;if(Object.keys(entry).length)out[t.key]=entry});return out}let clientAccessTargetId=null;async function openClientAccessModal(clientId,clientName){clientAccessTargetId=clientId;$("clientAccessName").textContent=clientName;$("clientAccessMsg").textContent="";const[{data:account,error:accountErr},{data:current},{data:currentGuides}]=await Promise.all([sb.from("client_accounts").select("email, phone, company_name, reports_enabled, reports_show_hours, portal_sections").eq("id",clientId).maybeSingle(),sb.from("client_campaign_access").select("form_id").eq("client_id",clientId),sb.from("client_campaign_guide_access").select("campaign_norm_name").eq("client_id",clientId)]);if(accountErr){$("clientAccessMsg").textContent="Failed to load this client's info: "+accountErr.message;$("clientAccessModalOverlay").classList.add("active");return}$("clientAccessEmail").value=account&&account.email||"";$("clientAccessPhone").value=account&&account.phone||"";$("clientAccessCompany").value=account&&account.company_name||"";$("clientAccessReportsEnabled").checked=!!(account&&account.reports_enabled);$("clientAccessReportsShowHours").checked=!(account&&account.reports_show_hours===false);renderClientPortalSections(account&&account.portal_sections||{});await renderClientCampaignCheckboxes($("clientAccessCampaignList"),(current||[]).map(r=>r.form_id));await renderClientGuideCheckboxes($("clientAccessGuideList"),(currentGuides||[]).map(r=>r.campaign_norm_name));await renderClientReadyModeChannelCheckboxes($("clientAccessChannelList"),clientId);$("clientAccessModalOverlay").classList.add("active")}$("clientAccessCancelBtn").addEventListener("click",()=>$("clientAccessModalOverlay").classList.remove("active"));$("clientAccessModalOverlay").addEventListener("click",e=>{if(e.target.id==="clientAccessModalOverlay")$("clientAccessModalOverlay").classList.remove("active")});$("clientAccessSaveBtn").addEventListener("click",async()=>{const msg=$("clientAccessMsg");msg.textContent="";const email=$("clientAccessEmail").value.trim();const phone=$("clientAccessPhone").value.trim();const company_name=$("clientAccessCompany").value.trim();if(!email){msg.textContent="Email is required.";return}const form_ids=Array.from(document.querySelectorAll("#clientAccessCampaignList .client-campaign-cb:checked")).map(cb=>cb.value);const guides=Array.from(document.querySelectorAll("#clientAccessGuideList .client-guide-cb:checked")).map(cb=>({norm_name:cb.value,display_name:cb.dataset.displayName}));const btn=$("clientAccessSaveBtn");btn.disabled=true;const contactResult=await invokeManageClient({action:"update_contact",clientId:clientAccessTargetId,email,phone,company_name});if(!contactResult.ok){btn.disabled=false;msg.textContent=contactResult.error||"Failed to update email/phone.";return}const result=await invokeManageClient({action:"update_access",clientId:clientAccessTargetId,form_ids});if(!result.ok){btn.disabled=false;msg.textContent=result.error||"Failed to update access.";return}const guideResult=await invokeManageClient({action:"update_guide_access",clientId:clientAccessTargetId,guides});if(!guideResult.ok){btn.disabled=false;msg.textContent=guideResult.error||"Saved, but Campaign Guide access failed to update.";return}const reportsEnabled=$("clientAccessReportsEnabled").checked;const reportsShowHours=$("clientAccessReportsShowHours").checked;const portalSections=readClientPortalSections();const{error:reportsErr}=await sb.from("client_accounts").update({reports_enabled:reportsEnabled,reports_show_hours:reportsShowHours,portal_sections:portalSections}).eq("id",clientAccessTargetId);if(reportsErr){btn.disabled=false;msg.textContent="Saved, but the Reports toggle failed to update: "+reportsErr.message;return}const channelBoxes=Array.from(document.querySelectorAll("#clientAccessChannelList .client-channel-cb"));for(const cb of channelBoxes){const channelResult=await callRmApi({action:"set_channel_client",channelId:cb.value,clientId:cb.checked?clientAccessTargetId:null});if(!channelResult.ok){btn.disabled=false;msg.textContent="Saved, but a campaign assignment failed to update: "+(channelResult.error||"unknown error");return}}btn.disabled=false;logActivity("client.update_access","client",clientAccessTargetId,`Updated contact info, campaign access (${form_ids.length} campaign${form_ids.length===1?"":"s"}), Campaign Guide access (${guides.length}), Reports tab (${reportsEnabled?"enabled":"disabled"}) and portal access (${Object.keys(portalSections).length} tab${Object.keys(portalSections).length===1?"":"s"} restricted)`);$("clientAccessModalOverlay").classList.remove("active");loadUmClientsList();const changed=contactResult.changed||{};const changedLabels=[];if(changed.email)changedLabels.push("email");if(changed.phone)changedLabels.push("phone number");if(changed.companyName)changedLabels.push("company name");let contactMsg=changedLabels.length?`Updated ${changedLabels.join(", ")}.`:"";if(contactResult.stripeWarning)contactMsg=contactMsg?`${contactMsg} ${contactResult.stripeWarning}`:contactResult.stripeWarning;else if(contactResult.stripeSynced)contactMsg=contactMsg?`${contactMsg} Synced to Stripe.`:"Synced to Stripe.";if(contactMsg)alert(contactMsg)});$("umClientsTbody").addEventListener("click",async e=>{const accessBtn=e.target.closest(".client-access-btn");if(accessBtn){openClientAccessModal(accessBtn.dataset.clientId,accessBtn.dataset.clientName);return}const resetBtn=e.target.closest(".client-reset-btn");if(resetBtn){const newPassword=generatePassword();if(!confirm(`Reset the password for ${resetBtn.dataset.clientEmail}? They'll be required to set a new one on next login.`))return;resetBtn.disabled=true;const result=await invokeManageClient({action:"set_password",clientId:resetBtn.dataset.clientId,password:newPassword});resetBtn.disabled=false;if(!result.ok){alert(result.error||"Failed to reset password.");return}logActivity("client.reset_password","client",resetBtn.dataset.clientId,`Reset password for ${resetBtn.dataset.clientEmail}`);let smsNote="";if(resetBtn.dataset.clientPhone&&confirm("Also text this new password to the client via SMS?")){const smsResult=await callSendClientSms(resetBtn.dataset.clientId,"account_password",newPassword);smsNote=smsResult.ok?"\n\n(Also sent via SMS.)":`

(SMS could not be sent: ${smsResult.error||"unknown error"} — share the password below directly.)`}alert(`New temporary password: ${newPassword}

Share this with the client — it won't be shown again.${smsNote}`);loadUmClientsList();return}const toggleBtn=e.target.closest(".client-toggle-btn");if(toggleBtn){const isActive=toggleBtn.dataset.active==="true";const nextActive=!isActive;if(!confirm(nextActive?"Reactivate this client account?":"Deactivate this client account? They will no longer be able to sign in."))return;toggleBtn.disabled=true;const result=await invokeManageClient({action:"set_active",clientId:toggleBtn.dataset.clientId,isActive:nextActive});toggleBtn.disabled=false;if(!result.ok){alert(result.error||"Failed to update status.");return}logActivity(nextActive?"client.reactivate":"client.deactivate","client",toggleBtn.dataset.clientId,nextActive?"Reactivated client account":"Deactivated client account");loadUmClientsList();return}const impersonateBtn=e.target.closest(".client-impersonate-btn");if(impersonateBtn){impersonateBtn.disabled=true;const origText=impersonateBtn.textContent;impersonateBtn.textContent="Opening…";const result=await invokeManageClient({action:"impersonate",clientId:impersonateBtn.dataset.clientId});impersonateBtn.disabled=false;impersonateBtn.textContent=origText;if(!result.ok||!result.tokenHash){alert(result.error||"Failed to open the client portal as this client.");return}logActivity("client.impersonate","client",impersonateBtn.dataset.clientId,`Logged in as client "${impersonateBtn.dataset.clientName}" to view their portal`);window.open(`${CLIENT_PORTAL_URL.replace(/\/$/,"")}?staff_token=${encodeURIComponent(result.tokenHash)}`,"_blank","noopener");return}const deleteBtn=e.target.closest(".client-delete-btn");if(deleteBtn){const email=deleteBtn.dataset.clientEmail;if(!confirm(`Permanently delete the client account for ${email}?

This can't be undone — their login, campaign access, and portal chat history are all removed. If you just need to revoke access, use Deactivate instead.`))return;deleteBtn.disabled=true;const result=await invokeManageClient({action:"delete_client",clientId:deleteBtn.dataset.clientId});deleteBtn.disabled=false;if(!result.ok){alert(result.error||"Failed to delete client.");return}logActivity("client.delete","client",deleteBtn.dataset.clientId,`Permanently deleted client account for ${email}`);loadUmClientsList()}});$("acctSaveBtn").addEventListener("click",async()=>{const msg=$("acctMsg");msg.textContent="";msg.className="msg";const full_name=$("acctFullName").value.trim();const role=$("acctRole").value;const email=$("acctEmailInput").value.trim();if(!email){msg.textContent="Email is required.";msg.className="msg error";return}const result=await callManageUser("update_profile",{full_name,role,email});if(!result.ok){msg.textContent=result.error||"Failed to save.";msg.className="msg error";return}const department=$("acctDepartment").value;const title=$("acctTitle").value.trim();const grade=$("acctGrade").value;const org=await saveStaffOrgInfo(currentDetailAgent.id,department,title,grade);if(!org.ok){msg.textContent=`Name, user type and email were saved, but the department/title was not: ${org.error}`;msg.className="msg error"}else{msg.textContent="Saved.";msg.className="msg ok";currentDetailAgent.department=department||null;currentDetailAgent.title=title||null;currentDetailAgent.grade=grade||null;detailSetTitleDept(currentDetailAgent)}const americanName=$("acctAmericanName").value.trim();if(!$("acctAmericanName").disabled&&americanName!==(currentDetailAgent.american_name||"")){const{error:anErr}=await sb.rpc("set_staff_american_name",{target:currentDetailAgent.id,p_name:americanName||null});if(anErr){msg.textContent=`Everything else was saved, but the American name was not: ${anErr.message}`;msg.className="msg error"}else{currentDetailAgent.american_name=americanName||null;logActivity("user.american_name","agent",currentDetailAgent.id,americanName?`American name set to "${americanName}"`:"American name cleared")}}const wasRole=currentDetailAgent.role;currentDetailAgent.full_name=full_name;currentDetailAgent.role=role;currentDetailAgent.email=email;$("detailName").textContent=full_name||currentDetailAgent.email;logActivity("user.update","agent",currentDetailAgent.id,`Updated ${email} — name: "${full_name}", role: ${role}, ${[title,department,grade].filter(Boolean).join(" · ")||"uncategorised"}`);if(role!==wasRole&&PROTECTED_ROLES.has(role)){$("backLink").click()}loadTeam();if(typeof umActiveSection!=="undefined"&&umActiveSection==="users")loadUmUsersList()});$("acctSetPasswordBtn").addEventListener("click",async()=>{const msg=$("acctPasswordMsg");msg.textContent="";msg.className="msg";const password=$("acctNewPassword").value;if(!password||password.length<8){msg.textContent="Password must be at least 8 characters.";msg.className="msg error";return}const result=await callManageUser("set_password",{password});if(!result.ok){msg.textContent=result.error||"Failed to set password.";msg.className="msg error";return}$("acctNewPassword").value="";msg.textContent="Password updated.";msg.className="msg ok";logActivity("user.set_password","agent",currentDetailAgent.id,`Set a new password for ${currentDetailAgent.email}`)});function renderAcctStatus(agent){const isActive=agent.is_active!==false;const badge=$("acctStatusBadge");badge.textContent=isActive?"Active":"Deactivated";badge.style.color=isActive?"var(--good)":"var(--danger)";$("acctToggleActiveBtn").textContent=isActive?"Deactivate…":"Reactivate";$("acctToggleActiveBtn").className=isActive?"btn-secondary":"btn-primary"}$("acctToggleActiveBtn").addEventListener("click",async()=>{const agent=currentDetailAgent;const isActive=agent.is_active!==false;const msg=$("acctStatusMsg");msg.textContent="";msg.className="msg";if(isActive){const ok=confirm(`Deactivate ${agent.full_name||agent.email}? They won't be able to sign in to the dashboard or extension until reactivated. Their tracked time, screenshots, and leads are all kept.`);if(!ok)return}const btn=$("acctToggleActiveBtn");btn.disabled=true;const result=await callManageUser("set_active",{isActive:!isActive});btn.disabled=false;if(!result.ok){msg.textContent=result.error||"Failed to update account status.";msg.className="msg error";return}currentDetailAgent.is_active=!isActive;renderAcctStatus(currentDetailAgent);msg.textContent=!isActive?"Account deactivated.":"Account reactivated.";msg.className="msg ok";logActivity(!isActive?"user.deactivate":"user.reactivate","agent",agent.id,`${!isActive?"Deactivated":"Reactivated"} ${agent.email}`);loadTeam();if(umActiveSection==="users")loadUmUsersList()});$("acctDeleteBtn").addEventListener("click",()=>{$("acctDeleteName").textContent=currentDetailAgent.full_name||currentDetailAgent.email;$("acctDeleteEmailHint").textContent=currentDetailAgent.email||"";$("acctDeleteStatusSelect").value="";$("acctDeleteReasonInput").value="";$("acctDeleteConfirmInput").value="";$("acctDeleteMsg").textContent="";$("acctDeleteConfirmBtn").disabled=true;$("acctDeleteModalOverlay").classList.add("active")});$("acctDeleteCancelBtn").addEventListener("click",()=>$("acctDeleteModalOverlay").classList.remove("active"));$("acctDeleteModalOverlay").addEventListener("click",e=>{if(e.target.id==="acctDeleteModalOverlay")$("acctDeleteModalOverlay").classList.remove("active")});function updateAcctDeleteConfirmEnabled(){const emailMatch=$("acctDeleteConfirmInput").value.trim().toLowerCase()===(currentDetailAgent.email||"").toLowerCase();const hasStatus=!!$("acctDeleteStatusSelect").value;const hasReason=!!$("acctDeleteReasonInput").value.trim();$("acctDeleteConfirmBtn").disabled=!(emailMatch&&hasStatus&&hasReason)}$("acctDeleteConfirmInput").addEventListener("input",updateAcctDeleteConfirmEnabled);$("acctDeleteStatusSelect").addEventListener("change",updateAcctDeleteConfirmEnabled);$("acctDeleteReasonInput").addEventListener("input",updateAcctDeleteConfirmEnabled);$("acctDeleteConfirmBtn").addEventListener("click",async()=>{const msg=$("acctDeleteMsg");const btn=$("acctDeleteConfirmBtn");btn.disabled=true;const agent=currentDetailAgent;const terminationStatus=$("acctDeleteStatusSelect").value;const terminationReason=$("acctDeleteReasonInput").value.trim();const result=await callManageUser("delete_user",{terminationStatus,terminationReason});if(!result.ok){msg.textContent=result.error||"Failed to delete user.";btn.disabled=false;return}$("acctDeleteModalOverlay").classList.remove("active");const statusLabel=terminationStatus==="found_other_offer"?"Found another offer":"Terminated";logActivity("user.delete","agent",agent.id,`Deleted user ${agent.email} — ${statusLabel}: ${terminationReason}`);$("backLink").click();loadTeam()});function populateTimezoneSelect(){const sel=$("timezoneInput");if(sel.dataset.populated)return;let zones;try{zones=Intl.supportedValuesOf("timeZone")}catch(e){zones=["UTC","Africa/Cairo","Europe/London","Europe/Berlin","America/New_York","America/Chicago","America/Los_Angeles","Asia/Dubai","Asia/Karachi","Asia/Kolkata","Asia/Dhaka","Asia/Singapore","Asia/Tokyo","Australia/Sydney"]}sel.innerHTML=zones.map(z=>`<option value="${z}">${z}</option>`).join("");sel.dataset.populated="1"}function updateLeadBonusRateLabel(){$("leadBonusRateLabel").textContent=$("leadBonusTypeInput").value==="bulk"?"Bonus rate ($ flat, once)":"Bonus rate ($ per lead)"}$("leadBonusTypeInput").addEventListener("change",updateLeadBonusRateLabel);function addExtraTierRow(tier){tier=tier||{extra_leads:"",bonus:"",currency:"USD"};const row=document.createElement("div");row.className="extra-tier-row";row.style.cssText="display:flex; gap:8px; align-items:center;";row.innerHTML=`
    <input type="number" class="extra-tier-leads edit-only comp-edit-only" min="1" step="1" placeholder="Extra leads (e.g. 10)" value="${tier.extra_leads!=null?tier.extra_leads:""}" style="flex:1;">
    <input type="number" class="extra-tier-bonus edit-only comp-edit-only" min="0" step="0.01" placeholder="Bonus (e.g. 50)" value="${tier.bonus!=null?tier.bonus:""}" style="flex:1;">
    <select class="extra-tier-currency edit-only comp-edit-only" style="width:90px; flex:0 0 90px;">
      <option value="USD" ${tier.currency==="EGP"?"":"selected"}>USD</option>
      <option value="EGP" ${tier.currency==="EGP"?"selected":""}>EGP</option>
    </select>
    <button type="button" class="btn-secondary extra-tier-remove edit-only comp-edit-only" style="width:auto; padding:8px 12px; color:var(--danger);">✕</button>
  `;row.querySelector(".extra-tier-remove").addEventListener("click",()=>row.remove());$("extraTiersList").appendChild(row);applyTimeManagementSectionEditGating("staff");applyCompensationEditGating()}$("addExtraTierBtn").addEventListener("click",()=>addExtraTierRow());function readExtraTiers(){return Array.from(document.querySelectorAll("#extraTiersList .extra-tier-row")).map(row=>({extra_leads:parseInt(row.querySelector(".extra-tier-leads").value,10)||0,bonus:parseFloat(row.querySelector(".extra-tier-bonus").value)||0,currency:row.querySelector(".extra-tier-currency").value||"USD"})).filter(t=>t.extra_leads>0)}function addSalaryTierRow(tier){tier=tier||{min_leads:"",amount:"",currency:"USD"};const row=document.createElement("div");row.className="salary-tier-row";row.style.cssText="display:flex; gap:8px; align-items:center;";row.innerHTML=`
    <input type="number" class="salary-tier-min-leads edit-only comp-edit-only" min="0" step="1" placeholder="Min accepted leads (e.g. 0, 40, 50, 60)" value="${tier.min_leads!=null?tier.min_leads:""}" style="flex:1;">
    <input type="number" class="salary-tier-amount edit-only comp-edit-only" min="0" step="0.01" placeholder="Monthly salary (e.g. 300)" value="${tier.amount!=null?tier.amount:""}" style="flex:1;">
    <select class="salary-tier-currency edit-only comp-edit-only" style="width:90px; flex:0 0 90px;">
      <option value="USD" ${tier.currency==="EGP"?"":"selected"}>USD</option>
      <option value="EGP" ${tier.currency==="EGP"?"selected":""}>EGP</option>
    </select>
    <button type="button" class="btn-secondary salary-tier-remove edit-only comp-edit-only" style="width:auto; padding:8px 12px; color:var(--danger);">✕</button>
  `;row.querySelector(".salary-tier-remove").addEventListener("click",()=>row.remove());$("salaryTiersList").appendChild(row);applyTimeManagementSectionEditGating("staff");applyCompensationEditGating()}$("addSalaryTierBtn").addEventListener("click",()=>addSalaryTierRow());function readSalaryTiers(){return Array.from(document.querySelectorAll("#salaryTiersList .salary-tier-row")).filter(row=>row.querySelector(".salary-tier-min-leads").value.trim()!=="").map(row=>({min_leads:parseInt(row.querySelector(".salary-tier-min-leads").value,10)||0,amount:parseFloat(row.querySelector(".salary-tier-amount").value)||0,currency:row.querySelector(".salary-tier-currency").value||"USD"}))}function canEditCompensationFields(){if(myPermissions.isProtected)return true;return!!myPermissions.canEditCompensation}function applyCompensationEditGating(){const panel=document.getElementById("tmSection-staff");if(!panel)return;if(canEditCompensationFields())return;panel.querySelectorAll(".comp-edit-only").forEach(el=>{el.disabled=true;el.style.opacity="0.4";el.style.cursor="not-allowed";el.title="Your role doesn't have permission to edit compensation."})}function applyPayModelVisibility(){const isTierSalary=$("payModelInput").value==="lead_tier_salary";$("hourlyRateFieldWrap").style.display=isTierSalary?"none":"";$("hourlyBonusExtrasWrap").style.display=isTierSalary?"none":"";$("leadTierSalaryWrap").style.display=isTierSalary?"":"none"}$("payModelInput").addEventListener("change",applyPayModelVisibility);async function loadSchedule(agentId){populateTimezoneSelect();const{data}=await sb.from("schedules").select("*").eq("user_id",agentId).maybeSingle();$("employmentType").value=data?data.employment_type:"full_time";$("dailyLimitInput").value=data?data.daily_limit_minutes:480;$("weeklyLimitInput").value=data?data.weekly_limit_minutes:2400;$("breakAllowanceInput").value=data?data.daily_break_allowance_minutes:60;$("idleLimitInput").value=data&&data.daily_idle_limit_minutes!=null?data.daily_idle_limit_minutes:"";$("tabSwitchGraceInput").value=data&&data.tab_switch_grace_seconds!=null?data.tab_switch_grace_seconds:"";$("hourlyRateInput").value=data&&data.hourly_rate!=null?data.hourly_rate:"";$("hourlyRateCurrencyInput").value=data&&data.hourly_rate_currency?data.hourly_rate_currency:"USD";$("targetLeadsInput").value=data&&data.target_leads!=null?data.target_leads:"";$("leadBonusTypeInput").value=data&&data.lead_bonus_type?data.lead_bonus_type:"per_lead";$("leadBonusRateInput").value=data&&data.lead_bonus_rate!=null?data.lead_bonus_rate:"";$("leadBonusCurrencyInput").value=data&&data.lead_bonus_currency?data.lead_bonus_currency:"USD";updateLeadBonusRateLabel();$("extraTiersList").innerHTML="";const tiers=data&&Array.isArray(data.extra_target_tiers)?data.extra_target_tiers:[];tiers.forEach(t=>addExtraTierRow(t));$("payModelInput").value=data&&data.pay_model?data.pay_model:"hourly_bonus";$("salaryTiersList").innerHTML="";const salaryTiers=data&&Array.isArray(data.salary_tiers)?data.salary_tiers:[];salaryTiers.forEach(t=>addSalaryTierRow(t));applyPayModelVisibility();const workingDays=data&&Array.isArray(data.working_days)&&data.working_days.length?data.working_days.map(Number):[0,1,2,3,4,5,6];document.querySelectorAll(".working-day-cb").forEach(cb=>{cb.checked=workingDays.includes(parseInt(cb.value,10))});$("enforceShiftInput").checked=data?!!data.enforce_shift:false;$("shiftStartInput").value=data&&data.shift_start?data.shift_start.slice(0,5):"";$("shiftEndInput").value=data&&data.shift_end?data.shift_end.slice(0,5):"";$("timezoneInput").value=data&&data.timezone?data.timezone:Intl.DateTimeFormat().resolvedOptions().timeZone;const cutSel=$("dayCutoverInput");if(cutSel){if(!cutSel.options.length){cutSel.innerHTML=Array.from({length:24},(_,h)=>{const label=`${String(h).padStart(2,"0")}:00 (${h===0?"12 AM":h<12?h+" AM":h===12?"12 PM":h-12+" PM"})`;return`<option value="${h}">${label}</option>`}).join("")}cutSel.value=data&&Number.isInteger(data.day_cutover_hour)?data.day_cutover_hour:6}$("scheduleMsg").textContent="";applyTimeManagementSectionEditGating("staff");applyCompensationEditGating()}$("saveScheduleBtn").addEventListener("click",async()=>{if(!currentDetailAgent)return;const enforceShift=$("enforceShiftInput").checked;const shiftStart=$("shiftStartInput").value;const shiftEnd=$("shiftEndInput").value;const msg=$("scheduleMsg");if(enforceShift&&(!shiftStart||!shiftEnd)){msg.textContent="Set both a shift start and end time, or turn off shift enforcement.";msg.className="msg error";return}if(enforceShift&&shiftStart===shiftEnd){msg.textContent="Shift start and end can’t be the same time — that would disable enforcement entirely.";msg.className="msg error";return}const idleLimitRaw=$("idleLimitInput").value.trim();const dailyIdleLimitMinutes=idleLimitRaw===""?null:parseInt(idleLimitRaw,10)||0;const tabSwitchGraceRaw=$("tabSwitchGraceInput").value.trim();const tabSwitchGraceSeconds=tabSwitchGraceRaw===""?null:parseInt(tabSwitchGraceRaw,10)||0;const hourlyRateRaw=$("hourlyRateInput").value.trim();const hourlyRate=hourlyRateRaw===""?null:parseFloat(hourlyRateRaw)||0;const targetLeadsRaw=$("targetLeadsInput").value.trim();const targetLeads=targetLeadsRaw===""?null:parseInt(targetLeadsRaw,10)||0;const leadBonusRateRaw=$("leadBonusRateInput").value.trim();const leadBonusRate=leadBonusRateRaw===""?null:parseFloat(leadBonusRateRaw)||0;const workingDays=Array.from(document.querySelectorAll(".working-day-cb")).filter(cb=>cb.checked).map(cb=>parseInt(cb.value,10)).sort((a,b)=>a-b);if(workingDays.length===0){msg.textContent="Select at least one working day, or check all 7 for no restriction.";msg.className="msg error";return}const payload={user_id:currentDetailAgent.id,employment_type:$("employmentType").value,daily_limit_minutes:parseInt($("dailyLimitInput").value,10)||0,weekly_limit_minutes:parseInt($("weeklyLimitInput").value,10)||0,daily_break_allowance_minutes:parseInt($("breakAllowanceInput").value,10)||0,daily_idle_limit_minutes:dailyIdleLimitMinutes,tab_switch_grace_seconds:tabSwitchGraceSeconds,hourly_rate:hourlyRate,hourly_rate_currency:$("hourlyRateCurrencyInput").value||"USD",target_leads:targetLeads,lead_bonus_type:$("leadBonusTypeInput").value||"per_lead",lead_bonus_rate:leadBonusRate,lead_bonus_currency:$("leadBonusCurrencyInput").value||"USD",extra_target_tiers:readExtraTiers(),pay_model:$("payModelInput").value||"hourly_bonus",salary_tiers:readSalaryTiers(),timezone:$("timezoneInput").value||"UTC",day_cutover_hour:function(){const v=parseInt($("dayCutoverInput")&&$("dayCutoverInput").value,10);return Number.isInteger(v)&&v>=0&&v<=23?v:6}(),shift_start:shiftStart||null,shift_end:shiftEnd||null,enforce_shift:enforceShift,working_days:workingDays,updated_at:new Date().toISOString()};const{error}=await sb.from("schedules").upsert(payload,{onConflict:"user_id"});if(error){msg.textContent=error.message;msg.className="msg error"}else{msg.textContent="Saved.";msg.className="msg";msg.style.color="var(--focus)";const agentLabel=currentDetailAgent.full_name||currentDetailAgent.email;const shiftNote=enforceShift?`, shift ${shiftStart}–${shiftEnd} ${payload.timezone}`:"";const idleNote=dailyIdleLimitMinutes!=null?`, idle limit ${dailyIdleLimitMinutes}m/day`:"";const graceNote=tabSwitchGraceSeconds!=null?`, tab-switch grace ${tabSwitchGraceSeconds}s`:"";const DAY_NAMES=["Sun","Mon","Tue","Wed","Thu","Fri","Sat"];const daysNote=workingDays.length<7?`, working days ${workingDays.map(d=>DAY_NAMES[d]).join("/")}`:"";logActivity("schedule.update","agent",currentDetailAgent.id,`Updated schedule for ${agentLabel} (${payload.daily_limit_minutes}m/day, ${payload.weekly_limit_minutes}m/week${idleNote}${graceNote}${shiftNote}${daysNote})`)}});async function renderDetail(agent){const{data:schedRow}=await sb.from("schedules").select("timezone, day_cutover_hour").eq("user_id",agent.id).maybeSingle();const effectiveSched=schedRow||{timezone:FALLBACK_TIMEZONE,day_cutover_hour:FALLBACK_CUTOVER_HOUR};const wdStartMs=workDayStartMsDash(effectiveSched);const wwStartMs=workWeekStartMsDash(effectiveSched);const wdStartISO=new Date(wdStartMs).toISOString();const wwStartISO=new Date(wwStartMs).toISOString();const{entries,idlePeriodsByEntry,breaksByEntry}=await fetchAgentEntries(agent.id,wdStartISO);const usedTodaySec=sumTrackedSecondsClipped(entries,idlePeriodsByEntry,breaksByEntry,wdStartMs);$("usedToday").textContent=fmtShort(usedTodaySec);const{data:breaksToday}=await sb.from("breaks").select("start_time, end_time").eq("user_id",agent.id).gte("start_time",wdStartISO);const tableBreakSec=(breaksToday||[]).reduce((t,b)=>{const bEnd=b.end_time?new Date(b.end_time).getTime():Date.now();return t+(bEnd-new Date(b.start_time).getTime())/1e3},0);const manualBreakSec=(entries||[]).filter(e=>e.is_manual).reduce((t,e)=>t+(e.break_seconds||0),0);const breakSec=tableBreakSec+manualBreakSec;$("breaksToday").textContent=fmtShort(breakSec);const leadsToday=await fetchAllRows((from,to)=>sb.from("leads").select(CREDITED_LEAD_COLS).eq("user_id",agent.id).in("status",CREDITED_LEAD_STATUSES).gte("submitted_at",wdStartISO).order("id",{ascending:true}).range(from,to));$("leadsToday").textContent=creditedLeadCount(leadsToday);const{entries:weekEntries,idlePeriodsByEntry:weekIdlePeriodsByEntry,breaksByEntry:weekBreaksByEntry}=await fetchAgentEntries(agent.id,wwStartISO);const weekUsedSec=sumTrackedSecondsClipped(weekEntries,weekIdlePeriodsByEntry,weekBreaksByEntry,wwStartMs);$("weekUsedDetail").textContent=fmtShort(weekUsedSec);const leadsMonth=await fetchAllRows((from,to)=>sb.from("leads").select(CREDITED_LEAD_COLS).eq("user_id",agent.id).in("status",CREDITED_LEAD_STATUSES).gte("submitted_at",startOfMonthISO()).order("id",{ascending:true}).range(from,to));$("leadsMonthDetail").textContent=creditedLeadCount(leadsMonth);const dayMs=24*3600*1e3;const track=$("detailTimeline");track.innerHTML="";const tasks=[...new Set(entries.map(e=>e.task_id))].map(id=>({id}));entries.forEach(e=>{const end=e.end_time?new Date(e.end_time).getTime():Date.now();const startPct=Math.max(0,(new Date(e.start_time).getTime()-wdStartMs)/dayMs*100);const endPct=Math.min(100,(end-wdStartMs)/dayMs*100);const div=document.createElement("div");div.className="segment";div.style.left=startPct+"%";div.style.width=Math.max(.15,endPct-startPct)+"%";div.style.background=colorFor(e.task_id,tasks);track.appendChild(div)});const nowISO=new Date().toISOString();const{data:visits}=await sb.from("url_visits").select("domain, start_time, end_time").eq("user_id",agent.id).gte("start_time",wdStartISO).lte("start_time",nowISO);renderSites(visits||[]);const{data:shots}=await sb.from("screenshots").select("id, storage_path, taken_at, activity_pct_at_capture").eq("user_id",agent.id).gte("taken_at",wdStartISO).lte("taken_at",nowISO).order("taken_at",{ascending:false});const grid=$("shotsGrid");grid.innerHTML="";if(!shots||shots.length===0){grid.innerHTML='<div class="empty">No screenshots yet today.</div>';return}const resolvedShots=[];for(const shot of shots){const{data:signed}=await sb.storage.from("screenshots").createSignedUrl(shot.storage_path,3600);resolvedShots.push({url:signed?signed.signedUrl:"",taken_at:shot.taken_at,activity_pct_at_capture:shot.activity_pct_at_capture})}resolvedShots.forEach((shot,idx)=>{const div=document.createElement("div");div.className="shot-card";div.innerHTML=`
      <img src="${shot.url}" loading="lazy">
      <div class="shot-meta">${new Date(shot.taken_at).toLocaleTimeString([],{hour:"2-digit",minute:"2-digit"})} · ${shot.activity_pct_at_capture??"—"}%</div>
    `;div.addEventListener("click",()=>openLightbox(resolvedShots,idx));grid.appendChild(div)})}const FINANCE_SECTIONS=[{key:"campaign_subscriptions",label:"Stripe Subscription",editable:true},{key:"stripe_pending_payment",label:"Stripe Pending Payment",editable:true},{key:"stripe_discount_codes",label:"Stripe Discount Codes",editable:true},{key:"stripe_payments",label:"Stripe Payments",editable:true},{key:"stripe_pricing",label:"Stripe Pricing",editable:true},{key:"skiptrace_credits",label:"Skiptrace Credits",editable:true}];function financeSectionDefault(key){return key!=="stripe_discount_codes"&&key!=="stripe_payments"&&key!=="stripe_pricing"&&key!=="skiptrace_credits"}function financeEditSectionDefault(key){return false}function canViewFinanceSection(key){if(key==="ispeedtolead_postback"||key==="commission_liability")return myIsOwner;if(myPermissions.isProtected)return true;const s=(myPermissions.financeSections||{})[key];return s&&typeof s.view==="boolean"?s.view:financeSectionDefault(key)}function canEditFinanceSection(key){if(key==="ispeedtolead_postback"||key==="commission_liability")return myIsOwner;if(myPermissions.isProtected)return true;const s=(myPermissions.financeSections||{})[key];return s&&typeof s.edit==="boolean"?s.edit:financeEditSectionDefault(key)}function financeSubtabBtns(){return Array.from(document.querySelectorAll(".finance-subtab-btn")).filter(b=>canViewFinanceSection(b.dataset.financeSection))}let financeActiveSection=null;function initFinanceTab(){document.querySelectorAll(".finance-subtab-btn").forEach(b=>{b.style.display=canViewFinanceSection(b.dataset.financeSection)?"":"none"});const usable=financeSubtabBtns();const stillValid=usable.find(b=>b.dataset.financeSection===financeActiveSection);const target=stillValid?financeActiveSection:usable[0]?usable[0].dataset.financeSection:null;if(target)activateFinanceSection(target)}function applyFinanceSectionEditGating(key){const panel=document.getElementById("financeSection-"+key);if(!panel)return;const editable=canEditFinanceSection(key);panel.querySelectorAll(".edit-only").forEach(el=>{el.disabled=!editable;el.style.opacity=editable?"":"0.4";el.style.cursor=editable?"":"not-allowed";el.title=editable?"":"Your role has view-only access to this subtab"})}function activateFinanceSection(key){financeActiveSection=key;document.querySelectorAll(".finance-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.financeSection===key));document.querySelectorAll(".finance-section").forEach(s=>{s.style.display=s.id==="financeSection-"+key?"block":"none"});if(key==="campaign_subscriptions")loadCampaignSubscriptionsStaffList();else if(key==="stripe_pending_payment")loadPendingCampaignPayments();else if(key==="stripe_payments"){loadOneOffChargeClientOptions();loadOneOffChargesList()}else if(key==="stripe_discount_codes")loadPromoCodesList();else if(key==="stripe_pricing")loadStripePricing();else if(key==="ispeedtolead_postback")loadFinanceIspeedtoleadPostbacks();else if(key==="skiptrace_credits"){loadSkiptraceCreditsClientOptions();loadSkiptraceCreditsHistory()}else if(key==="commission_liability")loadFinanceCommissionLiability();applyFinanceSectionEditGating(key)}document.querySelectorAll(".finance-subtab-btn").forEach(btn=>{btn.addEventListener("click",()=>{if(canViewFinanceSection(btn.dataset.financeSection))activateFinanceSection(btn.dataset.financeSection)})});function defaultFinPostbackRange(){return{from:todayDateInputValue(),to:endOfLoginMonthDateInputValue()}}function finPostbackRangeISO(){const fromVal=$("finPostbackFromInput").value;const toVal=$("finPostbackToInput").value;if(!fromVal&&!toVal)return{start:null,end:null};const[fy,fm,fd]=(fromVal||toVal).split("-").map(Number);const[ty,tm,td]=(toVal||fromVal).split("-").map(Number);const{startMs}=dayBoundsMsInZone(globalDisplayTzMode,null,fy,fm,fd);const{endMs}=dayBoundsMsInZone(globalDisplayTzMode,null,ty,tm,td);return{start:new Date(startMs).toISOString(),end:new Date(endMs).toISOString()}}async function loadFinanceIspeedtoleadPostbacks(){const body=$("finPostbackBody");body.innerHTML='<tr><td colspan="10" class="empty">Loading…</td></tr>';if(!$("finPostbackFromInput").value||!$("finPostbackToInput").value){const{from,to}=defaultFinPostbackRange();$("finPostbackFromInput").value=from;$("finPostbackToInput").value=to}const{data:agents}=await sb.from("profiles").select("id, full_name, email, ghl_user_id, readymode_username").order("full_name");const agentSel=$("finPostbackAgentFilter");const prevAgent=agentSel.value;agentSel.innerHTML='<option value="">All agents</option>'+(agents||[]).filter(a=>a.ghl_user_id||a.readymode_username).map(a=>`<option value="${a.id}">${escapeHtml(a.full_name||a.email||"—")}</option>`).join("");agentSel.value=prevAgent;const agentId=agentSel.value;const{start:rangeStart,end:rangeEnd}=finPostbackRangeISO();const nameById={};(agents||[]).forEach(a=>{nameById[a.id]=a.full_name||a.email});const{data:scheduleRows}=await sb.from("schedules").select("user_id, timezone");const agentTz={};(scheduleRows||[]).forEach(s=>{if(s.timezone)agentTz[s.user_id]=s.timezone});const resolveTz=userId=>globalDisplayTzMode==="agent"?agentTz[userId]:globalDisplayTzMode;const fmt=(iso,userId)=>{if(!iso)return"—";const tz=resolveTz(userId);return new Date(iso).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...tz?{timeZone:tz}:{}})};const setStat=(id,n,isError)=>{$(id).textContent=isError?"—":String(n??0)};let statsQuery=sb.from("ispeedtolead_postbacks").select("mapped_status, status_raw, amount, applied, matched_lead_id, needs_reconciliation");if(rangeStart)statsQuery=statsQuery.gte("received_at",rangeStart);if(rangeEnd)statsQuery=statsQuery.lte("received_at",rangeEnd);const{data:statsRows,error:statsError}=await statsQuery;if(statsError){["finPostbackStatTotal","finPostbackStatApplied","finPostbackStatAccepted","finPostbackStatRejected","finPostbackStatUnmatched","finPostbackStatNeedsReconciliation","finPostbackStatPurchasedAmount","finPostbackStatAcceptedAmount","finPostbackStatRefundedAmount"].forEach(id=>setStat(id,0,true))}else{let statsFiltered=statsRows||[];if(agentId){const matchedLeadIds2=[...new Set(statsFiltered.map(r=>r.matched_lead_id).filter(Boolean))];const leadUserById2={};if(matchedLeadIds2.length){const{data:matchedLeads}=await sb.from("leads").select("id, user_id").in("id",matchedLeadIds2);(matchedLeads||[]).forEach(l=>{leadUserById2[l.id]=l.user_id})}statsFiltered=statsFiltered.filter(r=>leadUserById2[r.matched_lead_id]===agentId)}setStat("finPostbackStatTotal",statsFiltered.length);setStat("finPostbackStatApplied",statsFiltered.filter(r=>r.applied).length);setStat("finPostbackStatAccepted",statsFiltered.filter(r=>r.mapped_status==="accepted").length);setStat("finPostbackStatRejected",statsFiltered.filter(r=>r.mapped_status==="rejected").length);setStat("finPostbackStatUnmatched",statsFiltered.filter(r=>!r.applied).length);setStat("finPostbackStatNeedsReconciliation",statsFiltered.filter(r=>r.needs_reconciliation).length);const isRefundRow=r=>r.mapped_status==="rejected"&&String(r.status_raw||"").toLowerCase().includes("refund");const sumAmount=rows=>rows.reduce((s,r)=>s+(typeof r.amount==="number"?r.amount:0),0);const acceptedRows=statsFiltered.filter(r=>r.mapped_status==="accepted");const refundedRows=statsFiltered.filter(isRefundRow);const acceptedAmount=sumAmount(acceptedRows);const refundedAmount=sumAmount(refundedRows);const fmtMoney=n=>`$${n.toLocaleString(void 0,{minimumFractionDigits:2,maximumFractionDigits:2})}`;setStat("finPostbackStatPurchasedAmount",fmtMoney(acceptedAmount+refundedAmount));setStat("finPostbackStatAcceptedAmount",fmtMoney(acceptedAmount));setStat("finPostbackStatRefundedAmount",fmtMoney(refundedAmount))}let q=sb.from("ispeedtolead_postbacks").select("id, phone_raw, status_raw, mapped_status, amount, reason, applied, received_at, matched_lead_id, matched_via, needs_reconciliation").order("received_at",{ascending:false}).limit(300);if(rangeStart)q=q.gte("received_at",rangeStart);if(rangeEnd)q=q.lte("received_at",rangeEnd);const{data,error}=await q;if(error){body.innerHTML=`<tr><td colspan="10" class="msg error">${escapeHtml(error.message)}</td></tr>`;return}const rowsData=data||[];const matchedLeadIds=[...new Set(rowsData.map(r=>r.matched_lead_id).filter(Boolean))];const leadUserById={};if(matchedLeadIds.length){const{data:matchedLeads}=await sb.from("leads").select("id, user_id").in("id",matchedLeadIds);(matchedLeads||[]).forEach(l=>{leadUserById[l.id]=l.user_id})}let filtered=rowsData;if(agentId)filtered=filtered.filter(r=>leadUserById[r.matched_lead_id]===agentId);const searchTerm=($("finPostbackSearchInput")?$("finPostbackSearchInput").value:"").trim().toLowerCase();if(searchTerm){filtered=filtered.filter(r=>{const who=leadUserById[r.matched_lead_id]?nameById[leadUserById[r.matched_lead_id]]||"":"";const haystack=[r.received_at,r.phone_raw,who,r.status_raw,r.mapped_status,r.amount,r.reason,r.matched_via,r.applied?"yes":"no"].filter(v=>v!==null&&v!==void 0).join(" ").toLowerCase();return haystack.includes(searchTerm)})}if(!filtered.length){body.innerHTML='<tr><td colspan="10" class="empty">No iSpeedToLead postbacks in this range.</td></tr>';return}body.innerHTML="";filtered.forEach(r=>{const rowUserId=leadUserById[r.matched_lead_id]||null;const who=rowUserId?escapeHtml(nameById[rowUserId]||"Agent"):`<span style="color:var(--text-dim);">unmatched</span>`;const mappedLabel=r.mapped_status?`<span class="${r.mapped_status==="accepted"?"pct-focus":"text-idle"}">${escapeHtml(r.mapped_status)}</span>`:`<span style="color:var(--text-dim);">unrecognized</span>`;const viaLabel=r.matched_via==="provider_lead_id"?'<span style="color:var(--focus);">provider_lead_id</span>':r.matched_via==="phone"?'<span style="color:var(--text-2);">phone</span>':r.matched_via==="phone_ineligible"?'<span style="color:var(--text-dim);" title="Matched by phone to a lead that was already accepted/rejected — most likely iSpeedToLead resold the same lead a second time. No status change was applied.">phone (already decided)</span>':'<span style="color:var(--text-dim);">—</span>';const tr=document.createElement("tr");tr.innerHTML=`
      <td class="mono" style="color:var(--text-2);">${fmt(r.received_at,rowUserId)}</td>
      <td class="mono">${escapeHtml(r.phone_raw||"—")}</td>
      <td>${who}</td>
      <td class="mono">${escapeHtml(r.status_raw||"—")}</td>
      <td>${mappedLabel}</td>
      <td class="mono">${r.amount!=null?r.amount:"—"}</td>
      <td style="max-width:220px; white-space:normal;">${r.reason?escapeHtml(r.reason):'<span style="color:var(--text-dim);">—</span>'}</td>
      <td class="mono">${viaLabel}</td>
      <td>${r.applied?'<span style="color:var(--focus);">yes</span>':'<span style="color:var(--text-dim);">no</span>'}</td>
      <td>${r.needs_reconciliation?`<button type="button" class="btn-secondary fin-postback-reconcile-btn" data-id="${escapeHtml(r.id)}" style="width:auto; padding:4px 10px; font-size:10px; color:var(--danger);">⚠ Needs reconciliation — Mark done</button>`:'<span style="color:var(--text-dim);">—</span>'}</td>
    `;body.appendChild(tr)});body.querySelectorAll(".fin-postback-reconcile-btn").forEach(btn=>{btn.addEventListener("click",async()=>{if(!confirm("Mark this postback as reconciled? Only do this once the $ mismatch with Leads has actually been resolved/verified."))return;const{error:reconcileErr}=await sb.from("ispeedtolead_postbacks").update({needs_reconciliation:false}).eq("id",btn.dataset.id);if(reconcileErr){alert(reconcileErr.message);return}logActivity("finance.postback_reconciled","ispeedtolead_postback",btn.dataset.id,"Marked an iSpeedToLead postback as reconciled");loadFinanceIspeedtoleadPostbacks()})})}$("finPostbackFromInput").addEventListener("change",loadFinanceIspeedtoleadPostbacks);$("finPostbackToInput").addEventListener("change",loadFinanceIspeedtoleadPostbacks);$("finPostbackAgentFilter").addEventListener("change",loadFinanceIspeedtoleadPostbacks);$("finPostbackSearchInput").addEventListener("input",debounce(loadFinanceIspeedtoleadPostbacks,250));$("finPostbackRefreshBtn").addEventListener("click",loadFinanceIspeedtoleadPostbacks);function defaultFinCommissionRange(){const[y,m]=todayDateInputValue().split("-").map(Number);return{from:`${String(y).padStart(4,"0")}-${String(m).padStart(2,"0")}-01`,to:todayDateInputValue()}}async function loadFinanceCommissionLiability(){const body=$("finCommissionBody");body.innerHTML='<tr><td colspan="7" class="empty">Loading…</td></tr>';if(!$("finCommissionFromInput").value||!$("finCommissionToInput").value){const{from,to}=defaultFinCommissionRange();$("finCommissionFromInput").value=from;$("finCommissionToInput").value=to}const[fy,fm,fd]=$("finCommissionFromInput").value.split("-").map(Number);const[ty,tm,td]=$("finCommissionToInput").value.split("-").map(Number);loadFinanceRecruiterLiability($("finCommissionFromInput").value,$("finCommissionToInput").value);const{startMs:rangeStartMs}=dayBoundsMsInZone(globalDisplayTzMode,null,fy,fm,fd);const{endMs:rangeEndMs}=dayBoundsMsInZone(globalDisplayTzMode,null,ty,tm,td);const{data:agents}=await sb.from("profiles").select("id, full_name, email, role").order("full_name");const{data:scheduleRows}=await sb.from("schedules").select("user_id, hourly_rate, hourly_rate_currency, target_leads, lead_bonus_type, lead_bonus_rate, lead_bonus_currency, extra_target_tiers, pay_model, salary_tiers");const scheduleByUser={};(scheduleRows||[]).forEach(s=>{scheduleByUser[s.user_id]=s});const liabilityAgents=(agents||[]).filter(a=>!PROTECTED_ROLES.has(a.role)).map(a=>{const s=scheduleByUser[a.id]||{};return{id:a.id,name:a.full_name||a.email,hourlyRate:s.hourly_rate!=null?Number(s.hourly_rate):null,hourlyRateCurrency:s.hourly_rate_currency||"USD",targetLeads:s.target_leads!=null?Number(s.target_leads):null,leadBonusType:s.lead_bonus_type||"per_lead",leadBonusRate:s.lead_bonus_rate!=null?Number(s.lead_bonus_rate):null,leadBonusCurrency:s.lead_bonus_currency||"USD",extraTiers:Array.isArray(s.extra_target_tiers)?s.extra_target_tiers:[],payModel:s.pay_model||"hourly_bonus",salaryTiers:Array.isArray(s.salary_tiers)?s.salary_tiers:[]}});if(!liabilityAgents.length){body.innerHTML='<tr><td colspan="7" class="empty">No agents.</td></tr>';return}const rows=await Promise.all(liabilityAgents.map(async agent=>{const stats=await computeAgentStats(agent.id,rangeStartMs,rangeEndMs);const payCalc=computeSalesPay(agent,stats);return{agent,stats,payCalc}}));const fmtMoney=n=>n==null?"—":`$${n.toLocaleString(void 0,{minimumFractionDigits:2,maximumFractionDigits:2})}`;const totalLeads=rows.reduce((t,r)=>t+r.stats.leadsCount,0);const totalBonus=rows.reduce((t,r)=>t+r.payCalc.leadBonus+r.payCalc.extraBonus,0);const totalBasic=rows.reduce((t,r)=>t+(r.payCalc.basicPay||0),0);$("finCommissionStatLeads").textContent=String(totalLeads);$("finCommissionStatBonus").textContent=fmtMoney(totalBonus);$("finCommissionStatBasic").textContent=fmtMoney(totalBasic);$("finCommissionStatTotal").textContent=fmtMoney(totalBonus+totalBasic);const withLiability=rows.filter(r=>r.stats.leadsCount>0||(r.payCalc.pay||0)>0);if(!withLiability.length){body.innerHTML='<tr><td colspan="7" class="empty">No pay or accepted leads for any agent in this range.</td></tr>';return}body.innerHTML="";withLiability.sort((a,b)=>(b.payCalc.pay||0)-(a.payCalc.pay||0)).forEach(({agent,stats,payCalc})=>{const tr=document.createElement("tr");const totalOwed=(payCalc.basicPay||0)+payCalc.leadBonus+payCalc.extraBonus;tr.innerHTML=`
        <td>${escapeHtml(agent.name)}</td>
        <td class="mono">${stats.leadsCount}</td>
        <td class="mono">${agent.targetLeads!=null?agent.targetLeads:"—"}</td>
        <td class="mono">${fmtMoney(payCalc.leadBonus)}</td>
        <td class="mono">${fmtMoney(payCalc.extraBonus)}</td>
        <td class="mono">${fmtMoney(payCalc.basicPay)}</td>
        <td class="mono" style="font-weight:700;">${fmtMoney(totalOwed)}</td>
      `;body.appendChild(tr)})}$("finCommissionFromInput").addEventListener("change",loadFinanceCommissionLiability);$("finCommissionToInput").addEventListener("change",loadFinanceCommissionLiability);$("finCommissionRefreshBtn").addEventListener("click",loadFinanceCommissionLiability);async function loadFinanceRecruiterLiability(from,to){const body=$("finRecruiterBody");if(!body)return;body.innerHTML='<tr><td colspan="5" class="empty">Loading…</td></tr>';const{data,error}=await sb.rpc("hr_recruiter_commissions");if(error){body.innerHTML=`<tr><td colspan="5" class="empty">${escapeHtml(error.message)}</td></tr>`;$("finRecruiterTotal").textContent="—";return}const due=(data||[]).filter(r=>r.qualifies&&r.qualifies_on&&r.qualifies_on>=from&&r.qualifies_on<=to);const total=due.reduce((t,r)=>t+(Number(r.commission_amount)||0),0);$("finRecruiterTotal").textContent=`EGP ${total.toLocaleString()} owed to recruiters in this period`;body.innerHTML=due.length?due.map(r=>`<tr>
      <td>${escapeHtml(r.recruiter_name)}</td>
      <td>${escapeHtml(r.candidate_name)}</td>
      <td class="mono">${hrFmtDate(r.employment_start_date)}</td>
      <td class="mono">${hrFmtDate(r.qualifies_on)}</td>
      <td class="mono" style="font-weight:700;">EGP ${Number(r.commission_amount).toLocaleString()}</td>
    </tr>`).join(""):'<tr><td colspan="5" class="empty">No recruiter commission became due in this period.</td></tr>'}function dataSciSubtabBtns(){return Array.from(document.querySelectorAll(".datasci-subtab-btn")).filter(b=>canViewDataSciSection(b.dataset.datasciSection))}let dataSciActiveSection=null;function initDataSciTab(){document.querySelectorAll(".datasci-subtab-btn").forEach(b=>{b.style.display=canViewDataSciSection(b.dataset.datasciSection)?"":"none"});const usable=dataSciSubtabBtns();const stillValid=usable.find(b=>b.dataset.datasciSection===dataSciActiveSection);const target=stillValid?dataSciActiveSection:usable[0]?usable[0].dataset.datasciSection:null;if(target)activateDataSciSection(target)}function activateDataSciSection(key){dataSciActiveSection=key;document.querySelectorAll(".datasci-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.datasciSection===key));document.querySelectorAll(".datasci-section").forEach(s=>{s.style.display=s.id==="dataSciSection-"+key?"block":"none"});if(key==="list_orders"||key==="data_orders")loadOrderReadiness();if(key==="list_orders")loadCsubListOrders();else if(key==="list_builder")initListBuilderSection();else if(key==="overview")loadDataSciOverview();else if(key==="data_orders")loadDataOrders();else if(key==="skiptrace")loadCsubSkipTrace();else applyDataSciSectionEditGating(key);if(key==="skiptrace")startCsubSkiptracePolling();else stopCsubSkiptracePolling()}function applyDataSciSectionEditGating(key){const panel=document.getElementById("dataSciSection-"+key);if(!panel)return;const editable=canEditDataSciSection(key);panel.querySelectorAll(".edit-only").forEach(el=>{el.disabled=!editable;el.style.opacity=editable?"":"0.4";el.title=editable?"":"Your role has view-only access to this section"})}function refreshDataSciSectionByKey(key){if(key==="list_orders"||key==="data_orders")loadOrderReadiness(true);if(key==="list_orders")loadCsubListOrders();else if(key==="data_orders")loadDataOrders();else if(key==="skiptrace")loadCsubSkipTrace();else if(key==="list_builder")initListBuilderSection();else if(key==="overview")loadDataSciOverview()}async function loadDataSciOverview(){const btn=document.getElementById("dsOverviewRefreshBtn");if(btn){btn.disabled=true}const emptyEl=document.getElementById("dsOverviewEmpty");if(emptyEl)emptyEl.style.display="none";loadDataSciExtraStats();try{const data=await listBuilderCall({action:"overview_stats"});if(!data||data.ok===false)throw new Error(data&&data.error||"overview_stats failed");const fmt=n=>typeof n==="number"?n.toLocaleString():"—";const set=(id,val)=>{const el=document.getElementById(id);if(el)el.textContent=fmt(val)};set("dsOverviewTotal",data.total);set("dsOverviewCook",data.cookTotal);set("dsOverviewDupage",data.dupageTotal);set("dsOverviewAbsentee",data.absentee);set("dsOverviewCorporate",data.corporateOwner);set("dsOverviewInherited",data.inherited);set("dsOverviewProbate",data.probateEstate);set("dsOverviewLawsuit",data.buildingCodeLawsuit);set("dsOverviewVacant",data.vacantBuilding);set("dsOverviewPreforeclosure",data.preforeclosure);set("dsOverviewTired",data.tiredLandlord);set("dsOverviewLongTerm",data.longTermOwner);set("dsOverviewMfh24",data.mfh2to4);set("dsOverviewMfh5p",data.mfh5plus);set("dsOverviewExcluded",data.excluded);renderDsOverviewCountyBreakdown(data.byCounty);renderDsOverviewTrend(data.trend);const computedEl=document.getElementById("dsOverviewComputedAt");if(computedEl)computedEl.textContent=data.computedAt?`Computed ${new Date(data.computedAt).toLocaleString()}`:""}catch(err){console.error("loadDataSciOverview",err);if(emptyEl){emptyEl.style.display="";emptyEl.textContent="Couldn't load overview stats: "+(err.message||err)}}finally{if(btn){btn.disabled=false}}}document.getElementById("dsOverviewRefreshBtn")?.addEventListener("click",loadDataSciOverview);async function dataSciStatsCall(payload){const{data,error}=await sb.functions.invoke("staff-datascience-stats",{body:payload});if(error)return{ok:false,error:error.message||String(error)};return data}function dsFmtNum(n){return typeof n==="number"&&isFinite(n)?n.toLocaleString():"—"}function dsFmtAge(hours){if(typeof hours!=="number"||!isFinite(hours))return"—";if(hours<1)return Math.max(1,Math.round(hours*60))+"m";if(hours<48)return Math.round(hours)+"h";return Math.round(hours/24)+"d"}let dsOverlapSignals=[];async function loadDataSciExtraStats(){const setText=(id,val)=>{const el=document.getElementById(id);if(el)el.textContent=val};try{const data=await dataSciStatsCall({action:"stats"});if(!data||data.ok===false)throw new Error(data&&data.error||"stats failed");const fresh=data.freshness||{};const ageH=typeof fresh.dataAgeHours==="number"?fresh.dataAgeHours:null;setText("dsOpsFreshness",ageH==null?"—":dsFmtAge(ageH)+" old");setText("dsOpsFreshnessSub",fresh.newest_source_updated_at?"Newest county data "+new Date(fresh.newest_source_updated_at).toLocaleDateString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{}):"No sync recorded yet");const freshValEl=document.getElementById("dsOpsFreshness");if(freshValEl)freshValEl.style.color=ageH==null?"":ageH>=168?"var(--danger)":ageH>=48?"var(--away)":"var(--good)";const aging=data.orderAging||{};setText("dsOpsAging",dsFmtNum(aging.total));setText("dsOpsAgingSub",aging.total?`oldest ${dsFmtAge(aging.oldestHours)} · ${aging.over168h||0} over a week`:"nothing waiting");const agingValEl=document.getElementById("dsOpsAging");if(agingValEl)agingValEl.style.color=aging.over168h>0?"var(--danger)":aging.h72to168>0?"var(--away)":"";const agingCard=document.getElementById("dsAgingDetailCard");const agingBody=document.getElementById("dsAgingDetailBody");const agingRows=Array.isArray(aging.rows)?aging.rows.filter(r=>r.ageHours>=24):[];if(agingCard&&agingBody){agingCard.style.display=agingRows.length?"":"none";agingBody.innerHTML=agingRows.map(r=>{const colour=r.ageHours>=168?"var(--danger)":r.ageHours>=72?"var(--away)":"var(--text-2)";return`<tr>
          <td class="mono" style="color:${colour}; white-space:nowrap;">${dsFmtAge(r.ageHours)}</td>
          <td>${r.kind==="data_order"?"Data Order":"Buy-a-List"}</td>
          <td>${escapeHtml(r.status||"")}</td>
          <td style="color:var(--text-2);">${escapeHtml(r.label||"—")}</td>
          <td style="color:var(--text-dim); white-space:nowrap;">${new Date(r.createdAt).toLocaleDateString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{})}</td>
        </tr>`}).join("")}const st=data.skiptrace||{};setText("dsOpsHitRate",typeof st.hitRatePct==="number"?st.hitRatePct+"%":"—");setText("dsOpsHitRateSub",st.completedOrders?`${dsFmtNum(st.recordsOut)} matched of ${dsFmtNum(st.recordsIn)} · ${st.completedOrders} order${st.completedOrders===1?"":"s"}`:"no completed orders yet");const del=data.deliveries||{};setText("dsOpsDelivered",dsFmtNum(del.distinctProperties));setText("dsOpsDeliveredSub",del.distinctProperties?`${typeof del.pctOfInventory==="number"?del.pctOfInventory+"% of inventory · ":""}${dsFmtNum(del.remaining)} never sent`:"nothing delivered yet");renderDsCoverage(data.coverage,data.snapshotComputedAt);renderDsSignalOverlap(data.signalOverlap)}catch(err){console.error("loadDataSciExtraStats",err);const covBody=document.getElementById("dsCoverageBody");if(covBody)covBody.innerHTML=`<tr><td colspan="5" style="color:var(--danger);">Couldn't load coverage: ${escapeHtml(err.message||String(err))}</td></tr>`;const ovBody=document.getElementById("dsOverlapBody");if(ovBody)ovBody.innerHTML=`<tr><td style="color:var(--text-dim);">Couldn't load signal overlap.</td></tr>`}}function renderDsCoverage(coverage,snapshotComputedAt){const body=document.getElementById("dsCoverageBody");const stamp=document.getElementById("dsCoverageComputedAt");if(!body)return;if(stamp){stamp.textContent=snapshotComputedAt?` Measured ${new Date(snapshotComputedAt).toLocaleString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{})}.`:""}if(!coverage||!Array.isArray(coverage.fields)||!coverage.fields.length){body.innerHTML='<tr><td colspan="5" style="color:var(--text-dim);">Not measured yet — click Recompute to build the first snapshot.</td></tr>';return}body.innerHTML=coverage.fields.map(f=>{const pct=typeof f.pct==="number"?f.pct:null;const colour=pct==null?"var(--text-dim)":pct<50?"var(--danger)":pct<90?"var(--away)":"var(--good)";const barPct=pct==null?0:Math.max(0,Math.min(100,pct));return`<tr>
      <td>${escapeHtml(f.label)}</td>
      <td class="mono" style="white-space:nowrap;">${dsFmtNum(f.present)}</td>
      <td style="min-width:120px;">
        <div style="display:flex; align-items:center; gap:8px;">
          <div style="flex:1; height:6px; background:var(--inset); border-radius:3px; overflow:hidden; min-width:60px;">
            <div style="width:${barPct}%; height:100%; background:${colour};"></div>
          </div>
          <span class="mono" style="color:${colour}; font-size:11px; white-space:nowrap;">${pct==null?"—":pct+"%"}</span>
        </div>
      </td>
      <td class="mono" style="color:var(--text-dim); white-space:nowrap;">${dsFmtNum(f.missing)}</td>
      <td style="color:var(--text-2); font-size:11px;">${escapeHtml(f.affects)}</td>
    </tr>`}).join("")}let dsReadinessCache=null;async function loadOrderReadiness(force){if(dsReadinessCache&&!force){renderOrderReadiness();return}try{const data=await dataSciStatsCall({action:"order_readiness"});if(!data||data.ok===false)throw new Error(data&&data.error||"order_readiness failed");const clientIds=Array.from(new Set((data.orders||[]).filter(o=>o.kind==="list_order"&&o.label).map(o=>o.label)));if(clientIds.length){const{data:cl}=await sb.from("client_accounts").select("id, full_name, company_name, email").in("id",clientIds);const nameById={};(cl||[]).forEach(c=>{nameById[c.id]=c.full_name||c.company_name||c.email});data.orders.forEach(o=>{if(o.kind==="list_order"&&nameById[o.label])o.label=nameById[o.label]})}dsReadinessCache=data;renderOrderReadiness()}catch(err){console.error("loadOrderReadiness",err);["data_orders","list_orders"].forEach(kind=>{const body=document.getElementById(`dsReadinessBody_${kind}`);if(body)body.innerHTML=`<tr><td colspan="7" style="color:var(--danger);">Couldn't load readiness: ${escapeHtml(err.message||String(err))}</td></tr>`})}}const DS_READINESS_VERDICT={ready:{label:"Ready",color:"var(--good)"},partial:{label:"Partial",color:"var(--away)"},no_coverage:{label:"No coverage",color:"var(--danger)"}};function renderOrderReadiness(){const data=dsReadinessCache;if(!data)return;[["data_orders","data_order"],["list_orders","list_order"]].forEach(([sectionKey,kind])=>{const body=document.getElementById(`dsReadinessBody_${sectionKey}`);const stamp=document.getElementById(`dsReadinessStamp_${sectionKey}`);if(!body)return;if(stamp){stamp.textContent=data.geoComputedAt?`Coverage measured ${new Date(data.geoComputedAt).toLocaleString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{})}`:""}const rows=(data.orders||[]).filter(o=>o.kind===kind&&o.status!=="pending_payment");if(!rows.length){body.innerHTML='<tr><td colspan="7" style="color:var(--text-dim);">No open orders.</td></tr>';return}body.innerHTML=rows.map(o=>{const v=DS_READINESS_VERDICT[o.verdict]||DS_READINESS_VERDICT.partial;const p=o.parsed||{};const bits=[];if(p.states&&p.states.length)bits.push(escapeHtml(p.states.join("/")));const places=[...p.counties||[],...p.cities||[]];if(places.length)bits.push(escapeHtml(places.slice(0,4).join(", "))+(places.length>4?` +${places.length-4}`:""));let geoCell=bits.length?bits.join(" · "):'<span style="color:var(--danger);">nothing parsed</span>';if(p.recovered&&p.recovered.length){geoCell+=`<div style="font-size:10px; color:var(--away);" title="Recovered by splitting a space-separated list — the order's own field is malformed">↳ recovered: ${escapeHtml(p.recovered.slice(0,4).join(", "))}</div>`}if(p.unmatched&&p.unmatched.length){geoCell+=`<div style="font-size:10px; color:var(--text-dim);" title="Requested, but we hold no data there (or it couldn't be read)">↳ no data: ${escapeHtml(p.unmatched.slice(0,4).join(", "))}${p.unmatched.length>4?` +${p.unmatched.length-4}`:""}</div>`}if(p.unresolvedStates&&p.unresolvedStates.length){geoCell+=`<div style="font-size:10px; color:var(--text-dim);">↳ unreadable state: ${escapeHtml(p.unresolvedStates.slice(0,3).join(", "))}</div>`}const cov=o.coverage||{};const ageColour=o.ageHours>=168?"var(--danger)":o.ageHours>=72?"var(--away)":"var(--text-2)";return`<tr>
        <td class="mono" style="color:${ageColour}; white-space:nowrap;">${dsFmtAge(o.ageHours)}</td>
        <td style="color:var(--text-2); max-width:180px; overflow:hidden; text-overflow:ellipsis;" title="${escapeHtml(o.label||"")}">${escapeHtml(o.label||"—")}</td>
        <td style="font-size:11px;">${geoCell}</td>
        <td class="mono" style="white-space:nowrap;">${dsFmtNum(cov.total)}</td>
        <td class="mono" style="white-space:nowrap;" title="Excludes plain absentee owners. Strong distress signals only: ${dsFmtNum(cov.motivatedStrong)}">${dsFmtNum(cov.motivatedAny)}<span style="color:var(--text-dim); font-size:10px;"> (${dsFmtNum(cov.motivatedStrong)} strong)</span></td>
        <td class="mono" style="white-space:nowrap; color:var(--text-2);">${o.requested!=null?dsFmtNum(o.requested):escapeHtml((o.requestedRaw||"—").slice(0,18))}</td>
        <td><span class="mono" style="font-size:10px; padding:2px 7px; border-radius:10px; border:1px solid ${v.color}; color:${v.color};">${v.label}</span></td>
      </tr>`}).join("")})}function renderDsSignalOverlap(overlap){const head=document.getElementById("dsOverlapHead");const body=document.getElementById("dsOverlapBody");const note=document.getElementById("dsOverlapNoSignal");if(!head||!body)return;if(!overlap||!Array.isArray(overlap.signals)||!overlap.signals.length){head.innerHTML="";body.innerHTML='<tr><td style="color:var(--text-dim);">Not measured yet — click Recompute above to build the first snapshot.</td></tr>';if(note)note.textContent="";return}dsOverlapSignals=overlap.signals;const byKey={};overlap.signals.forEach(s=>{byKey[s.key]=s});head.innerHTML=`<tr><th></th>${overlap.signals.map(s=>`<th title="${escapeHtml(s.label)}" style="font-size:10px;">${escapeHtml(s.label)}</th>`).join("")}</tr>`;body.innerHTML=(overlap.matrix||[]).map(row=>{const rowSig=byKey[row.key];const cells=(row.cells||[]).map(cell=>{const isDiagonal=cell.key===row.key;const colour=cell.count===0?"var(--danger)":isDiagonal?"var(--text)":"var(--text-2)";const patch=JSON.stringify({...rowSig?rowSig.lbFilter:{},...(byKey[cell.key]||{}).lbFilter||{}});return`<td class="dsOverlap-drill mono" data-lb-filter='${escapeHtml(patch)}' title="Click to view this combination in List Builder" style="cursor:pointer; color:${colour}; ${isDiagonal?"font-weight:600; background:var(--inset);":""}">${dsFmtNum(cell.count)}</td>`}).join("");return`<tr><th style="text-align:left; font-size:10px; white-space:nowrap;">${escapeHtml(rowSig?rowSig.label:row.key)}</th>${cells}</tr>`}).join("");if(note){note.textContent=typeof overlap.noSignal==="number"?`${overlap.noSignal.toLocaleString()} properties carry none of these signals — every criteria-based search necessarily skips them.`:""}}document.getElementById("dsOverlapBody")?.addEventListener("click",e=>{const cell=e.target.closest(".dsOverlap-drill");if(!cell)return;let patch={};try{patch=JSON.parse(cell.dataset.lbFilter||"{}")}catch(err){console.error("Bad data-lb-filter on overlap cell",cell,err)}lbDrillDownFromOverview(patch)});document.getElementById("dsCoverageRefreshBtn")?.addEventListener("click",async()=>{const btn=document.getElementById("dsCoverageRefreshBtn");if(!btn)return;btn.disabled=true;const original=btn.textContent;btn.textContent="Recomputing… (~15s)";try{const res=await dataSciStatsCall({action:"refresh_snapshot"});if(!res||res.ok===false)throw new Error(res&&res.error||"refresh failed");await loadDataSciExtraStats();logActivity("datascience.snapshot_refresh",null,null,`Recomputed List Builder stats snapshot${res.durationMs?` (${Math.round(res.durationMs/1e3)}s)`:""}`)}catch(err){alert("Couldn't recompute: "+(err.message||err))}finally{btn.disabled=false;btn.textContent=original}});const DS_OVERVIEW_COUNTY_COLS=[{key:"total",filter:()=>({})},{key:"absentee",filter:()=>({absenteeOnly:true})},{key:"corporateOwner",filter:()=>({corporateOwnerOnly:true})},{key:"inherited",filter:()=>({isInherited:true})},{key:"probateEstate",filter:()=>({isProbateEstateOnly:true})},{key:"buildingCodeLawsuit",filter:()=>({hasBuildingCodeLawsuitOnly:true})},{key:"vacantBuilding",filter:()=>({isVacantBuildingOnly:true})},{key:"tiredLandlord",filter:()=>({tiredLandlordOnly:true})},{key:"longTermOwner",filter:()=>({longTermOwnerOnly:true})},{key:"mfh2to4",filter:()=>({isMfh2to4:true})},{key:"mfh5plus",filter:()=>({isMfh5Plus:true})}];function renderDsOverviewCountyBreakdown(byCounty){const body=document.getElementById("dsOverviewCountyBody");if(!body)return;const rows=Array.isArray(byCounty)?byCounty:[];if(!rows.length){body.innerHTML='<tr><td colspan="12" style="color:var(--text-dim);">No county data yet.</td></tr>';return}const fmt=n=>typeof n==="number"?n.toLocaleString():"—";body.innerHTML=rows.map(row=>{const cells=DS_OVERVIEW_COUNTY_COLS.map(col=>{const patch={county:row.county,...col.filter()};return`<td class="dsOverviewCounty-drill" data-lb-filter='${JSON.stringify(patch)}' title="Click to view in List Builder" style="cursor:pointer;">${fmt(row[col.key])}</td>`}).join("");return`<tr><td>${row.county}</td>${cells}</tr>`}).join("")}document.getElementById("dsOverviewCountyBody")?.addEventListener("click",e=>{const cell=e.target.closest(".dsOverviewCounty-drill");if(!cell)return;let patch={};try{patch=JSON.parse(cell.dataset.lbFilter||"{}")}catch(e2){console.error("Bad data-lb-filter on county breakdown cell",cell,e2)}lbDrillDownFromOverview(patch)});function renderDsOverviewTrend(trend){const head=document.getElementById("dsOverviewTrendHead");const body=document.getElementById("dsOverviewTrendBody");if(!head||!body)return;const rows=Array.isArray(trend)?trend:[];if(!rows.length){head.innerHTML="<tr><th>Date</th></tr>";body.innerHTML='<tr><td style="color:var(--text-dim);">No sync history yet.</td></tr>';return}const counties=Array.from(new Set(rows.map(r=>r.county))).sort();const byDate=new Map;rows.forEach(r=>{if(!byDate.has(r.date))byDate.set(r.date,{});byDate.get(r.date)[r.county]=r.newCount});const dates=Array.from(byDate.keys()).sort();head.innerHTML=`<tr><th>Date</th>${counties.map(c=>`<th>${escapeHtml(c)} (new)</th>`).join("")}<th>Total (new)</th><th>Running total</th></tr>`;const fmt=n=>typeof n==="number"?n.toLocaleString():"—";let running=0;body.innerHTML=dates.map(d=>{const counts=byDate.get(d);const dayTotal=counties.reduce((sum,c)=>sum+(counts[c]||0),0);running+=dayTotal;const dateLabel=new Date(d+"T00:00:00Z").toLocaleDateString(void 0,{year:"numeric",month:"short",day:"numeric",timeZone:"UTC"});return`<tr><td>${escapeHtml(dateLabel)}</td>${counties.map(c=>`<td>${fmt(counts[c]||0)}</td>`).join("")}<td>${fmt(dayTotal)}</td><td>${fmt(running)}</td></tr>`}).join("")}async function lbDrillDownFromOverview(paramsPatch){activateDataSciSection("list_builder");await initListBuilderSection();lbApplyFilterParams(paramsPatch);await runListBuilderSearch()}document.querySelectorAll(".dsOverview-drill").forEach(el=>{el.addEventListener("click",()=>{let patch={};try{patch=JSON.parse(el.dataset.lbFilter||"{}")}catch(e){console.error("Bad data-lb-filter on Overview card",el,e)}lbDrillDownFromOverview(patch)})});document.querySelectorAll(".datasci-subtab-btn").forEach(btn=>{btn.addEventListener("click",()=>{if(canViewDataSciSection(btn.dataset.datasciSection))activateDataSciSection(btn.dataset.datasciSection)})});document.querySelectorAll(".datasci-refresh-btn").forEach(btn=>{btn.addEventListener("click",async()=>{if(btn.disabled)return;btn.disabled=true;const original=btn.innerHTML;btn.innerHTML="&#8635; Refreshing…";await refreshDataSciSectionByKey(btn.dataset.refreshKey);btn.innerHTML=original;btn.disabled=false})});const TAXES_SECTIONS=[{key:"spending",label:"Spending"},{key:"invoices",label:"Invoice Archive"},{key:"stripe_income",label:"Stripe Income"}];function canViewTaxesSection(key){if(myPermissions.isProtected)return true;const s=(myPermissions.taxesSections||{})[key];return s&&typeof s.view==="boolean"?s.view:true}function taxesSubtabBtns(){return Array.from(document.querySelectorAll(".taxes-subtab-btn")).filter(b=>canViewTaxesSection(b.dataset.taxesSection))}let taxesActiveSection=null;function initTaxesTab(){document.querySelectorAll(".taxes-subtab-btn").forEach(b=>{b.style.display=canViewTaxesSection(b.dataset.taxesSection)?"":"none"});const usable=taxesSubtabBtns();const stillValid=usable.find(b=>b.dataset.taxesSection===taxesActiveSection);const target=stillValid?taxesActiveSection:usable[0]?usable[0].dataset.taxesSection:null;if(target)activateTaxesSection(target)}function activateTaxesSection(key){taxesActiveSection=key;document.querySelectorAll(".taxes-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.taxesSection===key));document.querySelectorAll(".taxes-section").forEach(s=>{s.style.display=s.id==="taxesSection-"+key?"block":"none"});if(key==="spending")loadFinanceData();else if(key==="invoices"){loadFinanceInvoices();loadTaxesGmailInvoiceConnection()}else if(key==="stripe_income")initStripeIncomeSection()}document.querySelectorAll(".taxes-subtab-btn").forEach(btn=>{btn.addEventListener("click",()=>{if(canViewTaxesSection(btn.dataset.taxesSection))activateTaxesSection(btn.dataset.taxesSection)})});let taxesIncomeData=null;async function taxesIncomeInvokeErrorMessage(error,data){if(data&&data.error)return data.error;if(error&&error.context&&typeof error.context.json==="function"){try{const body=await error.context.json();if(body&&body.error)return body.error}catch(e){}}return error&&error.message||"unknown error"}function initStripeIncomeSection(){if(!$("taxesIncomeFromInput").value){const jan1=new Date;jan1.setMonth(0,1);$("taxesIncomeFromInput").value=jan1.toISOString().slice(0,10);$("taxesIncomeToInput").value=endOfLoginMonthDateInputValue()}}const TAXES_INCOME_TYPE_LABEL={charge:"Charges",refund:"Refunds",adjustment:"Adjustments",payment:"Payments",payment_refund:"Payment refunds",payout:"Payouts",payout_failure:"Payout failures",stripe_fee:"Stripe fees",application_fee:"Application fees"};async function loadStripeIncome(){const from=$("taxesIncomeFromInput").value;const to=$("taxesIncomeToInput").value;const msgEl=$("taxesIncomeMsg");const statsEl=$("taxesIncomeStatsRow");const bodyEl=$("taxesIncomeTbody");msgEl.classList.add("hidden");if(!from||!to){msgEl.textContent="Pick both a From and To date.";msgEl.className="msg error";msgEl.classList.remove("hidden");return}const runBtn=$("taxesIncomeRunBtn");runBtn.disabled=true;const originalText=runBtn.textContent;runBtn.textContent="Running…";bodyEl.innerHTML='<tr><td colspan="6" class="empty">Loading…</td></tr>';const{data,error}=await sb.functions.invoke("staff-stripe-income-report",{body:{action:"list_income",fromDate:from,toDate:to}});runBtn.disabled=false;runBtn.textContent=originalText;if(error||!data||data.ok===false){const msg=await taxesIncomeInvokeErrorMessage(error,data);bodyEl.innerHTML=`<tr><td colspan="6" style="color:var(--danger);">Failed to load: ${escapeHtml(msg)}</td></tr>`;statsEl.innerHTML="";taxesIncomeData=null;return}taxesIncomeData=data;if(data.truncated){msgEl.textContent=`Showing the first ${data.transactionCount.toLocaleString()} transactions — this range has more than that. Narrow the dates for a complete total.`;msgEl.className="msg error";msgEl.classList.remove("hidden")}const fmt=cents=>"$"+(cents/100).toLocaleString(void 0,{minimumFractionDigits:2,maximumFractionDigits:2});const typeCards=Object.entries(data.totalsByType).sort((a,b)=>b[1]-a[1]).map(([type,cents])=>`
    <div class="act-stat-card">
      <div class="act-stat-label">${escapeHtml(TAXES_INCOME_TYPE_LABEL[type]||type)}</div>
      <div class="act-stat-value" style="color:${cents>=0?"var(--good)":"var(--danger)"};">${fmt(cents)}</div>
    </div>`).join("");statsEl.innerHTML=`
    <div class="act-stat-card">
      <div class="act-stat-label">Net Income</div>
      <div class="act-stat-value">${fmt(data.netIncomeCents)}</div>
    </div>
    ${typeCards}`;if(!data.transactions.length){bodyEl.innerHTML='<tr><td colspan="6" class="empty">No Stripe activity in this range.</td></tr>'}else{const stripeIncomeTz=nonAgentDisplayTz();bodyEl.innerHTML=data.transactions.map(t=>`
      <tr>
        <td class="mono" style="white-space:nowrap;">${new Date(t.created).toLocaleDateString([],stripeIncomeTz?{timeZone:stripeIncomeTz}:{})}</td>
        <td>${escapeHtml(TAXES_INCOME_TYPE_LABEL[t.type]||t.type)}</td>
        <td>${escapeHtml(t.description||"—")}</td>
        <td class="mono">${fmt(t.amountCents)}</td>
        <td class="mono">${fmt(t.feeCents)}</td>
        <td class="mono" style="color:${t.netCents>=0?"var(--good)":"var(--danger)"};">${fmt(t.netCents)}</td>
      </tr>`).join("")}}$("taxesIncomeRunBtn").addEventListener("click",()=>{loadStripeIncome();logActivity("taxes.stripe_income_report",null,null,`Ran Stripe Income report for ${$("taxesIncomeFromInput").value} to ${$("taxesIncomeToInput").value}`)});$("taxesIncomeExportBtn").addEventListener("click",()=>{if(!taxesIncomeData||!taxesIncomeData.transactions.length){alert("Run a report first.");return}exportCSV(`stripe-income-${taxesIncomeData.fromDate}-to-${taxesIncomeData.toDate}.csv`,["Date","Type","Description","Amount","Fee","Net"],taxesIncomeData.transactions.map(t=>[new Date(t.created).toLocaleDateString([],nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{}),TAXES_INCOME_TYPE_LABEL[t.type]||t.type,t.description||"",(t.amountCents/100).toFixed(2),(t.feeCents/100).toFixed(2),(t.netCents/100).toFixed(2)]));logActivity("taxes.stripe_income_export_csv",null,null,`Exported Stripe Income report as CSV (${taxesIncomeData.fromDate} to ${taxesIncomeData.toDate})`)});const HR_SECTIONS=[{key:"submission",label:"Candidate Submission",editable:true},{key:"review",label:"Candidate Review",editable:true},{key:"commission",label:"My Commission"},{key:"history",label:"Candidate History Archive"}];function hrSectionDefault(key){return key!=="review"&&key!=="history"}function hrEditSectionDefault(key){return key!=="review"}const DIALER_ADMIN_SECTIONS=[{key:"overview",label:"Overview (live floor, performance, pipeline)",editable:true},{key:"campaigns",label:"Campaigns (campaigns, lists, inbound queues)",editable:true},{key:"conversations",label:"Conversations (inbox, contacts, call log)",editable:true},{key:"numbers",label:"DIDs & Research Calls (numbers, research & DNC)",editable:true},{key:"team",label:"Team & settings (statuses, availability, settings, change history)",editable:true}];const DIALER_ADMIN_LEGACY={reports:"overview",pipeline:"overview",agents:"team",settings:"team",lists:"campaigns",inbound:"campaigns",inbox:"conversations",calllog:"conversations",research:"numbers",contacts:"conversations",floor:"overview",history:"team"};function dialerAdminSectionDefault(){return true}function dialerAdminEditSectionDefault(){return false}function hasDialerAdminTab(){return!!myPermissions&&(myIsOwner||(myPermissions.allowedTabs||[]).includes("dialer_admin"))}function canViewDialerAdminSection(key){if(!hasDialerAdminTab())return false;if(myPermissions.isProtected)return true;const s=(myPermissions.dialerAdminSections||{})[key];return s&&typeof s.view==="boolean"?s.view:dialerAdminSectionDefault(key)}function canEditDialerAdminSection(key){if(!hasDialerAdminTab())return false;if(myPermissions.isProtected)return true;const def=DIALER_ADMIN_SECTIONS.find(x=>x.key===key);if(!def)return false;const s=(myPermissions.dialerAdminSections||{})[key];return s&&typeof s.edit==="boolean"?s.edit:dialerAdminEditSectionDefault(key)}function daVisibleSections(){return DIALER_ADMIN_SECTIONS.filter(s=>canViewDialerAdminSection(s.key)).map(s=>s.key)}const DIALER_ADMIN_VERSION="f98a62ca10";let daLoadPromise=null;let daActiveSection=null;let daActivePath=null;function loadDialerAdminModule(){if(daLoadPromise)return daLoadPromise;daLoadPromise=(async()=>{const host=$("daHost");if(!document.getElementById("daCss")){const link=document.createElement("link");link.id="daCss";link.rel="stylesheet";link.href=`dialer-admin/dialer-admin.css?v=${DIALER_ADMIN_VERSION}`;document.head.appendChild(link)}const res=await fetch(`dialer-admin/dialer-admin.html?v=${DIALER_ADMIN_VERSION}`,{cache:"no-cache"});if(!res.ok)throw new Error(`Dialer Admin could not load (${res.status}).`);host.innerHTML=await res.text();window.__dialerAdminCtx={sb,supabaseUrl:SUPABASE_URL,anonKey:SUPABASE_ANON_KEY,userId:myUserId,role:myRole,canEdit:key=>canEditDialerAdminSection(key),onNavigate:path=>{const group=path.split("/")[0];daActiveSection=group;daActivePath=path;document.querySelectorAll(".da-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.daSection===group));try{history.replaceState(null,"","#dialer-admin/"+path)}catch(e){}}};await new Promise((resolve,reject)=>{const el=document.createElement("script");el.src=`dialer-admin/dialer-admin.js?v=${DIALER_ADMIN_VERSION}`;el.onload=resolve;el.onerror=()=>reject(new Error("Dialer Admin could not load its code."));document.body.appendChild(el)});if(!window.DialerAdmin)throw new Error("Dialer Admin loaded but did not start.")})().catch(e=>{daLoadPromise=null;$("daHost").innerHTML=`<div class="msg error" style="margin:20px;">${escapeHtml(e.message)} Reload the page to try again.</div>`;throw e});return daLoadPromise}async function activateDialerAdminSection(key,path){daActiveSection=key;document.querySelectorAll(".da-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.daSection===key));try{await loadDialerAdminModule();if(daActiveSection!==key)return;await window.DialerAdmin.showSection(path&&path.split("/")[0]===key?path:key)}catch(e){console.error("Dialer Admin:",e)}}function initDialerAdminTab(){const vis=daVisibleSections();if(!vis.length){$("daHost").innerHTML='<div style="padding:28px; color:var(--text-dim); font-size:13px;">Your role cannot see any Dialer Admin section.</div>';return}const key=vis.includes(daActiveSection)?daActiveSection:vis[0];activateDialerAdminSection(key,daActivePath)}document.querySelectorAll(".da-subtab-btn").forEach(btn=>{btn.addEventListener("click",()=>{if(!canViewDialerAdminSection(btn.dataset.daSection))return;daActiveSection=btn.dataset.daSection;daActivePath=null;activateTab("dialer_management")})});let daDeepLinkDone=false;function daOpenDeepLinkOnce(){if(daDeepLinkDone)return;daDeepLinkDone=true;const m=/^#dialer-admin(?:\/([a-z]+))?(?:\/([a-z]+))?/.exec(location.hash||"");if(!m)return;const vis=daVisibleSections();if(!vis.length)return;const group=DIALER_ADMIN_SECTIONS.some(x=>x.key===m[1])?m[1]:DIALER_ADMIN_LEGACY[m[1]];daActiveSection=vis.includes(group)?group:vis[0];daActivePath=daActiveSection===group?m[2]?group+"/"+m[2]:DIALER_ADMIN_LEGACY[m[1]]?group+"/"+m[1]:null:null;setTimeout(()=>activateTab("dialer_management"),0)}function canViewHrSection(key){if(myPermissions.isProtected)return true;const s=(myPermissions.hrSections||{})[key];return s&&typeof s.view==="boolean"?s.view:hrSectionDefault(key)}function canEditHrSection(key){if(myPermissions.isProtected)return true;const s=(myPermissions.hrSections||{})[key];return s&&typeof s.edit==="boolean"?s.edit:hrEditSectionDefault(key)}function hrSubtabBtns(){return Array.from(document.querySelectorAll(".hr-subtab-btn")).filter(b=>canViewHrSection(b.dataset.hrSection))}let hrActiveSection=null;function initHrTab(){document.querySelectorAll(".hr-subtab-btn").forEach(b=>{b.style.display=canViewHrSection(b.dataset.hrSection)?"":"none"});const usable=hrSubtabBtns();const stillValid=usable.find(b=>b.dataset.hrSection===hrActiveSection);const target=stillValid?hrActiveSection:usable[0]?usable[0].dataset.hrSection:null;if(target)activateHrSection(target)}function applyHrSectionEditGating(key){const panel=document.getElementById("hrSection-"+key);if(!panel)return;const editable=canEditHrSection(key);panel.querySelectorAll(".edit-only").forEach(el=>{el.disabled=!editable;el.style.opacity=editable?"":"0.4";el.style.cursor=editable?"":"not-allowed";el.title=editable?"":"Your role has view-only access to this subtab"})}function activateHrSection(key){hrActiveSection=key;document.querySelectorAll(".hr-subtab-btn").forEach(b=>b.classList.toggle("active",b.dataset.hrSection===key));document.querySelectorAll(".hr-section").forEach(s=>{s.style.display=s.id==="hrSection-"+key?"block":"none"});if(key==="submission")loadHrMySubmissions();else if(key==="review"){loadHrReviewCandidates();loadHrWelcomeEmailSettings()}else if(key==="commission")loadHrMyCommissions();else if(key==="history")loadHrHistoryCandidates();applyHrSectionEditGating(key)}document.querySelectorAll(".hr-subtab-btn").forEach(btn=>{btn.addEventListener("click",()=>{if(canViewHrSection(btn.dataset.hrSection))activateHrSection(btn.dataset.hrSection)})});const HR_EMPLOYMENT_TYPE_LABEL={full_time:"Full-time",part_time:"Part-time"};const HR_STATUS_LABEL={pending:'<span style="color:var(--text-dim);">Pending</span>',approved:'<span class="good">Approved</span>',rejected:'<span class="bad">Rejected</span>',terminated:'<span class="bad">Terminated</span>',found_other_offer:'<span class="bad">Found another offer</span>'};const HR_FIT_LABEL={fit:'<span class="good">Fit</span>',not_fit:'<span class="bad">Not a fit</span>'};const HR_CHECKLIST_ITEMS=["communication","objection_handling","pitch_delivery","confidence_tone","closing_ability"];const HR_CHECKLIST_LABEL={communication:"Communication / English clarity",objection_handling:"Objection handling",pitch_delivery:"Pitch / script delivery",confidence_tone:"Tone & confidence",closing_ability:"Closing ability"};const HR_INTERVIEW_RESULT_LABEL={true:'<span class="good">Passed</span>',false:'<span class="bad">Not passed</span>'};function hrReasonCellHtml(c){if(c.status==="rejected")return textPreviewCellHtml(c.rejection_reason,"Rejection reason");if(c.status==="terminated"||c.status==="found_other_offer")return textPreviewCellHtml(c.termination_reason,"Reason");return"—"}function hrChecklistTooltip(checklist){checklist=checklist||{};return HR_CHECKLIST_ITEMS.map(key=>`${checklist[key]?"✓":"✗"} ${HR_CHECKLIST_LABEL[key]}`).join("\n")}function updateHrChecklistResult(){const boxes=Array.from(document.querySelectorAll(".hr-checklist-cb"));const allChecked=boxes.length>0&&boxes.every(cb=>cb.checked);const el=$("hrCandChecklistResult");el.textContent="Result: "+(allChecked?"Passed":"Not passed");el.style.color=allChecked?"var(--good, #2ecc71)":"var(--text-dim)"}document.querySelectorAll(".hr-checklist-cb").forEach(cb=>cb.addEventListener("change",updateHrChecklistResult));$("hrCandSubmitBtn").addEventListener("click",async()=>{const btn=$("hrCandSubmitBtn");const msg=$("hrCandSubmitMsg");const fullName=$("hrCandFullName").value.trim();const email=$("hrCandEmail").value.trim();const mobile=$("hrCandMobile").value.trim();const contactPhone=$("hrCandContactPhone").value.trim();const address=$("hrCandAddress").value.trim();const employmentType=$("hrCandEmploymentType").value;const startDate=$("hrCandStartDate").value;const interviewSummary=$("hrCandInterviewSummary").value.trim();const fitSuggestion=$("hrCandFitSuggestion").value;const videoFile=$("hrCandVideoInput").files[0]||null;const docFile=$("hrCandDocInput").files[0]||null;const checklistBoxes=Array.from(document.querySelectorAll(".hr-checklist-cb"));const checklist={};checklistBoxes.forEach(cb=>{checklist[cb.dataset.key]=cb.checked});const interviewPassed=checklistBoxes.length>0&&checklistBoxes.every(cb=>cb.checked);if(!fullName||!email||!mobile||!address||!startDate||!interviewSummary||!videoFile||!docFile){msg.textContent="Full name, email, mobile number, address, start date, interview summary, video recording, and the ID/passport document are all required (contact phone is the only optional field).";msg.className="msg error";msg.style.display="block";return}btn.disabled=true;btn.textContent="Submitting…";msg.style.display="none";try{const videoPath=`${myUserId}/${Date.now()}-${videoFile.name}`;const{error:videoUploadErr}=await sb.storage.from("hr-candidate-videos").upload(videoPath,videoFile,{contentType:videoFile.type||"video/mp4"});if(videoUploadErr)throw videoUploadErr;const docPath=`${myUserId}/${Date.now()}-${docFile.name}`;const{error:docUploadErr}=await sb.storage.from("hr-candidate-documents").upload(docPath,docFile,{contentType:docFile.type||"application/octet-stream"});if(docUploadErr)throw docUploadErr;const{error}=await sb.from("hr_candidates").insert({submitted_by:myUserId,full_name:fullName,email,mobile_number:mobile,contact_phone:contactPhone||null,address,employment_type:employmentType,employment_start_date:startDate,interview_summary:interviewSummary,fit_suggestion:fitSuggestion,interview_checklist:checklist,interview_passed:interviewPassed,video_path:videoPath,id_document_path:docPath});if(error)throw error;logActivity("hr.candidate_submitted","hr_candidate",null,`Submitted candidate "${fullName}"`);$("hrCandFullName").value="";$("hrCandEmail").value="";$("hrCandMobile").value="";$("hrCandContactPhone").value="";$("hrCandAddress").value="";$("hrCandEmploymentType").value="full_time";$("hrCandStartDate").value="";$("hrCandInterviewSummary").value="";$("hrCandFitSuggestion").value="fit";checklistBoxes.forEach(cb=>{cb.checked=false});updateHrChecklistResult();$("hrCandVideoInput").value="";$("hrCandDocInput").value="";msg.textContent="Candidate submitted — awaiting review.";msg.className="msg";msg.style.display="block";await loadHrMySubmissions()}catch(e){msg.textContent=e.message||"Failed to submit candidate.";msg.className="msg error";msg.style.display="block"}finally{btn.disabled=false;btn.textContent="Submit candidate"}});async function loadHrMySubmissions(){const body=$("hrMySubmissionsBody");const empty=$("hrMySubmissionsEmpty");const{data,error}=await sb.from("hr_candidates").select("id, full_name, email, employment_type, interview_passed, employment_start_date, status, rejection_reason, termination_reason, created_at").eq("submitted_by",myUserId).order("created_at",{ascending:false});if(error){console.error("Failed to load your candidate submissions",error);return}const rows=data||[];empty.style.display=rows.length?"none":"block";const hrMySubmissionsTz=nonAgentDisplayTz();body.innerHTML=rows.map(c=>`
    <tr>
      <td>${escapeHtml(c.full_name)}</td>
      <td style="color:var(--text-2);">${escapeHtml(c.email||"—")}</td>
      <td>${HR_EMPLOYMENT_TYPE_LABEL[c.employment_type]||c.employment_type}</td>
      <td>${HR_INTERVIEW_RESULT_LABEL[c.interview_passed]}</td>
      <td style="color:var(--text-2);">${c.employment_start_date?new Date(c.employment_start_date).toLocaleDateString():"—"}</td>
      <td style="color:var(--text-2);">${new Date(c.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...hrMySubmissionsTz?{timeZone:hrMySubmissionsTz}:{}})}</td>
      <td>${HR_STATUS_LABEL[c.status]||escapeHtml(c.status)}</td>
      <td style="color:var(--text-2);">${hrReasonCellHtml(c)}</td>
    </tr>
  `).join("")}async function loadHrMyCommissions(){if(ciIsAdmin())return loadHrRecruiterCommissions();const body=$("hrCommissionBody");const empty=$("hrCommissionEmpty");const{data,error}=await sb.rpc("hr_my_commissions");if(error){console.error("Failed to load commission data",error);return}const rows=data||[];empty.style.display=rows.length?"none":"block";const total=rows.reduce((sum,r)=>sum+(Number(r.commission_amount)||0),0);$("hrCommissionSummary").textContent=`EGP ${total.toLocaleString()} earned so far`;body.innerHTML=rows.map(r=>{const daysLeft=Math.max(0,14-(r.days_employed||0));let statusHtml;if(!r.is_active)statusHtml='<span class="bad">Account inactive — disqualified</span>';else if(r.qualifies)statusHtml='<span class="good">Qualified</span>';else statusHtml=`<span style="color:var(--text-dim);">Pending — ${daysLeft} day${daysLeft===1?"":"s"} left</span>`;return`
    <tr>
      <td>${escapeHtml(r.full_name)}</td>
      <td style="color:var(--text-2);">${r.employment_start_date?new Date(r.employment_start_date).toLocaleDateString():"—"}</td>
      <td style="color:var(--text-2);">${r.days_employed!=null?r.days_employed:"—"}</td>
      <td>${r.is_active?'<span class="good">Active</span>':'<span class="bad">Inactive</span>'}</td>
      <td>${statusHtml}</td>
      <td class="mono">${r.qualifies?`EGP ${Number(r.commission_amount).toLocaleString()}`:"—"}</td>
    </tr>
  `}).join("")}$("hrCommissionRefreshBtn").addEventListener("click",loadHrMyCommissions);function hrFmtDate(v){if(!v)return"—";const d=String(v).length===10?new Date(v+"T00:00:00"):new Date(v);return d.toLocaleDateString([],{month:"short",day:"numeric",year:"numeric"})}async function loadHrRecruiterCommissions(){const sec=$("hrSection-commission");const body=$("hrCommissionBody");const empty=$("hrCommissionEmpty");const h3=sec.querySelector("h3");if(h3)h3.textContent="Recruiter commission";const note=$("hrCommissionSummary").nextElementSibling;if(note)note.textContent="EGP 1,500 per candidate a recruiter submitted, once approved, given an employment start date, active for at least 14 days since that date, and still an active account. Every candidate is listed with their dates; only qualified ones earn.";body.closest("table").querySelector("thead").innerHTML="<tr><th>Candidate</th><th>Submitted</th><th>Approved</th><th>Start date</th><th>Qualifies on</th><th>Status</th><th>Commission</th></tr>";const{data,error}=await sb.rpc("hr_recruiter_commissions");if(error){console.error("Failed to load recruiter commissions",error);body.innerHTML=`<tr><td colspan="7" class="empty">${escapeHtml(error.message)}</td></tr>`;return}const rows=data||[];empty.textContent="No candidates submitted yet.";empty.style.display=rows.length?"none":"block";const groups=new Map;rows.forEach(r=>{const k=r.recruiter_id||"none";if(!groups.has(k))groups.set(k,{name:r.recruiter_name,rows:[]});groups.get(k).rows.push(r)});const earned=rows.reduce((t,r)=>t+(Number(r.commission_amount)||0),0);const pending=rows.filter(r=>r.status==="approved"&&r.employment_start_date&&r.is_active&&!r.qualifies).length;$("hrCommissionSummary").textContent=`EGP ${earned.toLocaleString()} earned by ${groups.size} recruiter${groups.size===1?"":"s"}`+(pending?` · ${pending} pending`:"");body.innerHTML=[...groups.values()].map(g=>{const total=g.rows.reduce((t,r)=>t+(Number(r.commission_amount)||0),0);const qualified=g.rows.filter(r=>r.qualifies).length;const head=`<tr class="hr-rc-head"><td colspan="7"><b>${escapeHtml(g.name)}</b> <span style="color:var(--text-dim);">— ${g.rows.length} candidate${g.rows.length===1?"":"s"} · ${qualified} qualified</span><span class="mono" style="float:right;">EGP ${total.toLocaleString()}</span></td></tr>`;return head+g.rows.map(r=>{let st;if(r.status!=="approved")st=`<span style="color:var(--text-dim);">${escapeHtml(HR_STATUS_LABEL[r.status]||r.status)}</span>`;else if(!r.employment_start_date)st='<span style="color:var(--text-dim);">Approved — no start date yet</span>';else if(!r.is_active)st='<span class="bad">Account inactive — disqualified</span>';else if(r.qualifies)st='<span class="good">Qualified</span>';else{const left=Math.max(0,14-(r.days_employed||0));st=`<span style="color:var(--text-dim);">Pending — ${left} day${left===1?"":"s"} left</span>`}const approved=["approved","terminated","found_other_offer"].includes(r.status)?hrFmtDate(r.reviewed_at):"—";return`<tr>
        <td>${escapeHtml(r.candidate_name)}</td>
        <td style="color:var(--text-2);">${hrFmtDate(r.submitted_at)}</td>
        <td style="color:var(--text-2);">${approved}</td>
        <td style="color:var(--text-2);">${hrFmtDate(r.employment_start_date)}</td>
        <td style="color:var(--text-2);">${r.employment_start_date?hrFmtDate(r.qualifies_on):"—"}</td>
        <td>${st}</td>
        <td class="mono">${r.qualifies?`EGP ${Number(r.commission_amount).toLocaleString()}`:"—"}</td>
      </tr>`}).join("")}).join("")}let hrReviewCandidates=[];async function loadHrReviewCandidates(){const body=$("hrReviewBody");const empty=$("hrReviewEmpty");const statusFilter=$("hrReviewStatusFilter").value;let query=sb.from("hr_candidates").select("id, submitted_by, full_name, email, mobile_number, contact_phone, address, employment_type, interview_summary, fit_suggestion, interview_checklist, interview_passed, video_path, id_document_path, employment_start_date, status, rejection_reason, termination_reason, created_at, submitter:profiles!hr_candidates_submitted_by_fkey(full_name)").order("created_at",{ascending:false});if(statusFilter)query=query.eq("status",statusFilter);const{data,error}=await query;if(error){console.error("Failed to load candidates for review",error);return}hrReviewCandidates=data||[];empty.style.display=hrReviewCandidates.length?"none":"block";body.innerHTML=hrReviewCandidates.map(c=>`
    <tr>
      <td>${escapeHtml(c.full_name)}</td>
      <td>${escapeHtml(c.email||"—")}</td>
      <td>${escapeHtml(c.mobile_number)}</td>
      <td>${escapeHtml(c.contact_phone||"—")}</td>
      <td>${textPreviewCellHtml(c.address,"Address")}</td>
      <td>${HR_EMPLOYMENT_TYPE_LABEL[c.employment_type]||c.employment_type}</td>
      <td>${textPreviewCellHtml(c.interview_summary,"Interview summary")}</td>
      <td>${c.fit_suggestion?HR_FIT_LABEL[c.fit_suggestion]||escapeHtml(c.fit_suggestion):"—"}</td>
      <td title="${escapeHtml(hrChecklistTooltip(c.interview_checklist))}">${HR_INTERVIEW_RESULT_LABEL[c.interview_passed]}</td>
      <td>${c.video_path?`<a href="#" class="hr-review-video-link" data-id="${c.id}">View</a>`:"—"}</td>
      <td>${c.id_document_path?`<a href="#" class="hr-review-doc-link" data-id="${c.id}">View</a>`:"—"}</td>
      ${hrStartDateCellHtml(c)}
      <td style="color:var(--text-2);">${escapeHtml(c.submitter&&c.submitter.full_name||"—")}</td>
      <td style="color:var(--text-2);">${new Date(c.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{}})}</td>
      <td>${HR_STATUS_LABEL[c.status]||escapeHtml(c.status)}</td>
      <td>
        ${c.status==="pending"?canEditHrSection("review")?`
          <button class="btn-secondary hr-review-approve-btn" data-id="${c.id}" style="width:auto; padding:5px 10px; font-size:11px;">Approve</button>
          <button class="btn-secondary hr-review-reject-btn" data-id="${c.id}" style="width:auto; padding:5px 10px; font-size:11px;">Reject</button>
        `:"—":c.status==="rejected"?textPreviewCellHtml(c.rejection_reason,"Rejection reason"):c.status==="terminated"||c.status==="found_other_offer"?textPreviewCellHtml(c.termination_reason,"Reason"):"Account created"}
      </td>
    </tr>
  `).join("")}$("hrReviewBody").addEventListener("click",async e=>{const videoBtn=e.target.closest(".hr-review-video-link");if(videoBtn){e.preventDefault();const cand=hrReviewCandidates.find(c=>c.id===videoBtn.dataset.id);if(!cand||!cand.video_path)return;const{data,error}=await sb.storage.from("hr-candidate-videos").createSignedUrl(cand.video_path,3600);if(error){alert("Failed to load video: "+error.message);return}window.open(data.signedUrl,"_blank","noopener");return}const docBtn=e.target.closest(".hr-review-doc-link");if(docBtn){e.preventDefault();const cand=hrReviewCandidates.find(c=>c.id===docBtn.dataset.id);if(!cand||!cand.id_document_path)return;const{data,error}=await sb.storage.from("hr-candidate-documents").createSignedUrl(cand.id_document_path,3600);if(error){alert("Failed to load document: "+error.message);return}window.open(data.signedUrl,"_blank","noopener");return}const approveBtn=e.target.closest(".hr-review-approve-btn");if(approveBtn){openHrApproveModal(approveBtn.dataset.id);return}const rejectBtn=e.target.closest(".hr-review-reject-btn");if(rejectBtn){await setHrCandidateStatus(rejectBtn.dataset.id,"rejected");return}});async function setHrCandidateStatus(candidateId,status){const cand=hrReviewCandidates.find(c=>c.id===candidateId);let rejectionReason=null;if(status==="rejected"){rejectionReason=prompt("Why is this candidate being rejected? (required)");if(rejectionReason===null)return;rejectionReason=rejectionReason.trim();if(!rejectionReason){alert("A reason is required to reject a candidate.");return}}const{error}=await sb.from("hr_candidates").update({status,rejection_reason:status==="rejected"?rejectionReason:null,reviewed_by:myUserId,reviewed_at:new Date().toISOString()}).eq("id",candidateId);if(error){alert(error.message);return}logActivity("hr.candidate_reviewed","hr_candidate",candidateId,`Marked ${status}${cand?` — ${cand.full_name}`:""}${status==="rejected"?` — reason: ${rejectionReason}`:""}`);await loadHrReviewCandidates()}$("hrMySubmissionsRefreshBtn").addEventListener("click",loadHrMySubmissions);$("hrReviewRefreshBtn").addEventListener("click",loadHrReviewCandidates);$("hrReviewStatusFilter").addEventListener("change",loadHrReviewCandidates);function hrCanEditStartDate(){if(myIsOwner||myRole==="admin")return true;const hs=myPermissions&&myPermissions.hrSections||{};return!!(hs.review&&hs.review.view)}async function hrRefreshCandidateTables(){const jobs=[];if(typeof loadHrReviewCandidates==="function"&&document.getElementById("hrReviewBody"))jobs.push(loadHrReviewCandidates());if(typeof loadHrHistoryCandidates==="function"&&document.getElementById("hrHistoryBody"))jobs.push(loadHrHistoryCandidates());await Promise.all(jobs)}function hrStartDateCellHtml(c){const shown=c.employment_start_date?new Date(c.employment_start_date).toLocaleDateString():"—";if(!hrCanEditStartDate()){return`<td style="color:var(--text-2);">${shown}</td>`}return`<td style="color:var(--text-2);" data-start-date-cell="${c.id}"><span class="hr-startdate-edit" data-id="${c.id}" data-value="${escapeHtml(c.employment_start_date||"")}" style="cursor:pointer; border-bottom:1px dashed var(--focus); color:var(--focus);" title="Click to change the employment start date">${shown}</span></td>`}document.addEventListener("click",async e=>{const trigger=e.target.closest&&e.target.closest(".hr-startdate-edit");if(!trigger)return;const cell=trigger.parentElement;const id=trigger.dataset.id;const current=trigger.dataset.value||"";cell.innerHTML=`<input type="date" class="hr-startdate-input" value="${escapeHtml(current)}" style="padding:4px 6px; font-size:11px; width:140px;">`;const input=cell.querySelector(".hr-startdate-input");input.focus();let done=false;const commit=async()=>{if(done)return;done=true;const value=input.value;if(!value||value===current){await hrRefreshCandidateTables();return}const{error}=await sb.from("hr_candidates").update({employment_start_date:value}).eq("id",id);if(error){alert(`Could not change the start date: ${error.message}`)}else{logActivity("hr.start_date_update","hr_candidate",id,`Employment start date changed from ${current||"(unset)"} to ${value}`)}await hrRefreshCandidateTables()};input.addEventListener("change",commit);input.addEventListener("blur",commit);input.addEventListener("keydown",ev=>{if(ev.key==="Escape"){done=true;hrRefreshCandidateTables()}})});let hrHistoryCandidates=[];async function loadHrHistoryCandidates(){const body=$("hrHistoryBody");const empty=$("hrHistoryEmpty");const statusFilter=$("hrHistoryStatusFilter").value;const search=$("hrHistorySearchFilter").value.trim().toLowerCase();let query=sb.from("hr_candidates").select("id, submitted_by, full_name, email, mobile_number, contact_phone, address, employment_type, interview_summary, fit_suggestion, interview_checklist, interview_passed, video_path, id_document_path, employment_start_date, status, rejection_reason, termination_reason, reviewed_by, reviewed_at, created_at, submitter:profiles!hr_candidates_submitted_by_fkey(full_name), reviewer:profiles!hr_candidates_reviewed_by_fkey(full_name)").order("created_at",{ascending:false});if(statusFilter)query=query.eq("status",statusFilter);const{data,error}=await query;if(error){console.error("Failed to load candidate history",error);return}let rows=data||[];if(search){rows=rows.filter(c=>[c.full_name,c.email,c.mobile_number,c.contact_phone].some(v=>v&&String(v).toLowerCase().includes(search)))}hrHistoryCandidates=rows;empty.style.display=rows.length?"none":"block";body.innerHTML=rows.map(c=>`
    <tr>
      <td>${escapeHtml(c.full_name)}</td>
      <td>${escapeHtml(c.email||"—")}</td>
      <td>${escapeHtml(c.mobile_number)}</td>
      <td>${escapeHtml(c.contact_phone||"—")}</td>
      <td>${textPreviewCellHtml(c.address,"Address")}</td>
      <td>${HR_EMPLOYMENT_TYPE_LABEL[c.employment_type]||c.employment_type}</td>
      <td>${textPreviewCellHtml(c.interview_summary,"Interview summary")}</td>
      <td>${c.fit_suggestion?HR_FIT_LABEL[c.fit_suggestion]||escapeHtml(c.fit_suggestion):"—"}</td>
      <td title="${escapeHtml(hrChecklistTooltip(c.interview_checklist))}">${HR_INTERVIEW_RESULT_LABEL[c.interview_passed]}</td>
      <td>${c.video_path?`<a href="#" class="hr-history-video-link" data-id="${c.id}">View</a>`:"—"}</td>
      <td>${c.id_document_path?`<a href="#" class="hr-history-doc-link" data-id="${c.id}">View</a>`:"—"}</td>
      ${hrStartDateCellHtml(c)}
      <td style="color:var(--text-2);">${escapeHtml(c.submitter&&c.submitter.full_name||"—")}</td>
      <td style="color:var(--text-2);">${new Date(c.created_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{}})}</td>
      <td>${HR_STATUS_LABEL[c.status]||escapeHtml(c.status)}</td>
      <td>${c.status==="rejected"?textPreviewCellHtml(c.rejection_reason,"Rejection reason"):c.status==="terminated"||c.status==="found_other_offer"?textPreviewCellHtml(c.termination_reason,"Reason"):"—"}</td>
      <td style="color:var(--text-2);">${escapeHtml(c.reviewer&&c.reviewer.full_name||"—")}</td>
      <td style="color:var(--text-2);">${c.reviewed_at?new Date(c.reviewed_at).toLocaleString([],{month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",...nonAgentDisplayTz()?{timeZone:nonAgentDisplayTz()}:{}}):"—"}</td>
      <td style="white-space:nowrap;">
        ${canEditHrSection("review")?`<button class="hr-history-edit-btn" data-id="${c.id}" style="background:transparent; border:1px solid var(--border-light); border-radius:6px; color:var(--text-2); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px;">Edit</button>`:""}
        ${myIsOwner||myRole==="admin"?`<button class="hr-history-delete-btn" data-id="${c.id}" style="background:transparent; border:1px solid var(--danger, #d9534f); border-radius:6px; color:var(--danger, #d9534f); cursor:pointer; font-size:11px; font-family:var(--mono); padding:4px 8px; margin-left:4px;">Delete</button>`:""}
        ${!canEditHrSection("review")&&!(myIsOwner||myRole==="admin")?"—":""}
      </td>
    </tr>
  `).join("")}$("hrHistoryBody").addEventListener("click",async e=>{const editBtn=e.target.closest(".hr-history-edit-btn");if(editBtn){openHrHistoryEditModal(editBtn.dataset.id);return}const deleteBtn=e.target.closest(".hr-history-delete-btn");if(deleteBtn){await deleteHrHistoryCandidate(deleteBtn.dataset.id);return}const videoBtn=e.target.closest(".hr-history-video-link");if(videoBtn){e.preventDefault();const cand=hrHistoryCandidates.find(c=>c.id===videoBtn.dataset.id);if(!cand||!cand.video_path)return;const{data,error}=await sb.storage.from("hr-candidate-videos").createSignedUrl(cand.video_path,3600);if(error){alert("Failed to load video: "+error.message);return}window.open(data.signedUrl,"_blank","noopener");return}const docBtn=e.target.closest(".hr-history-doc-link");if(docBtn){e.preventDefault();const cand=hrHistoryCandidates.find(c=>c.id===docBtn.dataset.id);if(!cand||!cand.id_document_path)return;const{data,error}=await sb.storage.from("hr-candidate-documents").createSignedUrl(cand.id_document_path,3600);if(error){alert("Failed to load document: "+error.message);return}window.open(data.signedUrl,"_blank","noopener");return}});$("hrHistoryRefreshBtn").addEventListener("click",loadHrHistoryCandidates);$("hrHistoryStatusFilter").addEventListener("change",loadHrHistoryCandidates);let hrHistorySearchDebounce=null;$("hrHistorySearchFilter").addEventListener("input",()=>{clearTimeout(hrHistorySearchDebounce);hrHistorySearchDebounce=setTimeout(loadHrHistoryCandidates,300)});let hrHistoryEditCandidateId=null;function openHrHistoryEditModal(candidateId){const cand=hrHistoryCandidates.find(c=>c.id===candidateId);if(!cand)return;hrHistoryEditCandidateId=candidateId;$("hrHistoryEditCandidateName").textContent=cand.full_name||"—";$("hrHistoryEditFullName").value=cand.full_name||"";$("hrHistoryEditEmail").value=cand.email||"";$("hrHistoryEditMobile").value=cand.mobile_number||"";$("hrHistoryEditContactPhone").value=cand.contact_phone||"";$("hrHistoryEditAddress").value=cand.address||"";$("hrHistoryEditEmploymentType").value=cand.employment_type||"full_time";$("hrHistoryEditVideoCurrent").textContent=cand.video_path?"(currently on file)":"(none on file)";$("hrHistoryEditDocCurrent").textContent=cand.id_document_path?"(currently on file)":"(none on file)";$("hrHistoryEditVideoInput").value="";$("hrHistoryEditDocInput").value="";$("hrHistoryEditMsg").style.display="none";$("hrHistoryEditModalOverlay").classList.add("active")}function closeHrHistoryEditModal(){$("hrHistoryEditModalOverlay").classList.remove("active");hrHistoryEditCandidateId=null}$("hrHistoryEditCancelBtn").addEventListener("click",closeHrHistoryEditModal);$("hrHistoryEditModalOverlay").addEventListener("click",e=>{if(e.target.id==="hrHistoryEditModalOverlay")closeHrHistoryEditModal()});$("hrHistoryEditSaveBtn").addEventListener("click",async()=>{const msg=$("hrHistoryEditMsg");msg.style.display="none";const candidateId=hrHistoryEditCandidateId;if(!candidateId)return;const fullName=$("hrHistoryEditFullName").value.trim();const email=$("hrHistoryEditEmail").value.trim();const mobile=$("hrHistoryEditMobile").value.trim();const contactPhone=$("hrHistoryEditContactPhone").value.trim();const address=$("hrHistoryEditAddress").value.trim();const employmentType=$("hrHistoryEditEmploymentType").value;const videoFile=$("hrHistoryEditVideoInput").files[0]||null;const docFile=$("hrHistoryEditDocInput").files[0]||null;if(!fullName||!email||!mobile||!address){msg.textContent="Full name, email, mobile number, and address are required.";msg.style.display="block";return}const btn=$("hrHistoryEditSaveBtn");btn.disabled=true;btn.textContent="Saving…";try{const patch={full_name:fullName,email,mobile_number:mobile,contact_phone:contactPhone||null,address,employment_type:employmentType};if(videoFile){const videoPath=`${myUserId}/${Date.now()}-${videoFile.name}`;const{error:videoErr}=await sb.storage.from("hr-candidate-videos").upload(videoPath,videoFile,{contentType:videoFile.type||"video/mp4"});if(videoErr)throw videoErr;patch.video_path=videoPath}if(docFile){const docPath=`${myUserId}/${Date.now()}-${docFile.name}`;const{error:docErr}=await sb.storage.from("hr-candidate-documents").upload(docPath,docFile,{contentType:docFile.type||"application/octet-stream"});if(docErr)throw docErr;patch.id_document_path=docPath}const{error}=await sb.from("hr_candidates").update(patch).eq("id",candidateId);if(error)throw error;logActivity("hr.candidate_edited","hr_candidate",candidateId,`Edited candidate details for "${fullName}"${videoFile?" (video replaced)":""}${docFile?" (ID/passport document replaced)":""}`);closeHrHistoryEditModal();await loadHrHistoryCandidates()}catch(e){msg.textContent=e.message||"Failed to save changes.";msg.style.display="block"}finally{btn.disabled=false;btn.textContent="Save"}});async function deleteHrHistoryCandidate(candidateId){const cand=hrHistoryCandidates.find(c=>c.id===candidateId);if(!cand)return;if(!confirm(`Permanently delete the candidate record for "${cand.full_name}" (${cand.email||"no email"})? This removes it from Your Submissions, My Commission, and this history — it cannot be undone.`))return;const{error}=await sb.from("hr_candidates").delete().eq("id",candidateId);if(error){alert(`Couldn't delete this candidate: ${error.message}`);return}await loadHrHistoryCandidates();logActivity("hr.candidate_deleted","hr_candidate",candidateId,`Permanently deleted candidate record for "${cand.full_name}" (${cand.email||"no email"})`)}let hrApproveCandidateId=null;function nextMondayAt930Local(){const d=new Date;const daysUntilMonday=(8-d.getDay())%7||7;d.setDate(d.getDate()+daysUntilMonday);d.setHours(9,30,0,0);const pad=n=>String(n).padStart(2,"0");return`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}T${pad(d.getHours())}:${pad(d.getMinutes())}`}function formatMeetingDatetimeLabel(datetimeLocalValue){if(!datetimeLocalValue)return"";const start=new Date(datetimeLocalValue);if(isNaN(start.getTime()))return"";const end=new Date(start.getTime()+30*6e4);const datePart=start.toLocaleDateString([],{weekday:"long",month:"long",day:"numeric"});const fmtTime=d=>d.toLocaleTimeString([],{hour:"numeric",minute:"2-digit"}).replace(" ","").toLowerCase();const tzAbbr=start.toLocaleTimeString([],{timeZoneName:"short"}).split(" ").pop();return`${datePart} · ${fmtTime(start)} – ${fmtTime(end)} ${tzAbbr}`}async function openHrApproveModal(candidateId){const cand=hrReviewCandidates.find(c=>c.id===candidateId);if(!cand)return;hrApproveCandidateId=candidateId;$("hrApproveCandidateName").textContent=cand.full_name;$("hrApproveFullName").value=cand.full_name||"";$("hrApproveEmail").value=cand.email||"";$("hrApproveMobile").value=cand.mobile_number||"";$("hrApproveStartDate").value=cand.employment_start_date||new Date().toISOString().slice(0,10);await refreshAddUserRoleOptions("hrApproveRole");$("hrApprovePassword").value=generatePassword();$("hrApprovePosition").value="";$("hrApproveGrade").value="staff";await fillDepartmentSelect("hrApproveDepartment","");$("hrApproveMeetingDatetime").value=nextMondayAt930Local();$("hrApproveSendWelcomeEmail").checked=true;$("hrApproveMsg").textContent="";$("hrApproveWelcomeEmailMsg").style.display="none";$("hrApproveFormView").style.display="block";$("hrApproveSuccessView").style.display="none";$("hrApproveModalOverlay").classList.add("active")}function closeHrApproveModal(){$("hrApproveModalOverlay").classList.remove("active");hrApproveCandidateId=null}$("hrApproveCancelBtn").addEventListener("click",closeHrApproveModal);$("hrApproveModalOverlay").addEventListener("click",e=>{if(e.target.id==="hrApproveModalOverlay")closeHrApproveModal()});$("hrApproveGenPasswordBtn").addEventListener("click",()=>{$("hrApprovePassword").value=generatePassword()});const HR_WELCOME_EMAIL_DEFAULT_SUBJECT="You're hired at proptech AI";const HR_WELCOME_EMAIL_DEFAULT_BODY=`Hi {first_name},

Welcome onboard! and congrats on your new position as {position}.

Please find our CRM portal credentials below

Link: https://staffportal.proptechnologyai.com
Email: {email}
Password: {password}

Please join the onboarding meeting link on {meeting_datetime}.

Onboarding meeting
{meeting_datetime}
{meeting_info}

Please find the extension installation link below
https://chromewebstore.google.com/detail/proptech-ai-time-manageme/ejpaamckgoggmegolclnifbmdnbnlmei

Regards,
Hiring Team`;const HR_ONBOARDING_MEETING_INFO_DEFAULT=`Time zone: America/Indiana/Knox
Google Meet joining info
Video call link: https://meet.google.com/gup-iiub-ngo
Or dial: (ZA) +27 10 823 0320 PIN: 567 814 214 1104#
More phone numbers: https://tel.meet/gup-iiub-ngo?pin=5678142141104`;const HR_WELCOME_EMAIL_DEFAULT_CC="rmariam190@gmail.com, info@proptechnologyai.com";async function loadHrWelcomeEmailSettings(){const{data:rows}=await sb.from("app_settings").select("key, value").in("key",["hr_welcome_email_subject","hr_welcome_email_body","hr_onboarding_meeting_info","hr_welcome_email_cc"]);const byKey={};(rows||[]).forEach(r=>{if(r.value!=null)byKey[r.key]=r.value});$("hrWelcomeEmailSubject").value=byKey["hr_welcome_email_subject"]||HR_WELCOME_EMAIL_DEFAULT_SUBJECT;$("hrWelcomeEmailBody").value=byKey["hr_welcome_email_body"]||HR_WELCOME_EMAIL_DEFAULT_BODY;$("hrOnboardingMeetingInfo").value=byKey["hr_onboarding_meeting_info"]||HR_ONBOARDING_MEETING_INFO_DEFAULT;$("hrWelcomeEmailCc").value=byKey["hr_welcome_email_cc"]||HR_WELCOME_EMAIL_DEFAULT_CC}if($("hrWelcomeEmailSettingsSaveBtn"))$("hrWelcomeEmailSettingsSaveBtn").addEventListener("click",async()=>{const msg=$("hrWelcomeEmailSettingsMsg");msg.textContent="";const btn=$("hrWelcomeEmailSettingsSaveBtn");btn.disabled=true;const now=new Date().toISOString();const rows=[{key:"hr_welcome_email_subject",value:$("hrWelcomeEmailSubject").value.trim()||HR_WELCOME_EMAIL_DEFAULT_SUBJECT,updated_at:now},{key:"hr_welcome_email_body",value:$("hrWelcomeEmailBody").value.trim()||HR_WELCOME_EMAIL_DEFAULT_BODY,updated_at:now},{key:"hr_onboarding_meeting_info",value:$("hrOnboardingMeetingInfo").value.trim()||HR_ONBOARDING_MEETING_INFO_DEFAULT,updated_at:now},{key:"hr_welcome_email_cc",value:$("hrWelcomeEmailCc").value.trim()||HR_WELCOME_EMAIL_DEFAULT_CC,updated_at:now}];const{error}=await sb.from("app_settings").upsert(rows,{onConflict:"key"});btn.disabled=false;if(error){msg.textContent=error.message;msg.className="msg error";return}msg.textContent="Saved.";msg.className="msg";logActivity("app_settings.update",null,null,"Updated HR welcome email settings")});$("hrApproveCreateBtn").addEventListener("click",async()=>{const msg=$("hrApproveMsg");msg.textContent="";const full_name=$("hrApproveFullName").value.trim();const email=$("hrApproveEmail").value.trim();const mobile=$("hrApproveMobile").value.trim();const role=$("hrApproveRole").value;const password=$("hrApprovePassword").value;const startDate=$("hrApproveStartDate").value;const candidateId=hrApproveCandidateId;if(!candidateId)return;if(!full_name){msg.textContent="Full name is required.";return}if(!email){msg.textContent="Email is required.";return}if(!mobile){msg.textContent="Mobile number is required.";return}if(!password||password.length<8){msg.textContent="Password must be at least 8 characters.";return}if(!startDate){msg.textContent="Employment start date is required.";return}const btn=$("hrApproveCreateBtn");btn.disabled=true;const result=await invokeManageUser({action:"create_user",email,password,full_name,role});btn.disabled=false;if(!result.ok){msg.textContent=result.error||"Failed to create user.";return}const{error}=await sb.from("hr_candidates").update({full_name,email,mobile_number:mobile,status:"approved",rejection_reason:null,reviewed_by:myUserId,reviewed_at:new Date().toISOString(),created_user_id:result.userId,employment_start_date:startDate}).eq("id",candidateId);if(error){msg.textContent=`Account was created, but failed to update the candidate record: ${error.message}`;return}const hrDepartment=$("hrApproveDepartment").value;const hrTitle=$("hrApprovePosition").value.trim();const hrGrade=$("hrApproveGrade").value;let hrOrgWarning="";if(hrDepartment||hrTitle||hrGrade){const org=await saveStaffOrgInfo(result.userId,hrDepartment,hrTitle,hrGrade);if(!org.ok){hrOrgWarning=` The account was created, but the department/title was not saved (${org.error}). Set it from User Management > Users.`;console.error("set_staff_org_info failed after candidate approval",org.error)}}logActivity("hr.candidate_approved","hr_candidate",candidateId,`Approved and created ${role} account for ${email}${full_name?" ("+full_name+")":""}${hrTitle||hrDepartment?` — ${[hrTitle,hrDepartment].filter(Boolean).join(" · ")}`:""}`);logActivity("user.create","agent",result.userId,`Created ${role} account for ${email}${full_name?" ("+full_name+")":""} (via Candidate Review approval)`);loadTeam();if(umActiveSection==="users")loadUmUsersList();await loadHrReviewCandidates();$("hrApproveSuccessEmail").textContent=email;$("hrApproveSuccessPassword").textContent=password;$("hrApproveOrgWarning").textContent=hrOrgWarning;$("hrApproveOrgWarning").style.display=hrOrgWarning?"block":"none";$("hrApproveFormView").style.display="none";$("hrApproveSuccessView").style.display="block";const welcomeMsg=$("hrApproveWelcomeEmailMsg");if($("hrApproveSendWelcomeEmail").checked){welcomeMsg.style.display="block";welcomeMsg.className="msg";welcomeMsg.textContent="Sending welcome email…";const{data:sendResult,error:sendErr}=await sb.functions.invoke("hr-send-welcome-email",{body:{toEmail:email,fullName:full_name,position:$("hrApprovePosition").value.trim(),password,meetingDatetime:formatMeetingDatetimeLabel($("hrApproveMeetingDatetime").value)}});if(sendErr||!sendResult||!sendResult.ok){let errText=sendResult&&sendResult.error||sendErr?.message||"Failed to send.";if(sendErr&&sendErr.context&&typeof sendErr.context.json==="function"){try{const parsed=await sendErr.context.json();if(parsed?.error)errText=parsed.error}catch(_e){}}welcomeMsg.className="msg error";welcomeMsg.textContent=`Account created, but the welcome email failed to send: ${errText}`}else{welcomeMsg.className="msg";welcomeMsg.textContent=`Welcome email sent to ${email}.`}}});$("hrApproveCopyBtn").addEventListener("click",async()=>{const text=`Email: ${$("hrApproveSuccessEmail").textContent}
Password: ${$("hrApproveSuccessPassword").textContent}`;try{await navigator.clipboard.writeText(text);const btn=$("hrApproveCopyBtn");const original=btn.textContent;btn.textContent="Copied!";setTimeout(()=>{btn.textContent=original},1500)}catch(e){}});$("hrApproveDoneBtn").addEventListener("click",closeHrApproveModal);let financeCategories=[];let financeVendorMap=[];let financeTransactions=[];let financeParsedRows=[];let financeCategoriesLoaded=false;async function loadFinanceCategoriesAndVendorMap(){const[{data:cats},{data:vmap}]=await Promise.all([sb.from("finance_expense_categories").select("id, name, is_transfer, sort_order").order("sort_order"),sb.from("finance_vendor_category_map").select("id, pattern, match_type, category_id")]);financeCategories=cats||[];financeVendorMap=vmap||[];financeCategoriesLoaded=true;const invCatSel=$("financeInvoiceCategory");if(invCatSel)invCatSel.innerHTML='<option value="">Uncategorized</option>'+financeCategories.map(c=>`<option value="${c.id}">${escapeHtml(c.name)}</option>`).join("")}function financeCategoryLabel(categoryId){const c=financeCategories.find(c2=>c2.id===categoryId);return c?c.name:"Uncategorized"}function financeNormalizeVendor(s){return String(s||"").trim().toUpperCase()}function lookupVendorCategory(payee){const norm=financeNormalizeVendor(payee);if(!norm)return null;const exact=financeVendorMap.find(v=>v.match_type==="exact"&&v.pattern===norm);if(exact)return exact.category_id;const contains=financeVendorMap.find(v=>v.match_type==="contains"&&norm.includes(v.pattern));return contains?contains.category_id:null}async function financeDedupKey(row){const norm=s=>String(s||"").trim().toUpperCase().replace(/\s+/g," ");const raw=[row.account_number,row.txn_date,row.amount.toFixed(2),row.balance!=null?row.balance.toFixed(2):"",norm(row.payee),norm(row.description),norm(row.reference)].join("|");const buf=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(raw));return Array.from(new Uint8Array(buf)).map(b=>b.toString(16).padStart(2,"0")).join("")}const RELAY_CSV_COLUMNS=["Date","Payee","Account #","Transaction Type","Description","Reference","Status","Amount","Currency","Balance"];function parseRelayCsv(headerRow,dataRows){const norm=s=>String(s||"").trim().toLowerCase();const idx={};RELAY_CSV_COLUMNS.forEach(h=>{idx[h]=headerRow.findIndex(c=>norm(c)===norm(h))});if(idx["Date"]<0||idx["Amount"]<0||idx["Transaction Type"]<0||idx["Status"]<0){throw new Error("This doesn't look like a Relay statement export — missing required columns.")}return dataRows.map(r=>{const dateRaw=String(r[idx["Date"]]||"").trim();const[m,d,yRaw]=dateRaw.split("/").map(Number);const y=yRaw&&yRaw<100?yRaw<=68?2e3+yRaw:1900+yRaw:yRaw;const amountRaw=String(r[idx["Amount"]]??"").replace(/[$,]/g,"").trim();const balanceRaw=idx["Balance"]>=0?String(r[idx["Balance"]]??"").replace(/[$,]/g,"").trim():"";return{txn_date:y&&m&&d?`${y}-${String(m).padStart(2,"0")}-${String(d).padStart(2,"0")}`:null,payee:(r[idx["Payee"]]||"").trim()||null,payee_account_ref:idx["Account #"]>=0?(r[idx["Account #"]]||"").trim()||null:null,transaction_type:(r[idx["Transaction Type"]]||"").trim(),description:(r[idx["Description"]]||"").trim()||null,reference:(r[idx["Reference"]]||"").trim()||null,status:(r[idx["Status"]]||"").trim(),amount:amountRaw?parseFloat(amountRaw):NaN,currency:(idx["Currency"]>=0?(r[idx["Currency"]]||"").trim():"")||"USD",balance:balanceRaw!==""?parseFloat(balanceRaw):null}}).filter(row=>row.txn_date&&!Number.isNaN(row.amount))}$("financeUploadInput").addEventListener("change",async e=>{const file=e.target.files[0];const msg=$("financeUploadMsg");msg.style.display="none";$("financeUploadPreviewWrap").style.display="none";if(!file)return;try{if(!financeCategoriesLoaded)await loadFinanceCategoriesAndVendorMap();const fnameMatch=file.name.match(/#(\d{3,6})/);if(fnameMatch)$("financeAccountSelect").value=fnameMatch[1];const accountNumber=$("financeAccountSelect").value;const buf=await file.arrayBuffer();await ensureXlsx();const wb=XLSX.read(buf,{type:"array"});const sheet=wb.Sheets[wb.SheetNames[0]];const rows=XLSX.utils.sheet_to_json(sheet,{header:1,defval:"",raw:false});if(!rows.length){msg.textContent="That file has no rows.";msg.className="msg error";msg.style.display="block";return}const headerRow=rows[0].map(h=>String(h||"").trim());const dataRows=rows.slice(1).filter(r=>r.some(c=>String(c||"").trim()!==""));if(!dataRows.length){msg.textContent="No transaction rows found in this file.";msg.className="msg";msg.style.display="block";return}const parsed=parseRelayCsv(headerRow,dataRows).map(row=>({...row,account_number:accountNumber}));if(!parsed.length){msg.textContent="No usable transaction rows found in this file.";msg.className="msg error";msg.style.display="block";return}const transfersCat=financeCategories.find(c=>c.is_transfer);for(const row of parsed){row.dedup_key=await financeDedupKey(row);row.is_internal_transfer=["Receive-transfer","Spend-transfer"].includes(row.transaction_type);row.category_id=row.is_internal_transfer?transfersCat?transfersCat.id:null:lookupVendorCategory(row.payee)}const allKeys=parsed.map(r=>r.dedup_key);const{data:existing,error:existErr}=await sb.from("finance_bank_transactions").select("dedup_key").in("dedup_key",allKeys);if(existErr){msg.textContent=existErr.message;msg.className="msg error";msg.style.display="block";return}const existingKeys=new Set((existing||[]).map(r=>r.dedup_key));parsed.forEach(row=>{row.isNew=!existingKeys.has(row.dedup_key)});financeParsedRows=parsed;financeParsedRows.sourceFilename=file.name;renderFinancePreview()}catch(err){msg.textContent=`Couldn't read that file: ${err.message}`;msg.className="msg error";msg.style.display="block"}});function renderFinancePreview(){const newCount=financeParsedRows.filter(r=>r.isNew).length;const skipCount=financeParsedRows.length-newCount;$("financePreviewHead").innerHTML=`<tr><th>Status</th><th>Date</th><th>Payee</th><th>Description</th><th>Amount</th><th>Category</th></tr>`;$("financePreviewBody").innerHTML=financeParsedRows.map(r=>`
    <tr style="${r.isNew?"":"opacity:0.5;"}">
      <td class="mono" style="white-space:nowrap; color:${r.isNew?"var(--focus)":"var(--text-dim)"};">${r.isNew?"New":"Already imported"}</td>
      <td class="mono">${escapeHtml(r.txn_date)}</td>
      <td>${escapeHtml(r.payee||"—")}</td>
      <td style="max-width:220px;" title="${escapeHtml(r.description||"")}">${escapeHtml((r.description||"").slice(0,40))}</td>
      <td class="mono" style="color:${r.amount<0?"var(--danger)":"var(--good)"};">${r.amount<0?"-":"+"}$${Math.abs(r.amount).toFixed(2)}</td>
      <td>${escapeHtml(financeCategoryLabel(r.category_id))}</td>
    </tr>
  `).join("");$("financeImportCount").textContent=`${newCount} new, ${skipCount} already imported (skipped)`;$("financeConfirmImportBtn").disabled=newCount===0;$("financeUploadPreviewWrap").style.display=""}$("financeCancelImportBtn").addEventListener("click",()=>{financeParsedRows=[];$("financeUploadPreviewWrap").style.display="none";$("financeUploadInput").value=""});$("financeConfirmImportBtn").addEventListener("click",async()=>{const msg=$("financeUploadMsg");const btn=$("financeConfirmImportBtn");const{data:{user}}=await sb.auth.getUser();const newRows=financeParsedRows.filter(r=>r.isNew);if(!newRows.length)return;btn.disabled=true;btn.textContent="Importing…";const sourceFilename=financeParsedRows.sourceFilename||"unknown.csv";const payload=newRows.map(r=>({account_number:r.account_number,txn_date:r.txn_date,payee:r.payee,payee_account_ref:r.payee_account_ref,transaction_type:r.transaction_type,description:r.description,reference:r.reference,status:r.status,amount:r.amount,currency:r.currency,balance:r.balance,category_id:r.category_id,is_internal_transfer:r.is_internal_transfer,source_filename:sourceFilename,imported_by:user?user.id:null,dedup_key:r.dedup_key}));const{error}=await sb.from("finance_bank_transactions").upsert(payload,{onConflict:"dedup_key",ignoreDuplicates:true});btn.disabled=false;btn.textContent="Import";if(error){msg.textContent=error.message;msg.className="msg error";msg.style.display="block";return}msg.textContent=`Imported ${newRows.length} transaction(s) from ${sourceFilename}.`;msg.className="msg";msg.style.display="block";logActivity("finance.import_transactions",null,null,`Imported ${newRows.length} bank transaction(s) from ${sourceFilename}`);financeParsedRows=[];$("financeUploadPreviewWrap").style.display="none";$("financeUploadInput").value="";await loadFinanceData()});if($("financeRelayfiSyncBtn"))$("financeRelayfiSyncBtn").addEventListener("click",async()=>{const msg=$("financeRelayfiSyncMsg");const btn=$("financeRelayfiSyncBtn");msg.style.display="none";btn.disabled=true;btn.textContent="Syncing…";try{const result=await callGmailApi({action:"sync_relayfi_statements"});if(!result.ok)throw new Error(result.error||"Sync failed.");if(!result.emailsFound){msg.textContent="No RelayFi statement email found. Send one from RelayFi (Accounts → Statements → Export → Email statements) to the connected invoices inbox, then sync again.";msg.className="msg"}else{const parts=[`Found ${result.emailsFound} statement email${result.emailsFound===1?"":"s"}`];parts.push(`imported ${result.transactionsImported} new transaction${result.transactionsImported===1?"":"s"}`);if(result.transactionsSkipped)parts.push(`${result.transactionsSkipped} already imported`);if(result.errors&&result.errors.length)parts.push(`${result.errors.length} issue${result.errors.length===1?"":"s"} — see below`);msg.textContent=parts.join(", ")+"."+(result.errors&&result.errors.length?"\n"+result.errors.join("\n"):"");msg.className=result.errors&&result.errors.length&&!result.transactionsImported?"msg error":"msg"}msg.style.display="block";if(result.transactionsImported){logActivity("finance.relayfi_statement_sync",null,null,`Synced RelayFi statement email — ${result.transactionsImported} transaction(s) imported`);await loadFinanceData()}}catch(err){msg.textContent=err.message||String(err);msg.className="msg error";msg.style.display="block"}finally{btn.disabled=false;btn.textContent="Sync bank statement email"}});const FINANCE_DATE_RANGE_STORAGE_KEY="ptai_finance_date_range";function restoreFinanceDateRange(){try{const saved=JSON.parse(localStorage.getItem(FINANCE_DATE_RANGE_STORAGE_KEY)||"null");if(saved&&saved.from)$("financeDateFromFilter").value=saved.from;if(saved&&saved.to)$("financeDateToFilter").value=saved.to}catch(e){}}function saveFinanceDateRange(){try{localStorage.setItem(FINANCE_DATE_RANGE_STORAGE_KEY,JSON.stringify({from:$("financeDateFromFilter").value,to:$("financeDateToFilter").value}))}catch(e){}}$("financeDateFromFilter").addEventListener("change",()=>{saveFinanceDateRange();loadFinanceData()});$("financeDateToFilter").addEventListener("change",()=>{saveFinanceDateRange();loadFinanceData()});$("financeRefreshBtn").addEventListener("click",()=>loadFinanceData());$("financeExcludeTransfersToggle").addEventListener("change",renderFinanceReport);$("financeExcludeReturnedToggle").addEventListener("change",renderFinanceReport);$("financeDeductibleOnlyToggle").addEventListener("change",renderFinanceReport);async function loadFinanceData(){if(!$("financeDateFromFilter").value&&!$("financeDateToFilter").value)restoreFinanceDateRange();if(!financeCategoriesLoaded)await loadFinanceCategoriesAndVendorMap();const from=$("financeDateFromFilter").value;const to=$("financeDateToFilter").value;let query=sb.from("finance_bank_transactions").select("*").order("txn_date",{ascending:false});if(from)query=query.gte("txn_date",from);if(to)query=query.lte("txn_date",to);const{data,error}=await query;if(error){console.error("Failed to load finance transactions:",error);financeTransactions=[]}else financeTransactions=data||[];renderFinanceReport()}function renderFinanceReport(){const excludeTransfers=$("financeExcludeTransfersToggle").checked;const excludeReturned=$("financeExcludeReturnedToggle").checked;const deductibleOnly=$("financeDeductibleOnlyToggle").checked;const filtered=financeTransactions.filter(t=>(!excludeTransfers||!t.is_internal_transfer)&&(!excludeReturned||t.status==="SETTLED")&&(!deductibleOnly||t.is_tax_deductible===true));let spend=0,deposits=0,deductible=0;const byCategory={};const byMonth={};filtered.forEach(t=>{const amt=Number(t.amount)||0;if(amt<0)spend+=-amt;else deposits+=amt;if(amt<0&&t.is_tax_deductible===true)deductible+=-amt;const catName=financeCategoryLabel(t.category_id);const catBucket=byCategory[catName]||={spend:0,deposits:0};if(amt<0)catBucket.spend+=-amt;else catBucket.deposits+=amt;const monthKey=(t.txn_date||"").slice(0,7);const monthBucket=byMonth[monthKey]||={spend:0,deposits:0};if(amt<0)monthBucket.spend+=-amt;else monthBucket.deposits+=amt});$("financeStatSpend").textContent=`$${spend.toFixed(2)}`;$("financeStatDeposits").textContent=`$${deposits.toFixed(2)}`;$("financeStatNet").textContent=`$${(deposits-spend).toFixed(2)}`;$("financeStatDeductible").textContent=`$${deductible.toFixed(2)}`;$("financeStatCount").textContent=filtered.length;const catRows=Object.entries(byCategory).filter(([,v])=>v.spend>0).sort((a,b)=>b[1].spend-a[1].spend);$("financeCategoryBody").innerHTML=catRows.length?catRows.map(([name,v])=>`<tr><td>${escapeHtml(name)}</td><td class="mono">$${v.spend.toFixed(2)}</td><td class="mono">${spend>0?(v.spend/spend*100).toFixed(1):"0.0"}%</td></tr>`).join(""):`<tr><td colspan="3" class="empty">No spend in this range.</td></tr>`;const monthRows=Object.entries(byMonth).sort((a,b)=>a[0].localeCompare(b[0]));$("financeMonthlyBody").innerHTML=monthRows.length?monthRows.map(([month,v])=>`<tr><td class="mono">${escapeHtml(month)}</td><td class="mono" style="color:var(--danger);">$${v.spend.toFixed(2)}</td><td class="mono" style="color:var(--good);">$${v.deposits.toFixed(2)}</td><td class="mono">$${(v.deposits-v.spend).toFixed(2)}</td></tr>`).join(""):`<tr><td colspan="4" class="empty">No transactions in this range.</td></tr>`;const catOptionsHtml='<option value="">Uncategorized</option>'+financeCategories.map(c=>`<option value="${c.id}">${escapeHtml(c.name)}</option>`).join("");$("financeTxnBody").innerHTML=filtered.length?filtered.map(t=>`
    <tr data-id="${t.id}">
      <td class="mono">${escapeHtml(t.txn_date)}</td>
      <td class="mono">${escapeHtml(t.account_number)}</td>
      <td>${escapeHtml(t.payee||"—")}</td>
      <td style="max-width:200px;" title="${escapeHtml(t.description||"")}">${escapeHtml((t.description||"").slice(0,40))}</td>
      <td class="mono" style="color:${Number(t.amount)<0?"var(--danger)":"var(--good)"};">${Number(t.amount)<0?"-":"+"}$${Math.abs(Number(t.amount)).toFixed(2)}</td>
      <td><select class="finance-txn-category-select" data-id="${t.id}">${catOptionsHtml}</select></td>
      <td style="text-align:center;"><input type="checkbox" class="finance-txn-transfer-toggle" data-id="${t.id}" ${t.is_internal_transfer?"checked":""}></td>
      <td>
        <select class="finance-txn-deductible-select" data-id="${t.id}">
          <option value="">?</option>
          <option value="true" ${t.is_tax_deductible===true?"selected":""}>Yes</option>
          <option value="false" ${t.is_tax_deductible===false?"selected":""}>No</option>
        </select>
      </td>
    </tr>
  `).join(""):`<tr><td colspan="8" class="empty">No transactions in this range.</td></tr>`;filtered.forEach(t=>{const sel=document.querySelector(`.finance-txn-category-select[data-id="${t.id}"]`);if(sel)sel.value=t.category_id||""});document.querySelectorAll(".finance-txn-category-select").forEach(sel=>{sel.addEventListener("change",async()=>{const id=sel.dataset.id;const categoryId=sel.value||null;const txn=financeTransactions.find(t=>t.id===id);const remember=categoryId&&txn&&txn.payee&&confirm(`Remember "${txn.payee}" as ${financeCategoryLabel(categoryId)} for future imports?`);const{error}=await sb.from("finance_bank_transactions").update({category_id:categoryId,updated_at:new Date().toISOString()}).eq("id",id);if(error){alert(error.message);return}if(txn)txn.category_id=categoryId;logActivity("finance.txn_recategorize","finance_bank_transaction",id,`Set category to "${financeCategoryLabel(categoryId)}" for ${txn?txn.payee||txn.txn_date:id}`);if(remember){const{data:{user}}=await sb.auth.getUser();await sb.from("finance_vendor_category_map").upsert({pattern:financeNormalizeVendor(txn.payee),match_type:"exact",category_id:categoryId,created_by:user?user.id:null},{onConflict:"pattern,match_type"});await loadFinanceCategoriesAndVendorMap()}renderFinanceReport()})});document.querySelectorAll(".finance-txn-transfer-toggle").forEach(cb=>{cb.addEventListener("change",async()=>{const id=cb.dataset.id;const{error}=await sb.from("finance_bank_transactions").update({is_internal_transfer:cb.checked,updated_at:new Date().toISOString()}).eq("id",id);if(error){alert(error.message);cb.checked=!cb.checked;return}const txn=financeTransactions.find(t=>t.id===id);if(txn)txn.is_internal_transfer=cb.checked;logActivity("finance.txn_recategorize","finance_bank_transaction",id,`${cb.checked?"Marked":"Unmarked"} ${txn?txn.payee||txn.txn_date:id} as an internal transfer`);renderFinanceReport()})});document.querySelectorAll(".finance-txn-deductible-select").forEach(sel=>{sel.addEventListener("change",async()=>{const id=sel.dataset.id;const val=sel.value===""?null:sel.value==="true";const{error}=await sb.from("finance_bank_transactions").update({is_tax_deductible:val,updated_at:new Date().toISOString()}).eq("id",id);if(error){alert(error.message);return}const txn=financeTransactions.find(t=>t.id===id);if(txn)txn.is_tax_deductible=val;logActivity("finance.txn_deductible_set","finance_bank_transaction",id,`Set tax-deductible to "${val===null?"?":val?"Yes":"No"}" for ${txn?txn.payee||txn.txn_date:id}`);renderFinanceReport()})})}$("financeExportCsvBtn").addEventListener("click",()=>{const excludeTransfers=$("financeExcludeTransfersToggle").checked;const excludeReturned=$("financeExcludeReturnedToggle").checked;const filtered=financeTransactions.filter(t=>(!excludeTransfers||!t.is_internal_transfer)&&(!excludeReturned||t.status==="SETTLED"));const headers=["Date","Account","Payee","Description","Amount","Category","Transfer","Status","Tax Deductible"];const rows=filtered.map(t=>[t.txn_date,t.account_number,t.payee||"",t.description||"",Number(t.amount).toFixed(2),financeCategoryLabel(t.category_id),t.is_internal_transfer?"Yes":"No",t.status,t.is_tax_deductible===true?"Yes":t.is_tax_deductible===false?"No":""]);exportCSV(`finance-transactions-${todayDateInputValue()}.csv`,headers,rows);logActivity("finance.export_csv",null,null,`Exported ${rows.length} finance transaction(s) to CSV`)});$("financeExportPdfBtn").addEventListener("click",()=>{const excludeTransfers=$("financeExcludeTransfersToggle").checked;const excludeReturned=$("financeExcludeReturnedToggle").checked;const filtered=financeTransactions.filter(t=>(!excludeTransfers||!t.is_internal_transfer)&&(!excludeReturned||t.status==="SETTLED"));const headers=["Date","Account","Payee","Description","Amount","Category"];const rows=filtered.map(t=>[t.txn_date,t.account_number,t.payee||"",(t.description||"").slice(0,40),Number(t.amount).toFixed(2),financeCategoryLabel(t.category_id)]);exportPDF("Finance — Transactions",headers,rows)});let financeInvoices=[];async function loadFinanceInvoices(){if(!financeCategoriesLoaded)await loadFinanceCategoriesAndVendorMap();const search=($("financeInvoiceSearchFilter").value||"").trim().toLowerCase();const from=$("financeInvoiceDateFromFilter").value;const to=$("financeInvoiceDateToFilter").value;let query=sb.from("finance_invoices").select("*, matched_transaction:finance_bank_transactions(id, payee, txn_date, amount)").order("invoice_date",{ascending:false});if(from)query=query.gte("invoice_date",from);if(to)query=query.lte("invoice_date",to);const{data,error}=await query;if(error){console.error("Failed to load finance invoices:",error);financeInvoices=[]}else financeInvoices=(data||[]).filter(inv=>!search||(inv.vendor||"").toLowerCase().includes(search));renderFinanceInvoices()}$("financeInvoiceRefreshBtn").addEventListener("click",()=>loadFinanceInvoices());$("financeInvoiceSearchFilter").addEventListener("input",debounce(()=>loadFinanceInvoices(),250));$("financeInvoiceDateFromFilter").addEventListener("change",()=>loadFinanceInvoices());$("financeInvoiceDateToFilter").addEventListener("change",()=>loadFinanceInvoices());function financeInvoiceDocTypeLabel(filename){const f=String(filename||"").trim();if(/^credit[\s-]?note/i.test(f))return"Credit Note";if(/^refund/i.test(f))return"Refund";if(/^invoice/i.test(f))return"Invoice";if(/^receipt/i.test(f))return"Receipt";return null}function groupFinanceInvoicesForDisplay(invoices){const groupsByKey=new Map;const order=[];invoices.forEach(inv=>{const key=inv.amount!=null&&inv.invoice_date?`${inv.vendor||""}|${Number(inv.amount).toFixed(2)}|${inv.invoice_date}`:`__solo_${inv.id}`;if(!groupsByKey.has(key)){groupsByKey.set(key,[]);order.push(key)}groupsByKey.get(key).push(inv)});return order.map(key=>{const docs=groupsByKey.get(key);const primary=docs.find(d=>financeInvoiceDocTypeLabel(d.original_filename)==="Invoice")||docs[0];const linked=docs.find(d=>d.matched_transaction_id)||null;return{key,docs,primary,actionDoc:linked||primary,matchedTransaction:linked?linked.matched_transaction:null,matchedTransactionId:linked?linked.matched_transaction_id:null,categoryId:(docs.find(d=>d.category_id)||{}).category_id||null}})}function renderFinanceInvoices(){const grid=$("financeInvoiceGrid");const empty=$("financeInvoiceEmpty");const groups=groupFinanceInvoicesForDisplay(financeInvoices);const linkSummaryEl=$("financeInvoiceLinkSummary");if(linkSummaryEl){const linkedGroups=groups.filter(g=>g.matchedTransactionId).length;linkSummaryEl.textContent=financeInvoices.length?`${linkedGroups} of ${groups.length} linked to a bank transaction`:""}if(!financeInvoices.length){grid.innerHTML="";empty.style.display="block";return}empty.style.display="none";grid.innerHTML=groups.map(g=>{const inv=g.primary;const docsHtml=g.docs.map(d=>{const label=financeInvoiceDocTypeLabel(d.original_filename)||d.original_filename;return`
        <div style="display:flex; justify-content:space-between; align-items:center; gap:8px; font-size:11px; padding:3px 0;">
          <span style="color:var(--text-2); overflow:hidden; text-overflow:ellipsis; white-space:nowrap;" title="${escapeHtml(d.original_filename)}">${escapeHtml(label)}</span>
          <span style="display:flex; gap:4px; flex-shrink:0;">
            <button class="btn-secondary finance-invoice-download-btn" data-doc-id="${d.id}" style="width:auto; padding:3px 8px; font-size:10px;">Download</button>
            <button class="finance-invoice-delete-btn" data-doc-id="${d.id}" style="background:transparent; border:none; color:var(--danger); cursor:pointer; font-size:10px; padding:3px 4px;">Delete</button>
          </span>
        </div>`}).join("");return`
    <div class="card" style="padding:14px;" data-group-key="${escapeHtml(g.key)}">
      <div style="font-weight:700; font-size:13px; margin-bottom:4px;">${escapeHtml(inv.vendor||"Unknown vendor")}</div>
      <div style="font-size:11px; color:var(--text-dim); margin-bottom:8px;">${inv.invoice_date?escapeHtml(inv.invoice_date):"—"}${inv.amount!=null?` · $${Number(inv.amount).toFixed(2)}`:""}</div>
      <div style="font-size:11px; color:var(--text-2); margin-bottom:4px;">${escapeHtml(financeCategoryLabel(g.categoryId))}</div>
      ${g.matchedTransaction?`<div style="font-size:10px; color:var(--good); margin-bottom:4px;" title="Matched by amount (±2¢) and date (±10 days) — verify this is really the same purchase.">&#10003; Linked to ${escapeHtml(g.matchedTransaction.payee||"a bank transaction")} — ${escapeHtml(g.matchedTransaction.txn_date)} · $${Math.abs(Number(g.matchedTransaction.amount)).toFixed(2)}</div>`:g.matchedTransactionId?'<div style="font-size:10px; color:var(--danger); margin-bottom:4px;">&#9888; Linked to a bank transaction that no longer exists</div>':""}
      ${inv.notes?`<div style="font-size:11px; color:var(--text-2); margin-bottom:4px;">${escapeHtml(inv.notes)}</div>`:""}
      <div style="border-top:1px solid var(--border); margin-top:8px; padding-top:6px;">${docsHtml}</div>
      <div style="display:flex; gap:8px; flex-wrap:wrap; margin-top:8px;">
        <button class="btn-secondary finance-invoice-reconcile-btn" data-action-id="${g.actionDoc.id}" style="width:auto; padding:5px 10px; font-size:11px;">${g.matchedTransactionId?"Change link":"Link transaction"}</button>
        ${g.matchedTransactionId?`<button class="btn-secondary finance-invoice-unlink-btn" data-action-id="${g.actionDoc.id}" style="width:auto; padding:5px 10px; font-size:11px;">Unlink</button>`:""}
      </div>
    </div>
  `}).join("");grid.querySelectorAll(".finance-invoice-download-btn").forEach(btn=>{btn.addEventListener("click",async()=>{const id=btn.dataset.docId;const inv=financeInvoices.find(i=>i.id===id);if(!inv)return;const{data,error}=await sb.storage.from("finance-invoices").createSignedUrl(inv.storage_path,3600);if(error){alert(error.message);return}downloadFileToDisk(data.signedUrl,inv.original_filename)})});grid.querySelectorAll(".finance-invoice-delete-btn").forEach(btn=>{btn.addEventListener("click",async()=>{const id=btn.dataset.docId;const inv=financeInvoices.find(i=>i.id===id);if(!inv||!confirm(`Delete this archived document (${inv.vendor||inv.original_filename})? This can't be undone.`))return;await sb.storage.from("finance-invoices").remove([inv.storage_path]);const{error}=await sb.from("finance_invoices").delete().eq("id",id);if(error){alert(error.message);return}logActivity("finance.invoice_delete","finance_invoice",id,`Deleted archived invoice — ${inv.vendor||inv.original_filename}`);await loadFinanceInvoices()})});grid.querySelectorAll(".finance-invoice-reconcile-btn").forEach(btn=>{btn.addEventListener("click",()=>openFinanceReconcileModal(btn.dataset.actionId))});grid.querySelectorAll(".finance-invoice-unlink-btn").forEach(btn=>{btn.addEventListener("click",async()=>{const id=btn.dataset.actionId;const inv=financeInvoices.find(i=>i.id===id);if(!inv||!confirm(`Unlink this invoice (${inv.vendor||inv.original_filename}) from its matched bank transaction?`))return;const{error}=await sb.from("finance_invoices").update({matched_transaction_id:null,updated_at:new Date().toISOString()}).eq("id",id);if(error){alert(error.message);return}logActivity("finance.invoice_unlink","finance_invoice",id,`Unlinked invoice (${inv.vendor||inv.original_filename}) from its matched bank transaction`);await loadFinanceInvoices()})})}async function fileToBase64(file){const bytes=new Uint8Array(await file.arrayBuffer());let binary="";const CHUNK=32768;for(let i=0;i<bytes.length;i+=CHUNK)binary+=String.fromCharCode.apply(null,bytes.subarray(i,i+CHUNK));return btoa(binary)}$("financeInvoiceUploadBtn").addEventListener("click",async()=>{const msg=$("financeInvoiceMsg");msg.style.display="none";const fileInput=$("financeInvoiceFileInput");const file=fileInput.files&&fileInput.files[0];if(!file){msg.textContent="Choose a file first.";msg.className="msg error";msg.style.display="block";return}const btn=$("financeInvoiceUploadBtn");btn.disabled=true;btn.textContent="Archiving…";try{const contentBase64=await fileToBase64(file);const result=await callGmailApi({action:"manual_import_invoice",filename:file.name,contentBase64,vendor:$("financeInvoiceVendor").value.trim()||null,amount:$("financeInvoiceAmount").value?parseFloat($("financeInvoiceAmount").value):null,currency:"USD",invoiceDate:$("financeInvoiceDate").value||null,categoryId:$("financeInvoiceCategory").value||null,notes:$("financeInvoiceNotes").value.trim()||null});if(!result.ok)throw new Error(result.error||"Failed to archive the invoice.");logActivity("finance.invoice_upload","finance_invoice",result.id,`Archived invoice — ${$("financeInvoiceVendor").value.trim()||file.name}`);["financeInvoiceVendor","financeInvoiceAmount","financeInvoiceDate","financeInvoiceNotes"].forEach(id=>{$(id).value=""});$("financeInvoiceCategory").value="";fileInput.value="";msg.textContent=result.duplicate?"This exact file is already archived — nothing new was added.":`Invoice archived.${result.relayfiForwarded?" Forwarded to RelayFi.":""}`;msg.className="msg";msg.style.display="block";await loadFinanceInvoices()}catch(err){msg.textContent=err.message||String(err);msg.className="msg error";msg.style.display="block"}finally{btn.disabled=false;btn.textContent="Archive invoice"}});let taxesGmailInvoiceConnections=[];async function loadTaxesGmailInvoiceConnection(){const statusEl=$("taxesGmailInvoiceStatus");const connectBtn=$("taxesGmailInvoiceConnectBtn");const syncBtn=$("taxesGmailInvoiceSyncBtn");statusEl.textContent="Loading…";const result=await callGmailApi({action:"list_connections"});if(!result.ok){statusEl.textContent="Only Owner/Admin can view or manage this connection.";connectBtn.style.display="none";syncBtn.style.display="none";return}taxesGmailInvoiceConnections=(result.connections||[]).filter(c=>c.purpose==="invoices");connectBtn.style.display="";if(taxesGmailInvoiceConnections.length){statusEl.innerHTML="Connected: "+taxesGmailInvoiceConnections.map(c=>`<b>${escapeHtml(c.email_address)}</b> <button class="taxes-gmail-invoice-disconnect-btn" data-email="${escapeHtml(c.email_address)}" style="background:transparent; border:none; color:var(--danger); cursor:pointer; font-size:11px; text-decoration:underline; padding:0;">disconnect</button>`).join(" &nbsp;·&nbsp; ");syncBtn.style.display="";statusEl.querySelectorAll(".taxes-gmail-invoice-disconnect-btn").forEach(btn=>{btn.addEventListener("click",async()=>{const email=btn.dataset.email;if(!confirm(`Disconnect ${email}? Its invoices will stop syncing until it's reconnected.`))return;const r=await callGmailApi({action:"disconnect",emailAddress:email});if(!r.ok){alert(r.error||"Failed to disconnect.");return}logActivity("finance.gmail_invoice_disconnect",null,null,`Disconnected Gmail invoice inbox ${email}`);await loadTaxesGmailInvoiceConnection()})})}else{statusEl.textContent="Not connected yet.";syncBtn.style.display="none"}}if($("taxesGmailInvoiceConnectBtn"))$("taxesGmailInvoiceConnectBtn").addEventListener("click",async()=>{const msg=$("taxesGmailInvoiceMsg");const btn=$("taxesGmailInvoiceConnectBtn");msg.style.display="none";btn.disabled=true;const originalLabel=btn.textContent;btn.textContent="Starting…";const result=await callGmailApi({action:"create_oauth_state",purpose:"invoices"});btn.disabled=false;btn.textContent=originalLabel;if(!result.ok){msg.textContent=result.error||"Failed to start the connect flow.";msg.className="msg error";msg.style.display="block";return}window.open(gmailConnectUrl(result.state,GMAIL_INVOICE_SCOPES),"_blank","noopener");msg.textContent="Finish signing in with the invoice inbox account in the tab that just opened (pick a different account there if you're adding another one), then click Refresh here (or reopen Invoice Archive) once it says Connected.";msg.className="msg";msg.style.display="block"});if($("taxesGmailInvoiceSyncBtn"))$("taxesGmailInvoiceSyncBtn").addEventListener("click",async()=>{const msg=$("taxesGmailInvoiceMsg");const btn=$("taxesGmailInvoiceSyncBtn");msg.style.display="none";btn.disabled=true;let totalImported=0,totalSkipped=0,totalScanned=0,totalForwarded=0,totalForwardFailed=0,pageToken=null,pages=0;const allErrors=[];const MAX_PAGES=8;try{do{btn.textContent=pages===0?"Syncing…":`Syncing… (${totalImported} found so far)`;const result=await callGmailApi({action:"sync_invoice_emails",pageToken:pageToken||void 0});if(!result.ok)throw new Error(result.error||"Sync failed.");totalImported+=result.imported||0;totalSkipped+=result.skipped||0;totalScanned+=result.scanned||0;totalForwarded+=result.relayfiForwarded||0;totalForwardFailed+=result.relayfiForwardFailed||0;if(result.errors&&result.errors.length)allErrors.push(...result.errors);pageToken=result.nextPageToken;pages++}while(pageToken&&pages<MAX_PAGES);const summary=totalImported?`Archived ${totalImported} new invoice${totalImported===1?"":"s"} (scanned ${totalScanned} email${totalScanned===1?"":"s"}, skipped ${totalSkipped} already-imported/unreadable attachment${totalSkipped===1?"":"s"}) — forwarded ${totalForwarded} to RelayFi${totalForwardFailed?` (${totalForwardFailed} forward${totalForwardFailed===1?"":"s"} failed, still archived here though)`:""}${pageToken?". More may be available, click Sync now again to continue.":"."}`:`No new invoices found (scanned ${totalScanned} email${totalScanned===1?"":"s"}).`;msg.textContent=summary+(allErrors.length?`
${allErrors.join("\n")}`:"");msg.className="msg";msg.style.display="block";logActivity("finance.gmail_invoice_sync",null,null,`Synced Gmail invoices — ${totalImported} archived, ${totalScanned} scanned`);await loadFinanceInvoices()}catch(err){msg.textContent=err.message||String(err);msg.className="msg error";msg.style.display="block"}finally{btn.disabled=false;btn.textContent="Sync now"}});async function runTaxesInvoiceBackfill(opts){const msg=$("taxesInvoiceBackfillMsg");const btn=$(opts.btnId);msg.style.display="none";btn.disabled=true;const originalLabel=btn.textContent;const totals={};opts.totalFields.forEach(f=>{totals[f]=0});let remaining=null,calls=0;const MAX_CALLS=20;try{do{btn.textContent=calls===0?"Working…":`Working… (${totals[opts.totalFields[0]]} so far)`;const result=await callGmailApi({action:opts.action});if(!result.ok)throw new Error(result.error||"Failed.");opts.totalFields.forEach(f=>{totals[f]+=result[f]||0});remaining=result.remaining??0;calls++}while(remaining>0&&calls<MAX_CALLS);msg.textContent=opts.formatMsg(totals,remaining);msg.className="msg";msg.style.display="block";if(totals[opts.totalFields[0]])await loadFinanceInvoices()}catch(err){msg.textContent=err.message||String(err);msg.className="msg error";msg.style.display="block"}finally{btn.disabled=false;btn.textContent=originalLabel}}if($("taxesExtractAmountsBtn"))$("taxesExtractAmountsBtn").addEventListener("click",()=>runTaxesInvoiceBackfill({action:"backfill_extract_amounts",btnId:"taxesExtractAmountsBtn",totalFields:["updated","notFound","failed"],formatMsg:(t,remaining)=>`Extracted an amount for ${t.updated} invoice${t.updated===1?"":"s"}${t.notFound?`, ${t.notFound} had no detectable total`:""}${t.failed?`, ${t.failed} failed to read`:""}.${remaining?` ${remaining} more to go — click again to continue.`:""}`}));if($("taxesBackfillRelayfiBtn"))$("taxesBackfillRelayfiBtn").addEventListener("click",()=>runTaxesInvoiceBackfill({action:"backfill_relayfi_forward",btnId:"taxesBackfillRelayfiBtn",totalFields:["forwarded","failed"],formatMsg:(t,remaining)=>`Forwarded ${t.forwarded} invoice${t.forwarded===1?"":"s"} to RelayFi${t.failed?`, ${t.failed} failed`:""}.${remaining?` ${remaining} more to go — click again to continue.`:""}`}));let financeReconcileInvoiceId=null;async function openFinanceReconcileModal(invoiceId){financeReconcileInvoiceId=invoiceId;const inv=financeInvoices.find(i=>i.id===invoiceId);const listEl=$("financeReconcileList");$("financeReconcileModalMsg").style.display="none";$("financeReconcileModalOverlay").classList.add("active");if(!inv||inv.amount==null||!inv.invoice_date){listEl.innerHTML='<div style="font-size:12px; color:var(--text-dim); padding:10px 0;">This invoice needs an amount and date before it can be matched.</div>';return}listEl.innerHTML='<div style="font-size:12px; color:var(--text-dim); padding:10px 0;">Searching…</div>';const invDateMs=new Date(inv.invoice_date).getTime();const rangeStart=new Date(invDateMs-10*864e5).toISOString().slice(0,10);const rangeEnd=new Date(invDateMs+10*864e5).toISOString().slice(0,10);const{data:windowTxns,error}=await sb.from("finance_bank_transactions").select("id, payee, txn_date, amount, is_internal_transfer").gte("txn_date",rangeStart).lte("txn_date",rangeEnd);if(error){listEl.innerHTML=`<div style="font-size:12px; color:var(--danger); padding:10px 0;">Failed to search for matches: ${escapeHtml(error.message)}</div>`;return}const takenTransactionIds=new Set(financeInvoices.filter(i=>i.id!==invoiceId&&i.matched_transaction_id).map(i=>i.matched_transaction_id));const matches=(windowTxns||[]).filter(t=>{if(t.is_internal_transfer)return false;if(takenTransactionIds.has(t.id))return false;const amtDiff=Math.abs(Math.abs(Number(t.amount))-Math.abs(Number(inv.amount)));return amtDiff<=.02});listEl.innerHTML=matches.length?matches.map(t=>`
      <div class="finance-reconcile-row" data-id="${t.id}" style="display:flex; justify-content:space-between; align-items:center; padding:8px 4px; border-bottom:1px solid var(--border); cursor:pointer;">
        <div>
          <div style="font-size:12px;">${escapeHtml(t.payee||"—")}</div>
          <div style="font-size:10px; color:var(--text-dim);">${escapeHtml(t.txn_date)}</div>
        </div>
        <div class="mono" style="font-size:12px;">$${Math.abs(Number(t.amount)).toFixed(2)}</div>
      </div>
    `).join(""):`<div style="font-size:12px; color:var(--text-dim); padding:10px 0;">No bank transaction within 10 days and 2 cents of this invoice's amount was found. It may not have posted/synced yet — try again once it has.</div>`;listEl.querySelectorAll(".finance-reconcile-row").forEach(row=>{row.addEventListener("click",async()=>{const{error:linkErr}=await sb.from("finance_invoices").update({matched_transaction_id:row.dataset.id,updated_at:new Date().toISOString()}).eq("id",financeReconcileInvoiceId);if(linkErr){$("financeReconcileModalMsg").textContent=linkErr.message;$("financeReconcileModalMsg").style.display="block";return}const linkedInv=financeInvoices.find(i=>i.id===financeReconcileInvoiceId);logActivity("finance.invoice_reconcile","finance_invoice",financeReconcileInvoiceId,`Linked invoice (${linkedInv?linkedInv.vendor||linkedInv.original_filename:financeReconcileInvoiceId}) to a bank transaction`);closeFinanceReconcileModal();await loadFinanceInvoices()})})}function closeFinanceReconcileModal(){$("financeReconcileModalOverlay").classList.remove("active");financeReconcileInvoiceId=null}$("financeReconcileCancelBtn").addEventListener("click",closeFinanceReconcileModal);$("financeReconcileModalOverlay").addEventListener("click",e=>{if(e.target.id==="financeReconcileModalOverlay")closeFinanceReconcileModal()});const BILLING_AK_DATA_RATE=.05;const BILLING_AK_DIALER_RATE_BY_CADENCE={monthly:200,biweekly:100,weekly:50};const BILLING_AK_CADENCE_DIVISOR={monthly:1,biweekly:2,weekly:4};const BILLING_AK_FULL_TIME_CALLER_MONTHLY_RECORDS=1e4;const BILLING_AK_PART_TIME_CALLER_MONTHLY_RECORDS=7500;function billingAkDetectCadence(text){if(/bi-?\s*weekly/.test(text))return"biweekly";if(/\bweekly\b/.test(text))return"weekly";return"monthly"}function billingAkExtractUnits(text,pattern){let total=0;const consumed=[];const numbered=new RegExp(`(\\d+(?:\\.\\d+)?)\\s*(?:${pattern})\\b`,"gi");let m;while(m=numbered.exec(text)){total+=parseFloat(m[1]);consumed.push([m.index,m.index+m[0].length])}const bare=new RegExp(`(?:${pattern})\\b`,"gi");while(m=bare.exec(text)){if(!consumed.some(([s,e])=>m.index>=s&&m.index<e))total+=1}return total}function billingAkExtractDataRecords(text){let total=0;const consumed=[];const patterns=[[/(\d+(?:\.\d+)?)\s*k\s*data\s*records?\b/gi,1e3],[/(\d+(?:\.\d+)?)\s*k\s*records?\b/gi,1e3],[/(\d+(?:\.\d+)?)\s*k\s*data\b/gi,1e3],[/(\d+(?:\.\d+)?)\s*k\s*leads?\b/gi,1e3],[/(\d+(?:\.\d+)?)\s*data\s*records?\b/gi,1],[/(\d+(?:\.\d+)?)\s*records?\b/gi,1],[/(\d+(?:\.\d+)?)\s*leads?\b/gi,1],[/(\d+(?:\.\d+)?)\s*data\b/gi,1]];for(const[re,mult]of patterns){let m;while(m=re.exec(text)){if(consumed.some(([s,e])=>m.index>=s&&m.index<e))continue;total+=parseFloat(m[1])*mult;consumed.push([m.index,m.index+m[0].length])}}return total}function billingAkExtractCallerBreakdown(text){let total=0;let partTime=0;const consumed=[];const numbered=/(\d+(?:\.\d+)?)\s*(?:(pt|part[-\s]?time)\s*)?callers?\b/gi;let m;while(m=numbered.exec(text)){const n=parseFloat(m[1]);total+=n;if(m[2])partTime+=n;consumed.push([m.index,m.index+m[0].length])}const bare=/(?:(pt|part[-\s]?time)\s*)?callers?\b/gi;while(m=bare.exec(text)){if(consumed.some(([s,e])=>m.index>=s&&m.index<e))continue;total+=1;if(m[1])partTime+=1}const partTimeUnits=Math.min(partTime,total);return{totalUnits:total,fullTimeUnits:Math.max(0,total-partTimeUnits),partTimeUnits}}function billingAkImplicitDataRecords(fullTimeUnits,partTimeUnits,cadence){const monthlyRecords=fullTimeUnits*BILLING_AK_FULL_TIME_CALLER_MONTHLY_RECORDS+partTimeUnits*BILLING_AK_PART_TIME_CALLER_MONTHLY_RECORDS;return monthlyRecords/BILLING_AK_CADENCE_DIVISOR[cadence]}const BILLING_AK_KNOWN_PACKAGES={"5k package":{callerUnits:2,partTimeCallerUnits:0,dialerUnits:2,acquisitionUnits:1},"part time package":{callerUnits:1,partTimeCallerUnits:1,dialerUnits:1,acquisitionUnits:0},"1 pt caller package":{callerUnits:1,partTimeCallerUnits:1,dialerUnits:1,acquisitionUnits:0},"1 caller package 25 leads":{callerUnits:1,partTimeCallerUnits:1,dialerUnits:1,acquisitionUnits:0},"1 caller package (25 leads)":{callerUnits:1,partTimeCallerUnits:1,dialerUnits:1,acquisitionUnits:0},"1 caller package (30 leads)":{callerUnits:1,partTimeCallerUnits:1,dialerUnits:1,acquisitionUnits:0},"package biweekly":{callerUnits:1,partTimeCallerUnits:0,dialerUnits:1,acquisitionUnits:0},"1 roofing and 1 flipping":{callerUnits:2,partTimeCallerUnits:0,dialerUnits:2,acquisitionUnits:0,impliesData:false}};const BILLING_AK_DATA_MENTION_RE=/\b(?:data|records?|leads?)\b/i;const BILLING_AK_NO_IMPLICIT_DIALER=new Set(["8 callers"]);function classifyBillingAkProduct(productText){const text=String(productText||"").trim().toLowerCase();const cadence=billingAkDetectCadence(text);const knownKey=text.replace(/[-_]+/g," ").replace(/\s+/g," ").trim();const known=BILLING_AK_KNOWN_PACKAGES[knownKey];let callerUnits,partTimeCallerUnits,dialerUnits,acquisitionUnits,explicitDataRecords,impliesData;if(known){({callerUnits,partTimeCallerUnits,dialerUnits,acquisitionUnits}=known);explicitDataRecords=0;impliesData=known.impliesData!==false}else{const breakdown=billingAkExtractCallerBreakdown(text);callerUnits=breakdown.totalUnits;partTimeCallerUnits=breakdown.partTimeUnits;dialerUnits=billingAkExtractUnits(text,"dialers?");acquisitionUnits=billingAkExtractUnits(text,"acquisitions?|acq");explicitDataRecords=billingAkExtractDataRecords(text);impliesData=BILLING_AK_DATA_MENTION_RE.test(text);if(dialerUnits===0&&acquisitionUnits===0&&!impliesData&&callerUnits>0&&!/\bremaining\b/.test(text)&&!BILLING_AK_NO_IMPLICIT_DIALER.has(knownKey)){dialerUnits=callerUnits}}const fullTimeCallerUnits=Math.max(0,callerUnits-partTimeCallerUnits);const dataRecords=explicitDataRecords>0?explicitDataRecords:impliesData?billingAkImplicitDataRecords(fullTimeCallerUnits,partTimeCallerUnits,cadence):0;return{callerUnits,dialerUnits,dataRecords,acquisitionUnits,cadence}}function parseBillingAkDate(raw){const datePart=String(raw||"").trim().split(" ")[0];const[mo,d,yRaw]=datePart.split("/").map(Number);if(!mo||!d||!yRaw)return null;const y=yRaw<100?yRaw<=68?2e3+yRaw:1900+yRaw:yRaw;return`${y}-${String(mo).padStart(2,"0")}-${String(d).padStart(2,"0")}`}const BILLING_AK_COLUMNS=["Amount","Status","Name","Email","Date","Product","Payment ID"];function parseBillingAkXlsx(headerRow,dataRows){const norm=s=>String(s||"").trim().toLowerCase();const idx={};BILLING_AK_COLUMNS.forEach(h=>{idx[h]=headerRow.findIndex(c=>norm(c)===norm(h))});if(idx["Amount"]<0||idx["Date"]<0||idx["Product"]<0||idx["Payment ID"]<0){throw new Error("This doesn't look like an agency_transactions export — missing required columns (Amount, Date, Product, Payment ID).")}return dataRows.map(r=>{const amountRaw=String(r[idx["Amount"]]??"").replace(/[$,]/g,"").trim();const amount=amountRaw?parseFloat(amountRaw):NaN;const productText=String(r[idx["Product"]]||"").trim();const{callerUnits,dialerUnits,dataRecords,acquisitionUnits,cadence}=classifyBillingAkProduct(productText);const dataRevenue=dataRecords*BILLING_AK_DATA_RATE;const dialerRevenue=dialerUnits*BILLING_AK_DIALER_RATE_BY_CADENCE[cadence];const remainder=amount-dataRevenue-dialerRevenue;const shareUnits=callerUnits+acquisitionUnits;const callerRevenue=shareUnits>0?remainder*(callerUnits/shareUnits):remainder;const acquisitionRevenue=shareUnits>0?remainder*(acquisitionUnits/shareUnits):0;return{payment_id:String(r[idx["Payment ID"]]||"").trim(),txn_date:parseBillingAkDate(r[idx["Date"]]),customer_name:idx["Name"]>=0?(r[idx["Name"]]||"").trim()||null:null,customer_email:idx["Email"]>=0?(r[idx["Email"]]||"").trim()||null:null,product_text:productText,status:idx["Status"]>=0?String(r[idx["Status"]]||"").trim()||"Unknown":"Unknown",amount,cadence,caller_units:callerUnits,dialer_units:dialerUnits,data_records:dataRecords,acquisition_units:acquisitionUnits,caller_revenue:callerRevenue,dialer_revenue:dialerRevenue,data_revenue:dataRevenue,acquisition_revenue:acquisitionRevenue}}).filter(row=>row.payment_id&&row.txn_date&&!Number.isNaN(row.amount))}let billingAkParsedRows=[];let billingAkTransactions=[];$("billingAkUploadInput").addEventListener("change",async e=>{const file=e.target.files[0];const msg=$("billingAkUploadMsg");msg.style.display="none";$("billingAkUploadPreviewWrap").style.display="none";if(!file)return;try{const buf=await file.arrayBuffer();await ensureXlsx();const wb=XLSX.read(buf,{type:"array"});const sheet=wb.Sheets[wb.SheetNames[0]];const rows=XLSX.utils.sheet_to_json(sheet,{header:1,defval:"",raw:false});if(!rows.length){msg.textContent="That file has no rows.";msg.className="msg error";msg.style.display="block";return}const headerRow=rows[0].map(h=>String(h||"").trim());const dataRows=rows.slice(1).filter(r=>r.some(c=>String(c||"").trim()!==""));if(!dataRows.length){msg.textContent="No transaction rows found in this file.";msg.className="msg";msg.style.display="block";return}const parsed=parseBillingAkXlsx(headerRow,dataRows);if(!parsed.length){msg.textContent="No usable transaction rows found in this file.";msg.className="msg error";msg.style.display="block";return}const allIds=parsed.map(r=>r.payment_id);const{data:existing,error:existErr}=await sb.from("billing_ak_transactions").select("payment_id").in("payment_id",allIds);if(existErr){msg.textContent=existErr.message;msg.className="msg error";msg.style.display="block";return}const existingIds=new Set((existing||[]).map(r=>r.payment_id));parsed.forEach(row=>{row.isNew=!existingIds.has(row.payment_id)});billingAkParsedRows=parsed;billingAkParsedRows.sourceFilename=file.name;renderBillingAkPreview()}catch(err){msg.textContent=`Couldn't read that file: ${err.message}`;msg.className="msg error";msg.style.display="block"}});function renderBillingAkPreview(){const newCount=billingAkParsedRows.filter(r=>r.isNew).length;const skipCount=billingAkParsedRows.length-newCount;$("billingAkPreviewHead").innerHTML=`<tr><th>Status</th><th>Date</th><th>Customer</th><th>Product</th><th>Cadence</th><th>Amount</th><th>Callers</th><th>Dialers</th><th>Data</th><th>Acq</th></tr>`;$("billingAkPreviewBody").innerHTML=billingAkParsedRows.map(r=>`
    <tr style="${r.isNew?"":"opacity:0.5;"}">
      <td class="mono" style="white-space:nowrap; color:${r.isNew?"var(--focus)":"var(--text-dim)"};">${r.isNew?"New":"Already imported"}</td>
      <td class="mono">${escapeHtml(r.txn_date)}</td>
      <td>${escapeHtml(r.customer_name||"—")}</td>
      <td style="max-width:200px;" title="${escapeHtml(r.product_text)}">${escapeHtml(r.product_text.slice(0,40))}</td>
      <td class="mono">${escapeHtml(r.cadence)}</td>
      <td class="mono">$${r.amount.toFixed(2)}</td>
      <td class="mono">${r.caller_units}</td>
      <td class="mono">${r.dialer_units}</td>
      <td class="mono">${r.data_records.toLocaleString()}</td>
      <td class="mono">${r.acquisition_units}</td>
    </tr>
  `).join("");$("billingAkImportCount").textContent=`${newCount} new, ${skipCount} already imported (skipped)`;$("billingAkConfirmImportBtn").disabled=newCount===0;$("billingAkUploadPreviewWrap").style.display=""}$("billingAkCancelImportBtn").addEventListener("click",()=>{billingAkParsedRows=[];$("billingAkUploadPreviewWrap").style.display="none";$("billingAkUploadInput").value=""});$("billingAkConfirmImportBtn").addEventListener("click",async()=>{const msg=$("billingAkUploadMsg");const btn=$("billingAkConfirmImportBtn");const{data:{user}}=await sb.auth.getUser();const newRows=billingAkParsedRows.filter(r=>r.isNew);if(!newRows.length)return;btn.disabled=true;btn.textContent="Importing…";const sourceFilename=billingAkParsedRows.sourceFilename||"unknown.xlsx";const payload=newRows.map(r=>({payment_id:r.payment_id,txn_date:r.txn_date,customer_name:r.customer_name,customer_email:r.customer_email,product_text:r.product_text,status:r.status,amount:r.amount,cadence:r.cadence,caller_units:r.caller_units,dialer_units:r.dialer_units,data_records:r.data_records,acquisition_units:r.acquisition_units,caller_revenue:r.caller_revenue,dialer_revenue:r.dialer_revenue,data_revenue:r.data_revenue,acquisition_revenue:r.acquisition_revenue,source_filename:sourceFilename,imported_by:user?user.id:null}));const{error}=await sb.from("billing_ak_transactions").upsert(payload,{onConflict:"payment_id",ignoreDuplicates:true});btn.disabled=false;btn.textContent="Import";if(error){msg.textContent=error.message;msg.className="msg error";msg.style.display="block";return}msg.textContent=`Imported ${newRows.length} transaction(s) from ${sourceFilename}.`;msg.className="msg";msg.style.display="block";logActivity("finance.billing_ak_import",null,null,`Imported ${newRows.length} Billing AK transaction(s) from ${sourceFilename}`);billingAkParsedRows=[];$("billingAkUploadPreviewWrap").style.display="none";$("billingAkUploadInput").value="";await loadBillingAk()});const BILLING_AK_DATE_RANGE_STORAGE_KEY="ptai_billing_ak_date_range";function restoreBillingAkDateRange(){try{const saved=JSON.parse(localStorage.getItem(BILLING_AK_DATE_RANGE_STORAGE_KEY)||"null");if(saved&&saved.from)$("billingAkDateFromFilter").value=saved.from;if(saved&&saved.to)$("billingAkDateToFilter").value=saved.to}catch(e){}}function saveBillingAkDateRange(){try{localStorage.setItem(BILLING_AK_DATE_RANGE_STORAGE_KEY,JSON.stringify({from:$("billingAkDateFromFilter").value,to:$("billingAkDateToFilter").value}))}catch(e){}}$("billingAkDateFromFilter").addEventListener("change",()=>{saveBillingAkDateRange();loadBillingAk()});$("billingAkDateToFilter").addEventListener("change",()=>{saveBillingAkDateRange();loadBillingAk()});$("billingAkRefreshBtn").addEventListener("click",()=>loadBillingAk());$("billingAkExcludeNonSucceededToggle").addEventListener("change",renderBillingAkReport);$("billingAkProductFilter").addEventListener("change",renderBillingAkReport);async function loadBillingAk(){if(!$("billingAkDateFromFilter").value&&!$("billingAkDateToFilter").value)restoreBillingAkDateRange();const from=$("billingAkDateFromFilter").value;const to=$("billingAkDateToFilter").value;let query=sb.from("billing_ak_transactions").select("*").order("txn_date",{ascending:false});if(from)query=query.gte("txn_date",from);if(to)query=query.lte("txn_date",to);const{data,error}=await query;if(error){console.error("Failed to load Billing AK transactions:",error);billingAkTransactions=[]}else billingAkTransactions=data||[];renderBillingAkReport()}function billingAkWeekStart(dateStr){const d=new Date(dateStr+"T00:00:00Z");const day=d.getUTCDay();const diff=(day===0?-6:1)-day;d.setUTCDate(d.getUTCDate()+diff);return d.toISOString().slice(0,10)}function fmtBillingAkMoney(n){return`$${Number(n).toLocaleString(void 0,{minimumFractionDigits:2,maximumFractionDigits:2})}`}function billingAkFilteredRows(){const excludeNonSucceeded=$("billingAkExcludeNonSucceededToggle").checked;const product=$("billingAkProductFilter")?$("billingAkProductFilter").value:"";const unitsField={data:"data_records",caller:"caller_units",dialer:"dialer_units",acquisition:"acquisition_units"}[product];return billingAkTransactions.filter(t=>{if(excludeNonSucceeded&&String(t.status).toLowerCase()!=="succeeded")return false;if(unitsField&&!(Number(t[unitsField])>0))return false;return true})}function renderBillingAkReport(){const filtered=billingAkFilteredRows();const productSel=$("billingAkProductFilter");const productLabel=productSel&&productSel.value?productSel.options[productSel.selectedIndex].text.toLowerCase():"";const billingAkEmptyMsg=productLabel?`No transactions including ${escapeHtml(productLabel)} in this range.`:"No transactions in this range.";const totalCallers=filtered.reduce((s,t)=>s+Number(t.caller_units),0);const totalDialers=filtered.reduce((s,t)=>s+Number(t.dialer_units),0);const totalDataRecords=filtered.reduce((s,t)=>s+Number(t.data_records),0);const totalAcquisitions=filtered.reduce((s,t)=>s+Number(t.acquisition_units),0);const totalCallerRevenue=filtered.reduce((s,t)=>s+Number(t.caller_revenue),0);const totalDialerRevenue=filtered.reduce((s,t)=>s+Number(t.dialer_revenue),0);const totalDataRevenue=filtered.reduce((s,t)=>s+Number(t.data_revenue),0);const totalAcquisitionRevenue=filtered.reduce((s,t)=>s+Number(t.acquisition_revenue),0);const totalProfit=totalCallerRevenue+totalDialerRevenue+totalDataRevenue+totalAcquisitionRevenue;$("billingAkStatCallers").textContent=totalCallers.toLocaleString();$("billingAkStatDialers").textContent=totalDialers.toLocaleString();$("billingAkStatDataRecords").textContent=totalDataRecords.toLocaleString();$("billingAkStatAcquisitions").textContent=totalAcquisitions.toLocaleString();$("billingAkStatTotalProfit").textContent=fmtBillingAkMoney(totalProfit);const catRows=[["Caller",totalCallers,totalCallerRevenue],["Dialer",totalDialers,totalDialerRevenue],["Data",totalDataRecords,totalDataRevenue],["Acquisition",totalAcquisitions,totalAcquisitionRevenue]];$("billingAkCategoryBody").innerHTML=filtered.length?catRows.map(([name,units,revenue])=>`<tr><td>${name}</td><td class="mono">${units.toLocaleString()}</td><td class="mono" style="color:${revenue<0?"var(--danger)":"var(--good)"};">${fmtBillingAkMoney(revenue)}</td><td class="mono">${totalProfit?(revenue/totalProfit*100).toFixed(1):"0.0"}%</td></tr>`).join("")+`<tr style="font-weight:700; border-top:2px solid var(--border);"><td>Total</td><td class="mono">—</td><td class="mono">${fmtBillingAkMoney(totalProfit)}</td><td class="mono">100.0%</td></tr>`:`<tr><td colspan="4" class="empty">${billingAkEmptyMsg}</td></tr>`;const byWeek={};filtered.forEach(t=>{const wk=billingAkWeekStart(t.txn_date);if(!byWeek[wk])byWeek[wk]={callers:0,dialers:0,dataRecords:0,acquisitions:0,profit:0};byWeek[wk].callers+=Number(t.caller_units);byWeek[wk].dialers+=Number(t.dialer_units);byWeek[wk].dataRecords+=Number(t.data_records);byWeek[wk].acquisitions+=Number(t.acquisition_units);byWeek[wk].profit+=Number(t.caller_revenue)+Number(t.dialer_revenue)+Number(t.data_revenue)+Number(t.acquisition_revenue)});const weekRows=Object.entries(byWeek).sort((a,b)=>b[0].localeCompare(a[0]));$("billingAkWeeklyBody").innerHTML=weekRows.length?weekRows.map(([wk,v])=>`<tr><td class="mono">${escapeHtml(wk)}</td><td class="mono">${v.callers.toLocaleString()}</td><td class="mono">${v.dialers.toLocaleString()}</td><td class="mono">${v.dataRecords.toLocaleString()}</td><td class="mono">${v.acquisitions.toLocaleString()}</td><td class="mono">${fmtBillingAkMoney(v.profit)}</td></tr>`).join(""):`<tr><td colspan="6" class="empty">${billingAkEmptyMsg}</td></tr>`;$("billingAkTxnBody").innerHTML=filtered.length?filtered.map(t=>`
    <tr style="${Number(t.caller_revenue)<0?"background:rgba(220,60,60,0.12);":""}">
      <td class="mono">${escapeHtml(t.txn_date)}</td>
      <td>${escapeHtml(t.customer_name||"—")}</td>
      <td style="max-width:200px;" title="${escapeHtml(t.product_text)}">${escapeHtml((t.product_text||"").slice(0,40))}</td>
      <td class="mono">${escapeHtml(t.cadence)}</td>
      <td class="mono">$${Number(t.amount).toFixed(2)}</td>
      <td class="mono">${Number(t.caller_units)}</td>
      <td class="mono">${Number(t.dialer_units)}</td>
      <td class="mono">${Number(t.data_records).toLocaleString()}</td>
      <td class="mono">${Number(t.acquisition_units)}</td>
      <td class="mono" style="color:${Number(t.caller_revenue)<0?"var(--danger)":"inherit"};">$${Number(t.caller_revenue).toFixed(2)}</td>
      <td class="mono">$${Number(t.dialer_revenue).toFixed(2)}</td>
      <td class="mono">$${Number(t.data_revenue).toFixed(2)}</td>
      <td class="mono">$${Number(t.acquisition_revenue).toFixed(2)}</td>
    </tr>
  `).join(""):`<tr><td colspan="13" class="empty">${billingAkEmptyMsg}</td></tr>`}$("billingAkRecalcBtn").addEventListener("click",async()=>{const btn=$("billingAkRecalcBtn");const msg=$("billingAkUploadMsg");if(!confirm("Re-run classification on every imported Billing AK transaction using today's rules? This rewrites caller/dialer/data/acquisition units and profit for any row where the rules have changed since it was imported."))return;btn.disabled=true;btn.textContent="Recalculating…";try{const{data:rows,error:selErr}=await sb.from("billing_ak_transactions").select("id, product_text, amount");if(selErr)throw selErr;let changedCount=0;for(const row of rows||[]){const{callerUnits,dialerUnits,dataRecords,acquisitionUnits,cadence}=classifyBillingAkProduct(row.product_text);const dataRevenue=dataRecords*BILLING_AK_DATA_RATE;const dialerRevenue=dialerUnits*BILLING_AK_DIALER_RATE_BY_CADENCE[cadence];const remainder=Number(row.amount)-dataRevenue-dialerRevenue;const shareUnits=callerUnits+acquisitionUnits;const callerRevenue=shareUnits>0?remainder*(callerUnits/shareUnits):remainder;const acquisitionRevenue=shareUnits>0?remainder*(acquisitionUnits/shareUnits):0;const{error:updErr}=await sb.from("billing_ak_transactions").update({cadence,caller_units:callerUnits,dialer_units:dialerUnits,data_records:dataRecords,acquisition_units:acquisitionUnits,caller_revenue:callerRevenue,dialer_revenue:dialerRevenue,data_revenue:dataRevenue,acquisition_revenue:acquisitionRevenue}).eq("id",row.id);if(updErr){console.error("Recalculate failed for row",row.id,updErr.message);continue}changedCount++}msg.textContent=`Recalculated ${changedCount} of ${(rows||[]).length} transaction(s).`;msg.className="msg";msg.style.display="block";logActivity("finance.billing_ak_recalculate",null,null,`Recalculated classification for ${changedCount} Billing AK transaction(s)`);await loadBillingAk()}catch(err){msg.textContent=`Couldn't recalculate: ${err.message}`;msg.className="msg error";msg.style.display="block"}finally{btn.disabled=false;btn.textContent="Recalculate all"}});$("billingAkExportCsvBtn").addEventListener("click",()=>{const filtered=billingAkFilteredRows();const headers=["Date","Customer","Product","Cadence","Amount","Callers","Dialers","Data records","Acquisitions","Caller $","Dialer $","Data $","Acquisition $"];const rows=filtered.map(t=>[t.txn_date,t.customer_name||"",t.product_text,t.cadence,Number(t.amount).toFixed(2),Number(t.caller_units),Number(t.dialer_units),Number(t.data_records),Number(t.acquisition_units),Number(t.caller_revenue).toFixed(2),Number(t.dialer_revenue).toFixed(2),Number(t.data_revenue).toFixed(2),Number(t.acquisition_revenue).toFixed(2)]);exportCSV(`billing-ak-transactions-${todayDateInputValue()}.csv`,headers,rows);logActivity("finance.billing_ak_export_csv",null,null,`Exported ${rows.length} Billing AK transaction(s) to CSV`)});const IDLE_TIMEOUT_MS=4*60*60*1e3;const IDLE_LAST_ACTIVITY_KEY="dashboardLastActivityAt";const IDLE_SIGNOUT_MESSAGE_KEY="dashboardIdleSignOutMessage";let lastActivityRecordedAt=0;function recordActivity(){const now=Date.now();if(now-lastActivityRecordedAt<5e3)return;lastActivityRecordedAt=now;try{sessionStorage.setItem(IDLE_LAST_ACTIVITY_KEY,String(now))}catch(e){}}function idleMsElapsed(){try{const last=Number(sessionStorage.getItem(IDLE_LAST_ACTIVITY_KEY));return last?Date.now()-last:0}catch(e){return 0}}["click","keydown","mousemove","scroll","touchstart"].forEach(evt=>{document.addEventListener(evt,recordActivity,{passive:true})});setInterval(()=>{if(myUserId&&idleMsElapsed()>=IDLE_TIMEOUT_MS)performSignOut({idleTimeout:true})},6e4);(async()=>{let staleMsg=null;try{staleMsg=sessionStorage.getItem(IDLE_SIGNOUT_MESSAGE_KEY);sessionStorage.removeItem(IDLE_SIGNOUT_MESSAGE_KEY)}catch(e){}if(staleMsg)$("loginMsg").textContent=staleMsg;if(!sb){$("loginScreen").style.display="block";prefillAndFocusLogin();return}try{const impParams=new URLSearchParams(location.search);const impToken=impParams.get("staff_token");if(impToken){history.replaceState(null,"",location.pathname);const{error:otpErr}=await sb.auth.verifyOtp({token_hash:impToken,type:"magiclink"});if(!otpErr){const{data:{session:impSession}}=await sb.auth.getSession();if(impSession&&impSession.user){staffImpersonating=true;try{await sb.rpc("claim_impersonation_grant")}catch(e){}recordActivity();try{await establishSession(impSession.user,{isNewLogin:false});showImpersonationBanner();return}catch(e){$("loginScreen").style.display="block";$("loginMsg").textContent=e.message||"Could not open the portal as that user.";prefillAndFocusLogin();return}}}console.error("Impersonation sign-in failed:",otpErr&&otpErr.message);$("loginScreen").style.display="block";$("loginMsg").textContent='That "log in as user" link has expired or was already used. Try it again from User Management.';prefillAndFocusLogin();return}}catch(e){console.error("Impersonation hand-off failed:",e)}if(idleMsElapsed()>=IDLE_TIMEOUT_MS){try{await sb.auth.signOut()}catch(e){}try{sessionStorage.removeItem(IDLE_LAST_ACTIVITY_KEY)}catch(e){}if(!staleMsg)$("loginMsg").textContent="You were signed out after 4 hours of inactivity — please sign in again.";$("loginScreen").style.display="block";prefillAndFocusLogin();return}const{data:{session}}=await sb.auth.getSession();if(session&&session.user){recordActivity();try{await establishSession(session.user,{isNewLogin:false})}catch(e){$("loginScreen").style.display="block";$("loginMsg").textContent=e.message||"Could not resume your session — please sign in again.";prefillAndFocusLogin()}}else{$("loginScreen").style.display="block";prefillAndFocusLogin()}})();
//# sourceMappingURL=staff.7344e73727.js.map
